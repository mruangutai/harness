feature_id: FEAT-08-remove-cost-tracking
phase: validate
branch: feat/FEAT-08-remove-cost-tracking
pr: none
status: in_progress
review_sha: 942505e   # INV-6 — pinned BEFORE the panel, never read from HEAD
cycles_used: 3
max_total_cycles: 10
cost_usd: "370.53 at 3503d1d — final measurable figure; T-03 deleted the meter"
max_cost_usd: 120
runs:
  - { id: plan-product, squad: product, verdict: PASS, cost_usd: 237.73 }
  - { id: s2-eng, squad: eng, verdict: BLOCKED, note: lane defect, resolved by user ruling }
  - { id: s4-product, squad: product, verdict: ESCALATE, note: one send-back, T-10 partial }
  - { id: a4-product, squad: product, verdict: ESCALATE, note: SC-01 redraft after ruling }
  - { id: t10r-product, squad: product, verdict: PASS, note: A-3 rows 10 and 11, zero send-backs }
  - { id: panel-validator, squad: validator, verdict: FAIL, note: four-wide, 3 must_fix at med }
  - { id: mf2-product, squad: product, verdict: PASS, note: MF-2 org.html fix cycle }
tasks: { T-01: DONE, T-02: DONE, T-03: DONE, T-04: DONE,
         T-05: DONE, T-06: DONE, T-07: DONE, T-08: DONE,
         T-09: DONE, T-10: DONE, T-11: DONE, T-12: DONE }
gate_status:
  qa_gate: PASS — 12/12 scripts, matrix_ok true, qa authored nothing (D-08)
  review: FAIL — 3 must_fix at med; MF-2 fixed, MF-3 at distillation, MF-1 goes up
  uat: NOT_REQUIRED — BRIEF states no uat criterion
  goal_check: NOT_RUN — dispatched after the MF-2 fix commit
  security: PASS — reviewer declared IN SCOPE and executed both suites; 0 findings
  ui: PASS — reviewer LOOKED and declined with a file-extension census; DESIGN.md absent
  ship_refresh: NOT_RUN — .harness/codebase/ does not exist, decided at ship
  unit: PASS — re-run by me at 8958840, exit 0
  docs: PASS — check-docs.sh re-run by me at 8958840, exit 0
  state: PASS — check-state.sh re-run by me at 8958840, exit 0, zero violations
verified_by_me:
  signed: 00f3e03 — A-1, A-3, A-4 all signed; both artifacts read status approved
  sc01: >-
    PASS. The amended sweep returns EXACTLY the four survivors A-4 enumerates - BUILD.md,
    SPEC.md, DECISIONS.md, DECISIONS-INDEX.md. It returned 18 at ae2443d and 6 before the
    A-4 batch, so it stays discriminating in both directions.
  sc04: >-
    PASS. An orchestrator DIGEST omitting the money field returns digest ok, exit 0. Run
    directly against the binary, not inferred from the suite.
  t10: >-
    All five verify clauses re-run by me at 942505e - the compound sweep leaves one hit and
    it carries the DEC-178 marker; the unchanged-count clause is 8 before and 8 after; the
    new plain-word sweep printed exactly the two defect lines mid-flight and prints nothing
    now; unit 0; docs 0. The diff is two hunks. Issue 95 closed.
  mf2: >-
    The plain word is now absent from org.html entirely; the money sweep leaves two hits and
    both are the CONTEXT budget this feature preserves; three rule cards under a heading
    claiming three; the document parses to 310 start tags. Diff is three hunks.
  mf1_domain: >-
    Ran check-domain.sh itself against four personas at .claude/commands/harness.md -
    documentor, orchestrator, dev-ops and pm are ALL BLOCKED. No agent may write it. This is
    a measurement of the arbiter, not a reading of team-config.yaml.
  commit_count: 21 in ae2443d..942505e — measured; the panel dispatch said 22 and was wrong
panel_result:
  verdict: FAIL — four-wide, all four reviewers PASS, the LEAD found three the members missed
  what: >-
    Three surviving REQ-08 violations that SC-01 structurally CANNOT see, because its sweep
    matches compound tokens while all three use only the plain English word. The verify blind
    spot's THIRD appearance in one feature.
  mf1: >-
    OPEN, BLOCKING, MAIN-SESSION-DIRECT. .claude/commands/harness.md :18 renders a cost vs
    budget column for in-flight features and :83 logs a cost field on every return. In NO
    agent domain - confirmed by running the arbiter. Two prose deletions. Only layer 0 can.
  mf2: DONE at 8958840 — org.html no longer advertises the deleted subsystem
  mf3: >-
    OPEN, mine alone. .harness/expertise/harness-orchestrator.md P-01 is an operating
    procedure for the deleted meter, INJECTED INTO EVERY ORCHESTRATOR SPAWN - the highest
    blast radius of the three. Distillation is the only sanctioned write window; applied there.
  scope_ruling_i_made: >-
    All three are REQ-08 violations - "no surviving document, index row or RULE SURFACE
    advertises a script, key or invariant this change deleted." Approved text, unmet. So they
    are fix cycles, NOT scope expansion, and no amendment or re-signature is needed.
  panel_paid_for_itself: >-
    security did NOT scope out - it declared in scope on a pure-deletion diff, reasoning that
    a guard which LOST a constraint is a Tampering question, then executed both fixture suites.
    Under FEAT-07's practice that spawn would have been skipped on inference. ui DID scope out,
    with a file-extension census and a confirmed-absent DESIGN.md. Both are reviewed findings.
findings_for_briefing: >-
  Full text in notes/handoff-build.md and runs/panel-validator/digest.md. The four that carry -
  the routing wall's FOURTH recurrence landing inside the feature running concurrently with the
  one built to prevent it; FOUR instances of a task intent prescribing prose containing a token
  its own verify forbids; T-10 passing all four clauses while leaving two live sites; and the
  panel then finding three MORE of the same shape, which is the same lesson at a wider radius.
open_q:
  - "Q1 CARRIED - the briefing loses its only size signal. perf-review row 10 is filed as issue 79, still unscheduled."
  - "Q3 CARRIED harness defect - a send-back gives the returning member a FRESH context, so open_questions it raised in its own previous DIGEST are unrecoverable."
  - "Q5 - SC-06's glob over-captures; restricted to FEAT-01..07 its numbers are pm's 89 and 67-of-67 exactly. The goal-check must record BOTH."
  - "Q6 - SC-03 is repo-wide; a concurrent flow can fail it. Re-rooting via CLAUDE_PROJECT_DIR is FORBIDDEN by user ruling."
  - "Q7 RULED low by the code-reviewer - three comments justify themselves with this task's verify:, which will not exist after ship."
  - "Q9 from eng-lead - nothing detects live/template config divergence; the suite exited 0 on a half-stripped pair."
  - "Q18 RULED by the user - add nothing. Filed as issue 104 with the measurement."
  - "Q19 harness defect - INV-4's task regex cannot tell a task DEFINITION from a REFERENCE."
  - "Q20 NEW, from BOTH leads independently - the DEPLOYED GLOBAL harness-team skill still tells every lead to run the deleted script and set cost pending_orchestrator. It propagated into my own panel dispatch. Run harness-deploy after merge, BEFORE the preload-trimming batch."
  - "Q21 NEW, from the panel - should .harness/expertise/ join SC-01's sweep scope? It is excluded today, and MF-3 is exactly that miss. Harness-design call."
  - "Q22 NEW - check-domain.sh:308 carries cost in its allowlist with no explanatory comment, unlike check-state.sh:335. FEAT-09 is LIVE on that file; the next editor removes it as cleanup and all 67 historical runs become violations."
  - "Q23 NEW - T-10's fifth clause passes with EXIT 1 while its stdout is empty, because grep -v exits 1 when it filters everything out. A wrapper treating nonzero as failure misreads a pass."
pending:
  - "OUT OF SCOPE BY RULING - cycle counting and max_total_cycles (DEC-157), and rewriting any historical DECISIONS entry."
  - "THE METER IS GONE as of 1a69d9d. Every figure from here is an honest string, never a number, and no run owes a cost block."
baseline:
  base_sha: ae2443d
  closes_issues: "#58"
  briefing: pending — the validate terminus
  answers: none
  cost_note: >-
    370.53 was the last measurable figure, at 3503d1d before T-03 deleted the meter - 237.73
    plan plus 53.81 and 78.99 depth deltas, both SHARED with the concurrent FEAT-09 flow and
    attributed here IN FULL because overstating is the safe direction. Everything after
    1a69d9d is unmeasurable BY DESIGN and nothing for it is invented. Over the 120 budget,
    reported not hidden. The harness no longer meters spend (DEC-178).
  cycles_note: >-
    3 of 10. Two from build. The third is MF-2 - a real send-back against approved-but-unmet
    REQ-08. T-10's re-dispatch added ZERO - it was forward work created by a SIGNED amendment,
    not rejected work, which is what DEC-157 distinguishes.
github:
  milestone: 3
  parent: 85
  closed: [T-01, T-02, T-03, T-04, T-05, T-06, T-07, T-08, T-09, T-10, T-11, T-12]
  perf_row_10: 79
  q18_ruled: 104
  issues: { T-01: 86, T-02: 87, T-03: 88, T-04: 89, T-05: 90, T-06: 91,
            T-07: 92, T-08: 93, T-09: 94, T-10: 95, T-11: 96, T-12: 97 }
