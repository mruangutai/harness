feature_id: FEAT-03-subissue-mirror
phase: plan
branch: feat/harness-native-foundation  # plan phase writes only .harness/ artifacts; build branches later
pr: none
status: awaiting_user
review_sha: 1ce886a
cycles_used: 1                # DEC-157: eng-lead's FAIL routed back to pm as one fix cycle, closed PASS
max_total_cycles: 10          # harness.json budgets.max_total_cycles
cost_usd: 87                  # approx, per-agent snapshot deltas (P-01), incl. orchestrator share
max_cost_usd: 120             # mirrors harness.json per_feature_usd — user raised 40 -> 120 at 1ce886a
runs:
  - id: 2026-07-31-01-product
    squad: product
    verdict: PASS
    cost_usd: 19  # snapshot delta: pm +10.09, product-lead +8.86
  - id: 2026-07-31-02-eng
    squad: eng
    verdict: FAIL   # 6 must_fix on PLAN/BRIEF task specs; destination and module design judged sound
    cost_usd: 16  # snapshot delta: eng-lead +16.37 (reviewed directly, no member spawn)
  - id: 2026-07-31-03-product
    squad: product
    verdict: PASS   # fix cycle 1 — pm answered MF-1..MF-6 in artifact text, no send-back
    cost_usd: 21  # snapshot delta: product-lead +11.53, pm +9.18
  - id: 2026-07-31-04-eng
    squad: eng
    verdict: PASS   # scoped re-verification — all six defects resolved by id, no new blocking findings
    cost_usd: 6   # snapshot delta: eng-lead +6.34 (no member spawn)
pending:
  - user signature on BRIEF.md and PLAN.md — both `## Approval` `status: pending`. Plan phase ends here.
resolved:
  - MF-1..MF-6 all resolved; eng-lead re-verified by id at run 2026-07-31-04-eng
  - MF-6 discharged outside the plan at f929d44 (gitignore __pycache__/*.pyc, tracked .pyc untracked)
  - Q12 tree dirt cleared by the same commit
  - Q7 budget answered by the user at 1ce886a — per_feature_usd 40 -> 120, task count held at 8
skipped_segments:
  - segment: 1b-visual-designer-design-pass
    reason: surface is bin/*.py, feature.yaml fields and GitHub API calls — no end-user visual surface, no DESIGN.md
  - segment: 3-ui-reviewer-contract-check
    reason: no DESIGN.md contract exists to review (Expertise O-01)
github:
  parent: none                # recorded at creation by gh-sync open, never discovered (DEC-138)
  milestone: none
  issues: {}
