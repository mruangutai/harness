# Ship review — FEAT-03-subissue-mirror

**For: Mike Ruangutai. Written by: harness-orchestrator, 2026-07-31, at `e68ba00`.**
**You decide: ship / fix / re-scope / stop. I have not merged, opened a PR, or written `## Approval`.**

---

## The conclusion first

**The feature is built, tested, reviewed and verified against its own success criteria. It is ready to
ship, with one edit that only you can make and one judgement call that is genuinely yours.**

Twelve of the thirteen success criteria are **met**. The reviewer panel and the blocking QA gate both
returned PASS with **zero** blocking findings. Nothing is outstanding in the code.

**The thirteenth criterion (SC-13) is a two-line edit to `.claude/skills/harness/SKILL.md` that no
agent in the org is permitted to make.** It is not an oversight — the plan you approved names it as
your step. Details below, with the exact text to check. Until it lands, one file the org reads at every
dispatch still describes the old, now-wrong behaviour.

**Two numbers you should hear plainly: the feature cost ~$341 against a $120 budget (2.8x), and used 6
of its 10 rework cycles.** Neither stopped the work — by design, cost never gates. But 2.8x is not a
rounding error and it deserves your attention more than any technical finding in this document.

---

## What the feature actually does now

Harness mirrors each feature's plan into GitHub Issues. Before this feature, that mirror was wrong in
three ways that would have quietly corrupted your issue tracker the first time you turned it on.

**1. Absorbed issues were being closed.** When a task "absorbs" an existing backlog issue, it means the
task is *part of* that issue — not a replacement for it. The old code closed the absorbed issue when the
task finished, which would have closed live backlog items that were only partly done. Now `absorbs:` is
a **citation and nothing more**. It closes nothing.

**2. There was no way to abandon a feature.** If you dropped a feature, its GitHub issues stayed open
forever, or you closed them as though the work had shipped. There is now an `abandon` path that closes
the feature's own issues as `not_planned` — the state that says "we decided not to", not "we finished".

**3. Shipping or abandoning could close an issue that was never ours.** This is the subtle one, and it
is the reason this feature went through three rounds of revision before you signed it. A feature's
"parent" issue is sometimes one *harness created* to hold the feature's tasks, and sometimes one **you
already had** in your backlog that the feature adopted. Closing the first kind is correct. Closing the
second kind asserts something false about your own live work — and `completed` is exactly as false as
`not_planned` when the thing isn't yours.

So the parent's fate now follows its **recorded origin**: harness closes what it created, and leaves
alone what it adopted or cannot prove it created. When in doubt, it leaves your issue open. The
milestone closes either way, because that one is always harness's own.

There is also housekeeping worth one line: the project's `unit` test gate **was not running the tests
that matter**. Its file pattern matched zero files in this repo, so eight criteria claiming "verified by
unit tests" rested on a gate that did nothing. That is fixed and the gate now runs three test scripts.

---

## Your one required step before ship (SC-13)

`.claude/skills/harness/SKILL.md` is documentation the org reads at dispatch time. Two lines in it still
describe the behaviour this feature reversed. **No agent domain covers that file**, which is why it
comes to you.

Check with:

```
grep -c 'closes its issue and everything it absorbs' .claude/skills/harness/SKILL.md
```

This reads **1** today and must read **0**.

- **Line 137** says `close-task` "closes its issue and everything it absorbs". It no longer closes what
  it absorbs.
- **Line 144**'s `ship` row mentions only the milestone. It must also name the **parent**, and must name
  it as **conditional on the parent's recorded origin**. A row saying ship closes the parent
  unconditionally does **not** satisfy this criterion — that was the exact defect the third round of
  revision removed from six other places.

**Nothing mechanical will catch this, deliberately.** The doc checker's only tool is a "this text is
stale" marker, and declaring one for wording that is still present turns the checker **red** — which
would block every `/harness` command in this repo on an edit only you can make. So the amendment
declared no marker, the checker stays green, and this criterion is carried by the grep above.

---

## Your one judgement call (not a defect)

**An abandonment can stop halfway through and still report success.**

If the network or GitHub fails partway through `abandon` — specifically while posting the reason comment
— the tool exits **0** having closed no issues and no milestone. Nothing records that it half-happened.

This is not a bug against anything you approved: the rule is "an environmental failure exits 0 with a
SKIP line", and this obeys it literally. But the *spirit* is that a skip means "did nothing", and here
it can mean "did some of it".

Product's recommendation, which I endorse: **do not fix it inside this feature.** It is a new
requirement, not a missed one — making a terminal state atomic is real design work, and it belongs with
the follow-on feature already sketched. **Your call**: accept it as a known limitation, or scope it now.

---

## What the assurance is worth, and where it stops

I want you to have the honest version of "PASS", because the reviewers were explicit about their own
limits.

- **No test touches a real GitHub.** `github.sync` is `false` and no repo is pinned in this project, so
  every assertion runs against a **fake** `gh` command. The behaviour is proven; the integration is not.
  **The first time you enable sync on a real project is a genuine moment of risk** — that is a
  user-gated step, not something this feature could have tested away.
- **One criterion (SC-08, the state checker) does not depend on the fake** and is proven outright.
- **The security review could not execute `gh`** — the sandbox denied it — so its argument-handling
  analysis is reasoned from upstream source rather than demonstrated. It found no injection path; the
  code passes arguments as lists, never as shell strings.
- **Test-first ordering cannot be audited** at task granularity, because each run's source and test
  changes share one commit.
- **UAT does not apply.** All thirteen criteria are automated or inspection; there are no
  user-acceptance criteria to run. Naming that explicitly rather than omitting it.

One finding is worth telling you because it is a lesson rather than a bug: **a test in this repo carried
a label that lied about what it measured** — it said "for the new subcommand" while testing an old one.
That false label travelled through two review tiers as though it were a measurement and produced a
report of a gap that does not exist. I caught it by checking the claim against the commit you signed.
It cost analysis time, no defect shipped, and it is now a written rule in two agents' expertise.

---

## Cost and cycles, stated against budget

| | Actual | Budget | |
|---|---|---|---|
| Cost | **~$341** | $120 | **2.8x over** |
| Rework cycles | **6** | 10 | 4 remaining |

**Cost never gates work in this system** — that is a deliberate decision, taken after a $9 overrun once
killed a flow one step from done. So this is reported, not an apology, and it never slowed anything.

Where it went: **~$162 was spent before the build even started** — planning plus three rounds of
revision on the brief and plan. The build itself was ~$74, validation ~$53, and close-out ~$49. The
single most expensive run was the six-task build at **~$54 against a per-run budget of $15**.

**The 6 cycles read worse than they are.** Three were the planning revisions you authorised. The other
three were send-backs *inside* runs — and all three were about **prose, not defects**: a stale comment
above a test, an incomplete usage string, an unnamed invocation form. No implementation was rejected
once. A cycle counts rework of any kind, which is why the number looks heavier than the work was.

**Worth your attention for next time:** this repo is unusually expensive per unit of work, because the
code being changed *is* the documentation every agent must read to be correct. The budget note in
`harness.json` says to lower the $120 once the decisions index lands and that mandatory-reading floor
drops. On this evidence, $120 is still too low for a feature of this size in this repo, not too high.

---

## Proposed backlog

**These become issues only if you accept the ship. Anything you strike here dies silently — so this is
the complete list, not a curated one.** Nothing below gates the ship.

### Real defects in shipped code (1)

| | Item | Nature |
|---|---|---|
| B-1 | **`post_body_path` catches only `OSError`** when reading the file it posts. A different failure class would escape. **Its exposure is entirely future** — the function never runs against a live repo until you enable sync, which makes "fix before the first live `open`" the actionable form. Ranked first not on severity but on **irreversibility: a wrong file posted to a public tracker cannot be un-posted.** Not fixed in flight because the approved plan pins `OSError` literally. | bug |

### Test and coverage gaps (3)

| | Item | Nature |
|---|---|---|
| B-2 | **A test label lies about what it tests** — it claims "the new subcommand" while invoking an old one. Cheapest item here and it already cost two review tiers real analysis. | chore |
| B-3 | **No test covers `abandon` against a failing API call.** This is what the false label was hiding. Explicitly *not* reopening SC-13 or SC-12. | chore |
| B-4 | **`wayfind.py` has no test coverage at all**, and this feature made it depend on a new shared module. Raised independently by **four** reporters. Adding `test-wayfind.py` also requires adding it to the runner's explicit list, or the drift detector fails. | enhancement |

### Harness defects — the tooling, not this feature (6)

| | Item | Nature |
|---|---|---|
| B-5 | **The bash write-guard reads any `>` inside quoted text and heredocs as a file redirect** and blocks the command. It blocked the *mandated* commit trailer, an arrow inside a print statement, and an HTML comment in prose — five distinct times this run. Every commit here had to route through a message file. | bug |
| B-6 | **The state checker's run parser silently drops a run** if its `squad:` line carries a trailing comment. The run then reports as unrecorded work. I hit this and traced it to `check-state.sh:109`. | bug |
| B-7 | **No `build` team definition exists**, so all four build runs were dispatched with hand-written step lists. **And the `review` team omits the QA step** even though QA is the project's one *blocking* gate — running it as shipped would pass validation with that gate never run. I added the step by hand. Both reach every future feature. | chore |
| B-8 | **The cost reporter has no per-run mode** and its documented usage produces a file the state checker rejects. Every one of the sixteen runs needed hand-correction. | bug |
| B-9 | **`validate-digest.py`'s change-type vocabulary lacks `logic`**, a value the plans legitimately use — two different vocabularies wearing one field name. | bug |
| B-10 | **The expertise checker misses two things:** it does not check the required file title, and it had been **passing a file that its own rules reject** — one agent's memory file carried four rule violations and was being injected at every spawn regardless. Repaired this run. Also: `gh` execution is blocked by the sandbox, which structurally limits security review of any tool that shells out. | bug |

### Documentation drift (2)

| | Item | Nature |
|---|---|---|
| B-11 | **Plan references cite line numbers in a file the orchestrator rewrites every run**, so four citations were stale before the build began. The fix is to cite field names, not lines. Nothing false was asserted. | chore |
| B-12 | **Two recorded baselines in the brief and plan drifted harmlessly** (a file count, and a checker's exit code that changed when you signed the approval). No criterion depends on either. Also: two criteria are loosely worded — one says "subcommands" plural where exactly one is new, one says "both test scripts" where there are now three. **Both are met as written**; these are wording chores, not defects. | chore |

---

## Two small things I decided, for the record

You can overturn either.

1. **I skipped the UI review and the map refresh.** No user-visible surface, no design document, and no
   architecture map exists in this repo to refresh. Both are recorded as skips with reasons rather than
   silently omitted.
2. **I let three prose corrections stand.** During the build, members completed comments and usage
   strings adjacent to behaviour their own task changed. On the strictest reading of the plan, two of the
   three exceed the task's letter. I judged them completion rather than scope creep, and the review panel
   passed them. Overturning it now would mean changing a passing test suite after the gate.

---

## What happens on your word

- **Ship** — I record your acceptance, and the unstruck backlog items above become issues. (The GitHub
  mirror itself posts nothing: sync is off in this project, so all three sync points skip.)
- **Fix** — name the item and I route it. Four rework cycles remain.
- **Re-scope or stop** — the plan goes back to product under your approval.

The code is committed on `feat/harness-native-foundation` in four commits (`2897b09`, `ae728e8`,
`e68ba00`, `4d4c3af`). **Nothing is merged and no PR is open** — both remain yours.
