feature_id: FEAT-04-decisions-index
phase: validate            # build CLOSED at bdfa3ab; seam note notes/handoff-build.md (58 lines)
branch: feat/decisions-index
pr: none
status: awaiting_user
review_sha: bdfa3ab        # re-pinned at build close; the panel reviews this SHA (INV-6)
cycles_used: 6             # 2 plan; +1 run 07 send-back; +1 run 08 header fix; +1 run 12 cap remediation; +1 run 12's own send-back
max_total_cycles: 10       # harness.json budgets.max_total_cycles
cost_usd: 275              # 2.3x the 120 budget, and a FLOOR (advisor spend is unmetered). DEC-134: never a gate
max_cost_usd: 120          # mirrors harness.json per_feature_usd
runs:
  - id: 2026-08-01-01-product
    squad: product
    verdict: PASS
    cost_usd: 18
  - id: 2026-08-01-02-eng
    squad: eng
    verdict: FAIL
    cost_usd: 9
  - id: 2026-08-01-03-product
    squad: product
    verdict: PASS
    cost_usd: 18
  - id: 2026-08-01-04-product
    squad: product
    verdict: PASS
    cost_usd: 13
  - id: 2026-08-01-05-eng
    squad: eng
    verdict: PASS
    cost_usd: 10
  - id: 2026-08-01-06-product
    squad: product
    verdict: PASS
    cost_usd: 12
  - id: 2026-08-02-07-eng
    squad: eng
    verdict: PASS
    cost_usd: 17
  - id: 2026-08-02-08-eng
    squad: eng
    verdict: PASS
    cost_usd: 4.5
  - id: 2026-08-02-09-product
    squad: product
    verdict: PASS
    cost_usd: 45.5
  - id: 2026-08-02-10-product
    squad: product
    verdict: ESCALATE
    cost_usd: 16
  - id: 2026-08-02-11-eng
    squad: eng
    verdict: PASS
    cost_usd: 5.7
  - id: 2026-08-02-12-product
    squad: product
    verdict: PASS
    cost_usd: 20
pre_ship_steps:
  - T-09 — CLAUDE.md:43, owner main-session. No agent domain covers CLAUDE.md. SC-09 verified by
    grep at the ship gate
  - T-10 — .claude/skills/harness-handoff/SKILL.md, owner main-session. No agent domain covers
    .claude/skills/harness-*/SKILL.md. SC-10 verified by grep at the ship gate
pending:
  - BLOCKING — USER RE-SIGNATURE. BRIEF.md SC-11 and PLAN.md D-07 were amended after the 2026-08-02
    signature (the 30-word ruling cap) and both `## Approval` blocks still carry the pre-amendment
    note. SC-11's amendment is a REVERSAL: it previously stated the length-cap question was closed
    without a character-count rule. No validator panel should run against unsigned criteria
  - BLOCKING for cleanliness, MAIN SESSION ONLY — delete `.harness/notes/pending-dec-advisor-disclosure.md`.
    Its line 1 says the decision is not yet in the authority; DECISIONS.md:4415 now contradicts that,
    so it is a false second copy. `.harness/notes/**` is in no member's, no lead's and no
    orchestrator's write domain, so no agent can do it
  - BRIEF.md's approval note still reads that PLAN is pending, which PLAN's own block contradicts.
    Stale text, not a gate — fold it into the re-signature pass
  - DEC-102's index row states its superseded conclusion with NO `— SUPERSEDED BY` clause, so a reader
    can act on a dead ruling. The clause is harvested from the superseding decision's TITLE and DEC-120
    declares the supersession in body prose instead. Generator gap or an authority-title edit — eng
  - Q1 (main session's, not mine) — declare a stale marker for the whole-read wording T-09 removes?
    Only decidable when T-09 runs. Yes re-pins the emitted pattern count 45 -> 46 and reverses D-08
  - Q2 (for the user at ship) — post-ship, a feature appending a decision must regenerate the index AND
    write the row's ruling in the same commit or the unit gate fails. DEC-170 was its first exercise
  - Confirm no LOWER bound on ruling length is wanted; the observed minimum is 13 words
  - COST is 2.3x the budget and the figure is a FLOOR — advisor spend appears in no cost-report.py row,
    which is the very gap DEC-170 leaves open. Never a gate (DEC-134), but the user should see it
  - Q5 HARNESS DEFECT — the orchestrator playbook mandates appending cost-report output to the run
    state file while harness-team has the lead pre-write `cost: pending_orchestrator`; the duplicate
    top-level key trips INV-16. Hit on all ten runs; repaired by hand each time
  - Q6 HARNESS DEFECT — every per-feature `.harness/**/*.md` artifact is a check-docs.sh scan target,
    documented nowhere an agent writing one would see it
  - Q7 HARNESS DEFECT, four occurrences this phase — bash-write-guard.sh reads `>` and `<` inside an
    unquoted heredoc body, and operands across a compound `;` line, as shell redirects. Every instance
    refused a legitimate read-only or in-domain command. The quoted-string fix does not cover these
  - Q8 HARNESS DEFECT — a member whose deliverable is code has no writable per-feature artifact path
    but its observations log, so a bin/ task overloads DEC-145's never-injected hot layer as a handoff
resolved:
  - APPROVAL GATE PASSED — BRIEF.md:186 and PLAN.md:586 both `status: approved`, Mike Ruangutai,
    2026-08-02. BRIEF's approval NOTE still says "PLAN remains pending"; PLAN's own block overrides
    it and records Q0 accepted. The note is stale text, not a gate
  - Q0 ACCEPTED by the user — T-09 and T-10 are main-session pre-ship steps (see pre_ship_steps)
  - Q4 ACCEPTED by the user — the unit gate is deliberately RED between T-03 and T-07, by design
  - Q3, MF-1..MF-6, the check-docs regression, D-01's pricing, A-1/A-2/A-4 — all closed in the plan
    phase; evidence in runs/2026-08-01-0{3,4,5,6}-*/digest.md and notes/handoff-plan.md
skipped_segments:
  - segment: 1b-visual-designer-design-pass
    reason: no end-user visual surface, no DESIGN.md
  - segment: 3-ui-reviewer-contract-check
    reason: no DESIGN.md contract exists to review (Expertise O-01)
github:
  parent: none            # github.sync false, repo null — all three mirror points SKIP (DEC-138)
  milestone: none
  issues: {}
baseline:                 # measured at f723194 unless noted
  check_docs_at_plan_exit: exit 0, 45 patterns across 95 files
  check_docs_at_build_open: exit 0, 45 patterns across 96 files   # measured by me at 71a2043
  stale_marker_lines: 48  # raw stale-marker lines; 3 dropped by the len<4 guard -> 45 patterns
  decisions_md: 4413 lines, 169 LIVE DECs (170 raw matches, one fenced at :1583), 426 in-body refs
  titles_flagged_by_check_docs: 1
  amendment_headings: 9   # `^### DEC-NNN amendment[ N]`; the `DEC-NNN am.N` form appears 0 times
  amendments_inline: 2    # bold-paragraph form at :3530 and :3536, not headings
  orchestrator_cost_cumulative: opus 233.8115, fable 40.7239   # next delta baseline, at plan exit
