# STATE

## Current

- feature: FEAT-20-migration-detector
- run: qa segment (validator squad) dispatched; review panel queued behind it
- squad: validator
- status: validating

**All four tasks are DONE and every `verify:` was re-run here, not taken on a receipt's word.**
T-01 `14ca661`, T-02 `d3207e7`, T-03 `2c35398`, T-04 `396f1ad`; statuses recorded in `plan.yaml`
before each `close-task`, so #361-#364 are closed and parent #360 derived to `Review`.

**`review_sha` is re-pinned at `11cb644`**, which contains all four tasks. The inherited pin
`88b1182` predated every change in this feature and would have reviewed a tree the work is absent
from — no validator run may use it.

**What the two dispatched tasks actually landed, checked by me at the file, not from the digest.**
T-03 is a 52-line pure addition to `.github/workflows/tests.yml`: `Layout gate` sits immediately
after `Plan-route gate` in the required `integration` job, hedges all three examined counts with
three differently worded `::error::` messages, and ends `exit "$rc"` so cannot-verify (2) fails the
step as surely as mixed (1). The false guard-claim comment at lines 110-114 is untouched — the whole
diff has zero deletions — and the new step's own comment says only that nothing in the repository
asserts it, which is true. T-04 is a 65-line pure addition: DEC-194 at `DECISIONS.md:5834` with the
index row regenerated by `gen-decisions-index.py`, not hand-written.

**Live evidence at this commit.** `layout_migration.py` prints `features: CLEAN` / `docs: CLEAN`,
`examined 20 feature dir(s), 1 doc root(s), 7 reader file(s)`, `layout: 2 surface(s) clean, 0 mixed,
0 cannot-verify`, exit 0. `check-state.sh` exits 0 with zero INV-27 lines. Unit and integration
suites both exit 0 with zero `FAIL` lines.

**Budget: `cycles_used` stays 2.** Both dispatched runs were clean first passes reporting zero
send-backs, and a first-pass run adds zero. `len(runs)` is 4 against a budget of 20 — nowhere near
the informational tripwire, and the count is a floor because T-01 and T-02 were main-session-direct
and are not runs.

**Next: qa segment, then the review panel at `11cb644`, then pm's goal-check against all 15 SCs,
then close-out and the briefing.** The panel's qa step is gate-only and writes
`notes/review-harness-qa-c0.md`; the segment ahead of it owns test authoring and writes
`notes/qa-c0.md`. They must not collide on that path.

## Open Questions

None blocking. Three items ride forward to the briefing:

- **Q3 is closed by T-01.** `validate_table` raises `LayoutTableError` on a row keyed to a non-member
  surface; unit case 17 proves it, and the suite ran green here.
- **Q4 is confirmed, not inherited.** T-03's squad re-derived the premise at `d3207e7`: the only
  `.py` naming the workflow passes its path to a domain fixture and never opens it, and
  `test-check-plan-routes.py` case 25 is about `status:` values. Issue #279 still owns the false
  comment.
- **Harness defect, non-blocking, unfixed.** The playbook says to record the phase in `feature.json`
  `phase:`, but `feature-schema.json` sets `additionalProperties: false` and defines no `phase`, so
  the write fails validation. The phase is recorded here instead. Belongs to the harness owner.
