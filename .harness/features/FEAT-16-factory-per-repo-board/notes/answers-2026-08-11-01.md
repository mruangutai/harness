# Operator answers — FEAT-16 — 2026-08-11

## Q1 — the board changed under the plan, and why

**Cause, stated plainly: the MAIN SESSION mutated board 2 mid-plan.** Not drift, not a race with
another agent — a deliberate one-off applied while this plan was being written, and nobody told the
running flow. The orchestrator measured the board twice, found the second reading contradicted its
own criteria, and stopped rather than signing over it. That was correct.

**Six IS the intended end state.** Both boards now carry one identical vocabulary, which is the
whole point — the file and the board cannot drift if they share a value set:

```
Backlog → Plan → Ready → Building → Review → Done
```

Applied and verified 2026-08-11 by counting items per column before and after, not by trusting the
mutation. Board 3: 204 items unchanged, plus a `Plan` column. Board 2: `Todo`→`Backlog` (82),
`In Progress`→`Building` (11), `Done` kept (118), total 211, **zero item writes** — renaming a
single-select option preserves assignment. Every pre-existing option id was re-sent unchanged,
because `updateProjectV2Field` REPLACES the whole list and an omitted option deletes it and strands
its items.

**Ruling 2 is superseded, and the reason matters.** The earlier `Todo → Ready` mapping was decided
when the target vocabulary was three stations and `Ready` was the only not-started-yet value
available. With six values the two separate: `Backlog` is filed-and-untriaged, `Ready` is
you-have-decided-the-factory-may-take-it. 82 unplanned issues in `Ready` would make `Ready`
meaningless. Board 3's own shape is the evidence — 88 in Backlog against 5 in Ready.

**The factory's `ready` station stays pointed at `Ready`, and an EMPTY `Ready` is CORRECT, not a
defect.** This is the operator's ruling on the orchestrator's sharpest finding. A factory run that
finds nothing to claim has found the truth: nothing has been promoted. The alternative — pointing
the intake at `Backlog` — makes the factory pick up work nobody triaged and leaves `Ready` with no
meaning on kaya at all.

**The consequence, recorded rather than left implicit:** promotion from `Backlog` to `Ready` is a
human decision and there is **no recorded step for it anywhere**. That is a real gap, it is NOT
this feature's to close, and it must not be silently absorbed. If a criterion here touches it, the
criterion is that a claim run against an empty `Ready` **reports** that it found nothing rather than
exiting 0 in silence.

**pm re-baselines SC-03, SC-07 and T-07 on this.** SC-07's four-option assertion is falsified —
assert six, by name, in the order above. T-07's board edit is already partly done: convert what
remains into a PRECONDITION check that reads both boards and confirms the six options exist, rather
than an edit that re-does work and would fight the live state.

## Q3 — the research note is corrected before signature

`notes/research-feat16-plan-2026-08-11.md` presents, as verbatim `check-plan-routes.py` output for
FEAT-16, output produced against **FEAT-12's** plan — the paths it shows appear **zero** times in
FEAT-16's plan. Its tree-wide claim of "exit 1, 2 DEC-182 budget violations in FEAT-14" measures as
**exit 0, `0 violation(s) across 12 plan(s)`**, and the two cited intents are 203 and 73 lines,
not 54 and 61.

**CORRECTION, 2026-08-11 — this paragraph originally said "the checker contains no budget logic
at all". THAT WAS FALSE**, written here from a first-pass report and not checked at source. The
checker does carry budget logic: `MACHINE_LINES_PER_TASK = 50` at `check-plan-routes.py:280`,
emitted as a DEC-182 VIOLATION at `:322-327`. The revision squad re-measured and caught it.

The note's FEAT-14 claim still fails, for a narrower and stronger reason: `BUDGETED_FIELDS`
(`:282-287`) **excludes `intent:` on purpose** — it is the literal dispatch prompt and it is READ,
which is DEC-154's test for which half a value belongs in. So intent length can never produce a
budget violation, and FEAT-14's real machine-field maximum is 46 against a cap of 50.

Recorded rather than quietly edited, because this file is exactly the kind of durable artifact
#247 is about — and the false sentence was written INTO a correction of someone else's false
evidence.

**Correct the note, do not strike it** — its genuine research is worth keeping and the real
measurements are already in the orchestrator's digest. Every corrected figure carries how it was
measured.

**Why this is not a nit.** The note is a durable artifact later readers cite. This is the defect
class of **#247** — nothing in the tree gates whether prose is TRUE, only whether tokens are
present — and the plan itself passed the real gate, so no mechanical check could have caught it.
The orchestrator found it by re-running the command rather than reading the record, which is the
behaviour to keep.

## Q2 — the live run stays the operator's

SC-06 creates a throwaway issue and a `factory/issue-N` branch in `mruangutai/kaya-ai`, a real
product repository, and moves a station on live board 2. Only the operator consents to those
writes. SC-06 stays `not_met` until they run it. **The issue used must not be one of the 118 in
`Done`** — the run moves a station.

## Q4, Q5, Q6 — not answered here

Left to the revision to decide or re-raise: whether T-10 appends DEC-174 am.2 or opens a new DEC
number; whether DEC-186 is amended or struck under DEC-188 (its one-board framing is falsified, but
its three-purpose read-back bound is alive and in gates, and a strike would remove that bound); and
the prototype-gate call made by the product lead rather than by visual-designer.

---

# SIGNATURE — 2026-08-11

BRIEF and plan.yaml both `approved`, operator, 2026-08-11. Eleven tasks, eleven decisions, zero
open questions at signature.

**Two things stay open BY DESIGN and are not defects.**

**SC-06 stays `not_met` until the operator runs it.** The live factory claim creates a throwaway
issue and a `factory/issue-N` branch in `mruangutai/kaya-ai`, a real product repository, and moves a
station on live board 2. Only the operator consents to those writes. **The issue used must not be
one of the 118 in `Done`** — the run moves a station, and moving a finished issue breaks the
118-in-Done criterion.

**The `Backlog → Ready` promotion step does not exist anywhere.** Recorded as D-08, out of scope for
this feature. `Ready` is the factory's intake and an empty `Ready` is correct — it means nothing has
been promoted. The criterion that guards it is SC-13: a claim run against an empty `Ready` REPORTS
and exits 1, never a silent 0.

**Build ordering, and it is the main session's to hold.** FEAT-16 does NOT build next. FEAT-14's
migration rewrites all 17 `feature.yaml` files and needs every other flow idle; a signed,
undispatched plan is idle, so this signature does not block it. FEAT-16 and FEAT-17 build AFTER the
migration, so they build once on the settled `feature.json` format rather than being rewritten by
another feature's migration mid-flight.
