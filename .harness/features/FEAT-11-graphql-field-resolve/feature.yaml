feature_id: FEAT-11-graphql-field-resolve
mission: ship
phase: build
status: in_progress
branch: feat/FEAT-11-graphql-field-resolve
pr: none
effort: https://github.com/mruangutai/harness/issues/211
briefing: none
review_sha: PENDING_MF1_COMMIT
review_sha_note: >-
  Re-pinned once per validator run, always at a commit that contains the work under review (INV-6).
  The panel's pin is set immediately after the MF-1 commit lands and is recorded here before the
  panel is dispatched.
review_sha_history: "835b297 (plan contract) -> 8dedeae (ship open) -> 5c433f2 (T-01) -> MF-1 commit (panel)"
cycles_used: 9
max_total_cycles: 12
cycles_raise: >-
  RAISED 10 -> 12 BY THE OPERATOR, 2026-08-10, recorded here as DEC-157 requires. Eight cycles went
  to the plan phase — six to the plan team's own convergence, one to the validator FAIL, one to a
  digest-contract repair the orchestrator itself flagged as bookkeeping rather than deliverable
  rework. The raise removes a mid-build BLOCKED interruption; it does not endorse rework.
max_total_runs: 20
cycles_note: >-
  8 charged in the plan phase. 1 charged in the ship phase: the qa segment returned FAIL with MF-1
  and I routed it back to eng — a FAIL routed back is rework by DEC-157 even though every lead
  reported ZERO internal send-backs. t01-eng, 2026-08-10-01-q5-product and the qa segment's own run
  charge nothing: first-pass forward work is not rework however many steps it has.
counts: { reqs: 5, scs: 12, tasks: 1, decisions: 4, main_session_direct: 0 }
counts_note: "SC-01 is operator-run by construction; every other criterion is agent-verifiable."
tasks: { T-01: BUILT }
tasks_note: >-
  change_type bugfix, absorbs #211, files: factory_gh.py + test-factory-gh.py +
  test-factory-integration.py, all resolving to harness-backend-dev. BUILT is my own vocabulary for
  the execution record; plan.yaml tasks[0].status is pm's field and its update rides the goal-check
  dispatch, because the signed plan otherwise reads as though the feature never built.
route_resolution: >-
  ZERO main-session-direct. Resolved by check-domain.sh --resolve, not by reading team-config, which
  is ambiguous for bin/** (granted to both backend-dev and dev-ops). None of the DEC-174 carve-out
  files is in scope. eng-lead confirmed the pick on semantics, since the grant cannot discriminate.
peer_feature_collision:
  peer: FEAT-12-end-copy-distribution
  overlap_files: none
  shared_surface: .claude/skills/harness/bin/run-unit-tests.sh
  risk: >-
    NO file in T-01 is in FEAT-12's plan union, but FEAT-12 edits run-unit-tests.sh, which is the
    command T-01's verify invokes. If FEAT-12 lands first and changes how the suite splits, T-01's
    verify changes meaning without T-01 changing. Landing order is the operator's call.
  ship_phase_measurement: >-
    MEASURED, not assumed: git status --porcelain is clean over run-unit-tests.sh and over all four
    DEC-174 carve-out files at every commit this mission made. FEAT-12 has landed nothing here, so
    T-01's verify means what the plan measured. Held dirt (.harness/logs/2026-08-10.md, the
    untracked FEAT-12 dir) is excluded from every commit by explicit pathspec.
preflight_measurements: >-
  Taken by me at 8dedeae BEFORE any dispatch, so no claim below is inherited. (1) All three sha256
  pins in T-01's verify recompute byte-identical — Q4 is MOOT and no plan amendment is owed.
  (2) Baseline green: --kind unit exit 0, test-factory-integration.py exit 0, 97/97.
  (3) The verify's discriminating clauses were RED before the change — factory_gh._FIELD_QUERY did
  not exist and the quoted "field-list" argv appeared once — so the green verify after it can only
  come from the change.
e1_ruling:
  claim: >-
    The grilling artifact states the two error paths at factory_gh.py:251-262 must survive because
    _validate_stations and the Redy typo case depend on them. The validator escalated that the
    dependency is false.
  verified_by_me: >-
    CONFIRMED at 835b297, read directly. _validate_stations (factory_decompose.py:255-268) calls
    project_field_options and propagates its GhError WITHOUT reading the text; the option-not-offered
    message operators actually see is produced by _validate_stations itself at :264-268 via
    factory_cli.refuse. The Redy case (test-factory-decompose.py:1025-1048) runs against a Recorder
    stand-in and never enters factory_gh.
  ruling: >-
    THE OPERATOR'S CONSTRAINT STANDS UNCHANGED — the two error paths survive unchanged in behavior.
    A falsified rationale does not release a constraint. Only the stated REASON is wrong. The
    grilling artifact is NOT edited: it is a dated record of that session, and this file carries the
    correction.
  the_real_dependency: >-
    _validate_stations depends on project_field_options RAISING on a missing field
    (factory_gh.py:206-210) — a THIRD error path the grilling never named. The plan names :206-210
    at plan.yaml:201, so no behaviour is at risk.
sc01_ruling: >-
  CLOSED BY THE OPERATOR at signature. Board 6 and mruangutai/harness-factory-smoke-a1 are RETAINED
  FIXTURES, not scratch and not cleanup owed. SC-01's proof runs a real factory_decompose against
  board 6, which moves items between stations, so the operator records every item's station value
  BEFORE the run and restores it after. pm authors that record-and-restore procedure into the UAT
  script at goal-check. A proof that leaves the four items moved has spent the fixture to measure a
  number. NO AGENT IN THIS FLOW MADE ANY LIVE gh CALL AGAINST BOARD 6 — every dispatch forbade it
  and every path in the changed tests is fed by stubs or the whole-CLI fake.
mf1_correction: >-
  THE RECORD, CORRECTED WHERE IT IS WRONG, WITHOUT REWRITING THE ARTIFACT THAT CARRIES IT.
  qa's MF-1 claimed BOTH test-factory-gh.py:407 and :428 asserted vacuously. Only :407 did.
  factory_gh.py:264-266 renders "organization-owned ... user-owned" — the prose carries "owned",
  never "owner" — so :428 was already discriminating on the value. Verified BY ME by substring test,
  not relayed. Consequence: SC-10's positive clause was nominal for ONE row, not two, so SC-10 was
  partially proven before this cycle and is fully proven after. The remedy was warranted in full
  anyway: :432 and :452 cross-compare messages for INEQUALITY, so moving one case's owner value
  without the others makes those inequalities pass on the value rather than on the prose. The
  qa-segment digest and notes/qa-c0.md are NOT edited — they are dated records of that run, the same
  treatment e1_ruling gives the grilling artifact, and this entry is the correction.
gate_status:
  brief_approval: "PASS — approved / operator / 2026-08-10"
  plan_approval: "PASS — approval.status approved, three rulings ride with the signature"
  arch_review: "PASS (plan phase) — eng-lead, 4 must_fix all closed"
  design_contract: "PASS (plan phase) — visual-designer, needs_prototype false"
  ui_contract_prebuild: "PASS (plan phase) — ui-reviewer, three passes, all must_fix closed"
  plan_contract: "PASS (plan phase) — validator, 3 must_fix closed in one round"
  docs: "NOT APPLICABLE — check-docs.sh was DELETED at 835b297 (issue #202). No docs gate exists."
  task_verify: >-
    PASS — re-run BY ME from the repo root against the plan's verify scalar, twice: after T-01 and
    again after MF-1. Exit 0, prints PASS. Not relayed.
  qa_gate: >-
    PASS AFTER ONE FIX CYCLE — blocking gate, and it earned its blocking status. Both required kinds
    ran as the standing test_kinds commands rather than as T-01's task-local verify: unit 10/10
    scripts (test-factory-gh.py 118/118), integration 12/12 scripts
    (test-factory-integration.py 97/97). The integration KIND had never been shown green before this
    segment — every "97/97" in the record until now was one script out of twelve. Re-run by me.
  mf1_mutant_proof: >-
    PASS — the fix was proven falsifiable rather than asserted. factory_gh.py was mutated to drop the
    value, three named checks reddened (:411 once, :433 twice inside the two-fixture loop), and the
    file was restored byte-identically. I verified the restore myself: git diff --stat over
    factory_gh.py is EMPTY, and all three D-04 frozen literals (:332, :476, :624) are untouched.
  review_panel: "PENDING — dispatched against the MF-1 commit, never an earlier pin"
  uat: "PENDING — blocking_when_uat_criteria_exist; SC-01 is uat and only the operator may run it"
  github_mirror: "PASS — open: milestone #5, parent #214 (created), T-01 -> #215; close-task closed #215, left #211 open for the briefing"
  commit_pen: "EXERCISED twice, always by explicit pathspec, held dirt excluded"
runs:
  - { id: plan-product, squad: product, verdict: PASS }
  - { id: plan-eng, squad: eng, verdict: PASS }
  - { id: plan-contract-validator, squad: validator, verdict: FAIL }
  - { id: plan-fix-product, squad: product, verdict: PASS }
  - { id: t01-eng, squad: eng, verdict: PASS }
  - { id: t01-qa-validator, squad: validator, verdict: FAIL }
  - { id: 2026-08-10-01-q5-product, squad: product, verdict: PASS }
  - { id: mf1-eng, squad: eng, verdict: PASS }
runs_note: >-
  plan-eng is the eng-lead architecture review's artifact dir from inside the plan-feature run; it
  holds no digest. 8 runs against a max_total_runs of 20 — INV-22 is not crossed and no note is owed.
residuals:
  d03_partial_success: >-
    MEASURED BY qa, escalated as E-1, and NOT a fix cycle. A constructed partial-success envelope
    (exit 1, complete data, no null anywhere, non-empty errors) makes project_field_set return
    successfully AND issue the gh project item-edit — a WRITE on a call gh reported as failed, worse
    than the read eng-lead predicted. It does NOT violate SC-07, whose clause is that a failed
    resolution never silently reuses the BARE BOARD NUMBER as --project-id: the id passed is the
    resolved PVT_ node id from a complete envelope. It is signed D-03 step 3 behaving exactly as
    written ("if it is a mapping containing a data key, set env to it and FALL THROUGH"), so the
    remedy contradicts the signed decision and no eng fix cycle can legitimately close it. Routed to
    pm for a recommendation, and it reaches the operator in the briefing as a named decision.
  data_null_wrong_cause: >-
    A top-level {"data": null} envelope raises "project owner not found: <owner>" — safe, no write,
    but factually wrong about the cause. SC-10 satisfied literally. Backlog.
  plan_anchor_43: >-
    plan.yaml's intent cites factory_cli.body "at factory_gh.py:43"; line 43 is self.stdout = stdout
    and the call is at :45. Harmless — the instruction it supports is correct either way — but
    recorded so the anchor is not copied forward. plan.yaml is pm's file, so the fix rides goal-check.
  qa_cannot_author_here: >-
    team-config.yaml:217-218 grants harness-qa `tests/**` and `web/src/**/*.test.*`, neither of which
    matches `.claude/skills/harness/bin/test-*.py` — the only test files this repo has. The qa
    segment is therefore GATE-AND-ASSESS by construction and a missing test is a finding, never
    something qa can write. Same class as the Q6 pm-receipt routing wall. Backlog.
  ok_stale_receipt_markers: >-
    Two ok-stale markers survive at notes/review-harness-ui-reviewer-plan-product.md:63-64. Left
    deliberately: that file is the validator squad's artifact and product cannot write it. Four other
    hits are PROSE ABOUT the syntax, not markers — deleting those would corrupt the sentences and
    rewrite a review record, which principles rule 15 forbids. Backlog, not owed here.
github:
  milestone: 5
  parent: 214
  parent_origin: created
  attached: [T-01]
  issues:
    T-01: 215
