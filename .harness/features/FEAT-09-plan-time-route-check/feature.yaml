feature_id: FEAT-09-plan-time-route-check
phase: build
branch: feat/FEAT-09-plan-time-route-check
pr: none
status: awaiting_user
review_sha: 1185d7f
cycles_used: 1
max_total_cycles: 10
cost_usd: 214.93
max_cost_usd: 120
runs:
  - id: plan-product
    squad: product
    verdict: ESCALATE
    cost_usd: 56.27
  - id: seg1-main-session
    squad: none
    verdict: PASS
    cost_usd: 99.22
tasks: { T-01: DONE, T-02: HELD, T-03: DONE, T-04: DONE }
gate_status:
  unit: "PASS — 13/13 exit 0, re-run by ME after the segment, not relayed"
  docs: "PASS — check-docs.sh exit 0, re-run by me"
  state: "PASS — check-state.sh exit 0, zero violations, re-run by me"
  qa_gate: NOT_RUN — validate phase
  review: NOT_RUN — the panel is FOUR-WIDE by user ruling, see validate_panel
  security: NOT_RUN — NOT a skip; the ruling forbids pre-emptive skips
  ui: NOT_RUN — NOT a skip; the ruling forbids pre-emptive skips
  uat: NOT_REQUIRED — nothing here is operated by hand
  goal_check: NOT_RUN — cannot run until T-02 lands; SC-05..SC-10 and SC-12 rest on it
  ship_refresh: "SKIP — .harness/codebase/ does not exist in this tree"
validate_panel:
  ruling: >-
    USER RULING, binds the validate phase: run ALL FOUR steps of teams/review.yaml —
    code, qa, security, ui. NO pre-emptive skips. A reviewer that looks and declines is
    a reviewed finding; an orchestrator predicting there is nothing to find is an
    inference standing in for a measurement.
  why_here_especially: >-
    This feature adds a resolve mode to check-domain.sh — the guard deciding which agent
    may write which path. Diff size does not argue a security review away.
  expect: >-
    Two spawns may return severity_max n/a with PASS. That is the CORRECT outcome —
    validate-digest.py already accepts that pair for a reviewer that scoped out.
  if_skipping: >-
    Any skipped step means returning awaiting_user with the reason. The call is the user's.
segment_1_receipts:
  verified_by: >-
    ME, re-running every verify: in the worktree after the segment returned. The
    coordinator's report is corroborated, not relayed. One of my own probes was
    malformed — escaped quotes made the hook payload invalid JSON, which read as a
    false exit 0; re-run from a real payload file it gave the expected exit 2.
  t01: >-
    PASS. Resolve probe prints OK; test-check-domain.py exit 0; unit suite exit 0. My
    independent open-pipe probe answered in 0.21s with harness-dev-ops — the pre-change
    tree hung past 10s on that exact call.
  t01_one_matcher: >-
    THE LOAD-BEARING CLAIM, VERIFIED NOT TRUSTED. glob_to_re and matches hash IDENTICALLY
    between ae2443d and the landed tree (dedented, blank-stripped: 2025f71963fd and
    59e448ad0fdb on both sides). Exactly one definition of each remains. fnmatch appears
    once, inside the comment explaining why it is wrong. REQ-04 and SC-08 clauses 2 and 3
    hold.
  t01_stdin: >-
    payload=$(cat) is in the ELSE branch at :40; the --resolve branch at :36 sets an env
    var and never reads stdin. The structure is right, not merely the timing.
  t01_hook_path: >-
    SC-04 confirmed with my own payload files: documentor writing bin/ exits 2, documentor
    writing docs/ exits 0. The hook path is unchanged.
  t03: "PASS. Full chained verify exit 0. 68 lines of an 80 cap."
  t04: "PASS. Full chained verify exit 0. 134 lines of a 140 cap — 6 spare."
  sc11: "git diff ae2443d -- .claude/agents/harness-pm.md is 0 bytes. Re-checked post-segment."
  deferred: >-
    STATED, NOT HIDDEN. The suite is 13 PASS, not 14. SC-10's registered-test clause is
    NOT exercised by anything in segment 1, and T-03's and T-04's durable evidence
    (cases 10-12) does not exist yet either — their land-time greps are the only gate
    they have passed. All of it rests on the held task.
rulings:
  q1_t02_held: >-
    USER RULING 2026-08-05. T-02 is HELD until FEAT-08 merges to main AND this worktree
    is rebased onto it. After that merge, FEAT-08's removal of test-cost-report.py means
    an array written from this tree would list a script that no longer exists. Rebase
    first, then append to the corrected 12-entry array. Option (c) — one feature owning
    both edits — is BROKEN: FEAT-08's tree has no test-check-plan-routes.py, so listing
    it fails FEAT-08's own drift detector.
  q2_decisions_entry: >-
    USER RULING 2026-08-05. This flow writes the DECISIONS.md entry AFTER FEAT-08 merges.
    Substance: task routing is resolved at plan time by check-plan-routes.py calling
    check-domain.sh --resolve, and an ungranted surface becomes a DECLARED main-session
    step, never a discovered one. The index is GENERATED — write the entry, run
    gen-decisions-index.py, never hand-edit generated fields, and
    gen-decisions-index.py --stdout | diff - must exit 0 after.
  q2_brief_override: >-
    RECORDED SO THE GOAL-CHECK DOES NOT FLAG IT. BRIEF.md:106-109 forbids any task here
    writing under docs/harness/. The q2 ruling OVERRIDES that for the single DECISIONS.md
    entry plus the regenerated index, on the user's authority, 2026-08-05.
  q2_route: >-
    docs/** is granted to harness-documentor at team-config.yaml:116 — verified, not
    recalled. The entry routes product-lead to documentor as a team task.
  q345_open: >-
    Q3 (promote the checker to a check-state.sh invariant), Q4 (consolidate the copied
    task-block regex), Q5 (normalise squad-dispatched in historical plans) stay open and
    NON-BLOCKING. They ride into the briefing as residual backlog.
  t04_window: >-
    ACCEPTED BY THE USER, DISCLOSED HERE. Between T-04 landing and T-02 landing,
    harness-spec-driven instructs a planner to run a script that does not exist. No
    feature is being planned in that window.
next_steps:
  s1: "FEAT-08 merges to main; this worktree rebases onto it — the user's signal, not mine"
  s2: "T-02 via eng-lead: append to the ALREADY-CORRECTED 12-entry array, re-read :6 first"
  s3: "The DECISIONS.md entry + regenerated index via product-lead to documentor"
  s4: "Validate: FOUR-WIDE panel, all of teams/review.yaml, no pre-emptive skips"
  s5: "pm goal-check on all 12 SCs, then distillation, then the CEO briefing"
cost_note: >-
  OVER BUDGET AND REPORTED, NEVER HIDDEN (DEC-134 — cost is informational, never a gate).
  Project cumulative 6173.0448 to 6272.2639, a delta of 99.2191 for the segment. Feature
  total 214.93 against a 120 budget: 1.79x. CONTAMINATED UPWARD AND PROVABLY SO —
  harness-eng-lead moved +4.2588 and its spawn count went 18 to 20 in this window, and I
  dispatched no eng-lead at all. That row is FEAT-08's flow metering into the same
  project-cumulative reporter, which is direct EVIDENCE for the contamination rather than
  an assertion of it. The true FEAT-09 share is materially lower than 214.93; no figure is
  invented and none is adjusted downward to flatter it.
cost_reconciliation_caveat: >-
  HONEST LIMIT, AND A POSSIBLE HARNESS DEFECT. Last stretch the delta reconciled exactly
  against the rows that moved. This one does NOT: the three rows I sampled sum to 99.7568
  against a total delta of 99.2191, a 0.5377 shortfall. Cumulative counters cannot
  decrease, so either a row I did not sample fell, or a session aged out of the 30-day
  transcript retention window and took its spend with it. I have not established which
  and am not guessing. Raised as a non-blocking question because it bears on whether any
  cost figure in this repo is stable over time.
baseline:
  base_sha: ae2443d
  head_sha: 1185d7f
  closes_issues: "#20"
  grilling: .harness/notes/grilling-routing-wall-2026-08-05.md
  briefing: none — not yet written
  worktree: /Users/molchairuangutai/GitHub/harness-FEAT-09
  cost_cumulative_for_next_delta: 6272.2639
  commits:
    - "06a680f — approved BRIEF/PLAN + build state"
    - "685901d — [harness:t-01] check-domain.sh --resolve + 8 cases"
    - "92d254b — [harness:t-03] templates/PLAN.md Lanes + execution_mode"
    - "1185d7f — [harness:t-04] harness-spec-driven routing rule"
  design_pass: "No DESIGN.md, no visual-designer spawn. Nothing here is operated by a human."
approved:
  brief: "approved — Mike Ruangutai, 2026-08-05, BRIEF.md:115-117"
  plan: "approved — Mike Ruangutai, 2026-08-05, PLAN.md:118-120"
resolved:
  - >-
    THE BUILDER HIT THE EXACT FAIL-OPEN THE PLAN PREDICTED, BY ACCIDENT. Its first
    insertion put the resolve branch below the identity early-exit and reproduced exit 0
    with empty stdout — the hazard the receipts table documented, produced unintentionally
    and caught by running rather than reading. The second attempt hit NameError on the
    hoisted matcher. Two wrong placements, both caught by execution. That is the strongest
    evidence this feature's premise was correct.
  - >-
    A DELIBERATE DIVERGENCE FROM THE HOOK, AND IT IS RIGHT. The hook fails OPEN on a
    missing manifest; the resolver exits 2. An unanswerable route reported as NOBODY would
    put a task in the main-session lane on the strength of a broken config. This sits
    inside T-01's approved intent, not outside it.
  - >-
    MY OWN PROBE LIED BEFORE THE CODE DID. Escaped quotes made a hook payload invalid JSON
    and returned a false exit 0 that looked like a regression. The tool under test was
    fine; the measurement was not. Re-run from a payload file it gave exit 2 as designed.
github:
  milestone: 4
  parent: 98
  attached: [T-01, T-02, T-03, T-04]
  closed: ["T-01 -> 99", "T-03 -> 101", "T-04 -> 102"]
  open: ["T-02 -> 100"]
