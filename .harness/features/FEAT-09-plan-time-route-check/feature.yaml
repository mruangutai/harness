feature_id: FEAT-09-plan-time-route-check
phase: validate
branch: feat/FEAT-09-plan-time-route-check
pr: none
status: awaiting_user
posture: >-
  Source COMPLETE and the four-wide panel RAN, no skips. It found a HIGH that I reproduced
  myself: an environment variable silently disables the whole PreToolUse guard, making SC-04
  FALSE AS WRITTEN, not merely under-tested. The fix is one line plus one test case on
  check-domain.sh — a DEC-174 carve-out — so it is a DECLARED main-session step I may neither
  perform nor dispatch. Escalated with the fix fully specified.
review_sha: 4918d06
review_sha_note: >-
  PINNED 2026-08-05, ONCE, AFTER T-02 AND DEC-179, per the user ruling on the timing conflict.
  INV-6 binds at REVIEWER DISPATCH and validate_panel.timing requires one pass against a
  COMPLETE diff. The prior pin 1185d7f was DANGLING after the rebase and was removed, not
  carried; a pin taken before T-02 would have gone stale a second time. All four reviewers
  scoped 47ed11f..4918d06. The briefing must say the pin was re-taken and why.
cycles_used: 2
max_total_cycles: 10
cycles_note: >-
  1 to 2 for the panel FAIL — rework is open. The increment is PRE-EMPTIVE: no fix cycle has
  been dispatched, so do not count it again on re-delegation. Both team runs this segment were
  CLEAN first passes reporting 0 send-backs, which correctly adds nothing.
tasks: { T-01: DONE, T-02: DONE, T-03: DONE, T-04: DONE }
runs:
  - id: plan-product
    squad: product
    verdict: ESCALATE
  - id: seg1-main-session
    squad: none
    verdict: PASS
  - id: t02-eng
    squad: eng
    verdict: PASS
  - id: dec179-product
    squad: product
    verdict: PASS
  - id: panel-validator
    squad: validator
    verdict: FAIL
gate_status:
  unit: "PASS — 13/13 scripts exit 0, re-run by ME at 4918d06, not relayed"
  docs: "PASS — check-docs.sh exit 0, re-run by me"
  state: "PASS — check-state.sh exit 0, after I trimmed handoff-build.md to shape"
  index: "PASS — gen-decisions-index.py --stdout | diff - exits 0, re-run by me"
  review: "FAIL — one HIGH, reproduced independently by me. See must_fix_open"
  qa_gate: "PASS — matrix_ok true, 13/13 scripts, 32 named checks, non-vacuous"
  security: "RAN. Graded the env-var defect med on reachability; the lead overruled to high"
  ui: "PASS — self-scoped out ON MEASUREMENT, not prediction. No rendered surface"
  uat: NOT_REQUIRED — nothing here is operated by hand
  goal_check: NOT_RUN — deferred; SC-04 is already known false, see sc04_status
  ship_refresh: "SKIP — .harness/codebase/ does not exist in this tree"
must_fix_open:
  vf1_env_var_disables_the_guard: >-
    HIGH, BLOCKING, AND I REPRODUCED IT RATHER THAN RELAYING IT. If HARNESS_RESOLVE_PATH is
    present in the environment, EVERY agent can write EVERY path: exit 0, no stderr, nothing
    logged. Mechanism, measurement, fix and routing are written up in full at
    notes/vf1-guard-bypass.md — read that, not this line.
  vf1_routing_summary: >-
    A DECLARED MAIN-SESSION STEP. Measured: the MANIFEST grants check-domain.sh to
    harness-backend-dev and harness-dev-ops, but DEC-174 forbids dispatching a change to it
    through a team run whose gates are the thing being changed, and the domain hook separately
    BLOCKS me. I may neither perform this fix nor delegate it.
sc04_status: >-
  SC-04 IS FALSE AS WRITTEN, NOT UNDER-TESTED — that distinction is what makes VF-1 gate.
  BRIEF.md:48-49 states it in ARGV terms; my test was exactly that shape and exited 0, and the
  implementation does not branch on argv at all. NOT a re-plan: the one-line fix MAKES the
  approved criterion true, so this is approved-but-unmet — a fix cycle in DEC-174's lane.
validate_panel:
  ruling_vindicated: >-
    The user ruling — all four steps, NO pre-emptive skips — is now VINDICATED rather than
    merely obeyed. No single reviewer could have produced the gating finding: security found
    the mechanism without checking it against SC-04, and code verified SC-04 correctly but
    under a CLEAN environment. The defect lived in the UNION of the scopes, and the lead's
    collation is what surfaced it. The ui reviewer declined ON MEASUREMENT — a reviewed finding.
  scope: >-
    47ed11f..4918d06, NINE source files named EXPLICITLY to the reviewers rather than left to
    each lens to infer, because a prior panel's gap was a property of self-chosen scopes.
  digest: .harness/features/FEAT-09-plan-time-route-check/runs/panel-validator/digest.md
backlog:
  vf2_shared_files_rejected: >-
    NEEDS A USER RULING EVENTUALLY. A task naming a `shared:` file (package.json, pyproject.toml)
    is FALSELY REJECTED by check-plan-routes.py. The dispatch predicted a fail-OPEN here; the
    truth is the opposite, it fails CLOSED at :64. Top of the non-gating list because dependency
    work hits it immediately. Answering it amends DEC-179.
  sc08_weaker_than_it_reads: >-
    Three of SC-08's four clauses are SOURCE GREPS and therefore respellable, and case 17's
    mid-pattern `*` never crosses a `/`, so an fnmatch reimplementation would PASS it. The
    design rule is about a third as guarded as the clause count suggests.
  matcher_unverified: >-
    matches()/glob_to_re() are UNMODIFIED, so the panel established only that the matcher was
    not RE-implemented, never that it is correct. Separately the two modes do NOT share input
    normalisation: hook uses os.path.abspath (cwd-relative), --resolve uses os.path.join(root).
  checker_never_observed_in_use: >-
    No reviewer could tell whether a planning agent actually RUNS the checker; BRIEF.md:91-93
    names this itself. Exercised against synthetic fixtures and FEAT-09's own PLAN only, so the
    false-positive rate on the tree's other live PLANs is UNMEASURED.
  q1_stale_anchor: >-
    NOT A BUILDER DEFECT. check-plan-routes.py:16 and test-check-plan-routes.py:142 cite
    check-domain.sh:190-197 for the prefix-comparison bug; the real record is at :61-69.
    PLAN.md:210 CARRIES THE SAME WRONG ANCHOR — the approved plan is the origin.
  q2_team_token_unvalidated: >-
    WITHIN THE APPROVED INTENT. LEGAL_TOKENS at :42 is display-only. PLAN.md:177-181 specifies
    that a granting agent means OK regardless of token — a design limit, not a defect.
  q3_promote_invariant: "Promote the route checker to a check-state.sh invariant?"
  q4_shared_regex: "The checker copies check-state.sh's task-block regex (D-08). Consolidate?"
  q5_squad_dispatched: "Two historical plans use a token this feature retires. Normalise?"
  q6_argvless_glob: >-
    check-plan-routes.py with no argv globs relative to CWD, so from a non-repo-root cwd it
    prints 0 violations across 0 plans and exits 0 — a clean answer from a run that checked
    nothing. Not reachable via the documented invocation. Becomes BLOCKING if q3 lands.
  q7_blank_files_field: "Code reviewer's new MED: a blank `files:` field is mishandled"
  q8_inv17_late_enforcement: >-
    FOUND BY ME, AND NARROWED. The handoff cap IS enforced at WRITE time — it blocked four of
    my own drafts. But check-state.sh INV-17 re-checks the note only once phase: moves PAST the
    seam, so a note that got onto disk over-cap sits unflagged for the whole phase it describes.
    handoff-build.md was 63 lines against a 60 cap and surfaced only when I advanced the phase.
  q9_state_cap: >-
    CORRECTED AGAINST MYSELF. feature.yaml's 200-line cap IS mechanically enforced — it rejected
    four successive drafts of mine, at 217, 208, 206 and 205 lines, and routed the rationale to
    notes/. The unenforced one is STATE.md, where a 205-line write once succeeded.
declined:
  dec126_ref: >-
    The product lead proposed adding a DEC-126 reference to DEC-179's refs. DECLINED with
    reason: DEC-126 governs digest-schema centralization and has no bearing on plan-time route
    resolution. A false edge in the refs graph costs more than a missing one.
rulings:
  q2_brief_override: >-
    BRIEF.md:106-109 forbids any task here writing under docs/harness/. The user OVERRODE that
    for the single DECISIONS.md entry plus the regenerated index — nothing else. Recorded so
    the goal-check does not read it as a breach. Verified honoured: the commit touched exactly
    those two files.
  qE_no_cost_line: >-
    Write NO cost line, invent NO figure, carry NO budget field. The mandate itself is gone:
    cost-report.py is deleted and cost_model stripped from harness.json by FEAT-08. State once
    in the briefing that the harness no longer meters spend (DEC-178).
receipts:
  method: >-
    ME, re-running every verify: after each run returned — corroborated, never relayed. Task
    detail lives in the run digests and in notes/handoff-build.md ## Trust, not here.
  t02: >-
    Re-run by me at ae28daf. 17 distinct case numbers (16 and 17 separate), 19 PASS lines since
    case 17 asserts thrice. Suite 13/13. On FEAT-09's own PLAN: zero violations, exit 0,
    exactly ONE DEVIATION naming T-01.
  t02_scripts_array: >-
    THE SHARED-ARRAY HAZARD DID NOT FIRE. git diff 47ed11f on run-unit-tests.sh is exactly one
    added element, 12 to 13. test-cost-report.py appears NOWHERE. FEAT-08's removal preserved.
  t02_no_reimplementation: >-
    Verified STRUCTURALLY, not by grep, because a grep is respellable. No entry-versus-grant
    comparison exists in check-plan-routes.py: every literal entry goes to the check-domain.sh
    subprocess at :52-57, and :64 parses that subprocess's OUTPUT.
  dec179: "Entry at DECISIONS.md:4927, 41 lines. Index regenerates byte-identical"
  sc11: "HOLDS: git diff 47ed11f -- .claude/agents/harness-pm.md is 0 bytes"
baseline:
  base_sha: 47ed11f
  base_sha_note: >-
    RE-PINNED AT THE REBASE. Was ae2443d, a PRE-FEAT-08 main commit that STILL RESOLVES, so
    nothing errors — git diff ae2443d..HEAD just returns the WRONG SCOPE, 71 files against a
    true 14. 47ed11f is git merge-base main HEAD, measured.
  head_sha: 4918d06
  closes_issues: "#20"
  briefing: none — not yet written; the feature is not shippable
  worktree: /Users/molchairuangutai/GitHub/harness/.claude/worktrees/FEAT-09
  commits:
    - "6792331 — [harness:t-01] check-domain.sh --resolve + 8 cases"
    - "e355401 — [harness:t-03] templates/PLAN.md Lanes + execution_mode"
    - "358fd36 — [harness:t-04] harness-spec-driven routing rule"
    - "ae28daf — [harness:t-02] check-plan-routes.py + 17-case test + runner registration"
    - "4918d06 — DEC-179 + regenerated index"
  commits_note: >-
    The PRE-REBASE ids (685901d, 92d254b, 1185d7f, 06a680f, abddb28, 2a242df) are DANGLING and
    must never be cited. Replay verified byte-identical on all four source files.
approved:
  brief: "approved — Mike Ruangutai, 2026-08-05, BRIEF.md:115-117"
  plan: "approved — Mike Ruangutai, 2026-08-05, PLAN.md:118-120"
github:
  milestone: 4
  parent: 98
  closed: ["T-01 -> 99", "T-03 -> 101", "T-04 -> 102"]
  open: ["T-02 -> 100 — close after VF-1 is resolved"]
briefing_must_carry:
  verbatim_finding: >-
    KEEP THIS WORDING, DO NOT SOFTEN IT (user instruction): this collision is exactly the
    failure FEAT-09 exists to prevent — and the checker this feature builds would not have
    caught it, because the collision is in a tool the plan uses, not a path a task writes.
  also: >-
    The unreviewed-guard window as a window that EXISTED — and that the panel closing it is
    what found VF-1; that the review pin was re-taken and why; that the harness no longer
    meters spend so there is NO cost line and no figure; the backlog above.
