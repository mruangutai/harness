feature_id: FEAT-09-plan-time-route-check
phase: validate
branch: feat/FEAT-09-plan-time-route-check
pr: none
status: awaiting_user
posture: >-
  Source COMPLETE, the four-wide panel RAN with no skips, and its HIGH (VF-1) is FIXED at
  7218d63 — applied by the MAIN SESSION under DEC-174, the lane the orchestrator identified and
  could not enter. SC-04 is now TRUE as written. TWO DECISIONS ARE OPEN and both are the user's:
  the re-review scope for the 34-line fix the panel has not seen, and the ruling on VF-2, a
  SECOND reachability gap found after the panel closed (the shape gate is bypassed by Edit).
  Goal-check, distillation and briefing all wait on the re-review scope call.
review_sha: 4918d06
review_sha_note: >-
  PINNED 2026-08-05, ONCE, AFTER T-02 AND DEC-179, per the user ruling on the timing conflict.
  INV-6 binds at REVIEWER DISPATCH and validate_panel.timing requires one pass against a
  COMPLETE diff. The prior pin 1185d7f was DANGLING after the rebase and was removed, not
  carried; a pin taken before T-02 would have gone stale a second time. All four reviewers
  scoped 47ed11f..4918d06. The briefing must say the pin was re-taken and why.
cycles_used: 2
max_total_cycles: 10
cycles_note: >-
  RESOLVED — the increment is now EARNED, not pre-emptive, and STAYS AT 2. It was recorded
  pre-emptively for a fix cycle nobody had dispatched; the rework then actually happened, as
  VF-1 at 7218d63. DEC-157 counts rework, and this was rework — the fact that it ran
  main-session-direct under DEC-174 rather than through a team run changes the LANE, not
  whether rework occurred. DO NOT INCREMENT AGAIN for VF-1 on any re-delegation; it is spent.
  Both team runs this segment were CLEAN first passes reporting 0 send-backs, which correctly
  adds nothing.
tasks: { T-01: DONE, T-02: DONE, T-03: DONE, T-04: DONE }
runs:
  - id: plan-product
    squad: product
    verdict: ESCALATE
  - id: seg1-main-session
    squad: none
    verdict: PASS
  - id: t02-eng
    squad: eng
    verdict: PASS
  - id: dec179-product
    squad: product
    verdict: PASS
  - id: panel-validator
    squad: validator
    verdict: FAIL
gate_status:
  unit: "PASS — 13/13 scripts exit 0, re-run by ME at 4918d06, not relayed"
  docs: "PASS — check-docs.sh exit 0, re-run by me"
  state: "PASS — check-state.sh exit 0, after I trimmed handoff-build.md to shape"
  index: "PASS — gen-decisions-index.py --stdout | diff - exits 0, re-run by me"
  review: >-
    FAIL AT 4918d06 — one HIGH. The HIGH (VF-1) is now FIXED at 7218d63, but this verdict is
    NOT flipped to PASS by the fixer: the panel has not seen the fix. Re-review scope is an
    OPEN USER DECISION — see review_delta_unreviewed.
  qa_gate: >-
    PASS — RE-RUN BY THE MAIN SESSION AT 7218d63, not carried from 4918d06. The prior PASS was
    pinned before VF-1's two new cases existed. change_type logic/bugfix both require `unit`;
    test-check-domain.py matches the unit detect pattern; cmd run-unit-tests.sh exits 0 with
    32 PASS. NON-VACUOUS AND PROVEN SO: cases (i)/(j) FAIL against the pre-fix guard (suite
    exit 1) and pass with it (10/10 --resolve cases) — the bug-class test the matrix asks for.
  security: "RAN. Graded the env-var defect med on reachability; the lead overruled to high"
  ui: "PASS — self-scoped out ON MEASUREMENT, not prediction. No rendered surface"
  uat: NOT_REQUIRED — nothing here is operated by hand
  goal_check: >-
    NOT_RUN — no longer blocked by SC-04 (now TRUE, see sc04_status). Outstanding only on the
    re-review scope decision, since a goal-check citing an unreviewed source commit would rest
    on an unpinned diff.
  ship_refresh: "SKIP — .harness/codebase/ does not exist in this tree"
must_fix_open: {}
must_fix_resolved:
  vf1_env_var_disables_the_guard: >-
    FIXED AND VERIFIED AT 7218d63 by the MAIN SESSION under DEC-174 — the lane the orchestrator
    identified and could not enter. Reproduced before fixing and re-measured after, with payload
    files. Test-first and proven non-vacuous: cases (i)/(j) FAIL against the pre-fix guard.
    Gates at 7218d63: unit 0 (32 PASS), docs 0, state 0, index 0. Full record:
    notes/vf1-guard-bypass.md.
must_fix_new:
  vf2_shape_gate_edit_bypass: >-
    FOUND BY THE MAIN SESSION AFTER THE PANEL CLOSED, and NOT YET FIXED — it needs a user ruling,
    not a line. The DEC-150 state-file shape gate is registered for Write|Edit but returns at
    check-domain.sh:376 unless tool_name == "Write", so EVERY budget below it is unreachable via
    Edit. Measured, 400-line feature.yaml: Write exits 2, Edit exits 0. Same class as VF-1 — the
    logic is correct, the REACHABILITY is not. The domain check is unaffected. Four options with
    costs, and why it is a ruling: notes/vf2-shape-gate-edit-bypass.md.
review_delta_unreviewed:
  what: >-
    OPEN USER DECISION. The panel reviewed 47ed11f..4918d06; the VF-1 fix at 7218d63 is a NEW
    SOURCE COMMIT it has not seen, on check-domain.sh — the file its HIGH was about. review_sha
    no longer describes the tree.
  size: >-
    MEASURED so the scope call rests on a number: `git diff 4918d06..HEAD` is 2 source files,
    34 lines — check-domain.sh +13 (one functional line plus comment), test-check-domain.py +22
    (two cases). The rest of that range is .harness state.
sc04_status: >-
  NOW TRUE AS WRITTEN, at 7218d63. THE GOAL-CHECK MUST NOT READ THE OLD LINE — pm would score a
  met criterion as failed. History, kept because the sequence is the finding: SC-04 was FALSE AS
  WRITTEN at 4918d06, not merely under-tested. BRIEF.md:48-49 states it in ARGV terms and the
  implementation did not branch on argv at all, so a payload with no --resolve in argv exited 0
  whenever the environment carried HARNESS_RESOLVE_PATH. It was approved-but-unmet rather than a
  re-plan, and the one-line fix made the approved criterion true rather than rewriting it.
  Now measured directly in BRIEF.md's own terms: an out-of-domain Write with no --resolve in
  argv exits 2 on a clean env, with the var set, and with it set to empty. Cases (i)/(j) are the
  durable evidence and they FAIL against the pre-fix guard, so they can fail.
validate_panel:
  ruling_vindicated: >-
    The user ruling — all four steps, NO pre-emptive skips — is now VINDICATED rather than
    merely obeyed. No single reviewer could have produced the gating finding: security found
    the mechanism without checking it against SC-04, and code verified SC-04 correctly but
    under a CLEAN environment. The defect lived in the UNION of the scopes, and the lead's
    collation is what surfaced it. The ui reviewer declined ON MEASUREMENT — a reviewed finding.
  scope: >-
    47ed11f..4918d06, NINE source files named EXPLICITLY to the reviewers rather than left to
    each lens to infer, because a prior panel's gap was a property of self-chosen scopes.
  digest: .harness/features/FEAT-09-plan-time-route-check/runs/panel-validator/digest.md
backlog:
  detail: notes/backlog-detail.md — one line each here, rationale there (DEC-150 routing)
  b1_shared_files_rejected: "NEEDS A RULING. A `shared:` file is falsely REJECTED — fails CLOSED, not open. Amends DEC-179"
  b2_sc08_weaker_than_it_reads: "3 of 4 clauses are respellable greps; case 17's `*` never crosses a `/`. Issue #74 mode 3, live"
  b3_matcher_unverified: "matches()/glob_to_re() proven not RE-implemented, never proven CORRECT. Modes differ on normalisation"
  b4_checker_never_observed: "Never observed actually RUN by a planner. False-positive rate on other live PLANs UNMEASURED"
  b5_stale_anchor: "Cites check-domain.sh:190-197; real record :61-69. PLAN.md:210 is the origin — a PLANNING defect"
  b6_team_token_unvalidated: "LEGAL_TOKENS is display-only. Within the approved intent — a design limit, not a defect"
  b7_argvless_glob: "No argv globs relative to CWD: 0 violations across 0 plans, exit 0. BECOMES BLOCKING if b8 lands"
  b8_promote_invariant: "Promote the route checker to a check-state.sh invariant?"
  b9_shared_regex: "The checker copies check-state.sh's task-block regex (D-08). Consolidate?"
  b10_squad_dispatched: "Two historical plans use a token this feature retires. Normalise?"
  b11_blank_files_field: "Code reviewer MED, unresolved: a blank `files:` field is mishandled"
  b12_inv17_late: "INV-17 re-checks a handoff only after phase: advances — unflagged during the phase it describes"
  b13_state_cap_story: "The cap is Write-only; Edit bypasses it. See must_fix_new.vf2 — that part is NOT backlog"
declined:
  dec126_ref: >-
    The product lead proposed adding a DEC-126 reference to DEC-179's refs. DECLINED with
    reason: DEC-126 governs digest-schema centralization and has no bearing on plan-time route
    resolution. A false edge in the refs graph costs more than a missing one.
rulings:
  q2_brief_override: >-
    BRIEF.md:106-109 forbids any task here writing under docs/harness/. The user OVERRODE that
    for the single DECISIONS.md entry plus the regenerated index — nothing else. Recorded so
    the goal-check does not read it as a breach. Verified honoured: the commit touched exactly
    those two files.
  qE_no_cost_line: >-
    Write NO cost line, invent NO figure, carry NO budget field. The mandate itself is gone:
    cost-report.py is deleted and cost_model stripped from harness.json by FEAT-08. State once
    in the briefing that the harness no longer meters spend (DEC-178).
receipts:
  method: >-
    ME, re-running every verify: after each run returned — corroborated, never relayed. Task
    detail lives in the run digests and in notes/handoff-build.md ## Trust, not here.
  t02: >-
    Re-run by me at ae28daf. 17 distinct case numbers (16 and 17 separate), 19 PASS lines since
    case 17 asserts thrice. Suite 13/13. On FEAT-09's own PLAN: zero violations, exit 0,
    exactly ONE DEVIATION naming T-01.
  t02_scripts_array: >-
    THE SHARED-ARRAY HAZARD DID NOT FIRE. git diff 47ed11f on run-unit-tests.sh is exactly one
    added element, 12 to 13. test-cost-report.py appears NOWHERE. FEAT-08's removal preserved.
  t02_no_reimplementation: >-
    Verified STRUCTURALLY, not by grep, because a grep is respellable. No entry-versus-grant
    comparison exists in check-plan-routes.py: every literal entry goes to the check-domain.sh
    subprocess at :52-57, and :64 parses that subprocess's OUTPUT.
  dec179: "Entry at DECISIONS.md:4927, 41 lines. Index regenerates byte-identical"
  sc11: "HOLDS: git diff 47ed11f -- .claude/agents/harness-pm.md is 0 bytes"
baseline:
  base_sha: 47ed11f
  base_sha_note: >-
    RE-PINNED AT THE REBASE. Was ae2443d, a PRE-FEAT-08 main commit that STILL RESOLVES, so
    nothing errors — git diff ae2443d..HEAD just returns the WRONG SCOPE, 71 files against a
    true 14. 47ed11f is git merge-base main HEAD, measured.
  head_sha: 4918d06
  closes_issues: "#20"
  briefing: none — not yet written; the feature is not shippable
  worktree: /Users/molchairuangutai/GitHub/harness/.claude/worktrees/FEAT-09
  commits:
    - "6792331 — [harness:t-01] check-domain.sh --resolve + 8 cases"
    - "e355401 — [harness:t-03] templates/PLAN.md Lanes + execution_mode"
    - "358fd36 — [harness:t-04] harness-spec-driven routing rule"
    - "ae28daf — [harness:t-02] check-plan-routes.py + 17-case test + runner registration"
    - "4918d06 — DEC-179 + regenerated index"
  commits_note: >-
    The PRE-REBASE ids (685901d, 92d254b, 1185d7f, 06a680f, abddb28, 2a242df) are DANGLING and
    must never be cited. Replay verified byte-identical on all four source files.
approved:
  brief: "approved — Mike Ruangutai, 2026-08-05, BRIEF.md:115-117"
  plan: "approved — Mike Ruangutai, 2026-08-05, PLAN.md:118-120"
github:
  milestone: 4
  parent: 98
  closed: ["T-01 -> 99", "T-03 -> 101", "T-04 -> 102"]
  open: ["T-02 -> 100 — close after VF-1 is resolved"]
briefing_must_carry:
  verbatim_finding: >-
    KEEP THIS WORDING, DO NOT SOFTEN IT (user instruction): this collision is exactly the
    failure FEAT-09 exists to prevent — and the checker this feature builds would not have
    caught it, because the collision is in a tool the plan uses, not a path a task writes.
  also: >-
    The unreviewed-guard window as a window that EXISTED — and that the panel closing it is
    what found VF-1; that the review pin was re-taken and why; that the harness no longer
    meters spend so there is NO cost line and no figure; the backlog above.
