feature_id: FEAT-06-team-layer-inv6
phase: validate
branch: main
pr: none
status: awaiting_user
review_sha: 9f87c48
cycles_used: 5   # 4 spent in planning; the 5th is the SC-05 remediation ROUTED in this return
max_total_cycles: 10
cost_usd: 176.0
max_cost_usd: 100
runs:
  - id: plan-product
    squad: product
    verdict: PASS
    cost_usd: 0   # UNMEASURED, see cost_note; comparable runs put it in the 12-45 band
  - id: plan-eng
    squad: eng
    verdict: FAIL
    cost_usd: 18.9
  - id: replan-product
    squad: product
    verdict: PASS
    cost_usd: 25.91
  - id: d08-settle-product
    squad: product
    verdict: PASS
    cost_usd: 17.1
  - id: arch-review-eng
    squad: eng
    verdict: FAIL
    cost_usd: 28.19
  - id: inter-gate-q1q2
    squad: none
    verdict: PASS
    cost_usd: 18.46   # TOP-LINE diff, not by_agent; upper bound on that segment
  - id: amf-fix-product
    squad: product
    verdict: PASS
    cost_usd: 25.47
  - id: delta-review-eng
    squad: eng
    verdict: FAIL
    cost_usd: 22.19
  - id: dmf-fix-product
    squad: product
    verdict: PASS
    cost_usd: 13.95
  - id: t08-product
    squad: product
    verdict: PASS
    cost_usd: 11.39   # by_agent delta: product-lead 7.25 + documentor 4.14. BUILD phase
  - id: qa-validator
    squad: validator
    verdict: PASS
    cost_usd: 26.97   # upper bound; validator-lead d2 18.85 + qa d3 8.12
  - id: panel-validator
    squad: validator
    verdict: PASS
    cost_usd: 27.67   # validator-lead 14.24 + code-reviewer 7.92 + security 3.60 + qa 1.91
  - id: goalcheck-product
    squad: product
    verdict: FAIL
    cost_usd: 16.24   # product-lead 8.30 + pm 7.94. SC-05 unmet as declared
tasks:
  T-01: PASS
  T-02: PASS
  T-04: PASS
  T-05: PASS
  T-06: PASS
  T-07: PASS
  T-08: PASS
  T-09: PASS
  T-10: PASS
  T-11: PASS
gate_status:
  qa_gate: PASS — matrix_ok true at 9f87c48, re-verified at the orchestrator's tier
  review: PASS — severity_max low, must_fix empty; advisory_unless_high, so non-blocking
  uat: PENDING — SC-13 is the only uat criterion and gates.uat is blocking_when_uat_criteria_exist
  goal_check: FAIL — 13 of 14 non-uat SCs met; SC-05 unmet as declared; SC-13 pending_uat
sc_summary:
  met: 13
  unmet: SC-05
  pending_uat: SC-13
  evidence: runs/goalcheck-product/digest.md carries all 15 with method and evidence
files_touched_union: >-
  git diff --name-only 635ef14..071e313, outside the feature dir — harness-team/SKILL.md,
  harness/SKILL.md, bin/{check-state.sh,harness_yaml.py,run-unit-tests.sh,test-check-state.py,
  test-harness-yaml-corpus.py,test-team-catalog.py,validate-digest.py},
  teams/{build.yaml,gate-probe.yaml (deleted),review.yaml}, .harness/logs/2026-08-04.md,
  docs/harness/{DECISIONS.md,SPEC.md}. 15 paths. Computed, not recalled.
baseline:
  base_sha: 635ef14
  closes_issues: "#8 #9 #16 #24"
  briefing: .harness/features/FEAT-06-team-layer-inv6/notes/ship-review-FEAT-06.md
  commits: f45fd0f, 510b7ff, 9f87c48, 071e313 on main. Not pushed, not merged
  cost_note: >-
    OVER BUDGET, REPORTED NOT HIDDEN (DEC-134). 176 is the midpoint of a MEASURED 154-199 range
    against the 100 build+validate allowance — 1.5-2x over. The range exists because cost-report.py
    is cumulative over a shared retained window: 154 counts only org-pinned opus rows, 199 counts
    every attributable row. THE DOMINANT LINE IS THE ORCHESTRATOR'S OWN SESSION at ~88, 45-57% of
    the total — DEC-148's square-of-session-length effect, measured. 9 of 10 tasks also ran at
    depth-0 in the main session and are not separable, so the true total is HIGHER than any figure
    here. The PLAN phase separately spent 170.17 of its own 160. NO FIGURE IS INVENTED.
  not_run_with_reason: >-
    (1) FEATURE-CLOSE DISTILLATION — its PRECONDITION IS UNMET, which is the reason; it runs after
    the SCs pass and they have not (SC-05 unmet, SC-13 pending_uat). It becomes due once both
    close. NOT a budget cut. Observation logs are on disk and lose nothing by waiting.
    (2) THREE-LEAD PARALLEL BRIEFING REPORT — all their digests are already held, and eng-lead did
    no build or validate work at all, so it would have returned "no activity" at real cost. An
    execution-time call, disclosed in the briefing. (3) SHIP-REFRESH is a genuine no-op:
    .harness/codebase/ does not exist, so there is no map to go stale.
  anchor_note: >-
    PLAN.md line anchors recorded before DMF-1 SHIFTED +8 through the T-02 middle and +9 below the
    carve. Cite T-02 fields by NAME, not by line.
  inv6_self_note: >-
    review_sha PINNED to 9f87c48 BEFORE the first squad: validator run entry was written. FEAT-06's
    own feature.yaml is INV-6's first real subject; case (j) in test-check-state.py is this shape.
    HEAD MOVES PAST THE PIN when the SC-05 line lands — that is FINE. INV-6 requires pinned, not
    equal-to-HEAD, and re-running an advisory panel for one assertion line would be waste.
approved:
  brief: approved 2026-08-04 by Mike Ruangutai (BRIEF.md `## Approval`)
  plan: approved 2026-08-04 by Mike Ruangutai (PLAN.md `## Approval`), 10 tasks, 8 decisions
resolved:
  - >-
    ROUTING WALL, MEASURED. NO agent domain grants write on .claude/skills/harness/teams/**,
    .claude/skills/harness/SKILL.md or harness-team/SKILL.md; the only .claude grant is
    .claude/skills/harness/bin/** (dev-ops, backend-dev). DEC-174's carve-out covers check-state.sh
    and validate-digest.py, extended by the mission and D-05 to four bin/ test and runner scripts
  - >-
    RECURRENCE 7, MEASURED: harness-qa has NO writable test surface here. tests/ and web/ do not
    exist; all 13 test scripts live in bin/. The qa segment's AUTHORING half is structurally
    unavailable. Stated in the dispatch so qa reported the gap instead of hitting the guard
  - >-
    #24 CLOSED IN THE PLAYBOOK. SC-14 measured both ways: at 635ef14 test_matrix lines 0 and the
    8-line window 0 hits; at 9f87c48 lines 2 and window 7 hits
  - >-
    FOUR DEFECTS CAUGHT MID-EXECUTION, each would have shipped GREEN (detail in the briefing and
    f45fd0f's message): T-05's glob scanned 0 files because Python glob skips DOTTED dirs, fixed
    with os.walk; SC-06 proven discriminating against the pre-widening scan; T-07's ten checks
    10-of-10 FAILING at 635ef14 and 10/10 at HEAD, re-run by the orchestrator through a detached
    worktree; T-02's sweep found a 5th and created a 6th comment site, caught by its closing re-grep
  - >-
    D-01 LANDED. PLACEHOLDER_UNSET defined once at harness_yaml.py:302 as an ordered tuple; both
    consumers carry the token; the literal count under bin/ is 1 AND STILL 1 after T-07 added a
    file there — the coupling conjunct was re-run post-T-07, which is what it was for
  - >-
    SC-15's THREE LEGS AGREE and T-07 check (9) parses unambiguously: SPEC.md:1978 carries 2 brace
    groups and exactly ONE containing the parallel bar; :1980 carries 1
  - >-
    No ui-reviewer step was spawned in the panel — recorded skip, not a gap. visual-designer ruled
    no end-user interaction and no prototype, and the diff is scripts, YAML and markdown
pending:
  - >-
    SEGMENT 4, ROUTED IN THIS RETURN, main-session-direct (DEC-174 carve-out): SC-05's count
    conjunct is unasserted — test-harness-yaml-corpus.py:180-181 asserts n > 0 per root and the "2"
    at :174-175 is an f-string LABEL. pm ruled UNMET AS DECLARED; premise verified at source twice.
    This is a FIX CYCLE, not a user decision — an unmet SC that CAN be met is not a plan change.
    Waiving would be the EXPENSIVE option (a pm run plus a signature, since it amends an approved
    BRIEF). TRAP: the line belongs in test-harness-yaml-corpus.py, NOT test-team-catalog.py, whose
    signed verify requires exactly TEN checks
  - >-
    Q2 BLOCKING, THE USER'S ALONE: SC-13 is the only uat criterion and gates.uat is blocking. The
    user reads teams/build.yaml and SKILL.md:40-53 and rules on whether they describe builds as
    they want them dispatched. No test settles it
  - >-
    NOT PROVEN, declared in BRIEF and signed open: build.yaml is never executed by this feature —
    the panel called it "prose with a passing shape-check"; no ship run exercises the new qa
    segment; four SCs assert only that files CONTAIN text. build.yaml derives from n=2 eng runs
  - >-
    BACKLOG for ship acceptance, full list in the briefing: #36; DECISIONS.md:1634 and :630 still
    three-wide; routing-wall recurrence 7; AQ-2 (panel qa gate-only is prompt-enforced only);
    advisory 3 (T-09 line caps 1 over); code-reviewer under-reported its own severity_max; PLAN.md
    :290-292 stale "3x slower"; #19
github:
  milestone: 1
  parent: 25
  parent_origin: created
  attached: [T-01, T-02, T-04, T-05, T-06, T-07, T-08, T-09, T-10, T-11]
  issues:
    T-01: 26
    T-02: 27
    T-04: 28
    T-05: 29
    T-06: 30
    T-07: 31
    T-08: 32
    T-09: 33
    T-10: 34
    T-11: 35
