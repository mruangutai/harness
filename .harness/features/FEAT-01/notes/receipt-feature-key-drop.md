Removed 3 key(s) from FEAT-01's feature.yaml because each had no reader; FEAT-14 closed the key set to eleven. This receipt is the only durable record of their values.

## status collapse (the pre-collapse pair survives only here)

- old status: `abandoned`
- old phase: `None`
- new status: `Done`  (rule)

## value normalization

- `pr`: `'none'` (string) -> `null`

## removed keys, full values

```yaml
cost_usd: 0
max_cost_usd: 40
runs[0].cost_usd: 0
```
