# Ship review — FEAT-02 (VERDICT shadowing fix)

**Bottom line: the fix is built, validated, and green — but the feature budget is
exhausted ($~49 of $40, approximate), so the pm goal-check did not run. Your call:
authorize ~$5 more to run it, or accept qa's evidence as the SC verification and sign
off directly.**

## What shipped (on branch `feat/harness-native-foundation`, not merged)

- `6546b1d` [harness:t-01] — 4 echo-shadowing regression cases (3 CLI + 1 hook),
  proven RED against the pre-fix validator per D-02.
- `d9b16e5` [harness:t-02] — `validate()` tail-anchors on the last `VERDICT:` line
  (D-01). Suite 40/40 green; pre-fix binary fails exactly the T-01 cases.
- review_sha pinned: `d9b16e5323526d6f02228b48652ded84328a40cb`.

## Gates

| Gate | Result |
|---|---|
| Build (eng, run 03) | PASS, 1 cycle (T-01 first attempt hook-blocked on write mechanism, re-routed via Bash path) |
| qa gate (blocking) | PASS — bugfix matrix floor met, suite green, pre-fix proof independently re-run |
| Review panel (code/security/ui, run 04) | Unanimous PASS, severity_max low, must_fix empty; ui self-scoped out (no UI surface) |
| pm goal-check | **NOT RUN — budget bound hit** |

## Success criteria — evidence exists, verdicts unadjudicated

- SC-01 (echo repro rejected/correctly routed): qa verified the pre-fix binary fails
  exactly the 3 defect cases and the fixed validator routes the real return; code-reviewer
  independently reproduced the D-02 proof. Evidence: `.harness/features/FEAT-02/notes/qa-FEAT-02.md`.
- SC-02 (all 36 existing cases pass): suite exits 0, 40/40 (36 existing + 4 new).
  Verified by qa and directly by the orchestrator.

## Residual findings (advisory, none gating)

- F-1: a return whose `DIGEST:` precedes its `VERDICT:` line is now rejected — a
  narrow REQ-02 deviation that fails CLOSED (rejection, never a masked pass). Panel
  assessed non-gating; residual of BUILD task 22 hardening if more validator work lands.
- qa coverage notes beyond the floor: echo-only messages, BLOCKED/ESCALATE behind an
  echo — candidates for a future hardening task, not this one.

## Budget

| | used | max |
|---|---|---|
| cycles | 4 | 10 |
| cost (approx, P-01 attribution) | ~$49 | $40 |

Attribution is per-agent snapshot deltas from project-cumulative `cost-report.py`; the
overrun is real even under the most charitable reading (~$48). Root cause: fable-tier
runs cost ~$20 each vs the ~$2-5 the first two runs were attributed.

## Decisions needed

1. **Fund the pm goal-check (~$5) or waive it** and sign SC-01/SC-02 on qa's evidence.
2. Merge is user-gated either way: branch is ready at `d9b16e5` when you sign.
3. Non-blocking: should `.claude/skills/harness/bin/**` gain an owner in
   team-config.yaml? (eng-lead's Q1 — first build attempt was blocked on it.)
