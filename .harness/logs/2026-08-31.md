
## T-07 — feature.json.status deleted, six readers repointed (e11e850, e575521)

Landed. Both kinds green: unit 493 PASS, integration 793 PASS. SC-02 is **0** for the first time.

**Four of the five readers failed SILENTLY when shown red.** `board_lifecycle`'s STATUS class
reported `0 finding(s)` and exit 0; `check-state` INV-28 warned about nothing and exit 0. Only
`gh-sync`'s terminal exemption was loud, and it is the only one that mutates. This is the whole
argument for the plan's insistence on five per-reader cases: the schema and absence assertions
all go green on a half-applied migration where two gates check zero features.

**The bug the suite caught that no per-task verify would have.** post-merge-sweep.sh runs
`python3 -I`; isolated mode ignores user site-packages, where PyYAML lives.
`worktree_terminal.py` read only JSON before this task, so it had no third-party dependency —
moving the station into plan.yaml gave the sweep one it cannot satisfy. Every worktree came back
`unresolved`, which is a SKIP, so no terminal worktree would ever have been reclaimed. Fixed with
a MissingDependency-only fallback (checked by name) to a column-0 scan of the one key, justified
because that key has exactly one writer. Pinned by five assertions, the load-bearing one being
that an indented task `status:` must never be read as the feature's station — getting that wrong
deletes a live feature's worktree.

**Two intent premises were false and are recorded as deviations.**
1. "A directory with no plan.yaml keeps nothing … no reader needs a station for them."
   `_is_shipped` needs one: ten legacy features stopped being skipped and the tree went 0
   violations/1 plan → 44/9, all legacy noise in long-shipped plans. Added a rule the plan did
   not specify — PLAN.md with no plan.yaml is a pre-format record — justified by measurement: no
   production code in this tree WRITES a PLAN.md. Consequence: a PLAN.md is now never
   route-checked, and the checker refuses a feature holding both files, so this cannot be worked
   around per-directory.
2. "Those features are finished." BUG-1030-stale-anchor-write-hazard was at `review`. Nowhere to
   record its station; inventing one would be fabrication, so it records none.

**Three defects found in passing, all the same class — dead vocabulary still being written.**
`factory_decompose.py` WROTE `"status": "Plan"` into fresh feature.json documents, a key the
schema now refuses. Two more live `or "pending"` defaults in gh-sync.py, after the one T-16
found. SC-02 is what surfaced the first; nothing but a grep would have.

**The quotable-history trap sprang a third time.** Two of SC-02's five hits were my own comments
QUOTING the dead spelling. T-05 and T-06 hit this too. The rule is now explicit in three
docstrings: describe a retired route, never quote it.

**A self-maintaining fixture beat a hand-listed one.** check-state.sh's x.5 case listed the
script's imports to build an isolated bin; adding `factory_config` (7 transitive modules) broke
it for the wrong reason — the third time that list has done this. Replaced with "every .py
except layout_migration.py", which is what the case actually means.

Docs beyond D-14, found by sweeping rather than by the decision: **SPEC.md §11.1 asserted
"Feature status lives ONLY in feature.json"** — the exact inverse of the truth — and
github-mirror.md taught the old capitalised call. Both corrected; the inversion is recorded in
SPEC rather than silently overwritten.

Gate exits 1 on two violations, neither from this code: FEAT-38's inherited missing
handoff-validate.md, and a board-card drift I created by marking T-06 done without moving issue
#1042's card. The second is mine and needs a mirror write.

Plan: T-01..T-07, T-16, T-17 done (9 of 15). Remaining T-08, T-09, T-10, T-11, T-14, T-15.

## T-08, T-09, T-10, T-11, T-14, T-15 — the plan is complete (15/15)

Both kinds green: **unit exit 0, 493 PASS; integration exit 0, 797 PASS.**

**Every task's red was earned, and three of them needed MUTANTS rather than a first run.** A new
file's first red is `rc=127`, which proves nothing about whether the cases discriminate, and two
tasks had cases that passed VACUOUSLY before their code existed. So:

|task|mutant|reds|
|---|---|---|
|T-08|blanket deny / naive substring|7 / 1 — the substring mutant is caught by exactly the case written for it|
|T-10|no commit / no worktree refusal / `git commit -a`|6 / 3 / 1|
|T-11|stop filtering CANNOT RUN|exactly the 3 deleted cases, none of T-04's|
|T-14|no terminal scope / no both-reads clause|(inv32.c) alone / (inv32.d) alone|
|T-09|route the refusal through `deny()`|the ROUTING-absence case alone|
|manifest fix|restore the byte compare|the new case plus the 6 it explains|

**T-11's deletion was justified by measurement, not by the mandate.** The file claimed the three
renamed-board cases had gone vacuous; unfiltering `CANNOT RUN` proved it — they went red and none
of T-04's did. They tested the filter, not the lookup.

**The quotable-history trap sprang a FOURTH time**, in the first draft of T-11's deletion comment
— the comment that exists to stop the next reader restoring the unit. It named the six retired
column words and the deleted helper, both of which that task's own verify greps for.

**T-09's real work was nine INVERTED cases, not the denial.** The approval-guard group asserted
the ALLOW direction of plan.yaml editor writes; T-09 makes that direction unreachable. Case 4 —
"the MAIN SESSION signing is ALLOWED" — was the case the old design turned on. Before accepting
that, I checked creation had another route: `plan-merge.py apply` writes a plan whole when the
base does not exist. Verified live: APPLIED, file created. Nothing legitimate was lost.

**Two verify-line defects found, both reported rather than silently rewritten.**
1. T-10 line 3 compares `st[n] != fc.station_column(w)` — a board read (LOWERCASED by
   `board_stations` at the boundary, per T-02) against a capitalised column name. It puts the
   capitals on the wrong side of a boundary this feature drew, and cannot pass: run verbatim it
   fails with tuples like `(215, 'done', 'done')`. Corrected comparison run instead, exit 0.
2. T-14's four `grep -q "ok - case (inv32.a) …"` lines cannot match under the `grep` on PATH here
   (`pi-uu-grep 0.2.0`), which reads the pattern as ERE — so `(inv32.a)` is a capture group and
   the literal parens never match. `-F` and escaped-ERE both exit 0. Environment-dependent, the
   same family of hazard T-14's own intent warns about for `grep -oE`.

**T-10's board pass measurement differed from the plan's and I reported it before writing.** Plan
at 8f8a6a3: 656 cards, ZERO writes, ONE skip. Mine: 796 items, 314 compared, 2 writes, 8 skips.
The 8 are all feature PARENT cards of shipped features, and the growth from 1 is a direct
consequence of T-07 — at 8f8a6a3 those seven plans carried no top-level station so `project`
placed no parent. Same gap, wider view. None added; the exemption holds.

**A production regression in T-14's first cut, caught by its own fixtures:** `git rev-parse
--show-toplevel` returns the resolved path while `abspath` does not, and every fixture's temp dir
is a symlink on macOS. `relpath` across the two made `git show` fail and the invariant go SILENT.
Same mismatch T-07 hit in worktree_terminal.

**The manifest byte-compare finding is the one worth carrying forward.** T-09's intent REQUIRED
re-wording two comments in team-config.yaml. `check-plan-routes.py` compared the branch and owner
manifests byte-for-byte, so a comment change read as a route DEVIATION and reddened six cases
that run fixture plans against the live checkout. Parsed content was identical. Now compared
parsed, with the byte compare kept as a fast path and an unparseable branch manifest still
deviating — it cannot be shown to agree, which is the whole job.

**Gate: 2 violations, both expected and neither from this code.** FEAT-38's inherited missing
handoff-validate.md, and FEAT-41's own knowingly-stale `review_sha` — reported by INV-32, the
invariant T-14 just built, on the feature that built it. T-14's intent forbids touching a real
`review_sha`; the orchestrator re-pins. INV-26: **zero lines**.

**The feature station is `building`, deliberately, not `review`.** Setting `review` claims the
build→validate seam was crossed, and INV-17 then demands `notes/handoff-build.md`. The
main-session-direct exemption does not apply because T-15 is declared `team` (14 of 15 tasks are
main-session-direct). No handoff exists, so I did not write one — fabricating a seam record is
the one thing worse than an unset station. Build-complete, awaiting the validator panel.
