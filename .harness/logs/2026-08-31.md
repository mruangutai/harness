
## T-07 — feature.json.status deleted, six readers repointed (e11e850, e575521)

Landed. Both kinds green: unit 493 PASS, integration 793 PASS. SC-02 is **0** for the first time.

**Four of the five readers failed SILENTLY when shown red.** `board_lifecycle`'s STATUS class
reported `0 finding(s)` and exit 0; `check-state` INV-28 warned about nothing and exit 0. Only
`gh-sync`'s terminal exemption was loud, and it is the only one that mutates. This is the whole
argument for the plan's insistence on five per-reader cases: the schema and absence assertions
all go green on a half-applied migration where two gates check zero features.

**The bug the suite caught that no per-task verify would have.** post-merge-sweep.py runs
`python3 -I`; isolated mode ignores user site-packages, where PyYAML lives.
`worktree_terminal.py` read only JSON before this task, so it had no third-party dependency —
moving the station into plan.yaml gave the sweep one it cannot satisfy. Every worktree came back
`unresolved`, which is a SKIP, so no terminal worktree would ever have been reclaimed. Fixed with
a MissingDependency-only fallback (checked by name) to a column-0 scan of the one key, justified
because that key has exactly one writer. Pinned by five assertions, the load-bearing one being
that an indented task `status:` must never be read as the feature's station — getting that wrong
deletes a live feature's worktree.

**Two intent premises were false and are recorded as deviations.**
1. "A directory with no plan.yaml keeps nothing … no reader needs a station for them."
   `_is_shipped` needs one: ten legacy features stopped being skipped and the tree went 0
   violations/1 plan → 44/9, all legacy noise in long-shipped plans. Added a rule the plan did
   not specify — PLAN.md with no plan.yaml is a pre-format record — justified by measurement: no
   production code in this tree WRITES a PLAN.md. Consequence: a PLAN.md is now never
   route-checked, and the checker refuses a feature holding both files, so this cannot be worked
   around per-directory.
2. "Those features are finished." BUG-1030-stale-anchor-write-hazard was at `review`. Nowhere to
   record its station; inventing one would be fabrication, so it records none.

**Three defects found in passing, all the same class — dead vocabulary still being written.**
`factory_decompose.py` WROTE `"status": "Plan"` into fresh feature.json documents, a key the
schema now refuses. Two more live `or "pending"` defaults in gh-sync.py, after the one T-16
found. SC-02 is what surfaced the first; nothing but a grep would have.

**The quotable-history trap sprang a third time.** Two of SC-02's five hits were my own comments
QUOTING the dead spelling. T-05 and T-06 hit this too. The rule is now explicit in three
docstrings: describe a retired route, never quote it.

**A self-maintaining fixture beat a hand-listed one.** check-state.py's x.5 case listed the
script's imports to build an isolated bin; adding `factory_config` (7 transitive modules) broke
it for the wrong reason — the third time that list has done this. Replaced with "every .py
except layout_migration.py", which is what the case actually means.

Docs beyond D-14, found by sweeping rather than by the decision: **SPEC.md §11.1 asserted
"Feature status lives ONLY in feature.json"** — the exact inverse of the truth — and
github-mirror.md taught the old capitalised call. Both corrected; the inversion is recorded in
SPEC rather than silently overwritten.

Gate exits 1 on two violations, neither from this code: FEAT-38's inherited missing
handoff-validate.md, and a board-card drift I created by marking T-06 done without moving issue
#1042's card. The second is mine and needs a mirror write.

Plan: T-01..T-07, T-16, T-17 done (9 of 15). Remaining T-08, T-09, T-10, T-11, T-14, T-15.

## T-08, T-09, T-10, T-11, T-14, T-15 — the plan is complete (15/15)

Both kinds green: **unit exit 0, 493 PASS; integration exit 0, 797 PASS.**

**Every task's red was earned, and three of them needed MUTANTS rather than a first run.** A new
file's first red is `rc=127`, which proves nothing about whether the cases discriminate, and two
tasks had cases that passed VACUOUSLY before their code existed. So:

|task|mutant|reds|
|---|---|---|
|T-08|blanket deny / naive substring|7 / 1 — the substring mutant is caught by exactly the case written for it|
|T-10|no commit / no worktree refusal / `git commit -a`|6 / 3 / 1|
|T-11|stop filtering CANNOT RUN|exactly the 3 deleted cases, none of T-04's|
|T-14|no terminal scope / no both-reads clause|(inv32.c) alone / (inv32.d) alone|
|T-09|route the refusal through `deny()`|the ROUTING-absence case alone|
|manifest fix|restore the byte compare|the new case plus the 6 it explains|

**T-11's deletion was justified by measurement, not by the mandate.** The file claimed the three
renamed-board cases had gone vacuous; unfiltering `CANNOT RUN` proved it — they went red and none
of T-04's did. They tested the filter, not the lookup.

**The quotable-history trap sprang a FOURTH time**, in the first draft of T-11's deletion comment
— the comment that exists to stop the next reader restoring the unit. It named the six retired
column words and the deleted helper, both of which that task's own verify greps for.

**T-09's real work was nine INVERTED cases, not the denial.** The approval-guard group asserted
the ALLOW direction of plan.yaml editor writes; T-09 makes that direction unreachable. Case 4 —
"the MAIN SESSION signing is ALLOWED" — was the case the old design turned on. Before accepting
that, I checked creation had another route: `plan-merge.py apply` writes a plan whole when the
base does not exist. Verified live: APPLIED, file created. Nothing legitimate was lost.

**Two verify-line defects found, both reported rather than silently rewritten.**
1. T-10 line 3 compares `st[n] != fc.station_column(w)` — a board read (LOWERCASED by
   `board_stations` at the boundary, per T-02) against a capitalised column name. It puts the
   capitals on the wrong side of a boundary this feature drew, and cannot pass: run verbatim it
   fails with tuples like `(215, 'done', 'done')`. Corrected comparison run instead, exit 0.
2. T-14's four `grep -q "ok - case (inv32.a) …"` lines cannot match under the `grep` on PATH here
   (`pi-uu-grep 0.2.0`), which reads the pattern as ERE — so `(inv32.a)` is a capture group and
   the literal parens never match. `-F` and escaped-ERE both exit 0. Environment-dependent, the
   same family of hazard T-14's own intent warns about for `grep -oE`.

**T-10's board pass measurement differed from the plan's and I reported it before writing.** Plan
at 8f8a6a3: 656 cards, ZERO writes, ONE skip. Mine: 796 items, 314 compared, 2 writes, 8 skips.
The 8 are all feature PARENT cards of shipped features, and the growth from 1 is a direct
consequence of T-07 — at 8f8a6a3 those seven plans carried no top-level station so `project`
placed no parent. Same gap, wider view. None added; the exemption holds.

**A production regression in T-14's first cut, caught by its own fixtures:** `git rev-parse
--show-toplevel` returns the resolved path while `abspath` does not, and every fixture's temp dir
is a symlink on macOS. `relpath` across the two made `git show` fail and the invariant go SILENT.
Same mismatch T-07 hit in worktree_terminal.

**The manifest byte-compare finding is the one worth carrying forward.** T-09's intent REQUIRED
re-wording two comments in team-config.yaml. `check-plan-routes.py` compared the branch and owner
manifests byte-for-byte, so a comment change read as a route DEVIATION and reddened six cases
that run fixture plans against the live checkout. Parsed content was identical. Now compared
parsed, with the byte compare kept as a fast path and an unparseable branch manifest still
deviating — it cannot be shown to agree, which is the whole job.

**Gate: 2 violations, both expected and neither from this code.** FEAT-38's inherited missing
handoff-validate.md, and FEAT-41's own knowingly-stale `review_sha` — reported by INV-32, the
invariant T-14 just built, on the feature that built it. T-14's intent forbids touching a real
`review_sha`; the orchestrator re-pins. INV-26: **zero lines**.

**The feature station is `building`, deliberately, not `review`.** Setting `review` claims the
build→validate seam was crossed, and INV-17 then demands `notes/handoff-build.md`. The
main-session-direct exemption does not apply because T-15 is declared `team` (14 of 15 tasks are
main-session-direct). No handoff exists, so I did not write one — fabricating a seam record is
the one thing worse than an unset station. Build-complete, awaiting the validator panel.

## Main session — merges, gate fixes and the FEAT-46 split

# FEAT-41, 2026-08-31

- T-05 committed a0650c0. My own history sentence tripped the verify ("prescribe a surgical `Edit`") - a note about a forbidden route must not be quotable AS that route. harness/SKILL.md named NO write route at all (0 occurrences of set-task-station); worse than a wrong one because there is nothing to notice.
- REVIEWED an incoming finding about #1035 colliding with T-07. Its conclusion (rebase) was right; most of its reasoning was not. STATUS_VALUES, _feature_status, _record_status, cmd_status and the schema enum ALL pre-existed in the worktree and are already named by T-06/T-07 - "none of which existed when T-07 was specified" was false. "eleven readers is stale" was false: zero new readers, and the 3 extra grep hits are false positives (plan approval status, plan task status, harness.json test_kinds). "9 behind" was LOCAL main vs origin/main; the worktree was 148 behind.
- The finding's real content, which it did not identify: #1035 introduced feature_json_write, and T-07's migration would hand-write feature.json - reintroducing the exact stale-anchor bug #1035 closed. Confirmed the shape gate does NOT deny raw feature.json writes, so it would not have been refused. -> D-13.
- Rebased onto origin/main (148 commits, 6 conflicts). TWO conflicts had main CORRECTING a citation DEC-192 -> DEC-203 inside text my side rewrites; a side-pick either way silently reverts a real correction. Combined.
- case_27b runs a PINNED HISTORICAL validator from a base64 fixture; T-04's change to the SHARED _yaml_project default reddened it. A fixture feeding a frozen tool must sit in the INTERSECTION of both vocabularies.
- FEAT-43's code-grade.py now grades this tree. main() was a FLAT LIST of case calls: grade 3 at 18 entries, below floor at 19, and the 19th was a one-line registration. Registration is now DATA (CASES tuple): ABC 26.2 -> 6.4. A list that costs a grade point per element is the wrong shape for a registry.
- MY OWN TOOL CORRUPTED THE SIGNED PLAN. plan-merge.py apply spliced a column-0 proposal item into a two-space list, printed APPLIED, exited 0, left 1541 lines that PyYAML could not load. Two fixes: uniform re-indent of the ADDITION only, and STEP 9 - parse the merged result before writing, checking ids not just syntax. Sabotage-proved the guard independently.
- GAP: the plan format has no amendment verb. apply is add-only (exit 7 on a changed value) and nothing rewrites a task's intent, so an approved amendment to existing task text can only land as a new decision id that governs it.
- INHERITED: check-state.py is red on PRISTINE origin/main - FEAT-38 Done with no notes/handoff-validate.md. Not mine (0 commits); operator routing it to another session. Not fabricating a handoff for a seam I did not cross.
- T-06 PARTIAL, committed a8d0693. gh_board.project done (44 assertions), check-state.py done (154 ok, INV-26 ran LIVE against board 3, zero violations), gh-sync.py code done and the set_station count is exactly 4. NOT DONE: T-06's verify requires test-gh-sync.py green.
- THREE sites deliberately do NOT consult project, same reasoning each time - they write a PHASE/TERMINAL fact project cannot express because it reads per-task statuses: ship's done pass (DEC-203 sole writer; measured all 31 shipped features have no top-level station so project would say review), _to_backlog (D-05 places no terminal card; probe #860 measured the post-close write), cmd_status Review (D-23 parks all cards; T-06 KEEPS the widening that tolerates exactly this).
- MY OWN COMMENT TRIPPED THE VERIFY AGAIN - named the dead identifiers while explaining their absence. Second time in two tasks. A note about a dead thing must not quote it.
- T-04 MISSED FIXTURES that build plan text via a helper KEYWORD (second_status="pending"). Neither the four-space plan.yaml grep nor the check-state.py grep could reach a keyword argument.
- **THE FINDING: the INTEGRATION suite has been red since T-01/T-02, four commits.** Measured at f1020e8 (pre-feature): test-gh-sync 0 FAILs, test-factory-integration 0 FAILs. At HEAD: 123 FAILs. 83 of test-gh-sync's predate today's edit (proved by stashing: 83 without, 84 with).
- WHY IT WAS MISSED: every per-task verify names specific test FILES and neither of these two is named by ANY task in the plan; and I ran `--kind unit` all session while calling it the full suite. Both files are INTEGRATION kind.
- SECOND verification shortcut to report green over red this session (first: grep -c "^FAIL" on a crashed suite). Same pattern - trusting a narrower signal than the one that answers the question.
- Cause of the bulk: fixtures still declare github.board.stations as a MAPPING (T-01 made it an error) and build capitalised CLI args (T-02 made illegal). Fixture debt from my own T-01/T-02.
- NEXT: pay the fixture debt, run BOTH kinds, finish T-06's verify. Do not start T-07 first.
- Filed #1052 for the cause-side verify-naming gap, with the measurement: a one-line grep for the symbol T-01 changed finds 10 consumer test files; T-01/T-02's verifies named 6; both red files are in the missed 4. Explicitly NOT "run the whole suite per task" - it changes which files a verify NAMES, not how many run (21s at T-01, not 167s). Enforcement layer, DEC-174, own brief, deliberately not bolted onto FEAT-41.
- Operator ruling: the two discussions are separated. FEAT-41 continues at T-16; #1052 is out of scope for it.
- T-16 done a9085c0: 84 -> 0. Two builders = 74 of them. write_plan_yaml emitted JSON - valid YAML so every READER accepted it; invisible until a tool WROTE the file, because plan-merge's verbs splice text and need a `- id: T-NN` LINE. Found two real code defects via fixtures: a live `or "pending"` in gh-sync cmd_status (T-04 missed it - the #1052 pattern), and T-06 letting project's FleetError traceback through main().
- T-17 done: 37 -> 0, 36 from default_board() alone. The last failure was the cleanest boundary illustration of the feature: board_lifecycle's STATUS finding prints "records status 'Done' ... but its parent #950 reads 'backlog'" - feature.json's recorded value (capitalised, T-07's job) and the BOARD's answer (lowercased at the read boundary) in ONE sentence.
- BOTH KINDS GREEN for the first time since T-01: unit 485 PASS, integration 779 PASS.
- T-06 done 4ea3f27: all seven verify lines pass. set_station count exactly 4.
- 8 of 15 tasks done. Remaining: T-07 (largest - eleven readers + feature.json migration, and D-13 applies to it), T-08, T-09, T-10, T-11, T-14, T-15.
- Discipline that stuck: both kinds after every task. It is what caught T-17's last assertion and would have caught T-01's damage on the day.
- 21:00 BUG-1071 · MERGED · PR #1076 at 9f2a070, integration pass 2m28s · INV-32 gets a per-project era guard, so the invariant can be satisfied. Was: exit 1 with 32 violations on every run, all 32 approved plans failing, 0 able to pass, FEAT-45's own plan among them — /harness-plan and /harness-ship blocked at step 0. Now: exit 0, 0 violations, 32 pre-era notes. Boundary is harness.json `panel_era_start` (per-project, not a literal — check-state.py is copied into every onboarded project); schema_version 1->2; migration rides upgrade-config.py's existing additive merge. Two panel cycles, both PASS, no must_fix; both MED findings closed rather than deferred. 53 lines (5e81c66) landed after cycle 1's pin and were never panel-reviewed — the panel's own requested changes, tested but unreviewed; disclosed in the PR body. Execution main-session-direct under DEC-174. Filed not fixed: #1072 shipped templates/team-config.yaml does not parse; #1073 nothing forces a migrating project to choose a boundary; #1074 validator-lead's returned and written digests can disagree.
- 21:20 FEAT-46 · plan SPAWNED · a standard for what constitutes a decision · worktree .claude/worktrees/harness/FEAT-46-decision-standard, cut from main at 9f2a070. Step-zero clarity satisfied by the existing grilling artifact (.harness/notes/grilling-decision-standard-2026-08-30.md, 21 rulings, frontier empty) — cited, not re-grilled, per /harness-plan. No source ticket named, so KICKOFF writes nothing. Three inputs re-derived at spawn: successor range DEC-208..DEC-371 (FEAT-45 consumed 206/207), citation-precision baseline re-pinned 3331559 -> 9f2a070, and DEC-206/DEC-207 added to the rewrite set — both are Chose-only with three non-clause prose blocks and no Because, so they fail the standard they would be measured by.
