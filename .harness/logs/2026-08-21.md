# 2026-08-21

- **merged** PR #658 (`d065b3b`) — a feature's `notes/` is exempt from the all-YAML-parses corpus
  test. Unblocked FEAT-31's T-02/T-06/T-07/T-13, whose verify blocks needed a green unit suite.
- **synced** FEAT-31's worktree to `main` (was 2 behind) and FEAT-32's (was 4 behind). FEAT-32's
  `plan.yaml` verified byte-identical across the merge.
- **FEAT-31 plan-amend returned** — 18 tasks at `33d894e`, `approval:` untouched. Operator
  re-affirmed the signature at 18 tasks; record in `notes/signature-reaffirmed-18-tasks.md`.
  T-15..T-18 mirrored as issues #659, #660, #661, #662 under parent #598.
- **moved** FEAT-31's worktree onto the segment layout and rewrote its 43 path citations
  (`fbee81f`). The `behind` gate had exited 3 for the one feature that motivated it.
- **spawn** FEAT-31 · ship · orchestrator, mission ship. Budgets at entry: cycles 3/10, runs 7/20.
  Six tasks are main-session-direct and mine: T-04, T-10, T-12, T-14, T-15, T-17.
