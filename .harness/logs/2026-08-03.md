# 2026-08-03

- 04:34 FEAT-05-pyyaml-file-parsers returned awaiting_user. Plan phase COMPLETE — 3 runs, 1 rework cycle, $92 of $120. BRIEF 7 REQ/13 SC, PLAN 17 tasks/13 decisions.
- 04:xx USER SIGNED both artifacts (one bundled signature, main session wrote both `## Approval` blocks — Q5 discharged). Rulings: Q1/Q2/Q3 amendments accepted (cost-report.py STAYS in scope though it parses no YAML — it still hand-rolls YAML manipulation; SC-03 judged against the census not the parenthetical; SC-02 re-baselined at build open). Q7 ROUTING WALL 3rd recurrence — T-10/T-11 stay MAIN-SESSION, team-config.yaml NOT widened. Budget HELD at 120, overrun accepted. Answers at notes/answers-2026-08-02-03-product.md. check-state back to exit 0 once signed.
- 04:xx PLANNING CORRECTED TWO OF MY FACTS, both were right: (1) check-domain.sh is 80.63ms on the governed path, NOT the 23.7ms I recorded — my `{}` payload had no agent_type and returned early, never exercising the hook. (2) cost-report.py parses no YAML; it line-scan-replaces the cost: block (:189). Grilling artifact corrected on both, plus the stale DEC-172 same-ship claim planning also flagged.
- 04:xx Q6 HARNESS DEFECT confirmed and it hits DEC-173: the SubagentStop hook runs ${CLAUDE_PROJECT_DIR}/.../validate-digest.py, which in a worktree is the MAIN CHECKOUT's copy. Verified main=0 GATE_FIELDS, worktree=2. Every digest produced in a worktree is judged by the main checkout's rules, so DEC-173 is NOT in force for agents until this branch merges. Written into PLAN's approval note so the build does not misdiagnose it.
- 05:xx COMMITTED the review baseline in two commits (user chose the split). f6c814b docs/decisions/FEAT-05 signature + issue #5; 225cc98 the DIGEST schema implementation. All four gates green at HEAD, tree clean. Reading the diff before committing caught a REAL defect in my own CLAUDE.md edit: it still said "scripts degrade loudly when it is absent" — the ORIGINAL DEC-171 wording the user had already reversed. Fixed pre-commit; swept CLAUDE.md, the index and the grilling artifact for other stale assertions of the rejected fallback, none found.
- 05:xx spawn harness-orchestrator mission=ship FEAT-05. review_sha MUST pin 225cc98 (INV-6/DEC-50) — the plan was written against an uncommitted tree; 37a8a66 contains none of this work.
- 06:xx T-01 step 1 EXECUTED by main session (the signed PLAN routes it here; my ship briefing had listed only T-10/T-11 — the orchestrator was right to correct me). Ran D-07 line 1 verbatim: refused with the literal `externally-managed-environment` text, so the gate condition fires as designed. Then installed WITH `--user` added — PLAN Amendment 1 records the deviation and why (Homebrew's own PEP 668 message recommends --user; D-07 omitting it was a defect). PyYAML 6.0.3 at ~/Library/Python/3.14, outside the Homebrew tree. Verified: heredoc form OK, scrubbed env OK, all four python3 on PATH now import yaml — only homebrew 3.14 was ever missing it. USER'S STATED GOAL was one install serving all projects, NOT repo-local; a vendored design was raised and rejected (it would have voided REQ-03/05 and SC-07/08/09). FOUND: PYTHONNOUSERSITE=1 defeats a user-site install — both a hook hazard and the clean SC-08/SC-09 absence-simulation mechanism the BRIEF said was still needed.
- 07:xx DECISIONS AUDIT (user-directed). Report `.harness/notes/audit-decisions-conflicts-2026-08-03.md`, reproducer `.harness/notes/audit-decisions.py`. 173 decisions / 173 index rows, no duplicate numbers, no dangling DEC- refs, index complete both directions. CONFIRMED (1): DEC-145 partially superseded DEC-24/25/66/67/68 and none of the five index rows carries a marker, so an agent grepping `expertise` reads dead mid-run timing as live — authoring gap, invisible to check-docs. CONFIRMED (2): four amendments (DEC-137 @3327, DEC-138 @4276/@4303/@4331) sit outside their parent's section, misattributing refs and @anchor — mechanism PROVEN today when my own DEC-142 amendment silently donated refs to DEC-173 and reverted on relocation. FRAGMENTATION: the DIGEST contract is 9 simultaneous live decisions (29/121/122/123/126/127/156/172/173) with no single entry stating it — the one real consolidation candidate; supersession chains like check-domain.sh 19->84->85 are history, not fragmentation. REJECTED two of my own signals and recorded them: DEC-62->DEC-35 was a regex misparse, and tag-cohesion (approval 19 live/0 cross-refs) over-flagged an incidentally-broad tag.
- 07:xx MAJOR FIND, out of the build's own RED gate: FOUR harness YAML files DO NOT PARSE under PyYAML — team-config.yaml:18 (` ##` inside a flow sequence never closes the bracket), FEAT-03:97 (backtick, a reserved indicator), FEAT-04:77 and FEAT-05:55 (`: ` in prose read as a mapping key). FEAT-01/02 and harness.json are fine. ONE root cause: unquoted prose in plain scalars; the line-scanners tolerated it for the project's whole life. FEAT-05's own feature.yaml was written TODAY, so this is live production of invalid YAML, not debt. Consequence: SC-03 is unsatisfiable against today's corpus — a converted check-domain.sh/bash-write-guard.sh would fail closed on every write immediately. Relayed to the orchestrator with ownership split (team-config.yaml is main-session; FEAT-05's is the orchestrator's; FEAT-03/04 are shipped-state scope questions) and an explicit instruction that no member may loosen the helper to accept it. team-config.yaml NOT yet edited by me — the hooks line-scan that exact line, so changing it mid-build could wedge every agent write; awaiting the user.
- 14:xx USER RULED 2 of 4. (1) The orchestrator's FEAT-03/FEAT-04 feature.yaml repair STANDS — verified independently: all five parse, top-level keys identical both files; recorded the honest limit that a data-level equality proof is impossible since the pre-repair files cannot parse, so the run-inventory zero-diff is the load-bearing evidence. (2) Validity gate APPROVED — wrote BRIEF Amendment 1 adding REQ-08 and SC-14 (unit test walking .harness/**/*.yaml, must be in run-unit-tests.sh SCRIPTS or it gates nothing per issue #5, and must be shown RED against a malformed fixture first). PreToolUse hook form explicitly rejected as over-engineering.
- 14:xx STILL PENDING, both gate spend: team-config.yaml:18 (candidate verified safe on my side — neither hook reads main_session, line-scan identical, hook suites 11/11 and 30/30, and the fix recovers ALL NINE top-level keys since today the doc dies at line 23) and Q3/T-12/T-14. Orchestrator told to HOLD and stay idle so it gets one unblock, not two partial ones. Q4 accepted as a verify-method defect: `RESOLVED VIA: mechanism-unknown` satisfies the grep while the criterion is unmet.
- 14:xx CORRECTED MY OWN EARLIER OVER-CLAIM to the user: I said DEC-173 "is not in force until merge" on the basis that the two validate-digest.py copies differ. That proves the files differ, NOT which one a hook executes. It is the same open question as Q3. Told the user my statement was firmer than the evidence.
- 15:xx team-config.yaml:18 FIXED by main session (its owner) at the user's instruction — the orchestrator's verbatim candidate, 1 line, quoted each glob so the ` ##` no longer opens a comment inside the flow sequence. Verified: 9 top-level keys reachable (was dying at line 23, making everything from `orchestrator:` onward invisible to a real parser), `## Approval` suffixes intact, test-check-domain 11/11 and test-bash-write-guard 30/30 unchanged from baseline, all 10 .harness/**/*.yaml now parse, run-unit-tests.sh now exits 0 (was 1 — the single failing test was test-harness-yaml.py and this was its only cause, exactly as the orchestrator predicted). ALL FOUR GATES GREEN.
- 15:xx Q3 HALF-RETIRED BY ANALYSIS, no probe needed. check-domain.sh:60-71 — the settings.json command path IS ${CLAUDE_PROJECT_DIR}/..., and `root` resolves from the same variable, falling back to `_derived` which is computed from BASH_SOURCE. So the script and the root always come from the SAME repo; the mixed case (worktree script + main-checkout root writing .pyyaml-bootstrap outside T-10's gitignore) CANNOT OCCUR. T-10 is not broken and the deadlock it prevents stays prevented. What remains is only WHICH value CLAUDE_PROJECT_DIR takes — i.e. whether SC-05/06/08 are exercisable pre-merge — which is all-or-nothing and therefore loud, not silent.
- 15:xx USER RULED: self-hosting stops at the enforcement layer. Recorded as DEC-174 + a CLAUDE.md constraint (plus the "run everything from the worktree" rule, which had never been stated despite being load-bearing for harness dev). FEAT-05's orchestrator STOOD DOWN with its work kept and cited — T-02's RED gate is what found the invalid corpus, D-03's equivalence proof (19 names, 0 mismatches) is the basis for the remaining hook conversions. Corrected its STATE.md over-claim that the SubagentStop validator "resolves the main checkout's" validate-digest.py: only the file DIFFERENCE is verified, not which executes — same open variable as B3, and I had made the identical over-claim to the user earlier.
- 16:xx T-07/T-08 DONE, ISSUE #11 CLOSED IN CODE. check-state.sh now parses feature.yaml with harness_yaml.load_file (PYTHONPATH resolved from the script's own dir, never cwd). Proven old-vs-new on the #11 shape: OLD 0 runs, NEW 1 run. T-08's typed-value hazard confirmed REAL, not theoretical — `cycles_used: 6` is an int under safe_load and the pre-existing `cu.isdigit()` raises AttributeError; every parsed consumer is now str()-coerced at the boundary. An unparseable feature.yaml is a VIOLATION, not a silent skip. Baseline preserved exactly: exit 0, 0 violations, 39 notes.
- 16:xx SC-14/REQ-08 DONE. test-harness-yaml-corpus.py registered in run-unit-tests.sh SCRIPTS (the runner's own drift detector enforces this — MISCONFIGURED exit 2 otherwise, issue #5's failure mode caught by construction). 8 checks: the real corpus, a non-vacuity check, FOUR negative fixtures reproducing today's defect classes, a must-not-cry-wolf case, and an assertion that findings carry line:column. Proven RED by reintroducing the real team-config.yaml defect, GREEN on restore. NOTE: I hardcoded "7/7" in the summary while 8 checks ran — issue #5's exact frozen-constant defect, reproduced by me and caught before commit; now counted.
- 16:xx Two regression cases added to test-check-state.py, both proven RED against the pre-fix script via CHECK_STATE_BIN. Case (e) asserts through the INVARIANT not the parser: fixture has a validator run and no review_sha, so a correct parse MUST report INV-6; pre-fix the run vanished and check-state said nothing. Committed 60b266c. All gates green, 11 suites.
- 17:xx T-04, T-06 DONE (35b7b9f). upgrade-config.py: yaml_names matched the literal text `name:` at any indent — demonstrated harvesting 'not-an-agent' out of a folded block scalar; yaml_version's `(\d+)` read `schema_version: "2"` as ABSENT, so a project that quoted it got "no schema gap" and the upgrade silently did nothing. gh-sync.py load_recorded: four single-format defects, each demonstrated old-vs-new — hardcoded 4-space issue indent, bare-digits-only parent, inline-only `attached`, and an indentation-sliced github: block where a column-0 comment yielded parent=7 while SILENTLY LOSING milestone=9 (partial success is the worst failure mode of the four). save_recorded deliberately NOT converted: safe_dump drops comments. ISSUE #12 CANNOT REPRODUCE — mechanical scan pairing every check() label against the subcommand its run() invoked finds 0 mismatches, sub-issue tests match gh_issues.py:22's current form, and the file has not changed since the issue was filed. Recommend closing as not-a-defect.
- 17:xx T-10, T-11 DONE. Bootstrap marker gitignored in BOTH files + proven behaviourally (touch -> no git-status entry, not just a grep). harness-init gained the 7th prerequisite as a CHECK not an install, quoting INSTALL_COMMAND verbatim — asserted mechanically that every line matches the module (D-07 single source). All 5 "six" occurrences updated, grep -ciw six now 0. Also corrected a statement DEC-171 made false in that SKILL ("these scripts carry no YAML library by design"). Under DEC-174 these were always main-session steps; doing them directly is now the sanctioned path rather than a routing-wall workaround.
- 18:xx PROBE RUN, Q3 MEASURED AND MY OWN CLAIM DISPROVEN. One additive line in the WORKTREE's check-domain.sh, one throwaway general-purpose agent write, reverted byte-identical to HEAD (git diff --quiet) with 11/11 and 30/30 re-confirmed. 11 fires, ALL IDENTICAL: BASH_SOURCE, CLAUDE_PROJECT_DIR and pwd all = the worktree. So the worktree's hook copies EXECUTE and root IS the worktree. Consequences: (1) DEC-173 IS IN FORCE — I told the user twice it was not, first as an assertion then retracted as unproven; it is now positively disproven, and PLAN.md Amendment 2 + the answers file are corrected. (2) The 13 fenced templates ARE live. (3) SC-05/06/08 are exercisable PRE-MERGE — no verify-after-merge needed. (4) T-10's gitignore covers the right repo. (5) Q4's proposed explanation is ELIMINATED (root is the worktree, which HAS the FEAT-05 dir, so the append had a writable target) but Q4 itself is still open with an unidentified cause. ROOT CAUSE OF MY ERROR: I verified the two files DIFFER and then asserted which one RUNS. Different claims. A file-difference check cannot answer a resolution question, and the probe that could took five minutes.
- 18:xx INCIDENTAL, not chased: 11 hook fires for one reported write attempt. Doesn't affect any conclusion (all 11 agree) but at 80.63ms governed-path latency an 11x multiplier is ~0.9s per tool call. Recorded so it isn't rediscovered.
- 19:xx T-12 DONE. check-domain.sh parses the manifest via harness_yaml.manifest_domains; census 10 -> 7 regexes, the 7 survivors all path-routing + glob_to_re's compile, none parse YAML. NEW: malformed manifest FAILS CLOSED (user ruling) — distinct from DEC-101's absent-manifest fail-open, which is asserted by its own test so the new paths cannot swallow it. No deadlock: zero agents hold team-config.yaml in their domain and the main session is exempt at :48, so the only party who can fix the rulebook is the one the guard never governs. Also blocks a duplicate key in the manifest, and a state.yaml that is not valid YAML (the old regex found no keys in a malformed file and reported nothing wrong). D-02 honoured: DEC-156 denial survives, now loader-raised, which also widens it to NESTED duplicates the column-0 regex could not see. 9 new cases, 5-RED against the pre-change hook via CHECK_DOMAIN_BIN, 9/9 green after. CAUGHT ONE OF MY OWN VACUOUS TESTS: "absent manifest fails open" first passed for the wrong reason (exit 0 came from "outside repo", not the absent-manifest branch, because root falls back to _derived) — now uses an isolated copy of the hook, and the trap is documented in the test comment.
- 20:xx T-13 DONE. check-domain.sh: 4 python3 launches -> 1. TIMING 80.63ms (original skimmer) -> 104.7ms (T-12 parser) -> 43.5ms (T-13 merged): 46% FASTER than the pre-feature original while doing strictly more work. At ~11 hook fires per tool call that is ~0.67s removed per call. Built by TRANSFORMING the file programmatically, not retyping — T-13 requires byte-identical stderr and retyping 350 lines is how a message drifts. Equivalence proven two ways: 20/20 unchanged test cases, and 11/11 payload shapes identical in exit code AND stderr bytes against a pre-T-13 copy. ONE REAL REGRESSION CAUGHT: the first draft imported harness_yaml at the top, but the 4-launch version reached DEC-101's fail-open in BASH before any interpreter needed the module — so a hook with a missing module crashed (exit 1) instead of printing "enforcement OFF". Import is now lazy, after the manifest check. Caught by the isolated-copy test I had rewritten an hour earlier for being VACUOUS; had I left it as written this would have shipped green.
- 20:xx Q4/B4 RESOLVED BY MEASUREMENT. Probe (same technique as Q3), 21 genuine hook fires, reverted byte-identical to HEAD with 9/9 re-confirmed. Payload carries ONLY agent_type/tool_input/tool_name — no session_id, no transcript_path — so _resolve_identity's first two entries are dead in this environment. But CLAUDE_CODE_SESSION_ID IS PRESENT at 36 chars in the hook subprocess: the chain's third entry, verified end-to-end against the module (resolves with the env var, returns None without it). THE BOOTSTRAP ESCAPE IS IMPLEMENTABLE AS DESIGNED — SC-08 and REQ-05 need no redesign, and T-09's "extra question" resolves with it (the env var is read from os.environ, so the :97 call site needs no HOOK_PAYLOAD addition). ALSO CORRECTS T-09's receipt, which concluded "most likely the main checkout's copy runs" while honestly flagging an alternative it could not rule out — the alternative was correct, as both of today's probes fired from the worktree copy. Its verify-method defect stands as a finding regardless. Incidental: 21 fires for one write (Q3 saw 11) — multiplier is not constant, not chased.
