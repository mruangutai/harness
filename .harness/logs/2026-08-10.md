# 2026-08-10

- **FEAT-10 SHIPPED.** Operator decision, after the live end-to-end verification against the real
  GitHub API passed on both halves of A1. `feature.yaml` status `awaiting_user` -> `complete`.
  12/12 tasks, 20/20 criteria, cycles 12/12 with zero headroom.
- Evidence: `features/FEAT-10-software-factory/notes/live-verification-a1-2026-08-10.md`.
- Nothing pushed, no PR opened. Both remain the operator's call. Local `main` and
  `feat/FEAT-10-software-factory` are ahead of `origin/main` (eafc8ad).
- Filed **#211** (P0, Ready): `project_field_set` costs 104 GraphQL points per issue moved, uncached,
  inside the per-task loop. A single-field GraphQL query returns the same three ids at cost 1.
  Measured on board 3: field-list 102 (unchanged by `--limit`), project view 2. Blocked on FEAT-10.
- Commented on **#209**: live evidence confirms the self-ownership short-circuit and bounds severity
  to a missing repair path. The failed-station-set scenario was never exercised — not coverage.
- Scheduled a one-shot cloud routine for 06:00 PDT today, emailing the brief to molchair@gmail.com.
- **Cleanup owed:** `mruangutai/harness-factory-smoke-a1` and board 6 survive; the token lacks
  `delete_repo`. Second undeleted throwaway alongside `mruangutai/harness-factory-smoke`.

## Distribution ends, and the global install with it

- **The global harness install is deleted.** 21 skills, 16 agents and 8 commands removed from
  `~/.claude/`, executed ahead of #203 on the operator's direct instruction: this repo is the source
  of truth. Backups at `~/.harness/global-harness-skills-backup-2026-08-10.tgz` and
  `~/.harness/global-harness-agents-commands-backup-2026-08-10.tgz`.
- The removal exposed that the global copy was **stale, not merely redundant** — `harness-wayfinding`
  and `harness-grilling` were last written Jul 31, ten days behind `c5597be`. #203's argument,
  demonstrated rather than asserted.
- `harness-grill.md` lived only in `~/.claude/commands/` and in no repository. It is grilling's only
  door, because the skill carries `user-invocable: false`. Brought into the repo (`dee043d`), then
  renamed to `harness-grilling.md` so both doors carry their skill's name (`aa18302`).
  `harness-wayfind.md` was deleted instead — `harness-wayfinding` is user-invocable and is its own
  door.
- **The constitution now reaches the floor** (`bd295b0`). `docs/principles.md` was an orphan: nothing
  linked it, no agent loaded it, and `check-docs.sh` does not reach outside `docs/harness/`. It is
  now a third universal preload, distilled to 4.7KB against the document's 19.9KB. Also fixed three
  broken citations that used the wrong case.
- Grilled #203 and spawned a plan flow. Artifact: `notes/grilling-end-distribution-2026-08-10.md`.
- Grilled #211 and spawned a plan flow. Artifact: `notes/grilling-graphql-cost-2026-08-10.md`.
  Measurement retired the caching idea: `gh project item-edit` costs 1 point, so read and write both
  land at 1 and there is nothing left to cache.
- **Open, unresolved:** `check-docs.sh` cannot see `docs/principles.md`, so the distilled skill can
  drift from the constitution with no gate noticing. Closing it is a DEC-174 direct edit.
