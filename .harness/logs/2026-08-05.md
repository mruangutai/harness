# 2026-08-05

- 0x:xx FEAT-07-verify-teeth-batch-probe SHIPPED — user accepted at the ship gate. 10/10 tasks, blocking qa gate PASS, 17 of 18 SCs met, SC-12 carved out (no agent in the org holds the notes/receipt-* grant it requires). Final cost $702.82 of $550 — 28% over, user-accepted, max_cost_usd NOT re-baselined. Cycles 8 of 10, every one a real defect a gate caught rather than churn. THREE USER RULINGS: (1) accept + open the PR; (2) NO formal pm re-grade of the four criteria closed after the goal-check — accepted the orchestrator's re-run of each criterion's own declared method, ~$60 saved; (3) file all nine backlog items. Main-session close-out per the playbook's `shipped` row: gh-sync.py ship closed parent #47 and milestone #2; gh-sync.py backlog filed #68-#76 (item 4 -> #71, filed as a SUPERSET of #46 which covers pm only, noted in the body rather than filed as a silent duplicate). Commit 9e14b6e carried the 2026-08-04 log; 12 commits on the branch, so the briefing's "eleven" is stale by one and was deliberately NOT edited — it was accurate when written. Branch pushed, PR #77 opened, NOT merged; the merge stays the user's.
- 0x:xx FEAT-07 retrospective worth carrying: the orchestrator's own $550 estimate was short by 28% and it named what it missed — the validation panel and goal-check finding REAL gaps that cost four follow-up runs, plus the close phase at $129.10. Better forecasting rule than any number: budget the panel finding something, because here it did twice. The architecture review the user ordered at the plan gate found the D-07 blocking design gap two planning passes had missed, and PROVED it with its own return (a harness-backend-dev returning PASS with no PLAN task — the exact case T-01 was about to make illegal). Distillation answered last feature's open question: relayed-candidate acceptance had been ~100% across six members, indistinguishable from members treating relay as instruction; this time eng-lead handed its member paths to its own prior artifacts and got 3 of 5 from the member's OWN material, while validation got 0 — so the variable is DISPATCH SHAPE, not persona, and neither validation member keeping an observations log is not the cause.
- 0x:xx FEAT-07 MERGED to main as ae2443d via PR #77 (--merge, branch deleted), on the user's explicit instruction. Gates on main after merge: run-unit-tests EXIT=0, check-docs EXIT=0, check-state zero VIOLATIONs. Issues #18/#19/#22 all closed — but #19 and #22 needed CLOSING BY HAND, from two independent causes worth keeping apart: (1) MY ERROR — the PR body wrote `Closes #18, #19, #22` with a single keyword, and GitHub applies the keyword only to the number immediately after it, so only #18 auto-closed; (2) issue #38 is LIVE — gh-sync ship never reads closes_issues, so the fallback that should have caught the other two does not exist. feature.yaml declared `closes_issues: "#19 #18 #22"` correctly; nothing consumed it. Both causes recorded on the closing comments. Orchestrator returned status: shipped with terminal state on disk (feature.yaml status: shipped, pr: 77, phase: ship) and added O-07 to its Expertise: price the validation panel and goal-check FINDING something plus the close phase, because a build-only estimate misses the fix runs those gates generate. It also corrected STATE.md to cite the ten task commits and their range 0a34989..98ed3e7 instead of a total commit count — a number that changes every time the file recording it is committed — and deliberately left the briefing's stale "eleven commits" alone: a presented artifact is not rewritten behind its reader.
- 0x:xx ISSUE #58 GRILLED and plan spawned — remove cost tracking entirely. Destination: the harness no longer meters, budgets, gates on or reports money; max_total_cycles (the only budget with teeth, DEC-157) untouched. Artifact notes/grilling-remove-cost-2026-08-05.md. USER RULINGS: (1) DEC-148's context watchdog DROPPED with the file, not preserved and not folded into check-state.sh — the new decision must record the drop so a future scan does not re-suggest it; (2) historical cost_usd/max_cost_usd in shipped feature.yaml LEFT IN PLACE as the only surviving record, check-state.sh merely stops requiring them; (3) plan and build it as a feature, same shape as FEAT-07, not direct edits. TWO DECISIONS SETTLED ON EVIDENCE RATHER THAN ASKED: `cost` STAYS in check-state.sh CHECKPOINT_KEYS (:344) because ALL 67 run state.yaml files on disk carry a cost: block and :401 flags unknown top-level keys — removing it would turn every historical run into a violation, so the keep-the-history ruling forces it; and render-brief.py has exactly ONE cost reference so the briefing rendering is a one-line check, while the cost hits in templates/BRIEF.md, PLAN.md and codebase-INDEX.md are prose about tradeoffs and must NOT be edited. KEY FINDING that shrank decision 1: DEC-148 made TWO changes and only one is being deleted — its watchdog is UNCONFIGURED (no context_per_turn_tokens key in harness.json; cost-report.py:338 hardcodes 200k) and its second half, the playbook relay rule, was SUPERSEDED by DEC-159 which makes one-phase-per-orchestrator mandatory regardless. So the watchdog is a diagnostic whose behavioural consequence is already hard-coded elsewhere. Also carried down: cost-report.py and test-cost-report.py must be deleted in the SAME task because run-unit-tests.sh:6 lists the test and the script's drift detector fails on a test-*.py outside that list — the red-suite-window shape FEAT-07's D-02 handled. OUT OF SCOPE: cycle counting, perf-row 10 (still unfiled, and it becomes the ONLY remaining lever for noticing a feature going long), rewriting historical DECISIONS entries.
- 0x:xx TWO TICKETS FILED from the supersession thread and the perf sweep. #78 — delete superseded decisions outright and remove the supersession-marking machinery; a superseding decision replaces its predecessor rather than annotating it. Collated evidence: 8 superseded rows of 177 (DEC-19/20/37/67/82/88/92/102), 1951 bytes = 4.9% of the index ~488 tokens; DECISIONS.md has ZERO literal "SUPERSEDED" text because the marker is GENERATED — gen-decisions-index.py:228-237 parses the SUPERSEDING decision's TITLE (em-dash split, last segment, first comma-clause, SUPERSESSION_VERB_RE), :274-290 collects targets from title then body prose, :324-326 appends the suffix to the TARGET's row, :252-259 strips it before the 30-word cap, :294-302 orphan-detects a row with no live heading as a HARD error. Two facts that make deletion cheap: ZERO of check-docs.sh's 45 <!-- stale: --> markers are declared by any of the 8 (top declarers are DEC-120 at 13, then five live decisions at 3), and orphan detection already blocks a partial deletion. The real cost is 42 dangling references — 26 from live index rows' refs: graphs plus 16 code/doc sites, DEC-19 alone accounting for 21 including gh-sync.py:14, check-domain.sh:6 and validate-digest.py:674/:802, where it is used as a PATTERN NAME not its literal holding. Ticket carries four deliberate decisions and an explicit counter-argument. #79 — count and budget RUNS not just cycles (perf row 10, never filed): FEAT-03 ran 19 runs against 6 cycles and tripped nothing because DEC-157 counts rework only; urgent now because #58 removes cost, the other long-feature signal.
- 0x:xx THREE CORRECTIONS I MADE TO MYSELF on the supersession mechanism, worth recording as a pattern rather than three slips. I told the orchestrator (1) the marker lives in DECISIONS.md and is regenerated, (2) no — it is hand-authored in the index, (3) no — it is GENERATED from the superseding decision's title. Only (3) is right and it came from reading gen-decisions-index.py; (1) and (2) were inferred from the index header comment "GENERATED except the text after ` :: `", which is true and which I over-read: the ruling PROSE is authored, the supersession SUFFIX is computed. The source was 40 lines away the whole time. Rule: when a file's header describes its own contract, the header is a claim about the code, not the code.
- 0x:xx PERF REVIEW STATE after FEAT-07: rows 1/2/5 SHIPPED; row 11 already CLOSED by FEAT-06's T-02 (review.yaml now has the qa step, verified); rows 3/4 are #20/#21; rows 6/7/8/9/10/12 unfiled except row 10 -> #79. USER SCOPED the next perf batch as #20 + row 10 + rows 8/9, and chose CONCURRENT planning with FEAT-08. COLLISION ANALYSIS CHANGED THE SPLIT: rows 8/9/10 all overlap FEAT-08 on shared files — row 8's first required rider is "move the ORCHESTRATOR-ONLY cost-report.py paragraph or INV-11's metering instruction is lost" and FEAT-08 DELETES both, so the two plans would contradict; row 10 and FEAT-08 both edit check-state.sh's invariant block, both DEC-174. #20 alone has an EMPTY intersection with FEAT-08. User ruling: plan #20 now concurrently, queue 8/9/10 behind FEAT-08. #20 grilled (notes/grilling-routing-wall-2026-08-05.md) and spawned. RULING on #20: enforcement is MECHANICAL not prose (same DEC-125 reasoning the user gave for #19), and ONE matcher — check-domain.sh gains a resolve mode rather than the matcher being duplicated or extracted, because check-domain.sh:215 has deliberately custom glob semantics (:193 records fnmatch is wrong since its * matches /). DEC-174 applies to that half. TWO ORCHESTRATORS NOW IN FLIGHT — a first for this repo; the #20 orchestrator carries an explicit do-not-touch list and instructions to return awaiting_user rather than edit any FEAT-08 file, including DECISIONS.md.
- 0x:xx STANDING USER INSTRUCTION, both in-flight features: SIGN THE PLAN ON ARRIVAL WITHOUT PRESENTING IT, then move straight to build. Applies to FEAT-08 (#58, remove cost) AND to #20 (routing wall) — the user was asked separately about #20, told that it ADDS a mechanism to check-domain.sh inside the DEC-174 carve-out and is the shape where the signature gate has historically earned its keep, and chose the same treatment. Concern raised once and reaffirmed; proceeding as instructed. WHAT WAS FLAGGED AND WAIVED: on FEAT-07 the signature gate produced the receipt-clause ruling (changed the feature's core mechanism), the D-01 reversal folding the live `suite: fail` fail-open into scope, and the order to run the architecture review that found D-07 — none came from the panel or the goal-check, all came from the user reading the plan. Counter-argument recorded too: remove-cost is a DELETION whose four decisions were already settled at grilling, so its gate has less to catch than FEAT-07's did. THE WAIVER IS THE USER'S READ, NOT MINE. Main session still verifies before signing: re-run the gates, check the digest's claimed numbers against the tree, confirm each verify: discriminates at base, check the plan against the grilling rulings, and for #20 check nothing touches FEAT-08's file list. Stop and surface rather than sign through if any of these fires — a task whose route contradicts the declared lanes, a verify: green at base, a scope addition grilling did not settle, or a cross-feature file collision.
- 0x:xx PERF ROWS 8 AND 9 FILED — #83 (drop harness-team from the orchestrator preload) and #84 (split harness-expertise's distillation half). MY ERROR, caught by the user asking what "next batch" meant: I had filed rows 6/7/10/12 and then kept referring to "8 and 9" as if they were tickets. They were only lines in the review doc. Measured for #83 at HEAD: harness-team/SKILL.md is 19,088 bytes, the orchestrator's whole four-skill preload is 53,445 bytes, so harness-team is ~36% of it, paid 3-4x per feature under DEC-159; flat mode is dead per SPEC.md:1309 "Verified (DEC-100, DEC-102): hierarchical works. The flat fallback is not needed" while harness-team/SKILL.md:12 still says "you are running it flat". The review's flat-mode anchors SPEC.md:158/:1387/:1936 have MOVED — re-measured to :1309/:1382/:2024. Measured for #84: 125 lines / 6,816 bytes, lines 51-113 govern distillation only, which DEC-145 makes once-per-agent-per-feature, ~850 tokens x ~34 spawns = ~29k tokens/feature; precedent is DEC-158 de-preloading harness-systematic-debugging in favour of a dispatch pointer; risk is DEC-125's "relied on being pointed at", bounded by check-expertise.sh catching format violations mechanically. BATCH: #83 + #84 + #43 are one preload-trimming set, all three colliding with #58 on shared files, so the batch goes AFTER #58 merges — at which point #83's first rider (move the cost-report paragraph or lose INV-11's metering instruction) EVAPORATES, because #58 deletes both.
- 0x:xx USER RULING binding BOTH in-flight features: RUN THE FULL VALIDATION PANEL — all four review.yaml steps (code, qa, security, ui). NO PRE-EMPTIVE SKIPS. Corrects FEAT-07, which ran TWO of four: feature.yaml:77-78 records `security: SKIPPED — no auth, secrets, input or network` and `ui: SKIPPED — zero end-user surface`, both the orchestrator's judgment made BEFORE dispatching. The argument, in the repo's own terms: harness-ui-reviewer and harness-security-reviewer are DESIGNED to self-scope out — looking and declining IS their job, and a decline from a reviewer that looked is a reviewed finding, while an orchestrator deciding in advance that there is nothing to find is an INFERENCE STANDING IN FOR A MEASUREMENT. That is precisely what issue #22 forbids and what FEAT-07 itself shipped into both the main-session command and the orchestrator playbook: a bounded question is measured before any claim about it is relayed, and "this diff has no security surface" is such a claim. validate-digest.py already accepts severity_max: n/a with PASS for a reviewer that scoped out, so the null result is a supported outcome rather than waste. Both orchestrators told: dispatch every step even where you believe it inapplicable, record the REVIEWER's verdict never your prediction, and if you skip anything return awaiting_user rather than recording a decided skip — that call is the user's now. Extra emphasis to the #20 orchestrator: its feature adds a resolve mode to check-domain.sh, the guard the whole domain-safety story rests on, so a security reviewer on that diff is not a formality. ship_refresh is a separate question and this ruling does not reach it. MONITORING COMMITMENT: main session verifies the panel width at each return — the PLAN's validate segment must be four-wide, and at ship the run records must show four validator steps with each reviewer's own verdict; a recorded skip is a stop-and-surface, not a sign-through.
- 0x:xx FEAT-08 PLAN ORCHESTRATOR DIED on an API error ("Connection closed mid-response") — and the recovery is the design working. All artifacts were on disk and complete: BRIEF 216 lines / 15 SCs, PLAN 703 lines / 12 tasks / 9 decisions / 4 segments, feature.yaml, STATE.md, runs/. STATE.md itself read "Plan phase is COMPLETE and terminal at the signature gate", so the crash landed AFTER the work, during wrap-up. THE TELL was the `## Approval` blocks: written truncated as bare `status: pending` with no `approved-by:`/`date:` lines, where FEAT-07's carry all three. Nothing else was damaged. Verified before signing per the standing waiver (the user waived THEIR read, not mine): the four-wide panel ruling IS recorded at feature.yaml:30; lanes are declared, 6 main-session-direct / 6 team; verify clauses discriminate at HEAD (cost-report.py exists so its delete-verify is red now; cost_usd appears 15x in check-state.sh); and the PLAN's own `## Verify receipts` rule is STRICTER than FEAT-07's — receipts paste the command's own output, and the three "unchanged from its pre-edit value" clauses must state BOTH numbers, "captured before and after, because an unchanged count is only evidence if the baseline was measured rather than recalled". SIGNED both, branch feat/FEAT-08-remove-cost-tracking off ae2443d, mirror opened (parent #85, sub-issues to #97), ship orchestrator spawned. It carries: repair-the-tail instructions (sweep for other half-written artifacts left by the crash), the four-wide panel ruling, the batch-the-main-session-segment rule, FEAT-07's two lessons (run the WHOLE unit suite on generated/constrained artifacts; count each SC's clauses and confirm every one has evidence), and an explicit hands-off on FEAT-09 which is the concurrent flow.
- 0x:xx COMMITMENT — FEAT-09 MOVES TO ITS OWN WORKTREE, user-instructed, at a named seam. THE SEAM: the moment FEAT-09's plan orchestrator RETURNS, before signing. Not earlier because that orchestrator is writing to those paths right now and moving files under a running agent corrupts the plan; not later because once its build starts there are commits to migrate rather than four static files. It is before FEAT-08 merges by construction — FEAT-08 is still in build and its PR does not exist.
  THE SEQUENCE, to run at that moment:
   1. FEAT-09 orchestrator returns with BRIEF + PLAN drafted.
   2. VERIFY first — four-wide validate segment declared; nothing touching FEAT-08's file list (harness/SKILL.md, harness-team/SKILL.md, harness-orchestrator.md, teams/*.yaml, harness.json, check-state.sh, validate-digest.py, cost-report.py, docs/harness/{SPEC,BUILD,DECISIONS,DECISIONS-INDEX}.md); verify clauses discriminate at HEAD.
   3. `git worktree add` off main at ae2443d, branch feat/FEAT-09-plan-time-route-check.
   4. Move .harness/features/FEAT-09-plan-time-route-check/ and notes/grilling-routing-wall-2026-08-05.md across; confirm the main checkout is clean of them (`git status --porcelain`).
   5. Sign BRIEF+PLAN THERE, open the mirror, spawn the ship orchestrator ROOTED IN THE WORKTREE.
  MECHANISM CORRECTION, found by loading the tool rather than assuming: do NOT use EnterWorktree. It switches the SESSION's working directory, which would move the main session out of the main checkout while FEAT-08's ship orchestrator is depending on it to execute main-session segments there — every verify: clause would then run against the wrong tree, which is exactly CLAUDE.md's "a stale copy silently tests the wrong logic". Use plain `git worktree add`, which creates the worktree without moving the session, and spawn FEAT-09's orchestrator with instructions to operate from that path (it holds Bash).
  OPERATIONAL RIDER for the FEAT-09 ship orchestrator: CLAUDE.md — "Working in a worktree, run everything from the worktree." Its gates, its unit suite and every task verify: must run from the worktree path or it validates FEAT-08's tree by accident. Tell it explicitly.
  WHY AT ALL: FEAT-09's dir is currently untracked inside a FEAT-08 branch checkout. Severity assessed LOW — harness/SKILL.md:161-162 already forbids `git add -A` ("the commit pen is yours... by explicit pathspec, never git add -A — the tree carries held dirt"), grep finds no add -A guidance anywhere, and the failure is trivially recoverable with git rm --cached + amend. The worktree makes it safe by construction rather than by vigilance.
- 0x:xx FEAT-09 WORKTREE MOVE EXECUTED at the named seam, then signed and shipped. Worktree /Users/molchairuangutai/GitHub/harness-FEAT-09, branch feat/FEAT-09-plan-time-route-check, based ae2443d, created with `git worktree add` NOT EnterWorktree (which would have switched the session cwd out of the main checkout that FEAT-08's ship orchestrator depends on for its main-session segments). Artifacts moved; main checkout verified clean of all FEAT-09 paths and now reports ZERO violations. Signed in the worktree; mirror opened from the worktree — parent #98, sub-issues #99-#102.
- 0x:xx CROSS-FEATURE COLLISION THE PLAN CAUGHT AND I MISSED — run-unit-tests.sh:6. MY ERROR: the 12-path do-not-touch list I handed the FEAT-09 orchestrator was derived from FEAT-08's GRILLING artifact, which predates FEAT-08's PLAN by construction and therefore could not name run-unit-tests.sh. Its lead caught it by opening FEAT-08/PLAN.md directly; the orchestrator re-verified at source rather than relaying on trust. Both features edit the single line holding the SCRIPTS array — FEAT-09 T-02 APPENDS test-check-plan-routes.py, FEAT-08 REMOVES test-cost-report.py (FEAT-08/PLAN.md:243,250-252). Blast radius: run-unit-tests.sh is the FEAT-07 whole-suite rider in nearly every verify: of BOTH plans, so a half-landed change reddens every remaining task in both features for an unrelated reason.
  THE WORKTREE CHANGED THE SHAPE, verified: the two trees now hold different copies (FEAT-09 worktree 13 entries at ae2443d, FEAT-08 branch 12 after its task, FEAT-09 14 after T-02), so a silent overwrite is impossible and the failure becomes a LOUD git merge conflict. But it introduced a second-order problem the orchestrator could not have seen: after FEAT-08 merges, FEAT-09's array lists test-cost-report.py, a script that no longer exists, and the runner fails on a listed-but-deleted entry.
  USER RULINGS: (Q1) HOLD T-02 until FEAT-08 merges, then rebase and append to the already-corrected 12-entry array — no conflict, no dangling reference, no coupling; FEAT-09 builds T-01/T-03/T-04 meanwhile and returns awaiting_user at T-02's gate. (Q2) FEAT-09 WRITES ITS OWN DECISIONS ENTRY after FEAT-08 merges, same seam, entry in DECISIONS.md then gen-decisions-index.py in one pass. Q3/Q4/Q5 stay open as residual findings.
  ALSO RECORDED: the orchestrator's option (c) — one feature owning both edits — is not merely inelegant but BROKEN, because FEAT-08's tree has no test-check-plan-routes.py and listing it fails FEAT-08's own drift detector immediately.
  Ship orchestrator spawned with the worktree root stated three times, the re-cd-every-Bash-call warning (the shell cwd resets between calls in this environment — I hit it repeatedly), the T-02 hold, the Q2 seam, the full-panel ruling with extra weight because this feature modifies check-domain.sh itself, and an instruction to say plainly in each affected receipt which whole-suite clause is deferred rather than reporting a green suite that never exercised the new test.
- 0x:xx STANDING USER INSTRUCTION: SPAWN FRESH ORCHESTRATORS AT PHASE BOUNDARIES. Stop resuming across them. MY ERROR, surfaced by the user noticing FEAT-09's orchestrator reaching ~200k tokens within 60 seconds of a resume: I twice chose SendMessage-resume over a fresh spawn and told the user it "saves a fresh ~10k preload". The arithmetic runs the other way — a fresh spawn costs ~10-13k preload plus a capped handoff note, while a resume REPLAYS THE ENTIRE ACCUMULATED TRANSCRIPT. Visible in the returns: FEAT-09's ship orchestrator reported subagent_tokens 112,919 then 185,360, and the second figure is mostly the first replayed. This is DEC-148 (dominant cost term is context length x turn count, growing with the square of session length) and DEC-159 (an orchestrator's mission is EXACTLY ONE PHASE, so ending at that boundary is normal termination and the successor reads a capped handoff note) — I worked around the mechanism built for this exact problem, on a rationale that inverted the numbers.
  THE RULE GOING FORWARD: resuming WITHIN a phase for a short exchange (hand back segment results -> it commits) stays fine and correct; the transcript is small and the context is genuinely needed. Resuming ACROSS a phase boundary, or resuming an orchestrator that has already run 60+ tool calls, is what compounds. At every boundary: have the outgoing orchestrator write notes/handoff-<phase>.md, let it terminate, spawn a successor that reads the note.
  BOTH handoff notes already exist (FEAT-08 notes/handoff-plan.md, FEAT-09 notes/handoff-plan.md), which is what makes the change free — the artifact a fresh successor is designed to read is already the house pattern.
- 0x:xx PERFORMANCE ROADMAP SET — notes/perf-roadmap-2026-08-05.md. User-scoped and user-sequenced. Accurate counts corrected first: 13 labelled (not 13 remaining), 3 shipped in FEAT-07, 10 open, #20 in flight as FEAT-09, so NINE remain. Also removed the `performance` label from #103 — I had added it unasked and it does not fit the taxonomy the user set when they scoped out #36/#74/#78; #103 is a worktree enforcement bug, not a speed-or-spend change. FOUR BATCHES, sequenced A -> B -> C -> D: (A) preload trimming #83/#84/#43, triggered by FEAT-08 merging, and waiting makes #83 CHEAPER not just safer because its cost-report rider evaporates once FEAT-08 deletes the script; (B) observability #79 count-and-budget-runs with tiny #82 riding along, urgent because FEAT-08 removes cost and leaves nothing that notices a feature running long — FEAT-03 ran 19 runs against a 6-cycle budget and tripped nothing; (C) the validation/close-out tier #7/#21/#80, blocked until #40/#41/#42 are scoped because they contest qa's write permissions and the panel's membership, and C MUST codify the four-wide panel ruling into the playbook or it stays prose-only enforcement depending on the main session remembering to paste it into every spawn prompt; (D) the hook multiplier #81 LAST and ALONE, and USER RULED measure-before-planning — nobody has established why it fires 11-21x, the review says "not constant, not chased", so the bounded probe runs first and only then is D plannable. Recorded that the review is now FULLY TRIAGED: rows 1/2/5 shipped, row 11 closed by FEAT-06, rows 6/7/8/9/10/12 all filed and batched, no unaccounted row.
- 0x:xx TASK LIST CREATED for the performance roadmap, with the FEAT-08/FEAT-09 INTERSECTION AT THE HEAD and everything else blocked behind it — the user's instruction, on the grounds that the rebase feels like the end of that work and is actually the beginning. Task #7 (do next, on FEAT-08 merging) carries the full eight-step sequence: rebase the worktree -> RE-VERIFY the relayed byte-identical claim rather than laundering it -> RE-PIN review_sha to post-rebase HEAD before any reviewer (the rebase rewrites all four hashes and INV-6 exists for exactly that) -> SPAWN A FRESH orchestrator reading handoff-build.md, never resume -> T-02 APPENDING to the already-corrected 12-entry SCRIPTS array -> the DECISIONS entry via documentor with the recorded BRIEF override -> FOUR-WIDE PANEL, which matters most here because check-domain.sh has sat committed with zero independent review since 2a242df -> goal-check, distillation, briefing. Dependencies wired: #1 batch A blocked by #7; #2 by #1; #4 by #2 and #3; #6 by #4 and #5. TAKEABLE NOW without waiting on either feature: #3 (scope #40/#41/#42, the qa-permissions and panel-width questions that make #21 plannable) and #5 (probe why the write hook fires 11-21x, which the user ruled must precede any #81 plan). Two things flagged to survive into FEAT-09's briefing unsoftened: its own finding that the checker it builds would NOT have caught this collision, because the collision is in a tool the plan uses rather than a path a task writes; and the review window on check-domain.sh named as a window that existed.
- 0x:xx PERF ROW 7 / ISSUE #81 WITHDRAWN ON MEASUREMENT — the probe the user ruled must precede any plan. Method: one additive line in check-domain.sh logging timestamp/pid/ppid/payload-size, one subagent Write each way, reverted BYTE-IDENTICAL (git diff --quiet clean), suite re-run green (unit 13/13, test-check-domain exit 0). RESULT: 1 Write tool call = 1 hook fire, on BOTH the allow path and the deny path — single pid, single ppid, single payload size in each. Cheap hypothesis checked first and eliminated: settings.json registers EXACTLY ONE PreToolUse entry matching Write|Edit and settings.local.json has empty hooks. THE ORIGINAL NUMBERS WERE NEVER MEASUREMENTS OF THIS: logs/2026-08-03.md:24 is labelled "INCIDENTAL, not chased: 11 hook fires" inside a probe of which script copy runs in a worktree, and it PREDATES T-13 when the hook still launched four python3 processes per invocation; :27 closes with "Incidental: 21 fires for one write (Q3 saw 11) — multiplier is not constant, not chased" inside a probe of identity resolution for the PyYAML bootstrap escape. Both were counts accumulated across a probe window with other work in flight. CONSEQUENCE: the review's "~0.9s per governed tool call" was 21 x 43.5ms and does not hold; the guard costs 43.5ms, already 46% faster than pre-FEAT-05 while doing strictly more work. So the row rated the review's BIGGEST REAL SPEEDUP and its HIGHEST RISK was chasing something that is not there — nothing to deduplicate. USER RULING: close #81, drop batch D. Done: #81 closed with the probe comment, task #6 deleted, perf-review note corrected in BOTH places (stack-rank row struck with the correction inline, section 7 annotated, original text KEPT because how it got ranked second is the more useful finding), roadmap headline corrected to 8 remaining in three batches. THE META-FINDING, which is the same defect class as everything else today: an incidental side-observation from a probe of another question was carried into a performance review as a measured multiplier, ranked second on the stack rank, and survived because nobody re-measured it in isolation — even though the review itself wrote "not chased" beside it. The measure-before-planning ruling is what made closing it cheap: one probe instead of a feature planned against an unobserved mechanism.
- 0x:xx TASK #3 DONE — scoped #40/#41/#42, and TWO OF THE THREE WERE NOT DEFECTS. Both closed with the measurement on the record so nobody re-files them. #41: its premise was flatly wrong. It claimed the panel's gate-only qa boundary is "enforced by wording in a prompt — an instruction the agent is asked to honour, not a constraint it operates under." Measured against the live hook: qa Edit -> validate-digest.py exit 2, qa Edit -> SPEC.md exit 2, qa Write -> its own notes/qa-c0.md exit 0. check-domain.sh refuses qa's Edit AND Write on any source path because no grant covers one, so the boundary is MECHANICAL, enforced by the domain and not the tool list. What Edit actually buys qa over the three reviewers is in-place modification of files it already owns (expertise, observations); reviewers do the same with Write by overwriting. Removing Edit would be cosmetic symmetry closing no hole. Caveat recorded on the issue: if check-domain fails open — as it does for a worktree outside .claude/worktrees/, issue #103 — Edit and Write are equally unconstrained, so the defence that matters is the hook working. #40: the table at DECISIONS.md:1634 sits inside DEC-105, whose subject is "the per-spawn baseline is ~15.3k tokens" — it is a SPAWN TALLY supporting a token-budget argument, not a panel specification. What is dated is the ·/∥ notation (qa sequenced before a 3-wide group, versus today's 4-wide panel); the SPAWN COUNT IS FIVE EITHER WAY, so the number the entry exists to make a point about still holds. The user's challenge — "what in the table is stale, and if it's really stale why keep it" — was right and dissolved the ticket: nothing to annotate, nothing to preserve, and the original "rewriting history is worse" framing was answering a question this site never raised. Real panel drift is already caught by test-team-catalog.py check (9)'s set-equality assertion across SPEC's two rows and the shipped review.yaml. #42 SURVIVES as the one genuine item, and the user ruled: fix the CONTRADICTION not the symptom — SPEC.md:463 says qa "Writes AND runs tests" while the Iron Law requires the DEV to write a failing test first, and all 12 test files live under bin/ granted only to backend-dev and dev-ops while qa's tests/** and web/src/** grants point at directories that DO NOT EXIST. Folded into batch C rather than direct-edited, because it changes a role definition and touches team-config.yaml (enforcement-layer policy, DEC-174). Task #4's description rewritten to carry all of it so batch C plans against the scoped result rather than the original three tickets.
- 0x:xx FEAT-08 — amendments SIGNED (A-1, A-3, A-4) and the five-edit main-session batch EXECUTED. BRIEF and PLAN both now read status: approved / Mike Ruangutai / 2026-08-05 / amendments-signed: A-1, A-3, A-4. The batch, all DEC-174 carve-out surfaces: deleted the cost_usd pin payload and its four-line comment from test-validate-digest.py, reworded the SURVIVING fixture's comment off the literal spelling, dropped INV-11 from test-check-state.py:205's aborted-invariant list, and reworded case_k's prose at :326 while KEEPING why the case exists. THE ORCHESTRATOR'S CATCH WAS RIGHT AND MATTERED: the token appeared THREE times in test-validate-digest.py (:749 comment, :753 payload, :769 the surviving fixture's comment), not the one I relayed — deleting the pin alone would have left the file in SC-01's sweep and silently re-created the exact defect the ruling removes. Measured after: cost_usd 0, INV-11 0, both test scripts exit 0, unit/docs/state gates all 0, and SC-01's sweep now returns EXACTLY the four survivors A-4 enumerates (BUILD.md, SPEC.md, DECISIONS.md, DECISIONS-INDEX.md) with both test files gone.
- 0x:xx MY ERROR, corrected by pm and confirmed by me: I advised the user that unknown-key tolerance was "already asserted structurally" at test-validate-digest.py:1213/:1233. BOTH ARE COMMENT LINES, and :1232-1234 names "unknown key ignored" as the BAD shape its detector rules out. So one leg of my SC-01 recommendation was wrong. The other leg — zero producers — I did verify and it holds, and that is the leg the ruling rested on; but "nothing is lost" was overstated. That is what turned Q18 from a formality into a real question.
- 0x:xx Q18 RULED, and the user's answer was a FOURTH option neither I nor the orchestrator offered: they want a STRICT schema — unknown keys REJECTED, and an agent that breaks it fixes its own return. So the answer for FEAT-08 is ADD NOTHING: a fixture asserting tolerance would cement the opposite of the mandate. Filed as issue #104 with the measurement that makes it a feature rather than a checkbox: 51 of 71 real run digests on disk carry at least one key outside their schema — needs_approval 35, cost 15, severity_max 9, adequacy_notes 8 (already issue #37), matrix_ok 3, plus one-off inventions defect_resolution / defect_verdicts / advisor_disclosure. Turning strictness on unprepared would have rejected ~72% of every return this repo has produced. But two of those rows are exactly the drift strictness would catch, so the mandate is right and the work is triage-before-enforcement.
- 0x:xx FEAT-08 VALIDATE-PHASE ORCHESTRATOR SPAWNED FRESH (not resumed) per the phase-boundary rule, reading notes/handoff-build.md. Carries: commit the main-session batch by explicit pathspec; DISPATCH T-10's remainder to documentor rather than doing it (docs/** is granted, issue #95 held open, A-3 makes the table ELEVEN rows with rows 10-11 unexecuted and adds a fifth plain-word grep clause with a fifteen-entry allow-list, and A-3's baseline ruling is capture-your-own against the post-T-10 tree); THE FULL FOUR-WIDE PANEL with review_sha pinned first; goal-check; distillation KEPT; briefing. Three findings to carry unsoftened: the routing wall's fourth recurrence landing inside this feature while FEAT-09 exists to prevent it, with eng-lead returning BLOCKED rather than taking the silent-PASS route a python3 -c rewrite would have given it past bash-write-guard; four instances of an intent prescribing prose containing a token its own verify: forbids, every one caught by RUNNING not reading; and T-10 passing all four clauses while still leaving two live cost sites because every clause matched compound tokens and both sites used the plain word — AN ALL-GREEN VERIFY IS NOT AN ABSENT DEFECT.
- 0x:xx #74 RESCOPED and retitled "A criterion's checks must be able to fail, and must not all share one blind spot" — the user ruled all three findings fold in rather than filing separately, on the grounds that they share an owner (harness-pm), a surface set (harness-spec-driven/SKILL.md + templates/PLAN.md + a checker) and a moment (plan authoring). THREE MODES: (1) N clauses named, fewer than N fixtured — caught by FEAT-07's goal-check on four criteria, SCRIPTABLE; (2) a clause already green at the base commit so it proves nothing — FEAT-07 rewrote two before signing, SCRIPTABLE; (3) every clause shares one matching TECHNIQUE so the set has ONE blind spot rather than N — NOT scriptable and the only one that SURVIVED A PASSING GATE. Mode 3 is FEAT-08's T-10: four verify clauses, four passes, two live cost sites left, because every clause matched compound tokens (cost_usd, cost-report, max_cost) while both survivors used the plain word. Modes 1 and 2 were caught OUTSIDE a gate; mode 3 walked through an approved, executed, passing one — not "we did not check" but "we checked, correctly, by the rules we wrote, and the thing was still there".
  THE FINDING THAT MADE THIS URGENT: the `## Verify receipts` discipline that caught real defects on THREE consecutive features appears in NO template and NO skill. harness-spec-driven:17 requires only an "unambiguous" result in under 60s and says nothing about whether a clause could fail; no script inspects a verify: clause at all. FEAT-07's orchestrator invented the table, FEAT-09's copied it only because the main session pasted it into a spawn prompt. The single most valuable practice of the last three features is house style carried by memory — prose-only enforcement of exactly the kind this repo keeps filing tickets about.
  USER RULING ON OWNERSHIP, which is the question that decides whether these are rules or wishes: harness-pm at AUTHORING time (rules into harness-spec-driven, the receipts table into templates/PLAN.md as a REQUIRED section), AND harness-eng-lead at its plan-time arch review, which already reads the plan before signature as a leaf reviewer — given one explicit question, "what would this check miss?", because mode 3 cannot be scripted and needs a reviewer asked to look for it. Recorded on the ticket: do NOT fake mode 3 with a heuristic counting distinct regex characters — a check that appears to test method diversity and does not would be this ticket's own defect under its own number. Task #8 created, blocked by #7 because it edits templates/PLAN.md and harness-spec-driven/SKILL.md, the same two files FEAT-09's T-03 and T-04 touch.
- 0x:xx #74 OWNERSHIP SETTLED — split by KIND OF CHECK, not by team. Modes 1 and 2 are COMPLIANCE: you count, or you run, and a script gives the answer, so harness-pm authors them (rules into harness-spec-driven/SKILL.md, the ## Verify receipts table into templates/PLAN.md as a REQUIRED section) with a plan-time script enforcing — self-authoring carries no bias risk because the numbers are the numbers. Mode 3 is JUDGMENT and goes to harness-qa ACROSS BOTH PHASES. MY EARLIER RECOMMENDATION OF harness-eng-lead WAS WRONG and the file says so: harness-eng-lead.md:61 — "You are reviewing your own squad's future work — one of two acknowledged self-review points in the design." I put a judgment question at a self-review point when an independence layer already existed for it. THE INSIGHT THAT MADE THE FIX SMALL: harness-qa's two phases are ALREADY the right shape for mode 3. Phase 1 (:32-37) reads BRIEF and PLAN with NO source access, on purpose, and writes what tests should exist; it can see from the PLAN alone that four clauses all use one technique, but it cannot confirm a real miss because it cannot read the code — a deliberate limit, not a defect. Phase 2 (:39-40) reads the code and confirms whether anything got past, and a gap is a FINDING not something to quietly close. That is shape-then-confirm, the order qa already runs. The only change: Phase 1 today compares its expected-coverage list against THE TESTS and never against THE PLAN'S OWN verify: CLAUSES. TIMING ACCEPTED ON THE RECORD: qa runs after the build so a miss costs one repair cycle; the user accepted that rather than waiting for #21 (which would make Phase 1 concurrent with the build but sits unplanned in batch C). PROHIBITION recorded on the ticket and in task #8: do NOT implement mode 3 as a script — no heuristic counting distinct regex characters, no pattern-difference metric, no proxy; a check that appears to test for a shared weakness and does not is this ticket's own defect under its own number.

## RESUME POINT — written before a context clear

**FEAT-08 IS MERGED.** PR #131, `main` at `47ed11f`. `cost-report.py` gone, `SCRIPTS` is 12, gates
all 0. Milestone #3 closed. Issue #58 closed by hand (gh-sync ship still does not read
`closes_issues` — bug #38, it bit again). Backlog #106-#130 filed, 25 items.

**FEAT-09 IS REBASED.** Worktree `.claude/worktrees/FEAT-09`, branch
`feat/FEAT-09-plan-time-route-check`, tip `f823426`, 7 commits ahead of `main`, tree clean.
Rebase verified byte-identical on all four committed files (`check-domain.sh`,
`test-check-domain.py`, `templates/PLAN.md`, `harness-spec-driven/SKILL.md`) — the claim
FEAT-09's handoff marked UNVERIFIED is now VERIFIED. Worktree gates: unit 0, docs 0, state 0.
`SCRIPTS` in the worktree now inherits main's 12 entries, which is what T-02 must append to.

### NEXT STEPS, in order — task #7, mid-flight

1. **RE-PIN `review_sha`** to the post-rebase HEAD, BEFORE any reviewer is dispatched. The rebase
   rewrote all seven commit ids. A pin to a vanished commit makes every reviewer's diff claim
   unfalsifiable (INV-6). This is the step most likely to be skipped.
2. **SPAWN A FRESH ORCHESTRATOR** — do NOT resume any prior one. It reads
   `.claude/worktrees/FEAT-09/.harness/features/FEAT-09-plan-time-route-check/notes/handoff-build.md`
   (63 lines, four sections). Per the standing rule: a resume replays the whole transcript, a fresh
   spawn costs a preload plus a capped note.
3. **T-02 via eng-lead** — APPEND `"test-check-plan-routes.py"` to `run-unit-tests.sh:6`'s SCRIPTS
   array. It already has 12 entries with `test-cost-report.py` removed. Append; never rewrite from
   a remembered list. Issue #100 is the open task issue.
4. **The DECISIONS entry** via product-lead -> harness-documentor (`docs/**`,
   `team-config.yaml:116`). FEAT-09 recorded a BRIEF override so the goal-check does not read it as
   a breach. The index is GENERATED — write the entry in DECISIONS.md, run
   `gen-decisions-index.py`, never hand-edit the generated fields.
5. **FOUR-WIDE PANEL** — code, qa, security, ui. No pre-emptive skips. Standing user ruling. It
   matters most here: this feature modifies `check-domain.sh`, the write-permission guard, and that
   file has sat committed with ZERO independent review since `2a242df` (now rebased).
6. Goal-check (7 of 12 SCs depend on T-02), distillation (KEPT, not a strike candidate), briefing.

### Carry into the briefing, unsoftened
- FEAT-09's own finding: "exactly the failure FEAT-09 exists to prevent — and the checker this
  feature builds would not have caught it, because the collision is in a tool the plan uses, not a
  path a task writes."
- The review window on `check-domain.sh`, named as a window that existed.
- Write NO cost line. The playbook no longer mandates one.

### Three near-term items from FEAT-08's backlog
- `/harness-deploy` must run — the deployed globals still tell every lead to run the deleted meter.
  AFTER merge, BEFORE the preload-trimming batch.
- `check-domain.sh:308` has `cost` in an allowlist with no comment. FEAT-09 is live on that file.
- `check-expertise.sh` silently ignores lines it does not recognise.

### Task list state
#7 in_progress (this). #1 batch A, #2 batch B, #4 batch C, #8 issue #74 — all pending, #1 blocked
by #7. #3 and #5 completed. Batch D dropped (issue #81 closed, not reproducible).
Roadmap: `.harness/notes/perf-roadmap-2026-08-05.md`.
