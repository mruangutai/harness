# 2026-08-22

- **FEAT-31 build closed by the main session.** All six main-session-direct tasks done: T-04, T-10,
  T-12, T-14, T-15, T-17. Commits `1929774`, `47ff239`, `8329575`. Suite green, gate at 3 violations
  (FEAT-26's brief plus two INV-26 board cards, none from these changes).
- **Q-HOOKCTX settled** by direct observation, not by reading source: hook stderr reaches a running
  agent in full, as a tool-result error string. `notes/settled-Q-HOOKCTX.md`.
- **spawn** FEAT-31 · resume · qa gate, panel, goal-check. Ship pre-approved by the operator;
  authorised through PR, CI and merge.
- **Seven residual findings filed** from FEAT-31's build briefing: #663, #664, #665, #666, #667,
  #668, #669. Eight rows died unstruck, as the briefing said they would.
- **FEAT-32 SIGNED** — both approvals `approved`/`operator`/`2026-08-22` at `d03a835`. Worktree
  brought current FIRST; `plan.yaml` verified byte-identical across the merge.
  **BUILD HELD DELIBERATELY: it starts only after FEAT-31 MERGES.** Operator's instruction, and it
  matches the feature's own subject — two orchestrators in flight is the collision class FEAT-32
  exists to fix, and its tasks reach `check-domain.sh` and `team-config.yaml`, which FEAT-31 also
  touches.
- **spawn** FEAT-26 · plan · narrow one overstated accepted-cost clause. The brief called the Goal's
  closing sentence "asserted by nothing"; SC-06 already asserts the renderer half, `verify:
  automated`, `evidence: integration`. Only the wider whole-feature claim is unasserted.
- **#670 filed** — feature-id coinage collided twice (FEAT-25 and FEAT-27 both coined
  `expertise-repository-tier`) and nothing detected it. INV-18 cannot see a feature directory with no
  `feature.json`, because it keys on `runs/` being non-empty. The orphan directory it named is
  already gone from all four checkouts; the collision is the defect, not the orphan.
- **FEAT-26 Q1 ANSWERED** by the operator: FEAT-01 and FEAT-02 to PR 4, FEAT-03 and FEAT-04 to PR 15.
  Attribution is by PR title; branch resolution cannot separate them because two PRs share one
  branch. Narrowing SC-08 to the 19 derivable features was offered and declined — dropping them
  would leave four `Done` features as permanent gate noise under SC-07's new line.
  Recorded at `notes/answers-Q1-pr-attribution.md`. **T-06 is unblocked.**
