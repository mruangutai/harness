# 2026-08-24

- **FEAT-35-orchestrator-stop-and-wake SHIPPED.** PR #812 merged at `92b3fec`; `gh-sync.py ship`
  closed milestone 24, recorded `pr: 812`, wrote `status: Done`. Parent #751 and sub-issues
  #798-#802 all CLOSED. `check-state.sh` clean.
- **What it fixes.** The playbook told the orchestrator to stay alive across lead round-trips, so it
  invented activity — 354 of 450 Bash calls on `echo hold` and `sleep`, 341 of them `echo hold`, the
  other 13 twelve distinct inventions — then died at the 600s watchdog, taking its lead and member
  with it. The loop is now stop-and-wake in seven steps, with a new step 5 for the context check.
- **The measurement that settled it.** Three UUID probes: a parent that STOPS is woken by its child
  at every depth the org uses. One stopped at 15:17:14, was notified at 15:18:05, resumed at
  15:18:09. The published sub-agent docs say the opposite and are wrong here.
- **SC-03 took four gradings and three real defects** — an empty candidate set, a copied nonce, a
  tool-level type filter. It was never untestable; it named a verifier who could not reach the
  subject. Split by verifier: reviewer proves the shape, main session proves the subject against a
  live orchestrator.
- **The nonce defect was MINE and this feature's own criterion caught it.** The playbook printed a
  literal example nonce AND required uniqueness. Every orchestrator copying it would have SKIPPED
  its own context check, silently, forever. Eight text assertions and a human read all missed it.
- **MY ERROR, and it put a red PR up.** I reported "ALL PASSED, 22 scripts, 0 FAIL" from a
  background job's output file WHILE IT WAS STILL BEING WRITTEN — 1409 of 2871 lines, the FAIL at
  1756. Real count 47 scripts. CI caught it. **Rule: wait for the completion notification.**
- **The org corrected me three times on evidence** and was right each time: `parent_origin` should
  be `adopted` not `created` (checked the code and the issue's authorship); the `echo hold` count is
  341 not my 342 (a bug in my own regex); and DEC-201's control sentence implied one watchdog death
  when both were.
- **Operator calibration recorded** (DEC-201): the context threshold is a warning line, not a
  budget. 270k against 200k is fine; roughly 2x is where the judgement gets hard.
- **SC-05 is PARTIAL by construction.** The 1057.1s survival was measured under a dispatch-level
  override, since the rewrite lived in a worktree while spawned agents load skills from main. Owed:
  one orchestrator round-trip over 600s under the MERGED skill, no override. Owner: main session,
  next feature that runs a build or validate phase.
- **Nine tickets filed today.** #803 no DEC-NN collision guard (T-04's verify passed VACUOUSLY on
  another feature's row) · #804 the regression test's assertions are exact-literal greps defeated by
  rewording · #805 a team task's `done` write has no owner · #806 `ship` routinely skipped, ten of
  23 milestones open · #808 `ship` and `abandon` duplicate four of five terminal steps and `ship`
  omits the sub-issue close · #810 `bash-write-guard.sh` may key on redirect syntax · #811 DEC-201
  and the #551 return contract now say *do not wait, and do not stop* · plus #781's umbrella gained
  #811 · #807 was a throwaway probe, closed.
- **Probe #807 settled a live question:** moving a card to the `Done` station auto-closes the issue
  in under 10 seconds, no `Closes` line involved. Confirmed a second time on FEAT-35's own five
  sub-issues. `DECISIONS.md:5652` recorded only the opposite direction.
- **FEAT-33 closed out** — it merged with `feature.json` still at `Review` because nothing runs
  `ship` at merge time. Filed as #806, folded into FEAT-34. Ten stale milestones closed by hand.
- **NEXT:** FEAT-34-worktree-act3-enforced. Brief signed, no plan yet. Its brief cites
  `SKILL.md:321`, which FEAT-35's rewrite has moved — pm must re-derive, not trust the line number.

## FEAT-34-worktree-act3-enforced — ship dispatched

- **Signed.** BRIEF.md Amendment 1 re-signed 2026-08-24 (`amendments-signed: Amendment 1`);
  `plan.yaml` approval `approved`, Mike Ruangutai, 2026-08-24. 13 tasks, 9 decisions.
- **Q6 settled by the main session, not the user.** D-08 signs as written, no fleet carve-out:
  `settings.snippet.json` already points all seven harness hooks at
  `${CLAUDE_PROJECT_DIR}/.claude/skills/harness/bin/`, and `harness-init/SKILL.md:42-43` merges that
  file into the target project — so a product repo must already carry that tree. `hooks/` beside
  `bin/` adds no new assumption. Recorded in `notes/answers-plan-2026-08-24.md`.
- **Unverifiable, stated rather than folded in.** No onboarded product repo exists.
  `harness-factories/kaya-ai/.claude/` holds commands, hooks and settings.json and NO `skills/`
  directory, and its settings carry no harness hook entries. Neither D-08 nor the seven existing
  gates have ever run in a fleet repo.
- **Gates at 9165162:** `check-state.sh` exit 0, zero violations. `check-plan-routes.py` exit 0,
  0 violation(s), four DEVIATION lines all correct (T-06/T-08 on check-state.sh, T-07/T-09 on
  test-check-state.py — DEC-174 am.4 overrides the domain grant).
- **`feature-worktree.py behind` exit 0** — current with main.
- **`gh-sync.py status ... Ready`** wrote feature.json status Ready and printed "no sub-issues
  recorded, nothing to move". Expected: SKILL.md:266 puts `gh-sync.py open` at the start of ship.
- **Spawned** `harness-orchestrator`, background, mission ship. Seven of thirteen tasks
  (T-06..T-12) are main-session-direct and come back to me.

### FEAT-34 ship — escalation 1 answered, resumed

- Orchestrator returned `ESCALATE` / `awaiting_user` after 5 runs, cycles_used 1. Five tasks built
  and green; four plan defects corrected.
- **Verified at source before relaying** (the orchestrator's numbers all held):
  `test-worktree-terminal.py` 19 PASS, exit 0. plan.yaml 13 tasks / 10 decisions.
  `grep -c "def classify_all" worktree_terminal.py` = **0** — D-10 is unbuilt.
  Sub-issues #818-#830 open. `check-state.sh` exit 0. Nothing committed, HEAD 9165162.
- **F7, the real gap.** `worktree_terminal.py:56` runs `git worktree list` with `cwd=root` — one
  repository. A served repo's worktrees sit in a different git repository that call cannot report,
  so REQ-04's "every repository" clause is genuinely unmet. The 19 green cases prove the per-repo
  predicate only; case (g) calls `classify(repo2)` on the second repo's own root.
- **USER RULING, Q2:** D-10 stands. `classify_all` goes in `worktree_terminal.py`, not
  `check-state.sh`. T-01 and T-02 reopen; `cycles_used` to 2 of 10 authorised. Cost named both
  ways — two spawns once, against test leverage lost permanently on the main-session-direct lane.
- **USER RULING, Q4:** add the criterion as Amendment 2. `BRIEF.md:179-182` shows SC-04 grades only
  the positive second-repo case, while `:62-63` REQ-04 requires no per-repository exception. A repo
  silently skipped would pass every criterion. pm authors the SC (DEC-132).
- **Plan RE-SIGNED**, second signature of the day, with a `signatures:` list recording what each
  covers. Gates re-run green after the write.
- **Q3 caveat recorded, not folded in.** pm claims T-10's intent was untouched; `plan.yaml` is
  untracked so no diff exists. Closing it permanently by committing `plan.yaml` once Amendment 2 is
  signed.
- Resumed the orchestrator with `notes/answers-ship-1-2026-08-24.md`, instructed to run the
  T-01/T-02 rework and Amendment 2 in parallel.

### FEAT-34 ship — T-06 written by the main session; INV-29 caught the live harm

- **BRIEF re-signed for Amendment 2** — `amendments-signed: Amendment 1, Amendment 2`. SC-15 live.
- **Verified the orchestrator's return at source:** `test-worktree-terminal.py` 31 PASS, exit 0
  (the two "FAILS" lines are red-proof stub descriptions, not failures). `classify_all` present.
  SC-15 at BRIEF.md:387-404. Q3's discriminator gift CONFIRMED by my own read —
  `worktree_terminal.py:202-206` and `:303-306` emit identical `feature_id None / repo None /
  unresolved`, so keying on `feature_id` alone would withhold the removal command from a real
  worktree.
- **T-06 DONE.** INV-29 written into `check-state.sh` at :1191, calling `classify_all` and never
  `classify`. Repository-level records are told apart by `feature_id is None AND (repo is not None
  OR path == FLEET_PATH)` — reads one constant, duplicates none of classify_all's resolution.
  Self-exclusion follows INV-25's precedent at :1173. Removal command is
  `feature-worktree.py remove --repo <repo> --id <id>` per plan.yaml:414, never raw git — git takes
  `--force` and the brief forbids one.
- **F10. INV-29 fired on its first run and caught the exact harm.** FEAT-33 and FEAT-35 worktrees
  standing at `Done`. USER RULING: remove both. Both removed.
- **A false refusal, filed as backlog.** `feature-worktree.py remove` exits 5 with `DIFFERS` when
  the DEFAULT BRANCH is strictly AHEAD. FEAT-35's worktree read `Review / pr: null`; main read
  `Done / pr: 812`. Nothing was unlanded. The guard cannot tell "you would lose work" from "you are
  behind". Cleared by merging main into the worktree first.
- **F11, BLOCKING, a defect in code green at 31/31.** `classify` skips the main checkout only when
  it equals `root`. Run from inside a linked worktree — how the harness ALWAYS runs — it emits an
  `unresolved` record for the main checkout. Probed both directions. Every fixture calls
  `classify(repo_root)` from the root, where the skip works by coincidence. `check-state.sh:1138`
  already records the correct rule: the first porcelain entry is always the main checkout.
- Gate now exits 1 with **exactly one** violation, and it is F11.
- **Spawned a successor orchestrator** for F11 plus the remaining build. `cycles_used` to 4 of 10
  authorised. T-10, T-11, T-12 are mine and I start them now.
