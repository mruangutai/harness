
## FEAT-40-harness-writes-done — plan dispatched

- **Grilled first** (`.harness/notes/grilling-board-done-and-parent-close-2026-08-25.md`), frontier
  empty. Source ticket **#842** filed and moved to `Plan`. Worktree cut at
  `.claude/worktrees/harness/FEAT-40-harness-writes-done`, gate exit 0 with zero violations,
  registry showed NO CLAIMS before the spawn.
- **The ruling in one line:** a ticket is open while its card is not at the `Done` station, the
  harness writes `Done` at `ship`, and a parent closes when it has no open children. Origin stops
  mattering — DEC-196 struck, DEC-138 am.7's parent table struck with it, `parent_origin` deleted.
- **Three of my own findings were wrong and are corrected on the record.** F2: gh-sync.py behaved as
  D-23 designed; the sub-issues were closed BY HAND (`reason=null`, where `close-task` always passes
  `--reason completed`). F9, twice: `gh-sync.py closes` is neither dead nor undocumented — it is an
  instructed main-session step at `github-mirror.md:36` that I skipped when composing PR #837, and
  it works (`Closes #840` in PR #841 closed #840 and landed its card at `Done`).
- **The leak is mine.** No document anywhere instructs the main session to close a task sub-issue.
  Measured across FEAT-33/34/35: every close reads `by mruangutai reason=null`. The same habit
  produced the two hand-recorded parents — FEAT-34 and FEAT-35 are the only two features whose
  `parent_origin` is present-and-null.
- **The post-merge sweep has never fired on this clone.** `core.hooksPath` is
  `/Users/molchairuangutai/GitHub/harness/.git/hooks`. `harness-init/SKILL.md:50` quotes THIS
  broken value from THIS checkout as its worked example, and the step lives in the onboarding skill
  that an already-onboarded clone never re-runs. Nothing checks it — FEAT-34 added INV-29 and
  INV-30 and neither asks whether the hook is installed.

## FEAT-34-worktree-act3-enforced — SHIPPED and merged

- PR **#837** merged as `18c4730` after one CI red: `test-hooks-install.py`'s fixture cloned a repo
  and committed inside the clone, and a clone inherits no git identity — a developer machine has an
  ambient one, a GitHub runner does not. Reproduced exactly before fixing. The fixture now sets its
  own identity and runs every git call loudly.
- `gh-sync.py ship` run explicitly: milestone #26 closed, pr recorded #837, status `Done`. Parent
  #728 left open (`parent_origin` none). Worktree removed from outside the tree. PR **#839** carried
  the bookkeeping commit, since branch protection rejects a direct push to main.
- **#840** struck the `absorbs:` rule from DECISIONS.md in both places — DEC-138's body said
  absorbed issues "close with it" and am.7 reversed it, so striking only the amendment would have
  revived the body. Merged as `cc84b29`.

## Sequencing ruled: FEAT-40 first, FEAT-37 waits

- **#551 is NOT in FEAT-40's scope** and should not be — measured, zero mentions in its `plan.yaml`
  or `BRIEF.md`, `source_issues: [842]` alone. No file overlap either: FEAT-40 touches `gh-sync.py`,
  the board and a Bash gate; the polling touches `validate-digest.py`, the `SubagentStop` hook and
  lead instructions.
- **The polling IS #551's own recorded symptom**, not a separate bug: "Leads now burn context
  holding turns open as a workaround." #811 states it sharpest — DEC-201 requires a dispatching
  agent to end its turn and `validate-digest.py` refuses exactly that, so "every correct turn-end
  trips a refusal that prevents nothing." The loop is what an agent does to survive both rules.
- **FEAT-37-lead-stop-and-wake already owns it** (`source_issues: [831]`, `status: Plan`, 6 runs,
  approval pending). It waits on a signature, not on scoping.
- **The operator ruled FEAT-40 first, as planned.** Accepted cost, named rather than argued away:
  every FEAT-40 build run pays the same context tax, and the operator is watching it happen.
