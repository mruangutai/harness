# 2026-08-23

- **#751 filed** — the orchestrator playbook's loop demands it stay alive across lead round-trips,
  so the 600s no-progress watchdog kills it. Diagnosed in the main session from three UUID probes;
  the decisive one shows a parent that STOPS after dispatching is WOKEN by its child
  (`DISPATCHED_AND_STOPPED` 15:17:14 → notification 15:18:05 → `RESUMED_WITH:<uuid>` 15:18:09,
  matching disk). `code.claude.com/docs/en/sub-agents.md` says the opposite and is stale.
- **FEAT-35-orchestrator-stop-and-wake** worktree cut at `569d417`. Grilling artifact at
  `.harness/notes/grilling-orchestrator-stop-and-wake-2026-08-23.md` in that worktree.
- **#742 returned to Backlog.** Its registry/cwd defect is real and separately specified, but it is
  not what kills the orchestrator. Four design answers for it are recorded in the grilling note.
- **Execution mode forced:** `check-domain.sh --resolve .claude/skills/harness/SKILL.md` → NOBODY,
  so the fix is main-session-direct whatever DEC-174 says.
- **FEAT-35 PLAN SIGNED** at `7cf42a0` on `feat/FEAT-35-orchestrator-stop-and-wake`. Both approvals
  `approved`/`operator`/`2026-08-23`, verified by reading the lines, not the report. Plan is 5 tasks,
  7+1 decisions, `check-plan-routes.py` 0 violations. One consolidated revision (DEC-176) covered
  T-02's zero/multi-match clause, the false am.4 sentence, and T-05's lane (became D-08).
- **The override worked.** The orchestrator ran the whole plan phase and both revisions without
  stalling once — no `echo hold`, no `Monitor` sleep, no death. Its context reached 195,850 against
  the 200,000 threshold, so the build phase needs a FRESH orchestrator. That is the rule this
  feature adds, arriving one run early.
- **#781 filed** — "An agent emits a verdict about a child it has not heard from", with #551, #461,
  #527, #608 and #669 attached as sub-issues. Its new content: a SECOND mechanism. Closure (the
  five) is the turn ending before the agent has heard; BELIEF is the agent inventing a completion
  while alive and unpressured. Measured — a parent fabricated UUID
  `91C39D91-4A48-4A69-8AAA-8DD87B9BD9DD` for a child that had not finished and wrote it to disk.
  That value appears only in its own assistant messages. #608's instrumentation is scoped to
  closure causes and cannot see this one.
- **BUILD HELD, operator's call.** FEAT-33 is live and both features edit
  `.claude/skills/harness/SKILL.md` (FEAT-33's T-14 is `done` on the GitHub-mirror table; FEAT-35
  rewrites the delegation loop) and `run-unit-tests.sh` (FEAT-35's T-05 adds to `UNIT_SCRIPTS`).
  FEAT-35 resumes after FEAT-33 merges, and its worktree needs bringing current first — it is at
  `569d417`, main is at `faf409e`.
- **Three main-session claims were false and are struck at source** in the grilling note: the
  branch's "142 unrelated deletions", DEC-174 am.4 as an exact enumeration, and a squad-run
  execution mode.
