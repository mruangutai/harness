- 2026-08-15: FEAT-21 SHIPPED — 14/14 (SC-12 ratified conditional-on-hygiene, condition
  verified by validator round 3 PASS at 4a98cc4). Ship close (#388 + milestone 12), review
  posted, 16 backlog rows filed, 2 folded (#369/#372), branch pushed, PR opened.
- 2026-08-15: Unit 4 (docs migration) plan flow spawned at 0f12f14. Constraints: one atomic
  commit for the coupled cluster (_PROBE, HARNESS_CONTROL_PLANE, DOCS_DIR, the move),
  detector-gated boundaries, historical prose = knowing survivors, PRINCIPLES.md stays
  global, FEAT-21's depth sweep inherited as method. pm coins the id.
- 2026-08-15: FEAT-22-docs-layout-migration PLANNED at pin 0f12f14 — BRIEF.md + plan.yaml
  (11 tasks, 7 decisions, 12 SCs), both approval: pending, awaiting signature. ui-reviewer
  mode-A recheck-2 PASS on r6; visual-designer PASS, needs_prototype false; eng-lead
  archreview-recheck PASS on r4 with r5/r6 deltas verified additive, not re-read by eng-lead.
  5 send-backs to pm, each closing a real defect. D-03 rules every task main-session-direct.
  D-06 records the tests.yml premise as false. feature.json + STATE.md instantiated (E1).
  Detector pre-state green: docs CLEAN — evidence legacy, features CLEAN — evidence migrated.
  Operator: sign conditional on eng-lead r6 re-fire PASS; Q2 confirmed both; Q3 re-fire ruled.
- 2026-08-15: FEAT-22 signature condition MET. eng-lead re-fire on r6 came back FAIL with MF-4 —
  test-check-domain.py:924 case (h) flips exit 0->2 after T-03 and would have redded T-05's own
  verify twice inside the unsplittable 28-file cluster commit. r4 text; two prior review rounds
  missed it; the operator's attach-to-signed-text ruling is what found it. pm closed it in one
  pass, 0 send-backs: :924 enumerated, subject pinned to .harness/harness/docs/SPEC.md, runnable
  grep -qF clause at plan.yaml:605; A4 folded. eng-lead PASS on r7. Orchestrator confirmed all 11
  task boundaries sit at the offsets the 3 declared insertions predict. Residual: ui-reviewer's
  PASS attaches to r6. Cycles 6/10. Q5 re-characterized: bash-write-guard scans heredoc BODIES as
  command text, so `->` in a body becomes a phantom redirect target — plain redirects are fine.
  Operator signature written on r7 (both blocks).
