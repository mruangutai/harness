# Log — 2026-08-13

- **spawn** · `#277 · plan · board truth during a build` · mission plan · orchestrator, background.
  BRIEF input is `.harness/notes/grilling-board-truth-2026-08-12.md` (complete: 11 decisions,
  frontier empty). Goal constraints passed: harness board only, loud-not-blocking writes, no retry,
  parent station derived. Routing constraint passed: any task touching `check-state.sh` is
  `main-session-direct` under DEC-174; `branch-create-gate.sh` is not one of the four.

## FEAT-18-board-truth — SHIPPED

- PR #334 squashed as `3e23907`; close-out PR #335 as `63b83c7`. Eight live criteria (SC-08 struck),
  all met. 2 rework cycles of 10, 8 runs of 20. `review_sha` `af204ed`.
- Validation found a real defect, E-01: INV-26 skipped any feature between two tasks — its own SC-05
  scenario. `derive_station({done, pending})` is `None`, and the code skipped the whole feature on
  `None`, taking the per-task comparison with it. Fixed main-session-direct under DEC-174, operator
  read the diff. Case v.8 proven able to redden against the reverted file.
- Two more premises broke the moment the last feature went `Done`: `case_19a3` asserted `> 0` plans
  against the live repo, and CI's route gate read `0 plan(s)` as broken discovery. Both encoded
  "this repo always has unshipped work". `check-plan-routes.py` now prints the count of feature dirs
  it ENTERED, and CI gates on that instead.
- Merges were the operator's explicit instruction, given before the panel returned.

## #206 — planning spawned

- Mission `plan`. Grilling artifact cited, not re-run:
  `.harness/notes/grilling-central-product-config-2026-08-12.md`, marked READY FOR pm, its one
  precondition (FEAT-16 first) met at `f01b7b6`.
- Constraint passed down: the artifact's anchors were re-derived at `862d270` and it records that
  four had already moved once. HEAD is `63b83c7`. pm re-derives every anchor at HEAD.
