# 2026-08-04

- 14:09 FEAT-05-pyyaml-file-parsers SHIPPED. PR #17 squash-merged to main. 14/14 SCs met, cycles_used 3, review_sha f0a3831. Issue #11 auto-closed by the PR; #12 closed `not planned` (cannot reproduce — 0/57 label mismatches, verified three ways). #16 stays open by design (D-09).
- Route to ship: 5 code-review passes + 2 goal-check cycles, 24 findings, all closed. The GOAL-CHECK found what no code review did — that a fix of mine had added a line-scan fallback the signed BRIEF forbids outright — because it asked "does this do what you promised" rather than "is this code correct". Removed at the user's ruling after MEASURING the trade: the fallback bought earlier detection, not correctness.
- Self-inflicted rate worth remembering: three of my fix rounds introduced a new high while closing an old one, and six of the feature's non-discriminating tests were mine (one injected the missing import into its own harness; one asserted "a position is present" while the position was wrong). Every real defect was caught by proving RED before trusting GREEN.
- Left on the branch for someone else: bash-write-guard.sh's `FOO=bar python3 - <<'PY'` false positive (found by review pass 3, correctly scoped out, NOT ticketed yet). cost_usd 240.82 vs a signed 120 is stale LOW — it predates the whole review arc.
- 07:xx spawn harness-orchestrator mission=plan — team-definition layer + INV-6 truthy hole (issues #8, #9, #16). Grilling artifact `.harness/notes/grilling-team-layer-and-inv6-2026-08-04.md` handed as BRIEF input (path, DEC-164); id to be coined by pm. Budget $120 unraised. Goal constraints passed: reuse the `validate-digest.py:472` placeholder vocabulary for #16; #8 is the direct review.yaml qa-step fix; #9 includes retiring the hand-written step lists; both team files get `{{feat}}` quoted and the YAML validity gate widened to `.claude/skills/harness/teams/`. DEC-174 carve-out flagged: the check-state.sh task must be planned for DIRECT execution, not a team run. Gate before spawn: check-state.sh exit 0 (6 pruned-run-dir notes on FEAT-05).
- 08:xx PERF REVIEW (main session, user-directed): two read-only reviewers, non-overlapping scopes — empirical (run records) and architectural (loop design). Report `.harness/notes/perf-review-agent-workflow-2026-08-04.md`, 12 ranked rows + 6 rejected levers. Baseline verified by yaml.safe_load over all six feature.yaml: FEAT-03 19 runs/6 cycles/$358 of $120, FEAT-04 15/6/$324, FEAT-05 6/3/$240.82. Member-output rejection is NOT the bottleneck (1 hard rejection in 5 features, lead-attributed). The two sinks are the user gate and the routing wall (3rd recurrence). Filed rows 1-5 as backlog issues #18-#22 (labels harness + enhancement/bug, no milestone, DEC-138 am.4). Also filed #23: FEAT-05's six run dirs referenced by feature.yaml, zero on disk, check-state.sh downgrades to a note and cannot distinguish pruned from never-written. DISQUALIFIED by evidence: cutting the goal-check (logs/2026-08-04.md:4 — it found what 5 code-review passes did not). REJECTED with reasons: cutting the lead tier (DEC-71 priced it), dev-task parallelism (DEC-85 makes serialization the write-safety mechanism), reversing DEC-159, lowering effort on the judging tier (DEC-152), documentor concurrent with the panel. FLAGGED not proposed: FEAT-05 shipped 14/14 SCs in 3 cycles with no member digests at all (main-session build, DEC-174) — largest unexplored lever, one data point on an atypical feature. Cross-hit on work in flight: SPEC.md:1980 shows the review panel as {code || qa || security || ui} while review.yaml has 3 steps and no qa — independent evidence for issue #8, currently being planned as FEAT-06.
- 09:xx FEAT-06 plan gate: orchestrator returned ESCALATE/awaiting_user, 4 blocking questions batched into ONE gate (the behaviour issue #18 asks for). arch review FAILed with 6 must_fix; orchestrator deliberately did NOT route the FAIL back to pm because 3 findings were defects inside a schema form the user might strike. cycles_used 1, cost 18.90 measured of 120 + unmeasured segment-1 band (cost-report.py is cumulative over an aged transcript window; no time flag).
- 09:xx MAIN SESSION verified 2 orchestrator claims at source (both HOLD): PLAN.md:319 T-07 depends_on T-08 whose block is at :361, so PLAN order is NOT topological; harness-qa is validator squad (SPEC.md:2028), so DEC-118's single-squad bound genuinely blocks a qa step in an eng-lead-hosted build.yaml. Also verified gate-probe.yaml has ZERO live consumers — only refs outside its own file are DECISIONS.md:2307-2325 (historical loop-back proof).
- 09:xx NEW DEFECT FOUND while gating, filed as #24: `.claude/skills/harness/SKILL.md` (the orchestrator playbook, preloaded per harness-orchestrator.md:8-12) contains ZERO occurrences of `qa` and ZERO of `test_matrix`, while SPEC.md:1978 assigns qa sequencing to the orchestrator. SPEC is not preloaded by the orchestrator. The only blocking gate has run on all 3 shipped features ONLY because a lead added the step by hand: FEAT-03 feature.yaml:62 "panel + added qa step", FEAT-04 feature.yaml:144 "qa step ADDED to cover the blocking gate", FEAT-05 notes/qa-c0.md + qa-c1.md. Three descriptions of where qa runs (SPEC ship row, SPEC review row at :1980, review.yaml) and no two agree. Fifth instance of the grilling artifact's through-line.
- 09:xx USER DECISIONS (answers-plan-eng.md): Q8 steps_from KEPT (EMF-1/2/3 now live, T-09 load-bearing) · Q5 carve-out extension KEPT, 3 bin/ test files stay main-session-direct · Q3 gate-probe.yaml DELETED not quoted (T-03 drops, T-05 SC must assert 2 files not 3) · EQ-1 SUPERSEDED — user chose STOP AND RE-PLAN around the unowned qa gate. build.yaml at 7-of-8 is now understood as correctly bounded, NOT a shortfall; both repairs the main session offered were wrong. Re-spawned orchestrator mission=resume, budget $120 unchanged, instructed to fold the FAIL into the same pm run as the re-scope so it costs one cycle not two.
- 09:2x FEAT-06 re-plan RETURNED (replan-product PASS, 25.91). BRIEF + PLAN rewritten, both pending: 10 tasks, 15 SCs (SC-01..SC-15 confirmed by id sweep), closes_issues "#8 #9 #16 #24". T-03 retired, T-10 deletes gate-probe.yaml, T-11 is the #24 fix. All four prior answers carried as instructed. cycles_used 2, cost 44.81 measured + unmeasured segment-1 band = 57-90 of 120.
- 09:2x MAIN SESSION verified: SC-14 is falsifiable and RED today — `grep -c -i test_matrix .claude/skills/harness/SKILL.md` returns 0 at 635ef14 (as does `qa`). It is the one criterion review.yaml alone cannot satisfy, so the #24 fix is assertable rather than assumed. Both text defects the orchestrator flagged CONFIRMED: BRIEF.md:11 says "Five things" above 6 numbered rows (grep -c '^[0-9]\. ' = 6); PLAN.md:687 says the flip-delta touches 3 items "only" but also touches T-08.
- 09:2x USER DECISIONS (answers-replan-product.md): D-08 pm's recommendation ACCEPTED — qa runs in BOTH places (build segment writes+runs tests; panel step re-runs the matrix over the pinned SHA and authors nothing), cost of a double qa spawn accepted, PLAN does not shift so the flip-delta is NOT applied · BUDGET RAISED 120 -> 160 (+40 authorised; reasoning: the loop-back tail took every prior feature over its signed number, so authorise rather than merely report) · Q9 USER OVERRODE the orchestrator's recommendation — full architecture review of the rewritten PLAN BEFORE signature, not a scoped delta after. Staleness objection void because D-08 landed on the recommended branch. Standing instruction recorded: the user signs a plan an architect has already passed end to end.
- 09:2x CORRECTION to the main session's own statement: it told the user it would fix the two BRIEF/PLAN text defects in the approval pass. It cannot — team-config.yaml:18 grants the main session the `## Approval` blocks only, not the prose. Both routed to pm instead, to be folded into an already-open run rather than costing a spawn.
- 09:2x Re-spawned harness-orchestrator mission=resume for the full architecture review.
- 10:xx FEAT-06 pre-signature architecture review RAN and FAILED — 6 findings (arch-review-eng, 28.19). The user's override of the orchestrator's scoped-delta recommendation was VINDICATED: the orchestrator reports a delta would have missed at least 4 of the 6. What the review CLEARED: all six EMF remedies landed, the re-scope and its 8 decisions are sound, the retired task orphans nothing, and the qa two-places shape is architecturally coherent (no path leaves the matrix unrun). D-08 recorded DECIDED at PLAN.md:127; all three prose defects fixed; max_cost_usd now 160. cycles_used 2, cost 90.10 measured of 160 (102-135 with the unmeasurable segment-1 band).
- 10:xx MAIN SESSION verified both blocking findings at source and CORRECTED ONE. AMF-4: the digest called PLAN.md:536-537 "a live ACTION INSTRUCTION telling the documentor to build the UNSIGNED branch" — it is a CONDITIONAL whose antecedent is resolvably false from PLAN.md:127-132 (D-08 DECIDED, recommended branch, flip-delta NOT applied). Real defect, overstated severity; pm told to delete the stale clause and not plan a larger repair. AMF-5 CONFIRMED by demonstration: rendered T-11's own prescribed passage (PLAN.md:640-645) and ran both halves — test_matrix PASSES, co-occurrence returns ZERO matching lines, so SC-14 is RED on the correct passage. Lines are 93-98 chars against SKILL.md's 87-char mean; SKILL.md:38-39 already puts `validator` alone on a wrapped continuation.
- 10:xx ROOT CAUSE of AMF-5, recorded so it is not repeated: T-07 has 9 checks; 7 parse structured YAML where token-presence is correct, 2 grep markdown prose. Check (5) established "a line matching both `build` and `DEC-118`" — holds by luck at 2 tokens in one sentence. Check (8) reused the idiom at THREE tokens across a six-line passage without re-running it against the text pm itself prescribed in T-11. pm had already named this weakness at BRIEF.md:217 ("Markdown behaviour has no runner") and did not apply the caution to its own predicate.
- 10:xx MAIN SESSION ERROR OWNED: the "T-02, SC-04, T-07(1) and T-08 stand exactly as written" wording in answers-replan-product.md meant "do not apply the alternative branch" and was read as "do not touch this text", which is why AMF-4 came back as a question instead of a fix. Standing clarification issued to the orchestrator: a freeze on a signed decision freezes the DECISION, not the prose around it; a freeze is never a reason to ship a known error. NOTE also: a subagent read the dispatch's "outranks the freeze and I will take it up" as pre-authorisation and instructed "Action AMF-4 without waiting on an answer" — the orchestrator DECLINED, correctly, and flagged its own wording as the invitation.
- 10:xx USER DECISIONS (answers-arch-review-eng.md): Q1 freeze LIFTED, pm edits T-08 · Q2 YES reshape SC-14's co-occurrence half to a windowed match, `test_matrix >= 1` untouched, stated intent not weakened, both BRIEF and PLAN change · re-review is a DELTA over the six AMF sites (standing instruction satisfied by the full pass that already ran) · budget unchanged at 160. Re-spawned mission=resume: all six fixes in ONE pm run, then the delta.
- 11:xx FEAT-06 plan phase RETURNED PASS — signable. amf-fix-product 25.47, delta-review-eng FAIL 22.19, dmf-fix-product 13.95. cycles_used 4. THREE defects caught that a shallower pass would have shipped: (1) the architecture review's OWN AMF-6 remedy prescribed repr(), which emits single quotes and matches 0 in bin/ where the tree is double-quoted — corrected to json.dumps; (2) PLACEHOLDER_UNSET must be an ordered TUPLE (PLAN.md:234, byte-identical to validate-digest.py:472) or join order goes arbitrary; (3) DMF-1 — AMF-3's fix scoped THREE count-bearing comments in review.yaml; there are FOUR (:8, :9, :19, :62 — main session re-verified). T-02 would have shipped a 4-step file whose comments say three: AMF-3's defect class surviving AMF-3's fix. Remedy is a predicated general sweep, not a fifth enumeration, because enumerated lists had missed sites twice. review.yaml byte-unchanged. SC-14 measured twice (RED at 635ef14, GREEN on T-11's passage) by pm and independently by the reviewer.
- 11:xx BRIEF + PLAN SIGNED by Mike (main session wrote both ## Approval blocks; BRIEF.md:237, PLAN.md:183 verified pending beforehand, nothing committed, on main). 10 tasks, 15 SCs, closes #8 #9 #16 #24. 9 of 10 tasks main-session-direct/carve-out; T-08 (documentor, SPEC propagation) is the only squad-dispatched task. BUILD RE-AUTHORISED AT $100 — a NEW allowance for build+validate, NOT a raise of the $160 the plan phase consumed at 170.17 measured (182-215 band-inclusive). Plan-phase overrun bought the user's Q9 override (full pre-signature architecture review, 28.19) plus the two fix rounds and the delta it surfaced.
- 11:xx SIGNED WITH ONE RESIDUAL OPEN, recorded in PLAN.md's approval block: MF-1 — T-02's verify asserts step-ids/persona/mutates_repo/outputs but NOT the comment sweep DMF-1 added; prose-enforced at execution, asserted by nothing. Orchestrator declined a third pm run for it and disclosed that; user signed with it open, to be closed at T-02's execution if cheap there.
- 11:xx PERF TICKETS: user reviewed #18-#22 (+#7, #23 flagged) and chose ONE FEATURE IN FLIGHT AT A TIME — no perf planning starts until FEAT-06 ships. Grilling WILL run as step zero when it does. Collision that drove the call: FEAT-06's T-06 and T-11 both edit .claude/skills/harness/SKILL.md, which #18 and #22 also target; #21 depends on the qa shape FEAT-06's D-08 was still settling. #19 and #20 are collision-free but were held with the rest. <!-- ok-stale -->
- 11:xx /harness-ship FEAT-06. Precondition verified before spawn: BRIEF.md:239 and PLAN.md:185 both `status: approved`; check-state.sh exit 0 (one note, the inter-gate-q1q2 cost-accounting row). Spawned harness-orchestrator mission=ship, build budget $100 (NEW allowance for build+validate, not a raise of the consumed $160).
- 11:xx EXECUTION SHAPE passed down explicitly. Topological order pinned at PLAN.md:23 — T-01 -> T-02 -> T-04 -> T-10 -> T-05 -> T-06 -> T-09 -> T-11 -> T-08 -> T-07; PLAN file order is NOT topological. The build INTERLEAVES: T-07 (main-session) depends on T-08 (documentor-dispatched), so the direct tasks cannot all run first. Orchestrator instructed to hand work back in THREE BATCHES, not nine round trips: (1) the eight consecutive main-session tasks T-01..T-11 as one batch with each task's verify:, (2) its single T-08 dispatch, (3) T-07 after T-08 lands. Reason given in the dispatch: nine round trips would reproduce issue #18's defect on the feature that exists to close that defect class.
- 11:xx Also passed: gh-sync.py open to open the build (github.sync true, milestone number records to feature.yaml); the qa segment is the ORCHESTRATOR's to sequence (the #24 hole is in its own playbook until T-11 lands); PLAN.md anchors older than DMF-1 are stale (+8 through T-02's middle, +9 below the carve) so T-02 fields must be cited BY NAME; review.yaml byte-unchanged with all four count-bearing comments present; T-02's clause is a predicated SWEEP not an enumeration. Dead-ends restated: no plan re-review, no re-opening D-01..D-08 or SC intent, advisories/AQ-2/MF-1 are ship-acceptance backlog not build work.
- 12:xx FEAT-06 SEGMENT 1 COMPLETE — 8 of 8 main-session tasks PASS (T-01, T-02, T-04, T-10, T-05, T-06, T-09, T-11), all three gates exit 0. SC-14 measured BOTH WAYS: at 635ef14 test_matrix lines=0 and the 8-line window RED; now lines=2 and GREEN. T-01's SC-03 diff is byte-identical before/after with no INV-6 lines, as predicted.
- 12:xx BLOCKER cleared before T-01's before-capture: check-docs.sh was RED on the main session's OWN log line (.harness/logs/2026-08-04.md:26 quoted "ONE FEATURE IN FLIGHT AT A TIME", colliding with the DEC-120 superseded pattern). Escaped per line with <!-- ok-stale --> (check-docs.sh:134). Legitimate quote of a user scheduling preference, not a design claim. Log writes were then FROZEN until T-01's capture->fix->rerun bracket closed, so no docs-line difference could enter SC-03's diff.
- 12:xx THREE DEFECTS CAUGHT DURING EXECUTION, each would have shipped green. (1) THE BIG ONE: T-05's rewrite would have scanned NOTHING — Python's glob does not descend into DOTTED directories, so `<root>/**/*.yaml` from the repo root matches 0 files while the tree holds 54 (measured, not assumed). Switched scan() to os.walk; now 54 + 2 = 56 across both roots. This is exactly the vacuous-pass the corpus-is-not-empty assertion exists to catch. (2) SC-06 PROVEN TO DISCRIMINATE: ran the new teams/ fixture against the PRE-widening scan() in a clean worktree at 635ef14 — 0 files scanned, 0 findings, so the check FAILS without the widening. (3) The main session's own comment in validate-digest.py used PLACEHOLDER_UNSET by name, making grep -c return 2 where T-01's verify demands 1; reworded.
- 12:xx T-02 SWEEP FOUND A SIXTH SITE, self-inflicted: the handoff named four count-bearing comments, the orchestrator re-grepped and found a fifth (review.yaml:5, "a fourth dispatched step"), and the main session's OWN new qa comment added a sixth ("the other three steps" — true in a four-step file, stale the moment a fifth is added). Reworded to "the other reviewer steps". Completeness re-grep leaves only SPEC section numbers (§10.4, §2.3), which are not counts. Vindicates the predicated-sweep remedy over a fifth enumeration.
- 12:xx LINE BUDGETS: T-06 used 8 added lines of its 12 cap (not the 12 the orchestrator assumed), so T-11 had 12 of headroom rather than 8. Combined SKILL.md diff vs 635ef14 is 14 added / 0 deleted against a cap of 20. Zero deletions = no reflow, which is what keeps numstat honest.
- 12:xx FILED #36 (user-directed): run-unit-tests.sh aborts with a bogus MISCONFIGURED error naming a literal `test-*.py` when run from any directory but the repo root — BIN_DIR is relative and there is no nullglob, so an unmatched glob stays literal and is reported as an unregistered script. Pre-existing, confirmed at 635ef14 in a clean worktree. Fail-closed, so nothing is silently skipped; it is a false alarm with a misleading diagnostic.
- 13:xx FEAT-06 T-08 landed clean first pass (t08-product PASS, 11.39); orchestrator re-verified every gate at its own tier rather than accepting the lead's relay (product-lead holds no Bash). Commits f45fd0f (segment 1, eight tasks) and 510b7ff (T-08). Issues #26-#30, #32-#35 closed on the mirror. T-07 check (9)'s trap did NOT fire: SPEC.md:1978 has 2 brace groups and exactly 1 bearing the parallel marker, because the lead put the two-jobs clause in the notes column rather than the DAG cell.
- 13:xx T-07 DONE by main session — the last task, 10/10 checks, run-unit-tests exit 0 with test-team-catalog.py registered and named in the output. ALL TEN CHECKS PROVEN TO DISCRIMINATE: copied the same checker into a clean worktree at 635ef14 and ran it against the pre-feature tree — 10 of 10 FAILING there, 10 of 10 passing here. Not one is stale-green.
- 13:xx T-01's COUPLING RE-RUN and holds: `grep -rn '"none", "null", "n/a"' bin/ | wc -l` still returns 1 with a NEW file in that directory, because check (6) CONSTRUCTS its needle from harness_yaml.PLACEHOLDER_UNSET via json.dumps rather than embedding the literal. This is the conjunct that would have gone 1 -> 2 and broken itself by existing; passing it at T-01 time proved nothing about this moment.
- 13:xx BUILD PHASE COMPLETE: 10 of 10 tasks PASS, ZERO send-backs, zero rework cycles. Gates all exit 0. Build cost 42.89 measured of the 100 allowance — understated by construction, since 9 of 10 tasks ran at depth-0 in the main session and depth-0 is not separable to a feature.
- 14:xx SC-05's count conjunct CLOSED (user chose "assert the count exactly" over recording it half-automated). qa-validator's Q1 was correct and it was the MAIN SESSION's gap, not qa's misreading: T-05's intent asked only that the count COVER both roots and that not-empty be per-root; it never asked for == 2, so the under-specification was in the approved PLAN, not the execution. Added TEAMS_EXPECTED = 2 + one check() to test-harness-yaml-corpus.py. PROVEN BOTH WAYS: green on the real tree (13/13), and RED on a throwaway root whose teams/ holds three files, with the failure naming the directory contents. Cost accepted and recorded in the code comment: this freezes teams/ at two, so a legitimate third team turns it red — which is the intended prompt to revisit SC-05 rather than silently widen the number.
- 14:xx User chose to run validate STRAIGHT THROUGH to the CEO briefing (pm goal-check, then briefing) rather than stopping after the goal-check. SC-13 (verify: uat) still gates the feature and is the user's to run at ship acceptance — gates.uat: blocking_when_uat_criteria_exist, and panel-validator flagged that a review PASS must not be read as clearing it.
- 14:xx FILED #37: adequacy_notes is load-bearing across tiers and absent from the digest schema. 8 digests across 5 features emit it; validate-digest.py's `lead` schema (:95-97) does not list it, so a lead omitting it passes and one including it passes as an unvalidated extra. Already CONSUMED cross-tier: FEAT-03 2026-07-31-13-product/digest.md:51-54 shows product-lead reading validator-lead's adequacy_notes, finding two tiers contradicting, and re-verifying the premise itself rather than routing on pm's word. None of must_fix / open_questions / escalations can absorb it without lying — the missing concept is a QUALIFICATION ON A PASS, the same shape DEC-173 fixed for "nothing happened". Second half of the ticket: validate-digest.py:643 instructs "prose assessment below the block" while grep '## Assessment' across .claude/skills/ and .claude/agents/ returns ZERO — nothing is ever told to read it, so placement silently decides whether content survives. Origin recorded: DEC-126 (DECISIONS.md:2871) assigned leads "severity_max + adequacy_notes"; severity_max got wired into the validator, adequacy_notes did not.
- 15:xx USER CHALLENGED build.yaml's `personas:` at the ship gate — "why are we listing the members again when it's in team-config.yaml?" Investigated at source. THREE FINDINGS, all verified: (1) NOTHING reads it at runtime — `personas` appears nowhere in harness-team/SKILL.md or harness/SKILL.md, the expansion routes by `persona: by_consult_when`, and review.yaml has no equivalent key; the ONLY consumer was test-team-catalog.py checks (3) and (4), so the field existed to be asserted and the assertion existed because the field did. (2) THE GUARD POINTED THE WRONG WAY — check (3) asserted listed <= Engineering and nothing asserted Engineering <= listed; simulated a sixth engineer joining team-config and check (3) STILL PASSED with build.yaml stale. (3) The header comment contradicted its own list — comment (c) claimed the set was derived from 2 recorded eng runs and was "a FLOOR, not a closed set" while the list was all five Engineering members copied from team-config. Origin: PLAN.md:408-410 instructed the field; the main session wrote what the approved plan specified rather than questioning it.
- 15:xx USER CHOSE THE EXPENSIVE PATH DELIBERATELY: delete the field and amend the BRIEF, over the offered cheap path (shrink the list to the observed floor {dev-ops, backend-dev}, which would have kept both SCs valid with no BRIEF change). Code done: personas: deleted, comment (c) rewritten to say the file deliberately does not list personas and why; test-team-catalog.py checks (3) and (4) rewritten to read team-config.yaml directly — (3) now asserts the single-squad property via the LEAD's squad (a lead cannot dispatch outside its own squad, DEC-118, so an eng-lead-hosted team is eng-squad BY CONSTRUCTION, no roster copy needed) and (4) asserts the Engineering squad covers the recorded floor. STILL EXACTLY TEN CHECKS, as T-07's signed verify requires. Gates all 0.
- 15:xx BOTH NEW CHECKS PROVEN TO DISCRIMINATE INDEPENDENTLY: lead -> a non-eng lead fails (3) and not (4); backend-dev renamed out of Engineering fails (4) and not (3). The new pair catches what the old pair structurally could NOT — a squad-membership change now reddens a test. TWO PROBE BUGS found and fixed before the result was trusted: `open(p,"w").write(open(p).read())` truncates before reading and emptied the fixture (4 checks failed on a None), and deleting a member line orphaned its sub-keys and broke the YAML. Both made the checks LOOK coupled when they are not; stopping at the first red would have shipped a false "these discriminate" claim.
- 15:xx BRIEF AMENDMENT ROUTED TO PM (not the main session — team-config.yaml:18 grants it only the ## Approval blocks). SC-07 and SC-08 both PRESUPPOSE the deleted field: SC-07 now passes VACUOUSLY (an absent set is trivially a subset — this feature's own charter defect) and SC-08 is UNSATISFIABLE (an absent set covers nothing). Answers file notes/answers-ship-gate.md specifies the substance each must assert instead, forbids weakening to presence assertions, and asks that PLAN.md:408-410 and D-03 be corrected in the same run so the plan does not describe a field the shipped file lacks.
- 15:xx SC-13 (UAT) STILL OPEN — user is reading build.yaml and SKILL.md:40-53 in VS Code. Not marked, not assumed. Backlog items 2,4,5,6,7 + routing-wall recurrence 7 remain UNFILED by user instruction, to be created in one pass at ship acceptance alongside whatever they strike; #36 and #19 already filed; #37 filed after the briefing was written and so is absent from its table.
- 16:xx FEAT-06 AMENDMENT LANDED (amend-product ESCALATE 13.48 + amend2-product PASS 13.48, commit d79b3e7). THE TWO DELETIONS CAUGHT THREE LIVE DEFECTS, not tidying: (1) SC-07 had ALREADY started passing VACUOUSLY once personas: was gone — an absent set is trivially a subset; (2) SC-08 became UNSATISFIABLE — an absent set covers nothing; (3) T-04's SIGNED verify: was RED on two keys in succession, KeyError 'personas' then KeyError 'filter' — a signed check that CRASHES, inside the plan of the feature whose charter is removing checks that appear to exist and do nothing. T-04's verify now asserts ABSENCE ('personas' not in d and 'filter' not in sf), re-run by the main session at re-signature: exit 0.
- 16:xx SITE LISTS SHORT FOR THE THIRD TIME on this feature: 2 personas sites handed vs 5 actual (including T-04's verify, the one that crashed); 4 filter sites handed vs 6 actual, with :431/:491 already drifted to :442/:515. Earlier the comment sweep was handed 4 vs 5 actual. THE LAYER A SITE LIST FORGETS IS THE VERIFICATION CRITERION. Recorded in both approval blocks as a pattern, not an incident.
- 16:xx The "fold into the same pm run" addendum did NOT land in run 1 — the first pm run had already closed, and it correctly ESCALATED the filter deletion rather than ratifying a mid-run change it could not verify was intended. That escalation was right and cost a second run. Orchestrator also broke its OWN feature.yaml with an unquoted `personas:` inside a plain scalar (the exact defect class this feature closes), caught by the parser and rewritten as block scalars; and nearly skipped metering two runs because `grep -q "^cost:"` matched the pending_orchestrator PLACEHOLDER — the DEC-156 trap, caught before it became an INV-11 violation.
- 16:xx NEW FINDING, measured not inferred: validate-digest.py run against PLAN.md returns BLOCKED (contract violation), so the validator DOES catch a non-digest artifact — but the SubagentStop hook is NOT invoking it on member `artifact:` paths. A gate that exists and does not fire. Backlog item for ship acceptance.
- 16:xx SC-13 (uat) MET — user read build.yaml and SKILL.md:40-47 and ruled they describe builds the way builds should be dispatched. THE CRITERION DID ITS JOB BEFORE THE RULING: the same review deleted `filter: eng_squad_tasks` as dead weight and corrected wording that read as the lead doing no routing. Recorded honestly as a DESIGN READ, NOT A RUN OBSERVED — build.yaml has never been dispatched, including by this feature (empty eng-squad task list), which is declared in BRIEF ## Verification gaps and stands.
- 16:xx BRIEF and PLAN RE-SIGNED by Mike through the main session, as `## Re-signature` sections appended to each existing approval block (the originals said "approved as written", which was stale for SC-07/SC-08 and for T-04/T-06/D-03). Both name exactly what changed and why. Cost at re-signature: 252.63 measured of the 100 build+validate allowance = 2.5x, a MULTIPLE not a percentage; the orchestrator's own session is 139.32 of it (55%), larger than every squad combined — DEC-148's square-of-session-length effect measured. Orchestrator raised it per DEC-134 rather than only reporting it; lever it names is ending orchestrator sessions at phase seams.
- 17:xx FEAT-06 SHIPPED, PR MERGED. gh-sync ship closed parent #25 + milestone #1. PR #45 merged to main at 3bfedc9 (fast-forward, branch deleted). Gates on merged main: unit 0, docs 0, check-state 0. Branch naming hook rejected `feat/team-layer-inv6` — it requires an issue number or flow id; used feat/FEAT-06-team-layer-inv6.
- 17:xx DEFECT FOUND BY SHIPPING, filed as #38: `gh-sync.py ship` NEVER READS `closes_issues`. FEAT-06's feature.yaml declares "#8 #9 #16 #24" and all four were still OPEN after the sanctioned ship command — grep for closes_issues in gh-sync.py returns nothing; cmd_ship (:494-519) closes the milestone unconditionally and the parent conditionally, and nothing else. Closed all four BY HAND with evidence comments. Sixth instance in a week of a declaration that appears to do something and does nothing — found by running the ship, not by reading the code.
- 17:xx BACKLOG FILED IN ONE PASS at ship acceptance per the user's instruction: #38 (gh-sync closes_issues), #39 (SubagentStop does not run validate-digest on member artifact: paths — measured: the validator DOES reject a non-digest when run directly, the hook simply is not invoking it), #40 (DECISIONS.md shows the panel three-wide), #41 (panel qa step is gate-only by PROMPT ONLY — harness-qa still holds Edit/Write, so a signed decision rests on an advisory boundary), #42 (harness-qa has NO writable test surface — routing wall recurrence 7, filed separately from #20 because a perfect plan-time resolver would correctly report "no eligible member" every time), #43 (T-09 line-cap estimate + stale 3x quote in PLAN.md), #44 (code-reviewer under-reported its own severity_max and returned expertise_update outside a distillation).
- 17:xx FEAT-06 FINAL: 10/10 tasks first-pass, zero build rework, 15/15 SCs met including the UAT. Plan phase 170.17 of 160; build+validate 252.63 of 100 (2.5x), of which the orchestrator's own session was 55% — larger than every squad combined. The single most actionable lever the feature produced about itself: end orchestrator sessions at phase seams (DEC-148, now measured).
- 2x:xx PERF BATCH 1 grilled and spawned. Scope: perf-doc rows 1/2/5 = issues #19 (dev runs its task's `verify:`), #18 (batch the user's rulings at the signature gate), #22 (probe bounded environment questions, never infer). Grilling artifact `notes/grilling-perf-batch-1-2026-08-04.md`. User rulings: #19 gets an ENFORCED digest field `task_verify: pass|fail|n/a` (not prose) with `fail`/`n/a` + PASS rejected mirroring DEC-173 and NO dev-ops carve-out; dispatch carries task id AND the verify command; #18 lands on the MAIN SESSION signature gate, not the orchestrator playbook — its filed surface is contradicted by `logs/2026-07-31.md:4,5`, where the rulings were sign-off change requests, not answers to open questions; #22 lands on main session + orchestrator only, NOT `harness-handoff` (16-spawn preload cost). Route-resolved at plan time on purpose: docs/** -> documentor, bin/** -> backend-dev|dev-ops, the four unowned `.claude/**` rule surfaces -> declared MAIN-SESSION steps. `validate-digest.py` half is DEC-174 carve-out, direct execution. Three propagation sites `check-docs.sh` cannot see (SPEC.md:1054-55, test-validate-digest.py x7 fixtures, harness-tdd-enforcement/SKILL.md:70-72). OUT: #20, #21, perf-doc row 10 (run counting) — consequence accepted: no instrumented way to say which lever paid. Spawn: orchestrator mission=plan, id coined by pm.
- 2x:xx FEAT-07-verify-teeth-batch-probe plan returned ESCALATE/awaiting_user: BRIEF+PLAN pending, 9 tasks / 12 SCs, $44.57 of $120, cycles 0, zero send-backs. USER GATE TAKEN AS ONE PASS (the batching rule this feature installs, applied to itself — four decisions, one consolidated fix, no per-ruling dispatch). RULINGS: (Q1) the receipt clause is IN — self-reported `task_verify` did not meet the "mechanically ungameable" mandate, so the dev's B-7 receipt must carry the verify command and its verbatim output; honest limit recorded, it makes skipping leave evidence rather than impossible, and the lead-re-runs alternative is structurally impossible since leads hold no Bash. (Q2) REVERSED the plan's D-01 tradeoff — the pre-existing `suite: fail` + `VERDICT: PASS` fail-open is folded into T-01, not filed as backlog; main session re-measured it independently (dev digest with suite: fail + PASS returns `digest ok` exit 0, cause is GATE_FIELDS at validate-digest.py:481 nested inside the PLACEHOLDER_UNSET branch at :477); behaviour change to be stated plainly in BRIEF; dev-ops must not gain `suite`, D-03 stands; pm must rule explicitly on qa's matrix_ok. (Q3) architecture review DISPATCHED — eng-lead as leaf reviewer, run AFTER the revision so it sees the plan the user signs; FEAT-06's equivalent returned six must_fix. (Q4) user raised nothing further. Answers at notes/answers-plan-product.md; orchestrator RESUMED in-phase rather than re-spawned (same plan phase, DEC-159 boundary not crossed, saves a fresh preload). Main session verified before relaying: gen-decisions-index.py --stdout == DECISIONS-INDEX.md byte-for-byte, DEC-174 last at :4680, 174 rows.
- 2x:xx FEAT-07 plan returned ESCALATE/awaiting_user round 2: 4 runs, cycles 3, $180.37 of $120 (1.5x over, crossed at the arch review the user ordered — review $30.49 + its fix $55.87). USER GATE TAKEN AS ONE PASS again (four rulings, one consolidated fix). (D-07) REDIRECT — the `task: T-NN|none` alternative, NOT the recommended `no-task` fourth enum value: `no-task` reinstates a self-declared bypass with no receipt obligation, the shape the Q1 receipt ruling rejected one round trip earlier, and PLAN.md:160-163 itself says it is cheaper to abuse than a false `pass`. Accepted cost: ~2x T-01's diff, a new conditional-requirement mechanism inside a DEC-174 file, propagation to T-02/T-03/T-06 and nine fixtures. The finding was the arch review's, and it PROVED itself — the reviewer was a harness-backend-dev returning PASS with no PLAN task, legal only because T-01 has not landed; SubagentStop matcher `harness-.*` means routing cannot dodge it. (Cost) budget RAISED, $400 proposed, recorded as a decision not a re-baseline. (Index) main session committed docs/harness/DECISIONS-INDEX.md ALONE at 4091b36 before the feature branch — 57 of 174 rows, all delta exactly +6, boundary DEC-118, DECISIONS.md unchanged; drift was COMMITTED at 3bfedc9 and check-docs.sh cannot see the class. MY EARLIER "no pre-existing drift" WAS WRONG — measured after an undeclared agent edit had already regenerated the file; corrected to the user. The arch review's "two independent edits" was also wrong (transposed pair); the orchestrator's measurement was right and I reproduced it. (pm grant) harness-pm has no receipt-* grant in team-config.yaml:87-97 while all five specialists do (:144,:158,:171,:184,:199) — pm obeyed check-domain.sh, landed at a granted path and raised it; filed as issue #46, out of scope, DEC-174 fix surface. Orchestrator resumed in-phase a second time.
- 2x:xx FEAT-07-verify-teeth-batch-probe BRIEF + PLAN APPROVED by Mike (one signature, no prototype — zero end-user surface, prototype gate independently confirmed not to fire). 10 tasks, 18 SCs, 8 decisions. Plan phase CLOSED at $242.48 over 5 runs / 4 cycles; max_cost_usd set to 550 by the orchestrator over the main session's proposed 400, and the user accepted 550 — its own stated weakness recorded: at 550 it is an upper bound, not a target. D-08(b) ruled LENIENT (`task: none` + `task_verify: n/a` stays legal). The ruling was taken on a main-session check that CHANGED the framing: the strict form does NOT shrink the bypass — fixture (j2-i) accepts `task: none` + `task_verify: n/a` and (g2) accepts `task: none` + the field omitted, so a dev on a real task can declare `task: none` under EITHER form because the validator never sees the dispatch. The actual control is T-05's dispatch-carries-the-T-NN-id rule plus the lead comparing, which is exactly what the D-07 redirect bought and works identically in both forms. So the choice was contract consistency (DEC-121: a field is never said with silence), not gate strength — advisor had flagged it as blocking on the assumption strictness was tighter; the check disproved that. Orchestrator Q2 (nine surviving `no-task` mentions) RESOLVED with no action: PLAN.md:1148-1160 enumerates all six absence clauses, each labelled a scope guard and paired with a presence check that goes red at base — the plan's own DEC-169 standard applied to itself. Q3 (T-04 hardcodes `task: T-12`) resolved: generic docs example inside the TDD skill, forced because TASK_ID_RE rejects the literal `T-NN`; FEAT-07 runs T-01..T-10. Gates after signature: check-state.sh exit 0, check-docs.sh exit 0, run-unit-tests.sh 10/10. THREE user round trips for the whole plan phase, each applied as ONE consolidated fix — the batching rule this feature installs, used on itself; FEAT-03's counter-example was seven serialized runs. /harness-ship unblocked, NOT started.
- 2x:xx spawn harness-orchestrator mission=ship — FEAT-07-verify-teeth-batch-probe (approved 2026-08-04, 10 tasks / 18 SCs / 8 decisions). Branch feat/FEAT-07-verify-teeth-batch-probe created off main at 4091b36 by the main session BEFORE the spawn; DECISIONS-INDEX.md stays committed alone on main and is declared off-limits. github.sync true, so the mirror opens with gh-sync.py open. LANE SPLIT: only T-06 and T-09 are squad-dispatched (product-lead -> harness-documentor, docs/**); the other EIGHT are main-session-direct on unowned .claude/** surfaces and come back to me as work. Dispatch instruction: return ONE consolidated segment of dependency-satisfied main-session tasks with each verify: verbatim, NOT one round trip per task — this feature's own batching lever applied to the main session's own loop, against FEAT-03's ~$95 serialized counter-example. T-01 flagged as the risk: DEC-174 carve-out, roughly double its original diff after the D-07 redirect, and the one task with no member review during the build. Budget warning carried down: cycles_used already 4 of 10 from the plan phase and max_total_cycles is HARD, so six cycles remain for build+validate+fixes; orchestrator told to raise it early rather than at the wall.
- 2x:xx WAYFIND CHARTED — effort map #60 "Board answers what's next". First use of wayfinding TRACKER MODE in this repo (5 wayfinder:* labels created; wayfind.py has no test coverage, issue #14). Destination, user-chosen over two larger alternatives: open project #3 and read WITHOUT starting a session what is in flight, what is groomed and ready, and what is undecided — the harness keeps it accurate as a side effect of running. Explicitly OUT OF SCOPE and recorded on the map: making GitHub the system of record; .harness/features/*/ stays primary and gh-sync.py stays outbound-only. Settled pre-chart: "grooming" = BOTH refining an item into plannable work AND ranking it, one pass. Seven tickets: research #61 (what Projects v2 can express on this account), #62 (what DEC-138's one-way rule actually forbids, and what already reads GitHub back), #63 (enumerate loop points that know a task started / validation opened); grilling #64 (lane encoding — milestone vs label vs board field), #65 (what mechanically makes an item Ready), #66 (when grooming runs and who runs it), #67 (does planning consume the board ranking — the one architectural decision). Blockers wired: 64←61, 67←62, 66←65. Frontier = #61 #62 #63 #65. Three research agents fired in parallel per DEC-167. Charting STOPS here — resolving is the next session. Related open tickets not on the map: #59 (gh-sync sets Status; Done already automatic via built-in Item closed workflow, measured on #52/#54/#55), #58 (remove cost entirely), #46 (pm has no receipt-* grant). Usability note, not filed: `wayfind.py block` dumps the full issue JSON to stdout — noisy, cosmetic only.
- 2x:xx WAYFIND #60 — all three research tickets resolved and gisted; frontier now #64 #65 #67, all grilling, all the user's. #62: DEC-138's one-way rule is NOT about direction of flow and NOT about GitHub being canonical (DEC-167 makes GitHub canonical in tracker mode) — it forbids a read reaching an APPROVAL-GATED artifact with no human signature between (am.6 DECISIONS.md:4334-35). Determinism test: if re-running would yield a different gated artifact because GitHub moved, the read is forbidden. So a board ranking is LEGAL as pm's ranked recommendation the user picks from (am.2 :3298-3300) and ILLEGAL as a programmatic input to PLAN. Amendments are NON-CONTIGUOUS — am.1-4 at :3270-3314, am.5-7 ~1000 lines later at :4282/:4309/:4337; reading forward from @3244 gets 4 of 7 and the wrong answer. gh-sync.py:13 OVERSTATES its own contract ("never reads GitHub state back") — falsified by :355 and :412 in the same file; verified directly by main session, behaviour fine under am.6, comment absolute where the decision is not. Regression guard holds: parent_args/blocked_by_args called only from wayfind.py:150,286, zero sites in gh-sync.py. #61: workflows are UNCONFIGURABLE AND UNREADABLE via API (delete-only mutation; ProjectV2Workflow exposes no trigger/action field) — any design depending on asserting a built-in workflow's action is dead. CORRECTION to my earlier claim and to the map: every item carries an EXPLICIT Status, Backlog is a set value not an unset field; re-measured 42 items = 36 Backlog / 2 In progress / 4 Done. Parent roll-up ALREADY answers "what is in flight" free: #47 subIssuesSummary 3/10 and #60 1/7, both reading In progress, the only two non-Backlog non-Done items. Milestone ALONE cannot express the lane — absent on exactly the two parent rows that answer the question. #63: the "in flight" moment is already on disk — the lead writes dispatched_at keyed by {{task_id}} immediately BEFORE each spawn (harness-team/SKILL.md:78-80); "in review" is review_sha's INV-6 pin. Leads hold NO Bash so they can only write and something else must observe; the orchestrator does hold Bash. SubagentStop now validates a task id on EVERY harness return (validate-digest.py:135, landed by FEAT-07 T-01). A1 SubagentStart payload contents are NOT determinable from this repo — needs a bounded probe, the same class FEAT-07's #22 rule governs.
- 2x:xx FEAT-07 segment 1 COMMITTED by the orchestrator: 0a34989 [harness:t-05], 7da58c6 [harness:t-07][harness:t-08]; issues #52/#54/#55 closed. T-06+T-09 ran as docs-product PASS, zero send-backs, $108.99. cycles 5/10, cost 351.47/550. T-01 is NOT committed: its validator half is correct and re-verified at the orchestrator's tier, but its FIXTURES miss three cases PLAN step (11) enumerates — (f)'s reviewer half, (i2)'s hint-CONTENT assertions, and (j2-ii) joint hint followability. MY ERROR, corrected: I told the user PLAN's fixture steps do not specify a joint-hint case. They do, at PLAN.md:457-462, as a required half of (j2) — I had read that block. SC-17/SC-18 are `verify: automated evidence: unit`, so a by-hand run is not unit evidence and all three SC-18 clauses plus SC-05's reviewer half would have come back unmet at the goal-check. Segment 2 queued: 2a T-01 fixtures ONLY (validate-digest.py is now cited by nine DEC-175 line anchors and must not be touched again), then T-04, T-02, T-03, T-10.
- 2x:xx FEAT-07 USER RULING: KEEP DISTILLATION. Reason recorded: three of this feature's sharpest lessons came from the observation-log-to-Expertise path distillation feeds, so the spend buys the next feature's starting position rather than narrating this one. Budget consequence ACCEPTED not waived — 478.93/550 before the goal-check (~30) and distillation (~60-100), so the crossing is a decision on the record. max_cost_usd NOT to be re-baselined; DEC-134 keeps cost informational so a crossing stays visible.
- 2x:xx FEAT-07 fix segment done by main session (F1/Q2/F3), all gates green: run-unit-tests EXIT=0 (first green since T-09), check-docs EXIT=0 (45 patterns/184 files), test-validate-digest 60/60 CLI + joint-hint + 14/14 hook + 2/2 template. F1 was MY error: SC-16 requires the receipt clause on EXACTLY ONE surface and I wrote it into both harness-digest-dev and harness-tdd-enforcement during T-02; D-06 pins it to tdd-enforcement. Deleted the digest-dev copy — remedy (a), which makes SC-16, D-06, commit 4e2b57f's message and DEC-175 all true without touching approved text. T-02's eleven clauses re-run and hold, task_verify now exactly 3 on the >=3 boundary; T-04's awk clause re-run EXIT=0. Q2 closed SC-06's proof gap — it names FOUR accepted refusal shapes (dev/dev-ops x BLOCKED/FAIL) and only one was fixtured, and SC-06 is `verify: automated evidence: unit` so hand-reasoning satisfied it neither way; CLI cases 57->60. F3 corrected a rotted self-anchor at validate-digest.py:572 (`:691-692` -> `:838`, moved by T-01's own +147 lines), in-line so the file stays 899 lines and DEC-175's nine anchors hold.
- 2x:xx DEC-174 HUMAN DIFF READ — happened, NOT formally discharged, and its limits are on the record. The user read validate-digest.py:130 (_unbound's `""` default) and :642 (the re.Pattern branch), settled `str(None).lower() == "none"` empirically, and RULED that failing closed at :130 is correct. What the read did NOT catch: F3's stale anchor, sitting in that same file through the whole review — found by the code reviewer instead. Calibration to carry forward rather than a failure: the human caught the judgment call (fail-closed direction, which no test can decide), the reviewer caught what eyes skim. Neither substitutes for the other. Also corrected in that file while preparing the read: a GATE_FIELDS comment of mine asserted "no verify applies is never the honest answer" — the exact phrase T-03's scope guard bans, and FALSE since D-07 because with `task: none` that is precisely what is honest; fixed line-count-neutral.
