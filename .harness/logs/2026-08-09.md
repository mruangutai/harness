- 07:13 FEAT-10 T-01 executed main-session-direct: .harness/factory/fleet.yaml written, verify passed, three stations cross-checked against live board 3. Task status left pending — the build orchestrator owns task state.

## FEAT-10-software-factory · ship · spawn

Main session spawned one harness-orchestrator, mission ship. Preconditions verified at source:
BRIEF ## Approval and plan.yaml approval.status both `approved` (Mike Ruangutai, 2026-08-09);
board 3 Status now carries Building and Review, so the T-01 live pre-condition is satisfied;
T-01 `done` in plan.yaml, T-02..T-12 `pending`.

check-state.sh exits 1 with 4 violations, all pre-existing FEAT-04/FEAT-07 lead digests. Not
FEAT-10, not fixed inside this mission — validate-digest.py is enforcement layer under DEC-174.

DEC-174: T-08 (check-state.sh + test-check-state.py) is main-session-direct and withheld from
the orchestrator. T-01 was the other, already landed by hand in 8a2cbca.

## Doors collapsed, and #202 filed

Main session, direct edits under DEC-174. Deleted commands/harness-wayfind.md and
commands/harness-grill.md after migrating the three facts the skills lacked. Hid
harness-grilling with user-invocable: false and rewrote harness-wayfinding's entry test to run
the interview in place, so the single door never bounces the user to a hidden one. Repointed
every inbound ref. Deleted DEC-161 per the operator, cleared its deploy.sh citation, removed the
orphan index row, regenerated. check-docs.sh exit 0, run-unit-tests.sh ALL PASS.

Operator ruling: remove the <!-- stale: --> mechanism entirely; a decision the tree flatly
contradicts is struck, not marked. Deferred until FEAT-10's orchestrator returns. Filed as
issue #202, P0, with the full change list, the INV-10 fail-closed blocker, and the cascade to
DEC-103/104/181 plus DEC-165's narrow strike.

## FEAT-10 · operator ruling · github mirror

Asked whether FEAT-10's T-NN task issues should appear on board 3. They do not, and
feature.yaml records the skip as deliberate: gh-sync.py and the factory both publish T-NN into
mruangutai/harness, so running both yields two issues per task.

Operator ruling: WAIT FOR THE FACTORY TO OWN IT. gh-sync.py open stays unrun for FEAT-10; T-04
is the owner. Absent T-NN issues are not a defect and must not be closed by the legacy mirror.
Recorded at notes/answers-github-mirror.md, to be relayed into the CEO briefing.

## FEAT-10 · ship · orchestrator returned

VERDICT ESCALATE, status awaiting_user. 11 of 12 tasks landed across 4 runs, zero send-backs.
cycles_used 8 of 10 — all from the plan phase, none spent building. qa gate BLOCKED on
harness.json's functional kind (cmd null AND detect glob matches zero files). Panel PASS
(severity_max med), security PASS (info). Goal-check ESCALATE: 19/20 or 16/20 depending on bar.
Briefing at notes/ship-review-build-2026-08-09.md, presented verbatim.

Correction to the briefing's D-4: the two staged deletions of .claude/commands/harness-grill.md
and harness-wayfind.md are MINE, made by the main session under DEC-174 earlier this session.
Not an unexplained index entry and not a rogue agent. The orchestrator was right to flag them
and right that it could not account for them.

## FEAT-10 · step 1 and 2 landed by the main session, direct under DEC-174

T-08: INV-24 added to check-state.sh with 8 cases in test-check-state.py. Proved RED against a
mutant with INV-24 stripped — 5 of 8 cases went red; the 3 that stayed green assert silence.
T-08's own verify command passes. 93/93 integration checks.

DEC-187 written and index row filled: the test matrix is per-project, and a kind with no runner
is excluded by decision, never inferred. Settles the eight-file null-cmd contradiction toward
BLOCKED. Config edit signed: functional removed from api/cross_module/feature always lists,
kept in test_kinds with status excluded and signed DEC-187, status added to all seven kinds,
_matrix_provenance added with the template-at-DEC-187 baseline named.

Gates: check-docs.sh exit 0 (301 files), upgrade-config --check clean, index matches authority,
full suite 93/93. check-state.sh went 4 -> 5 violations; the new one is FEAT-10's own
panel-validator digest, not the config change.

Re-spawning the orchestrator, mission resume, to re-run the qa gate.

## FEAT-10 · live verification against a real repo — the gap SC-07's deletion left

Ran the full journey twice against mruangutai/harness-factory-smoke and a throwaway board 4.

RUN 1 found a BLOCKING production defect stubs could not catch: factory_gh.py passed the board
NUMBER to `gh project item-edit --project-id`, which takes the GraphQL node id. Every station
change was dead — publish could not board at ready, claim could not reach building, land could
not reach review. Measured both directions by hand. Fixed by eng, test-first, red-proved.

RUN 2, on the fixed tree with no patch of mine: publish exit 0 (parent #10, task #11 at Ready),
claim exit 0 (#11 -> Building, branch factory/issue-11), workspace exit 0, land exit 0 (PR #12
opened, #11 -> Review). All four stdouts parse in one json.loads.

Also verified the second fix: a plan with no top-level `feature` key now exits 2 with a real
message and makes ZERO remote calls (issue count 4 before and after). Previously it wrote the
literal label `feature:None` and exited 0.

Goal-check: 20 met / 0 partial / 0 not_met on the strict bar. Suite 97/97. cycles 10 of 10.

## PENDING before the FEAT-10 PR — operator instruction

Separate the OMP port from the FEAT-10 branch before opening the PR. `feat/FEAT-10-software-factory`
carries THREE commits and only one is OMP:

  2a3e91c  door consolidation (unrelated to both)
  b89c00a  THE OMP PORT — separate this one, unfinished by the operator's own account
  8bbb246  FEAT-10

Sequence agreed: panel returns -> separate OMP -> push -> PR -> CI `integration` green (the only
gate that can block; enforce_admins is true and a direct push to main is rejected) -> operator
merges. #202 starts only after that, branching off a main that already carries FEAT-10's
check-state.sh and DECISIONS.md, which removes the overlap.

Also open: cycles at the 10 ceiling (raise to 12 or overturn to 8), and the throwaway repo
mruangutai/harness-factory-smoke needs `gh auth refresh -h github.com -s delete_repo` before it
can be deleted. Board 4 already deleted.

- FEAT-10 · a1fix-eng returned PASS. A1 closed, red proven by injecting the pre-fix module
  (17/172 failing), green at 22 test files / 0 FAIL. Five questions back, four blocking.
- FEAT-10 · operator ruled all four in one pass: raise max_total_cycles to 12; the digest
  stands as the record for _validate_stations (no plan amendment); the disclosed panel2
  worktree edit is accepted and nothing is filed; re-pin review_sha and re-run nothing.
  Q5 (live re-run against a real repo) stays open for the ship decision.
  Answers: .harness/features/FEAT-10-software-factory/notes/answers-a1fix-eng.md
- main session direct (DEC-174): bf8f191 [harness:human] check-state.sh — two crash fixes,
  four INV-24 defects, INV-15 imports validate-digest.py once (93x, 113 digests, 0 disagreements).
- Filed #203 (remove deploy.sh after FEAT-10, P2) and #204 (feature.yaml -> feature.json with
  an enforced schema, P1). Both Backlog, both blocked on FEAT-10.
