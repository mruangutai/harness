# Observations - harness-pm

- 2026-09-06 (BUG-440): plan-merge.py apply BOOTSTRAPS a missing plan.yaml when the proposal carries NO approval mapping - exit 0, APPLIED, and it splices approval status pending itself. Confirms P-07 and falsifies G-07's "write it directly first" for this shape.
- 2026-09-06 (BUG-440): tests/integration/test-check-state.py full run = 51.9s at 772790be, which has no headroom under a 60s task verify. Importing the file with importlib and calling ONE case function ran in 0.58s and does not trigger main() (guarded at :4789) - a reusable pattern for gate-suite task verifies.
- 2026-09-06 (BUG-440): run directories are UNTRACKED, so a build worktree's feature tree has zero runs/* while the control-plane root has 344 entries with state.yaml. Any measurement of run corpus behaviour must be taken at the control-plane root; a fixture for such a check must be synthetic.
- 2026-09-06 (BUG-440): the contract's own motivating defect was worth re-measuring - the grilling attributed the PASS-over-FAIL record to FEAT-22 but the live PASS/FAIL mismatch sits in FEAT-25-claim-feature-root; FEAT-22's two are INCOMPLETE/PASS. Shipping the check reds the gate with 4 findings on day one, which became a BRIEF disclosure rather than a discovered surprise.
- 2026-09-06: BUG-440 plan goal-check — a corpus figure copied from a probe was the COMPARED count (298 claimed entries) presented as the TOTAL (308 completed lead runs with digests); the same wrong sentence had already propagated from BRIEF into the task intent. Re-run the probe and print every stage count separately, then grep the whole feature dir for the number.
- 2026-09-06: BUG-440 — an intent said 'in the existing else: arm, where _errs is FALSY', but check-state.sh:1527's else is entered for valid AND invalid digests; the falsy test is the if _errs: at :1532. Naming a control-flow arm by its condition rather than its line invites a doer to attach code one level too high.
- 2026-09-06: transcribing a panel, the pm domain grant is notes/research-*.md only, so the set-panel value file had to be a markdown-named file holding bare YAML (harness_yaml.load_file parses by content, not extension). Naming it research-<FEAT>-panel-value-c1 kept it in-domain and doubled as the transcription source.
- 2026-09-06: the dispatch cited the unconditioned runs.append((...)) at check-state.sh:646-648; at working-tree state it is :650-652 (:646-648 is the not-a-mapping guard). Re-derived at source before writing the anchor into T-01 intent.
- 2026-09-06: INV-32 (check-state.sh:534-547) expects three panel readers including goalcheck, but this panel ran two and the acceptance criterion pinned two. Recorded two, added a reader_coverage_note, raised the gap rather than inventing a third entry.
- 2026-09-06: plan-merge.py amend preserved the literal block scalar for verify when the replacement value file carried real newlines; confirmed by reloading and finding newlines in the scalar.
- 2026-09-06: BUG-440 panel record — the plan-panel team declares only should-not-exist and scope, but INV-32 (check-state.sh:534) expects a third reader, goalcheck, which the orchestrator sequences as a product segment OUTSIDE the team. Transcribing exactly what the validator lead's digest named therefore leaves a record that goes blocking-red at signature (INV-32 grades approved plans only, :426-428). Recording the out-of-team reader is precedent, not invention: BUG-1286 and BUG-1305 do it.
- 2026-09-06: BUG-440 — set-panel value derived by yaml.safe_load of the panel already on disk plus one appended reader, dumped and re-applied; all five finding ids re-derived 5/5 after the rewrite. Deriving the value from disk rather than retyping it is what makes the id-preservation check pass trivially. pm may not write a .py helper under features/*/notes/ (check-domain permits only research-*.md / uat-*.md there) — the helper goes to /tmp, the value file is named research-…-panel-value-*.md.
- 2026-09-06: BUG-440 goal check. SC-01 demanded "six separate substring assertions" and got them (test-check-state.py:4691-4695), but the token set is `("FEAT-TEST","M","FAIL","PASS","feature.json","digest.md")` — a one-char run id and two presence-only verdicts, so transposing the two `!r` fields in the INV-37 emitter ships green (carried as V-04, med, non-gating). The criterion counted assertions and never demanded they DISCRIMINATE; graded MET on its literal text. Next time an SC enumerates N items, say each assertion must fail if that item is replaced by another item from the same set.
- 2026-09-06: BUG-440 SC-05 rested on a redproof note. The note is internally sound at the pin but its recorded output shows a `mixed=False; clean=True` diagnostic the c3 case (test-check-state.py:4725) no longer prints — the fix cycle restructured the test after the proof was captured. An inspection SC over a build-time artifact goes stale under later cycles by default; only demands about existence/invocation/no-pass survive, which is exactly what SC-05 happened to ask.
