# UI review — BUG-440-digest-verdict-reconciliation — c1

**Verdict: PASS (n/a-scoped).** No rendered UI surface exists in this diff. One adjacent operator-facing
terminal message is in scope per dispatch instruction; it passes with one `info` observation.

## Census (measured, not predicted)

`git diff --stat 772790be5..2964cddb` (merge-base of `main` → pinned sha) — full feature diff, 19 files,
+1693/-7:

- `.claude/skills/harness/bin/check-state.sh` (+24/-… code)
- `tests/integration/test-check-state.py` (+90/-…)
- 4 `feature.json` records (BUG-440's own + `FEAT-07`, `FEAT-22`, `FEAT-25` — the historical corrections)
- Harness process scaffolding: `BRIEF.md`, `STATE.md`, `plan.yaml`, `observations/harness-pm.md`,
  6 files under `notes/` (panel records, redproof, goalcheck plan, ship-review docs)

Extension census across the full diff for rendered-UI kinds (`html|css|scss|less|tsx|jsx|vue|svelte`):
**one hit** — `notes/ship-review-plan-BUG-440.html`. Opened it directly
(`notes/ship-review-plan-BUG-440.html:1-18`): it carries the footer `"Derived from
ship-review-plan-BUG-440.md — the markdown is the record; do not edit this file. Regenerate with
bin/render-brief.py."` This is a generated ship-review report (project Expertise P-02/repo Expertise
P-02), not authored product UI — excluded per standing convention for this repository.

The narrower dispatch-cited scope (`check-state.sh`, `test-check-state.py`, four `feature.json` records)
is pure Python/bash/JSON — no markup, no stylesheet, no template.

## DESIGN.md

Checked directly: `.harness/harness/features/BUG-440-digest-verdict-reconciliation/DESIGN.md` —
**absent** (`test -f` returns false). Consistent with there being no rendered surface to spec.

## Adjacent operator-facing surface (in scope per dispatch, despite no-UI decline)

`check-state.sh:1531-1557`, message text at `:1552-1557` (verified identical between the worktree file
and `git show 2964cddb:.claude/skills/harness/bin/check-state.sh`, i.e. not stale):

```
INV-37: {feature} run {run_id}: digest verdict {digest_verdict!r} in {digest_path} differs from
feature.json verdict {fj_verdict!r} in {fj_path}; the gate does not decide which record is wrong.
```

Checked against the acceptance question (does it name both values, both paths, and avoid implying the
gate has decided):

- Both values named: digest verdict and feature.json verdict, each `repr()`-quoted so an empty string or
  stray whitespace is visible rather than silently blank.
- Both paths named: `os.path.relpath(dg, H)` (digest.md) and `os.path.relpath(.../feature.json, H)`,
  both relative to the harness root — an operator can open either from the message alone.
- Feature and run id named: `os.path.basename(_feat_dir)` and `_rid`.
- Explicitly declines a verdict: the trailing clause "the gate does not decide which record is wrong"
  states in-band that this is a report, not an adjudication — matches the brief's requirement that the
  check only flags divergence, never resolves it.

**`info`**: the message states the triggering fact and explicitly withholds a decision, but names no
remedy action (e.g., "reconcile the two records"). Per this role's general guidance on withhold/block
messages that name only the fact, that would ordinarily be a gap — but here it is not: the brief's
required behavior (context, T-01) is specifically that the gate must not decide which record is right,
so a message that also refrained from suggesting a fix direction is the correct behavior, not an
omission. Non-blocking, does not gate.

## Scope note

No dimension in the Fidelity/States/Interaction/Accessibility/Theme-parity table applies: there is no
rendered surface, no colour, no focus, no theme. All five are `n/a` by measured absence of a rendered UI
surface, not by omission.
