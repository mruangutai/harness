# Operator decision — FEAT-1714 signature

Signed by Main on the operator's blanket authorization in session 2026-09-15 ("do all of them from plan (or patch) to ship"; ask answered: "Sign on my authorization"). Rework ruling: propose-rework default, 2 rounds / 90 min.

Panel record: pm applied every finding (T-02 migrates all six gh-sync.py TERMINAL_MARKER sites before T-03 deletes it; the invariant number is assigned at implementation time; `superseded_by: none` gets a disposition path in T-02; one writer for the station in T-03) but left `disposition: open` on the record. Main set each to `resolved` with its resolving task via `plan-merge.py set-panel` — a transcription of what the plan text already says, not a ruling on substance. No finding overruled.

Build note: BUG-1723 (in validation) has already taken INV-43; T-03's "next free number" is INV-44 unless something else lands first.
