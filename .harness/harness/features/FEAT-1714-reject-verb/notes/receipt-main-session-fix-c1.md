# Fix c1 — FEAT-1714 (validate c0 FAIL: VAL-01..VAL-06) — by Main, main-session-direct (DEC-174)

- **VAL-01 (substance, first-sync record).** `gh-sync.py reject` refused with "needs a recorded parent" on exactly the record a first-run intake has (`load_recorded`'s all-None default: the parent is `open`'s at build entry). Now `_reject_steps` has two shapes: with a parent, the SC-03 lifecycle; with none, the disposition is the reason posted on each `plan.yaml` `source_issues` ticket (`parse_source_issues` — the plan is the truth) and that card returned to backlog, never a close or a label (the harness closes only cards it created). Neither parent nor source issue → refused, nothing written. SKILL.md step 1 now says what the record must hold before the verb runs: the station-only plan (`station_only: true`, `status: plan`, `source_issues`, `tasks: []`) when none exists.
- **VAL-02 (substance, fail-open).** Every reject write is a `_RejectWrites` method raising `_RejectHalt`; `_run_reject_steps` runs the printed list in order and on the first failure exits 1 naming the failed step, what landed (irreversible) and what did not run; NO station is written on that path (numeric) and the exit code tells the `none` caller not to. `case reject numeric, label fails` (fake gh with `--add-label` failing) proves exit 1, the landed/not-run report, no milestone, no backlog, no station.
- **VAL-06 (qa gap).** `reject first-sync` cases: report names the source ticket and the station; `--yes` comments on #1714, reseats ITEM_1714 to backlog, closes/labels nothing; station `rejected` last; `plan.yaml` differs from before by exactly `status: rejected` (source_issues intact); the drafted BRIEF is byte-identical; `none` on the same record exits 0.
- **VAL-03/04/05, F-05/F-06 (grade).** `_reject_judgement_errors` → per-rule checkers (`_reject_keys/_kind/_successor/_reason/_cycles`, `_reject_mapping`); `_landed_station_record` → `_landed_classification` + `_landed_plan_classification` + a record builder; `case_inv44` → run-shape / ledger-and-signatures / scope; `case_plan_station_is_the_landed_authority` → `_landed_station_records` fixture + assertions; `cmd_reject` → `_RejectWrites`, `_parent_reject_steps`, `_source_reject_steps`, `_report_reject_halt`. `code-grade.py --base origin/main --head HEAD`: 0 FAIL.
- `gh_sync_support.write_plan_yaml` emits `station_only: true` + `tasks: []` for an empty task list (a plan with no tasks is legal only as the station-only record).
- Matrix at HEAD: `python3 .claude/skills/harness/bin/run-unit-tests.py --kind unit` OK; `--kind integration` OK. `sync-agent-adapters.py --check` clean; orchestrator preload 4549 words.
