# Observations — harness-visual-designer — FEAT-10-software-factory

- 2026-08-08 (revise2-product seq-1): my dispatch instructed me to write
  `notes/receipt-harness-visual-designer-revise2.md`, and `check-domain.sh` BLOCKED it. My permitted
  set is `DESIGN.md`, `notes/mockups/**`, `notes/prototypes/**`, my expertise file and this log — no
  `notes/receipt-*` path. `harness-handoff`'s default receipt rule and the visual-designer domain in
  `team-config.yaml` disagree for this role. I did not work around it; DESIGN.md carried the whole
  deliverable and became the `artifact:`.
- 2026-08-08: re-measuring a claim I had written earlier paid off — DESIGN.md C-3 asserted #181 was
  closed; `gh issue view 181` says OPEN, and it is a `wayfinder:map` container with ten closed
  sub-issues. That same fact turned the C-5 adopt-or-create parent from hypothetical into a named
  candidate. Re-verify measured GitHub state at every revision cycle; it moves under the file.
