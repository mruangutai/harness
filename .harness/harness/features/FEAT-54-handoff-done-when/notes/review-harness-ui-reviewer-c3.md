# UI review — FEAT-54 handoff Done when — c3

**FAIL.** The author-facing five-section contract is now coherent and the F-08/F-09 repairs hold, but literal SC-04 still exits 1. Its complete tagged-violation census is one FEAT-51 missing-handoff line and its output has zero `Done when` matches. This criterion cannot be waived, fixture-substituted, or repaired here by mutating FEAT-51. SC-10 remains pending operator UAT and was not simulated.

## Scope and immutable census

Mode B source audit of `0ec44965a961d19177de871c3bb1f02b701e646b..39602414e1cfe792655b7e68bce367e92790c32a`. I inspected the pinned objects for every required source/document/test, the BRIEF, plan rulings, DEC-62/159/214 reached through the pinned decision index, both FEAT-54 handoffs, and the five repaired lead digests. The run digests are intentionally disk-only and absent from the Git object; all five were read from their named run directories and validated individually.

The full diff contains **87 paths**. Extension census: **1 HTML**, **0 CSS/SCSS/TSX/JSX/Vue/Svelte/Less**. The HTML is an archived generated ship-review reading view whose footer says its markdown is the record, “do not edit this file,” and names `render-brief.py`; no renderer changed. The pinned feature has no `DESIGN.md` and no prototype. The changed template, handoff notes, playbook, hook refusals, state findings, and manual-probe output are nevertheless author/operator-facing text surfaces, so this review is in scope.

## Author-facing contract

- `templates/HANDOFF.md:1-40` presents five ordered sections, puts `## Done when` last, defines one immediate `Next` action rather than phase/feature scope, states exactly one `Scope:` plus one-to-four `Authority:` lines, AND semantics, the four legal pointer kinds, and write-time-only resolution. The playbook seam and DEC-159/214 agree.
- The two real handoffs use ordered, non-empty immediate-action labels: “re-sign the amended plan and brief” and “validate the final build against the signed success criteria.” Their pinned sizes are **55** and **37** lines, below the unchanged 60-line whole-file cap.
- `_scope_problems` and `_order_problems` refuse whitespace-only scope and Authority-before-Scope (`handoff_done_when.py:187-216`). A real hook probe exited 2 with `Scope: value must be non-empty; follow templates/HANDOFF.md`.
- Nested `###` no longer truncates parsing: `_body` stops only at a strict H2 and the nested heading is rejected as unexpected prose. Duplicate `## Done when` headings are counted before parsing and refused (`handoff_done_when.py:24-35,272-287`). A real hook probe reported both the nested heading and the dangling later `SC-99` authority, proving it did not hide the pointer.
- Approval resolution now accepts only real one-to-six-hash ATX headings with required separating whitespace and optional closing hashes (`handoff_done_when.py:154-174`). `#Approval` and `####### Approval` are separately rejected; ordinary `## Approval` remains the positive control.
- Finding and approval authorities independently traverse the same containment path. Absolute, traversal, control-character, symlink-escape, non-regular, oversized, unreadable, and unexpected-resolver paths fail closed (`handoff_done_when.py:57-101,143-174,224-253`). Approval has its own absolute/traversal/symlink/FIFO cases in both unit and real-write-gate coverage.
- PreToolUse reconstructs protected handoff Edit candidates before mutation. Invalid shape exits 2; non-UTF-8 prior bytes return the unreadable sentinel and exit 2 with “cannot be reconstructed safely.” Integration checks require byte identity after both refusals (`check-domain.sh:1821-1881`; `test-check-domain.py:4138-4184`).
- Refusals are single-line, linear, uncoloured text under a `check-domain: BLOCKED` head and identify the broken count/value/pointer plus the template. Missing-section enforcement emits two overlapping actionable lines, but the remedy is unambiguous and no approved contract forbids duplication.

## Prior F-01..F-09 disposition

| Finding | c3 disposition | Evidence |
|---|---|---|
| F-01 — authority containment/fail closed | **CLOSED** | Shared bounded reader and exception boundary hold; independent approval absolute/traversal/symlink/FIFO coverage is now present and green. |
| F-02 — probe admission before model calls | **CLOSED** | The six focused tests passed: outside/traversal/symlink/directory/wrong-name/oversize inputs make zero calls; the valid control makes exactly two. Dry-run on `handoff-build.md` planned two calls and executed none. |
| F-03 — invalid/unreadable Edit mutates before refusal | **CLOSED** | Pre-mutation candidate validation and non-UTF-8 fail-closed handling passed with byte-identity assertions. |
| F-04 — literal SC-04 | **SURVIVES — HIGH** | Exact root command exits 1; full tagged-violation text is recorded below; zero output lines name `Done when`. |
| F-05 — blank Scope | **CLOSED** | Unit, write gate, state gate, and direct hook probe refuse it with a non-empty-value remedy. |
| F-06 — Scope order | **CLOSED** | Product ruling makes REQ-02 controlling; unit and both real gates refuse Authority-before-Scope. |
| F-07 — complexity grades | **CLOSED** | Exact pinned base/head grader exited 0 with **86 passing functions**; every production function meets bar 4 and every test/probe function meets bar 3. |
| F-08 — nested/duplicate heading truncation | **CLOSED** | Unit plus both gate paths reject nested and duplicate shapes; 54 parser assertions, 41 FEAT-54 write-gate outcomes, and 17 FEAT-54 state-gate outcomes passed. |
| F-09 — non-Markdown approval heading | **CLOSED** | `#Approval` and seven-hash controls fail while valid ATX approval resolves, in unit and write-gate tests. |

A separate prior security advisory remains: `probe-handoff-comprehension.py:86-98,157-197` writes repository/model-controlled path, fact, error, and answer text directly to the terminal. A crafted ESC/OSC byte can alter visible evidence. **Severity med, non-gating; owner lane: harness-dev-ops via harness-eng-lead.** This does not reopen the authoring contract, but it remains an operator-output readability/integrity gap.

## Literal SC-04 — blocking finding

From the exact repository root, I ran exactly:

```sh
bash .claude/skills/harness/bin/check-state.sh
```

Result: **exit 1**. Tagged `VIOLATION` census: **1**. Complete remaining violation:

> `FEAT-51-claude-code-lifecycle-safety: status is 'done' but notes/handoff-validate.md is missing — the validate seam was crossed without a handoff; the successor is on the disk-only path (DEC-159).`

Case-sensitive `Done when` matches in the complete captured output: **0**. The many `note` rows are non-gating and are not misreported as tagged violations. Failure scenario: a reviewer or operator cannot truthfully record SC-04’s required clean root-state result at this pin even though the new Done-when corpus itself is clean. **Owner lane: harness-orchestrator/main-direct repository-state and review-pin reconciliation; this dispatch expressly forbids treating FEAT-51 mutation as the repair.**

## Repaired digests, verification, and non-visual dimensions

All five repaired lead digests contain canonical fenced lead YAML and passed `validate-digest.py lead`: `2026-09-03-qa-validation-c2-validator`, `2026-09-03-validation-c1-eng`, `2026-09-03-validation-c2-eng`, `2026-09-03-qa-post-simplify-c2-validator`, and `2026-09-02-validation-c1-eng` — **5/5 `digest ok`**.

Focused executions at the pin: parser **54/54** assertions; probe admission **6/6** tests; the complete required write-gate file with **41/41 FEAT-54 outcomes**; the complete required state-gate file with **17/17 FEAT-54 outcomes**; code grade **86 passing**. Literal SC-04 was separate and failed as recorded above. No formatter, linter, project-wide suite/build, credentialled model call, or UAT ran.

The implemented authoring surface has no controls, focus, motion, colour-only state, or theme tokens. Keyboard/focus accessibility and dark/light parity are therefore not applicable. Refusals remain readable without ANSI or colour. Narrow-terminal wrapping and the operator’s judgment of actionability/value against the 60-line budget require eyes; SC-10 is the designated pending UAT.

```yaml
VERDICT: FAIL
DIGEST:
  headline: "The five-section author contract and all F-08/F-09 repairs hold, but literal SC-04 still exits 1 with one non-Done-when handoff violation; SC-10 remains pending UAT."
  mode: B
  in_scope: true
  severity_max: high
  findings: 2
  must_fix:
    - "F-04: obtain a review pin where the exact repository-root SC-04 command exits 0 without waiving it, substituting a fixture, or mutating FEAT-51 as this dispatch forbids. Owner: harness-orchestrator/main-direct repository-state and review-pin lane."
  contract_violations:
    - { path: ".claude/skills/harness/bin/check-state.sh", actual: "literal root command exit 1; one tagged FEAT-51 missing-handoff violation; zero Done when matches", specified: "BRIEF SC-04 requires a clean review-time root check" }
  a11y: []
  open_questions: []
  files_touched: [.harness/harness/features/FEAT-54-handoff-done-when/notes/review-harness-ui-reviewer-c3.md]
  expertise_update: []
artifact: .harness/harness/features/FEAT-54-handoff-done-when/notes/review-harness-ui-reviewer-c3.md
```
