# Observations - harness-pm

- 2026-09-02: FEAT-54 plan draft. The load-bearing planning choice was the INV-17 era discriminator: a git-history discriminator (git log --since over the notes glob) is one subprocess and rots in a depth-1 CI checkout (tests.yml uses bare actions/checkout@v4), so I froze a 141-path list in harness.json keyed on git ls-tree at b7956fc4 instead. Measured: 141 notes match the glob, 0 carry a Done when heading, 0 untracked.
- 2026-09-02: test-check-domain.py and test-check-state.py are INTEGRATION_SCRIPTS in run-unit-tests.sh, so every SC resting on their assertions is evidence: integration, not unit — the unit detect glob (bin/test-*.py) would have said unit and been wrong.
- 2026-09-02: FEAT-54 cycle 1 send-back — the dispatch named plan-merge.py apply as the ONLY write route, but apply refuses exit 7 CONFLICT on any changed field of an existing task id (probed: base vs proposal dump printed, plan unchanged). Editing T-05/T-09 needed amend --field with --expect-sha256; --value-file takes a file only, no stdin, so the replacement text was built with a python3 script from yaml.safe_load of the current intent to preserve prose byte-for-byte.
- 2026-09-02: amend --yaml-value renders a list via yaml.safe_dump, so depends_on came out block style (dash at the parent indent) beside the file's inline [T-01] elsewhere. Verified safe: every consumer (factory_claim, factory_decompose, teams/build.yaml from_task_depends_on, check-plan-routes) reads the parsed field, none greps the text. No flow-style option exists on the verb.
- 2026-09-02: FEAT-54 plan fix c1 — plan-merge.py has NO write route for the top-level 'lanes' block: apply is compare-and-swap on any non-union key (plan-merge.py:674-684) and amend takes --key tasks|decisions only, so a new task's surface cannot be recorded in lanes at all. Put the routing in execution_reason and raised it as an open question; check-plan-routes.py reads tasks, not lanes, so nothing gates on it.
- 2026-09-02: FEAT-54 — a criterion whose clause is a git command (corpus untouched between base and review_sha) cannot be 'automated': automated needs a registered test kind. Split it out as its own verify: inspection criterion carrying the command plus a comm -13 positive control, rather than pinning it to the integration kind.
- 2026-09-02: FEAT-54 — SC evidence kind is decided by which bash array holds the asserting test file, not by what the assertion is about: the probe-registration check lives in run-unit-tests.sh itself and only test-run-unit-tests-kinds.py drives it, which sits in INTEGRATION_SCRIPTS, so 'evidence: unit' was unreachable.
- 2026-09-02: FEAT-54 — the feature's own build handoff notes are a real planning gap, not a code bug: notes written between the base commit and the write-gate task escape the gate and are non-baselined, so the corpus scan reddens for a reason no code fix touches. Closed with an explicit sweep task the scan task depends_on.
- 2026-09-02: FEAT-54 c2 — a positive control on a diff-vs-baseline comm pair could not be observed on the feature's own range (HEAD == merge-base, diff arm 0 lines, both comm arms empty); proved discrimination on a stand-in historical commit range in the same worktree instead (primary 0, control 4, inverted comm -13 137).
- 2026-09-02: FEAT-54 c2 — plan-merge.py amend refuses a list field without --yaml-value (exit 4) and prints the sha256 only alongside --show; a directory entry in files: resolves through check-domain.sh --resolve to the same lane as a file under it, so declaring the sweep directory costs no route deviation.
- 2026-09-02: FEAT-54 panel transcription — the dispatch's finding-id formula (8 hex of sha256(summary)) matched templates/plan.yaml:75 but not panel_findings.py (32 hex, reader-scoped, normalized) which its own suite calls the ONE source. Wrote canonical ids, recorded both derivations in the artifact, raised the divergence as an open question.
- 2026-09-02: proving "every other key byte-unchanged" needs the pre-write file sha256 captured FIRST; per-key safe_dump hashes alone cannot see a formatting change. The revert-and-rehash check caught set-panel eating the blank line between panel: and lanes:, which per-key hashes reported as clean.
- 2026-09-02: bash-write-guard parses the whole command line textually, so a python heredoc whose text contains a literal dash-plus-greater-than arrow (a code contract line) is refused as a shell redirect; tokenise the angle brackets and substitute them inside python when generating plan text.
- 2026-09-02: plan-merge.py apply over an ABSENT plan.yaml passes the proposal through verbatim and seeds the approval block after the feature line, so the proposal must omit approval and the feature key can only be set on that first create (a rename therefore needs the file deleted first).
- 2026-09-02: transcribing an 815-line plan by hand invites drift; generating it with a line-range replacement table and then reporting git diff --no-index hunk headers makes every hunk attributable to a ruling and catches an accidental edit immediately.
- 2026-09-02: FEAT-54 goal-check c2. A lead pre-check reported plan.yaml's 'risks block (line 31)' as a dangling reference to T-06 case (g). There is no risks key; line 31 is panel.findings[1]'s verbatim summary, and it was accurate — the pre-check had matched T-01's case (g) at :201. Two tasks in one plan both label sub-cases (a)..(h), so a bare 'case (g)' is ambiguous; always re-read both tasks before grading a case-label reference.
- 2026-09-02: FEAT-54. REQ-09 read 'No live document OR GATE still tells an author the contract is four sections', but SC-08 scoped the gate half to check-domain.sh's required-section LIST. The stale normative comment at check-domain.sh:1512-1513 ('four fixed sections', split across two lines so a single-line grep for 'four sections' misses it) sat outside every criterion and outside T-04's intent.
- 2026-09-02: FEAT-54 c2b cycle 2 — cycle 1 widened SC-08 to "ANY four-section assertion ANYWHERE in check-state.sh" and thereby swept in two dated FEAT-31 measurement/incident comments (check-state.sh:1188, :1203) that rule 15 protects, making the criterion satisfiable only by falsifying the record. Fix was an exemption test written INTO the criterion (names a past sha or feature id AND reports what was observed then), plus naming the two exempt sites by content. Also: the live DEC-159 claim in check-domain.sh wraps across :1512-1513, so a falsifier phrased "any line" would have missed the very site the finding was about — the falsifier had to say "any line, or any comment wrapped across lines".
- 2026-09-02: planfix-c2c — plan-merge.py set-panel re-dumps the whole panel through yaml.safe_dump, so every carried-over summary is re-wrapped; proving a carried finding unchanged means re-deriving its id through panel_findings.py (which lowercases and collapses whitespace), never diffing the block. Building the value file by READING the old findings out of the landed plan and copying severity/reader/summary is what made all 5 carried ids re-derive first try.
- 2026-09-02: planfix-c2c — a per-id pre/post equality proof needs the pre-image copied BEFORE the first plan-merge write; amend + set-panel both rewrite the file, and after the fact there is no in-repo copy to compare against (never a git command here — HEAD is untouchable in this dispatch).
