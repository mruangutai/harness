# Fix c1 — BUG-1756 (validate c0 FAIL: Q1, Q2, Q3) — by Main, main-session-direct (DEC-174)

- **Q1 (SC-04 spawn error / timeout).** Two in-process arms load the validator, monkeypatch `subprocess.run` to raise a real `OSError(8)` / `subprocess.TimeoutExpired` when handed the runner, and call `check_qa_matrix_claim`: exit 0 + "could not independently re-run". Mutation (`except Exception: raise`) turns both `FAIL … RAISED <type>` — receipt arm 3. The c0 directory fixture is gone.
- **Q2 (per-SC red).** Receipt arm 2: the final test file run against the pre-fix validator at 24e766bb — SC-01, SC-02, SC-03 each FAIL with exit 2 and `argv=[]` (the Python stub never ran under bash).
- **Q3 (unit red at omp-hooks.test.ts:1261).** Not this feature's: the worktree had been cut from LOCAL main carrying the operator's two unpushed commits (`run-end --cycles-used` required), which broke BUG-1724's bare `run-end` call in the bun test. The branch is rebased onto origin/main WITHOUT those commits (they stay local; the bun test call is fixed inside the operator's own commit on main). Pristine origin/main passes that test.
- **Found while verifying BUG-1724 (issue #1771), carried here as an amendment to T-01's files:** the agent files' marker example `HARNESS-FEATURE: FEAT-42-one-root-resolver` matched the hook's regex, so every orchestrator/lead session keyed on FEAT-42 — `feature-root` unresolved → `stamp-tokens` never ran. Example now `<FEAT-NN-slug>` (non-matching); adapters synced; harness.md §3 requires the marker as Main's first spawn-prompt line. `amendment` judgement T-01.files recorded; signature kept (BUG-1716 D-04).
- 10/10 bug919 cases; `code-grade.py --base origin/main --head HEAD`: 0 FAIL; matrix at HEAD: unit OK, integration OK (below).
