# Observations - harness-pm

- 2026-09-05: BUG-1306 — plan-merge.py apply bootstrapped an ABSENT plan.yaml cleanly when the proposal carried no approval mapping; it spliced approval status pending itself. My repo-tier G-07 (write the first plan.yaml directly) is wrong; P-07 is right.
- 2026-09-05: BUG-1306 goal-check — a FAIL-line count recorded in a ship briefing (15) disagreed with the plan's (14) purely because run_pool.py:105 prints its own per-file FAIL line on top of the test file's own summary line; reproducing the invocation, not re-measuring, settled it.
- 2026-09-05: BUG-1306 panel close. plan-merge.py has NO delete verb (apply, add-tasks, set-task-station, set-feature-station, set-panel, sign-approval, amend). A panel finding that says "drop decision D-NN" is therefore unactionable by construction; the only honest response is keep-and-correct via amend --field because. Verified against plan-merge.py --help before ruling.
- 2026-09-05: check-state.sh INV-32 (bin/check-state.sh:533) requires ALL THREE reader rows should-not-exist, scope, goalcheck even when the plan-panel team runs only two steps. A goal-check dispatched separately (notes/research-<FEAT>-goalcheck-plan-c0.md) is the goalcheck reader; record it ran with persona harness-pm plus a note that it ran outside the validator run dir, otherwise the record either lies (skipped) or hard-fails at signature.
- 2026-09-05: amend --value-file preserves a literal | block for verify: with shell comment lines (#) intact; verified by re-running amend --show after the write. Shell comments are a usable place to put the 'why this half is not redundant' argument that would be noise in intent:.
- 2026-09-05: BUG-1306 goal-check. SC-04 pinned absolute line numbers (305/309) for call sites that the fix's own inserted lines pushed down by 10 (315/319). A criterion whose anchor sits BELOW the insertion point anchors itself out of date; the binding clause was ordering, so I graded met and filed the numbers as a ship-briefing note rather than a failure.
- 2026-09-05: measuring exit code plus a FAIL-line filter in one bash call: `$?` and `${PIPESTATUS[0]}` are rejected by this shell parser. `set -o pipefail; <cmd> | sed -n '/^FAIL/p'` works — the tool's own reported exit code then reflects the test process, and sed (not grep) is permitted.
