# QA gate — BUG-148-gate-record-correction — 2026-09-06

## BLUF

**PASS.** `test_matrix.docs.always` is `[]` (confirmed independently against the diff, not
adopted from the plan): every changed path at `f60d5d27` is a `.harness/`-owned record — no
runtime code, no test file — so the required-kind set is `[]`. SC-04's evidence ran green
(runner exit 0, 0 `FAIL` lines, both named tests `ok`). The discrimination question — cycle-1
correction — is now answered by read-only measurement, not code reading: a fresh regeneration
against the stale (`41c16c7`) index diffs non-empty (42 lines, exit 1) — the test would have
printed `FAIL`; against the current (`review_sha`) index it diffs empty (exit 0), matching the
observed `ok`. The live write perturbation is still correctly refused by the write-guard/domain
grants, in the assigned worktree and a fresh disposable one alike (Q1, unchanged).

## 1. Change-type confirmation (independent of the plan)

`git diff --name-only 41c16c7..f60d5d27` (17 paths, reproduced in full at §6). Every path is
under `.harness/harness/docs/`, `.harness/harness/features/BUG-148-gate-record-correction/**`, or
`.harness/harness/features/FEAT-05-pyyaml-file-parsers/STATE.md`. None is `tests/**`, none is a
source/runtime file. This independently confirms `change_type: docs` for both tasks (plan D-03)
— no required kind is being dodged by the label.

## 2. Kind-by-kind resolution (`matrix_ok: true`)

Required-kind set for `docs`: **`[]`** — `test_matrix.docs.always` is empty and no `when`
predicate in the matrix fires for `docs` (the only conditional, `config.touches_config_shape`,
binds `config` changes, not this one; `harness.json` itself is untouched by this diff).

| kind | status (harness.json) | required here? | resolution |
|---|---|---|---|
| unit | active | no (docs floor is `[]`; diff touches no `tests/unit/**`) | not required |
| integration | active | not by matrix, but by SC-04 explicitly | **satisfied** — run below |
| functional | excluded, DEC-187 | no | not applicable (pre-excluded) |
| omp_session_accessor | locally_run | no — diff doesn't touch `inflight_registry.py` session-file resolution | not on this diff's surface, no run required |
| handoff_comprehension | locally_run | no — diff doesn't touch the handoff contract | not on this diff's surface |
| issue_types_live | locally_run | no — diff doesn't touch the issue-type path | not on this diff's surface |
| component | unresolved (`cmd: null`) | no — diff touches no `.spec.tsx`/`.stories.*` | null runner not load-bearing here |
| ui | unresolved (`cmd: null`) | no — diff touches no e2e surface | null runner not load-bearing here |
| eval | excluded, DEC-187 | no — not `ai_behavior` | not applicable |
| typecheck | unresolved (`cmd: null`) | no — diff touches no `.ts`/`.tsx` | null runner not load-bearing here |

No verdict rests on a null or excluded runner. `matrix_ok: true`.

## 3. SC-04 evidence — OBSERVED

`env -u HARNESS_AGENT_TYPE bash .agents/skills/harness/bin/run-unit-tests.sh --kind integration`

- Runner's own exit status (captured `$?` of the runner process, not a tail read): **`RUNNER_EXIT=0`**
- `grep -c '^FAIL '` over the full runner output: **`0`**
- Named tests, verbatim from the runner's own output:
  ```
  ok - test_committed_index_matches_a_fresh_regeneration
  ok - test_no_amendment_construct_survives_in_the_authority
  ```

## 4. Test-first audit

The matrix requires no kind for this change (docs floor `[]`), and this feature adds no test —
SC-04's evidence is an existing integration suite already asserting the index invariant, as the
BRIEF's own "Verification gaps" section states and which I independently confirm from §1/§2.
There is no coverage gap to report and none manufactured: the diff is prose-only, no behavioral
code changed, so "was a test written first" does not apply.

## 5. Discrimination proof — MEASURED, read-only (cycle-1 correction)

**The live perturbation is still declined**, and the refusal record from cycle 0 stands unchanged
(bash-write-guard and check-domain.sh both deny `.harness/harness/docs/DECISIONS-INDEX.md` to
`harness-qa`'s domain in `.harness/team-config.yaml`; the disposable-worktree route was also
refused under DEC-218 claim-set binding). That refusal is correct conduct, not a shirk — Q1 below
is unchanged.

What cycle 0 answered by reading `test_committed_index_matches_a_fresh_regeneration`'s source
instead of measuring is answerable read-only, because the test's whole body reduces to one
comparison: `gen-decisions-index.py --stdout` output vs. `REAL_INDEX`'s on-disk bytes. So the
counterfactual — would the stale (pre-fix, `41c16c7`) index have reddened this test — is exactly
the question "does a fresh regeneration at `review_sha` differ, line for line, from the index
content committed at baseline?" Measured directly, touching nothing in the tree:

1. `git show 41c16c736e3cc4b2b331757081c90a24f2ba977d:.harness/harness/docs/DECISIONS-INDEX.md`
   → `/tmp/index-at-base.md` (219 lines). `git show` exit **0**.
2. `env -u HARNESS_AGENT_TYPE python3 .agents/skills/harness/bin/gen-decisions-index.py --stdout`
   from the worktree root → `/tmp/index-fresh.md` (219 lines). Generator exit **0** — it fails for
   no reason other than content divergence.
3. `diff /tmp/index-fresh.md /tmp/index-at-base.md` → exit **1**, one hunk, **42 lines differing on
   each side** (rows for DEC-175 through the file's tail). Representative rows, verbatim:
   ```
   < - DEC-175 @4432 [digest,plan,qa,dispatch] refs: DEC-100 DEC-121 DEC-173 DEC-174 :: `dev`/`dev-ops` returns declare `task: T-NN|none`; ...
   > - DEC-175 @4424 [digest,plan,qa,dispatch] refs: DEC-100 DEC-121 DEC-173 DEC-174 :: `dev`/`dev-ops` returns declare `task: T-NN|none`; ...
   < - DEC-176 @4531 [cost,dispatch,plan,brief] refs:  :: At the BRIEF/PLAN signature gate ...
   > - DEC-176 @4523 [cost,dispatch,plan,brief] refs:  :: At the BRIEF/PLAN signature gate ...
   < - DEC-177 @4553 [budget,cost,orchestrator,skills] refs:  :: A bounded runtime-environment question ...
   > - DEC-177 @4545 [budget,cost,orchestrator,skills] refs:  :: A bounded runtime-environment question ...
   ```
   Every row after DEC-174 carries a fixed `@line` offset (fresh minus stale = +8, matching
   DEC-174's evidence sentence growing by 8 lines) — the exact mechanism cycle 0's reasoned
   account predicted, now measured rather than inferred.
4. **Conclusion, as a measurement, not a reasoned account:** with the stale (`41c16c7`) index in
   place, `fresh != committed` evaluates **TRUE** (42 lines differ). The test's own logic
   (`tests/integration/test-gen-decisions-index.py:339-392`) prints `FAIL -
   test_committed_index_matches_a_fresh_regeneration: ...` on that branch, never `ok`. The test
   **would have gone red** had the index not been regenerated alongside `DECISIONS.md`. The diff
   was non-empty, so this is not the empty-diff finding that would have re-opened the verdict.
5. **Sanity anchor, both-directions:** `diff /tmp/index-fresh.md
   .harness/harness/docs/DECISIONS-INDEX.md` (the CURRENT, `review_sha`-committed index) → exit
   **0**, **empty**. This is §3's green run (`ok - test_committed_index_matches_a_fresh_regeneration`)
   measured directly rather than taken on the runner's report: the regenerated file matches: green
   here, red on the stale file above — the both-directions proof the send-back asked for.
6. **Tree confirmed untouched** — `git status --porcelain` in the assigned worktree, verbatim:
   ```
    M .harness/harness/features/BUG-148-gate-record-correction/feature.json
   ?? .harness/harness/features/BUG-148-gate-record-correction/notes/qa-BUG-148-2026-09-06.md
   ```
   The `feature.json` modification is the pre-existing orchestrator bookkeeping already noted in
   cycle 0 (`review_sha` pinned before I started). The untracked `notes/qa-*.md` is this artifact.
   No other tracked file is modified; no other new path exists anywhere in the tree. All scratch
   files (`/tmp/index-at-base.md`, `/tmp/index-fresh.md`, `/tmp/diff-*.txt`) live outside the tree.

**Q1 (open, harness defect, unchanged from cycle 0):** DEC-153's disposable-worktree carve-out for
perturbation proofs is unreachable for `harness-qa` perturbing a non-`tests/**` path: the assigned
worktree denies the write via both bash-write-guard and check-domain.sh (no `.harness/harness/docs/**`
grant in `team-config.yaml`), and a freshly created sibling worktree is refused under DEC-218's
claim-set binding (an agent's claim does not auto-extend to a worktree it creates itself). QA can
read-around the gap when a proof happens to reduce to a diffable comparison, as this one did, but
that will not hold for every discrimination question DEC-153 was meant to enable live.

## 6. Changed-path list — complete, no historical artifact moved

```
.harness/harness/docs/DECISIONS-INDEX.md
.harness/harness/docs/DECISIONS.md
.harness/harness/features/BUG-148-gate-record-correction/BRIEF.md
.harness/harness/features/BUG-148-gate-record-correction/STATE.md
.harness/harness/features/BUG-148-gate-record-correction/feature.json
.harness/harness/features/BUG-148-gate-record-correction/notes/grilling-gate-record-correction-2026-09-06.md
.harness/harness/features/BUG-148-gate-record-correction/notes/handoff-plan.md
.harness/harness/features/BUG-148-gate-record-correction/notes/receipt-harness-documentor-2026-09-06-05-product.md
.harness/harness/features/BUG-148-gate-record-correction/notes/research-BUG-148-goalcheck-plan-c0.md
.harness/harness/features/BUG-148-gate-record-correction/notes/research-BUG-148-planfix-c1.md
.harness/harness/features/BUG-148-gate-record-correction/notes/research-BUG-148-rulings-application-2026-09-06.md
.harness/harness/features/BUG-148-gate-record-correction/notes/research-panel-transcription-2026-09-06.md
.harness/harness/features/BUG-148-gate-record-correction/notes/review-harness-code-reviewer-planpanel-c0.md
.harness/harness/features/BUG-148-gate-record-correction/observations/harness-orchestrator.md
.harness/harness/features/BUG-148-gate-record-correction/observations/harness-pm.md
.harness/harness/features/BUG-148-gate-record-correction/plan.yaml
.harness/harness/features/FEAT-05-pyyaml-file-parsers/STATE.md
```

All 17 paths fall inside the SC-05 allowlist (the three named files plus this feature's own
directory). No foreign path (no `BUG-440-...`, no `FEAT-55-...`) appears — the `merge-base
origin/main HEAD` contamination the BRIEF warned about is absent from this baseline-anchored diff.

## Tree state

`git status --porcelain` in the assigned worktree shows one pre-existing modification I did not
make: `.harness/harness/features/BUG-148-gate-record-correction/feature.json` (`review_sha`
pinned from `"none"` to `f60d5d27...`) — orchestrator dispatch bookkeeping from before I started,
not a QA change. No other tracked file is modified. My only write is this artifact.
