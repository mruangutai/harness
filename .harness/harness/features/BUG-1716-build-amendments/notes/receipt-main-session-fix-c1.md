# Fix c1 — BUG-1716 (validate c0 FAIL: V-01..V-08) — by Main, main-session-direct (DEC-174)

- **V-01 (substance).** `record-amendments` wrote the ledger BEFORE the plan under the plan lock, so a plan write that failed afterwards left a judgement for an amendment that never reached the plan. Now `_record_amendments_locked` splices the plan first (`_amended_plan_bytes` is pure; `_replace_bytes` is the same tempfile+os.replace shape as `locked_update`), then appends the judgements; a ledger refusal RESTORES the plan byte for byte and re-raises with both paths named. `case_b1716_record_amendments_is_all_or_nothing` now also asserts the "restored byte for byte" line under a held ledger lock.
- **V-02/V-03/V-07/V-08 (grade).** One amendment-shape authority, `amendment_contract.py` (`entry_errors`, `normalized`), consumed by BOTH `validate-digest.py` (`_amendments_errors` + `_amendment_mapping`) and `plan-merge.py` (`_amendment_entry`): the two graders cannot drift, and each graded symbol is ≤ 4 cyclomatic. `_select_amendment` = `_sole_judgement_at` + `_refuse_unless_live_amendment`. `cmd_record_amendments` = preflight + locked stages. INV-40 (d) = `_signed_hashes_to_grade` + `_amended_task_ids` + `_unledgered_edit_hit`. `test-validate-feature-json.py` runner is a CASES tuple.
- **V-04/V-05/V-06 (grade).** Test cases split by claim: sign (canonical / value-not-presentation / refused), BUG-285 (splices / touches-only-named / ledgers / keeps-valid-and-signed / rerun-refused), approval (survives / resets-only-on-task-set), INV-40 (d) (detects / grades-every-field / coverage / scope).
- `code-grade.py --base origin/main --head HEAD`: 0 FAIL. Matrix at HEAD: `python3 .claude/skills/harness/bin/run-unit-tests.py --kind unit` 40 files OK; `--kind integration` 72 files OK.
- One expectation re-pinned to substance (`"['by']"` instead of the old phrasing) because the shared message now says `unknown key(s) ['by']`.
