# Goal-check — BUG-1716 build amendments plan

does this plan deliver the operator's stated intent?

- **end user — pass** — SC-01 and SC-06 are carried by T-01 and T-02, with T-04 carrying the command-level replay: the engineering lead keeps eligible `intent`/`files`/`verify` corrections in the same active run, emits the closed `amendments: [{task, field, was, now, reason}]` digest shape, blocks with a recommendation when any of the three eligibility conditions fails, and accepts all three BUG-285 T-03 recommendations as fixtures without a new ask or dispatch.
- **orchestrator — pass** — SC-02 is carried by T-04 and T-06: one orchestrator-runnable `record-amendments --digest` invocation byte-preservingly transcribes task-field values and appends their amendment judgements, preserves approval, and explicitly forbids a product amendment run, separate transcript run, task re-dispatch, or cycle increment.
- **operator — pass** — SC-03 and SC-04 are carried by T-03, T-04, T-05, T-06, and T-07: signing stores the canonical per-task hash over `intent`, `files`, and `verify`; INV-40 detects an unledgered post-signature difference and names the `record-amendments` remedy; `amendment` becomes the sixth judgement kind; and exact-entry ship overrule plus the ledger-derived overrule rate are specified.
- **reader — pass** — SC-05 is carried by T-08, with the governing safety case recorded by T-09: Stage 1 treats amendments as departures from signed how but judges them only against BRIEF success criteria and decisions, while Stage 2 and `code-grade.py` remain recomputed from the reviewed diff; the decision text explicitly states that validation must never be re-anchored to task text.
- **code maintainer — pass** — SC-07 is carried by T-09 and T-07, supported by T-01 through T-06 and T-08: the plan defines one closed digest contract, one controlled all-or-nothing transcription route, a six-kind ledger with amendment identity and overrule semantics, one canonical task-text hash definition enforced through INV-40, explicit byte-identity remedies, and consistent updates to the decisions, schemas, scripts, tests, and operating instructions.

**Overall conclusion — pass.** Judged against the grilling artifact rather than the derived BRIEF, the plan carries every settled core requirement, including same-run engineering-lead action, digest recording, one-command no-redispatch transcription, the sixth judgement kind, signed task-text hashing with INV-40 enforcement, task-text-independent validation/review anchors, and the three-return BUG-285 acceptance replay. No declared perspective is unserved.
