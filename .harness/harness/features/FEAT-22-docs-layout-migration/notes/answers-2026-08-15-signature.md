# Operator answers — FEAT-22 signature round, 2026-08-15

- **Signature: APPROVED — conditional on eng-lead's re-fire on r6 returning PASS.** The main
  session writes both approval blocks once that PASS lands; if it FAILs, findings come back
  as one revision pass before anything is signed.
- **Q2 CONFIRMED, both halves.** (a) The two always-injected expertise edits ride the cluster
  commit as DECLARED plan tasks — the declaration is what cures FEAT-21 B-8's shape, which was
  an UNdeclared edit. (b) DEC-189 gains its amendment via the append-only mechanism, under the
  operator's signature.
- **Q3 RULED: re-fire eng-lead on r6.** One spawn; the architecture PASS must attach to the
  text being signed.
- Q4 accepted as judged (residual not worth a cycle). Q5/Q6/Q7/Q8 are backlog rows for the
  briefing, not this feature's scope.
