# /simplify pass over the signed r10 plan — disposition, 2026-08-16

Four review agents (reuse, simplification, efficiency, altitude), read-only over the signed
artifacts. **Zero findings warrant an r11** — every candidate edit loses to the signature
cost or to a counterweight the agents named themselves. Dispositions:

## Binding executor notes (the build is main-session-direct; these bind ME)

1. **Before trusting T-10's survivor arithmetic** (25−15−3−2=5), verify the ~5 spellings of
   the legacy pattern across T-04/T-05/T-06/T-10 still agree (reuse F2).
2. **T-01's RED-STATES section is mandated by intent only — no grep enforces it.** Write it,
   table form (interval × suite × expected FAILs × clearing task); treat its absence in the
   boundary note as a T-01 failure (altitude F4).
3. **T-08's corrective span: plain text or whole-sentence bold only** — interior markup reds
   the anchored clause (altitude F3).
4. **The anchoring residual is slightly wider than the accepted A-1**: clauses 2–3, like
   clause 1, never terminate at section end and could match in DEC-190+ text (simplification
   F1 parenthetical). Same accepted class, same real compensator — D-03's human diff read.
   NOTE the correction: T-09's regeneration does NOT validate amendment placement; the
   digest's claim that it compensates is withdrawn (altitude F3 verification).

## Left in signed text, with reasons

- T-09's duplicate suite run (~1 min, one-shot; the post-commit run binds evidence to HEAD).
- T-03's copies of the detector regexes (byte-identical at the pin, panel-verified; drift is
  a future-revision concern).
- T-09's hand-enumerated allowlist; clause-1's self-naming awk; the five "earlier draft"
  provenance clauses; the guide.md awk (its surplus is two distinct failure messages).

## Briefing rows (carry to the ship review)

- A SECOND amendment-span gate in any future plan is the crystallization trigger for a
  shared decision-span checker (bin/, DEC-174 lane).
- The partition rule's authority is BRIEF §"The partition rule"; every task-intent
  restatement is a convenience copy — future edits change the BRIEF sentence and re-derive.
- The control-plane-redundancy mechanism is spelled in full twice (D-02, T-08 intent);
  D-02 is the authority.
- T-01's red-states enumeration duplicates T-04/T-05 file lists; the covering rule + three
  exceptions (~10 lines) is the maintainable form if this plan shape recurs.
