# Operator answers — FEAT-35, one consolidated revision — 2026-08-23

The operator read the plan and approved it **subject to the three fixes below**. This is the ONE
consolidated revision (DEC-176). Nothing else is open. The signature follows this revision, not
before it, so the signed bytes are the corrected ones.

## Scope ruling — asked and settled

**T-04 and T-05 both STAY.** The operator was shown that both reach outside the "SKILL.md only"
constraint and kept them: T-04 because DEC-158 makes the playbook the wrong home for the incident
measurement, and T-05 because a playbook rewrite with no regression test is unverifiable. Amend
BRIEF's scope constraint so it no longer reads as SKILL.md-exclusive — the constraint is that the
PLAYBOOK BEHAVIOUR is the only behaviour changed, with its decision record and its regression test
as sanctioned companions. Do not widen it further.

## FIX 1 (was Q2, blocking) — T-02 fails silently as written

The self-identification step has no instruction for a zero-match or a multi-match result, and the
failure is REPRODUCED, not hypothetical: a non-unique probe string matched THREE transcripts.

Add to T-02 a clause covering both outcomes explicitly, and make it part of the task's verify:

- **exactly one match** — proceed, using that agent id.
- **zero matches** — the nonce has not flushed yet or the sidecar layout has changed. The
  orchestrator SKIPS the context check for this wake, says so in one line, and continues the loop.
  It never guesses an id and never treats a skipped check as a passed one.
- **two or more matches** — the nonce was not unique. Same outcome as zero: skip, say so, continue.

The check is advisory under DEC-198, so skipping it is legal. Reporting a headroom figure derived
from the wrong transcript is not.

## FIX 2 (was Q8) — a false sentence in BRIEF, and it originated with the main session

BRIEF's "What supplies the mechanism" says DEC-174 am.4's enumeration IS the enforcement layer.
That is false and it came from the main session's dispatch, which took it from the grilling note.
Both have now been struck at source.

Am.4's heading is *"the enumeration is a list of examples, not a boundary"* and it rules *"The
category governs"* (`DECISIONS.md:4854`, `:4862`). Rewrite the bullet so the operative test is
`check-domain.sh --resolve .claude/skills/harness/SKILL.md` returning `NOBODY` at `569d417` —
which is independent of am.4 and is what actually forces the lane. Cite am.4 for the CATEGORY only.

## FIX 3 (was Q3) — T-05's lane versus the decision it needs

T-05 lanes `run-unit-tests.sh` as enforcement layer while T-04 writes DEC-200 only. Am.4 requires
that a script becoming a gate joins the list ON THE DAY it does, with the entry amended. Resolve it
one of two ways and say which in a decision:

- T-05 does NOT make `run-unit-tests.sh` a gate — it adds a case to an existing suite — in which
  case correct T-05's lane and record why, or
- it does, in which case T-04 also writes DEC-174 amendment 5 adding it, and T-05's lane stands.

Pick on the evidence, not on which is less work.

## Two things NOT to act on

- **Q9, the fabricated completion, stays out of scope here.** It shapes REQ-03 and that is all it
  does in this feature. The main session owns filing it.
- **Q5, the leads' identical pattern, stays out of scope.** #610 and #552 track it. Your finding
  that the Stop hook ENFORCES the wait rather than merely describing it is valuable — record it in
  the plan as a known cost of the boundary, and make T-01 state that the refusal on a
  dispatch-and-stop return is EXPECTED and fires at most once, so a future orchestrator does not
  read it as "you may not return yet" and start stalling again.

## After the revision

Return `awaiting_user` with the revised artifacts still `pending`. The main session signs both, then
you commit once.
