# Observations - harness-documentor

- 2026-08-24: FEAT-35 DEC-201 control sentence — dispatch table said 342 `echo hold`; my own recount over Bash tool_use blocks in agent-a95e1e6e97e80de87.jsonl gave 341 (and 355, not 354, for echo-hold-or-sleep). Every other figure re-measured exactly. Kept the handed-down 342 because the frozen preceding narrative uses it; contradicting it two paragraphs apart would have been worse than an off-by-one.
- 2026-08-24: escalating a count mismatch (Q1: entry said 342, my recount said 341) overturned the tier above me — its checker's regex counted `echo hold` OR a bare `sleep` and reported the union as the echo-hold figure. Two lessons: a handed-down count derived by regex is worth re-deriving over the primary source (P-01 held), and my own 355/14 was ALSO wrong because I counted `sleep` as a substring rather than a word. Both sides of a disputed number need the same word-boundary discipline.
- 2026-08-24: a count buries the finding when the distribution is the point — 354 stall calls read as one command repeated, but they are 341 identical plus 12 distinct invented forms. Grouping the verbatim commands (`Counter` over the tool_use inputs) is what produced the sentence worth writing.
