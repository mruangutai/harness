# Observations - harness-orchestrator

- 2026-08-23: I passed a false claim into a dispatch. I told pm that DEC-174 am.4 "enumerates the enforcement layer exactly"; am.4's own HEADING is "the enumeration is a list of examples, not a boundary" and DECISIONS.md:4862 rules "The category governs." I had read the grilling note's paraphrase and restated it as fact without opening the entry. pm caught it. The conclusion survived by category reasoning, but two artifacts now carry the falsified sentence and this repo has no propagation checker.
- 2026-08-23: the grilling note's Settled section carried two falsified items and I forwarded one. `git show --stat f5194d2` = 1 file, 5 insertions; the "142 unrelated deletions" it attributed to that branch are 569d417's own +142/-16 (#750), a two-dot-diff mis-measurement. pm flagged it but has no shell; ONE command settled what a grilling artifact asserted as settled. An input artifact's "Facts I verified" section is a hypothesis, not a floor.
- 2026-08-23: I reached for the Agent tool intending to message a running child and spawned a fork instead — I hold no SendMessage tool, only Read/Agent/Write/Bash. Cost ~72k tokens for a returned "noop" because a fork inherits the parent's full context. Once a lead is dispatched there is NO channel to it; a finding discovered mid-run either waits for its return or goes on disk where the NEXT dispatch can cite it.
- 2026-08-23: the SubagentStop hook (validate-digest.py:907-921, #551) refused my dispatch-and-stop return with "returned with children in flight". Its own comment says it fires at most once and is explicitly NOT a wait, so the second return ships. But the refusal READS as "you may not return yet", which is the exact pressure that makes an orchestrator invent a stall. When a guard refuses a return, re-read the guard's source before changing behaviour — the refusal may be advisory-by-design.
- 2026-08-23: dogfooded the self-id mechanism this feature specifies. A unique nonce echoed in one Bash call, grepped from ~/.claude/projects/*/*/subagents/agent-*.jsonl in a LATER call, resolved to exactly one id (af05a0d5a321741b6); context-watch.py takes it POSITIONALLY, not as --agent. A deliberately NON-unique probe string matched THREE transcripts, which is the failure mode: the method needs an assert-exactly-one, not just a grep.
- 2026-08-23: the #551 Stop-hook refusal fired TWICE in one orchestrator run — once per dispatch-and-stop return, not once per run. validate-digest.py's comment says "Fires AT MOST ONCE per return" and that is literal: an orchestrator dispatching N times in a phase pays N extra returns. I had relayed it downstream as "fires at most once", which reads as once per run and is wrong. Per-return is the load-bearing detail: the cost is proportional to dispatch count, and each occurrence is a fresh chance for the model to misread it as "you may not return yet".
