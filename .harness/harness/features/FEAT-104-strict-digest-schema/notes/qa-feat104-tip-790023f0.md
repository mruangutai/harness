# QA gate — FEAT-104-strict-digest-schema @ 790023f0

**BLUF: PASS.** Both canonical suites are green with zero real failures at file counts at-or-above
baseline; the SC-08 renamed case passes 12/12 and both its new stderr substrings trace to a single
named emitter on the undeclared-step-key path; `matrix_ok: true` for this delta (integration required
and satisfied, unit run and satisfied though not obligated by the bugfix predicate). Tree clean of
tracked modifications.

## Pin identity
- `git rev-parse HEAD` → `790023f0c57a3d436983d935aa0948a3d99cfdf1` ✓
- `git status --porcelain` → empty (pre-run) ✓
- `git diff --stat 168f875f 790023f0 -- tests/` → `tests/integration/test-check-domain.py | 6 ++++--`, 1 file changed, 4 insertions(+), 2 deletions(-) ✓ matches expectation exactly.

## Suite runs (both `env -u HARNESS_AGENT_TYPE`, exit captured in `rc`)
**unit** — `.agents/skills/harness/bin/run-unit-tests.sh --kind unit`
- `rc=0`, wall 2s, `pool: 8 workers, 36 files` → **36 = baseline, no drop**.
- Raw `grep -c '^FAIL '` = 4, but all four are inside `----- test-factory-claim-mutation.py (exit 0) -----`,
  which itself prints `PASS test-factory-claim-mutation.py`: a deliberate mutation-proof that reddens
  3 `BUG-1290` cases to demonstrate discrimination, then reports `MUTATION PROOF: 3/3 cases reddened`.
  This is the project's own known G-08 gotcha (self-test verdict tokens byte-identical to a real
  `^FAIL`). **Real failing-file count: 0.**

**integration** — `.agents/skills/harness/bin/run-unit-tests.sh --kind integration`
- `rc=0`, wall 73s, `pool: 8 workers, 70 files` → **70 = baseline, no drop, no unexplained excess.**
- `grep -c '^FAIL '` = 0.

## SC-08 independent confirmation
- `env -u HARNESS_AGENT_TYPE python3 tests/integration/test-check-domain.py` → `rc=0`, `12/12 T-06 check-domain cases passed`.
- Renamed case, quoted verbatim from run output: `ok    schema_version 2 refuses an undeclared step key, names it and gives its route`
- Assertion (`tests/integration/test-check-domain.py:85-89`) checks `strict.returncode == 2`, `"undeclared step key"`,
  `"rogue_step_key"`, `"run-state-schema.json"`, and `` "`evidence`" `` all in `strict.stderr`.
- Emitter trace: `.claude/skills/harness/bin/check-domain.sh:1653-1659`, inside the
  `if _schema_errors:` block reached only on the undeclared-step-key/evidence-shape path (guarded by
  `_valid_version and isinstance(doc, dict)` at :1618, populated via `_offending`/`_schema_errors` at
  :1630-1651). `"run-state-schema.json"` is emitted at line 1656, backticked `` `evidence` `` at line
  1657 — **both substrings originate in this one block**; the only other occurrence of
  `"run-state-schema.json"` in the file is inside the separate `except Exception` branch at :1660-1667
  ("run-state schema CANNOT be checked"), which fires on a schema-load failure, not on the
  undeclared-key path exercised by this test, so there is no coincidental double-emitter risk.
- **Caveat (reasoned, not measured):** per DEC-174 no mutation of `check-domain.sh` was performed to
  independently prove red capability outside this test. Red-capability assurance rests on reading the
  guard condition (`_schema_errors` populated only when `_offending`/`iter_errors` finds a mismatch)
  plus the test's own pre-change-vs-current comparison; the c9 QA reviewer note at
  `notes/review-harness-qa-c9.md` reasoned the same way for the parent behavior.

## Per-kind matrix — resolved for THIS DELTA only (single test file, no source/doc changed)
Change is `bugfix`-shaped: `always: []`; `when` predicates: `touches_runtime_code` (unit),
`fix_confined_to_tests_and_contract_docs` (integration), `match_bug_class` (unresolved placeholder,
per repo Expertise G-08, never fires in this project).

| kind | required? (this delta) | runner state | command | exit | resolution |
|---|---|---|---|---|---|
| unit | no — no runtime code touched | active | `run-unit-tests.sh --kind unit` | 0 | satisfied (ran anyway, exceeds floor) |
| integration | **yes** — `fix_confined_to_tests_and_contract_docs` fires (delta is 100% test-file) | active | `run-unit-tests.sh --kind integration` | 0 | satisfied |
| functional | no | excluded (DEC-187, no 3rd bucket) | null | — | not_applicable |
| component | no | unresolved (cmd null) | null | — | not_applicable — not obligated by any bugfix leg, and kind itself has no cmd |
| ui | no | unresolved (cmd null) | null | — | not_applicable — not obligated |
| eval | no — not `ai_behavior` | excluded (DEC-187) | null | — | not_applicable |
| typecheck | no — no `.ts`/`.tsx` in delta, not in matrix | unresolved | null | — | not_applicable |

`matrix_ok: true` for this delta. Note (O-03/P-13 style caveat): this resolves ONLY the cycle delta as
dispatched; it does not re-derive the whole-feature `matrix_ok` (merge-base..HEAD), which was already
established at the `168f875f` panel pin and is out of this dispatch's scope.

## Carried forward, unchanged, not re-litigated
- **F-QA-1** (standing advisory): T-05 declares `change_type: logic` while DEC-212's
  `touches_config_shape` predicate would apply to `run-state-schema.json`'s shape changes — still open.
- **Q9**: REQ-08's generic-lead archive exemption at `validate-digest.py:1407` has no test able to
  redden it — still open.

## Lead run-state validation (read-only, measurement only)
`bash .claude/skills/harness/bin/check-state.sh` (full sweep; no per-file mode exists) → overall
`rc=1` from 5 `VIOLATION` lines, **none referencing FEAT-104** (they're stale-worktree INV-29 hits on
other features). For this feature's seeded state at
`runs/2026-09-10-12-qa-gate-validator/state.yaml`: **no VIOLATION** against `run-state-schema.json`
(schema_version 2, all keys present and typed) — **PASS**, no offending key. One `note` (not a
violation): `"FEAT-104-strict-digest-schema: run dir 2026-09-10-12-qa-gate-validator exists on disk
but feature.json does not record it — orphaned work (interrupted flow?)"` — this is a feature.json
bookkeeping gap, the lead's own domain; reported, not corrected.

## Final tree state
`git status --porcelain` (post-run) → 4 untracked files under `notes/receipt-*-simplify-sc08.md`
(other agents' concurrent output, not mine). **Zero tracked modifications** — no file under `tests/`
or any gate script was touched.

## Digest fields
`matrix_ok: true`, `unit: exit 0 / 36 files (baseline 36)`, `integration: exit 0 / 70 files (baseline 70)`,
`cycles_used: 0` (from lead's seeded state.yaml).
