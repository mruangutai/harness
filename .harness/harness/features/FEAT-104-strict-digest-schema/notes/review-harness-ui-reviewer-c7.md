# UI Review (Mode B) — FEAT-104-strict-digest-schema — pin 6126ac07

## Scope verdict — census taken

`git diff abff2a84 6126ac07 --name-only` → 56 files. Extension census over that full list:
`md`×39, `py`×6, `html`×4, `sh`×2, `json`×2, `yaml`×1, `txt`×1, `fixture`×1. **Zero** hits on
`css/scss/less/tsx/jsx/vue/svelte`.

The 4 `.html` hits are `notes/ship-review-plan-signature-c{1,2,3,4}.html`. Opened all four
(head+tail): each ends `<p class='derived'>Derived from ship-review-plan-signature-cN.md —
the markdown is the record; do not edit this file. Regenerate with bin/render-brief.py.</p>`.
These are generated ship-review report artifacts from an earlier plan-signature cycle, not
authored product UI (repo Expertise P-02/G-11 — confirmed directly, not assumed).

**Ruling: no rendered UI surface in this diff.** The remaining 52 files are enforcement
scripts (`validate-digest.py`, `check-domain.sh`, `check-state.sh`), a JSON schema, agent/skill
instruction markdown, a decision log, and tests/fixtures — none of it a rendered surface this
role's Mode A/B lens (spacing/colour/states/interaction/contrast) applies to.

## Adjacent surface named in dispatch — ruled IN SCOPE

Operator-facing rejection text is CLI output, not rendered UI, but it is text an operator/agent
reads and acts on, which is squarely this role's actionability lens even with no HTML/CSS
involved. I graded it.

### Grade against REQ-05 / SC-05 / SC-07 / SC-08

**Digest-key rejection** — `validate-digest.py:1412-1419` (undeclared-key message):
- Names **every** offending key via `", ".join(repr(field) for field in undeclared)` →
  satisfies REQ-05's "names every offending key" and SC-07 (one message, all three keys —
  confirmed against the message's own test, `tests/integration/test-validate-digest.py:3128-3145`,
  which asserts all three literal key names plus `PASSTHROUGH`/`DOCUMENTED_OPTIONAL`/`SCHEMAS`
  as substrings of the single message).
- Names the declared-route **symbols** (`PASSTHROUGH`, `DOCUMENTED_OPTIONAL`, `SCHEMAS`, and
  `DEC-216`) → satisfies the "symbol" half of SC-08.
- **Does NOT name the file.** Grepped the literal string `validate-digest.py` across the whole
  file (`.claude/skills/harness/bin/validate-digest.py`): it appears only in the usage banner and
  docstring (lines 18-19, 2030), never inside an emitted error string. SC-08's own text is
  explicit: *"the declaration route by file and symbol — `validate-digest.py`
  `SCHEMAS`/`PASSTHROUGH` for a digest key"* — the file half is unmet. The criterion's own
  verifying test (`test-validate-digest.py:3128-3145`) does not check for the filename substring
  either, so this gap is invisible to the suite that is supposed to gate it.

**Step-key rejection** — `check-domain.sh:1652-1658` (undeclared step key / evidence shape):
names every offending key (`_names = ", ".join(repr(key) for key in sorted(_offending))`) AND
names the file (`.claude/skills/harness/bin/run-state-schema.json`) AND the symbol (`evidence`).
This path fully satisfies REQ-05/SC-08's "by file and symbol" bar.

**Finding — LOW, advisory, non-gating** (in the DEC-174 enforcement-script carve-out, so I
cannot repair it; routing up): the digest-key rejection path is asymmetric with the step-key
path — one names file+symbol, the other names symbol only. Concrete scenario: `PASSTHROUGH`,
`SCHEMAS`, and `DOCUMENTED_OPTIONAL` are each defined in exactly one place in this repo (checked
directly — `grep -rln "^PASSTHROUGH\|^SCHEMAS\|^DOCUMENTED_OPTIONAL" --include=*.py .` returns
only `validate-digest.py` for all three), so practical actionability holds *today, in this
specific repo*. But the message text itself gives no such guarantee — nothing in the string tells
a reader which file to open, unlike the step-key message which states the path outright. If this
convention is ever reused in a second hook script, or read by someone unfamiliar with this one
file, the digest-key message stops being self-sufficient while the step-key message would not.
This is a literal SC-08 shortfall (file half unmet) that also escapes SC-08's own test, not a
functional or accessibility defect — I rate it LOW, non-gating.

SC-05 is a coverage assertion (which fields validate), not a wording criterion, so it does not
bear on message-text grading; not assessed here.

## Accessibility / theme parity
Not applicable — no rendered surface, no colour-only state encoding, batch/CLI text only (repo
Expertise G-02).

## Verdict rationale
No `must_fix`. One LOW advisory finding on operator text, non-gating per this role's own rule
(pure aesthetic/wording preference on a permitted contract never gates). Severity_max: low.

```yaml
VERDICT: PASS
DIGEST:
  headline: No rendered UI surface in this diff (confirmed by extension census); the one adjacent CLI-text surface named in dispatch is in-scope and graded, with one LOW non-gating finding.
  mode: B
  in_scope: true
  severity_max: low
  findings: 1
  must_fix: []
  contract_violations:
    - { path: ".claude/skills/harness/bin/validate-digest.py:1412-1419", actual: "undeclared-digest-key rejection names offending keys and route symbols (PASSTHROUGH/DOCUMENTED_OPTIONAL/SCHEMAS) but never names the file `validate-digest.py`", specified: "SC-08: rejection text names the declaration route by file AND symbol" }
  a11y: []
  open_questions:
    - { id: Q1, question: "SC-08's own test (test-validate-digest.py:3128-3145) checks for key names and symbol names but never asserts the literal substring `validate-digest.py` in the digest-key rejection message — is the LOW file-naming gap acceptable as-is, or should a follow-up task add the filename to the message and its test? This falls inside the DEC-174 carve-out so no reviewer on this panel can edit it directly.", blocking: false }
  files_touched: []
  expertise_update: []
artifact: /Users/molchairuangutai/GitHub/harness/.claude/worktrees/harness/FEAT-104-strict-digest-schema/.harness/harness/features/FEAT-104-strict-digest-schema/notes/review-harness-ui-reviewer-c7.md
```
