# QA gate c10 — PF-C10-01 re-gate at 984bd26b

**BLUF: PASS.** Both required kinds ran clean at HEAD, discovery matches the pinned baseline exactly,
the focused suite is a true 13/13, and read-only probing of the two riskiest un-exercised shapes
(mixed missing+undeclared, non-mapping step) found no silent-accept regression. One cosmetic
message-labeling gap noted as advisory, not blocking.

## 1. Change-type derivation

`git diff 790023f0 984bd26b --stat`: 14 files, but 12 are feature-tree notes/receipts/STATE.md/
feature.json — non-code artifacts from prior review/simplify cycles. The **code** delta is exactly
two files: `.claude/skills/harness/bin/check-domain.sh` (+34/-8, executable runtime governance code)
and `tests/integration/test-check-domain.py` (+6, one new integration case). This is a **bugfix**:
`touches_runtime_code=true` (check-domain.sh is executed, not just docs) → `unit` required.
`fix_confined_to_tests_and_contract_docs=false` (runtime code changed, not just the test) → that
`when` leg does not itself fire — but the fix's own regression test lives in
`tests/integration/`, and the matrix is a floor, not a ceiling, so `integration` is required
regardless. `run-state-schema.json` itself is untouched, so `config`/`touches_config_shape` does not
apply to this delta (F-QA-1's open question about T-05 is about a different task, not this diff).
**Required kinds: unit + integration. Both run below.**

## 2. Canonical suite at HEAD

- `env -u HARNESS_AGENT_TYPE .claude/skills/harness/bin/run-unit-tests.sh --kind unit`: **exit 0**,
  36 files, `grep -c '^FAIL '` = 4 — all 4 are inside `test-factory-claim-mutation.py`'s own deliberate
  mutation-proof output (`MUTANT ACTIVE` / `FAIL BUG-1290 5a/5b/5c` printed under a live-tee capture,
  immediately followed by `PASS test-factory-claim-mutation.py`); zero real failures.
- `env -u HARNESS_AGENT_TYPE .claude/skills/harness/bin/run-unit-tests.sh --kind integration`:
  **exit 0**, 70 files, 0 `^FAIL ` lines.
- `--kind all`: not run separately — read `run-unit-tests.sh:26-31`, `all` is literally
  `SCRIPTS=(tests/unit/test-*.py tests/integration/test-*.py)`, the textual union of the two globs
  already run; no distinct behavior to verify.

## 3. Discovery vs pinned baseline (790023f0/168f875f: 36 unit / 70 integration)

Runner's own pool-summary line reported **36 unit files** and **70 integration files** — exact match,
zero drift either direction.

## 4. Focused suite alone

`env -u HARNESS_AGENT_TYPE python3 tests/integration/test-check-domain.py`: **13/13 T-06 check-domain
cases passed**, exit 0. Denominator is 13 as claimed, up from the 12/13 RED witness.

## 5. Reasoned RED-capability check (no mutation, per DEC-174)

Read `test-check-domain.py:82-95` and `check-domain.sh:1645-1685`. The new case's conjunction
(`returncode==2` AND `"missing required step key" in stderr` AND `"status" in stderr`) can be
satisfied ONLY by the `if _missing_required:` block (lines 1660-1668). The `except Exception` route
(1678-1685) emits a structurally different head, `"run-state schema CANNOT be checked"`, which does
not contain the literal substring `"missing required step key"` — it cannot satisfy the conjunction
short of an implausible exception message collision. The `_offending`/"undeclared step key" branch
(1669-1677) also does not emit that phrase. **No other branch can satisfy this assertion — the case
is reasoned, not mutation-proven, per DEC-174's prohibition on editing this file.**

## 6. Read-only mixed/non-mapping probes (`mktemp -d` via `check_domain_support.fixture()`)

Script: `/tmp/qa_probe_mixed.py` (disposable, outside worktree; not tracked).

- **(a) mixed** — `status` omitted AND `rogue_step_key` added on a `schema_version: 2` step: exit 2,
  stderr carries BOTH heads — `missing required step key.` / `missing key(s): 'status'` AND
  `undeclared step key or evidence shape.` / `offending key(s): 'rogue_step_key'`. Both diagnostics
  fire together as required.
- **(b) non-mapping step** (`steps: ["just a string"]`): exit 2, `undeclared step key or evidence
  shape.` / `offending key(s): '<step>'`. Still refused.
- **(c) declared-field type error** (`cycles: not-an-int`, exercised beyond the assignment's ask,
  cheap and load-bearing for the regression question): exit 2, but reported under
  `offending key(s): 'cycles'` — a **cosmetic mislabel** (a type error on a *declared* field is
  described as "undeclared step key"), not a silent-accept. Advisory only.

**No accept-shape regression found.** Reasoning: every manual accumulation into `_offending`/
`_missing_required` (lines 1630-1644, computed independent of jsonschema's error list) corresponds
1:1 with a schema vocabulary jsonschema itself would also flag for that same condition
(`additionalProperties`, `propertyNames`, `oneOf`, `type`, `required`) — so `_schema_errors` is never
empty when a manual violation exists, keeping the `if _schema_errors:` gate (line 1645) truthy and at
least one message always emitted. Validator families **exercised** live: `required` (dict instance,
existing case + probe a), `additionalProperties` (existing "refuses undeclared" case + probe a),
`propertyNames` (existing evidence-space-key case), `oneOf` (existing nested-evidence case), `type`
on whole-step (probe b), `type` on a declared scalar (probe c). Families **reasoned about, not
separately constructed**: `required` firing on a non-dict instance (jsonschema's own `required`
implementation only evaluates object instances, so this is structurally unreachable, confirmed by
reading jsonschema's validator behavior rather than a live case) and errors whose `_path` is
genuinely empty for a reason other than `additionalProperties`/whole-step `type` (none exist in this
schema's shape — the two families producing empty paths are both covered above).

## 7. Tree state — re-run at the true end (send-back)

`git -C <worktree> status --porcelain -- .claude tests`: **empty output.** No tracked file under
`.claude/` or `tests/` shows as modified — no DEC-174 violation.

`git -C <worktree> diff --stat HEAD -- .claude/skills/harness/bin/check-domain.sh
.claude/skills/harness/bin/run-state-schema.json tests/integration/test-check-domain.py`: **empty
output.** All three carve-out files are byte-identical to HEAD.

`git -C <worktree> status --porcelain`, full output (6 entries, all untracked, all attributed):
- `.harness/harness/features/FEAT-104-strict-digest-schema/notes/qa-2026-09-10-15.md` — **mine**,
  this artifact.
- `notes/receipt-harness-backend-dev-2026-09-10-16-simplify-eng-reuse.md` — sibling `SimplifyC10`
  run (a `SecretParrotfish`/`SlightScorpion` backend-dev leaf).
- `notes/receipt-harness-backend-dev-2026-09-10-16-simplify-eng-simplification.md` — sibling
  `SimplifyC10` run (the other backend-dev leaf).
- `notes/receipt-harness-data-engineer-2026-09-10-16-simplify-eng-enumeration.md` — sibling
  `SimplifyC10` run (`PrintedPig`).
- `notes/receipt-harness-dev-ops-2026-09-10-16-simplify-eng-altitude.md` — sibling `SimplifyC10` run
  (a `HugePlatypus`/`CharacteristicDog` dev-ops leaf).
- `notes/receipt-harness-dev-ops-2026-09-10-16-simplify-eng-efficiency.md` — sibling `SimplifyC10`
  run (the other dev-ops leaf).

All five sibling entries map 1:1 onto the five `SimplifyC10.*` leaf personas visible on the roster
(2 backend-dev, 1 data-engineer, 2 dev-ops). **Zero unexplained entries.**

## 8. Send-back: delta hunk enumeration

`git diff 790023f0 984bd26b --numstat -- check-domain.sh tests/integration/test-check-domain.py`:
`26\t8\t.claude/skills/harness/bin/check-domain.sh` and `6\t0\ttests/integration/test-check-domain.py`.
**Resolved: the dispatch's `+26/-8` is correct.** My first-pass `+34/-8` was a misread of the
`--stat` graph column, which prints the *total changed lines* (26+8=34) for that file, not the
insertion count — a `--numstat` re-read confirms 26 insertions, matching the dispatch exactly.

`git diff 790023f0 984bd26b -U0 -- check-domain.sh` hunk headers, verbatim, all three inside
`shape_problems()`:
- `@@ -1647,0 +1648 @@` — inserts one line, `_missing_required = set()` (new accumulator init).
- `@@ -1648,0 +1650,7 @@` — inserts the new `if (_error.validator == "required" and
  isinstance(_error.instance, dict)): _missing_required.update(...); continue` branch that diverts
  `required`-validator errors into the new set before the old `_offending`-only path runs.
- `@@ -1652,8 +1660,18 @@` — replaces the old unconditional single-head "undeclared step key or
  evidence shape" emission (8 lines) with the two-headed emission (18 lines): a `missing key(s)`
  block gated on `if _missing_required:` and the pre-existing `undeclared step key` block now gated
  on `if _offending:` instead of running unconditionally.

**All three hunks are confined to the `_missing_required`/`_offending` split inside `shape_problems`**
— read against the full diff (reproduced above in this pass), there is no line of the change outside
lines 1647–1685 of that one function. `git diff 790023f0 984bd26b --stat -- . ':!.harness'` lists
only `check-domain.sh` and `tests/integration/test-check-domain.py` — **no third code file** is in
the delta.

## Findings, ranked

1. (advisory, low) A `type` error on a *declared* step field surfaces under the "undeclared step key
   or evidence shape" head/message rather than a dedicated "invalid step field" message — functionally
   correct (still refused, exit 2) but the diagnostic text is misleading about which failure occurred.
   Not blocking; a future cycle could split this into a third head.
2. (resolved, send-back) First-pass line-count discrepancy (`+34/-8` vs dispatch's `+26/-8`) was a
   misread of the `--stat` graph column; `--numstat` confirms `+26/-8` — see §8.

## Open questions
- none blocking this gate.

```yaml
VERDICT: PASS
DIGEST:
  headline: "984bd26b re-gated clean and send-back doubts closed: 26/8 line count confirmed via --numstat (not +34/-8), all 3 hunks confined to shape_problems' _missing_required/_offending split with no third code file, status --porcelain -- .claude tests empty, carve-out files byte-identical to HEAD, all 6 untracked entries attributed."
  suite: pass
  failures: 0
  matrix_ok: true
  kinds:
    - { kind: unit, state: satisfied, cmd: ".claude/skills/harness/bin/run-unit-tests.sh --kind unit", named_tests: 36 }
    - { kind: integration, state: satisfied, cmd: ".claude/skills/harness/bin/run-unit-tests.sh --kind integration", named_tests: 70 }
  coverage_gaps: []
  sc_evidence:
    - { id: PF-C10-01, test: "tests/integration/test-check-domain.py:92-95 (missing required step key case, 13/13 exit 0)" }
  open_questions: []
  files_touched: []
  expertise_update: []
artifact: /Users/molchairuangutai/GitHub/harness/.claude/worktrees/harness/FEAT-104-strict-digest-schema/.harness/harness/features/FEAT-104-strict-digest-schema/notes/qa-2026-09-10-15.md
```
