# Observations - harness-orchestrator

- 2026-09-09: FEAT-104 plan. check-state.sh INV-26's not-started skip is `all(status == "ready")` (check-state.sh:2136-2148), so ONE task at `abandoned` on an unsigned plan trips a false 'the mirror never ran' violation. I first 'fixed' it by setting the other nine tasks to `plan` — wrong: `ready` IS the not-started station per FEAT-41 T-04, and the change made the skip strictly less likely to fire. Proved the real cause by deep-copying the plan doc in memory and dropping T-02: skip fires without it, not with it. Read the guard before moving stations to satisfy a gate.
- 2026-09-09: FEAT-104 plan. The plan-phase panel is THREE readers to check-state.sh (`expected_readers = {should-not-exist, scope, goalcheck}`, :534), not the two in plan-panel.yaml. pm refused to write the goalcheck entry as an invention; it was not one — the playbook's panel segment 1 IS that reader. An unrecorded reader is a BAD line at INV-32 the moment approval flips, so the operator would have signed into a red gate.
- 2026-09-09: FEAT-104 plan. Verifying two panel finding premises myself before routing cost about four minutes total (grep of the persona blocks, sed of the test file) and both held. The one I did NOT verify — a claimed off-by-one in the read tool's line header — did not reproduce, and would have entered the signature packet as a filed defect had I relayed it.
- 2026-09-09: FEAT-104. inflight_registry.py resolves .harness/.inflight-claims.json PER ROOT, so a bare `list` from the main checkout returns NO CLAIMS while a guard in a worktree is actively refusing. I read that as 'the claim expired' and wrote it into a run digest AND an operator packet before a peer corrected me. `list --root <that worktree>` plus `ps` on supervisor_pid is the real measurement: PID 9988 alive, start time matching supervisor_started_at, claim LIVE. Releasing would have cut a running flow. A registry read is scoped to a root the same way a gate read is (cf. G-15 on check-state.sh) and the negative answer looks identical to the true one.
- 2026-09-09: FEAT-104. check-domain.sh enforces single-flight by agent TYPE across every linked worktree, so a concurrent feature holding harness-product-lead refuses MY feature's product-lead writes into MY OWN run dir — mid-run, after earlier writes to the same path succeeded. Blast radius is runs/** only; notes/ is granted per persona and lands fine. The fix from the tier above: have the lead return its digest inline and write it yourself, carrying the FENCED CONTRACT BLOCK — prose alone fails validate-digest.py and costs a round.
- 2026-09-09: FEAT-104. A retained inflight claim proves its SUPERVISOR pid is alive, not that the run is: the pid is a long-lived omp supervisor, so a claim leaked by an interrupted dispatch cannot expire inside its lifetime and `reconcile` returns RECONCILED 0 correctly. The discriminating read is the OTHER feature's run states — glob its runs/*/state.yaml for a non-complete status. Measured on FEAT-57: its validator claim had a live run (2026-09-09-02-validator, step running, mtime 872s), its product-lead claim had none. Still not mine to release: an absent state.yaml is not proof of an absent run, since a lead blocked from writing its own run dir is exactly this failure.
- 2026-09-09: FEAT-104. An absent or stale run `state.yaml` is NOT evidence of an absent run. Under a persona-scoped claim collision a lead cannot write its own run dir at all while its `notes/` artifacts land perfectly, so a missing checkpoint is the SIGNATURE of the collision, not of a leak — the two are indistinguishable from outside the run. Releasing on that evidence destroys exactly the run that cannot defend itself. Route the discriminating read up as a measurement; the release decision belongs to the tier that can see which flows are live.
- 2026-09-09: FEAT-104. When co-occurring defects share one incident, report them as SEPARATE rows. Three here: (1) type-scoped single-flight across linked worktrees, (2) per-ROOT registry resolution making a bare `list` answer about the wrong checkout, (3) claim retention bound to a long-lived supervisor pid rather than the run. Folding them is how a fix closes one and leaves the other two live. A peer's escalation carried all three as one; splitting them was the improvement.
- 2026-09-09: FEAT-104. I wrote 'do not re-investigate, this is settled' into a dispatch about a tooling capability, and the premise was FALSE — true at my worktree's HEAD 25918bd2, false at the control plane where a delete verb had landed the same day. pm checked anyway and refused to write on it, which is the only reason the operator's ruling got executed rather than approximated. A dispatch that forbids re-investigation of a premise disarms the people best placed to correct it; state the premise WITH its measurement basis ("true at HEAD X as of DATE") instead of as settled fact.
- 2026-09-09: FEAT-104. Tooling lives at the CONTROL PLANE, not at the feature worktree's HEAD. A worktree cut days ago carries a stale copy of every bin/ script, so `--help` there answers about history. Check <control-plane>/.agents/skills/harness/bin/ before concluding a verb does not exist, and invoke the control-plane script by absolute path against the worktree's file when the local copy lags.
- 2026-09-09: FEAT-104. When landing another agent's stranded run record from the tier above, PRESERVE its run_uid from the existing state.yaml. I generated a fresh one and check-state.sh INV-36 correctly reported the directory as occupied by a different run than the witness described. The uid is the record's identity, not a formality; only fabricate one when no prior state.yaml exists, and disclose it when you do.
- 2026-09-09: FEAT-104 build entry — every Build precondition was green (both approvals signed, gh-sync open receipt with 9 sub-issues) and the phase was STILL undispatchable: 8 of 9 tasks main-session-direct, and the single team task T-09 depends_on four of them. An all-green precondition set says nothing about whether a lane exists to execute through; the lane census is a separate read.
- 2026-09-09: FEAT-104 handoff-plan.md '## Next' told me to dispatch T-10 to harness-eng-lead while plan.yaml:643 lanes T-10 main-session-direct. The predecessor wrote the correct caveat two sentences later ('read the lanes: table before routing anything') and still got its own Next wrong. Validating Next against the plan is not ceremony — it caught a real misroute on the first step of the phase.
- 2026-09-09: FEAT-104 T-10 is main-session-direct for TOOL REACH (owner-root manifest, gitignored runs/), not for the DEC-174 carve-out, unlike the other seven. Two different reasons wearing one execution_mode value; only the execution_reason field tells them apart, and only one of them is an operator-changeable lane.
- 2026-09-09 (FEAT-104): the commit I dispatched two segments against (99035a9c) was AMENDED AWAY mid-run by the main session; the new tip 168f875f was its SIBLING, not its descendant. Both leads independently read the missing hunk as an unattributed revert and one raised it as a high must_fix. Nothing inside a run can tell an amend from a revert. Cheap discriminator I should have run BEFORE dispatching: `git merge-base <assigned-sha> HEAD` plus a per-file md5 of the graded files across the old pin, the assigned sha and the tip — that table settled the whole question in one call afterwards, and it would have scoped the dispatches correctly the first time.
- 2026-09-09 (FEAT-104): my own dispatch caused a falsified measurement. I told qa to run the at-rest corpus sweep "from the main checkout" (citing the run-it-where-the-features-are rule) but FEAT-104's runs/ tree is GITIGNORED and exists only in the worktree, so the sweep was structurally blind to every strict record the feature produced: it reported discovery 309 / strict_count 0 and concluded the finding was a no-op. Re-taken from the worktree: 324 / 4, with 1 of the 4 actually failing. The checkout that holds a feature's TRACKED files is not the checkout that holds its gitignored ones.
- 2026-09-09 (FEAT-104): two lead run digests ended contract-invalid on disk (one missing a required key, one declaring PASS beside a FAIL member step) and NEITHER could be repaired: check-domain.sh:1327 only permits a strict textual prefix-append correction, while validate-digest.py's parser stops at the indent-0 `artifact:` line, so appended text is never parsed. The owning lead was refused twice and had to supersede with a new run dir. Validating a lead's digest with `validate-digest.py <persona> <path>` the moment it returns is worth doing — after the run closes there is no repair route at all.
- 2026-09-09 (FEAT-104): both lead task results came back marked failed/exit 1 with "Subagent called yield with null data" while their fenced digest and their on-disk artifacts were complete and valid. The transport verdict and the digest verdict are independent; reading only the job status would have discarded two good runs.
- 2026-09-10: FEAT-104 re-panel. My dispatch REQUIRED a top-level `code_grade` in the validator
  lead's digest, but `SCHEMAS['lead']` declares no such field, so the demand collided with the very
  undeclared-key rejection this feature builds; the lead carried it on the `code` member entry and
  raised the contradiction as Q4. Before demanding a named field from a persona's digest, read that
  persona's schema entry — a dispatch can ask for something the contract forbids.
- 2026-09-10: FEAT-104 cycle accounting. The predecessor handoff said increment to 9 for the
  re-panel; I kept 8. The F1/F3 fix loop was already counted at 7 to 8, and the qa re-run, simplify
  re-run and panel re-run are all the re-verify LEG of that one loop. DEC-157 counts loops, not runs
  — its own rationale names counting forward runs as the failure mode. A predecessor's cycle
  recommendation is a claim, not an instruction.
- 2026-09-10: FEAT-104. Both budget-gated feature files rejected my FIRST write on length —
  STATE.md at 143 lines against a 120 cap, handoff-validate.md at 62 against 60. Both caps are
  enforced at Write time by check-domain, so a full draft then a trim costs two writes every time.
  Draft to the cap, not to completeness, and move detail into the run digest which has no cap.
- 2026-09-10: FEAT-104. Telling the panel's qa step to ADOPT same-pin QA evidence and to state the
  adoption plainly in its note worked: it verified provenance by md5 rather than trusting the path,
  re-ran only the two applicable kinds, and disclosed that matrix_ok was adopted. An explicit
  licence to adopt plus a duty to disclose beats both a forced full re-run and a silent adoption.
- 2026-09-10: FEAT-104. Runs 05 through 10 (six of twenty) all trace to ONE cause: a commit was
  amended while a validator cycle was live grading it, so the graded base became a SIBLING of the
  tip rather than its ancestor and everything needed re-grading. That is the expensive shape to
  watch for, not a high run count on its own.
- 2026-09-10: FEAT-104 goal-check. A four-reviewer panel PASSED at 168f875f with must_fix empty, and
  the pm goal-check at the SAME pin then found SC-08 unmet. The panel graded the diff; the goal-check
  graded the CRITERIA. SC-08 named two seams (digest key, step key) and demanded each "asserted as a
  substring"; delivery emitted both, only the digest seam had a test asserting it. Nobody reviewing a
  correct diff can see a missing assertion the diff was never required to add. Direct evidence for
  OQ-01: panel and goal-check have disjoint blind spots, and this is the first case where the
  goal-check caught what the panel could not.
- 2026-09-10: the SC-08 remedy was one assertion inside a DEC-174 carve-out test file, so no squad
  could take it at any severity. The routing that mattered was not "who fixes it" but "which of the
  two routes needs a signature": ADDING the assertion satisfies an already-approved criterion and
  needs none; NARROWING the criterion amends a signed BRIEF and is the operator's alone. Naming that
  split in the return turned an unroutable FAIL into a one-step decision for the tier above.
- 2026-09-10: bash-write-guard misparsed `mktemp /tmp/orch-sc12-XXXX.txt` and refused the command,
  reporting the redirect target as "xx" — the X-template, not the redirect, is what confused it.
  Doing the same mutation in memory inside python -c produced identical evidence with no temp file.
  A discrimination proof rarely needs a file on disk.
- 2026-09-10: SECOND time I demanded a digest field the CLOSED contract forbids. c9 asked a `lead` digest for `code_grade`; this cycle asked a qa-gate lead for "per-kind exits, both file counts", and it invented `suite_results` to comply. The lead was obedient and correct; the acceptance criterion was the defect. Before writing "your DIGEST must carry X", check X against validate-digest.py's SCHEMAS/PASSTHROUGH/DOCUMENTED_OPTIONAL — per-run measurements belong in the run state's `evidence` container, which is exactly where this lead had already put all of them.
- 2026-09-10: MEASURED that a run digest's append-only correction channel CAN repair an undeclared-key violation — validate-digest.py parses the LAST fenced yaml block, not the first. STATE's Q6 asserted the opposite ("parser stops at the indent-0 artifact: line") and I nearly inherited it and spent a 9-minute lead re-spawn plus a second run dir. Probe was bounded: copy the digest to a temp path, append a corrected block, run the validator. Two probes, 0.3s total. The channel still cannot repair a MISSING required field or a verdict contradiction, so Q6 was narrowed rather than deleted.
- 2026-09-10: An acceptance criterion phrased as "zero ^FAIL lines" is unsatisfiable-as-literal on this repo's GREEN unit suite: tests/unit/test-factory-claim-mutation.py prints `FAIL  BUG-1290 <case>:` as its own passing mutation proof. Phrase suite acceptance as "runner exit 0 and zero failing FILES" plus a discovery-count floor; the token count is not the signal.
- 2026-09-10: Naming the BASELINE discovery counts (unit 36, integration 70) in the qa dispatch made the lead report counts against them and let me detect at a glance that nothing had been dropped. A green exit code cannot see a vanished test file; the count comparison is what can. Cheap to include, and the lead adopted the framing without being asked twice.
- 2026-09-10: `check-domain.sh --resolve <path>` printed BOTH harness-orchestrator and harness-validator-lead for a runs/*-validator/ digest, which is how I established I could append the correction myself instead of re-spawning the lead. Running the guard beat reading the org config, again.
- 2026-09-10 (FEAT-104 c10): a panel can return `must_fix: []` and still gate. gates.review is advisory_unless_high, so a CONTESTED severity (ui high vs lead med) on a single finding decides the ship while the gate field itself reads clean — read the split, not just must_fix.
- 2026-09-10 (FEAT-104 c10): reproduced a reviewer's finding myself under a read-only carve-out by importing the standing test module's own `_fire_new` helper from /tmp and firing four payloads into disposable fixture roots. No tracked file written, DEC-174 intact, and the relayed claim became a measurement with two controls (type-error and rogue-key) that discriminated the branch.
- 2026-09-10 (FEAT-104 c10): naming the persona's DECLARED digest field set in the dispatch, and forbidding extras explicitly, stopped the third recurrence of the undeclared-key digest repair (Q4, Q-B3). The lead's return validated at exit 0 first time where the two prior dispatches demanded fields the closed contract has no home for.
- 2026-09-10 (FEAT-104 c10): STATE.md's 120-line shape gate cost four rejected writes. Budget the Current section BEFORE composing it; the guard counts the proposed content, so the file on disk stays at the old revision through every rejection and `wc -l` reads the OLD file, not the attempt.
- 2026-09-10: FEAT-104 c10. A fix that splits ONE unconditional denial into TWO conditional messages
  creates an accept-shape hole by construction: before, any non-empty error list denied; after, an
  error populating neither bucket emits nothing and the write is ACCEPTED. I made the enumeration of
  every keyword that can report at the container's own path the GATING question in both dispatches
  rather than trusting a green suite — no existing test could have caught it, since the shape has no
  case. Both segments answered it independently and agreed (3 keywords, all still caught upstream).
- 2026-09-10: FEAT-104 c10. Dispatching qa (validator) and simplify (eng) concurrently over one
  read-only commit cost nothing and produced independent corroboration of the same enumeration — and
  each found the SAME residual (a declared-key type error still printing under the "undeclared step
  key" head) from opposite directions, qa by live probe and eng by reading. Two lenses agreeing on a
  finding neither was asked for is worth more than either alone.
- 2026-09-10: FEAT-104 c10. Counting `^FAIL ` lines to grade run-unit-tests.sh is DEFEATED here:
  tests/unit/test-factory-claim-mutation.py reprints 4 `FAIL BUG-1290 ...` lines as its own success
  output between `MUTANT ACTIVE` and `MUTATION PROOF`, at exit 0. My own expertise P-01 says to count
  those lines; the runner's exit status is the only reliable signal, and the FAIL census needs a
  `^FAIL <script>` shape to mean anything.
- 2026-09-10: FEAT-104 c10. The "yield called with null data" exit-1 defect hit the ENG LEAD itself
  plus two dev-ops members this cycle, having previously only been seen on harness-qa. The fenced
  digest and the on-disk artifact were complete and validated at exit 0 in every case. Verifying the
  artifact on disk and accepting the return is correct; re-dispatching would have duplicated twenty
  minutes of work over a transport defect.
- 2026-09-10: FEAT-104 c10. STATE.md's 120-line shape gate refused twice (141, then 122 lines) before
  accepting. Budget the OPEN QUESTIONS list first when a feature accumulates seventeen of them: the
  narrative compresses easily, the question list does not, and each refusal costs a full re-emit.
- 2026-09-10 (FEAT-104 c11): the reviewer that rated a defect `high` at c10 (`harness-ui-reviewer`, PF-C10-01) MEASURED its own closure at the new pin and WITHDREW the rating. Re-dispatching the SAME panel over the fix — rather than a fresh reader — is what made the retraction available; a new reviewer could only have failed to find it, which is weaker evidence.
- 2026-09-10 (FEAT-104 c11): forbidding adoption of the previous cycle's enumeration bought four independent re-derivations of the same fail-OPEN hypothesis, and the security lens found a mechanism (nested `required` mis-bucketing, no path check) that the other three missed while agreeing on the conclusion. Agreement across lenses is not redundancy when the derivations are independent.
- 2026-09-10 (FEAT-104 c11): STATE.md's DEC-150 shape gate rejected at 144 then 122 lines against a 120 budget; two rewrites. Pre-count lines before writing a panel-cycle STATE, and put per-finding detail in the run digest where it belongs.
- 2026-09-10 (FEAT-104 c11): telling the lead to carry `code_grade` INSIDE its headline string (the c10 workaround for the lead schema declaring no such field, Q-B3) validated first time at exit 0. Naming the declared field set in the dispatch keeps working.
- 2026-09-10 (FEAT-104 c11): a bookkeeping-only HEAD ahead of `review_sha` needs no re-pin — `git diff --stat <pin>..HEAD -- . ':!.harness'` EMPTY is the discriminator, and stating it in the dispatch stopped four reviewers from each re-deriving whether the working tree matched the pin.
