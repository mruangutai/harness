# Observations - harness-code-reviewer

- 2026-09-10: FEAT-104 c9 re-grade at 168f875f. F1 (schema_version downgrade guard, check-domain.sh:1755-1774), F3 (declaration-route message now names validate-digest.py by path, validate-digest.py:1414), and F2 (check-state.sh's INV-15 sweep passes literal "lead" not the real `_host`, so historical digests skip the new undeclared-key/adequacy_notes checks by design, tested at test-validate-digest.py:2912-2914) all independently re-derived from source/execution, confirmed exactly as characterized in the c9 dispatch. New finding not previously raised: `.harness/harness/features/FEAT-56-central-onboarding-model/plan.yaml` status flip (review->done) is this branch's own ROOT commit (abff2a84) relative to origin/main — an unrelated feature's housekeeping commit riding inside FEAT-104's reviewed diff range, invisible to DEC-174's SC-13 (scoped to 4 enforcement files) and untraced by any FEAT-104 REQ/D. Low severity, non-gating, but worth a general pattern: always `git log --oneline origin/main..review_sha` the WHOLE range, not just `git diff --stat`, to catch a stray root commit belonging to a different feature.
