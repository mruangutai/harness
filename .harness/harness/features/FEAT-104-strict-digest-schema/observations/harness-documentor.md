# Observations - harness-documentor

- 2026-09-09 (FEAT-104 T-09): my first `edit` used the hashline path relative (`.harness/harness/docs/DECISIONS.md`) while my process cwd was the OWNER ROOT, not the dispatched worktree. The two copies were byte-identical, so the `#C087` snapshot tag matched and the append landed silently in the wrong tree; only `wc -l` on the worktree file (still 7090) exposed it. Reverted by truncating the owner-root file to its first 7090 lines after confirming `git diff -U0` showed exactly one hunk, all mine. Absolute paths in every hashline header from the second attempt on.
- 2026-09-09 (FEAT-104 T-09): `gen-decisions-index.py` recomputed DEC-223's `[tags]` from my hand-written `[digest,state,tests,plan]` into `[state,digest,dispatch,domain]` and filled the empty `refs:` with exactly the ten DEC ids named in the entry's `**Record:** refs` line. Writing a plausible left-hand side by hand is wasted effort — only the text right of ` :: ` survives.
- 2026-09-09 (FEAT-104 T-09): the plan claimed the DEC-126 correction "changes the length of an entry near the middle of the file, so every later row's anchor shifts". A 2-line-for-2-line replacement shifts nothing; the whole index diff was one inserted row. Length-change claims in a plan are worth checking before reporting them as an effect of your own edit.
- 2026-09-09 (FEAT-104 T-09): two sibling commits landed mid-run (test files left `git status` between my baseline and my final stat), so HEAD moved off the dispatch-pinned SHA. Taking the baseline `git status` at spawn is what let me attribute the remaining dirt (the lead's own `plan.yaml` task-status flip) rather than claiming it.
- 2026-09-09 (FEAT-104 T-09): the bash write-guard reads an entries heredoc for shell metacharacters — an ASCII arrow inside prose was parsed as a redirect and BLOCKED the whole observations merge. Keep arrows and angle brackets out of observation text.
