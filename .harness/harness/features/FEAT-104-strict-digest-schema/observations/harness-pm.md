# Observations - harness-pm

- 2026-09-09: FEAT-104 — run artifacts (runs/**) are GITIGNORED, so a feature worktree carries zero digest.md and zero state.yaml. A dispatch saying "re-measure in the worktree" is unsatisfiable for corpus work; the record lives only in the owner root working tree (370 digest.md, 356 state.yaml). Record the corpus location in the note or the figures are unreproducible.
- 2026-09-09: FEAT-104 — bash-write-guard parses the command line textually, so a right-angle-bracket character inside a python heredoc AND inside a placeholder in YAML prose both trip the redirect blocker. Writing a plan.yaml proposal through plan-merge.py apply --proposal - means the whole proposal must be free of that character.
- 2026-09-09: FEAT-104 — inferring a digest's persona by maximising the schema-key intersection resolved all 319 readable run digests to lead. Run digest.md is a lead artifact; there are no member digests on disk, so a digest-key-drift measurement is entirely a measurement of lead roll-up behaviour.
- 2026-09-09: FEAT-104 c1. A criterion I wrote over gitignored files used `git status --porcelain --ignored` to detect modification. Git reports an ignored untracked path as `!!` and NEVER as modified, so the clause returned the same answer whether the corpus was rewritten or not. Lesson: before writing a git-status criterion, check whether the paths are tracked at all; for ignored corpora the only decidable mechanism is content hashing against a recorded manifest.
- 2026-09-09: FEAT-104 c1. `check-plan-routes.py` exited 1 on my plan with ZERO task-level VIOLATION lines. The counted violation is the MANIFEST deviation (`:898-900` adds 1 to the total when the worktree `.harness/team-config.yaml` routes differ from the owner's). My repo-tier note that "only VIOLATION lines gate, deviations exit 0" holds for TASK deviations only. Read the summary against the finding lines before treating exit 1 as a plan defect.
- 2026-09-09: FEAT-104 c1. A send-back instructed me to correct `validate-digest.py:1744` to 1745 for the `stop_hook_active` guard, and told me to re-read first. The cycle-0 citation was RIGHT: the `if` is 1744, its `return 0` is 1745, and 1743 is the preceding prefix-check return. Applying an inherited "correction" unverified would have introduced the error. Re-read every line number handed down, including in a send-back from a tier above.
- 2026-09-09: FEAT-104 c1. `plan-merge.py amend --show` REFUSES a list field without `--yaml-value` ("is a list, not text") — the flag is needed for the read, not just the write, so the sha256 needed for the compare-and-swap is unobtainable without it.
- 2026-09-09: FEAT-104 c1. Proving a `verify:` block discriminates is cheapest by loading the stored string out of plan.yaml, splitting off the python conjunct, and running it against fixture trees — that proves the string ON DISK rather than a retyped copy of it. Four fixtures (correct, one misspelled name, one dropped name, additionalProperties true) separated a name-set assertion from the count assertion it replaced.
- 2026-09-09: FEAT-104 — a strictness plan whose key triage was measured on run digest.md artifacts is measured on ONE persona: all 319 readable digests infer as lead, so the 15 fields the other 7 personas' own documented DIGEST blocks instruct (spec_violations, in_scope, threat_model, kinds, sc_evidence, stale_found, needs_prototype, test_kinds_written, ...) were invisible to the corpus and to every criterion derived from it. Grade a closed-key-set change against each persona's TEMPLATE, never against the artifacts on disk.
- 2026-09-09 (FEAT-104 plan fix c1): .claude/agents/*.md is GENERATED from .omp/agents/*.md by .claude/skills/harness/bin/sync-agent-adapters.py, which copies the BODY wholesale (:191-226), not just frontmatter. Two FEAT-104 tasks were drafted to hand-edit both twins 'identically'; --check would report drift the moment they diverge. Edit .omp/, run --apply, assert --check exits 0.
- 2026-09-09 (FEAT-104): tests/integration/test-validate-digest.py already carries CONTRACT_SOURCES (:297-314) mapping all 16 raw personas to their documented block, plus documented_block (:281-286) and documented_contract_gaps (:289-294). The reverse direction (documented -> declared) is a case reusing them, not new machinery - worth grepping for before proposing a parser.
- 2026-09-09 (FEAT-104): a dispatch asserted plan-merge apply 'unions by id, so a changed decision or task is re-proposed under the SAME id'. False - apply exits 7 CONFLICT on any changed value; amend --expect-sha256 is the route. The claim reads plausible and is in a constraint block, so it needs re-deriving rather than obeying.
- 2026-09-09: FEAT-104 panel — plan-merge.py apply CANNOT change an existing top-level panel key: UNION_KEYS is (tasks, decisions) only, so panel falls to step 8 and raises exit 7 CONFLICT (probed both panel-only and whole-document proposals; file untouched both times). set-panel --value-file is the only verb that owns the key; amend --key takes tasks|decisions only. A dispatch naming apply as the write route for panel is unexecutable as written.
- 2026-09-09: FEAT-104 panel — cmd_set_panel re-emits the whole panel block through yaml.safe_dump at default width, so long plain scalars in findings/dismissed/transcription_rule get line-wrapped. Values survive (the verb refuses unless the block reloads equal, plan-merge.py:1063) but bytes move, so a 'keep it byte-identical' instruction over panel cannot be met for anything except short scalars.
- 2026-09-09: FEAT-104 — check-state.sh INV-32 grades APPROVED plans only (gate at :438-487). A pending plan shows zero INV-32 lines, including for an open high finding, so 'no INV-32 output' is never evidence the panel record is complete. Prove the reader branch by running :534-553 against the data and a mutant instead.
- 2026-09-09 (FEAT-104 sigfix-c2): an operator ruling fixed PASSTHROUGH at 5 rows, but the repaired bar it mandated (lead-documented-block-only) literally excluded 2 of the 5 — matrix_ok and coverage_gaps appear in no lead block. Resolved by making T-01 PART 3 CREATE that documentation rather than by weakening the bar or silently retaining the rows; reported the movement either way.
- 2026-09-09 (FEAT-104): plan-merge.py set-panel re-emits the whole panel through yaml.safe_dump, so line wrapping changes even where values do not. Diffed loaded VALUES against git HEAD before applying, not text.
