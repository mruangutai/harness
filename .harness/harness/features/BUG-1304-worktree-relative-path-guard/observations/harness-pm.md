# Observations - harness-pm

- 2026-09-05: BUG-1304 plan. The routing dispatch carried a CORRECTED form of an Advisor ruling (build the claim set by resolving each claim's own feature field through worktree_for_feature, never by which registry file the claim sits in). Recording the correction as its own decision entry rather than folding it into the key's entry kept the defect visible to a reader who only skims decisions.
- 2026-09-05: every anchor in both Advisor notes had drifted 2-5 lines against the worktree (DEC-193 bullet cited 5334-5335, actual 5338-5339; basePayload cited 220-222, actual 215-217). Re-deriving all of them before writing intents cost one tool call; without it a doer would have been sent to the wrong line with a correct instruction.
- 2026-09-05: BUG-1304 goal-check — a plan-time TTL residue hid in plain sight: `_expire` keeps OMP claims live by supervisor identity but expires compatibility-host claims at CLAIM_TTL_SECONDS=1200, so any guard whose input is "the live claim set" silently unbinds long-running agents. A task intent saying "the TTL the module already applies" is not specific enough when the module applies two.
- 2026-09-05: BUG-1304 panel repair. A verify greping `case_name()` to prove a test is WIRED is satisfied by the `def case_name():` line itself; the aggregate in test-inflight-registry.py lists bare names in a CASES tuple, so the wiring grep must be `case_name,`. Two vacuous conjuncts found this way (T-08's `worktree_for_feature`, already at dispatch-guard.sh:174, and T-09's wiring grep) beyond the one the panel reported.
- 2026-09-05: BUG-1304. A "refused with a corrupt registry present" criterion is vacuous when the fixture also carries a readable claim — the ordinary rule refuses it anyway. The discriminating fixture is the agent whose ONLY claim is unreadable, plus a paired well-formed control that must be allowed.
- 2026-09-05: BUG-1304. bash-write-guard.sh fail-opens SILENTLY (exit 0, no stderr) at :78-80 and :267-269, while check-domain.sh prints markers. A "pre-change guard did not fail open" assertion built only on stderr markers is blind on the Bash route; a positive control refused at the same frozen binary is the only proof there.
- 2026-09-05 (BUG-1304): plan-merge set-panel REPLACES the whole panel mapping, so a second cycle must rebuild cycle 1 from the loaded file. Building the value file with a python script that deepcopies the existing readers/findings is the only way carry-forward stays verbatim; retyping it invites silent drift. Extra top-level panel keys (history:, cycle_2_verification:) are accepted by the validator.
- 2026-09-05 (BUG-1304): appending an enumerated case to a task intent can collide with a LATER enumeration in the same task for the same test file (T-01 had 1..7 then 8,9 in a second paragraph). Grep the whole intent for the next numbers before appending, and renumber only the trailing ones nothing cross-references.
- 2026-09-05: BUG-1304 c8 — plan-merge.py has NO delete verb (add-only `apply`, plus set-task-station / set-feature-station / set-panel / sign-approval / amend), so a ruling that says "remove the task" is executable only as `set-task-station --station abandoned` (factory_config.TERMINAL_MARKER). No id gap results; the struck task stays in the file with a strike record.
- 2026-09-05: BUG-1304 c8 — `amend` replaces a WHOLE field, so a one-sentence repair to a 100-line `intent` means retyping it; `--show` first, keep the untouched paragraphs byte-identical, and verify afterwards by reloading and re-reading the amended item.
- 2026-09-05: BUG-1304 c8 — a ruling's numbered repair list is a floor: D-09's backstop framing and BRIEF SC-11's "struck with T-09" header both carried open choices the ruling closed without naming them. Grep the whole pair of artifacts for the choice, not just the cited anchors.
- 2026-09-05: BUG-1304 c10 — a multi-word `grep -q` conjunct in a verify is line-bound, so a phrase the doer may wrap across two lines goes red on correct work. False red, not false green, but worth pricing when choosing an anchor: prefer a fragment short enough that no plausible wrap splits it.
- 2026-09-05: BUG-1304 c10 — for a "keep every other paragraph byte-identical" edit, generate the plan-merge value file with a script that loads the field from the file and does a single-occurrence str.replace (asserting count == 1), never by retyping. Pair it with --expect-sha256. Same trick proved the set-panel value file additive: deepcopy the old panel, assert every prior finding compares equal and set(added keys) is what you intended, BEFORE merging.
- 2026-09-05: BUG-1304 panel re-key. plan-merge.py `apply` CANNOT edit an existing top-level `panel`:
  `panel` is not in UNION_KEYS (plan-merge.py:104), so step 8 (:764-774) raises CONFLICT exit 7 on any
  differing value. Observed exit 7 on this plan. The verb that works is `set-panel --value-file`
  (:1040), which safe_dumps the supplied mapping and self-verifies by reload (:1063). A dispatch that
  names `apply` as the sole route for a panel repair is naming a route that cannot run.
- 2026-09-05: BUG-1304 — check-state INV-32 keys `panel.readers[]` by STEP name (`scope`,
  `should-not-exist`, `goalcheck`), not persona. This plan wrote the persona there and INV-32 reported
  "never ran or was not recorded" three times, which reads like a missing panel rather than a wrong key.
- 2026-09-05: `set-panel` re-dumps the WHOLE panel, but on this plan every untouched sub-block
  (`transcription_rule`, `cycle_2_verification`, 276-line `findings`, 61-line `history`) came back
  byte-identical — the source was already safe_dump-shaped. Verify text-block-wise, not just by value
  equality, before claiming non-goals are unchanged.
