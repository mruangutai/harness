# BUG-1304 · identity plumbing feasibility probe — dev-ops receipt

## BLUF

**A. Keys**: both hosts converge on `agent_type` (identity) + a Task-only `session_id`. Claude Code supplies `agent_type` natively per DEC-64; OMP synthesizes it via `basePayload()`. Neither host's Write/Edit/Bash guard payload ever carries a feature id — only the Task/dispatch payload does. No divergence that blocks REQ-04 (see table below).

**B. No.** None of `orphan_write`/`live_claim`/`_matches`/`_visible` parses a feature id out of a featureless source path, and they were never meant to — that's the wrong mechanism. The right one already exists: resolve the **destination's owning worktree** (the same `harness_boundary` primitive `check-domain.sh` already uses for its out-of-place-worktree check, and the same one `dispatch-guard.sh` uses via `worktree_for_feature`), take its basename as the feature id, then call `inflight_registry.live_claim(root=<that worktree>, agent=<payload agent_type>, feature=<that basename>)`. No path-text parsing needed.

**C. Corrected — the key's direction was inverted, and the resulting collision is real, not theoretical.** REQ-01's key must run **AGENT → assigned worktree**, not destination → feature: the destination-keyed `live_claim` from my cycle-1 read is blind to the exact incident BUG-1304 exists to catch, because when the destination is the main checkout its registry is the one file guaranteed to stay empty (section D), so the lookup reads UNBOUND and waves the write through. The correct direction requires a **fan-out**: for each worktree in `harness_boundary.linked_worktrees(owner_root)`, call `inflight_registry.live_claim(root=<that worktree>, agent=<payload agent_type>)`, collect every worktree with a hit, and compare that set against the destination. No existing function performs this fan-out — `live_claim` (`:316`) and `live_children` (`:336`) both take exactly one `root`; confirmed by re-reading both signatures. Once the lookup is a fan-out, my cycle-1 claim that same-persona claims "never collide in one lookup" is **false**: the fan-out reads every worktree's file in one pass. Measured this run (command and full findings in section C below): **3 agent identities hold live claims in ≥2 worktrees simultaneously, right now.**

**D. The eng lead's report is correct as a reading but wrong as a conclusion — re-measured myself, same number, different cause.** Main checkout `.harness/.inflight-claims.json` really is `{"claims": [], "schema_version": 2}` right now (command below). But the registry is **partitioned one file per worktree** (`REGISTRY_REL` is joined onto whatever `root` a caller resolves, and `dispatch-guard.sh`'s `_root_for()` resolves to the **matching linked worktree**, not the main checkout, whenever one exists for the declared feature). I read all 21 linked worktrees directly: **six have live, well-formed claims right now**, including my own — `BUG-1304-worktree-relative-path-guard/.harness/.inflight-claims.json` currently holds three: `harness-eng-lead`, `harness-validator-lead`, and me (`harness-dev-ops`), all `feature: "BUG-1304-worktree-relative-path-guard"`, `runtime: "omp"`. The registry is not routinely empty; it is only empty **at the one location the eng lead looked**, because none of the six sibling orchestrators run features that lack a linked worktree. Of the seven documented dispatch-guard pass-through branches (current line numbers: unreadable payload `:34`, non-harness dispatcher `:38`, non-harness dispatched persona `:72`, registry libs unavailable `:112`, no checkout root `:138`, OMP-no-supervisor-pid `:193`, internal exception `:235` — the inline comment at `harness-hooks.ts:733-736` cites slightly different line numbers than what's in the file today; treat the ones above as current, git-grounded), a normal nested lead→specialist OMP dispatch takes **none** of them — confirmed directly by the three real claims recorded for this very dispatch chain.

**E. Ranked, most→least robust:**
1. **Live-claim `feature` field, read back via `inflight_registry.live_claim()`** — durable (persisted to disk at dispatch time from the `HARNESS-FEATURE:` prompt line), already the mechanism `check-domain.sh` imports today for a sibling purpose. Failure mode: stale if `_expire`'s TTL logic misfires, and it's blind to any feature with no linked worktree (falls back to the shared main-checkout file, where two concurrent un-worktreed claims for the same agent could collide on `min(started_at)`); not a concern for any of the six live features observed here, all of which have linked worktrees.
2. **`HARNESS-FEATURE:` prompt line itself** — dies at dispatch. `dispatch-guard.sh` parses it from `tool_input.prompt`, which exists **only** on the Task payload; Write/Edit/Bash payloads never carry a `prompt` key. It survives only by proxy, via (1) above.
3. **`HARNESS-FEATURE-TREE-ROOT:` line** — also dispatch-only, used solely as a cross-check at dispatch time (`dispatch-guard.sh:161-186`) and never persisted onto the claim record. Dead end for the write guard.
4. **`HARNESS_AGENT_ID` marker** — confirmed the BRIEF's claim: it's a literal line inside the static agent-definition file (`.omp/agents/harness-dev-ops.md:23`), not `os.environ`. It is identical across every concurrent instance of that persona and every feature it's ever bound to — it cannot discriminate at all.
5. **`cwd`** — the claim's `cwd` is the **dispatcher's** cwd at Task-call time (`d.get("cwd")` in `dispatch-guard.sh:212`), and every claim I read (across all six populated worktrees) records the identical value, the main checkout path. It is constant, not per-feature, and useless as a discriminator. (One real environment variable *does* exist, contrary to a literal reading of "none exists": OMP injects `HARNESS_AGENT_TYPE` into the **Bash command's own env** at `harness-hooks.ts:711` — but that's for a governed agent's own subprocess to read its identity, not a channel the guard itself uses; the guard already has `agent_type` from the payload directly.)

**REQ-01 verdict:** `VIABLE WITH A NAMED SUBSTITUTE KEY: fan-out live_claim(root=w, agent=<payload agent_type>) over every w in harness_boundary.linked_worktrees(owner_root), then compare the matching worktree set against the destination worktree` — the direction must invert to AGENT→worktree. Missing plumbing, none of which exists today: (1) the aggregation loop itself — no function in `inflight_registry.py` fans a lookup out across worktrees, it must be composed from `live_claim` (exists) + `harness_boundary.linked_worktrees` (exists, already imported by `check-domain.sh`/`dispatch-guard.sh`); (2) a tie-breaker for the ≥2-candidate case — none exists (section C findings below shows session, cwd, and `HARNESS_AGENT_ID` all fail to discriminate).

---

## A — payload keys, by host

| Key | Claude Code (Write/Edit/Bash payload) | OMP (Write/Edit/Bash payload) | Read by check-domain.sh / bash-write-guard.sh |
|---|---|---|---|
| `agent_type` | native, host-supplied for subagent-scoped hooks (DEC-64) | synthesized in `basePayload()` (`harness-hooks.ts:216`) | `d.get("agent_type") or ""` — both scripts, no default beyond empty string |
| `session_id` | present on the standard Claude Code hook envelope (per `code.claude.com/docs/en/hooks`, cited at DECISIONS.md:2366) | **absent** from `preDomain`'s `base` (`harness-hooks.ts:226-239`) and from the Bash-guard payload (`:686-690`) — `session_id` is added **only** to the Task/dispatch payload (`:723`) | `check-domain.sh:1850` reads `d.get("session_id")` only inside the orphan-quarantine branch (canonical-artifact writes); neither guard reads it for ordinary Write/Edit/Bash |
| `cwd` | native | `ctx.cwd`, set in every `basePayload()` call | both scripts derive their own `root` from `BASH_SOURCE`, never from payload `cwd` |
| `tool_input.prompt` | absent outside Task | absent outside Task | n/a — the one field that would carry a feature id lives only on the Task payload, on both hosts alike |

**Verdict on A:** no exploitable divergence for REQ-04. Both hosts omit `session_id` from the two write-guard payloads identically, so a write-time guard cannot use `session_id` on either host — it must use `live_claim(feature=…)` (agent+destination-worktree), not `session_id`, on both hosts equally. That is a converging constraint, not a per-host gap.

## B — can existing registry functions resolve the bound feature from a featureless path?

Worked examples, read against `inflight_registry.py`:
- `orphan_write(root, agent, feature, session, now=None)` (`:291`) — **no**, `feature` is a required *caller-supplied* argument; the function never looks at a path at all. It exists for the canonical-artifact case where `check-domain.sh` already extracted `feature` via `canonical_artifact()`'s **path-name regex** (`.harness/<repo>/features/<FEAT>/plan.yaml` etc.) — which is exactly the "feature id embedded in path" case B explicitly excludes.
- `live_claim(root, agent, now=None, session=None, feature=None)` (`:316`) — **yes, with `feature` supplied by the caller**, and the caller can supply it via destination-worktree resolution rather than path-text parsing (see BLUF). Cannot answer anything with `feature=None` beyond "some live claim for this agent, oldest first" — usable only when the destination worktree is the only feature-scoping signal needed, which it is here.
- `_visible(claim, feature=None, session=None)` (`:252`) — pure predicate, no path awareness; not a resolver at all.
- `_matches(claim, agent=None, feature=None, ...)` (`:240`) — same, pure predicate over already-known keys.

None of the four can turn `.claude/skills/harness/bin/foo.py` into a feature id. That parsing was never their job — `canonical_artifact()`'s regex is the only path-to-feature resolver in this file, and it only fires for the four canonical basenames. The needed resolver already lives in `harness_boundary.py` (`linked_worktrees`/`worktree_for_feature`, basename-matched, `:150`/`:205`) and is already imported by both `check-domain.sh` and `dispatch-guard.sh`.

## C — concurrency, session vs. registry partitioning vs. the fan-out's real collision

- `claim_with_receipt` (`inflight_registry.py:361-408`) stores `session` verbatim from the caller (`:394-395`); it never derives or validates it. `attach_runtime_identity` (`:426`) is a separate, later mutation keyed by `agent_id`/`job_id`/`claim_id` and never touches `session`.
- Origin of the value: `sessionId(ctx)` (`harness-hooks.ts:324`) reads the **dispatcher's** tool-call context, evaluated once per Task call and batched across every dispatch in that call — not a per-agent or per-feature id. `_visible()` (`inflight_registry.py:257-258`) ignores it entirely whenever `claim.get("runtime") == "omp"`, which is every claim in this repo right now.
- **Measured ambiguity, this run.** Command: for each linked worktree, read `.harness/.inflight-claims.json` and tabulate `(agent, worktree)`:
  ```
  for wt in . .claude/worktrees/harness/*/; do
    f="$wt/.harness/.inflight-claims.json"
    [ -f "$f" ] && python3 -c "import json;d=json.load(open('$f'));[print(c.get('agent'),c.get('feature')) for c in d.get('claims',[])]"
  done
  ```
  Output, tabulated by agent across the 8 populated registries (main checkout + `BUG-1290/1302/1303/1304/1305/1306/1308` + `FEAT-05`): `harness-product-lead` — live in 6 worktrees at once (`BUG-1290`, `BUG-1302`, `BUG-1303`, `BUG-1305`, `BUG-1306`, `BUG-1308`); `harness-pm` — live in 4 (`BUG-1302`, `BUG-1303`, `BUG-1305`, `BUG-1306`); `harness-validator-lead` — live in 2 (`BUG-1304`, `BUG-1305`). **3 distinct agent identities**, not "rare," not "common" — a plain count against live production data.
- Consequence for REQ-01: a fan-out keyed on `agent_type` alone, run against any of these 3 identities' next write, returns ≥2 matching worktrees with no signal to break the tie — session is ignored for `omp` claims, `cwd` is identical on every claim read across all populated registries (section E), `HARNESS_AGENT_ID` is a static per-persona literal. This is not a corner case constructed for the probe; it is the state of the registry at the moment this note was written.
- **Findings on what the fan-out can resolve, by candidate count** (these are findings about resolvability, not a guard design):
  - **0 candidates** — no linked worktree holds a live claim for this `agent_type`. The agent reads UNBOUND under this key exactly as it did under the old one; whether an unbound write is refused or allowed is REQ-02's orphan question, not something this key resolves differently.
  - **Exactly 1 candidate** — fully resolvable: compare that one worktree to the destination. Mismatch is precisely BUG-1304's incident (agent assigned to worktree W, destination is the main checkout or a different worktree) and is now catchable, which destination-keyed resolution was not. Match is legitimate traffic, unaffected.
  - **≥2 candidates** — **not resolvable with any signal available today.** Every discriminator this probe checked (session, cwd, `HARNESS_AGENT_ID`, the `feature` field itself, which is redundant with which file the claim lives in) fails to distinguish which of the agent's several live assignments the current write belongs to. This is exactly REQ-05's "bound but unplaceable" branch, and it is measured to fire for 3 real agents right now, not a hypothetical. Judged against REQ-03 ("legitimate traffic keeps working unchanged"): a legitimate write by any of these 3 agents into any one of their several correctly-assigned worktrees would ALSO read ≥2 candidates and get refused as unplaceable — the fan-out cannot tell "this write belongs to worktree X" from "this write belongs to worktree Y" using anything the guard currently has. **This is a direct collision between REQ-05's refusal and REQ-03's non-regression guarantee, not a resolved tension** — closing it needs a new discriminator (the dispatch's own declared destination/feature, threaded through to write-time, is the only candidate this probe has surfaced and it does not exist in the current payload per section A).

## D — registry population, re-measured

Command and my own output, verbatim:
```
$ cat .harness/.inflight-claims.json   # main checkout
{
  "claims": [],
  "schema_version": 2
}
```
Matches the eng lead's figure exactly. But reading every linked worktree's **own** copy (`git worktree list` → 21 linked trees → `cat <wt>/.harness/.inflight-claims.json` for each) shows **six populated, well-formed registries** as of this probe: `BUG-1290-factory-claim-repo-root` (1 claim), `BUG-1303-plan-code-review-digest` (2), `BUG-1304-worktree-relative-path-guard` (3, including my own dispatch chain), `BUG-1305-run-state-clobber` (1), `BUG-1306-agent-type-hermetic-tests` (1), `BUG-1308-expertise-replace-drop` (1), plus one older one from a different day (`FEAT-05-factory-doc-smoke`, 1). `.harness/.inflight-claims.json` is gitignored (`.gitignore:40`), so `git log` on it is structurally empty on every checkout — expected, not evidence of anything; I did not treat the empty `git log` as corroboration, only as a confirmed non-signal.

Cause: `dispatch-guard.sh`'s `_root_for(declared)` (`:115-126`) resolves the claim's `root` to the **linked worktree whose basename matches the declared feature**, falling back to the main checkout only when no such worktree exists. Every one of the six live sibling orchestrators dispatches into a feature that *does* have a linked worktree, so none of their claims ever reach the main checkout's file. The eng lead's number is correct; the inference "the registry is empty" does not follow — it was read at the one location guaranteed to stay empty under this repo's current worktree-per-feature convention.

Seven dispatch-guard pass-through branches, current line numbers (re-read, not trusted from the stale inline comment at `harness-hooks.ts:733-736`, whose own line citations no longer match the file): unreadable payload `dispatch-guard.sh:34`; dispatcher not `harness-*` `:38`; dispatched persona not `harness-*` `:72`; `inflight_registry`/`harness_boundary` import failure `:112`; no checkout root resolved `:138`; OMP runtime with an invalid/missing `supervisor_pid` `:193`; claim-step exception `:235`. A normal nested lead→specialist OMP dispatch (my own chain: `harness-orchestrator → harness-eng-lead → harness-dev-ops`) takes **none** of the seven — proven directly by the three real, well-formed claims sitting in this feature's own registry file right now.

## E — see BLUF ranking above; no new candidate found beyond the four named in the dispatch, plus the one real (but guard-irrelevant) env var noted.

## Probe discipline

No filesystem probe was planted. Every command above is a read (`cat`, `git worktree list`, `git status --porcelain`, `git log --oneline`, `git check-ignore`, `git ls-files`) against already-existing files; nothing was created, mutated, or deleted anywhere outside this note. `git status --porcelain` was captured from both the main checkout and this worktree before writing this note (see digest) — every entry shown was pre-existing sibling-agent state, none of it mine.

**REQ-01 premise:** `VIABLE WITH A NAMED SUBSTITUTE KEY` — the direction inverts to AGENT→worktree, composed from `harness_boundary.linked_worktrees()` + a per-worktree `live_claim()` fan-out that does not exist as a single call today, resolves the BUG-1304 incident cleanly at exactly 1 candidate, and is measured to hit an unresolvable ≥2-candidate collision with REQ-03 for 3 real agents in the live registry right now (see section C).

---

## EXTENSION — cycle 2: `currentFeature` re-examined (extends, does not redo, cycle 1)

**Section C above is SUPERSEDED for the OMP host only** — not by silent rewrite, this paragraph
records why. Cycle 1's fan-out design is no longer the recommended key; `currentFeature` is,
with the residue below. Cycle 1's Claude Code findings (A, B, D, E) stand unchanged.

### 1 — the four anchors: mostly confirmed, one materially wrong
- (a) **Confirmed exactly.** `:598` is `const setFeature = (feature: string | undefined, ctx: any): void => {`.
- (b) **Confirmed exactly.** `:630` `detectHarnessAgent`, `:631` `detectHarnessFeature(event.systemPrompt)`, `:632` assigns `currentAgent`, `:633` `setFeature(detectedFeature, ctx)`.
- (c) **Confirmed as a closure**, refined on origin: `currentFeature` (`:584`) is a `let` captured once per call to `registerHarnessHooks` (`:582`), so it is per-registration state, not literally "module-scope." Whether one registration = one agent session is an OMP-host fact this repo cannot prove from source; the corroborating evidence is DEC-204's own text, "**Parent and child sessions differ**" (`DECISIONS.md:6213`), and `tests/unit/omp-hooks.test.ts`'s `fixture()` (`:192`), which instantiates `registerHarnessHooks` once per test and never models two agents sharing one closure for their own Write/Edit gating — only for tracking a dispatched child's claim receipt via `pendingTaskCalls`. Treat "one closure per agent session" as **well-corroborated, not code-proven**.
  Materially: DEC-204 (`DECISIONS.md:6197-6201`) states the feature marker does **not** come from the system prompt for an OMP dispatch — it arrives only in the first user message — because reading `systemPrompt` alone was "measured losing the feature." So `:633`'s `setFeature(detectedFeature, ctx)` is very likely a no-op (`detectedFeature` undefined, `:599` returns early) for a governed member; the real capture is `captureFeatureFromMessage` (`:622-627`) firing off `message_update`/`message_end` (`:660-676`).
- (d) **WRONG as stated — this is the load-bearing correction.** `currentFeature` is lexically in scope everywhere inside `registerHarnessHooks`, but it is plumbed into the **yield/`validate-digest.py` payload only** (`:763`, `harness_feature: currentFeature`, inside the `toolName === "yield"` branch). It is **absent** from `preDomain`'s `basePayload` for the Write/Edit route (`:215-217`, `:226`: `{agent_type, hook_event_name, cwd}` only, no feature key — checked `:227-241` too, neither the `write` nor `edit` branch adds one) and **absent** from the Bash-route payload (`:686-690`, same three keys plus `tool_name`/`tool_input`). `check-domain.sh`/`bash-write-guard.sh` are therefore given no `harness_feature` today on either route. Using `currentFeature` as REQ-01's key requires **new plumbing** — one field added to two payload-construction sites — not a read of something already wired through.

### 2 — branches where `currentFeature` is empty or wrong (the fail-open surface)
- **Empty, non-`harness-*` or marker-less dispatch**: `setFeature` no-ops on `undefined` (`:599`); `currentFeature` never leaves `undefined` for the session. Anchor `:598-599`.
- **Empty, timing window**: between `before_agent_start` and the first captured user message, `currentFeature` is `undefined`. No guarded `tool_call` should reach this window in practice, because a `tool_call` is model output produced only after the model has processed its input — but that ordering is an OMP scheduling fact, not something this file enforces or asserts anywhere.
- **Wrong, first-wins on a non-assignment message**: `featureCaptured` (`:604`,`:623`) locks on the first qualifying `role:"user"` message. The code's own DEC-204 comment (`:619-621`) names the residual directly: "if the foreign marker lands first, reconciles against the WRONG feature's claims." A later steering/interrupt user message landing before the assignment is captured is the concrete shape.
- **Throw, unresolved host behavior**: `setFeature` (`:599-602`) throws on two distinct markers in one scanned message (e.g., a dispatch prompt quoting a sibling feature's own `HARNESS-FEATURE:` line — routine Harness prose, not an attack). The throw is inside an `async` `pi.on("message_end"/"message_update")` callback (`:660-676`) with no surrounding try/catch anywhere in this file. **What the host does with an unhandled rejection inside its own event-emitter callback is not answerable from this repo** — it could silently continue (leaving `featureCaptured=false`, retryable on a later message) or terminate the session's handler registration (fail-closed for future calls, or fail-open if crash-recovery defaults to unguarded). This is a genuine open host-behavior question.
- **Untested, all of it**: `grep` of `tests/unit/omp-hooks.test.ts` for `detectHarnessFeature`, `setFeature`, `captureFeatureFromMessage`, `featureCaptured` returns **zero matches**. The entire feature-capture mechanism this cycle is being asked to bind REQ-01 to has no unit coverage in this repo today — only `detectHarnessAgent`'s conflicting-marker throw is tested (`:33-38`), not the feature-marker analog.

### 3 — the other host: Claude Code has no equivalent signal today
`.claude/settings.json`'s hook payload (verified by reading the file) carries only the standard envelope; `check-domain.sh` and `bash-write-guard.sh` (grepped for `stdin`/`json.load`/`d.get`) read `agent_type`, `hook_event_name`, `tool_name`, `tool_input`, and — in exactly one branch, orphan-quarantine only (`check-domain.sh:1850`) — `session_id`. **No feature field exists on this host's payload at all**, and none of the three SubagentStart/PreToolUse/PostToolUse hook types carries message-level content the way OMP's `pi.on("message_update"/"message_end")` does — Claude Code's hook model has no per-message hook to piggyback a capture on. Per DEC-110 (cited verbatim in `check-domain.sh:9-11`), agent-frontmatter hooks don't even fire per subagent, which is why identity is payload-carried at all on this host. Adding an equivalent signal would mean either (a) reading `transcript_path` (present in the standard envelope but read by neither script today, confirmed by the same grep) and parsing the subagent's own transcript JSONL for its first user message's marker — unverified whether a spawned subagent's `transcript_path` actually resolves to its own dispatch transcript rather than its parent's, and adds a parse cost to every governed Write/Edit/Bash call — or (b) some new injected env var/session file, which does not exist and is not implied by anything read here. **State plainly: this does not exist on Claude Code and must be added; cost and even feasibility (transcript scoping) are unverified.**

### 4 — does this collapse Q1?
**PARTIALLY.** On OMP, `currentFeature` genuinely dissolves cycle-1's ≥2-candidate collision: it is the agent's own dispatched feature, read directly, not a fan-out lookup across sibling worktrees, so the "which of my several live assignments does this write belong to" ambiguity (section C, cycle 1) does not arise — the value is intrinsically single-valued per session. That is real progress. The residue, for the Advisor: on OMP the key needs new plumbing into the Write/Edit and Bash payloads (§1d), carries three named empty/wrong branches with one host-behavior question genuinely unanswered from this repo (the conflict-throw's fate, §2) and zero existing test coverage of the mechanism it would bind to; and on Claude Code — the compatibility host DEC-174 keeps in scope — no equivalent per-agent signal exists at all today and one would have to be designed and built, at unverified cost. The plan needs to decide whether REQ-01 ships OMP-only behind a feature-detection capability check (an agent-type key remains the fallback there, per cycle-1 §A), or is deferred until Claude Code carries an equivalent signal, or is redesigned again.

### 5 — Q2 recommendation: unbound-no-worktree
**Recommend not-applicable/allow**, and this is already the BRIEF's own stated scope, not a new call: REQ-01 (`BRIEF.md:28-32`) reads "an agent whose claimed feature has no linked worktree... are all unbound and unaffected," and REQ-05 (`BRIEF.md:53-56`) states "no live claim, or a claimed feature with no linked worktree" as the rule's own unbound-allowed scope, distinct from the refused bound-but-unplaceable case. Whichever key REQ-01 lands on (fan-out or `currentFeature`), a feature with no linked worktree has no "assigned worktree" to compare the destination against, so refusing would be refusing on the absence of the very thing the rule exists to protect — allow, don't invent a placeholder worktree to fail the comparison against.

### Probe discipline (cycle 2)
No write, no probe planted. Every claim above is a `read`/`grep` against `harness-hooks.ts`, `tests/unit/omp-hooks.test.ts`, `DECISIONS.md`, `.claude/settings.json`, `check-domain.sh`, `bash-write-guard.sh`, and this feature's own `BRIEF.md`. `git status --porcelain` in this worktree shows only this feature directory itself (untracked, expected — my own note).
