# Advisor ruling — BUG-1304 plan phase — REQ-02 vs DEC-153 — 2026-09-05

## BLUF
- Q1: AMENDS DEC-153. Its rationale survives; its extension narrows. Recorded under DEC-205 as a same-edit rewrite of the entry, never a dated sub-section.
- Q2: No legitimate cross-sibling governed write found. One real outside-worktree write class exists — merge-time Expertise distillation into the control-plane root — and it is already outside REQ-04's governed routes (python3 CLI). Pin it with a negative test; fix REQ-06's advice for it.
- Q3: Keep the sibling direction IN scope. Excluding it is not a reasoned boundary — it would be added code preserving a known hole (DEC-208's recorded gap) under live six-flow concurrency.
- Q4: New DECISIONS.md entry (REQ-07 stands) PLUS same-edit rewrites of DEC-153's carve-out clause and DEC-193's first divergence bullet. A plan D-NN alone is insufficient.

## Q1 — contradict, amend, or specialise
DEC-153's carve-out rationale is that perturbation proofs need a legal home in a disposable checkout (DECISIONS.md:3443-3446). A qa proof perturbs the code under test, which lives in the proving agent's own feature worktree; nothing in the entry's evidence (five qa incidents, all backup-perturb-restore of the tree under test) needs a SIBLING checkout. So the OWN assigned worktree satisfies the rationale fully — no contradiction. But this is more than specialisation: the class-wide allow is recorded, load-bearing truth — DEC-193 lists "the Bash route keeps DEC-153's blanket allow" as a deliberate surviving divergence (DECISIONS.md:5334-5335) — and narrowing refuses writes the signed entry permits. That is an amendment of extension with rationale intact. Under DEC-205 the recording form is: rewrite DEC-153's sanction clause to current truth in the same edit that lands the new rule; DEC-153 is NOT superseded/deleted (its audit findings and commit-pen sanction stand).
RULING Q1: AMENDS DEC-153 — extension narrowed, rationale intact; recorded as a same-edit rewrite per DEC-205, no dated sub-section — ADVISOR'S TO MAKE (operator ratifies via the plan signature, since DEC-153's sanctions were user-decided).

## Q2 — any legitimate cross-worktree governed write
Examined all four named candidates plus the factory base.
(a) Distillation: runs at MERGE, while the worktree is still linked and the lead/members plausibly hold live claims (harness/SKILL.md:244-258). Destination is `<control-plane root>/.harness/expertise/<agent>.md` — MAIN checkout, not a sibling. But the sanctioned route is `python3 expertise-merge.py apply` via Bash (harness-distill/SKILL.md:31-34; made the recommended path by FEAT-30 T-07, expertise-merge.py:165-168), which is not among the Bash guard's extractable write findings — by REQ-03's own criterion, not a governed write on either route. So no collision on the sanctioned path, and REQ-03's FINDING holds under a strict reading of "must": no bound agent MUST use Write/Edit for Expertise. Two obligations follow for the plan: (1) a negative SC pinning that a bound agent's `expertise-merge.py apply` invocation still exits 0 on the Bash route — this is the carve-out, stated by ROUTE, not by destination glob; (2) REQ-06's refusal text must not tell an agent refused on a Write-tool Expertise write to "write it in your worktree" — the correct redirect there is the CLI.
(b) DEC-208 ruling 5 is artifact RESOLUTION in the lead's own checkout — no cross-worktree write. (c) DEC-210's orphan holds no live claim, so REQ-01 leaves it unbound; adopt is a resumed parent's `quarantine.py` CLI in its own feature checkout. (d) Concurrent siblings: DEC-199's single-flight-per-checkout supports that no cross-write is legitimate; none found.
Flag [unverified at flow level]: DEC-193 names a second legal write location, `workspace_root/<repo>` (DEC-189 two-base). No factory module links worktrees, so a factory-dispatched agent should be unbound under REQ-01 — but the plan must state explicitly that the assigned-worktree rule keys on the harness base only.
RULING Q2: No legitimate cross-sibling governed write exists; the one outside-worktree class (control-plane Expertise via expertise-merge.py CLI) is carved out by ROUTE and must be pinned by a negative test, with REQ-06's message corrected for it — ADVISOR'S TO MAKE.

## Q3 — sibling direction in scope, or a separate issue
Include it. Three grounds. First, cost inversion: REQ-02's destination-vs-assignment formulation covers both directions with one predicate; EXCLUDING the sibling direction would require extra code (an allow for non-assigned `.claude/worktrees/**` destinations) whose only effect is preserving the gap DEC-208 ruling 2 already records ("does not reach one aimed at a sibling feature's worktree"). Second, the excluded direction is not safe: identical harm to B-10 — work silently on the wrong branch — and strictly likelier today, with six concurrent flows each holding a sibling worktree, where a stray write gets committed by the sibling orchestrator's pen (DEC-153 sanction 2). Third, the operator's one-flow-one-issue intent (grilling note, ## Settled / ## Out of scope) is not violated: this is one rule's natural extension, not unrelated cleanup — B-10's stated mechanism is destination-outside-assignment, and the sibling case is the same mechanism aimed one directory over. A narrowing here would be deferral of known breakage, which the operator forbids.
RULING Q3: Sibling direction stays IN BUG-1304's scope; excluding it is not a defensible boundary — ADVISOR'S TO MAKE.

## Q4 — recording form
Three obligations, all in one landing: (1) the NEW entry BRIEF REQ-07 already requires (the boundary extends the DEC-208 checkout binding from target-path-derived to claim-derived — new doctrine, new number); (2) rewrite of DEC-153's worktree-allow clause to "the assigned worktree" in the same edit (DEC-205: correction rewrites the entry it corrects); (3) rewrite of DEC-193's first surviving-divergence bullet (DECISIONS.md:5334-5335), which would otherwise state a falsified blanket allow — exactly what DEC-188/DEC-205 exist to prevent. "Amendment" is not an available form (DEC-205). A plan-level D-NN alone fails because plans are per-feature records while both DEC-153 and DEC-193 would go on asserting the old truth in the authority every future reader opens. Note DEC-174: the plan records these as `main-session-direct` documentation tasks like the guard work itself.
RULING Q4: New DECISIONS.md entry + index row (per REQ-07) AND same-edit rewrites of DEC-153 and DEC-193's divergence clause; plan D-NN carries the recommendation but is not the terminal record — ADVISOR'S TO MAKE.

