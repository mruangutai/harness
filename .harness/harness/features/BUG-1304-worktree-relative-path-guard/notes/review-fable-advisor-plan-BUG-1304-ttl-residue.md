# Advisor ruling — BUG-1304 plan phase — compatibility-host claim-TTL residue — 2026-09-05

Third ruling; extends and never modifies `review-fable-advisor-plan-BUG-1304-dec153.md` and `review-fable-advisor-plan-BUG-1304-binding-key.md`. Every anchor below re-measured by my own read of the WORKTREE copy.

## BLUF
The asymmetry HOLDS as relayed, and it is worse than relayed: the 1200s expiry is not only a read-side filter but a FILE-pruning side-channel that runs at every dispatch and every parent return. RULING A: DEFECT this feature must close, not a residue. RULING B: split binding-liveness from dispatch-liveness — an expired-but-present compatibility claim binds until the module's existing 86400s backstop, and the record must survive dispatch-side pruning to that backstop; dispatch answers stay at 1200s, so FEAT-37's unstranding is untouched. RULING C: Q3(a) empty-S = ALLOW stands, with its premise sharpened, not restated. RULING D: the goal-check's proposed T-02 remedy ("name `_expire` as the reuse") is REJECTED — it would hard-wire the defect into the new enumerator; six assertions named instead, all of which can actually go red because claim age is stored data (`started_at`), so a back-dated fixture sees the "time-dependent" hole without any clock mocking.

## Verification — asymmetry confirmed, anchors corrected
- `CLAIM_TTL_SECONDS = 1200` at `inflight_registry.py:29` (FEAT-37 ground in the comment at `:27-28`); `OMP_UNVERIFIED_TTL_SECONDS = 86400` at `:36`, justified at `:30-35` as "well above the 7,200s longest measured leaf run".
- `_omp_claim_live` def at `:173` (relay said ~189-192 — that is the identity-check body: proven pid+start-time live at ANY age `:188-189`; unproven falls to the 86400s backstop `:190-192`).
- `_expire` def `:204`; OMP branch `:215-218`; non-OMP hard 1200s comparison `:221-224`. `runtime` defaults `"claude"` at `:369` (`claim_with_receipt`) and `:411-412` (`claim`); v1 migration `setdefault("runtime","claude")` `:82`. So every compatibility-host claim takes the 1200s path. CONFIRMED.
- Correction to the relay: `:593` is inside `_all_live` (`:591`, the CLI list helper), not `live_claim`. `live_claim` (`:316`) prunes via `_expire_where` scoped to (agent, feature) (`:323-328`); `live_children` (`:337`) prunes via `_expire_where` scoped to (feature, dispatcher) (`:343-349`); `reconcile` prunes per-claim at `:519`. Mechanism unaffected; the pruning surface is LARGER than relayed.
- The side-channel is live, not theoretical: `dispatch-guard.sh:197` calls the mutating `live_claim` on every dispatch, and `validate-digest.py:1755-1760` calls `live_children` at every lead/orchestrator return — the DEC-210 suspend path itself. A compatibility child past 1200s is pruned FROM THE FILE at its parent's suspension, so any read-side-only fix reopens the hole through the exact mechanism (DEC-201/DEC-210 long-lived agents) that makes long runs the norm on this host.

## RULING A — defect, not residue — ADVISOR'S TO MAKE
RULING A: The compatibility-host TTL expiry is a DEFECT this feature must close; it fails both tests an accepted residue must pass. (1) No named reason it CANNOT be closed here: D-03's residues were accepted because exact-match is UNBUILDABLE at the guard; here the host capability limit (`:27-28`, no durable child-process owner) forces only the DISPATCH presumption of death, not the BINDING one — the two questions can be answered separately over the same stored claim, inside files T-02 already owns (`plan.yaml:148-149`). (2) It is materially worse than the accepted shape: D-03's residues never reopen B-10 (goal-check §5 rests on exactly that), while this one reopens B-10 VERBATIM — governed agent, main-checkout destination, relative path — on the host where DEC-201/DEC-210 make >1200s runs routine, invisible to every fresh-fixture test. DEC-204/DEC-210 tolerate a host split only when capability forces it; nothing forces this one, so under DEC-208 ruling 4 "canonical enforced, compatibility not" is a bypass by construction, and deferring a measured B-10 harm on a supported host is the scope reduction D-04 already refuses.

## RULING B — the closing boundary — ADVISOR'S TO MAKE, one operator checkpoint below
RULING B: Candidate (ii), with a retention corollary candidate (i) alone misses: BINDING-LIVENESS AND DISPATCH-LIVENESS ARE TWO QUESTIONS OVER ONE STORED CLAIM. The guards' enumerator (`live_claims`) answers binding: OMP via `_omp_claim_live`; non-OMP live until `OMP_UNVERIFIED_TTL_SECONDS`. Every dispatch-side ANSWER (single-flight admission, `live_children`, `reconcile` counts) keeps 1200s unchanged — FEAT-37's unstranding is preserved because admission and child-counting behave identically. The record, however, must not be DELETED before the backstop: pruning sites (`:234`, `:323-328`, `:343-349`, `:519`, `:593`) retain a dispatch-expired non-OMP claim in the file while excluding it from their returned answers. Costs, stated: a crashed compatibility session over-binds its persona up to 24h — fail-closed (REQ-05's chosen direction), the refusal names the stale member (REQ-06), and `release`/`release-all` (`:41`) clears it; blast radius of a wrong refusal is one persona's outside-S writes, never the host. Candidate (i) read-side-only: REJECTED as insufficient — the suspend-path pruning above deletes the evidence mid-run. Candidate (iii) refuse-on-empty-S per host: REJECTED — a per-host default split D-04/Q2 already forbids, converts every legitimately unbound governed agent on Claude Code into refusal (a REQ-01 scope change), and still wrongly punishes the expired agent's LEGITIMATE in-worktree writes.

## RULING C — Q3(a) stands, premise sharpened — ADVISOR'S TO MAKE
RULING C: Q3(a) empty-S = ALLOW is NOT restated — but it was sound only conditionally, and the condition is now explicit: empty-S = unbound is a valid inference ONLY while the construction of S guarantees a bound agent's claim cannot silently leave S within any horizon a real run reaches. RULING B supplies that guarantee (86400s is 12× the longest measured run, `:35`); without it, Q3(a) was unsound on the compatibility host, because never-claimed and expired are indistinguishable at the guard. The indistinguishability that survives RULING B — a claim past the backstop, or released — is the legitimate residue: its named reason is the module's own ground at `:30-35` (no supervisor proof exists on this host, and a claim that can never age out strands its parent's yield forever). The defect was never that a horizon exists; it is that the horizon sat at 1200s, inside routine runs, and invisible.

## RULING D — what must be assertable to fail — ADVISOR'S TO MAKE
RULING D: The goal-check's remedy is half right and half harmful: KEEP its T-01 assertion (aged OMP claim with live supervisor IS returned); REJECT "T-02 should name `_expire` as the reuse" — under RULING B that instruction bakes the 1200s non-OMP expiry into the new enumerator, which is the compounding half of this very finding. Required assertions, each able to go red because `started_at` is stored data and back-dated fixtures see the hole: (1) goal-check's OMP case, kept; (2) a non-OMP claim back-dated past `CLAIM_TTL_SECONDS` but under the backstop IS returned by `live_claims` — fails if anyone re-wires it to `_expire`; (3) a non-OMP claim past the backstop is NOT returned — pins the horizon from above, fails a binds-forever implementation; (4) after `reconcile` and `live_children` run with an injected `now` making a non-OMP claim dispatch-expired-but-under-backstop, the claim is STILL PRESENT in the registry file AND still excluded from their returned answers — the only assertion that catches the suspend-path side-channel; (5) end-to-end on BOTH routes (T-03/T-05): a registry fixture holding a back-dated (>1200s) `runtime:"claude"` claim, governed relative write to the main checkout, exit 2 — the B-10-shaped case that fails if the hole reopens anywhere in the chain; (6) run (5) against the pre-change binaries via the SC-06 idiom (`CHECK_DOMAIN_BIN`/`BASH_WRITE_GUARD_BIN`) so discrimination survives at `review_sha` — aligns with goal-check gap 3, do both in one stroke.

## What the plan must change — itemised for pm
1. **T-02 `intent`** (`plan.yaml:159`): delete "drop claims older than the TTL the module already applies"; replace with the binding predicate — OMP claims via `_omp_claim_live`; non-OMP claims live until `OMP_UNVERIFIED_TTL_SECONDS`; MUST NOT filter with `_expire` or `CLAIM_TTL_SECONDS`.
2. **T-02 `intent`**: add the retention rule — pruning sites (`_expire_where` callers at `:234/:323-328/:343-349`, `reconcile:519`, `_all_live:593`) retain a dispatch-expired non-OMP claim younger than the backstop in the file while returning answers exactly as today. (Operator checkpoint below.)
3. **T-01 `intent`**: add cases (1)–(4) of RULING D to the enumerator/registry tests, each its own case, time injected via `now=` and back-dated `started_at`.
4. **T-03 and T-05 `intent`**: add case (5), the back-dated-claim main-checkout refusal, on each route, using the pre-change-binary idiom of case (6).
5. **New D-09** (dec: DEC-204): record the two-liveness-questions boundary and the surviving residue — past-backstop silent unbinding is ACCEPTED with its named reason (`inflight_registry.py:30-35`: no supervisor proof on the host; must eventually clear); the 1200s hole is NOT accepted. D-03 stays as written — its two residues are a different class.
6. **T-07 `intent`**: the DECISIONS.md entry must carry D-09's boundary and residue verbatim, the same pattern D-03 already mandates for its two.
7. **BRIEF success criteria**: one criterion asserting RULING D case (5) verbatim (aged-claim main-checkout refusal, both routes). Numbering and placement are the pm's shape call; the assertion text is not.

## Operator's calls — named, not decided
- **OC-1**: whether change 2 (file retention) lands inside BUG-1304 or splits into its own issue. My reading: in scope — FEAT-37 and DEC-204 pin dispatch ANSWERS, which are unchanged; but if the operator reads FEAT-37 as pinning file-deletion semantics too, it is theirs. If split out, say so honestly in D-09: the guard hole then stays OPEN on the suspend path until that issue lands, because assertion (4) is the only thing that catches it and moves with the retention change.
- **OC-2**: the binding backstop value — I rule reuse of the existing `OMP_UNVERIFIED_TTL_SECONDS`; a different horizon is the operator's to name.
- **OC-3** (adjacent, out of scope — file separately, Q4 precedent): `live_children` at `validate-digest.py:1755` cannot see a compatibility child past 1200s, so a parent can release and complete while a long child still runs — a DEC-210 dispatch-side defect this guard work does not own.

**Severity: HIGH.** Reopens the feature's founding incident on a supported host under routine conditions, and one drafted task intent would extend the hole to the canonical host — but caught at plan phase with approval pending, and closable inside the plan's existing file surface.
