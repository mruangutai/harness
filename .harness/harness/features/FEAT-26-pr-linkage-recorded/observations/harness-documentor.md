# Observations - harness-documentor

- 2026-08-23: FEAT-26 T-08 — the index generator derives tags AND the refs graph from the entry body, so writing a row means appending `- DEC-NN @0 [] refs:  :: <ruling>` and running gen-decisions-index.py in place; a pre-change `--stdout | diff` against the committed index is the only way to tell your row apart from pre-existing drift.
- 2026-08-23: a dispatch that says "say it plainly" can push me past what the evidence supports; the counterexample to my DEC-200 write-only claim sat in the SAME file (DEC-138 am.7) and one grep of `write-only` would have surfaced it before I wrote "always". Grep the term I am about to make a universal claim about, in the file I am writing into, first.
- 2026-08-23: an index row's GENERATED half can drift from body edits — tag order is derived from the entry prose, so revising a paragraph flipped `[state,github,...]` to `[github,state,...]`. Re-run the generator and diff after ANY body edit, not just after adding a heading.
- 2026-08-23: FEAT-26 T-08 cycle 3 — weakening an entry's BODY without weakening its HEADING left the doc self-contradicting for a whole review cycle; the heading is what a grep-and-stop reader carries away, so a claim narrowed in prose must be narrowed in the title in the SAME edit, not flagged for later. Also: replacing a heading line in place (rather than adding lines) left `gen-decisions-index.py`'s @-anchor AND its derived tag order byte-identical — the cycle-2 tag flip came from added prose, not from the heading.
