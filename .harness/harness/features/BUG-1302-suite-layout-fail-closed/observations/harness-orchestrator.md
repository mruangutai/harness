# Observations - harness-orchestrator

- 2026-09-05: BUG-1302 validate. check-domain.sh refuses a Write that REPLACES a recorded run digest but ALLOWS one that EXTENDS it — the refusal text says 'replace rather than extend' and that is literal. A malformed lead digest (no contract block) is therefore repairable by appending the fenced block after the original bytes; I verified the prefix with head -c <origbytes> | md5sum before and after. My first instinct, prepending the block, was refused and I briefly recorded the defect as unrepairable in three artifacts and had to retract it.
- 2026-09-05: BUG-1302 validate. handoff_done_when.py resolves brief-sc: and plan-task: authority pointers by deriving the feature dir and reading it against the MAIN checkout root, so both are unusable from a worktree whose feature dir exists only on the branch. Only approval: and finding:, which carry an explicit path, work — and the path must include the .claude/worktrees/<seg>/<feat>/ prefix.
- 2026-09-05: BUG-1302 validate. Closing INV-32 (missing panel reader) required a plan.yaml write, which immediately restaled review_sha under INV-33 because INV-33 byte-compares plan.yaml at the pin against disk. Repair sequence that worked: commit all artifacts, re-pin to that commit, commit the pin alone. The pin commit does not touch plan.yaml so it cannot restale itself.
- 2026-09-05: BUG-1302 validate. pm graded SC-06 partial for want of two mutation transcripts while the validator segment's qa had already produced both, in a disposable probe worktree, in the SAME cycle. Neither segment could see the other. Before spending a fix cycle on an unmet criterion, grep the sibling segment's artifacts for the missing evidence — the gap was evidentiary, not behavioural, and cost one dispatch instead of a rebuild.
- 2026-09-05: BUG-1302 validate. feature-json-merge.py set-key parses its value as JSON, so a sha must be passed single-quoted-around-double-quotes ('"abc123"'); a bare hex string exits 2 with 'value is not valid JSON'.
