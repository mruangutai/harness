# Observations - harness-pm

- 2026-09-05: BUG-1302 — the obvious absence-grep for a widened assertion was already satisfied by a sibling case in the same file (integration case 4 carried the generic sentinel clause), so the planned verify would have greened before any change. Ran the grep against the file at HEAD before pinning it; replaced it with absence of the NARROW clause plus an occurrence count of 2.
- 2026-09-05: BUG-1302 — pm may not write a .yaml under features/<FEAT>/notes/; check-domain permits only research-*.md there, so a plan proposal must be staged outside the harness tree (/tmp) before plan-merge.py apply --proposal <path>.
- 2026-09-05: BUG-1302 — plan-merge.py apply DID bootstrap a non-existent plan.yaml with approval.status pending (exit 0); amend --key tasks --field verify --expect-sha256 was the working route to correct one field afterwards.
- 2026-09-05: BUG-1302 cycle 2. I wrote a structural-absence criterion (SC-02 / T-02 step 3) as "zero
  ast.Call to any inside _literal_key_present" after deleting one of TWO any() calls in the same
  boolean chain — the survivor was the SOURCE_EXTENSIONS extension check. Measured post-removal:
  any=1, "*?["=1; pre-removal both 2. Re-anchored on "exactly 1, reintroduction makes 2". Lesson:
  when a removal deletes ONE occurrence of a construct, count the construct in the actual
  post-removal body with ast before writing an absence anchor; never derive it from the shape of the
  deleted fragment. SC-04's ".."-count (pre 2, post 1) was already correct by the same measurement.
- 2026-09-05: BUG-1302 goal-check — code-grade.py refuses paths outside the repo (exit 2), so to measure a grade transition on a proposed edit I imported code_grade and called grade_source(mutated_text, real_relative_path); _literal_key_present moved grade 2 -> 3, which the BRIEF's non-goal denied happening.
- 2026-09-05: BUG-1302 plan c1 — a verify built as `python3 SUITE && grep -q "CHECKNAME" SUITE` is a SOURCE grep: it greens on the name sitting in a comment, or on a check placed after a line that never runs, while the suite still exits 0. Proved on a stub (name in a comment only, run exits 0): the source-grep form exited 0, the run-output form `out=$(python3 SUITE)` plus `printf | grep -q '^PASS CHECKNAME'` exited 1. Assert the RUN OUTPUT whenever a task adds a NAMED check; `check()` here prints PASS at line start (tests/unit/test-suite-layout.py:38).
- 2026-09-05: BUG-1302 — code_grade.py has no CLI (no argparse, no `__main__` block), so a plan-time code-grade pin has to be a `python3 -c` that imports `grade_source` from .claude/skills/harness/bin. Measured `_literal_key_present` grade 2 at c369fb1 and grade 3 after the tautological-conjunct deletion, so the assert clause is red pre-fix and discriminates.
- 2026-09-05: BUG-1302 — bash-write-guard parses the command line textually and reads an angle bracket inside a quoted grep pattern as a redirect target; piping an entry containing `^PASS NAME-in-angle-brackets` into observations-merge was BLOCKED. Keep angle brackets out of any bash string, including heredoc and printf bodies.
- 2026-09-05: BUG-1302 plan c2 — a red demonstration is only evidence if it holds every OTHER clause of the assertion true and varies only the property the new assertion adds; T-05's untrack-the-rogue mutation falsified all three clauses at once and was replaced by an in-place mutation of the fixture's copied runner (tree() copies run-unit-tests.sh, run() executes that copy).
- 2026-09-05: BUG-1302 — a whole-feature regression SC that spans two files cannot be owned by a task clause when the two files sit in unrelated depends_on chains; naming the build-time qa gate as executor is the one-owner fix and needs no plan structural change.
- 2026-09-05: BUG-1302 validate — plan-merge.py apply CANNOT add a panel.readers entry: UNION_KEYS is only (tasks, decisions), so panel falls to the whole-value equality branch and any change exits 7 CONFLICT. The verb is set-panel --value-file, which validates last_run/cycle/readers/findings and reloads before splicing; measured blast radius was 3 added lines, zero removed.
- 2026-09-05: BUG-1302 validate — closing INV-32 by writing plan.yaml immediately reddens INV-33 (review_sha STALE), because the pinned file is the file the fix must edit. Any reader-record repair after a pin is taken must be handed back with a re-pin instruction, or it trades one violation for another.
- 2026-09-05: BUG-1302 SC-06 re-graded partial->met in validate cycle 2. Lesson: before grading a red-half clause unproven, check every note in the feature notes/ tree, not only the file the criterion names — qa had produced both mutants at the pin in qa-2026-09-05-6.md while the criterion pointed at red-demonstrations-2026-09-05.md. An "either X or Y" mutation clause can be half-discharged by source symmetry (two peer conjuncts of one and-chain) rather than a second transcript.
