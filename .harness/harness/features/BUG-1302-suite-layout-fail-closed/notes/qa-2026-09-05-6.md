# QA gate — BUG-1302-suite-layout-fail-closed — pin ac8dd671

## Verdict: PASS (matrix HARD GATE satisfied; all five B-rows discriminate; both SC-09 and B-6 probes hold)

## 1. Matrix (floor)
All five tasks (T-01..T-05) are `change_type: bugfix`. `test_matrix.bugfix` = `always:[unit]`,
`when: kind=__bug_class__ if match_bug_class` — per repo-tier Expertise G-08 this predicate is an
unresolvable placeholder (no bug-class taxonomy entry fires today), so it never adds `integration` to
the floor. **Required kind = `unit` only.** `integration` is not obligated by the matrix, but T-05's own
surface *is* `tests/integration/test-run-unit-tests-layout.py`, so I ran it anyway (BRIEF's own SC-08
evidence: integration). `unit` is `active` (`run-unit-tests.sh --kind unit`), `integration` is `active`
(`--kind integration`) — both real runners, both exercised directly. **matrix_ok: true.**

## 2. Re-derived run numbers (my own run, not trusted from the dispatch)
- `env -u HARNESS_AGENT_TYPE python3 tests/unit/test-suite-layout.py`: exit 0, **54 PASS / 0 FAIL** — matches orchestrator's claim exactly.
- `env -u HARNESS_AGENT_TYPE python3 tests/integration/test-run-unit-tests-layout.py`: exit 0, **14 PASS / 0 FAIL** — matches.

## 3. Per-B-row discrimination (mutation-proven, disposable worktree `.claude/worktrees/harness/qa-probe-bug1302` @ pin, all mutations reverted with `git checkout` before removal; worktree cleanly removed after)

| Row | (a) can go red | (b) discriminates the named defect | Evidence |
|---|---|---|---|
| B-4 | yes | **yes** | Reintroduced tautological conjunct in `_literal_key_present`: behavioural corpus (`b4 corpus`) stayed PASS (confirms it's genuinely invisible to any input — matches BRIEF's own admission); structural check `b4 structural: the tautological conjunct is absent` FAILed with `any calls=2, wildcard constants=2`. Structural AST pin is the *only* detector and it fires. |
| B-5 | yes | **yes** | Restored `normalized in (".", "..")`: `b5 corpus` stayed PASS (dead code confirmed, no behavioural input reaches it); `b5 structural: no unreachable dotdot comparison` FAILed with `dotdot constants=2`. Same pattern as B-4 — structural-only detector, and it fires. |
| B-6 | yes | **yes**, three independent mutants, all reddened | (1) reverted the branch to the original `print(INAPPLICABLE...)` fail-open: `case 11 behavioural: positive control offender is detected` stayed **PASS** (proves the outer check alone is fail-open, exactly BRIEF's residual-risk narrative) but the new `b6 message: the no-candidate failure names both remedies` FAILed (`detail='', condition=None`). (2) deleted one remedy phrase from the detail string with the real `check(..., False, ...)` call intact: `b6 message` FAILed citing the truncated detail. (3) replaced the literal `False` with `control_candidate is None` (a truthy-when-correct expression that keeps both phrases): `b6 message` FAILed on the `condition=Compare(...)` clause, proving the fail-closed literal-False pin is load-bearing and not merely phrase-matching. |
| B-14 | yes | **yes** | Reverted the guarded `read_text()` to the original unguarded form with the non-UTF-8 fixture present: `b14: unreadable tracked sources are reported, not raised` FAILed with the literal `UnicodeDecodeError` text (the case's own try/except converts the would-be crash into a named FAIL rather than an unhandled traceback, so the suite still completes and reports it). |
| B-8 | yes | **yes** | Standalone probe (`/tmp/b8_probe.py`, git_tree + real `run-unit-tests.sh` copy, sentinel files printing `PASS test-unit.py`/`PASS test-integration.py` verbatim) mutated the runner copy to print the integration sentinel immediately before `layout_out=`, held every other clause of case 2 true, and toggled only the sentinel-clause: **widened** clause (`"PASS test-" not in p.stdout`, the shipped fix) → case 2 reddens (`False`); **narrow** clause (`"PASS test-unit.py" not in p.stdout`, the pre-fix code) → case 2 falsely passes (`True`) on the identical mutated-runner state. This independently reproduces T-05's red demonstration and, unlike PF-fc35850348334fa161835c8a8d817636's untrack-the-fixture mutant (which is admitted not to discriminate), this is the mutant the plan actually ships against — it does discriminate. |

## 4. B-6 fail-closed three-part probe
(i) **Reachability confirmed by reading code, not just the passing check**: `CANDIDATE_CORPUS` entries are all under `.harness/tools/...`; `select_control_candidate` requires `code_grade._is_test_path(cand, cfg)` true under the *given* config. `CORPUS_BLIND_KINDS = {"unit": {"detect": "tests/unit/**"}}` has no active kind whose detect pattern matches a `.harness/tools/...` path, so every candidate fails `_is_test_path` and the loop returns `None` — no CANDIDATE_CORPUS entry qualifies, confirmed both by direct read and by the live `PASS b6 reachability: no candidate under a corpus-blind config` check.
(ii) **Detail names both remedies**: live run prints `PASS b6 message: the no-candidate failure names both remedies` and mutation (2) above shows deleting either phrase reddens it.
(iii) **Second positional arg is the literal `False`**: mutation (3) above shows a truthy *expression* (not the literal) reddens the same check via the `ast.Constant` / `is False` clause — confirmed structurally, not merely by string content.

## 5. SC-09 delete-a-check probe
Deleted the `check("real layout is valid", ...)` call outright (temp mutation, reverted) in the disposable worktree copy: whole-suite exit stayed 0, PASS-line count dropped 54→53, **no FAIL line appeared** — confirming BRIEF's own claim that exit 0 alone does not discharge SC-09. `grep -q '^PASS real layout is valid'` against that mutated output **exits 1** (non-zero) — the named-check grep set SC-09 specifies does catch a silently deleted pre-existing check. SC-09 is adequate as written.

## 6. BRIEF git-show clauses, run verbatim over `git show ac8dd671...:<path>`
- SC-01 (`grep -q "b4 corpus: _literal_key_present verdicts unchanged"`): exit 0.
- SC-03 (`grep -q "b5 corpus: _is_inside_tests verdicts unchanged"`): exit 0.
- SC-05 (`grep -q INAPPLICABLE`, expected non-zero): exit 1. Confirmed absent.
- SC-08 clause 1 (`grep -q '"PASS test-unit.py" not in p.stdout'`, expected non-zero): exit 1. Confirmed absent.
- SC-08 clause 2 (`grep -c '"PASS test-" not in p.stdout'`, expected 2): returned 2 (cases 2 and 4).

## Coverage gaps
None found against the BRIEF's five requirements — every REQ/SC pair I could evaluate is backed by a
test that both exists and discriminates the named defect, not merely one that can go red on something.

## Open questions
None — everything in scope was directly measurable at the pin.
