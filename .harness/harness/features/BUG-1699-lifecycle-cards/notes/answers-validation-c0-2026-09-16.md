# BUG-1699 validation c0 rulings

On 2026-09-16, the operator resolved the four blocking questions from pinned validation run `2026-09-16-07-validate-validator`:

1. **QA-01:** authorize a narrow main-session-direct repair for the inherited feature-record/OMP-hooks unit failure, recorded as an operator-approved scope expansion.
2. **QA-02:** assign the factory case-L regression to T-04 and repair it in the bounded fix cycle.
3. **QA-04:** reclassify the unsupported `coverage` finding as `substance`, owned by T-02/T-03, and add execution-bound proof that plan mutation performs no GitHub network write.
4. **Goalcheck return:** do not accept the on-disk artifact alone; repair the Harness owner/runtime terminal-yield contract first, then rerun the canonical goalcheck.

The task-owned must-fixes QA-03, CR-01, and CR-02 remain required.
