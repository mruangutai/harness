# Goal-check — BUG-1699-lifecycle-cards — validate c1

## Boundary and canonical verdict

**FAIL.** This is a fresh assessment of pinned fixed SHA `d7310f865e03534c233085e5f0a768eb9eca4687` against merge base `8ef4731e816f08dbc562206134c100b0c034a812`, the approved `BRIEF.md`, and approved `plan.yaml`. The pin is recorded in `feature.json:5`. The operator and orchestrator perspectives pass, and SC-01–SC-10 plus SC-12–SC-16 are met. The code-maintainer perspective is partial because SC-11 is not met: its focused unit assertions execute after that runner's sole failure-accounting/exit block, so assertion failures cannot make the unit command fail (`tests/unit/test-gh-board.py:408-412,415-471`). The c1 QA matrix and signed-gate exit therefore do not establish the unit method the criterion requires.

C1 provenance is the final validator digest (`runs/2026-09-16-08-fix-c1-validator/digest.md:2-12,35-42`), exact-head QA gate (`notes/review-harness-qa-c1-r1.md:1-35`), exact-head code review (`notes/review-harness-code-reviewer-c1-r1.md:1-18`), Main direct repair receipt (`notes/receipt-main-direct-validation-c1.md:7-35`), and backend controlled-red receipt (`notes/receipt-harness-backend-dev-fix-c1-r1.md:1-28`). The implementation and test pointers below were inspected in the pinned diff; no working-tree behavior was substituted.

## Perspective grades

- **operator — pass** — carries SC-01, SC-02, SC-03, SC-06, SC-07, SC-08, and SC-09, all met. Signature/open/status and ordinary Build/validation ordering are enforced with eight negative controls (`tests/integration/test-station-argument-spelling.py:94-159,288-358`), every active status writes the exact source/parent/live-task projection (`tests/integration/test-gh-sync-record.py:243-276`), reset/reapproval persists and consumes the classified resume station (`.claude/skills/harness/bin/plan-merge.py:811-921,2027-2038`; `tests/integration/test-plan-merge.py:3248-3409`), ship covers every recorded card (`tests/integration/test-gh-sync-ship.py:33-56`), and reconcile is bounded, continuing, and idempotent (`tests/integration/test-board-lifecycle.py:1148-1232`). Exact-head green and fail-first provenance are recorded in `notes/review-harness-qa-c1-r1.md:23-35`.
- **orchestrator — pass** — carries SC-04, SC-05, and SC-10, all met. Must-fix Building precedes dispatch, Review remains at the next validation boundary, and the fix DAG contains no lifecycle writer (`tests/integration/test-station-argument-spelling.py:148-178,332-358`; `.claude/skills/harness/references/build-phase.md:50-66`). Status records local authority before best-effort per-card writes (`.claude/skills/harness/bin/gh-sync.py:1483-1529`), and the powered no-network reset control proves local plan mutation does not invoke GitHub (`tests/integration/test-plan-merge.py:3461-3465`; `notes/receipt-main-direct-validation-c1.md:13-16`).
- **code maintainer — partial** — carries SC-11, SC-12, SC-13, SC-14, SC-15, and SC-16. SC-12–SC-14 are met by exact-head behavior plus criterion-specific reconstructed mutation proofs (`notes/review-harness-qa-c1-r1.md:31-35`; `notes/receipt-harness-backend-dev-fix-c1-r1.md:12-20`). SC-15 is met by the one `gh_board.project` policy and its status, INV-26, and reconcile callers (`.claude/skills/harness/bin/gh_board.py:126-159,190-228`; `.claude/skills/harness/bin/gh-sync.py:1304-1342,1483-1529`; `.claude/skills/harness/bin/check-state.py:2323-2411`; `.claude/skills/harness/bin/board_lifecycle.py:488-529`). SC-16 is met by current pinned authority and generated index rows (`.harness/harness/docs/DECISIONS.md:2956-2988,3321-3333,6170-6201,7098-7133,7247-7284,7472-7495`; `.harness/harness/docs/DECISIONS-INDEX.md:143,150,203,220,224,229`). SC-11 is not met because its focused unit assertions are outside the runner's exit accounting (`tests/unit/test-gh-board.py:408-412,430-471`), notwithstanding the c1 QA citation to those assertions.

## Fail-first provenance

The c1 QA gate's classification is retained exactly:

- **Historical fail-first evidence:** SC-01–SC-05 use the pre-repair checkpoint violations; SC-06–SC-07 use the pre-repair reset/resume failures; SC-09 uses the pre-repair reconciliation failures; SC-10 uses the pre-repair local-first/reset failures and powered no-network control (`notes/receipt-main-direct-validation-c1.md:13-16`); SC-11 is classified historical from the original T-01 run (`notes/receipt-harness-backend-dev-T-01-c0.md:9-15`). Fresh inspection does not promote SC-11 to met because the focused unit runner cannot return non-zero for those assertions.
- **Reconstructed fail-first evidence:** SC-08, SC-12, SC-13, and SC-14 use narrow live mutants, not historical failures. The receipt records the exact mutant, observed failing assertion and exit 1, restoration hashes, and fixed-tip counterpart (`notes/receipt-harness-backend-dev-fix-c1-r1.md:7-28`). QA independently labels them reconstructed and confirms the restored hashes and exact-head greens (`notes/review-harness-qa-c1-r1.md:27,31-35`).

## Canonical SC status

| SC | Status | Method and concrete evidence |
|---|---|---|
| SC-01 | met | automated — signature captures/validates `RESUME`, opens before dynamic status, and active Ready projects all cards (`test-station-argument-spelling.py:94-114,288-310`; `test-gh-sync-record.py:243-276`); historical red: Main receipt line 14; exact-head green: QA c1-r1 lines 15-17,25. |
| SC-02 | met | automated — Building is ordered before ordinary task dispatch (`test-station-argument-spelling.py:133-147,316-322`), with complete-card projection in `test-gh-sync-record.py:243-276`; historical red and exact-head green: QA c1-r1 lines 25,35. |
| SC-03 | met | automated — Review is ordered before validation dispatch (`test-station-argument-spelling.py:145-147,324-330`) and uses the same projection; historical red and exact-head green: QA c1-r1 lines 25,35. |
| SC-04 | met | automated — must-fix Building precedes fix dispatch and the negative control detects reversal (`test-station-argument-spelling.py:148-158,332-340`); historical red and exact-head green: QA c1-r1 lines 25,35. |
| SC-05 | met | automated — post-fix Review occurs at the next boundary, outside the fix DAG, with both wrong placements detected (`test-station-argument-spelling.py:148-178,342-358`); historical red and exact-head green: QA c1-r1 lines 25,35. |
| SC-06 | met | automated — task-set mutation atomically writes pending approval, resume metadata, and Plan, with a landed-reset receipt (`plan-merge.py:811-921`; `test-plan-merge.py:3248-3284`); historical 24-assertion red: Main receipt line 13; exact-head green: QA c1-r1 line 26. |
| SC-07 | met | automated — Ready/Building/Review classification and reapproval emission are covered (`test-plan-merge.py:3331-3409`); historical red: Main receipt line 13; exact-head green: QA c1-r1 line 26. |
| SC-08 | met | automated — all task, source, and parent cards reach Done (`test-gh-sync-ship.py:33-56`); reconstructed parent-omission red and restoration: backend receipt lines 11,16,20; exact-head green: QA c1-r1 lines 27,35. |
| SC-09 | met | automated — one snapshot covers dry-run/apply, later cards continue after failure, and the second apply is mutation-free (`test-board-lifecycle.py:1148-1232`); historical ten-failure red: Main receipt line 15; exact-head green: QA c1-r1 lines 28,16. |
| SC-10 | met | automated — local status precedes best-effort writes (`gh-sync.py:1483-1529`), reset has a powered no-network control (`test-plan-merge.py:3438-3465`), and per-card reconcile failure does not stop later cards (`test-board-lifecycle.py:1222-1232`); historical/control reds: Main receipt lines 13-16; exact-head green: QA c1-r1 lines 29,16. |
| SC-11 | not_met | automated/unit — the policy implements the six-station active/terminal projection (`gh_board.py:126-159,190-228`), but the focused unit assertions at `test-gh-board.py:430-471` occur after the only `FAILURES` exit check at lines 408-412. A regression can print `FAIL` and still exit 0, so neither the 40-file unit exit nor signed gate proves this criterion. |
| SC-12 | met | automated — abandoned cards are excluded from active and terminal projection (`test-gh-sync-record.py:243-276`; `test-gh-board.py:441-448`) and the signed T-01 gate includes abandonment coverage (QA c1-r1 line 17); reconstructed abandoned-card-admission red and restoration: backend receipt lines 12,16,20. |
| SC-13 | met | automated — ship moves cards without direct issue closure (`test-gh-sync-ship.py:33-54`); reconstructed injected-close red and restoration: backend receipt lines 13,16,20; exact-head green: QA c1-r1 lines 32,35. |
| SC-14 | met | automated — open children hold cards and in-run refresh permits newly completed children (`test-gh-sync-ship.py:85-156`); reconstructed stale-refresh red and restoration: backend receipt lines 14,16,20; exact-head green: QA c1-r1 lines 33,35. |
| SC-15 | met | inspection — pinned `gh_board.project` is reused by status, INV-26, and reconcile, reset/resume stays in plan approval metadata, and checkpoint callers are singular and ordered (`gh_board.py:126-159`; `check-state.py:2323-2411`; `board_lifecycle.py:488-529`; `test-station-argument-spelling.py:94-190`); c1 code review confirms traceability at lines 7-9. |
| SC-16 | met | inspection — the pinned DEC-138/203/220/224/229 text states current lifecycle truth, DEC-146 remains the issue-to-project-item contract, and generated index summaries agree (`DECISIONS.md:2956-2988,3321-3333,6170-6201,7098-7133,7247-7284,7472-7495`; `DECISIONS-INDEX.md:143,150,203,220,224,229`). |

## Stale c0 disposition

The c0 FAIL at `ed64ea9cc4ef92e3e54adfa0849a0147230b480b` is **superseded as a verdict**, not copied forward. The operator authorized routed repairs (`notes/answers-validation-c0-2026-09-16.md:3-10`): Main repaired QA-01, QA-02, QA-04 and the terminal-yield defect and supplied T-02/T-03/T-04 historical reds (`notes/receipt-main-direct-validation-c1.md:7-35`); the backend fix resolved CR-01/CR-02 and supplied the four honestly reconstructed proofs; final QA then passed 40 unit files, 72 integration files, and exact T-01 at the pinned head (`notes/review-harness-qa-c1-r1.md:11-19,37-46`), while final code review found no remaining fix-delta finding (`notes/review-harness-code-reviewer-c1-r1.md:13-31`). The present SC-11 failure is a fresh pinned-head finding about the unit runner's own failure accounting, not a carried-forward c0 finding.

## Open questions

`[]`
