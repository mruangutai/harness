# QA gate — FEAT-31, review_sha ed62d7429d1f3e3f9321fd21885393d2ce8fd525

## BLUF

**FAIL.** The test suite is fully green and the matrix is satisfied — but SC-09, an approved,
`verify: inspection` success criterion, is demonstrably **not met** at the pinned sha: DEC-159 was
never edited. `git diff d065b3b..ed62d74 -- .harness/harness/docs/DECISIONS.md` shows the *only*
change to that file is a brand-new entry, **DEC-198** (the budgets key). DEC-159's text at the
pinned sha (lines 3945–3995) is byte-identical to before this feature, including the exact clause
SC-09 requires corrected: *"the watchdog remains the post-hoc audit"* (line 3991) — which the
feature's own BRIEF (line ~232) calls false. No clause about what to do when a phase is genuinely
mid-flight at the threshold was added anywhere in DEC-159 either.

This is not a coverage gap I found by inference — the plan's own task T-09 explicitly instructs
"Do NOT edit DEC-159 — that is SC-09 and belongs to a later planning run" (`plan.yaml:677`), and no
later task (T-10–T-18) touches `DECISIONS.md` at all. Yet `plan.yaml`'s own bookkeeping (D-84, line
84) asserts *"The SECOND run (plan4) ... closed SC-09"* — that claim is false against the diff. This
is a genuine new finding, not one of the seven filed/accepted ones (#663–#669).

## Matrix verdict

All three commands reproduced exactly at HEAD == review_sha (tree clean except local
`STATE.md`/`feature.json` run bookkeeping):

| Command | Exit | Result |
|---|---|---|
| `run-unit-tests.sh --kind unit` | 0 | 187 PASS, 0 FAIL, 0 MISCONFIGURED, 0 KIND-DRIFT — matches operator baseline exactly |
| `run-unit-tests.sh --kind integration` | 0 | 19 file-level PASS (incl. `test-context-watch-cli.py`, `test-context-watch-hook.py`, `test-run-unit-tests-kinds.py`), 0 FAIL, 0 MISCONFIGURED |
| `run-unit-tests.sh --check-kinds` | 0 | `check-kinds: the script arrays and test_kinds.integration.detect agree.` |

Task change types: 13 `logic` → `always: [unit]` (satisfied); 4 `config` + 1 `docs` → `always: []`
(no obligation). **Matrix floor is met on a literal read.** Beyond the floor: most of this
feature's own new/changed test files are registered `integration` (`test-context-watch-cli.py`,
`test-context-watch-hook.py`, `test-run-unit-tests-kinds.py`, `test-check-state.py`,
`test-upgrade-config.py`, `test-gen-decisions-index.py`, `test-check-domain.py`) and I ran that kind
— it is the majority of the real evidence for SC-01, SC-06, SC-13, SC-14, SC-07, SC-09(T-09 verify),
and it is green. **Judgment: the configured floor (unit-only for `logic`/`config`/`docs`) is
inadequate on its own for a change like this, but I did not stop there** — I ran integration too, so
no gap in *my* gating results, only a standing config observation (see findings).

**Verification-gaps claim (BRIEF lines 208–216) — judged, not inherited: accurate.** `harness.json`
confirms `functional` alone carries `status: excluded` + `signed: DEC-187` (legitimate soft skip
under the `_test_kinds_note` rule); `component`, `ui`, `eval`, `typecheck` are `status: unresolved`
with `cmd: null`. None of the four `unresolved` kinds fires for `logic`/`config`/`docs` in
`test_matrix` (only `ai_behavior` needs `eval`, and no task here is `ai_behavior`). So none of the
five null-cmd kinds is required and none is silently starved of a criterion — the BRIEF's claim
holds.

## Q-IRONLAW ruling (TDD ordering)

**Acceptable here, flagged as a violation, not a blocker.** I independently reproduced both red
proofs referenced (`L-RED`, `N1-RED` in `test-context-watch.py`, lines ~895–913 and the N1 group):
each asserts the mutant text differs from the original *before* running anything, then asserts a
**count differential** on the same fixture (rows found: 0 vs >0; `current`: 0 vs 300) — never an
exit-status-only proof. Both passed when I ran the unit suite. The substance of test discrimination
is proven and was independently reproduced by a second member per the digest. But the plan-level
record should say plainly: this was code-then-test, a genuine Iron Law breach, not a substitute for
it — record it as a lesson, not a pattern to repeat. **Severity: med, not must_fix** (the specific
defect is well-understood, pinned in the BRIEF before code was written, and the discriminating power
is independently verified).

## Compensating-control judgments (already-filed items — not re-raised)

- **(a) T-13 `not found` grep vacuous (#663):** confirmed still true — `verify-context-watch-live.py`
  hits the early-return at `os.path.isdir` check (line ~195) before `_find_agent_paths` ever runs,
  and nothing else in the committed suite (including `--self-test`) exercises the real "sidecar not
  found under an *existing* projects dir" branch. **Compensating control does not hold** — genuinely
  untested, but out of scope to re-raise (filed).
- **(b) T-16 `-ge 22` floor already satisfied at 29 (#666):** the actual `test-context-watch.py`
  count is now **76** cases — the floor is even more vacuous than filed, but this is the same filed
  defect class, not new.
- **(c), (d):** not independently re-derived; no new information found.

## Coverage-lesson check (mandatory, item 3)

**Both directions are now asserted, named:** `L1` ("at the CORRECT two-level depth, row count equals
the sidecar count found by glob", asserting `expected_count_l > 0`) and `L2` ("at the WRONG
one-level depth, discovery finds ZERO rows") — both in `test-context-watch.py` (verified via
`git show <review_sha>`, lines 820, 841). Both pass live. **This closes the coverage lesson for the
depth defect.** The `_build_row` contradiction (D-11) is separately pinned by `N1a`–`N1d` /
`N2a`–`N2b`, also present and passing.

## Q-FOOTERCOV

**Committed-tested today, contrary to the "throwaway probe" framing.** `test-context-watch.py`
carries `M0`–`M4` (exactly-3-lines, per-blind-spot content, unmeasured-row-count) at the pinned sha
(verified via `git show`, lines 1093–1115) and all five pass. The scratchpad probe mentioned in the
prompt is superseded; the committed suite now covers this.

## T-17 hook / non-observability

Correctly not treated as a test failure. Coverage given the constraint is adequate:
`test-context-watch-hook.py` drives the hook as a subprocess with a crafted PostToolUse payload
(4 cases + never-raises), which is the strongest evidence obtainable pre-merge. The live
stderr-reaches-context question is separately settled (Q-HOOKCTX, closed) — not reopened here.

## sc_status

```yaml
sc_status:
  - { id: SC-01, verdict: met, method: "T-07 test-context-watch-cli.py CASE 1 (fixture half, PASS) + verify-context-watch-live.py --self-test (live-half plumbing, PASS, printed 747992 not 1494870); true live-orchestrator run is the operator's per D-12/D-17" }
  - { id: SC-02, verdict: met, method: "test-context-watch.py::A2/A3 — peak=747992, not 1494870" }
  - { id: SC-03, verdict: met, method: "test-context-watch.py::B1/B2 — agentType filter, 1 row survives of 3" }
  - { id: SC-04, verdict: met, method: "test-context-watch.py::D1-D4" }
  - { id: SC-05, verdict: met, method: "test-context-watch.py::F1-F3 (before/after mutant, warning-line counts 0 vs 1)" }
  - { id: SC-06, verdict: met, method: "test-context-watch-cli.py CASE 2 + context-watch.py --resolve-dir (T-01 verify), exact slug match" }
  - { id: SC-07, verdict: met, method: "test-validate-feature-json.py t15_a..t15_e + test-check-domain.py case F (write-path refusal); validate-feature-json.py sweep of 31 files/390 runs exits 0" }
  - { id: SC-08, verdict: met, method: "inspection — git show <sha>:context-watch.py lines 640-690, all 3 blind-spot lines interpolate a computed/read value" }
  - { id: SC-09, verdict: not_met, method: "inspection — git show <sha>:.harness/harness/docs/DECISIONS.md lines 3945-3995: DEC-159 unedited, false watchdog clause still present, no mid-phase-threshold clause added; diff to DECISIONS.md only adds new DEC-198" }
  - { id: SC-10, verdict: not_met, method: "verify: uat — requires the operator against a live orchestrator; not gradeable here" }
  - { id: SC-11, verdict: met, method: "test-context-watch.py::C1-C5 — 4-fixture unmeasured branch, exit non-zero, row count == glob count" }
  - { id: SC-13, verdict: met, method: "test-context-watch.py::H/I/J (library half) + test-context-watch-hook.py cases 1-4 (cutover half)" }
  - { id: SC-14, verdict: met, method: "test-check-state.py case_d..case_h + T10_MARKER empty-body check; INV-17 glob-widened, no double-report (case_f)" }
  - { id: SC-15, verdict: partially_met, method: "gate half MET — test-check-state.py empty-Next mutant proof; behaviour half NOT_MET — requires hand grading against a live relay, not yet recorded (per D-21/D-12)" }
```

## New findings (ranked)

1. **[high, must_fix] SC-09 unmet at the pinned sha.** DEC-159 not edited; the known-false
   "the watchdog remains the post-hoc audit" clause ships uncorrected, and no clause covers the
   mid-phase-threshold case SC-09 requires. `plan.yaml`'s own D-84 falsely records this as closed.
   Route: a docs/decision task, likely folded into whatever plan round finishes this feature —
   someone has to actually edit `DECISIONS.md`'s DEC-159 entry in place.
2. **[med, not must_fix] Q-IRONLAW: code-before-test on the depth/`_build_row` fix.** Discriminating
   power is independently proven and reproduced by me, but the ordering itself was wrong. Record as
   an observation for the team, not a re-do.
3. **[low, not must_fix] `verify-context-watch-live.py`'s "no such agent id" real code path
   (`_find_agent_paths`) is never exercised by any committed test** — the CLI-level check hits an
   early-return on the nonexistent-projects-dir fixture. Filed already (#663); repeating here only
   because the prompt asked me to judge the compensating control, which does not hold.
4. **[info] `test-feature-worktree.py` and `test-expertise-merge.py` each print two `PASS` lines
   in `--kind integration`** — pre-existing, unrelated to this diff's files (not in any T-NN's
   `files:`), not investigated further; noting for whoever owns `run-unit-tests.sh`'s registration
   next.

## Open questions

```yaml
open_questions:
  - { id: Q1, question: "SC-09 requires DEC-159 edited in place; no task in this plan does it, and plan.yaml's own D-84 incorrectly claims it was closed. Should this route back to pm/eng-lead as a new task before ship, or does the operator want to hand-edit DEC-159 directly?", blocking: true }
  - { id: Q2, question: "SC-10 and SC-15's behaviour half both require a live orchestrator hand-grading, per D-12/D-21 — has the operator scheduled that UAT pass, or is it still outstanding?", blocking: true }
```
