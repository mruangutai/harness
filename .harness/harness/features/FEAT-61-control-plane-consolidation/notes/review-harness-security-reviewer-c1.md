# Security review — FEAT-61 control-plane consolidation — c1

**PASS.** Reviewed immutable range `066638e8acf68b47e74637006a01c8823cff939c..57ef1c5739f55dd67d9daffd5da69d7d7b980ea7`, targeting `57ef1c5739f55dd67d9daffd5da69d7d7b980ea7`. The diff is security-scoped **in**: it changes untrusted JSON decoding, authorization-adjacent checkout binding, repo-local dynamic execution, and gate policy. OWASP injection/auth-secrets/exposure/input-validation/dependency/SSRF classes and STRIDE were assessed. No exploitable regression was found; maximum severity is `info` because this was an in-scope audit, not a scoped-out review.

## Findings

None. `must_fix: []`.

## Threat model and assessed candidates

- **Tampering / elevation — feature-artifact checkout binding (T-01/T-03), dismissed.** A governed agent supplying a feature artifact path cannot exploit the extraction into shared code to change the decision: `feature_artifact_id` retains the anchored, slash-bounded feature path match and `feature_artifact_checkout_mismatch` retains realpath equality against the linked checkout (`.claude/skills/harness/bin/harness_boundary.py:247-282`). Both enforcement adapters still refuse mismatch and ambiguity and preserve their pre-existing unexpected-error absorption (`.claude/skills/harness/bin/check-domain.py:681-713`; `.claude/skills/harness/bin/bash-write-guard.py:796-829`). The diff moves the predicate without widening accepted paths or actor capability.
- **Tampering / injection — strict JSON consolidation (T-01/T-03/T-04), dismissed.** An agent or repository file author can provide duplicate keys, non-finite constants, malformed UTF-8, or a non-mapping document. The sole decoder still rejects duplicate keys at every nesting depth and non-finite constants (`.claude/skills/harness/bin/artifact_accessors.py:21-51`); mapping readers retain their shape checks (`.claude/skills/harness/bin/artifact_accessors.py:67-81`), and the write path translates decode failures to the existing refusal instead of accepting them (`.claude/skills/harness/bin/feature_json_write.py:60-91`). Run-schema navigation uses ordinary keyed access, so malformed structure reaches the existing caller catch boundaries rather than being normalized into a permissive default (`.claude/skills/harness/bin/artifact_accessors.py:54-64`). This tightens/centralizes validation and introduces no SQL, shell, template, spreadsheet, or deserialization sink.
- **Elevation / tampering — dynamic repo-local module loading (T-01/T-02/T-03), dismissed.** A repository committer already controls the gate scripts and therefore gains no new privilege from executing the same fixed repo-local paths. The helper checks for a missing spec/loader, registers only when explicitly requested, removes only its own registration after failed execution, and re-raises the original exception (`.claude/skills/harness/bin/harness_boundary.py:285-325`). Call sites replace equivalent `spec_from_file_location` sequences; no user-controlled module name or path was added. The accepted five bootstrap prologues only gained comments (T-05), not executable behavior.
- **Elevation / repudiation — policy surface removal (T-04), dismissed.** `qa_gate`, `uat`, and `merge` had no consumer per signed D-04; removing their validation cannot disable an authorization decision. The effective `review` policy remains required and vocabulary-checked. Old extra keys are ignored deliberately, while missing/invalid `review` still refuses. No security control was moved client-side.
- **Information disclosure / secrets — complete added-line and changed-file inspection, dismissed.** No credential, token, private key, authorization header, URL token, PII log/export, or spreadsheet export was introduced. Matches in the feature ledger are token-count metadata, and secret-shaped strings elsewhere are unchanged detector fixtures/comments rather than credentials. No dependency or outbound-request change exists, so dependency-vulnerability, SSRF, and redirect classes have no introduced sink.
- **Denial of service — strict station predicates (T-01/T-02/T-05), dismissed.** Unknown or empty station values now fail loudly by signed D-03 rather than silently selecting a lifecycle branch. The actor capable of editing plan state already controls the repository artifact; refusal is the intended integrity response, not a cross-user availability escalation.

## Per-file census

Every path in the pinned `git diff --name-status` was classified:

- **Runtime/security-relevant, inspected:** `.claude/skills/harness/bin/artifact_accessors.py`, `bash-write-guard.py`, `board_lifecycle.py`, `branch-create-gate.py`, `check-domain.py`, `check-plan-routes.py`, `check-state.py`, `factory_config.py`, `feature_json_write.py`, `gate_policy.py`, `gh-close-gate.py`, `gh-sync.py`, `gh_board.py`, `handoff_done_when.py`, `harness_boundary.py`, `merge-gate.py`, `plan-merge.py`, `plan-sign-gate.py`, `run-unit-tests.py`, `worktree_terminal.py`.
- **Configuration/templates, inspected for policy and secrets:** `.harness/harness.json`, `.claude/skills/harness/templates/harness.json`, `.claude/skills/harness/templates/examples/harness.kaya-ai.json`.
- **Doctrine/feature records, inspected for exposure and trust claims:** `.harness/glossary.md`, `.harness/harness/docs/DECISIONS.md`, `.harness/harness/docs/DECISIONS-INDEX.md`, and the 15 added FEAT-61 files: `BRIEF.md`, `STATE.md`, `feature.json`, `plan.yaml`, `notes/answers-2026-09-20-simplify.md`, `notes/build-divergences.md`, `notes/handoff-plan.md`, four `notes/receipt-harness-{backend-dev,dev-ops}-simplify-eng-*.md`, `notes/research-FEAT-61-control-plane-consolidation.md`, `notes/research-FEAT-61-control-plane-consolidation-goalcheck-plan.md`, `notes/review-harness-code-reviewer-plan-c1.md`, `notes/review-harness-code-reviewer-plan-c2.md`, `notes/review-harness-ui-reviewer-plan-c1.md`. These add no executable boundary or secret.
- **Verification-only, inspected as evidence and for committed sensitive fixture data:** `tests/integration/canonical-reader-classification.json`, `tests/integration/fixtures/feat61-check-plan-routes-lifecycle.receipt.json`, 11 changed integration tests (`test-bash-write-guard.py`, `test-board-lifecycle.py`, `test-check-domain-worktree.py`, `test-check-domain.py`, `test-check-plan-routes.py`, `test-check-state-feat59.py`, `test-gh-sync-record.py`, `test-gh-sync-start-task.py`, `test-plan-merge.py`, `test-validate-digest.py`, `test-worktree-terminal.py`) and 7 changed unit tests (`test-artifact-accessors.py`, `test-factory-config.py`, `test-feature-json-reader.py`, `test-gate-policy.py`, `test-gh-board.py`, `test-handoff-done-when.py`, `test-harness-boundary.py`). No live credential or novel production sink is present.

## Scope/verification note

Compared the complete pinned diff with the approved BRIEF and `plan.yaml`, including D-03 through D-09 and ownership by T-01 through T-05. Per dispatch, no tests, formatter, linter, build, or project-wide validation command was run. There are no open security questions and no unowned scope-change recommendation.
