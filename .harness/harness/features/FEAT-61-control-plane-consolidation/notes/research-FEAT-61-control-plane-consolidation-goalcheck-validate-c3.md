# FEAT-61 goal-check — validate c3

**Verdict: PASS.** The exact immutable range `066638e8acf68b47e74637006a01c8823cff939c..f798e2e600ed08aeb49d61a3a229a626750a9ccb` discharges all three signed perspectives and all eight success criteria. The only product-spec change after the c2 evidence pin was the authoritative correction of T-01/T-02/T-03 from unsupported `code` to supported `cross_module`; no source, configuration, documentation, fixture, or test evidence changed between the c2 pin and this pin (`notes/answers-2026-09-20-validate-c2.md:3`; `plan.yaml:154-214`). Per the operator ruling, this is a corrected task classification, not a missing code mapping.

## Perspective grades

- **operator — pass — SC-01, SC-03, SC-04.** Recorded fail-first runs and byte receipts distinguish preserved behavior from the signed strict-station, rejected-review, module-registration, schema, and missing-policy divergences; the baseline corpus receipt establishes that every live baseline plan status is declared or omitted/defaulted; and the shipped policy has only the effective `review` key (`notes/fail-first-receipts.md:3-141`; `notes/build-divergences.md:8-48`; `notes/research-FEAT-61-control-plane-consolidation.md:14-16`; `.harness/harness.json:381-383`).
- **code maintainer — pass — SC-02, SC-05, SC-06, SC-07.** `STATION_ROWS` derives every lifecycle view, the two governed write adapters call one checkout predicate, strict JSON and run-step navigation have one accessor home, and repo-local loading has one `spec_from_file_location` implementation. The two shipped AST locks pass on the clean tree and fail on controlled independent mutations (`.claude/skills/harness/bin/factory_config.py:59-99`; `.claude/skills/harness/bin/harness_boundary.py:260-313`; `.claude/skills/harness/bin/artifact_accessors.py:38-64`; `tests/integration/test-check-plan-routes.py:2477-2510`). The accepted bootstrap duplication is explicit at every copy and in DEC-234 (`.claude/skills/harness/bin/branch-create-gate.py:46-49`; `.harness/harness/docs/DECISIONS.md:7647-7668`).
- **reader — pass — SC-08.** The pinned tree contains all five reciprocal bootstrap comments, DEC-234's no-import-seam rationale, the lifecycle vocabulary and historical `_work_started` distinction, and the signed direct-execution boundary (`.claude/skills/harness/bin/branch-create-gate.py:46-49`; `.claude/skills/harness/bin/gh-close-gate.py:39-42`; `.claude/skills/harness/bin/merge-gate.py:39-42`; `.claude/skills/harness/bin/plan-sign-gate.py:65-68`; `.claude/skills/harness/bin/run-unit-tests.py:41-44`; `.harness/harness/docs/DECISIONS.md:7647-7668`; `.harness/glossary.md:27-45`; `plan.yaml:145-147`).

## Success-criterion outcomes

| SC | Verdict | Evidence at `f798e2e600ed08aeb49d61a3a229a626750a9ccb` |
|---|---|---|
| SC-01 | **met** | The durable baseline-overlay receipt records the changed test files red against baseline and green after the implementation, and the consolidation detector reports 16 baseline copies versus 0 at the shipped tree (`notes/fail-first-receipts.md:3-4,120-141`). The divergence ledger identifies the compared T-02, T-03, and T-05 fixture populations and separately rules every changed observable (`notes/build-divergences.md:8-48`). The pinned lifecycle receipt stores exit/stdout/stderr for every top-level and task lifecycle case, and its test compares each whole result (`tests/integration/fixtures/feat61-check-plan-routes-lifecycle.receipt.json:1-108`; `tests/integration/test-check-plan-routes.py:2403-2421`). |
| SC-02 | **met** | One ordered tuple of eight string rows derives `MANDATED_STATIONS`, `TERMINAL_STATIONS`, `ACTIVE_STATIONS`, and `FINISHED_STATIONS`; both predicates reject undeclared values (`.claude/skills/harness/bin/factory_config.py:59-99`). Unit assertions cover exact order, tuple derivation, bucket partitioning, every legal value, and empty/unknown/non-string failure (`tests/unit/test-factory-config.py:541-608`); the baseline overlay was red (`notes/fail-first-receipts.md:5-20`). |
| SC-03 | **met** | Integration cases prove `rejected` completes review, `abandoned` plus `rejected` alone resumes `ready`, and empty/unknown values refuse without writing (`tests/integration/test-plan-merge.py:3342-3389`). The one-time baseline corpus receipt states every live baseline status is declared or omitted/defaulted to `ready` (`notes/research-FEAT-61-control-plane-consolidation.md:14-16`), and the lifecycle cases were red at baseline (`notes/fail-first-receipts.md:61-78`). |
| SC-04 | **met** | Production loads and evaluates only `review`, while removed keys are ignored and missing/non-string/unknown review values fail (`.claude/skills/harness/bin/gate_policy.py:5-12,25-62`; `tests/unit/test-gate-policy.py:24-92`). The live config contains only `review` (`.harness/harness.json:381-383`), the public validator exercises review enforcement (`tests/integration/test-validate-digest.py:3749-3761`), and both affected tests were red at baseline (`notes/fail-first-receipts.md:80-97`). |
| SC-05 | **met** | Both governed adapters call the single shared predicate while retaining route-owned wording, exit behavior, ambiguity handling, and unexpected-failure absorption (`.claude/skills/harness/bin/check-domain.py:679-711`; `.claude/skills/harness/bin/bash-write-guard.py:794-823`; `.claude/skills/harness/bin/harness_boundary.py:247-280`). Exact integration cases cover wrong/correct checkout, absent/ambiguous worktrees, nonmatching paths, exact refusal channels, and injected unexpected failures (`tests/integration/test-check-domain-worktree.py:730-823`; `tests/integration/test-bash-write-guard.py:1026-1117`); the baseline overlay was red (`notes/fail-first-receipts.md:99-118`). |
| SC-06 | **met** | `strict_json_loads` and `load_run_step_contract` are centralized in `artifact_accessors`, and `load_repo_module` contains the only bin `spec_from_file_location` call (`.claude/skills/harness/bin/artifact_accessors.py:38-64`; `.claude/skills/harness/bin/harness_boundary.py:283-313`). Unit and integration cases cover nested duplicate/non-finite JSON, natural schema errors, duplicate schema keys, null specs, registration lifetime, original exec failures, and caller catch boundaries (`tests/unit/test-artifact-accessors.py:155-210`; `tests/unit/test-harness-boundary.py:395-457`; `tests/integration/test-check-state-feat59.py:609-714`; `tests/integration/test-check-domain.py:151-189`). Those cases were red at baseline (`notes/fail-first-receipts.md:22-60`). |
| SC-07 | **met** | The shipped-tree assertion expects no findings; independent mutations introduce a whole active bucket, drifted active and finished subsets, and a second loader, while the cross-bucket historical predicate remains a negative control (`tests/integration/test-check-plan-routes.py:2440-2510`). The same detector reports 16 named baseline copies and 0 after consolidation (`notes/fail-first-receipts.md:120-141`). |
| SC-08 | **met** | Each of the five bootstrap comments names the other four and DEC-234 (`.claude/skills/harness/bin/branch-create-gate.py:46-49`; `.claude/skills/harness/bin/gh-close-gate.py:39-42`; `.claude/skills/harness/bin/merge-gate.py:39-42`; `.claude/skills/harness/bin/plan-sign-gate.py:65-68`; `.claude/skills/harness/bin/run-unit-tests.py:41-44`). DEC-234 explains why no shared import seam exists before the trusted bin path is established (`.harness/harness/docs/DECISIONS.md:7647-7668`), and the glossary defines `not_started`, `active`, and `finished`, including the `_work_started` distinction (`.harness/glossary.md:27-45`). |

## Evidence provenance and ruling

- The signed brief declares three perspectives and SC-01 through SC-08 (`BRIEF.md:7-32`); the signed plan traces every SC through completed T-01 through T-05 (`plan.yaml:153-283`).
- The c3 QA regate grades this exact immutable range with `matrix_ok: true`: all 41 unit files, all 68 integration files, and all five signed task chains pass, and every automated SC has named fail-first evidence (`notes/review-harness-qa-c3.md:3-34,36-62`).
- The authoritative ruling corrected T-01/T-02/T-03 to `cross_module`, which requires and matches their unit-plus-integration chains; it expressly forbids treating the correction as a missing code mapping (`notes/answers-2026-09-20-validate-c2.md:3`; `notes/review-harness-qa-c3.md:7-19`).

## Findings

None.

## Open questions

None.
