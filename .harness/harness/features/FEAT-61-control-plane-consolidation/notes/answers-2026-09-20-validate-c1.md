# Answers — 2026-09-20 validate c1

- VAL-01: APPLIED. Durable fail-first receipts for SC-01..SC-06 in `notes/fail-first-receipts.md` — every new/changed test file run against a disposable worktree at the plan baseline 066638e8 with HEAD's tests overlaid; each exits 1 there and 0 at HEAD. SC-01/SC-07's red half: HEAD's `consolidation_findings` reports 16 findings on the baseline bin/, 0 at HEAD. Commit 14667224.
- VAL-02: APPLIED. `check-plan-routes.py` station lock now flags a literal collection of ≥2 names inside ONE bucket when used as a predicate (Compare / Return / Assign target), so a drifted subset such as `("plan", "ready", "building")` is reported; `_work_started`'s cross-bucket trio and a `for` over station keys are not. The one site the stricter lock surfaced — `plan-merge._approval_resume_station`'s `{"ready","building","review"}` — is now derived as `set(ACTIVE_STATIONS) - {"plan"}` rather than exempted. Mutants: drifted active bucket + drifted finished bucket (both flagged), D-11 control (not flagged). Commits 14667224, 910bdee2. code-grade 89/89 PASS.
- VAL-03: RULED by the operator — TypeError accepted as an allowed divergence; recorded in `notes/build-divergences.md` #12. No code change.
- CR-02 (low, form): T-02's signed `files:`/`verify:` omit `tests/integration/test-gh-sync-record.py`; left for the orchestrator to record as a plan amendment per its own procedure.
