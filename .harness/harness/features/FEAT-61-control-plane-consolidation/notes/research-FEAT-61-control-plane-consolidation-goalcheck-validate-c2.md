# FEAT-61 goal-check — validate c2

**Verdict: PASS.** The reviewed checkout is exactly `f3825ca1dcb1d5bb6ff6e62b876988ebd46ebb08`. All five signed task verification clauses exited 0 with `HARNESS_AGENT_TYPE` removed, the direct consolidation audit reported `0 consolidation finding(s) under bin/`, every signed perspective is met, and SC-01 through SC-08 are met.

## Perspective grades

- **operator — pass — SC-01, SC-03, SC-04:** the fail-first receipt, byte receipts, strict lifecycle cases, one-time baseline-corpus record, and review-only policy checks establish preserved behavior plus the signed and operator-ruled divergences (`notes/fail-first-receipts.md:3-141`; `notes/build-divergences.md:8-48`; `tests/integration/test-plan-merge.py:3358-3387`; `tests/unit/test-gate-policy.py:63-82`).
- **code maintainer — pass — SC-02, SC-05, SC-06, SC-07:** the station table, checkout predicate, strict JSON/schema accessors, and repo-module loader each have one implementation, while the two controlled-mutation locks pass and independently detect copied station buckets and a second loader (`.claude/skills/harness/bin/factory_config.py:59-99`; `.claude/skills/harness/bin/harness_boundary.py:260-316`; `.claude/skills/harness/bin/artifact_accessors.py:38-64`; `tests/integration/test-check-plan-routes.py:2446-2509`).
- **reader — pass — SC-08:** all five reciprocal bootstrap comments, DEC-234, lifecycle glossary entries, and the direct-execution decision are present at the pin (`branch-create-gate.py:46-49`; `gh-close-gate.py:39-42`; `merge-gate.py:39-42`; `plan-sign-gate.py:65-68`; `run-unit-tests.py:41-44`; `.harness/harness/docs/DECISIONS.md:7647-7668`; `.harness/glossary.md:31-45`; `plan.yaml:145-147`).

## Success-criterion outcomes

| SC | Status | Evidence at `f3825ca1` |
|---|---|---|
| SC-01 | **met** | The durable baseline overlay run records every new/changed SC-01..SC-06 test file exiting 1 at baseline and 0 at the reviewed implementation, and the current lock finds 16 baseline copies versus 0 at the pin (`notes/fail-first-receipts.md:3-4,120-141`). The ledger records the compared corpus and the ruled divergences (`notes/build-divergences.md:8-48`). The pinned lifecycle fixture stores exit/stdout/stderr for 20 cases and the integration test compares each whole result (`tests/integration/fixtures/feat61-check-plan-routes-lifecycle.receipt.json:1-108`; `tests/integration/test-check-plan-routes.py:2403-2421`). All integration clauses in T-02, T-03, and T-05 exited 0 on direct re-run. VAL-03's malformed-list `TypeError` is the operator-allowed divergence, not a reopened defect (`notes/answers-2026-09-20-validate-c1.md:5`; `notes/build-divergences.md:38`). |
| SC-02 | **met** | One ordered string-row table derives all four exports and strict predicates (`.claude/skills/harness/bin/factory_config.py:59-99`); unit assertions cover exact order, tuple derivation, the full partition, all legal values, and empty/unknown/non-string rejection (`tests/unit/test-factory-config.py:541-607`). Its baseline overlay exited 1 and the current signed unit clause exited 0 (`notes/fail-first-receipts.md:5-20`). |
| SC-03 | **met** | Integration cases prove rejected completes review, abandoned/rejected alone resume ready, and empty or unknown values refuse without writing (`tests/integration/test-plan-merge.py:3358-3387`); the one-time baseline corpus record says every live baseline status was declared or omitted/defaulted to ready (`notes/research-FEAT-61-control-plane-consolidation.md:14-16`). The fail-first receipt records the lifecycle tests red at baseline (`notes/fail-first-receipts.md:61-78`), and the complete amended T-02 chain exited 0. |
| SC-04 | **met** | Production loads and evaluates only `review` (`.claude/skills/harness/bin/gate_policy.py:5-12,52-62`); live, template, and example configs contain only that gate (`.harness/harness.json:381-383`; `.claude/skills/harness/templates/harness.json:167-169`; `.claude/skills/harness/templates/examples/harness.kaya-ai.json:64-66`). Unit tests prove removed keys have no effect and missing/non-string/unknown review fails (`tests/unit/test-gate-policy.py:25-82`); the public validator exercises review enforcement (`tests/integration/test-validate-digest.py:3749-3789`). Both tests were red at baseline and the signed T-04 clause exited 0 (`notes/fail-first-receipts.md:80-97`). |
| SC-05 | **met** | Both adapters call the single `feature_artifact_checkout_mismatch` implementation (`.claude/skills/harness/bin/check-domain.py:695-699`; `.claude/skills/harness/bin/bash-write-guard.py:810-814`; `.claude/skills/harness/bin/harness_boundary.py:260-280`). Exact route-specific cases cover mismatch, allowed paths, absent and ambiguous worktrees, wording/channel, and absorbed unexpected failures (`tests/integration/test-check-domain-worktree.py:730-823`; `tests/integration/test-bash-write-guard.py:1039-1116`). The baseline overlay was red and the current T-03 clause exited 0 (`notes/fail-first-receipts.md:99-118`). |
| SC-06 | **met** | `strict_json_loads`, `load_run_step_contract`, and `load_repo_module` are the sole implementations (`.claude/skills/harness/bin/artifact_accessors.py:38-64`; `.claude/skills/harness/bin/harness_boundary.py:283-316`); direct searches find the only `spec_from_file_location` call at `harness_boundary.py:299` and only two run-schema callers at `check-state.py:1383` and `check-domain.py:1569`. Unit and integration cases cover duplicate/non-finite JSON, natural schema errors, null specs, registration lifetime, original exec failures, and caller catch boundaries (`tests/unit/test-artifact-accessors.py:158-210`; `tests/unit/test-harness-boundary.py:395-455`; `tests/integration/test-check-state-feat59.py:615-713`; `tests/integration/test-check-domain.py:151-188`). Their baseline overlays were red and the signed T-01/T-03 clauses exited 0 (`notes/fail-first-receipts.md:22-59`). VAL-03 remains the accepted outcome named above. |
| SC-07 | **met** | The AST detector recognizes lifecycle-bucket predicates, including drifted subsets, while exempting D-11's cross-bucket historical predicate, and detects any second bin loader (`.claude/skills/harness/bin/check-plan-routes.py:1619-1634,1663-1769`). Controlled mutations separately prove a copied exact bucket, two drifted partial buckets, the D-11 control, and a second loader (`tests/integration/test-check-plan-routes.py:2446-2509`). Direct audit at the pin reports zero findings; the baseline comparison reports 16 (`notes/fail-first-receipts.md:120-141`). |
| SC-08 | **met** | `git diff --quiet f3825ca1 --` over all seven inspected files confirmed the working copies equal the pinned blobs. Each bootstrap comment names the other four and DEC-234 (`branch-create-gate.py:46-49`; `gh-close-gate.py:39-42`; `merge-gate.py:39-42`; `plan-sign-gate.py:65-68`; `run-unit-tests.py:41-44`). DEC-234 explains the pre-`sys.path` impossibility of a shared import seam (`.harness/harness/docs/DECISIONS.md:7647-7668`), and the glossary defines `not_started`, `active`, and `finished`, including the historical `_work_started` distinction (`.harness/glossary.md:31-45`). |

## Trusted resolution verification

- **VAL-01 applied:** commit `14667224` is an ancestor of the pin; its committed receipt identifies baseline `066638e8`, the overlay method, red exits, current green exits, and the baseline/current lock counts (`notes/answers-2026-09-20-validate-c1.md:3`; `notes/fail-first-receipts.md:3-141`).
- **VAL-02 applied:** commit `910bdee2` is an ancestor of the pin. The detector now catches any predicate collection of at least two names within one lifecycle bucket (`check-plan-routes.py:1684-1721`); the split mutant cases prove drifted active and finished subsets while preserving the D-11 control (`tests/integration/test-check-plan-routes.py:2446-2498`); `_approval_resume_station` derives its codomain from `ACTIVE_STATIONS - {plan}` (`plan-merge.py:2054-2068`).
- **VAL-03 honored:** the operator accepted divergence #12; it remains explicitly recorded and was not reopened (`notes/answers-2026-09-20-validate-c1.md:5`; `notes/build-divergences.md:38`).
- **CR-02 ledgered:** the pinned `feature.json` contains amendment judgements for both `T-02.files` and `T-02.verify`; the reviewed plan now names `tests/integration/test-gh-sync-record.py` in both the file scope and exact verification chain (`plan.yaml:189-204`).

## Direct signed-clause re-measurement

- T-01: exit 0 — `test-factory-config.py`, `test-artifact-accessors.py`, `test-feature-json-reader.py`, `test-harness-boundary.py`.
- T-02: exit 0 — all eight signed files, including the amended `test-gh-sync-record.py`.
- T-03: exit 0 — `test-check-state-feat59.py`, `test-check-domain.py`, `test-check-domain-worktree.py`, `test-bash-write-guard.py`.
- T-04: exit 0 — `test-gate-policy.py`, `test-validate-digest.py`.
- T-05: exit 0 — `test-check-plan-routes.py` and the generated decisions-index diff.

## Findings

None.
