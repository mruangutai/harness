# Goal-check — BUG-1129 validate c1

**Verdict: PASS.** At immutable review SHA `499eaf0b9c1eb04e0f51dcfec47fab9ea50abd54`, T-01 traces every signed criterion (`plan.yaml:35-40`) and fully discharges both declared perspectives and SC-01 through SC-04. All four c0 goal-check findings are closed; none is carried.

## Perspective dispositions

- **operator — pass:** SC-01 and SC-02 are met: the unvalidated non-exempt ship path refuses before the complete GitHub/repository-station write boundary, emits the required diagnostic without `SKIP`, and the real sweep retains the recovery worktree; the all-direct path still ships (`tests/integration/test-gh-sync-ship.py:458-545`; `tests/integration/test-post-merge-sweep.py:577-608`; `notes/review-harness-qa-c1.md:15-23,44-47`).
- **code maintainer — pass:** SC-03 and SC-04 are met: ship and INV-17 share the fail-closed predicate, its valid and invalid shapes are regression-protected with mutation evidence, validated fixtures write the handoff by default, and the lifecycle cases remain green (`.claude/skills/harness/bin/handoff_policy.py:9-68`; `.claude/skills/harness/bin/check-state.py:1186-1210`; `notes/review-harness-qa-c1.md:17-23,35-36,45-46`).

## Success-criterion dispositions

- **SC-01 — pass; c0 Q1 / GC-01 closed.** The guard emits `validation incomplete` and the missing note before body handling or any later write (`.claude/skills/harness/bin/gh-sync.py:2200-2232`). The focused regression asserts exit 1, both diagnostic terms, an empty whole-GitHub write set with `--body-file`, byte-identical `plan.yaml`, and exact pre/post station equality (`tests/integration/test-gh-sync-ship.py:458-494`). QA identifies that case as SC-01 evidence and records no failure in its script (`notes/review-harness-qa-c1.md:15,33-36,44`); its pre-fix red is `notes/receipt-main-session-T-01-fail-first.md:6-9`.
- **SC-02 — pass; c0 Q1 whole-boundary portion / GC-02 closed.** The ship regression binds no `SKIP` and the full zero-write boundary, including comment posting (`tests/integration/test-gh-sync-ship.py:471-489`); the real sweep regression observes the refusal, retains the worktree, and records no milestone mutation (`tests/integration/test-post-merge-sweep.py:577-608`). QA identifies the sweep case as SC-02 evidence and closes the prior boundary gap (`notes/review-harness-qa-c1.md:16,42-47`); the incident path is red before the fix at `notes/receipt-main-session-T-01-fail-first.md:11-17`.
- **SC-03 — pass; c0 Q2 / GC-03 closed.** Ship and INV-17 call the single `handoff_policy.exempt_reason` predicate (`.claude/skills/harness/bin/gh-sync.py:2210-2218`; `.claude/skills/harness/bin/check-state.py:1186-1210`). The predicate permits only a non-empty all-direct task list and denies absent, unreadable, malformed, non-mapping, empty, malformed-task, missing-mode, team, and mixed plans (`tests/unit/test-handoff-policy.py:43-80`); ship also exercises malformed-plan refusal/no-write (`tests/integration/test-gh-sync-ship.py:496-512`), while existing INV-17 cases remain among the integration successes. QA's unit matrix passes and only unrelated route-manifest assertions fail integration (`notes/review-harness-qa-c1.md:11-18,33-36,45`); Arm 5 records the focused unit and verb cases red under a fail-open mutant (`notes/receipt-main-session-T-01-fail-first.md:28-48`).
- **SC-04 — pass; c0 Q3 / GC-04 closed.** `write_validate_handoff` creates every required section and `stage_ship` invokes it by default (`tests/integration/gh_sync_support.py:61-69,789-814`); the refusal fixture explicitly opts out and the fixture-contract assertion verifies the note and sections (`tests/integration/test-gh-sync-ship.py:462-468,514-526`). QA reports only unrelated route-manifest integration failures, so the existing ship, record, open, abandon, sweep, and INV-17 lifecycle scripts remain green, and identifies this assertion as SC-04 evidence (`notes/review-harness-qa-c1.md:18,33-36,42,46`). Arm 4 records the same assertion red against the pre-migration fixture (`notes/receipt-main-session-T-01-fail-first.md:20-26`).

## Prior-finding disposition

- **GC-01:** closed by the exact diagnostic assertion in the SC-01 focused case.
- **GC-02:** closed by the empty whole-GitHub call set with a supplied body/comment path, combined with the real sweep retention case.
- **GC-03:** closed by the exhaustive shared-predicate unit cases, malformed-plan ship case, and Arm 5 fail-open mutation.
- **GC-04:** closed by the default-fixture contract assertion and Arm 4 pre-migration red.

No T-01 or scope-change finding survives. QA's overall matrix verdict remains independently FAIL because `test-check-plan-routes.py` has six manifest-parity failures caused by the isolated pin worktree; QA classifies them as unrelated to T-01 and reports every SC coverage/fail-first gap closed (`notes/review-harness-qa-c1.md:3-25,31-50`). That separate gate result does not negate the named SC evidence above. QA removed its isolated worktree, and the final `.claude/worktrees/qa-*-pin` census is zero (`notes/review-harness-qa-c1.md:38`).
