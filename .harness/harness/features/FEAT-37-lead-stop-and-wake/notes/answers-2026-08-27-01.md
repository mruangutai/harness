# Answers — FEAT-37 build seam — 2026-08-27

Written by the main session. Each of your three questions is answered; none is deferred.

## Q1 — T-02 is DONE. Build resumes.

I executed it myself, as DEC-179 requires. `check-domain.sh --resolve` on
`.claude/skills/harness-team/SKILL.md` returns `NOBODY`, which I re-verified before writing.

**Commit `8f784b5`.** Three edits, one file, 240 lines to 255. All anchors re-derived by text.
`plan.yaml` records T-02 `done`.

Evidence, and I ran all of it myself rather than reporting the edit:
- `--group playbook` exits **0**, all ten cases PASS.
- `--self-check` exits **0**, all seven variants behave as designed.
- **A live mutant on the shipped text**: deleting the nudge-and-denial sentence turns
  `case7_overrides_tool_text` red **and nothing else**. So case7 grades that sentence, not its
  neighbourhood. That is the proof the group is not vacuous against the real file.

**T-05 and T-06 are unblocked.** Both depend on T-02 and nothing else outstanding.

## Q2 — Your widening is CONFIRMED. Keep it. The plan text was what was wrong.

I re-derived it rather than taking your word. `DECISIONS.md:6872` reads "a second identical return
**ships**"; `plan.yaml`'s signed alternation spelled only "will ship"; and T-06's own intent at the
`decisions:` block mandates correcting that exact sentence. So the signed list could not see the one
occurrence T-06 is told to fix, and T-06's verify would have exited 0 either way. That is the
issue-804 shape D-05 exists to refuse, reached through a contradiction inside the plan rather than
through drift.

Measured after your change: the bound group names **three** occurrences on `DECISIONS.md` where it
named two, the new one at 6872. It discriminates.

**I have reconciled `plan.yaml` to match the shipped guard, at `c0262e6`.** The approved spec now
carries both members with the reason recorded inline, so a later reader re-deriving from the plan
does not rebuild the vacuous version. No requirement, scope or decision changed. **Do not narrow the
detector back**, and do not treat the plan edit as licence to re-open T-01.

## Q3 — Confirmed, structural, and MINE. Do not work around it, and do not let it block you.

I reproduced it: `check-state.sh` exits 1 with exactly two INV-26 violations, T-01 (#905) and T-04
(#907), both cards at Building while the plan says done. I verified both of your citations at disk:
`check-state.sh:1522` widens the accepted set only when `feature.json` status is `Review`, and
`gh-sync.py:1257` is inside `cmd_ship`, a main-session subcommand.

`check-state.sh` is enforcement-layer, so DEC-174 makes the fix mine and it is not this feature's
work. **Treat those two violations as known and expected for the rest of this run.** Do not edit
`check-state.sh`, do not move cards by hand, and do not report the run BLOCKED on them. Every other
violation is still yours to surface.

## Budget

Runs 10 of 20 at your return; **ten remain** for T-05, T-06, the QA gate, the simplify pass, the
review panel, the goal-check and the docs sweep. Cycles 1 of 10.

## Unchanged from the original dispatch

- The task numbering gap at T-03 is deliberate. The struck regression is issue **#903**.
- **SC-08 must not be scored from this build.** Your own eng lead corroborated why: the
  `SubagentStop` hook resolved `inflight_registry.py` from the main checkout and printed the OLD
  sentence mid-run. That is independent evidence for D-13. SC-08 stays `not_met`.
- Re-derive every line anchor before editing. `origin/main` was merged in before the signature.
