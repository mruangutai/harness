# FEAT-37 — the build is done and green; the qa gate blocks on a missing eval. One decision is yours.

**All five tasks are built, committed and independently verified. Every suite is green. The feature
cannot ship anyway, because a signed decision requires an `eval` for the one task that changed agent
behaviour, and no eval exists — not an unset runner, an unwritten test. Only you can resolve it.**

Runs 13 of 20, cycles 1 of 10. Seven runs and nine cycles left — the budget is not the problem.

## The decision I need from you

**T-02 rewrote `.claude/skills/harness-team/SKILL.md` — prompt text that shapes how three lead agents
behave. The approved plan types it `change_type: ai_behavior`. DEC-70 (`DECISIONS.md:852`) says an
`ai_behavior` change requires an `eval`: `ai-dev` authors it, `qa` runs it, `validator-lead` judges
its adequacy, and "an AI change with no eval FAILs exactly as a missing unit test does."**

There is no eval. `harness.json` `test_kinds.eval` is `cmd: null`, `status: unresolved`. And the
deeper problem is not the missing runner — **the plan contains no `ai-dev` task at all**, because
T-02 was routed `main-session-direct` under DEC-179 and you executed it yourself. So pointing a
runner at the existing text-assertion suite would register a runner for an eval nobody wrote.

`gates.qa_gate` is `blocking` — the only blocking gate this project has. DEC-36 (`:448`) and DEC-187
(`:5861`) both say a null `cmd` is **BLOCKED, never a soft skip**.

**Your two routes, and both edit files outside every FEAT-37 task's declared set:**

1. **Author the eval.** `ai-dev` writes failure modes, a rubric, a reference dataset and a threshold
   for the playbook rewrite. That is a new task, so it is a plan change — pm's to draft, yours to
   approve. It is the route DEC-70 actually describes. It costs at least two runs and it is the only
   route that produces evidence about *conduct*.
2. **Sign an exclusion.** Set `test_kinds.eval` to `status: excluded` with an `excluded_because` and
   a `signed:` reference, the same shape `functional` already carries under DEC-187. Cheap, honest
   about what this repo can actually measure, and it concedes that `ai_behavior` changes here are
   graded by text assertions plus your post-merge UAT.

**I am not neutral, and here is the reasoning rather than the preference.** The thing this feature
changes is what three lead agents *do*. Route 2 ships that change with no behavioural evidence from
any source, because SC-08 — the only criterion that measures conduct — is also ungradable from this
build (below). Route 1 is the only one where anything ever tests the behaviour. But route 1 is
expensive, and an eval you commission reluctantly tends to be a rubric written to pass. If you take
route 2, the honest form is to say out loud that this repo grades prompt changes by text and by your
own post-merge read, and to make SC-08's UAT a hard precondition of merge rather than a nicety.

**One thing you should weigh before deciding: your own approved BRIEF disagrees with the decisions.**
`BRIEF.md:275-278` states the eval requirement "resolves to a soft skip and proves nothing" and calls
it a standing dev-ops gap this feature does not close. You signed that. But DEC-36 and DEC-187 say it
blocks, and a signed BRIEF does not override a signed decision. I am surfacing the contradiction
rather than picking the reading that lets me proceed. If your intent when signing was route 2, say
so — but it still wants the `harness.json` change, because the BRIEF governs this feature and the
config governs the gate.

## How this briefing was assembled (DEC-69 disclosure)

**No report round was spawned.** I read from disk:

- `runs/2026-08-27-01-validator/digest.md` — the blocking qa result
- `runs/2026-08-27-01-t01-eng/digest.md` and `runs/2026-08-27-01-t04-eng/digest.md` — build
- `runs/t06-product/digest.md` — T-06
- `notes/ship-review-2026-08-26-02-product.md` — the plan phase, already synthesised for you there
- `notes/qa-FEAT-37-2026-08-27-01.md` and `-02.md` — qa's own artifacts

**One digest cannot be read, because it no longer exists.** `runs/2026-08-27-01-product/digest.md`
held T-05's result; the T-06 run reused the same `run_id` and overwrote it in place. I separated
T-06's artifacts into `runs/t06-product/` and left an honest loss note at the old path rather than
reconstructing it. T-05's PASS is not in doubt — I re-ran its verify myself — but its digest is gone.

**I did not read the six plan-phase digests from 2026-08-24.** The earlier ship review covers that
phase and you wrote the history that produced it.

**I took nothing on report.** Every task's own `verify:` block I re-ran myself. I re-derived DEC-70's
text, `BRIEF.md`'s wording, the `check-state.sh` output and the suite results at disk before writing
any of the claims above — including the ones that make the validator lead right and me wrong.

## What actually got built, and it is good work

| Task | Commit | What it does |
|---|---|---|
| T-01 | `79c2a46` | adds the guard suite `test-lead-stop-and-wake.py`, red by design |
| T-02 | `8f784b5` | gives the lead tier the never-wait rule and its inoculation (you executed this) |
| T-04 | `e73d545` | states the measured refusal bound in the text an agent actually reads |
| T-05 | `1449c66` | binds DEC-201's never-wait rule to the three domain leads, in place, one voice |
| T-06 | `5e09992` | corrects DEC-199's falsified once-only clauses to the measured bound |

`run-unit-tests.sh --kind unit` exits **0**. It was red by design for the entire build and T-06 closed
the last group. `--kind integration` exits **0** with 532 passing assertions and zero failures. All three
`test-lead-stop-and-wake.py` groups pass and `--self-check` exits 0. qa added the `integration` kind
itself under DEC-35 — not required by the `logic` floor, and it is the only thing that *executes*
`children_refusal_lines()` rather than grepping its source. That was good judgement and I want it on
the record.

**Two members exercised judgement I endorsed, where following the plan literally would have put a
falsehood into the decision record.** T-05's intent told the documentor to write that the platform's
dispatch nudge "appears nowhere under `.claude/`" — false, and false *because of this feature's own
T-02*, which denies that nudge at `harness-team/SKILL.md:119`. T-06's intent told it to cite
`harness/SKILL.md` as stating the same refusal bound — a paragraph `c5e59aa` had already deleted.
Both wrote the true form of the same constraint and declared the departure. I verified both by grep.

## SC-08 cannot be scored from this build, and that is not a dodge

A spawned agent loads its skills from the **main checkout**, while the rewritten playbook sits in this
worktree. So nothing observed during this run is evidence about the shipped behaviour. This has now
been corroborated four times, most recently by this run's own `SubagentStop` refusals, which kept
printing the **old pre-T-04 sentence** even though `inflight_registry.py:339-340` in this worktree
carries the corrected one. SC-08 stays `not_met`. **It needs you to run its UAT after merge, from the
main checkout.** Nobody inside the factory can close it.

The goal-check has not run — the blocking gate stopped the sequence before it. So has simplify, the
`review_sha` pin, the review panel and the docs sweep. `review_sha` is still `none`, deliberately: an
apply commit after a pin invalidates the panel's verdict.

## Proposed backlog — strike any row by ID

| ID | Nature | What |
|---|---|---|
| B-1 | bug | `gh-sync.py` has no subcommand that moves a single task card to Done, so every finished task trips INV-26 until `ship` runs. Six live violations here: #905, #906 (at Backlog), #907, #908, #909, parent #904. Structural, enforcement-layer, DEC-174 makes it yours |
| B-2 | bug | `BRIEF.md:275-278` says the eval requirement is a soft skip; DEC-36 and DEC-187 say it blocks. An approved artifact contradicts two signed decisions |
| B-3 | bug | `harness.json` `test_kinds.ui._reason` still licenses "qa records skipped and does NOT fail" while `status: unresolved`. DEC-187 settled against exactly that phrasing and corrected the top-level note but not this per-kind one. Latent today; it licenses the silent no-op DEC-36 exists to prevent |
| B-4 | bug | Nothing prevents a lead reusing a `run_id`, and the second run overwrites the first's `digest.md` and `state.yaml` in place. It destroyed T-05's digest this run. The playbook's `<task-or-purpose>-<squad>` slug convention would have prevented it but nothing enforces it |
| B-5 | chore | T-01's `verify:` block asserts its suite is red (`pb=1 cv=1`), true only at its own commit. Re-running it at HEAD now prints `T01_FAIL`. Not a defect, but a successor reads it as a regression |
| B-6 | bug | `SendMessage` was unavailable to the validator lead, so an intended follow-up probe to its running qa member spawned a second member instead. It paid for itself but nearly collided two contexts on one artifact path |
| B-7 | chore | Engineer DIGESTs carry no `files_touched`, so a member that wrote a receipt reports no files and the lead reconstructs it by hand. Schema gap or intended? |
| B-8 | chore | `notes/root-cause-*.md` is in no member's domain, so debug reports fall back to receipt paths |
| B-9 | chore | Single-flight is keyed per checkout, so several orchestrators' children can share one registry when they run from one cwd |
| B-10 | chore | Issue #811 stays OPEN and returns to the backlog, per your ruling of 2026-08-24 (plan D-07 is the strike record) |
| B-11 | chore | Issue #903 holds the regression struck from this plan at signature, with REQ-08 and SC-09. It stays struck; the row exists so it is not forgotten |

## What I need back

**Pick route 1 or route 2 on the eval.** Everything else waits on it. Once it resolves I run simplify,
pin `review_sha`, run the panel and the goal-check, then the docs sweep — five runs of the seven left,
which fits.

If you want the run count read: 13 of 20, and I think they earned their place. One send-back in the
whole feature, zero this session, and every run either landed a task or closed a real question. The
one genuinely wasted spawn is B-6, and the validator lead reported it against itself.
