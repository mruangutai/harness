# Answers — FEAT-37 eval amendment — 2026-08-27

## THE PLAN IS SIGNED. Seven tasks. Build T-07, then T-08.

`approval:` reads `approved` and now covers T-07 and T-08. Commit `a53e6b3`.

## Q1 — RULED, and your stated cause was WRONG. Read this before you dispatch T-07.

The operator ruled that **`harness-ai-dev` authors the eval**, not the main session. The main
session wrote the playbook text T-02 installed, so it authoring the test of that text is a closed
loop. QA running it and validator-lead judging its adequacy do not undo that.

**Your escalation was right to happen. The reason you gave for it was not**, and I ran the
discriminating probe rather than accepting it:

You said `evals/**` fails because it is a PRODUCT-base glob and this checkout's product bases are
other repositories. **It is not.** `harness_boundary.select_base` returns a glob filter that accepts
EVERYTHING in the harness base — nothing is filtered on the glob side there at all — and rejects on
the **TARGET** side through `is_control_plane_target`. Measured:

| probe | result |
|---|---|
| glob filter applied to `evals/**` in the harness base | **accepts it** |
| target test on `evals/x/y.py` | **rejects** — not control plane |
| target test on `.claude/skills/harness/evals/x.py` | **accepts** |

You were honest that you had not isolated which half rejects, and that honesty is why this got
caught rather than shipped. Do not repeat the product-base explanation; it is in no artifact now.

**So the fix was neither option you offered.** The harness's own evals move INSIDE the control plane
to `.claude/skills/harness/evals/`, and ai-dev gains a grant there — one line in `team-config.yaml`,
main-session work, **zero runs**. The repository-root `evals/**` grant stays exactly as it is; it
serves a SERVED repository's evals and was never wrong. Recorded as **D-16**, and D-14's overturned
clause is corrected in place rather than left standing beside the truth.

**Proof it works, and the control that shows the grant is doing the work:**

- `check-domain.sh --resolve .claude/skills/harness/evals/lead-never-wait/run-eval.py` → **`harness-ai-dev`**
- `check-domain.sh --resolve evals/lead-never-wait/run-eval.py` → still **`NOBODY`**
- `check-plan-routes.py` → **`OK T-07 granted to harness-ai-dev`**, 0 violations

## Q3 — CONFIRMED. `harness.json` is not enforcement-layer, but check your own evidence.

Your conclusion is right and I verified it. Your evidence was imprecise: `check-state.sh` DOES
contain the string `test_kinds` — at line 479 — but only inside a comment. Nothing enforcing
consumes `test_kinds.eval.cmd`. `run-unit-tests.sh` reads `test_kinds.integration.detect` and is a
runner, not a gate, and is not in DEC-174's category. T-08 stays with dev-ops.

You were also right that DEC-174 amendment 4 makes "not in the named list" void reasoning. The
category governs. It still does not cover this.

## Q4 — Folded in, as you asked. Do not spend a run on it.

T-07's and T-08's `execution_reason` fields carry their lanes. The `lanes:` block's stale
`resolved_at` is a record tidy, not a build blocker — leave it. Neither task carrying a
matrix-required test is correct: `scaffolding` and `config` both map to `always: []`.

## Q5 — Budget, ruled. Runs 14 of 20, SIX remain.

Main-session steps do NOT count against the 20; the counter is agent runs. T-07 and T-08 are both
`team` now, so both cost a run. That leaves four for the qa gate, the review panel, the goal-check
and the docs sweep — exactly enough with nothing spare. **Spend nothing on rework you can settle by
reading the artifact yourself, and if any segment threatens to exceed one run, return and tell me
before spending it.**

## Q6 — SC-10 ADOPTED as you proposed it, since T-08 is approved.

The eval flags every labelled violating lead-turn case and none of the compliant ones. That is a
discrimination requirement, not a quality score, and it is what T-07's `--prove-discrimination` flag
exists to demonstrate. pm adds it when it next touches the BRIEF; do not spend a run on it alone.

## Q7 — Recorded as a backlog row, not this feature's work.

A lead holds no `SendMessage`, so a finding made after dispatch cannot reach a member in flight.
That is D-03's deliberate consequence — a lead with a message tool gains a reason to stay awake and
watch, which is the behaviour this whole feature removes. The cost you hit twice is real and the
trade was made knowingly. It belongs on the backlog, not in a fix here.

## Your own error, and the count you corrected

Naming the struck task id in STATE.md and re-tripping the checker: caught, corrected, and the
correction says so in the file. That is the right handling.

You were also right that the INV-26 count is **five**, not the six I carried. I re-measured. Still
mine, still not yours to work around.

## Unchanged

SC-08 stays `not_met`, `review_sha` stays `none` until the panel pins it, no PR, no merge.
