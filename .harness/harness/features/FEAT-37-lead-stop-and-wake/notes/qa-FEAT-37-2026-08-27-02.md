# QA gate — FEAT-37 lead-stop-and-wake — addendum run (supersedes qa-FEAT-37-2026-08-27-01.md on two points)

**BLUF: ESCALATE stands**, same root cause as `-01` (T-02's `ai_behavior` requires `eval`, which is
`cmd: null` / `status: unresolved` in `harness.json` — a dev-ops-owned file no task here may touch).
This run does not re-litigate that call. It answers two dispatched predictions with measurement, and
in doing so **corrects one factual claim in `-01`**: that note said `test-inflight-registry.py`'s
`case_6b` was "unchanged by this diff" and therefore didn't cover T-04. `git show e73d545 --
.claude/skills/harness/bin/test-inflight-registry.py` shows it WAS changed by T-04 — the vacuous
`any("once" in l.lower() ...)` substring check was replaced by a regex asserting the actual
prescribed-response text. `-01`'s bottom line (ESCALATE, eval blocked) is unaffected; its coverage
narrative for T-04 was wrong and is corrected here.

## Prediction 1 — unit vs integration binding T-04 — CONFIRMED, with a wrinkle

`run-unit-tests.sh:30-31`: `test-lead-stop-and-wake.py` is in `UNIT_SCRIPTS`,
`test-inflight-registry.py` is in `INTEGRATION_SCRIPTS`. T-04 is `change_type: logic`, whose matrix
floor (`test_matrix.logic.always`) is `[unit]` only. So the floor, taken literally, does not obligate
`integration` — the prediction's premise is right.

But "not one line of T-04's change" is exercised by `unit` alone is **not quite right**: T-04 edited
two things in `children_refusal_lines()`'s message —(a) the once-only **bound** qualifier and (b) the
**prescribed response** ("correct any claim... end the turn again"). `unit`'s
`test-lead-stop-and-wake.py --group bound` (`case_floor_inflight_registry.py`,
`case_occurrence_inflight_registry.py_339_1`) reads `inflight_registry.py`'s source text directly and
asserts (a) is present and qualified — that's real, ran, PASS. What `unit` alone does **not** reach is
(b), and does not reach (a) or (b) via *execution* of the function — only via static text grep.

`test_kinds.integration.detect` names `test-inflight-registry.py` explicitly (confirmed by reading
`harness.json`). Under DEC-35 floor-not-ceiling, this diff clearly warrants it: `case_6b` is the only
test in the repo that calls `children_refusal_lines()` and asserts its *runtime output* rather than
reading the file as text. **Added `integration` as a required kind for this diff.**

Commands run, this session, both completed (not partial reads of a still-running job):
```
.agents/skills/harness/bin/run-unit-tests.sh --kind unit          # exit 0, 27/27 scripts PASS
.agents/skills/harness/bin/run-unit-tests.sh --kind integration   # exit 0, 27/27 scripts PASS (backgrounded, ~6min, waited out to completion)
python3 .claude/skills/harness/bin/test-inflight-registry.py      # exit 0, 69/69 checks PASS
```
`case6b: the message prescribes ending the turn again` — asserts T-04's (b) via a real regex
(`end your turn again|end the turn again|stop again|return again`), not a token/exit-code proxy.
`case_occurrence_inflight_registry.py_339_1` (unit, bound group) — asserts T-04's (a).
**Both halves of T-04's diff are now demonstrated exercised — (a) by unit (text), (b) by integration
(execution) — where before only (a) was confirmed and (b) was reported (wrongly, in `-01`) as
uncovered.**

## Prediction 2 — BRIEF's "soft skip" claim contradicts DEC-187 — CONFIRMED, same call as `-01`

- `BRIEF.md:275-278` (read directly): *"that requirement resolves to a soft skip and proves
  nothing."* Confirmed verbatim.
- `harness.json` `test_kinds.eval`: `cmd: null`, `status: "unresolved"` (read directly, not taken on
  report).
- `DECISIONS.md:5856-5861` (DEC-187): `unresolved` "means nobody has decided yet; it blocks."
  A soft skip requires `status: excluded` **and** a `signed` decision id — inferred exclusion is
  explicitly ruled out ("never inferred").
- Second independent check, as asked: `find . ... -iname '*eval*'` filtered to the `eval.detect`
  glob (`evals/**|tests/**eval**`) — **zero matches** in this checkout.
- T-02 is `change_type: ai_behavior` (`plan.yaml:356`), whose matrix requirement is `eval` (always).

So this resolves to **BLOCKED per DEC-187**, not the soft skip the BRIEF's own "Verification gaps"
section asserts. The BRIEF is wrong on this one clause; the remedy is unchanged from `-01`'s call:
`harness.json` `test_kinds.eval` needs either a working `cmd` (`status: active`) or a signed exclusion
(`status: excluded`, `signed: <decision-id>`) — the file is dev-ops-owned and outside every task's
`files:` list in this feature's `plan.yaml`. That is exactly why `-01` called ESCALATE rather than
FAIL, and this run reaches the identical conclusion by the identical authority chain, now with the
BRIEF's own misstatement named explicitly as the artifact needing the edit (not `harness.json` alone
— someone also has to correct BRIEF.md:275-278's characterization, or a future reader inherits the
same false "soft skip" belief).

## Updated matrix table (supersedes `-01`'s table only by adding the `integration` row)

| Task | change_type | Required kind(s) | State |
|---|---|---|---|
| T-01 | logic | unit | satisfied |
| T-04 | logic | unit (floor); **integration (added, DEC-35)** | satisfied, both |
| T-02 | ai_behavior | eval | **BLOCKED** — `cmd: null`, `status: unresolved`, `detect` zero-match |
| T-05 | docs | none | n/a |
| T-06 | docs | none | n/a |

`matrix_ok: false` — the `eval` requirement is not satisfied and cannot be inferred as skipped.

## Everything else from `-01` stands unmodified

Phase-1 derivation, SC evidence pointers, the board/state INV-26 findings (#904/#906/#908/#909), and
the ESCALATE routing (owner `harness-dev-ops`, gap `harness.json test_kinds.eval`) are unchanged and
not re-derived here — see `qa-FEAT-37-2026-08-27-01.md`. This addendum's only net changes: (1)
`integration` added as a bound kind for T-04, run and green; (2) `-01`'s case_6b "unchanged by this
diff" claim corrected — it changed, and it does cover T-04's response-clause half.
