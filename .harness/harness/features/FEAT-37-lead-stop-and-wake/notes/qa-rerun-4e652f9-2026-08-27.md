# QA re-run at pinned `review_sha` 4e652f9 — FEAT-37 lead-stop-and-wake

**HEAD before: `4e652f9`. HEAD after: `4e652f9`.** No checkout/reset/commit made. Confirmed by
`git rev-parse --short HEAD` twice, bracketing the whole run.

## BLUF

**matrix_ok: true. Suite: pass. 0 failures across every command run.** All six `done` tasks
(T-01, T-02, T-04, T-05, T-06, T-09) are covered by a passing, non-vacuous test at this sha. The two
previously-unmeasured groups (`bound`, `coverage`) both discovered a non-zero case count and both
passed. The three prior claims in the dispatch are all **CONFIRMED**, not merely restated.

## The two never-measured groups — measured now

| Command | Exit | Cases discovered | Result |
|---|---|---|---|
| `test-lead-stop-and-wake.py --group bound` | 0 | 6 (`case_floor_DECISIONS.md`, 3× `case_occurrence_DECISIONS.md_*`, `case_floor_inflight_registry.py`, `case_occurrence_inflight_registry.py_339_1`) | ALL PASS |
| `test-lead-stop-and-wake.py --group coverage` | 0 | 3 (`case_index_row`, `case_entry_heading`, `case_entry_scope`) | ALL PASS |

Neither is a zero-discovery sweep — both counts are non-zero and named individually above, so the
`exit 0` is evidence, not a null-set artifact.

## Full suite, no `--group` filter

`python3 .claude/skills/harness/bin/test-lead-stop-and-wake.py` → **exit 0**, 19 cases total
(10 playbook + 6 bound + 3 coverage), **ALL PASS**.

## Three claims confirmed, not restated

1. **`--group playbook` → ALL PASS, 10 cases.** CONFIRMED verbatim: `case0_region_markers_present`
   through `case9_collect_on_waking`, exit 0, 10 named cases, all PASS.
2. **`--self-check` → variants A–F discriminate exactly as SC-03 specifies.** CONFIRMED: A/C/D/E/F
   report `expected_pass=False got_pass=False`, B reports `expected_pass=True got_pass=True`, exit 0.
   Note: the run also exercises a 7th, unlabelled `bound_only_unmatched` variant (an `--only` with no
   matching site) — that is additional coverage beyond A–F, not a contradiction of the A–F claim.
3. **`gen-decisions-index.py --stdout | diff - DECISIONS-INDEX.md` → exit 0.** CONFIRMED: ran fresh,
   `diff` printed nothing, exit 0.

## Full matrix re-run

- **unit** (`.agents/skills/harness/bin/run-unit-tests.sh --kind unit`): exit 0. **27 scripts
  discovered** (`grep -c '^PASS test-'`), all PASS, 0 failures. Includes `test-lead-stop-and-wake.py`
  (T-01's own guard) and confirms `--check-kinds` drift detection is silent (no `KIND-DRIFT` line).
- **integration** (`.agents/skills/harness/bin/run-unit-tests.sh --kind integration`): exit 0.
  **27 scripts discovered** (counted from the `INTEGRATION_SCRIPTS` array in
  `run-unit-tests.sh`), run completed to its final script `test-gh-close-gate.py`, banner
  `ALL PASSED`, `INTEGRATION_EXIT=0`. Includes `test-inflight-registry.py` (T-04),
  `test-gen-decisions-index.py` (T-05/T-06), and `test-validate-digest.py` (T-04's before/after gate
  check, per D-08).
  **Operational finding, not a code defect:** this command took ~7 minutes wall-clock in this
  sandbox because `test-gh-sync.py` makes a live `gh api rate_limit --jq .resources.graphql.used`
  call rather than a mocked one; it eventually resolved and the suite finished clean. Flag this for
  `dev-ops`/`test-gh-sync.py`'s owner as a flakiness/CI-time risk, not as a FAIL — the completed run's
  exit code and banner are the evidence, and I killed a second, redundant background invocation of
  the same command mid-flight after the first had already completed cleanly (that kill is what
  produced a stray "failed with exit code 1" background-task notification; it is an artifact of my
  own cleanup, not a suite failure).

## Discovered set binds all four logic/docs task commits

- T-01 (`logic`) → `test-lead-stop-and-wake.py`, registered in `UNIT_SCRIPTS`, discovered and PASS.
- T-04 (`logic`) → `test-inflight-registry.py` + `test-validate-digest.py`, both in
  `INTEGRATION_SCRIPTS`, discovered and PASS. Verified the corrected sentence directly at
  `inflight_registry.py:338-342` — carries "at most once per consecutive stop sequence", "re-fires on
  a later wake", and "end the turn again", matching REQ-06 and T-04's own verify.
- T-05 (`docs`) / T-06 (`docs`) → no dedicated unit/integration file, but bound to
  `test-lead-stop-and-wake.py`'s `bound`/`coverage` groups (PASS, above) plus
  `test-gen-decisions-index.py` (integration, PASS) for the index-regeneration check. Spot-checked
  `DECISIONS.md:6985` heading — "Neither an orchestrator nor a lead ever waits" — confirms SC-06's
  lead-tier heading requirement directly.
- T-02 (`ai_behavior`) → per the dispatch's ruling (DEC-70 narrowed by D-17/T-09), the `eval` matrix
  row does not reach a markdown-playbook change; not re-litigated. Its own gate,
  `test-lead-stop-and-wake.py --group playbook`, is the `logic`-shaped guard and is green.
- T-09 (`docs`) → `gen-decisions-index.py --stdout | diff -` exit 0 (item 3 above); its literal
  `verify:` clause calling `--check` is known-defective and already recorded — not reported as new.

## sc_evidence

| SC | Test |
|---|---|
| SC-01 | `test-lead-stop-and-wake.py --group playbook` cases 1–3 (`case1_stop_half`..`case3_halves_adjacent`) |
| SC-02 | same, cases 4–6 (`case4_refusal_expected`, `case5_stop_again`, `case6_refusal_recurs`) |
| SC-03 | `--self-check`, variants A–F, all as specified |
| SC-04 | `case7_overrides_tool_text` (mechanical floor only) + `harness-code-reviewer`'s inspection (not qa's) |
| SC-05 | inspection, not qa's — no automated test claimed |
| SC-06 | `--group coverage`, all 3 cases, plus `gen-decisions-index.py` diff clean |
| SC-07 | `--group bound`, both sites, all occurrences qualified |
| SC-08 | `not_met` by design (D-13) — post-merge UAT, not graded here |

## coverage_gaps

None found against the matrix as scoped by this feature's own rulings (eval narrowed away from T-02
per D-17; SC-04/SC-05 are inspection by design, not gaps). SC-08 is a declared, ruled `not_met` —
not a gap, a scheduled post-merge step.

## Findings

- **F-01 (info):** `test-gh-sync.py` makes a live network `gh api` call during the integration
  suite, adding several minutes of wall-clock and a dependency on external reachability/auth. Not
  part of this feature's diff; flagging for whoever owns CI time on that file.
