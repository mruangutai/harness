# Answers — FEAT-37 qa gate — 2026-08-27

Written by the main session after putting Q1 to the operator.

## Q1 — RULED: AUTHOR THE EVAL. Do not sign an exclusion.

The operator chose the eval over the exclusion. Your reading of the conflict was correct and I
re-derived all of it at disk before asking: DEC-70 at `DECISIONS.md:852` requires `eval` as a
required test kind for `change_type: ai_behavior`, with ai-dev authoring and qa gating.
`test_kinds.eval` reads `cmd: null, status: "unresolved"` — and **unresolved is not excluded**. The
`excluded` shape carries `excluded_because` and `signed`, which DEC-187 gave `functional` and nobody
gave `eval`. You were right to refuse the soft-skip reading.

**This is a plan change, so it comes back to me for signature before any of it is built.** The
sequence:

1. **pm drafts the amendment** — a new task for ai-dev to author the eval, and whatever the eval
   needs to be runnable. DEC-70 names what an eval is: failure modes, rubric, reference dataset,
   threshold. pm authors the task, not the eval itself.
2. **Return it to me.** I sign it. No build starts on an unsigned amendment.
3. **Then ai-dev authors, qa runs and gates, and validator-lead assesses eval adequacy** — DEC-70
   assigns all three roles explicitly.

**Name the runner problem in the amendment rather than assuming it away.** `test_kinds.eval.cmd` is
`null`, so even a perfect eval has nothing to execute it. Whether that is dev-ops work inside this
feature or a dependency on separate work is pm's call to state and mine to approve. `harness.json`
sits outside every current task's `files:` set either way.

## Q2 — CORRECT THE BRIEF, in the same pm pass. One revision, not two.

`BRIEF.md:275-278` calls the eval requirement "a soft skip that proves nothing". That is false, and
it is false in a signed document, which is the state PRINCIPLES rule 15 forbids. A signed BRIEF does
not override a signed decision, and you were right to surface it rather than take the reading that
let you proceed.

pm corrects it in the same pass as the amendment — I approve both together. Do not send this back to
me as a separate cycle.

## Q3 — Still MINE, still not yours to work around.

Six INV-26 violations, now including the parent. Unchanged from my last answer: `check-state.sh` is
enforcement-layer, DEC-174 makes the fix mine, and `gh-sync.py` exposing no single-card-to-Done
subcommand is the same defect widening. Treat them as known and expected. Do not edit
`check-state.sh`, do not move cards by hand, do not report BLOCKED on them.

## Three things from your return that I am recording, not re-opening

- **The run-dir collision that destroyed T-05's digest.** Separating T-06's artifacts and writing an
  honest loss note was right; reconstructing what was lost would have been worse. It stays a backlog
  row.
- **Both members who refused to write a falsehood the plan told them to write.** Endorsing them was
  right. A plan is not a licence to state something the tree contradicts.
- **SC-08.** Four independent corroborations now. It stays `not_met` and the goal-check says so
  plainly. Nobody inside the factory can close it; I run it after merge from the main checkout.

## Budget

Runs 13 of 20 at your return; **seven remain**. The amendment pass, the eval authoring, the qa
re-run, the panel, the goal-check and the docs sweep have to fit in that. If the amendment alone
looks like it will spend more than two, return and tell me before spending them.
