# FEAT-37 — lead stop-and-wake — ship review

## The decision in front of you

**The feature is done and verified. Seven of its eight success criteria are met by their own declared
methods; the eighth is the deferral you already signed, and it can only be run after merge.** Nothing
is blocking. What remains is your ship call, plus three decisions that are yours because they touch
signed artifacts.

**What it does.** A domain lead now carries a never-wait rule and its inoculation in the playbook it
preloads: end the turn on dispatch, the member's completion wakes you, expect the refusal, end the
turn again, and it can recur. It also contradicts the dispatch tool's own "continue other work in the
meantime" rather than sitting silently beside it.

## How this briefing was assembled

**No report round was spawned.** I read every run digest from disk, which is what `feature.json`
`runs:` is for. The paths, all under `.harness/harness/features/FEAT-37-lead-stop-and-wake/`:

- `runs/2026-08-24-01-product/digest.md`, `runs/2026-08-24-02-product/digest.md`,
  `runs/2026-08-24-03-product/digest.md`, `runs/2026-08-26-01-product/digest.md`,
  `runs/2026-08-26-02-product/digest.md` — planning, re-planning, rulings
- `runs/2026-08-24-01-eng/digest.md`, `runs/2026-08-24-02-eng/digest.md` — cause confirmed, bound measured
- `runs/2026-08-27-01-t01-eng/digest.md`, `runs/2026-08-27-01-t04-eng/digest.md`,
  `runs/t06-product/digest.md` — the build
- `runs/2026-08-27-01-product/digest.md` — **T-05's digest is LOST**; that file is a note saying so
- `runs/2026-08-27-01-validator/digest.md`, `runs/amend-eval-product/digest.md`,
  `runs/2026-08-27-02-t07-t08-eng/digest.md` — the eval attempt and its strike
- `runs/2026-08-27-01-t09-docs-product/digest.md` — T-09 plus the docs sweep
- `runs/qa-2026-08-27-03-validator/digest.md` — the qa gate
- `runs/panel-2026-08-27-validator/digest.md` — the review panel
- `runs/goalcheck-product/digest.md` — the goal-check

## Where each squad landed

**Product** (`runs/goalcheck-product/digest.md`) — PASS. Seven SCs met by declared method, SC-08 left
ungraded as the declared deferral. pm judged the DEC-70 preload gap a genuinely NEW criterion and
recommended a backlog row rather than a retro-added SC, on the ground that adding a criterion to a
signed BRIEF after the work is graded changes what was signed.

**Engineering** — the six tasks. T-01 shipped the guard test red-by-design; T-02 wrote the rule into
the playbook; T-04 corrected the refusal an agent actually reads; T-05 and T-06 bound the rule to the
lead tier in DEC-201 and corrected DEC-199; T-09 narrowed DEC-70.

**Validation** (`runs/qa-2026-08-27-03-validator/digest.md`, `runs/panel-2026-08-27-validator/digest.md`)
— both PASS. Matrix green at `4e652f9`. The panel closed the only two criteria with no independent
evidence, and reported honestly that three PASSes cannot distinguish a clean diff from a shallow
panel, since only one member ran a falsification check.

## Success criteria

| SC | Verdict | How |
|---|---|---|
| SC-01 | met | automated — playbook group, exit 0 |
| SC-02 | met | automated — all three inoculation clauses separately asserted |
| SC-03 | met | automated — self-check, six fixtures discriminating both directions |
| SC-04 | met | inspection — `harness-team/SKILL.md:119-120`, one sentence names the tool's nudge and denies it |
| SC-05 | met | inspection — anchors graded at `:81`, `:126`, `:196`; sweep proven non-vacuous |
| SC-06 | met | automated — coverage group plus index drift check, exit 0 |
| SC-07 | met | automated — 6 cases, **both floor assertions firing** |
| SC-08 | **not_met** | uat — the declared post-merge deferral. **Yours to run, from the main checkout, after merge.** |

**SC-08 is the only criterion that measures conduct, and I am not softening that.** Everything else
grades text on disk. This build produced first-hand proof of why it cannot be graded here: the stop
guard refused me repeatedly saying *"this refusal fires ONCE"* — the **main checkout's**
`inflight_registry.py:274` — while the branch's corrected text at `:339` reads *"at most once per
consecutive stop sequence"*. Every agent in this build read the unedited playbook.

## Three decisions that are yours

1. **The DEC-70 narrowing never reached the surfaces an agent preloads.** It landed in `DECISIONS.md`,
   `DECISIONS-INDEX.md` and `SPEC.md`, but `.claude/skills/harness-qa-gate/SKILL.md:40` still reads
   `ai_behavior | prompts, model calls, agent definitions, tool definitions` with no playbook
   carve-out, and `.claude/agents/harness-ai-dev.md:38,41` and `harness.json test_matrix` are
   unqualified. I confirmed all three by grep. **The next feature touching a playbook re-runs this
   entire blockage.** pm and validator-lead reached the same recommendation independently: backlog
   row, not a retro-added SC.
2. **T-09's signed `verify:` block can never grade green.** It calls `gen-decisions-index.py --check`,
   a flag that has never existed — the docstring says so and it exits 2. The property it meant to
   gate is gated by SC-06, which names the correct form and exits 0. Recommend amending the clause.
3. **SC-05's line citations in `BRIEF.md` are stale** — `:112` is now `:126`, `:181` is now `:196`.
   Confirmed by three independent measurements. `BRIEF.md` is approval-gated and was not edited.

## Proposed backlog — anything not listed dies silently

| ID | Nature | Finding |
|---|---|---|
| B-1 | bug | A run-dir slug collision overwrote **T-05's digest**; the runs directory is gitignored so there is no copy. Nothing prevented it. |
| B-2 | chore | SC-05's line citations in the signed `BRIEF.md` are stale. Needs an operator-side correction. |
| B-3 | bug | DEC-70's narrowing did not reach the three surfaces an agent preloads at classification time. **The highest-value row here.** |
| B-4 | bug | T-09's signed `verify:` calls a flag that never existed. **Nothing validates that a `verify:` block is runnable before signature.** |
| B-5 | bug | SC-07 enumerated two once-only sites; a third existed in `SPEC.md`. Caught by a sweep's diligence, not by any gate. |
| B-6 | chore | The panel's reviewer-artifact naming convention is denied by the write guard, which permits only the agent-name form. |
| B-7 | chore | The feature's diff base is undocumented and was derived wrong once. **I tested recording it in `feature.json`: the validator refuses an undeclared key**, so this needs a schema change first. |
| B-8 | chore | The integration suite takes ~7 minutes. **qa attributed this to `test-gh-sync.py` making a live rate-limit API call; I checked and that file stubs the `gh` binary entirely.** Real slowness, wrong cause, needs diagnosis. |
| B-9 | bug | The in-flight registry is a single file at the **outer** checkout root, shared by every worktree and context, so a lead was refused naming a `harness-pm` it never spawned. |
| B-10 | enhancement | A lead holds no `SendMessage`, so a finding made after dispatch cannot reach a member in flight. D-03's deliberate consequence. |
| B-11 | bug | Engineer DIGESTs carry no `files_touched`, so a member that wrote a receipt reported no files. |
| B-12 | chore | The root-cause note path is in no member's domain, so debug reports fall back to receipt paths. |
| B-13 | chore | T-05 and T-06 have no dedicated test file; their assurance rests entirely on two groups of one script. |
| B-14 | chore | `bash-write-guard.sh` parses heredoc **body text** as shell tokens: writing this briefing was blocked because a filename pattern appeared inside a prose table cell. |

Already filed, not new rows: **#910** (route checker validates against the wrong config; no script
reads the `gates` block) and **#903** (the struck orchestrator-tier regression).

## Budget, and one thing I did not do

**Runs 19/20. Cycles 1/10** — and that single cycle predates this session; it came from the T-01 build
run. Every segment this session returned **zero send-backs**. You budgeted five runs for five
segments with zero spare; combining T-09 with the docs sweep into one documentor dispatch made it
four segments in four runs, so **run 20 is unspent**.

**SIMPLIFY was not run, and the reason is structural rather than a preference.** It must run before
`review_sha` is pinned, because an apply commit after the pin invalidates the panel's verdict. The
panel has graded at `4e652f9`, so running it now costs the panel run as well as its own — two runs
against a budget holding one. Either accept the omission or re-open it as a follow-up with a fresh
pin and a fresh panel. It is no longer a one-run decision.

**No PR and no merge** — both yours, as instructed. The branch is `feat/FEAT-37-lead-stop-and-wake`,
the state gate is clean for this feature, and all seven INV-26 card violations cleared on the Review
station write without a card being touched by hand.
