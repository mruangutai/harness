# Observations - harness-orchestrator

- 2026-08-25: FEAT-37. The #551 single-flight refusal listed TWO harness-product-lead children when I had dispatched one. Cause: `.harness/.inflight-claims.json` is keyed per CHECKOUT by the spawning session's cwd, and my session's cwd was the FEAT-34 worktree even though my feature was cut into its own FEAT-37 worktree. Both entries carried `cwd: .../FEAT-34-worktree-act3-enforced`; the 01:44:04 one was the OTHER live orchestrator's lead, mine was 01:49:54. The refusal named a sibling's child as if it were mine. The discriminator that settled it: read the registry and compare `started_at` against my own dispatch time, rather than assuming a duplicate dispatch.
- 2026-08-25: FEAT-37. `mkdir -p` sharing a command with a heredoc redirect the write-guard denies never runs at all — the guard blocks the WHOLE Bash call. A later `observations-merge.py` then failed on a missing `.lock` because its parent dir was never created. Put directory creation in its own call, with literal absolute paths (the guard cannot expand a shell variable and reads `$D/...` as outside my domain).
- 2026-08-25: FEAT-37. An orchestrator holds NO `SendMessage` (tools are Read/Glob/Grep/Agent/Write/Bash), so a scope amendment arriving while a lead is in flight CANNOT be relayed to that lead — its run completes against the superseded scope and must be re-scoped on return. Consequence for sequencing: when an amendment is foreseeable, prefer starting the SLOWEST INDEPENDENT segment first and holding the scope-sensitive one, because the independent segment survives an amendment and the scope-sensitive one does not.
- 2026-08-25: FEAT-37. `.inflight-claims.json` is a poor liveness signal for a LEAD: `validate-digest.py` releases the agent's own claim at STEP ONE and only then applies the #551 return contract at STEP TWO, so a lead that attempted a return and was refused is still running while absent from the registry. Read a disappearance as "attempted to return", never as "finished".
- 2026-08-25: FEAT-37. Before passing another agent's cited line numbers into a dispatch as established, open the file and count the lines. Here all four of the operator's citations in validate-digest.py held exactly, which converted the dispatch from "the operator claims" to "confirmed at 9165162" — and that difference is what stops the specialist re-deriving what is already settled.
- 2026-08-25: FEAT-37, FIRST-HAND. The #551 refusal fired TWICE on me (one agent, a36c5954df4f6303d) across substantial intervening work: once with product-lead in flight (~01:51), once with eng-lead in flight (02:00), with a return that SHIPPED in between. So `validate-digest.py:903`'s "Fires AT MOST ONCE per return" is literally true PER RETURN and misleading PER AGENT — every new turn-end is refused afresh. The refusal did not trap me, because the orchestrator playbook carries an explicit inoculation ("the single-flight refusal on your return is EXPECTED ... NEVER as 'you may not return yet'"). `harness-team/SKILL.md` carries no such paragraph. The two candidate causes therefore COMPOUND rather than compete: the hook refuses the correct act, and the lead's playbook never tells it the refusal is expected.
- 2026-08-25: FEAT-37, IMPORTANT. My product-lead REPRODUCED the loop's trigger first-hand (dispatched pm, ended its turn, was refused by the #551 hook) and then WITHDREW that correct observation because it read the hook's own COMMENT at validate-digest.py:903-908 asserting "Fires AT MOST ONCE" and "a lead cannot be made to wait". It substituted the code's SELF-DESCRIPTION for behaviour, on the one claim under test, and concluded "compatible as built". My own measurement — two refusals across intervening work — contradicts the comment. LESSON: when a dispatch says a comment is the claim under test, a member reading that comment as evidence is the predictable failure; the dispatch must say "do not let this comment settle it" in those words. It also shows a lead can hit the refusal WITHOUT looping (it investigated instead), so the refusal is at most necessary-not-sufficient for #831's symptom.
- 2026-08-25: FEAT-37. The main-session-direct lane has TWO INDEPENDENT SOURCES and they must not be conflated in a plan's D-NN. `check-domain.sh --resolve .claude/skills/harness-team/SKILL.md` returns NOBODY, so the GUARD forces the lane. `--resolve .claude/skills/harness/bin/validate-digest.py` returns `harness-backend-dev harness-dev-ops` — the guard PERMITS a squad write, and only DEC-174's carve-out (enumeration at DECISIONS.md:4709, am.4 at :4854-4877) forbids it. So a main-session-direct task on validate-digest.py makes `check-plan-routes.py` print DEVIATION, and the D-NN must cite the POLICY carve-out, never a NOBODY resolve — writing "check-domain says NOBODY" there would be falsifiable and would invite a later reader to "fix" the lane back to a squad.
- 2026-08-25: FEAT-37, LIVE Q1 SPECIMEN forming on me (`a36c5954df4f6303d`). Stop attempt #1 at ~02:13Z was REFUSED with child `harness-eng-lead` (started 02:11:39.78Z) in flight. I then VERIFIED the same child was STILL LIVE at 02:13:17Z (claim still held in `.inflight-claims.json`, run dir `2026-08-24-02-eng` present with no digest) and made stop attempt #2. This is the specimen eng's Q1 asks for and the one `aa4bb05730add8058` could not supply, because there the child had already returned and the stop passed at `validate-digest.py:917` on an empty child set. Refused again => CONTRADICTS "fires at most once"; ships => CONFIRMS it. KEY DISCIPLINE: the second return is an act I owe anyway (ending my turn is the rule), so observing its outcome costs nothing and is NOT filler — manufacturing extra work to "improve" the test would have been the very bug under investigation.
- 2026-08-25: FEAT-37. The operator wrote "apply Q1, Q2 and Q3 — they are narrow and you already hold the context", which reads as an instruction for ME to edit plan.yaml. It is not mine to do: the authority boundary says plan-level changes are pm's, and narrowness does not change ownership. I delegated, and the delegation EARNED ITS KEEP — Q2's three DEC-199 copies sit in three DIFFERENT lanes (DECISIONS.md resolves to documentor/team; inflight_registry.py to backend-dev/team; harness/SKILL.md to NOBODY, hence main-session-direct). So "correct all three copies" is a plan-structure question about whether a task exists that can reach each one, not a text edit. Hand-patching would have silently produced a plan with an uncoverable correction.
- 2026-08-25: FEAT-37. When a plan strike removes ids (T-07/T-08/T-09, REQ-08, SC-09), the predictable defect is a DANGLING REFERENCE from a surviving task or SC. Instruct a grep for each struck id as part of the strike, not as a review afterwards. Reusable corollary: a gate's output should CHANGE in a PREDICTED direction after a strike — here the T-08 DEVIATION must disappear — so name the expected gate delta in the dispatch and treat its absence as evidence the strike was incomplete.
- 2026-08-25: FEAT-37. `bash-write-guard.sh` scans heredoc BODY text for redirect operators: an observation containing the two characters of an ASCII arrow was refused as a `redirect` targeting the words after it. Avoid arrows and stray angle brackets inside heredoc prose, or the write is blocked with a message that looks like a domain violation rather than a parse artifact.
- 2026-08-25: FEAT-37. SINGLE-FLIGHT IS A GLOBAL PER-PERSONA BOTTLENECK ACROSS CONCURRENT FEATURES, not a per-flow guard. A `harness-pm` claim with `cwd: /Users/molchairuangutai/GitHub/harness` (the MAIN checkout) blocked my product-lead's pm dispatch in the FEAT-37 worktree, because the registry keys claims by PERSONA NAME only. With three features live, exactly ONE pm can run in the whole factory at a time, and a lead cannot tell a foreign claim from its own. The discriminator that settles ownership is the claim's `cwd` field compared against your own children's `cwd`, plus `started_at` age — read those before ever considering a release. NEVER blind-release: `release_all` drops every persona's claim and re-opens the #551 write hazard for the other live flows.
- 2026-08-25: FEAT-37. A lead reporting "children in flight" may be reporting a claim it never created: a refused dispatch never reaches `claim()`, so the guard's own refusal cannot have registered anything. When a lead returns BLOCKED citing an in-flight child, verify ownership from the registry yourself rather than accepting or dismissing it — mine was genuinely foreign, and both the credulous and the dismissive reading would have been wrong.
- 2026-08-27: B1b is live, measured on my own return. Ending my turn with harness-product-lead in
  flight took `check-digest: BLOCKED - returned with children in flight (harness-orchestrator)`.
  The refusal keys on HAVING CHILDREN, not on SINGLE_FLIGHT_AGENTS (`inflight_registry.py:32` is
  `("harness-pm",)`), so it is the orchestrator's routine path — and `c5e59aa` deleted the clause
  at `.claude/skills/harness/SKILL.md` that told the orchestrator the refusal is EXPECTED. Only
  the surviving "There is no waiting anywhere in this loop" (:60) kept me from reading it as "you
  may not return yet". An orchestrator without that clause reinstalls the wait loop this feature
  removes at the tier below.
- 2026-08-27: FEAT-37 build run, direct runtime evidence for the bound T-06 corrects. The
  SubagentStop children-in-flight refusal fired at MY tier on THREE separate stop sequences in one
  session (after the T-01 dispatch, after the T-04 dispatch, and once more), each time printing
  "this refusal fires ONCE; a second identical return will ship". It is not once per session; it is
  once per consecutive stop sequence and it re-fires on every later wake while a child is live. The
  eng lead independently reported the same recurrence twice at ITS tier in the t01-eng run. These
  are fresh #551 occurrences beyond the eight DEC-199 records; DEC-199 warns its count is a FLOOR
  and that writing a bare total there becomes a false statement nothing detects, so this is logged
  as occurrences, never as a new total.
- 2026-08-27: FEAT-37, the shape of a gate that cannot see its own mandate. D-11 and T-06 both
  mandate correcting DECISIONS.md:6872, but T-01's approved once-only alternation spells only "a
  second identical return will ship" while 6872 reads "a second identical return ships". T-06's
  verify would therefore have exited 0 whether or not the mandated correction happened. Found by
  grepping each site's literal wording rather than trusting that one detector covers all sites of
  "the same" claim - the two sites were worded differently by different authors.
