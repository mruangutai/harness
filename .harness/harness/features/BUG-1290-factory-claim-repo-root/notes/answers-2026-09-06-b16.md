# Operator answers — BUG-1290 — 2026-09-06

## Ship review decision

- Q1 / B-16 — **resolve B-16 with the committed test.** Add the permanent mutation/regression test that makes the issue-map cache-key proof self-defending: it must fail case `5b` when the issue-map cache is mutated to discard the repository key, so deleting the fixture dependency cannot silently return the suite to the pre-B-3 blind state.

No decision was given on other backlog rows or the optional REQ-05 wording correction; leave approved artifacts unchanged unless a required replan makes it necessary.
