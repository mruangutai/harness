# Operator answers — BUG-1290 — 2026-09-06

## Ship review decision

- Q3 / B-3 — **fix B-3 first.** Add the missing regression fixture proving `_BlockerCache`'s issue-map cache is keyed by `(repo, feature)`, then re-run the affected build/QA/validation/goal-check path before returning to the ship decision.

No decision was given on the optional REQ-05 wording correction; leave approved artifacts unchanged unless a required replan makes it necessary.
