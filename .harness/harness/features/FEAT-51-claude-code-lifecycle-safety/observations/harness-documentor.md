# Observations - harness-documentor

- 2026-09-01: T-06. My first DECISIONS.md append landed in the MAIN checkout, not the FEAT-51 worktree: I passed a repo-relative path to the edit tool and the process cwd was the main checkout. The two copies were byte-identical, so the content-derived snapshot tag (#EDFB) matched in BOTH files and nothing in the edit response flagged the wrong tree. Only the generator writing no DEC-210 row exposed it. Absolute paths for every write when a worktree is in play; and `git status` in both trees is the cheap detector.
- 2026-09-01: T-06. The dispatch said "every later row anchor shifts when your entry lands, which is why regeneration is part of this task" — false for an entry appended LAST: nothing follows it, so the index diff was a single added line. Regeneration was still required, but for the row itself, not for anchor drift.
- 2026-09-01: T-06. DEC-174's index-row ruling ("the harness plans its own work but never EXECUTES changes to its own hooks, validators or gate scripts") is NOT about route-completeness, which is how DEC-208's body loosely cites it alongside DEC-151. I had drafted a DEC-174 sentence on route-completeness and it would have been false. Reading the cited entry's body, not its neighbour's citation of it, is what caught it.
