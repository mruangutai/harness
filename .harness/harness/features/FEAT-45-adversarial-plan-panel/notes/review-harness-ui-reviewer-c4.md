# UI review — c4 (FINAL) — FEAT-45-adversarial-plan-panel

Pin: `bdd566679377eb5a55d1092064fe444e86d2f49f`. Scope: the B-1 delta
(`git diff 302ae9d bdd5666`) plus its regression impact on operator-facing output.

## Census — `git diff --stat 302ae9d bdd5666`

5 files, 10 insertions / 11 deletions:

| file | kind | rendered UI surface? |
|---|---|---|
| `.claude/skills/harness/bin/panel_findings.py` | source (`finding_id()` widened `[:8]`→`[:32]`) | no |
| `.claude/skills/harness/bin/test-panel-findings.py` | test (length/charset assertions updated 11→35, 8→32 hex) | no |
| `.../notes/ship-review-2026-08-31.html` | **HTML**, but generated — verified `<!-- ... -->` equivalent footer text at the bottom: *"Derived from `ship-review-2026-08-31.md` — the markdown is the record; do not edit this file. Regenerate with `bin/render-brief.py`."* Per repo Expertise G-11, a regenerated ship-review report is not product UI. | no (generated report) |
| `.../notes/ship-review-2026-08-31.md` | markdown, the backlog table's B-1 row (32-bit-id ratchet warning) replaced by a B-2 row (unrelated bug) — the finding this feature itself surfaced got resolved and dropped off its own backlog | no (not a design contract, no PF- id appears in it — grepped, zero hits) |
| `.../plan.yaml` | decision/plan record, D-05 wording updated | no |

**Census verdict: the delta itself renders nothing.** No `.html/.css/.scss/.tsx/.jsx/.vue/.svelte` product surface, and the one `.html` match is a confirmed generated artifact with a do-not-edit footer.

## In-lane surface named by dispatch: INV-32 operator output

The id now appears in `check-state.sh`'s INV-32 messages (`INV-32: {feat} finding {fid} is ...`,
`STALE OVERRIDE {fid}`, ruling-attribution lines) — built at `check-state.sh:182-241` and printed
at `check-state.sh:1868-1869`:
```
for m in bad:  print(f"  VIOLATION  {m}")
for m in warn: print(f"  note       {m}")
```

**1. Readability — fine, with evidence.** Every `fid` interpolation (`check-state.sh:200,202,209,219`)
is a plain f-string, no width formatting, no column alignment keyed to id length — the `  VIOLATION  `
/ `  note       ` prefixes are fixed-width regardless of message length, so a longer id doesn't skew
alignment with neighboring lines; it just makes that one line longer. Reproduced a real id and built
the message shape it would appear in:
```
$ python3 .claude/skills/harness/bin/panel_findings.py id --reader scope --summary 'T-04 traces REQ-99, which does not exist'
PF-87ea6603a72829829a3983c3c766f884
```
Full VIOLATION line = 150 chars (was ~126 at 11-char id) — longer, wraps naturally in a terminal at
normal widths, same as several pre-existing INV-26/INV-27 messages in this file that already run
150-250+ chars. No degradation in kind, only in length. No table/columnar renderer touches this path.

**2. Truncation search — ran, found none.** Grepped every file that references the id mechanism at
all (`grep -rl "PF-\|finding_id\|panel_findings"` across `.claude/skills/harness/bin/*.py` and
`*.sh`): only `panel_findings.py`, `check-state.sh`, `test-panel-findings.py`, `test-check-state.py`
touch it — no renderer, no `gh_board.py`, no ship-review generator is in the id's path. Then searched
those + the full bin/ tree for truncation idioms — `[:8]`, `[:11]`, `[:32]`, `[:35]`, `cut -c`,
`.ljust(`, `.rjust(`, `.center(`, `textwrap`, `…`, `...` — **zero hits** outside the intentional
`digest[:32]` in `finding_id()` itself (the identity computation, not a display truncation) and the
updated test assertions. No column width, no ellipsis, no formatter narrows the 35-char id anywhere
on the operator-output path.

## Corroborating the lead's pre-reads (not new findings, verified directly)

- `panel_findings.py:29,33` — **not a mismatch.** Docstring: *"PF- followed by the first 32
  characters ... Length 35"* — that's `3 (PF-) + 32 = 35`, and the code (`digest[:32]`) agrees.
  Verified with a live id above (35 chars). No defect here.
- Feature `plan.yaml` T-09's `verify:` clause (line ~1019, `test 11 -eq "${#A}"`) and its `intent:`
  text (~1030, ~1052: "first 8 characters" / "length 11") are confirmed stale against the shipped
  32-hex code — running that literal verify clause against current code fails, actual length 35.
  Lines 473 and 641 carry the same stale "8-hex"/"never typed" phrasing; lines 157 and 773 were
  already updated to 32-hex/35-char. This is a **plan.yaml self-consistency gap** — a document
  scattered-edited so one restatement (D-05, line 157) was updated and its siblings (T-09's own
  verify/intent, line 473, line 641) were not swept. This is corroborated fact, but it is a spec/plan
  consistency question, not an operator-facing UI defect — **routing to code-reviewer/PM lens**, not
  filed here as must_fix (repo Expertise pattern: a stale plan restatement is a plan-change question,
  not this role's remedy to write).
- `.claude/skills/harness/templates/plan.yaml:56` (`id: PF-0123abcd`, 8 hex) — confirmed **untouched**
  by this feature's diff (`git diff main...bdd5666` does not list `templates/plan.yaml`). Stale
  relative to the new format, but out of this review's scope: no line of this feature's contribution
  edited it.

## Verdict

No display truncation exists anywhere on the operator-output path; INV-32 output remains legible at
35 characters. No UI defect. The one real gap found (plan.yaml T-09 self-inconsistency) is real but
outside this role's remit and is surfaced as an open question for the appropriate lens, not a
must_fix here.

```yaml
VERDICT: PASS
DIGEST:
  headline: "No rendered UI in the B-1 delta (generated ship-review.html excluded); INV-32's 35-char id is legible and no display truncation exists anywhere on the operator-output path — searched exhaustively, zero hits."
  mode: B
  in_scope: true
  severity_max: none
  findings: 0
  must_fix: []
  states_unspecified: []
  contract_violations: []
  a11y: ["not applicable — batch/CLI text output, no colour-only state encoding, no rendered surface in the delta"]
  open_questions:
    - { id: Q1, question: "plan.yaml T-09's own verify clause (line ~1019, `test 11 -eq \"${#A}\"`) and intent text (lines 473, 641, 1030, 1052) are stale against the shipped 32-hex/35-char format, while D-05 (line 157) and line 773 were updated. Confirmed by direct read/execution of the clause — should code-reviewer or PM sweep the remaining stale restatements in plan.yaml?", blocking: false }
  files_touched: []
  expertise_update: []
artifact: .harness/harness/features/FEAT-45-adversarial-plan-panel/notes/review-harness-ui-reviewer-c4.md
```
