# Receipt — T-01 — harness-documentor — record two carve-out decisions

**BLUF.** DEC-205 and DEC-206 are appended and both index rulings are hand-written and
regeneration-stable. T-01's `verify:` block, run verbatim, exits **1** — and it cannot exit 0 while
this change is uncommitted, because clause 2 is `git diff --quiet -- .harness/harness/docs/DECISIONS-INDEX.md`
and the change necessarily dirties that file. Six of the seven clauses pass. The orchestrator, which
holds the commit pen, must re-run the block after committing; nothing in the documents needs changing.

## What landed

- `.harness/harness/docs/DECISIONS.md` +49 lines, appended at end of file (no renumbering, every
  existing `@line` anchor stable):
  - **DEC-205** — "A harness lead may wrap a non-harness panel reader, and owns its shape but never
    its content". Carries the literal `wrapped non-harness reader`; the three precedent facts
    (unvalidated return, lead-stop-event-only coverage, contractual `unrated` sentinel transcribed
    not invented); the SHAPE/CONTENT line stated explicitly; `unrated` gating-equivalent to `high`.
  - **DEC-206** — "A gate may grade a specification before any code exists, and its findings enter
    the one batched review pass". Carries the literal `plan-phase gate`; spec-not-diff, no
    `review_sha`, no suite, evidence is a reader's judgement in a lead digest; DEC-176's ONE batched
    pass with no separate pre-signature fix dispatch; high-or-worse withholds the presentation until
    resolved or overruled; the operator is the terminus.
  - Both cite `FEAT-45-adversarial-plan-panel` as origin.
  - Shape copied from DEC-204: `## DEC-NNN — <title>`, blank line, `**Chose:**` opener, then
    bold-lead-in paragraphs. No sibling field DEC-204 lacks was invented.
- `.harness/harness/docs/DECISIONS-INDEX.md` +2 rows, generated by
  `.claude/skills/harness/bin/gen-decisions-index.py`, with the text after ` :: ` written by hand
  (the generator emits `⚠ RULING PENDING` there). Each ruling carries its entry's literal phrase and
  is one sentence inside the 30-word cap asserted in `test-gen-decisions-index.py:446`.

Nothing else was touched: `git diff --numstat` is exactly `2 0 …DECISIONS-INDEX.md` and
`49 0 …DECISIONS.md`. Tree left uncommitted.

## Evidence, clause by clause (worktree, HEAD `fe9f95a`)

| clause | exit | note |
|---|---|---|
| 1 `gen-decisions-index.py` | 0 | |
| 2 `git diff --quiet -- DECISIONS-INDEX.md` | **1** | the index is legitimately modified and uncommitted |
| 3 `test-gen-decisions-index.py` | 0 | 10 `ok -`, zero `FAIL` |
| 4–5 `grep` both phrases in DECISIONS.md | 0, 0 | |
| 6–7 `grep` both phrases in DECISIONS-INDEX.md | 0, 0 | run **after** clause 1's regeneration |

Baseline before any edit: clause 1 exited 0, clause 2 exited 0, both DECISIONS.md greps found 0
occurrences — so the work had not already landed and the index was regeneration-clean at spawn.

**Regeneration stability of the hand-written rulings is proven, not assumed.** md5 of the index
before a regeneration and after it are both `3d6bfe75ddc081f18f1c76b91bf433ac`; clauses 6 and 7 also
pass after clause 1 re-runs. The generator preserves the ruling text. No wipe occurred.

## The finding on clause 2

Clause 2 is a **post-commit** assertion living in a pre-commit `verify:` block. Its intent — the
committed index matches a fresh regeneration — is already covered by clause 3's
`test_committed_index_matches_a_fresh_regeneration`, which passes. For any task that appends a
decision, clause 2 is unsatisfiable by construction while the dispatch forbids committing. Either
run the block after the commit, or replace clause 2 with an idempotence check that does not consult
the index (regenerate to a temp copy and `cmp`).

## Open questions

- Q1 (blocking false): re-run T-01's `verify:` verbatim after the commit and record the exit there;
  or amend clause 2 in `plan.yaml` — an amendment I have no authority to make on an approved plan.
