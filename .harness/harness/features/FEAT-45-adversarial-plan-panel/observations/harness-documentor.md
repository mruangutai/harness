# Observations - harness-documentor

- 2026-08-31: FEAT-45 T-01 — a verify block that appends to DECISIONS.md carried `git diff --quiet -- DECISIONS-INDEX.md` as clause 2; it is a POST-COMMIT assertion and cannot pass in a dispatch mandated to leave the tree dirty. Six of seven clauses green; the intent is already covered by test-gen-decisions-index.py's test_committed_index_matches_a_fresh_regeneration. Check a verify block for git-state clauses before accepting its exit code as a verdict on your prose.
- 2026-08-31: FEAT-45 T-01 fix. Picked the "next two free DEC numbers" from inside a branch cut before main gained DEC-205 (PR #1032), so 205/206 were taken; correct answer was 206/207. Lesson: read the DEC ceiling from `git show main:.harness/harness/docs/DECISIONS.md`, never from the branch file. Renumber high-number-first so the two edits cannot alias; hand-rename the index rows before regenerating, because gen-decisions-index.py refuses to write while an orphan ruling row survives (G-06).
