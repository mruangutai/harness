# Ship review — FEAT-50-run-artifact-integrity

> **Correction, 2026-09-01, after this document was posted to issue #1082.** Two claims below were
> wrong when posted and are corrected in place, with the original wording quoted so the record
> shows what changed rather than hiding it. Both concern `check-state.sh`, and both are marked
> **[CORRECTED]**. The comment on #1082 carries the pre-correction text; this file is the record.

**The feature is delivered and already on `main`.** Pull request **#1105** ("fix: preserve feature
run artifact integrity") merged at **`75bf0901`**; the branch tip `9f50ee66` is an ancestor of
`origin/main`. This document closes the record: it is written *after* the merge, as a late
lifecycle closeout, not as a request to ship something.

**Recommendation: accept the closeout, land the two-line finalization on `main`, and file the
backlog rows you do not strike.** Nothing about the *code* gates. The final validation panel
returned PASS with an empty `must_fix`, `severity_max: med`, and `gates.review` is
`advisory_unless_high`. Eighteen residual findings survived collation; none blocks, and each is
listed below so none dies silently.

## Disclosure — how this briefing was assembled

**No report round was spawned.** Per DEC-69 the digests were read from disk rather than
re-narrated by a fresh agent. The sources are:

- `runs/2026-08-31-01-product/digest.md` — plan readiness
- `runs/2026-08-31-1-eng/digest.md` — first build attempt (FAIL)
- `runs/2026-08-31-3-product/`, `-4-product/`, `-5-product/`, `-7-product/digest.md` — plan
  amendment, operator rulings, plan goal-check, panel transcription
- `runs/2026-08-31-6-validator/digest.md` — plan panel, cycle 1
- `runs/t08-eng/digest.md` — T-08, the one team-mode task
- `runs/2026-09-01-1-validator/digest.md` — the final code panel (PASS)
- `notes/qa-feat50-pinned-review.md`, `notes/qa-feat50-recheck.md` — the per-SC and test-matrix
  evidence
- `notes/review-harness-code-reviewer-feat50-{pinned,recheck}.md`,
  `notes/review-harness-security-reviewer-feat50-{pinned,recheck}.md`,
  `notes/review-harness-ui-reviewer-feat50-{pinned,recheck}.md`
- `notes/answers-2026-08-31-plan.md` — the operator's INV-32 ruling
- `plan.yaml` `panel:` — the eleven transcribed plan-panel findings

Two things this briefing does **not** rest on, stated so you can price it: there is **no post-build
`harness-pm` goal-check run** on disk (see "Goal-check" below), and the closeout ran under an
explicit instruction to skip project-wide suites, so the only measurement taken first-hand here is
`check-state.sh`.

## What was built

Four defects in how the harness records and locates a run's own artifacts, closed as one feature:

1. **#1056** — `validate-digest.py` could not tell a *present-and-empty* `last_assistant_message`
   from an *absent* one, so an agent that returned nothing passed the gate. Now exit 2 for empty,
   exit 0 with a stated reason for absent/null.
2. **#1057** — a governed agent could write a feature artifact into the **main checkout** while its
   feature had a registered worktree. Now bound on both write routes: the `PreToolUse` Write route
   (`check-domain.sh`) and the Bash route (`bash-write-guard.sh`), each naming the target and the
   checkout the write belonged in.
3. **#1058** — a `Write` to an existing `runs/<runid>/digest.md` could destroy the recorded digest.
   Now refused unless the payload carries the existing text as a prefix; an unreadable file is
   treated as "recorded", not as "absent".
4. **(operator-authorized, no issue)** — a lead's digest was resolved against the owner root rather
   than the checkout the lead ran in, so DEC-156 could not see a contract-violating digest written
   inside a worktree. Now resolved through `inflight_registry.feature_root`.

Supporting seam: `harness_boundary.worktree_for_feature` selects a feature's worktree **by prefix**
and raises `AmbiguousWorktree` on two candidates; `inflight_registry.feature_root` was cut over to
it with its signature, return type and total-fallback contract intact.

The rules are on the permanent record as one decision in `.harness/harness/docs/DECISIONS.md`
("A run's own record is enforced…"), with `DECISIONS-INDEX.md` regenerated byte-identical.

## Verdicts

| Gate | Result | Source |
|---|---|---|
| Plan panel (cycle 1) | PASS, `severity_max: med`, no high/critical/unrated | `runs/2026-08-31-6-validator/digest.md` |
| Code panel (final) | **PASS**, `must_fix: []`, `severity_max: med` | `runs/2026-09-01-1-validator/digest.md` |
| qa test matrix (blocking gate) | **PASS**, `matrix_ok: true` | `notes/qa-feat50-recheck.md` |
| Security re-check | PASS — all three prior findings closed | `notes/review-harness-security-reviewer-feat50-recheck.md` |
| Code re-check | PASS — both formerly grade-1 functions now grade 4 | `notes/review-harness-code-reviewer-feat50-recheck.md` |
| UI re-check | PASS — 0 of 37 changed files carry a rendered surface | `notes/review-harness-ui-reviewer-feat50-recheck.md` |
| SIMPLIFY (four angles) | ran — `notes/receipt-harness-{backend-dev,dev-ops}-feat50-simplify-*.md` | build phase |

Three findings gated during the cycle and **all three are closed by fix, not by overrule**:

1. HIGH — `bash-write-guard.sh` skipped `feature_checkout_guard` on the `shared` outcome, so a
   feature-scoped shared path could be written into the main checkout via `echo hi >` at exit 0
   while `Write` refused at exit 2. Closed at `bash-write-guard.sh:784-785`, with a scoped mutation
   built **outside** the shipped suite proving the line load-bearing.
2. HIGH ×2 — `code_grade` 1 on `run_feat50_checkout_binding` and `run_feat50_artifact_integrity`.
   Re-graded 11/9/59.6 → 1/0/16.1 and 17/14/85.8 → 1/0/20.0, both grade 4, with case-name sets
   confirmed **supersets** (5→6, 9→10) so nothing was dropped to buy the grade.
3. MED — the digest-clobber guard read `OSError` as "file absent" and allowed the clobber. Closed
   at `check-domain.sh:1142-1155`.

## Goal-check — read this before accepting

**There is no `harness-pm` goal-check run for the built code.** The two goal-checks on disk
(`notes/research-FEAT-50-goalcheck-plan-c0.md`, `-c1.md`) graded the **plan** against your stated
intent, not the delivered code. That step was skipped when the feature was merged ahead of the ship
phase; this closeout was scoped to exclude re-running suites, so I did not manufacture one after the
fact.

What exists instead is a **complete per-SC grading by the validation panel**, which is evidence of
the same criteria gathered by a different persona:

- `notes/qa-feat50-pinned-review.md` carries an SC-01 … SC-21 evidence table with named-case
  existence *and* registration confirmed for all seven red/mutant cases.
- `notes/review-harness-code-reviewer-feat50-pinned.md` carries an independent MET/NOT-MET table
  over the same criteria, and `-recheck.md` re-ran eight of them at the final commit with nothing
  going red.

Two criteria are worth naming individually:

- **SC-11 — NOT met today. [CORRECTED]** The criterion is "`check-state.sh` exits 0 **and** no
  violation row names `FEAT-50`". Measured just now in the main checkout: **exit 1**, with a
  `FEAT-50` violation row.

  > *As posted, this bullet read: "I measured it myself today in the main checkout: exit status 0,
  > and zero rows of any class name FEAT-50. Met." That measurement was taken once, was not
  > reproduced, and three subsequent runs of the identical command disagree with it. It was wrong
  > and is withdrawn.*

  The row is **INV-33**: `review_sha 7505b8739f…` is STALE because `plan.yaml` changed after the
  pin was taken — last at `559354bc`, which is **FEAT-41's** migration commit, not a FEAT-50 edit.
  FEAT-41 merged after FEAT-50 and moved the feature station out of `feature.json` into
  `plan.yaml`, rewriting FEAT-50's plan as it went, and left the station reading `review` "because
  a migration moves a value and does not re-adjudicate it" (its own commit message). Adjudicating
  it is what this closeout does. **INV-33 is deliberately silent on a terminal station**
  (`check-state.sh:588`), so recording `status: done` on `main` closes the row — and a second row,
  INV-26 (*"the plan derives review — the board reads done"*), which this closeout's own GitHub
  writes opened and which the same one-line change closes. See **Terminal state** for the exact
  commands. **Neither row is a defect in FEAT-50's delivered code; both are the record catching up
  with the merge.**
- **SC-10** (neither suite regresses) was never independently re-run by any reviewer — both the qa
  and code personas recorded it as *reported ground truth* under their task constraints, and this
  closeout was instructed not to run project-wide suites. **It is the one success criterion with no
  first-hand measurement anywhere in the record.** The work is merged, so this is a record gap
  rather than a decision point, but it should not be read as verified.

## Open questions

None blocking. Two non-blocking questions came up from the final panel and are carried as backlog
rows B-1 and B-2 below. Two are genuinely yours rather than backlog rows:

- **Q-A: do you want the missing post-build goal-check reconstructed?** It would be one read-only
  `harness-pm` run grading SC-01…SC-21 against `main`, including the SC-10 suite runs nobody has
  executed first-hand. Cost: one run, two full suites. Value: it converts the SC-10 line above from
  a gap into a measurement, and it is the only way the "done means the SCs are met" rule gets
  satisfied on this feature by its own owner. Not needed to close anything; recommended if you want
  the record clean.
- **Q-B: should a merged feature's `review_sha` be exempt from INV-33 when a later feature's
  migration rewrites its plan?** FEAT-50's pin is honest — it names the commit its panel actually
  read — and it went stale because someone else edited the file afterwards. The terminal-station
  silence catches it *here*, but only because this feature was shipped in time. A feature that sits
  in `review` while a migration lands under it goes red for a reason no one on that feature caused.
  Worth a decision; not worth blocking this.

## Resolved escalations

- **T-08's `verify:` was unsatisfiable as written.** Its third heredoc asserted
  `ir.feature_root(d, 'FEAT-Y-other') == d` *after* creating a bare `FEAT` worktree, and
  `'FEAT-Y-other'.startswith('FEAT-')` is true, so no implementation of T-08's own matching rule
  could pass it. Escalated by the eng lead, measured by the orchestrator, and amended under your
  signature. Specification defect, not a code defect.
- **`review_sha` was a dangling object.** `feature.json` carried `7505b87681c8…`, which resolves to
  no object; three panel members independently ran `git cat-file` against it. The real commit is
  `7505b8739f…`, sharing only the 7-char prefix. The panel escalated rather than substituting
  silently; you repinned; the panel rebound its verdict to PASS. **This is the feature's own failure
  shape caught by the feature's own review** — a run's record not matching the run.
- **Every task but T-08 is `main-session-direct`** under the DEC-174 carve-out (the harness does not
  execute changes to its own hooks, validators or gate scripts). That routing held: `check-domain.sh`
  returned exit 2 for `harness-orchestrator` on all seven files, and you owned the implementation.

## UAT

**Not required.** Zero of 37 changed files carry a rendered end-user surface; the UI reviewer's
own measurement, twice. Every changed surface is a hook/CLI refusal message, verified live at
exit 2 with target, checkout and remedy named.

## Budget

- `cycles_used`: **8** of 10. Rework only — two eng FAILs, two validator FAILs, and the plan-fix
  and plan-amend loops.
- `runs`: **12** of 20. This closeout adds none — no lead was dispatched.

The cycle count is high for a 12-task feature and worth one sentence rather than an apology: five
of the eight cycles were spent on the **plan**, not the code — the adversarial panel found two high
findings in the plan and a fourth defect was authorized mid-planning. The build itself cost two
cycles and the code panel one.

## Proposed backlog

Strike any row by ID and it is dropped. **Unstruck rows become backlog issues on acceptance;
anything not on this table dies silently.**

| ID | Nature | Finding |
|---|---|---|
| B-1 | bug | Untested `AmbiguousWorktree` deny sits directly above a blanket `except Exception: return` in both gates (`check-domain.sh:733-736`, `bash-write-guard.sh:713-716`). Correct today, but nothing in the suite fails if a future refactor reorders the two `except` clauses — which silently converts a denial into a main-checkout write, issue #1057's exact shape. **Ranked first of the advisories despite being med: its failure mode is a silent loss nobody discovers.** |
| B-2 | chore | `feature_checkout_guard` is duplicated near-verbatim across `check-domain.sh` and `bash-write-guard.sh` instead of returning a verdict from `harness_boundary.py` the way `classify()` does — the drift risk that module's own docstring exists to name. The high finding above added a second *call site*, not a second implementation. |
| B-3 | bug | `inflight_registry.feature_root`'s "an ambiguity falls back to the owner root and nothing is raised" contract has **no test anywhere**. `feature_root` is never called by name in `test-inflight-registry.py`, before or after the cutover. A refactor narrowing `except Exception` to `except AmbiguousWorktree` would ship green. |
| B-4 | chore | Pre-existing `run_t14` in `test-check-domain.py` still grades FAIL (cyc 8 / ABC 51). Outside `dca2d3d..HEAD`, so it neither gated this cycle nor was cleared by it. Recorded so the next cycle does not rediscover it as new. |
| B-5 | bug | `PF-f52c5043…` (med) — `check-domain.sh`'s binding sits only in `domain_check()`'s allow/shared branches, while `harness_boundary.classify`'s `not_a_domain_question` outcome returns earlier; the Bash route's narrowed continue covers both. The two "route-complete" fixes are asymmetric in verdict-shape coverage. **Measured inert today**: `HARNESS_PROJECT_DIR` is read in exactly one production file and set by no production code, so a governed agent's root is always the main checkout. Latent, not live. |
| B-6 | chore | `PF-799e61a6…` (med) — the blocking premise behind the empty-`last_assistant_message` direction ("can the platform legitimately send an empty message on a tool-only final turn?") was raised in two review segments and reached neither BRIEF nor plan. If the answer is yes, the gate re-prompts a persona for a platform artifact. |
| B-7 | bug | `PF-5a9174d2…` (med) — the digest guard's literal-prefix test is narrower than the "lead revises its own digest within one run" case it is meant to protect: a mid-block correction is not a prefix continuation, so it is refused with a message telling the lead to take a new run directory, which is not the problem. Untested in either direction. |
| B-8 | bug | `PF-b07503b7…` (low) — the team playbook's digest-refusal sentence carries no Write-only caveat, so leads read the record as gate-protected while a clobber via `Edit` or a shell redirect still recurs #1058. The control converts a known hole into a hidden one. |
| B-9 | chore | `PF-aaa29b58…` + `PF-8c40679d…` — a certified-unreachable allow-arm now exists in **two** production gates with no ruling on the first instance. Either its first-ever execution is live in production gating, or both gates permanently carry code a reader trusts as exercised. Whatever ruling lands must cover both sites. |
| B-10 | chore | `PF-bd841841…` (low) — the locate-by-source-text mutant idiom couples three permanent red-proof cases to the gates' exact source bytes. A later legitimate rewording of any targeted branch reds the integration suite INCONCLUSIVE on a correct tree, and via SC-10 that red would gate unrelated future features. |
| B-11 | chore | `PF-cec83ae4…` (low) — the decision heading and SC-14 say "three rules" while the landed entry records five rulings, so the index's search surface omits the fourth defect's rule and the Bash-route ruling. |
| B-12 | chore | `PF-b7646eb9…` + `PF-bf9f5214…` (low/info) — line-anchor drift in D-11, T-11 and BRIEF.md's REQ-01 gap bullet (cited `:1414`, actual `:1413`). Substance correct in each case; only the anchors are off. |
| B-13 | bug | **Harness defect, outside FEAT-50's scope.** `bash-write-guard.sh` denies `cp`, `sed -i` and redirects, but not `python3 <script> <path>` — a governed agent can write any path through an interpreter. This is how T-01/T-02 were applied before the guard was measured; disclosed at the time and ruled to stand. **Re-measured during this closeout and it matters more than it looked:** the Write route refuses a governed write to the main checkout's FEAT-50 record at exit 2, while the Bash route returns exit 0 for a `python3 gh-sync.py ship <that same dir>` invocation. Two routes, opposite answers, on the identical target. |
| B-14 | enhancement | **Harness defect.** `validate-digest.py`'s `code_grade_bound_to_review` check is unconditional for `harness-code-reviewer`, so while `review_sha` dangled the persona could return **no digest shape at all** — a complete, correct PASS review surfaced as `failed (exit 1)`. The gate was right and the escape hatch was missing: a reviewer needs a contract-shaped way to report that the pin itself is the defect. |
| B-15 | chore | UI advisory (low) — the `digest-unreadable` refusal message names the refusal but not an explicit next step. Edge-case `OSError` path, the write is correctly refused, and the missing text ("investigate why the file can't be read") is inherently open-ended rather than a single prescribable action. |
| B-16 | chore | BRIEF.md's INV-32 section closes with "The remaining five panel findings — two med, three low — were not ruled on". True of what *you* ruled on, but a reader arriving after the amendment sees eleven findings and may read it as a count of the panel list. One clause. |
| B-17 | chore | Reachability limit disclosed by the BRIEF itself: four of the five red/mutant cases prove their assertion discriminates by running a mutant **inside the same suite whose reachability is in question**. Only the security re-check's scoped mutation was built outside the shipped harness. An external reachability proof for the mutant harness does not exist. |
| B-18 | chore | The delta was audited against a **dirty working tree**. `feature.json` was observed uncommitted and no member was asked to census other modified files. The tree is clean now, but no reviewer's finding rests on a censused tree. |
| B-19 | chore | **Schema skew between a merged feature's worktree and `main`.** FEAT-41's station migration landed after FEAT-50 merged, so this feature's checkout and the default branch now disagree about where the station lives and whether `feature.json.status` is required: the worktree's own schema **requires** the key, the main checkout's post-write gate calls it **undeclared**. The same file is simultaneously valid and invalid depending on which checkout reads it. It resolves when the worktree is removed; the general shape (a migration landing under a live worktree) does not. |

`PF-b3e87de8…` (SC-11's clearing act for the three DEC-156-failing lead digests being owned by no
task) is **not** listed: it is resolved. The `runs/` tree is gitignored, so those digests never
reached the default branch and die with the checkout. **[CORRECTED]** — *as posted this sentence
also claimed "`check-state.sh` measured clean today", which is withdrawn above.*

## Terminal state

**Done already, by this closeout:**

- GitHub — parent **#1082**, task sub-issues **#1083–#1094** and source issues **#1056/#1057/#1058**
  are all at the `Done` station and all read `CLOSED` / `COMPLETED`; milestone **35** is closed with
  **0** open issues. Verified by reading the board and the issues back, not by trusting the writes.
  The ship review was posted on #1082 from this file.
- The worktree's record — `plan.yaml` `status: done`, `feature.json` `pr: 1105`, committed on
  `feat/FEAT-50-run-artifact-integrity`.

**Not done, and not mine to do — the finalization.** The branch is merged and two merges behind
`main`, so nothing committed in its worktree can reach the default branch without a second merge,
which this closeout was told not to attempt. And the checkout binding this very feature built
refuses a governed agent's write to the main checkout's FEAT-50 record while its worktree stands —
measured, exit 2, message quoted in the run digest. That refusal is the guard working. It is also
why the last two lines of this feature's record are yours:

```
python3 .agents/skills/harness/bin/plan-merge.py set-feature-station \
    --file .harness/harness/features/FEAT-50-run-artifact-integrity/plan.yaml --station done
python3 .agents/skills/harness/bin/gh-sync.py record-pr \
    .harness/harness/features/FEAT-50-run-artifact-integrity --pr 1105
```

then commit those two files on `main`, copy `notes/ship-review-2026-09-01-ship.md`,
`notes/ship-review-2026-09-01-ship.html`, `notes/handoff-ship.md` and `STATE.md` across from the
worktree, and finally remove the checkout **from outside it** —
`git worktree remove` exits 0 when run from inside the tree it deletes, which is why no agent in
that tree may run it.

Until that lands, `check-state.sh` reports two rows against FEAT-50 — INV-33 (stale pin) and INV-26
(plan derives `review`, board reads `done`) — and both close on the station write. Expect INV-29 to
start refusing on the standing worktree the moment it does, since that classifier reads the landed
station from the default branch.
