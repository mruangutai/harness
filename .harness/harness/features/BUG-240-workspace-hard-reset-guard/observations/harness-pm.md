# Observations - harness-pm

- 2026-09-07: BUG-240 — a test-first verify that expects FAIL lines only discriminates if the pre-change code can reach the destructive path: the first draft monkeypatched run_git for the dirty-file case, so "file survives byte-identical" was green pre-fix by accident (fetch failed on a repo with no remote). Rewrote the fixture as a local bare origin plus working clone so the unguarded reset --hard really destroys the file, and the case reddens for the real reason.
- 2026-09-07: BUG-240 plan goal-check — a plan intent that told a test to assert "os.environ occurs exactly once" in the production file measured 3 (docstrings count), so BOTH tasks verify chains were unsatisfiable. Source-text substring/count assertions inside an intent must be greped at the pinned sha before the plan is signed; a docstring mention is source text too.
- 2026-09-07: BUG-240 plan repair — a case name quoted inside a plan.yaml literal block scalar got LINE-WRAPPED, so the intent's copy carried a newline the verify's grep pattern could never match; the both-directions name check caught it only because it compared the loaded YAML value, not the raw file. Keep every verify-matched literal on one unbroken line in intent, however long.
- 2026-09-07: BUG-240 c1b: a verify clause counting FAIL lines is only well-founded if the intent pins how many check() calls each case emits; the target file's own idiom (A/D/F2 in tests/unit/test-factory-workspace.py) splits one fixture across 2-3 named checks, so an unconstrained builder reddens a correct implementation. Fix was one-check-per-name plus a stated purpose for the total, not deleting the clause.
- 2026-09-07: BUG-240 panel record — plan-merge set-panel took a plain safe_dump mapping with last_run/cycle/readers/findings and folded long summaries into quoted continuation lines; ids still re-derived byte-exact afterwards, so the dumper's line-wrapping does not disturb panel_findings identity.
- 2026-09-07: BUG-240 — the env-pollution remedy a panel asks for in a TEST task can be structurally weaker than it looks: factory_config resolves its root at IMPORT (factory_config.py:56-58), so an os.environ.pop inside a case body cannot suppress a discarding line already printed at module import. Recorded the limit in the artifact rather than substituting a different remedy.
- 2026-09-07: BUG-240 — a dispatched acceptance step "diff the plan.yaml pre/post" was void: plan.yaml is UNTRACKED in the feature worktree, so git diff prints nothing and passes whatever the write did. Substituted a splice-locality probe (copy the file into a legal .harness/<repo>/features/ path, re-splice with a marker panel, difflib the opcodes against the panel block's line range). plan-merge refuses any --file outside a features dir with exit 9.
