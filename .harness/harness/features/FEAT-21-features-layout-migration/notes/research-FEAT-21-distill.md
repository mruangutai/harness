# Distillation — harness-pm — FEAT-21 close

**Six entries land, four by displacement; five candidates rejected with reasons. All six are CRAFT
— no repository-layer file was created.** Craft file `.harness/expertise/harness-pm.md`:
Patterns 15→15, Gotchas 15→15, Outcomes 0→2, Open 0→0. File 109 lines of the 150 budget.
`check-expertise.sh` over `.harness/expertise/` exits 0.

## Ops (the receipt)

| op | section | target | becomes |
|---|---|---|---|
| replace | Patterns | P-07 | ambiguous criterion settled by the sanctioned remedy set |
| replace | Patterns | P-11 | a parity test needs one real side, not a mirror |
| replace | Gotchas | G-04 | a first-occurrence region anchor is relocatable |
| replace | Gotchas | G-09 | the remedy commit falsifies verdicts already taken |
| add | Outcomes | — | O-01 mutate per branch, not per side |
| add | Outcomes | — | O-02 aim the mutant at the case's own fixture |

Displaced and why weaker: old P-07 (presupposition between an absence check and a survivor clause)
fires on a rare drafting shape; old P-11 (criterion names a mechanism) fails recoverably — a
re-signature — where the parity rule's failure is a criterion graded met on a test that cannot
fail. Old G-04 (include your own additions when re-deriving) is a subset of general re-derivation
discipline already carried by G-01; old G-09 (a CLI resolving its target from config) almost never
fires for this role.

## Candidate dispositions

- **C1 — ambiguous criterion settled by the remedy set: ACCEPTED**, became P-07, displacing the
  presupposition entry. Source: **own** — the full argument is mine, verbatim in
  `notes/research-FEAT-21-goalcheck-sc10.md:29-30` ("would have made both sanctioned remedies
  non-compliant on arrival"). Not covered by P-05/P-06: P-06 forbids narrowing on your own
  preference, and this entry supplies the external evidence P-06 demands before narrowing. The
  entry says so explicitly so the two do not read as contradictory.
- **C2 — a met verdict went stale under a later fix commit: ACCEPTED**, became G-09. Source:
  **own** — my Q3 in `notes/research-FEAT-21-goalcheck-sc10.md:97-100` is the collision itself.
  Distinct from G-07 (evidence predating the graded commit): this is a *verdict* predating a later
  commit. **Sharpened from the relay's detective form to a preventive one** — the DO fires when the
  fix cycle is routed, because after the commit lands nothing can unland it.
- **C3 — a first-occurrence region anchor is relocatable: ACCEPTED**, became G-04. Source:
  **relayed** — the hole was found in the lead's own pre-dispatch recipe
  (`runs/2026-08-14-1-product/digest.md` Q3), not in anything I wrote. It **contradicted my own
  measurement** and I read the source before writing: my tested evasion relocated the docstring and
  stayed RED because the relocated region contained zero satisfying occurrences; the digest's spoof
  writes the label itself into a docstring that already carries one, so the relocated region
  satisfies the count and GREENs. Both are true; relocation is the mechanism, and the entry states
  it. The two smaller Q3 residuals (inline-literal pinning false-REDs, a literal parked in a detail
  f-string false-GREENs) are **rejected as instances** — they are consequences of each intent's own
  wording, not a rule, and P-01's discriminating-verify test already covers writing them.

## Rejected, with reasons

- Parity mutation needed a real artifact → **accepted** as P-11 (own material; the feature's only
  fix cycle).
- "Capture the checker's output before editing an untracked plan" — covered by G-15 (working-tree
  grading needs named capture commands).
- "Re-run the quoted command before attaching a sha to its result" — covered by the preloaded
  `harness-spec-driven` baseline rule (a recorded baseline carries its sha and condition). An
  Expertise entry duplicating a preloaded rule costs a slot and buys nothing.
- "A comment naming its own compensating coverage is a claim to grep" — covered by P-09 ("cited
  lines are often comments, not assertions").
- Cycle-1 literal-sweep blindness (three findings none of my literal sweeps carried) — covered by
  P-12 (derive from the weakest fragment every site contains). The segment-qualify key trap dies
  with it as an instance.
- Running each new verify against the unfixed tree — covered by P-01. `--resolve` on a lane row's
  representative path — covered by G-02.
- `bash-write-guard.sh` blocking a plan's own quoted-redirect `verify:` — **not Expertise.** It is a
  harness design consequence needing the operator's ruling; a workaround recorded here would outlive
  the ruling. Stays an open question (raised in the SC-10 note's Q2).

## Open

- Nothing blocking. `Open` in the Expertise file stays empty by choice — padding it would tax every
  spawn with a question no rule turns on.
