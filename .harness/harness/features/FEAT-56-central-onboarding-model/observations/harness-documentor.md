# Observations - harness-documentor

- 2026-09-08: FEAT-56 T-07 — `check-instruction-paths.py` scans only `.omp/agents`, `.claude/agents`, `harness-*/SKILL.md`, `references/` and `templates/` (:48-52). The task's acceptance claimed my new doc prose "introduces instruction paths" the checker would grade; `docs/` and `README.md` are not in its scan set at all, so the claim was unfalsifiable either way. Verified the checker's roots before reporting rather than inferring from its name.
- 2026-09-08: FEAT-56 T-07 — the `edit` section header resolves relative paths against the process cwd, not the dispatch worktree. First DECISIONS.md hunk was refused by check-domain because cwd was the control-plane root while my claim was the worktree. Absolute paths in every hunk header fixed it; the two copies were byte-identical so the snapshot tag matched both (G-18 confirmed live).
- 2026-09-08: dispatch asked me to write "check-state.sh deliberately makes no network call" into DEC-220; the script DOES call gh (INV-26 :2062-2080, INV-30 :2291-2327). The true, narrower pair is (a) it never reads a member's remote config and (b) no network result can raise a violation (:2270-2273). Wrote the narrow claim; the finding's conclusion survived intact.
- 2026-09-08: SPEC.md:450 cites harness_boundary.py:158 for "check-domain.sh fails CLOSED"; :158 is linked_worktrees' NO-GIT-SUBPROCESS docstring line. Real deny path is classify's outcome:"deny" at harness_boundary.py:711. Declined to repeat an unverifiable citation in a decision entry; flagged as stale instead.
- 2026-09-08: edit tool refused a hunk whose section header carried the control-plane-relative path while I held a worktree claim (check-domain BLOCKED, "write it from a bound worktree"). Header must spell the absolute worktree path even though `read` echoes a repo-relative one.
- 2026-09-08: a body-only edit to a DECISIONS.md entry recomputed its INDEX row tag set (state in, expertise out) without moving the @line anchor — so an append-free edit still requires regenerating DECISIONS-INDEX.md.
- 2026-09-08: FEAT-56 fix-anchor. Dispatch, the lead and my own prior receipt all named SPEC.md:451 for the rotted `harness_boundary.py:158` citation; it was actually on :450, and :451 is the next sentence. Three independent handoffs carried the same off-by-one because each cited the paragraph, not the line. Editing the dispatch's line number blind would have replaced the wrong sentence and left the rotted anchor standing.
- 2026-09-08: FEAT-56 fix-anchor. Confirming an anchor target is unmodified in `git status` is cheap corroboration that a measured line number is committed state and not a sibling's in-flight shift — worth pairing with the grep whenever a concurrent segment is live.
- 2026-09-09: FEAT-56 T-16 — an `edit` section header written as a worktree-RELATIVE path resolved against the process cwd (the control plane), not the dispatch worktree, and check-domain refused it. Absolute worktree paths in every section header from then on. G-18 confirmed live.
- 2026-09-09: BUILD.md needs a two-bucket rule before editing: `## Step 0` and `## Critical files` are present-tense claims about the live system; `## Task NN` and `## GSD-removal migration map` are delivery records. Sections state their own tense ("the spec below is what was built", "kept as the record ... in the past tense it now belongs to") — read the section preamble, not the sentence.
- 2026-09-09: allocating a DEC number, the branch sweep found feat/FEAT-46-decision-standard holding a 396-entry DECISIONS.md numbered to DEC-548 with its own DEC-221. A wholesale-renumbering branch is not "another feature took the number"; the integration branch remains the ceiling (P-02).
- 2026-09-09: FEAT-56 T-20 — the edit tool resolves a section-header path against the process cwd, not the dispatch worktree; a relative `.harness/harness/docs/DECISIONS.md` header was refused by check-domain as belonging to the main checkout. Absolute worktree paths in the header fixed it first try.
