# Observations - harness-pm

- 2026-08-31: FEAT-48 planning. The research note's fix direction (tempdir first on the child PYTHONPATH) cannot work for check-domain.sh: its heredoc runs sys.path.insert(0, _bin_dir) at line 125, so the real bin dir always wins. Re-derived the mechanism from the script before writing the task, and the fix became a private bin copy instead. A dispatch that hands me a fix direction is a hypothesis, not a spec.
- 2026-08-31: prototyping the guard BEFORE specifying it changed three rule decisions and cut findings 47 -> 15 -> 10 with zero false positives. Specifying a static scanner without running it once would have shipped a rule someone deletes on its first false positive.
- 2026-08-31: ran every verify block against the pre-change tree. Two of five would have passed unchanged if written the obvious way (bytes-only comparison for T-01, name-set comparison for T-02); polling during the run and adding mtime is what made them discriminating.
- 2026-08-31: re-planning FEAT-48 after the plan panel. A verify clause I wrote to prove
  attribution (`o.split(header)[-1].split("PASS "+n)[0]`) passed against a mutant that deleted the
  block header entirely: a missing anchor made the slice the whole output. Only a mutation run
  caught it; reading the block did not. Rewrote with `find` on both anchors plus an ordering check.
- 2026-08-31: re-deriving the ten historical violation sites from the research note's reference
  scanner gave 25, not 10 — my re-implementation of the per-scope taint model was broader than the
  note's. The ten named sites were a subset, so I made the verify assert `want - sites == {}` rather
  than set equality, and moved the false-positive burden onto the live half of the same block.
- 2026-08-31: the plan-route checker's 50-line machine-field budget forced the T-04 split. An
  independent reconstruction that does not trust the task's own new test file costs ~30 verify
  lines, which one task cannot hold beside registration and measurement checks. Splitting the task
  was cheaper than weakening the block.
- 2026-08-31: FEAT-48 c1. A verify conjunct can be UNSATISFIABLE rather than merely weak: T-06 asserted `not [l for l in sh if l.strip().startswith("for s in")]` while the same intent mandated keeping the drift detector, whose line 64 is `for s in "${ALL_SCRIPTS[@]}"`. Nobody caught it in two panel cycles because reading a block confirms what it INTENDS; only substituting the post-change file and running the predicate shows it can never green. Substituting the file took one command.
- 2026-08-31: FEAT-48 c1. Two readers attacked D-11 from opposite directions (too narrow / too broad) and both were right — the watched set was wrong on two axes at once. The synthesis was not a compromise between them but a change of the SET's definition (git-tracked-under-ROOT -> all-files-under-bin), which strictly widened one axis while narrowing the other. When two findings look contradictory, check whether they are about the same dimension before trading them off.
- 2026-08-31: FEAT-48 c1. Prototyping a proposed check's mechanism in a tempdir before writing the plan text cost ~2 minutes and produced the __pycache__ exclusion, which nothing in either panel note anticipated and which would have made the shipped guard redden on every real run.
- 2026-08-31: FEAT-48 cycle 2. A verify regex I authored for a DECISIONS.md section assumed the heading form `## DEC-NN Title` with a plain space; all 188 real headings use an em-dash. Generalised: when a verify block anchors on text a HUMAN will author, derive the anchor from the existing corpus (grep the real file and count) rather than from the sentence the intent tells them to write. Widening the regex beat documenting the required spelling, because the widened form cannot be broken by a later style pass.
- 2026-08-31: exercising a regex as typed is not the same as exercising it as loaded. I re-extracted the pattern from yaml.safe_load(plan)[tasks] and ran it against a synthetic appended entry, which is the only form the doer ever sees.
- 2026-08-31: bash-write-guard parses the two-character comparison operator (greater-than followed by equals) inside a python heredoc as a shell redirect and blocks the whole command. Rewrite the comparison as a negated less-than or the probe never runs.
- 2026-08-31: re-derived FEAT-48's baselines at ccf674a after the FEAT-45 rebase. The bin census (56->58) was the advertised change and was the LEAST consequential; two unadvertised things moved. (a) FEAT-45's commit 70fd441 added a FOURTH mutant-beside-the-original site (test-check-state.py:3066-3088, .check-state-inv32-mutant.sh) inside a file T-02 already edits — so a sibling feature can add a violation to a file your task already owns without touching your census. (b) Reimplementing the research note's own reference scanner from its written rule gave 25 findings at ea6f51f, not the 10 the note's table claimed 0-false-positives for: file-CONTENT reads propagated taint onto tempdir paths. Both were invisible to a count-based re-check; only executing the claim found them.
- 2026-08-31: the durable lesson from the above — when a note states BOTH a rule and the number that rule produces, the number is the claim to re-derive by RUNNING the rule, never by re-counting inputs. A cycle-2 reviewer had already cited that table's "0 false positives" as grounds to pass T-03's verify, so one unexecuted number propagated into a review verdict.
- 2026-08-31: measured evidence trail for the above: T-02's verify block run verbatim at ccf674a -> exit 1, appeared ['.check-state-inv32-mutant.sh', '.mutant-check-state-t10.sh', '.mutant-check-state-t14.sh', '.mutant-feature-worktree-behind.py'], moved []. Discovery walk 58 in both trees. run-unit-tests.sh byte-identical to origin/main (serial loop still 147-157). DECISIONS.md headings 188->190.
- 2026-08-31: FEAT-48 c3 advisories. F2 was a disclosure gap opened by the previous cycle own fix: a repair to make a verify satisfiable (content-read exclusion) widened the blind-spot set, and neither the decision nor the DEC-authoring task was updated to match. Lesson: when a fix narrows a mechanism, grep every completeness claim about that mechanism in the same edit, including the ones inside a task intent that will become a signed DEC.
- 2026-08-31: an independent-reader note that rates a consequence low and loud is still worth a recorded ACCEPTED CONSEQUENCE paragraph in the decision that causes it; silence reads as oversight to the next auditor and gets re-raised every cycle.
- 2026-09-01: FEAT-48 c4 goal-check. Reimplementing a plan's OWN written scanner rule and reproducing its pinned numbers (10 sites at ea6f51f, 25 without the exclusion) is what licensed every other number I published from the same tool — a re-derivation that matches the plan where the plan is verifiable is the cheapest way to earn trust for the places it does not match (13 -> 20).
- 2026-09-01: a rebase's own diffstat is a bad proxy for hazard-census drift. The orchestrator's grep of ADDED lines for dirname(__file__) shapes found none, yet the rebase added 7 taint violations across 3 files: the new sites bind HERE/SCRIPT module-level constants, so the write site carries no dirname() call at all. Grade the census by running the rule, never by grepping the diff.
- 2026-09-01: counting test files at the sha the plan CITES (ccf674a=58) vs the branch tip (59) distinguished 'the plan was always wrong' from 'the rebase broke it'. The shared hypothesis had it as the former; it was the latter. Always measure at the cited sha before calling a recorded number wrong.
- 2026-09-01: transcribing the c4 panel, the digest's summary cells carried backticks (`test-check-state.py`) and one began with `#1053`. panel_findings.py only lowercases and collapses whitespace, so stripping backticks changes the id: the normalization must be recorded in the plan itself (panel.transcription_rule) or the next reader recomputing from the digest gets a different hash and INV-32 reports a stale override. Quoted every prose scalar too — an unquoted `#1053 ...` would have loaded as an empty value.
- 2026-09-01: plan-merge.py has NO verb that edits a list field on an existing item — amend exits 4 ("apply's job"), apply exits 7 CONFLICT on any changed value. So widening a task's `files:` or `depends_on:` is unreachable; the only sanctioned route is a NEW task id via apply, with any ordering constraint carried as prose in `intent:`. Cost me a whole re-decomposition mid-cycle (FEAT-48 c5, T-07).
- 2026-09-01: when a dispatch says "make the verify exercise the derived set", check the 60s budget FIRST by timing each candidate file. FEAT-48's four census files measured 33.6/22.5/7.8/5.7s — the union blew the budget, which is what forced the ownership/completeness split (per-task poll vs T-03's live-tree zero) rather than one big derived verify.
- 2026-09-01: a verify can read its OWN task's `files:` out of plan.yaml. That kills the "hardcoded pair drifts from the contract" failure for free, and is honest as long as you SAY it is an ownership check, true by construction about completeness.
- 2026-09-01: FEAT-48 c5 goal-check. A plan can carry an ordering the SCHEDULER cannot see: teams/build.yaml:83 reads depends_on: from_task_depends_on and nothing else, and both id order and file order put the later-added task last, so a prose-only 'X must land before Y' is not a coin flip, it fails by default. Check the sequencer's own field before accepting an ordering argument written in intent.
- 2026-09-01: FEAT-48 c5. Cycle 4 called T-06's control leg unroutable; I settled it by BUILDING the route (copytree bin to tempdir, git show ea6f51f copy of the test into it, poll the copy) rather than reasoning about it: 4,764 broken reads of 491,176 polls, live module untouched. First poll attempt returned zero because I guessed the injected marker; read the pinned source for the marker string before trusting a zero.
- 2026-09-01: FEAT-48 c5. plan-merge.py `amend --show` on a FOLDED (`>`) scalar returns one line per paragraph and DROPS the blank separators; feeding that straight back through `--value-file` collapsed D-10's eight-paragraph `because` into one. Recovered by splitting the flattened value on its own section openers and re-wrapping at 98 cols. Literal (`|`) fields round-trip fine.
- 2026-09-01: FEAT-48 c5. A write-once `files:` list can be routed around instead of fought: the verify derives its run set from plan.yaml's ownership graph — own files, plus files of tasks stationed `abandoned` whose `depends_on` names me, minus files of still-live tasks. Asserting the absorbed set is non-empty is what stops it silently narrowing back.
- 2026-09-01: FEAT-48 c5. Proving a `verify:` block without touching the live tree: extract it from plan.yaml, run it verbatim under `bash -c` with `cwd` a synthetic root holding a copy of plan.yaml and real stub files at the paths the plan names. Cost 0.25s and gave a genuine RED/GREEN pair.
- 2026-09-01: amending a folded `>` field is safe WITHOUT round-tripping `--show`: import plan-merge.py and use _item_range + _field_block to lift the RAW dedented block body, edit it textually, feed it back with --value-file. _render_field reuses the original block header verbatim, so `>` stays `>` and `|` stays `|`, and --show is then needed only for the sha256.
- 2026-09-01: a phrase replacement inside a hard-wrapped block scalar leaves a 150+ char merged line where the new text meets the remainder of the original physical line. Re-wrap the touched paragraph (textwrap, width 98) and assert maxlen — but only where the paragraph is flat prose; FEAT-48 T-03's print-contract paragraph carries an indented two-line sublist a whole-paragraph re-wrap would destroy.
- 2026-09-01: transcribing a panel value, the safe route is a two-file pipeline — write the verbatim draft with the file tool, then a python driver that `safe_load`s BOTH the digest's fenced block and the draft and asserts structural equality before hashing. Eyeballing 16 long summaries for an em dash or a collapsed space cannot be done; a dict comparison can.
- 2026-09-01: pass the summary to `panel_findings.py id` as an argv list via `subprocess.run`, never through a shell. FEAT-48's summaries carry em dashes, apostrophes (`#1053's`) and `--yaml-value`; any of them can be mangled by quoting, and a mangled hash reads as a legitimate id while making the operator's later ruling a STALE OVERRIDE.
- 2026-09-01: a text-level splice keyed on `  - reader: ` matches the `readers:` list entries as well as the findings — it raised IndexError after 3 of 16. Gate the splice on having passed the `findings:` line. The failure was loud only because the id list ran out; with 20 ids it would have silently written ids onto reader entries.
