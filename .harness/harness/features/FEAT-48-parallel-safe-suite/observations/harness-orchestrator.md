# Observations - harness-orchestrator

- 2026-09-02: FEAT-48 build dispatch was unexecutable and the tell was in the plan, not the tree: every task carried `execution_mode: main-session-direct` (DEC-174 carve-out). `references/github-mirror.md:32-34` assigns those segments to the main session by name. Checking `execution_mode` across all tasks costs one `harness_yaml.load_plan` and should be step zero of accepting any build dispatch — I read BRIEF, plan decisions and T-01's full intent before noticing.
- 2026-09-02: confirming the routing by RUNNING `check-domain.sh` on real payloads for my own agent_type (exit 2 on 8 of 9 task surfaces) turned "I think this isn't mine" into a measurement I could hand back. `--resolve` alone was not enough — it names who MAY write, not whether I may.
- 2026-09-02: a blocked phase is not an empty session. Running D-10's census (257s) while blocked found `test-validate-digest.py`'s 3 mutation sites that no prior artifact had flagged, and reading `gh-sync.py:1158` disproved a standing "must fix before review" blocker in STATE.md. Both were cheap and both change what the next agent does.
- 2026-09-02: my census implementation snapshotted `os.path.isfile` and then reported every dirent absent from the snapshot, so the pre-existing directory `bin/fixtures/` showed as an "appeared" site on all 61 files — 61 false positives against 13 real ones. A discovery instrument that cannot distinguish a directory from a created file inflates its own finding count; say so in the record rather than quietly filtering.
- 2026-09-02: `test-check-domain.py` exited 1 once and 0 in four re-runs, and I could not recover WHY because my scan captured stdout/stderr and printed neither. Capturing output and discarding it on the one run that fails is the worst of both; keep the failing run's bytes.
- 2026-09-02: validate phase. `feature.json`'s run-entry `code_grade` enum is CLOSED to `n_a`, and absence is the signal "this run graded CODE". I dispatched the lead telling it `n_a` is wrong for a code-grading run — true of its DIGEST, false of feature.json — and `append-run` refused until I dropped the key. Two different fields, same name, opposite defaults.
- 2026-09-02: a seam commit that writes the station into plan.yaml AFTER review_sha is pinned trips INV-33 every time. The build handoff was written before the seam commit landed, so it honestly reported a clean check-state that the very next commit invalidated. Re-pinning to the seam commit is free when `git diff --stat <old> <new> -- <code paths>` is empty; prove that first, then re-pin.
- 2026-09-02: I contaminated my own first two suite runs by passing HARNESS_AGENT_TYPE in the bash tool's env parameter, which leaks into every test subprocess; `test-plan-merge.py` then failed 11 sign-approval checks and the suite exited 1. I nearly reported a squad-visible red as a FEAT-48 regression. The env parameter is sticky per call — `env -u VAR` inside the command is what actually clears it.
- 2026-09-02: an unused import is a reliable fingerprint of dropped work. `test-suite-independence.py` imports `tempfile` at :9 and never uses it — the exact module the six mandated fixture cases would have needed. Grepping for the absent construct found nothing to look at; the vestigial import is what made the omission legible.
- 2026-09-02: the panel rated the symlink fail-open `high` and pm rated it `low, no SC covers it`. Both were reasoning from the SUCCESS CRITERIA. The signed plan's TASK INTENT block (T-04: catch a path that "APPEARED") settled it as approved-but-unmet, which neither had consulted. Task intent is a third authority above SCs for grading delivery completeness.
- 2026-09-02: re-validation. A criterion can be unmet for two completely different reasons and the routing differs entirely. Round one SC-03 was unmet because the work was not done; round two it is unmet because CI's shallow `actions/checkout@v4` puts the required historical commit out of reach, so SC-03 and SC-04 cannot both hold. Same grade, and the second is pm's re-plan while the first was a fix cycle. Ask WHY before routing.
- 2026-09-02: when a fix rewrites the very component a criterion's evidence was measured against, that evidence goes stale silently. SC-05's ten runs were taken before `snapshot()` was rewritten and nothing flagged them; the note still reads green. Re-taking them cost 8 minutes and was the only thing that made SC-05 true of the shipped pool.
- 2026-09-02: a review panel's prediction about what a fix will do to a mechanical gate is not evidence. The panel said decomposing for M2/M4 would clear three of five code_grade records; the fix ADDED two, taking 7 to 9. Re-run the gate after the fix rather than carrying the prediction forward as an expected outcome.
- 2026-09-02: verifying a fix means re-running the probe that CAUGHT the defect, plus the controls. My symlink probe re-run showed both vectors now exit 1 — but the clean control still exiting 0 is what proved no false positive was traded for the fix, and only the control could show that.
- 2026-09-02: I graded five of six mandated fixture cases delivered by reading the file, then found the sixth gap (`src.replace` leg) only by grepping for the specific construct the plan enumerated. Enumerated requirements need enumerated checks; reading for general shape found the five and missed the one.
- 2026-09-02: FEAT-48 c8. My first fault-injection probe of `run_pool.snapshot()` patched `os.lstat`
  alone and reported "no crash" — wrong branch: `os.path.islink` itself calls `os.lstat` and swallows
  OSError, so the victim returned False and never reached the line I meant to test. Pinning `islink`
  True as well flipped the result to `FileNotFoundError` out of `snapshot()`. A probe that misses its
  target returns a clean, plausible, entirely false negative.
- 2026-09-02: FEAT-48 c8. The panel's code reviewer partitioned 9 `code_grade` records as
  "7 pre-existing / 2 introduced"; its own lead retracted it, and one read of `code_grade.py:427-431`
  settled it — `gated_set` gates only functions with no pre-image or a worsened grade, so the
  inherited-debt shape is unrepresentable. Checking the tool's selection rule was cheaper than
  adjudicating between two reviewers' readings of the output.
- 2026-09-02: FEAT-48 c8. Dispatching the validator panel and the product goal-check in one message
  cost nothing: they share no files (each persona owns its own `notes/` name) and neither moves the
  tip. The only sequencing that mattered was doing my own mechanical checks FIRST, so both dispatches
  carried measured numbers instead of asking two squads to re-measure the same thing.
- 2026-09-02: FEAT-48 c8. The handoff shape gate refused four successive drafts at 74, 70, 68, 67, 66
  and 65 lines against a 60-line cap. Shaving a line per attempt wasted six writes; cutting whole
  bullets and budgeting the section line counts up front would have landed it in one.
- 2026-09-02: worktree_terminal.classify reads plan.yaml status from the DEFAULT BRANCH (worktree_terminal.py:388-393), so the post-merge sweep fires only if `done` is already landed. Writing the station inside the feature PR collapses the ship to one merge; FEAT-50 (#1111) and FEAT-51 (#1158) each paid a second closeout PR for landing it afterwards.
- 2026-09-02: gh-sync.py ship refuses at exit 1 when the feature dir resolves inside .claude/worktrees/, and the refusal is the first statement of cmd_ship, before any write or network call. Measured in the FEAT-48 worktree; it prints the main-checkout path to use instead.
- 2026-09-02: post-merge-sweep.sh runs gh-sync.py ship with NO --body-file, so the automatic path never posts the CEO briefing on the parent issue. A second explicit ship call with --body-file is the only way that comment lands, and it is idempotent for the station writes.
- 2026-09-02: .gitignore:7 ignores .harness/*/features/*/runs/**, and because the feature dir itself is tracked, git status shows nothing at all for its 21 run dirs. The digests die with the worktree removal, so a distillation dispatched after the sweep must be pointed at notes/ and observations/.
- 2026-09-02: gh-sync.py backlog accepts only bug|chore|enhancement, so a briefing row whose nature is "harness defect" has no home and must be mapped explicitly (FEAT-48 B-13, B-14 filed as bug). Prefixing each staged title with [FEAT-48 B-N] is what makes the returned issue numbers map back to the briefing table without a second lookup.
