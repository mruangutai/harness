# D-10 census scan — run at `cd8a0c34`, 2026-09-02

**DATED OBSERVATION, NON-BINDING (D-10). Re-run it before editing.** T-01's and T-02's `intent:`
both instruct the doer to re-derive the sites at its own commit. This record exists so the doer
knows what to expect and can diff against it — it does not discharge that instruction.

## Result: 13 sites in 6 files (D-10's dated figure was 8 in 4)

| file | sites | owner |
|---|---|---|
| `test-check-domain.py` | `feature_schema.py` (overwrite), `.feat50-check-domain-<pid>.sh` | **T-01** |
| `test-check-state.py` | `.mutant-check-state-t10.sh`, `.mutant-check-state-t14.sh`, `.check-state-inv32-mutant.sh`, `.check-state-inv32-era-mutant.sh` | **T-02** |
| `test-feature-worktree.py` | `.mutant-feature-worktree-behind.py` | **T-02** |
| `test-bash-write-guard.py` | `.feat50-bash-write-guard-<pid>.sh` | **T-02** (via abandoned T-07) |
| `test-check-fixture-secrets.py` | `.check-fixture-secrets-mutant-<pid>.sh`, `.check-fixture-secrets-skant-mutant-<pid>.sh` | **widened — see below** |
| `test-validate-digest.py` | `.validate-digest-bug919-red-<pid>.py`, `.validate-digest-dec156-red-<pid>.py`, `.validate-digest-empty-red-<pid>.py` | **widened — see below** |

The first four files and eight sites are exactly D-10's dated observation at `2a5cbada`. Two files
and five sites are new, and D-10 anticipated this precisely: *"If the scan at build time reports a
different set, the SCAN is right and this sentence is stale."*

## The two widened files

`test-check-fixture-secrets.py` arrived on the rebase and was already flagged in `STATE.md`'s open
questions and in `notes/handoff-plan.md`. **`test-validate-digest.py` was NOT flagged anywhere** —
this scan is the first thing to see it. Its three sites are the same shape: `<pid>`-suffixed files
written into the live `bin/` and unlinked in a `finally`.

Both are under the lanes glob `.claude/skills/harness/bin/**`, so both are lane-resolved,
**neither is a D-10 boundary escalation**, and the operator has already ruled this shape in-scope
for the fixture-secrets file. `test-validate-digest.py` is the identical case and takes the same
ruling. Neither appears in any task's `files:` list, which is the understatement panel finding
`PF-6e5e4d2b22b169d401d2bdcbab2425c1` already recorded against T-02 at `disposition: open`.

**Consequence for T-03.** T-02's derived run set is its own `files:` plus abandoned-T-07's, minus
other live tasks' — so it reaches four files and does **not** reach these two. T-03's live-tree
zero over D-03's repository-wide walk is what sees them, exactly as D-10's "two instruments, one
division" intends. **T-03 cannot ship green until all five new sites are fixed.** The doer must
name them in its receipt.

## Method and its one artifact

Implemented from D-10's five steps verbatim; script at `/tmp/feat48-census.py`, 257.0s wall over
61 discovered test files, root printed and asserted as the worktree root.

**Instrument artifact, stated so nobody mistakes it for a site:** step 2 snapshots *files*
(`os.path.isfile`), so the pre-existing **directory** `bin/fixtures/` is absent from the snapshot
and step 3 reports it as "appeared" for all 61 files. It is not a mutation site. The raw scan
therefore printed 74 SITE lines; 61 of them are that one directory and 13 are real. A doer
re-running this should either snapshot dirents rather than files, or filter `fixtures`.

## `test-check-domain.py` intermittency — matters for T-01's verify

It exited **1** during the census and **0** in four subsequent runs (one bare, three with the
polling thread attached). I could not reproduce the failure, including under a deliberately
concurrent `check-state.sh` in the same worktree; and the census discarded the failing run's
output, so **the mechanism is UNVERIFIED and the instrument gap is mine**.

Why it matters: T-01's `verify:` requires `r.returncode == 0` *and* an empty `appeared` set. A
doer that hits this intermittency will read a spurious red. Run T-01's verify on a quiet tree, and
re-run before concluding the task failed. That a gate-test's verdict here depends on what else is
running is the premise of this whole feature, not an aside.
