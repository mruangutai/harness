# Operator rulings — FEAT-48, c9 validate seam — relayed by the main session 2026-09-02

These answer the three questions this orchestrator returned with
`notes/ship-review-2026-09-02-c9.md`. They are recorded verbatim in substance, not paraphrased into
a softer form.

## The rulings

1. **Ship FEAT-48** at `review_sha 27f8105b`. **The review pin does not move.**
2. **All fifteen proposed backlog rows B-1 through B-15 are ACCEPTED.** None is struck.
3. **B-9 is a separate bug**, not FEAT-48 scope — the suite's dependence on `HARNESS_AGENT_TYPE`
   being unset (`test-plan-merge.py` fails 11 checks, in a file outside this diff).
4. **Source issue #1053 closes through the ship.**
5. **No backlog row is folded into this feature.** No code, plan or BRIEF change is authorised by
   these rulings, and none was made.

## What each ruling settles

- **Q1 (blocking) is CLOSED by ruling 1 + 2.** The traversal-permission blinding of
  `run_pool.py --mutation-check` (B-1) ships as an accepted, recorded limit rather than a
  pre-ship fix. The evidence that made that a legitimate choice, and not a shrug, is on disk: it
  fails no success criterion, and it reproduces identically at `b86ce66a`, `e64e863e` and
  `27f8105b`, so it is not a regression introduced by this cycle's fix
  (`notes/validate-evidence-c9.md` §2 and `notes/ship-review-2026-09-02-c9.md`).
  **B-2 travels with it**: `plan.yaml` D-11's "Inside DIR it is vector-agnostic" is measurably
  false and is corrected as its own backlog row, not here.
- **Q2 is CLOSED by ruling 3.** B-9 leaves this feature as its own bug. It is not adopted as an
  emergent success criterion, so "done" for FEAT-48 is unchanged.
- **Q3 is CLOSED by ruling 2 + 5.** Every remaining row — including B-6 (T-06's `verify:` clause),
  B-10 (the unre-signed `## Approval` block) and B-11 (issue #1053's stale `## Scope`) — is a
  backlog row. In particular **B-6 is not fixed here**, so T-06 keeps `status: done` behind a
  verify block that has never returned 0; that is now a recorded, accepted state rather than an
  unnoticed one.

## Consequences the ship phase owns

- Open the PR at the current tip; **`review_sha` stays `27f8105b`** and the code under review is
  unchanged by these rulings.
- File B-1 … B-15 as backlog issues on ship acceptance (DEC-138). The table with each row's nature
  is `notes/ship-review-2026-09-02-c9.md` under "Proposed backlog".
- Close issue #1053 through `gh-sync.py ship`. Its `## Scope` still reads "Folded into FEAT-47"
  (B-11); only the operator's hand can edit an issue body.
- B-9 becomes its own `BUG-NN`.

## What did NOT change

No source file, no `plan.yaml`, no `BRIEF.md`, no `review_sha`. The only writes this seam made are
the feature's own records: `STATE.md`, `feature.json`, `notes/handoff-validate.md` and this file.
