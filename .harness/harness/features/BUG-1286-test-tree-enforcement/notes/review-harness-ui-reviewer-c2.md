# UI Review — BUG-1286-test-tree-enforcement — Mode B — c2 (re-pin at bb3a31ed)

## Verdict
PASS. No rendered UI surface. Live re-execution at `bb3a31ed` (via HEAD `be46f5d4`, confirmed
byte-identical to `bb3a31ed` on every non-feature-directory path — `git diff bb3a31ed..HEAD --stat
-- . ':!.harness/harness/features/BUG-1286-test-tree-enforcement'` is empty, `suite_layout.py` diff
is 0 lines) shows the operator-facing text is unchanged from cycle 1. Both cycle-1 backlog notes
are re-confirmed present and unchanged; neither re-raised.

## 1. Enumeration — full diff at the re-pinned sha
`git diff --name-only "$(git merge-base origin/main bb3a31ed)" bb3a31ed` → **80 files** (up from
70 at cycle 1's pin; the extra 10 are this cycle's own process artifacts). Class breakdown:
1 guard module (`suite_layout.py`), 3 test files (`test-suite-layout.py`,
`test-run-unit-tests-layout.py`, `suite-census.py`), 3 doc files (`DECISIONS.md`,
`DECISIONS-INDEX.md`, `.harness/notes/grilling-*.md`), 3 feature-core (`BRIEF.md`, `STATE.md`,
`feature.json`), 1 `plan.yaml`, 4 observations logs, 65 feature `notes/` artifacts (research,
review, receipts, ship-review). Extension census for rendered-UI classes
(html/css/scss/tsx/jsx/vue/svelte/less): **one hit**, `notes/ship-review-2026-09-05-ship.html`.
Checked its content directly (`G-11`): it is new in this diff (93 insertions, no prior blob at
merge-base) but carries an explicit footer — `"the markdown is the record; do not edit this file.
Regenerate with bin/render-brief.py"` — a generated report artifact, not authored product UI.
Scoped out on that basis, not predicted.

## 2. DESIGN.md — still absent
`find .harness/harness/features/BUG-1286-test-tree-enforcement -iname 'DESIGN*'` → no hits, same
as c1. Mode A does not apply; this is a filesystem-invariant/CLI feature (repo-tier Expertise P-01).

## 3. Live tool execution at the re-pinned sha — matches the orchestrator's stated measurements
- `suite_layout.violations('.')` (direct call, post-decomposition `violations()` at line 203,
  delegating through `_unit_integration_findings` → `_runner_selection_findings` →
  `_bin_planted`/`_bin_planted_findings` → `_tracked_scan` → `_registry_findings`) → `[]`.
- `bash .claude/skills/harness/bin/run-unit-tests.sh --check-layout` → empty stdout/stderr, exit 0.
- `python3 tests/manual/suite-census.py tree-audit` → same 85-row body as c1 (9 `out-of-vocabulary`,
  1 `documented-exception`, 75 `in-tests-tree`), final line **`TOTAL 85 OUTSIDE 9 VIOLATIONS 0`**,
  byte-identical to the orchestrator's claim and to c1's recorded output.

All six message-string forms from c1's table were grepped directly against the decomposed source
and are byte-identical, now split across three helpers instead of one function:
`cannot enumerate tracked files under {root}: {error}` (`_tracked_scan`, was the single
`violations()`), `tracked test-shaped file outside tests/: {rel}` (`_tracked_outside_tests_findings`),
and the four `documented exception is …` forms (`_duplicate_or_malformed`/`_unnecessary_or_stale`/
`_entry_finding`). No wording drift, no reordering visible in the executed output (file findings,
sorted, then registry findings appended — matches c1's observed order).

## 4. The two c1 backlog notes — re-confirmed unchanged, NOT re-raised
- **`tracked test-shaped file outside tests/: {rel}`** names fact + path, no remedy verb. Still true
  at `bb3a31ed` (grepped, line 177 of the decomposed file). This string is a signed contract
  (`plan.yaml:559`, graded by literal list-equality) — a wording change is a plan amendment, not a
  code fix, and the spec is not on trial this cycle. **Assessed-and-dismissed, not a new finding.**
- **`tree-audit`'s `TOTAL 85 OUTSIDE 9 VIOLATIONS 0` has no legend** on the summary line alone.
  Confirmed unchanged by live execution above. Manual/review instrument, not a CI gate read
  line-by-line; low severity, unchanged from c1. **Assessed-and-dismissed, not a new finding.**

## Accessibility / theme parity / contrast
Not applicable, confirmed not assumed: both executed tools are plain stdout/stderr with zero ANSI/
colour escapes (unchanged from c1's grep). No colour-only state encoding exists on this surface.

## Not verifiable from this lens
No rendered surface exists in this diff to fail to verify; nothing withheld.

## Scope declined (looked, not predicted)
- `DECISIONS.md`/`DECISIONS-INDEX.md` — internal governance prose, not a product/CLI operator
  surface; left to code-reviewer's decision-graph lens, per c1.
- The D-03 ordering-precondition structural question (git-toplevel check before self-ownership
  test) named in the shared panel context is a control-flow/fail-closed correctness property, not
  operator-facing text or rendered UI — left to code-reviewer/qa, who can trace the call graph.
- B-3's discriminating-power probe (whether the new assertion can go RED against a second caller,
  and resists satisfaction by `notes/`/BRIEF.md prose) is a test-adequacy judgment, not a UI
  contract question — left to code-reviewer/qa.
