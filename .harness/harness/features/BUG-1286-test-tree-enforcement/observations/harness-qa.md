# Observations — harness-qa — BUG-1286-test-tree-enforcement

- 2026-09-05: T-04 SHA-correction (cycle 11): confirmed 4b343d8083d94d97477d3f2ebd7b848e83f01871 is the post-rebase ancestor twin of the orphaned 5f76d6b1… (same subject, both readable pre-prune); replaced only line 3 of qa-tree-audit.md; fence count unchanged 2/2; both --ref HEAD and --ref 4b343d80 --against runs exit 0 with no MISSING/EXTRA both before and after the edit.
- 2026-09-05: adjudicated the c2 integration-suite contradiction (qa reported clean, code-reviewer reported 15 FAIL in test-plan-merge.py). Root cause: test-plan-merge.py:139-141,1121-1124 uses env=None (ambient os.environ) by default for most run_verb() subprocess calls; a HARNESS_AGENT_TYPE left set in the invoking shell leaks into cases exercising sign-approval's main-session exemption and flips them to the governed-agent path, producing exactly 13-14 case FAILs (plus a run_pool.py double-print of the file's own summary line, unrelated latent bug). Reproduced at merge-base eb9d044e in a disposable worktree too — pre-existing, orthogonal to BUG-1286, not a regression. This corroborates repo-tier G-07 with a full mechanism trace rather than just the symptom.
