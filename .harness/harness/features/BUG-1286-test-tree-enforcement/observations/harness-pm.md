# Observations - harness-pm

- 2026-09-04: BUG-1286 — plan-merge.py apply DOES bootstrap a brand-new plan.yaml: with the base absent it refuses only a proposal that CARRIES an approval mapping (exit 8) and otherwise splices an approval mapping with status pending straight after the feature: line (plan-merge.py:602-630). My repository Expertise G-07 says to write a new plan.yaml directly; that is now wrong and cost a detour to verify. Sequence that worked: proposal with no approval and no status key, then apply, then set-feature-station --station plan.
- 2026-09-04: BUG-1286 — run-unit-tests.sh resolves its root through harness_boundary.resolve_root, which honours HARNESS_PROJECT_DIR, so a repository-wide predicate handed that root could enforce Harness rules on a product checkout. The gate I specified (the index must carry suite_layout.py itself) came from reading resolve_root, not from the ticket; the ticket's product-checkout-unchanged criterion looks satisfied by doing nothing until you trace where root comes from.
- 2026-09-04: BUG-1286 — the two existing layout fixtures build temp roots that are NOT git repos, so fail-closed-on-enumeration-failure and no-index-at-all must be different answers or the fixtures redden. Distinguishing them on the filesystem fact of a .git entry at the root avoids a chicken-and-egg detection through git itself.
- 2026-09-04: BUG-1286 c1 — the dispatch told me to write notes/plan-review-application-c1.md; check-domain denies it for harness-pm (grants only notes/research-*.md, notes/uat-*.md). Wrote notes/research-BUG-1286-plan-review-application-c1.md instead and said so in the artifact's first paragraph.
- 2026-09-04: BUG-1286 c1 — a dispatch asserted plan-merge `apply` unions by id "so re-proposing an existing T-NN with amended fields is how a value changes". False: apply raises exit 7 CONFLICT (plan-merge.py:729-743). Eleven field changes went through `amend --expect-sha256`; `_render_field` preserves `|` and `>-` form so folded prose and literal verify blocks both survive byte-shape.
- 2026-09-04: BUG-1286 c2 — a criterion whose second clause is graded at a different TIME than its
  first (behaviour at review time vs redness before the edit) is always two criteria: no single
  verify method reaches both. Splitting it named a grader (qa's test-first audit) that already
  exists rather than inventing evidence.
- 2026-09-04: BUG-1286 c2 — renumbering a BRIEF's SC list was free because tasks trace REQ only
  (grep `SC-` over plan.yaml returned nothing); the only cross-reference to fix was a prose mention
  inside the BRIEF's own Verification gaps section. Grep the artifact for its own ids before
  assuming a renumber is contained.
- 2026-09-04: BUG-1286 goal-check c2. A gap "closed" by adding the missing assertion can leave the criterion still ungradable when the criterion carries TWO clauses with different subjects: SC-06's clause B (no finding names the manual file) got its assertion, while clause A (that fixture produces no violations) is false by construction on the same fixture, which plants a rogue deliberately. Grade each clause against its own fixture, not the criterion as a unit.
- 2026-09-04: re-grading a plan after an SC renumber (15->17) invalidated every c1 BRIEF line citation; re-resolving anchors by grep on `^- SC-` was cheap and caught that only the plan.yaml anchors survived intact (three `amend` splices moved no line I had cited).
- 2026-09-04: BUG-1286 — a plan-level exact-equality assertion over a predicate's return value must account for MODULE-LEVEL registry state, not just the fixture's files: suite_layout's DOCUMENTED_EXCEPTIONS self-policing clause fires on every fixture that does not track the seeded FEAT-44 path, so the specified case-1 call returned 2 findings, not 1. Caught only by prototyping the spec against the real fixture; reading the intent prose alone read as satisfiable.
- 2026-09-04: BUG-1286 — before amending a criterion to a stronger form, check whether the stronger form subsumes a sibling clause: exact list equality made 'no finding names X' unfailable-while-true, so the split the goal-check contemplated had nothing to split and SC-07..SC-17 kept their numbers (and every prior note's citations).
- 2026-09-04: set-panel accepts extra keys past its four required ones (severity_max, transcription_rule, cross_references, adequacy_notes all survived the re-parse refusal check), and with no existing panel key it splices the whole mapping immediately BEFORE tasks:, so the diff is added-only and no task line moves. It also safe_dumps the value, so the scratch file's own quoting is irrelevant to the on-disk form — but a summary containing ' #1286' MUST still be quoted in the scratch file or safe_load truncates it there before the tool ever sees it.
- 2026-09-04: BUG-1286 — plan-merge.py set-panel dumps the panel with yaml.safe_dump width 80 and offers no style control, so a summary containing ' #<digit>' wraps into a multi-line single-quoted scalar and trips check-state.sh INV-35 (the scanner's own documented unreached gap, check-state.sh:192-199). Cleared it by driving set-panel from a throwaway script that registers a SafeRepresenter override forcing style='|' on that one value; set-panel's reload-equality check then proves the decoded panel is unchanged. A literal block scalar is never wrapped by PyYAML, so it is the form INV-35 is built to skip.
- 2026-09-04: BUG-1286 — a guard vocabulary NARROWER than the kind map detect globs it is meant to enforce is the same defect class the guard exists to close, one size down; closing it from the guard side (two pattern groups, one extension policy each) left harness.json frozen and was measured inert (0 tracked agnostic-pair matches outside tests/ at c040c319). Measured with git ls-files + fnmatch before writing the amendment, per dispatch; ls-tree HEAD agreed.
- 2026-09-04: plan-merge.py amend --show TRUNCATES the printed value to ~768 chars but its sha256 is over the whole field, so the compare-and-swap is safe even when you cannot see the value you are replacing. Amend the same field twice in one session by re-running --show for the new sha.
- 2026-09-04: BUG-1286 c4 goal-check — re-measured the census with 'git ls-tree -r --name-only HEAD' instead of the amendment note's 'git ls-files' to keep the check independent of the pipeline being verified; both give TOTAL 85 / OUTSIDE 9 at c040c319, and the agnostic pair matches exactly one tracked path repo-wide (tests/unit/omp-hooks.test.ts, inside the tree) which doubles as a positive control that the widened shapes are not vacuous.
- 2026-09-04: BUG-1286 cycle-4 panel transcription. Generating the set-panel value file by loading the digest's own embedded ```yaml block (rather than retyping summaries) makes the "wording preserved, wrapping collapsed" rule mechanical - PyYAML plain-scalar loading already collapses the wrap, so the stored string is provably the digest's own.
- 2026-09-04: INV-35 did not fire on this write. The trigger is narrow - a panel scalar containing space-hash-digit that safe_dump(width=80) wraps so the hash lands at a line start. Checking every scalar for " #<digit>" before writing predicts it without running check-state.
- 2026-09-04: check-state.sh output is ~885 lines here; grepping for "^  VIOLATION" and "INV-35" is the only tractable read. Exit 1 is expected while any feature has an unsigned BRIEF and says nothing about this feature.
- 2026-09-04: git diff was the WRONG isolation baseline for a plan.yaml write - HEAD predated the operator's amendment, so it showed 202/132 lines. A copy taken immediately before the merge call is the only baseline that attributes only my hunk to me.
- 2026-09-04: BUG-1286 amend — plan-merge amend --value-file re-renders a folded '>-' field from whatever the value file holds, and yaml.safe_load hands back the FOLDED value as one long line, so a round-trip through safe_load collapses a wrapped 'because' into a single 2100-char line. Re-wrap with textwrap before amending, then reload and confirm the value still carries 0 newlines.
- 2026-09-04: BUG-1286 T-01 case 11 shipped `"test_*.py" -> strips to "test_.py"` under a rule that says `segment.strip("*?")`; strip removes only leading/trailing chars so the segment is unchanged. The verdict (IN) and the rule were both right, which is why three reviewers and a panel passed over it — a worked example is only checked if someone runs it. Run every worked arithmetic in an intent through the actual operation named.
- 2026-09-04: amending a mid-sentence substring inside a hand-wrapped literal block joins the tail of the original wrap onto the new last line (142-char line here). After any plan-merge amend on a prose field, re-read the amended region and rewrap; the second amend needs a fresh --show sha.
- 2026-09-04: BUG-1286 c5 goal-check. An assertion that partitions a glob list and asserts "exactly N out of scope" catches ADDITIONS to the list but not SUBSTITUTIONS of the out-of-scope member. Case 11's directory-only branch is out of scope on the argument that such a glob cannot name a file outside tests/ - true only because today's one such glob is rooted inside tests/. Emulated the specified predicate over six candidate detect values to find it; the mutant the plan advertises (**/*.spec.* to both files) is genuinely red.
- 2026-09-04: plan-merge.py amend replaces a WHOLE field, so a one-paragraph edit inside a 190-line
  intent means reproducing the other 185 lines. Retyping is where the wrap corruption comes from;
  instead generate the replacement in python from the safe_load'ed intent (locate the region by line
  prefix, splice the new block, write to stdout) and pipe it into `--value-file /dev/stdin`. No temp
  file, nothing to delete, untouched text is byte-identical by construction.
- 2026-09-04: an "exactly one out of scope" count assertion catches ADDING an exemption but never
  SUBSTITUTING for the existing one. When a partition excuses a bucket, assert a property of the
  excused member (here: its literal prefix is inside tests/), not just how many members it has.
- 2026-09-04: panel transcription c6 — a dispatch naming `notes/review-plan-panel-c6.md` for pm is denied by check-domain (review-* is the validator lead's path); the durable digest copy went to my own `notes/research-<FEAT>-panel-c6.md` grant, as at cycle 4.
- 2026-09-04: plan-merge.py has a dedicated `set-panel --value-file` verb that REPLACES the top-level panel mapping wholesale and re-verifies it reloads equal — the right route for a supersession, where `apply` would union old finding ids in.
- 2026-09-04: `harness_yaml` lives in `.claude/skills/harness/bin/`, not a `lib/` directory; sys.path must point at bin.
