# Observations - harness-orchestrator

- 2026-08-25 (FEAT-40 plan): measured the inbound reference graph for the three decisions being struck. Only ONE decision outside the struck set cites them: DEC-200 @6824 refs DEC-186 and DEC-196 (DECISIONS-INDEX.md:218). DEC-196's own refs are internal to the set plus DEC-138/174/188. So the citation-resolution obligation under DEC-188 is exactly DEC-200 plus DEC-138 am.7. Check pm's plan covers DEC-200 — it is not on the operator's constraint list and postdates the grilling.
- 2026-08-25 (FEAT-40 plan): github-mirror.md carries two statements this feature must reverse — the station table's Done row 'The harness writes this column never', and the close-task row's D-23 rationale 'a closed sub-issue cannot sit at Review', which the operator's constraint 11 says is measurably false. Both are quotable anchors for a plan task.
- 2026-08-25: I have no SendMessage tool in this host, so a mid-run course correction to a lead is not available to me — observations plus a post-return check is the only route. I burned one spawn discovering that by reaching for Agent instead.
- 2026-08-25 (FEAT-40 plan): re-measured constraint 9's premise myself at dispatch time. core.hooksPath on /Users/molchairuangutai/GitHub/harness is /Users/molchairuangutai/GitHub/harness/.git/hooks and that dir holds ONLY .sample files. The tracked hook is .claude/skills/harness/hooks/post-merge — that is the path constraint 10's new invariant must resolve core.hooksPath against. Premise holds; the grilling's fact is confirmed, not merely inherited.
- 2026-08-25 (FEAT-40 plan): baseline for the acceptance SC, measured by me: #728 state OPEN closedAt null; #842 state OPEN. If either is closed before ship runs, the end-to-end test has been contaminated by a hand close and the SC no longer discriminates.
- 2026-08-25 (FEAT-40 plan): surface map I measured at cc84b29 for spot-checking pm's plan. gh-sync.py holds NINE parent_origin sites in six regions: 431 (record init), 477-478 (normalize), 671 (emit), 742/751 (adopted/created recording in open), 1001+1009 (abandon's parent branch), 1080+1088 (ship's parent branch). cmd_closes is def'd at 1039, dispatched at 1156-1162, and documented in the usage string at 11-12. A plan claiming fewer than nine parent_origin sites has missed one.
- 2026-08-25 (FEAT-40 plan): VERIFIED constraint 11 myself against board 3. Fifteen cards sit at the Review station: #728 OPEN, #817 OPEN, and #818-#830 all CLOSED — thirteen closed sub-issues sitting at Review right now. D-23's claim that a closed sub-issue CANNOT sit at Review is false, measured, not inferred. #817 open + 13 closed = the 14 stranded FEAT-34 cards the grilling names.
- 2026-08-25 (FEAT-40 plan): the acceptance ticket #728 currently sits at the Review station, NOT Backlog. Any SC asserting 'lands at Done' must state Review as the starting station or it cannot be shown to discriminate.
- 2026-08-25 (METHOD, cost me two calls): 'gh project item-list --format json' content objects carry ONLY body/number/repository/title/type/url — there is NO state field. Filtering on state there silently yields zero matches and looks like a real finding. Get the station from item-list, then the open/closed state from 'gh issue view --json state', as two steps. Also: --limit defaults low enough to truncate; board 3 has 639 items.
- 2026-08-25 (FEAT-40 plan): check-state.sh's highest existing invariant is INV-30 (26 defined, with 2/11/14/20 absent). Constraint 10's hook-install invariant should therefore be INV-31; if the closed-but-not-Done leak invariant is also planned it is INV-32. A plan reusing an existing number is a collision I can catch by grep.
- 2026-08-25 (FEAT-40 plan): the PreToolUse:Bash chain already holds TWO gates — branch-create-gate.sh then bash-write-guard.sh (.claude/settings.json). Constraint 5's gh-issue-close gate is a third entry in that same chain or a branch inside one of them; it is not a new hook event.
- 2026-08-25 (FEAT-40 plan): NO dual-edit trap here, measured. .agents/skills/harness/bin/ and .claude/skills/harness/bin/ resolve to the SAME INODE (169397084 for gh-sync.py) — one file, two paths, not two copies to keep in sync. A plan task that edits one and 'mirrors' to the other would be doing nothing twice.
- 2026-08-25 (FEAT-40 plan): confirmed the sweep chain myself. post-merge-sweep.sh:174-175 runs 'gh-sync.py ship <feat_dir>'; the tracked hook .claude/skills/harness/hooks/post-merge is mode 755, already executable. So the ONE broken link is core.hooksPath, nothing else — that is the whole content of the 'prove the sweep fires' task.
- 2026-08-25 (FEAT-40 plan): RISK the grilling does not name. post-merge-sweep.sh:173 orders ship FIRST then worktree removal, and :188-190 skips removal only when ship's RETURNCODE is non-zero. But github-mirror.md states a station-write failure mid-run prints one stderr line, CONTINUES, and 'the exit status is still 0', and nothing is ever re-attempted. This feature makes ship write Done to every recorded card, so a batch that half-fails exits 0, the worktree is removed, and cards are left stranded with no gate tripping — the exact FEAT-34 failure, re-created by the fix. Either ship's Done writes stop being best-effort under DEC-146, or the sweep stops trusting returncode. Check whether pm's SCs or eng-lead's arch review cover it; if neither does, it is a fix cycle.
- 2026-08-25 (FEAT-40 plan): the Done-writes-close-the-issue direction is STILL NOT VERIFIED, and probe #807 cannot verify it. #807 is titled 'PROBE: does moving a card to Done auto-close the issue?' and is CLOSED, but its timeline shows exactly one close event, actor=mruangutai, state_reason=None. That is the SAME signature the grilling uses to identify a HAND close. The record cannot distinguish the project workflow from the operator's own hand, so it corroborates nothing. The whole feature rests on this direction.
- 2026-08-25 (FEAT-40 plan): the grilling's discriminator is weaker than stated. 'reason=null therefore closed by hand' only rules out close-task, which always passes --reason completed (gh-sync.py:855). GitHub's built-in project workflows are attributed to the project owner with no state_reason, so reason=null + actor=mruangutai is consistent with BOTH a hand close AND a workflow close. Anywhere the plan leans on 'every close on record was by hand', that inference needs a second discriminator.
- 2026-08-25 (FEAT-40 plan): DISPROVED the grilling's discriminator with a controlled case. I sampled the close event on all 13 recent Done-station issues (775,776,777,798-802,814-816,836,840): EVERY ONE reads actor=mruangutai state_reason=None. That set includes #840, whose cause is independently known — PR #841 merged 2026-08-25T14:45:01Z carrying 'Closes #840', and #840 closed at 14:45:02Z, one second later. A PR-keyword close is attributed to the PR author with a null reason, so the signature is IDENTICAL for a hand close, a Closes-keyword close, and a project-workflow close. Grilling fact 2 ('every sub-issue close on record was by hand, all read by mruangutai reason=null') is NOT supported by the evidence it cites.
- 2026-08-25 (FEAT-40 plan): the hand-close conclusion may still survive on its OTHER leg — the grilling verified PR #837 carried no Closes lines at all, so FEAT-34's sub-issue closes cannot be explained by keywords. That leg is independent of the broken actor/reason discriminator. Anywhere the plan cites the actor/reason signature as evidence, it must switch to the PR-body leg or re-derive.
- 2026-08-25 (FEAT-40 plan): CONFIRMED the surviving leg myself. PR #837 carries no Closes lines at all and merged 2026-08-25T13:55:59Z, while FEAT-34's sub-issues closed much earlier — #818 at 00:44:08Z, #819 at 00:44:10Z (two seconds apart), #830 at 04:00:55Z. Closes before the merge, in tight bursts at two separate sittings, cannot be keyword closes. The hand-close DIAGNOSIS holds; only the actor/reason EVIDENCE for it was invalid. Cite the timing-and-PR-body leg, never the signature.
- 2026-08-25 (FEAT-40 plan): DECIDED NOT to run the Done-writes-close probe myself, and the reasoning matters. The decisive bounded test exists — move #807 (an existing throwaway probe, already at Done, already closed, titled 'delete after') off Done and back onto it and watch whether GitHub re-closes it, creating no new artifact. I declined on four grounds: it is a live-board mutation my dispatch did not ask for; pm may be authoring exactly this verification task right now and I would be duplicating it; the plan already needs a 'prove the sweep fires' task for this premise to sit beside; and the operator scoped acceptance to the shipped change closing #728 'with nobody touching GitHub'. Surfaced as the top risk instead. If it comes back unplanned, this probe is the cheapest way to close it.
- 2026-08-25 (FEAT-40 plan): FOUND A DECISION NOBODY NAMED — DEC-168 @4353, 'Sub-issue closure does not cascade, in either direction (measured)'. It is not in the operator's eleven constraints and not in the grilling's decision set, yet it is directly load-bearing twice over. First, its index summary reads 'a parent is closed deliberately at ship acceptance', and DEC-186 (one of the three being struck) refs it, so the rewrite has a fourth inbound edge beyond DEC-200.
- 2026-08-25 (FEAT-40 plan): DEC-168's SECOND bearing is the sharper one and it is a measured hazard for constraint 3. DECISIONS.md:4372 records 'sub_issues_summary is eventually consistent — it read total:1 immediately after the second attach and corrected to total:2 seconds later.' Constraint 3 makes ship SKIP any card that still has an open child. If that check reads sub_issues_summary, a stale read either skips a parent that was ready or moves one whose child is still open — and per github-mirror.md nothing is ever re-attempted and ship still exits 0. The plan needs its open-child check to name its data source and its staleness handling.
- 2026-08-25 (FEAT-40 plan): DEC-168's measured table at DECISIONS.md:4359-4362 does NOT contradict the new rule and must not be struck with the others. It measures GITHUB's native non-cascade, which is precisely WHY the harness has to compute 'no open children' itself. Only its prose 'closure is entirely manual in both directions' goes stale. Do not let a rewrite over-strike it.
- 2026-08-25 (FEAT-40 plan): GOOD NEWS that de-risks constraint 3, measured. gh-sync.py does NOT read sub_issues_summary anywhere — the parent's station is derived from plan.yaml task statuses via gh_board.derive_station, i.e. from local disk. So DEC-168's eventual-consistency gotcha does NOT bite the plan-derived children. And for arbitrary cards the machinery already exists: gh_board.board_stations + gh_board.read_station, already used by start-task (gh-sync.py:821-822) and documented at :804 as 'ONE board read... cost-1'. Constraint 3's open-child test can reuse that, once per ship invocation, not once per card.
- 2026-08-25 (FEAT-40 plan): the read-back authority question is already answered, do not let it become a blocker. Constraint 1 makes 'open' a STATION fact, so the open-child test is a board read — but DEC-186 already bounds read-back to five purposes with stations among them, and start-task's existing station read is the precedent. The replacement decision should carry that licence forward explicitly rather than the plan inventing new authority.
- 2026-08-25 (FEAT-40 plan): BRIEF.md cites DEC-138/146/164/174/186/188/192/196 and does NOT cite DEC-168 or DEC-200 — the two inbound edges I measured. DEC-200 refs both DEC-186 and DEC-196, so the rewrite must keep its citations resolving under DEC-188. DEC-168's index summary reads 'sub-issue closure is manual in both directions... a parent is closed deliberately at ship acceptance', which goes stale the moment ship writes Done automatically from child state. Neither is fatal to the approach; both are unstruck text that will contradict the new decision. Check plan.yaml before flagging — pm may cite them there.
- 2026-08-25 (FEAT-40 plan): pm INDEPENDENTLY found the same best-effort/exit-0 risk I did, cited to DEC-146 and github-mirror.md:59-70, and routed it to the architecture review with a default rather than deciding it. Two contexts reaching the same finding from different directions is the strongest signal I have that it is real.
- 2026-08-25 (FEAT-40 plan): pm's two corrections both overturn operator premises and MUST ride my return. (a) #728 has THIRTEEN children (#818-#830), not none as the dispatch stated — so the acceptance test exercises the open-child path, not the trivial one. (b) Constraint 5's environment marker CANNOT work: a PreToolUse:Bash hook is handed tool_input.command only (branch-create-gate.sh:47), and gh-sync.py reaches gh through subprocess, which never traverses the Bash tool — so no marker set inside gh-sync.py can reach the gate. Refusing every 'gh issue close' unconditionally delivers the named outcome because abandon's close is a subprocess, not a Bash call.
- 2026-08-25 (FEAT-40 plan): I VERIFIED pm's gate correction myself rather than relaying it. branch-create-gate.sh:49 pulls only tool_input.command from the hook payload, and gh-sync.py:147 reaches gh via subprocess.run([GH]+args) — a direct child process that never traverses the Bash tool, so no PreToolUse:Bash hook fires for abandon's close at all. The exemption marker is not merely hard, it is unnecessary: the gate cannot see the call it was meant to exempt. Unconditional refusal is correct. A hand close typed as 'gh issue close 728' IS a Bash command and IS refused; 'python3 gh-sync.py abandon <dir>' contains no such substring and passes. The mechanism the operator named is impossible and the outcome the operator named is delivered.
- 2026-08-25 (FEAT-40 plan): CORRECTING MY OWN EARLIER ENTRY. I guessed #817 was FEAT-34's parent and that 817+13 made the 14 stranded cards. Wrong. Verified: 'gh api repos/mruangutai/harness/issues/728/sub_issues' returns exactly 13, #818-#830, ALL CLOSED — so #728 is the parent and the 14 is #728 plus its 13 subs. #817 is a separate card at Review. My inference was unverified when I wrote it; the API call settles it.
- 2026-08-25 (FEAT-40 plan): the sharpest consequence of constraint 1, and pm has it right. All 13 of #728's children are CLOSED on GitHub but sit at the Review station, so under the new definition — open while not at Done — #728 has THIRTEEN OPEN CHILDREN. The acceptance test therefore exercises the skip-and-then-move path, not the trivial no-children path, and it only passes because SC-03 makes ship write the children's cards BEFORE it evaluates the parent. The operator's dispatch said #728 has no children; that is the premise most worth correcting upward.
- 2026-08-25 (FEAT-40 plan): constraint 10 is less new than it looks. FEAT-34 T-13 already shipped test-hooks-install.py (in run-unit-tests.sh's INTEGRATION_SCRIPTS), which grades harness-init/SKILL.md's per-clone step and asserts its two command strings appear byte-for-byte in the skill. The canonical target value is the RELATIVE path '.claude/skills/harness/hooks'. There is NO installer script — the setup is prose only, which is exactly why an already-onboarded clone never re-runs it. So the new invariant has a defined target and existing test precedent to copy.
- 2026-08-25 (FEAT-40 plan): implementation subtlety for the new invariant that nobody has named. The tracked target is RELATIVE ('.claude/skills/harness/hooks') but 'git config --get core.hooksPath' on this clone prints an ABSOLUTE path. The invariant must compare resolved paths, not strings, or it reports a false violation on a correctly-configured clone. Worse, core.hooksPath lives in the COMMON config shared by every worktree, and a relative value resolves against the current top-level — so the same config is correct in the main clone and points somewhere else from inside .claude/worktrees/<repo>/<FEAT>. Whether the invariant runs in a worktree changes what it should assert.
- 2026-08-25 (FEAT-40 plan): CORRECTING MY OWN OVERSTATEMENT from the entry above. I claimed a relative core.hooksPath 'points somewhere else' from inside a worktree. Measured: core.hooksPath is shared through the common config (.git/config) — the worktree and the main clone both read the same value — and git resolves a RELATIVE hooksPath against the current working tree's top-level. The tracked hooks dir with an executable post-merge exists in BOTH (verified in this worktree). So the relative form resolves to a different COPY OF THE SAME TRACKED FILE in each tree, which is correct, and is a positive argument for the relative form over an absolute one that would pin every worktree to the main clone. The only real subtlety left for the invariant is that the configured value may be absolute while the tracked target is relative, so it must compare RESOLVED paths, never strings.
- 2026-08-25 (FEAT-40 plan): the coordinator's TWO additions were already largely in the plan before they arrived. Addition 1 (rewrite github-mirror.md) is T-09 item A, which enumerates ten edits covering ALL NINE lines the coordinator listed (:28 :30 :32 :33 :36 :43-46 :54-57 :84 :90-91) plus a DEC-203 re-citation. Addition 2 (the sixth read-back purpose) is BRIEF.md:163, plan D-02 lines 46-47, D-?? line 66 'widened to six purposes', decision text at 224-225 bounded to gh-sync.py ship with the no-approval-artifact clause preserved, and T-04 implements it via gh_issues.sub_issues_args. pm found both independently.
- 2026-08-25 (FEAT-40 plan): what the additions DO leave uncovered, verified by me, three items. (1) github-mirror.md:7-9 'The one read-back is record-pr ... Nothing else reads GitHub state back' is NOT in T-09's A-list and addition 2 falsifies it — and it is ALREADY false at cc84b29, because start-task performs a board station read (gh-sync.py:804 'ONE board read', :821-822). So it is a correction plus an update. (2) T-09 A.5 only deletes the 'cannot sit at Review' clause; the coordinator asks that the same cell's 'close-task remains' sentence be RE-DECIDED, not reworded. (3) optional record correction at :84 — that row is false today for a second reason, cmd_closes emits Closes #N only for source_issues, never task sub-issues.
- 2026-08-25 (FEAT-40 plan): VERIFIED the coordinator's justification for the sixth read-back. 'gh api repos/mruangutai/harness/issues/615/sub_issues' returns exactly two children, #78 and #438, BOTH OPEN — low numbers the harness never created and feature.json cannot know about. That is the concrete proof the open-child test cannot be answered from local state. One reconciliation left: D-02 prices the read PER CARD, the coordinator prices it PER PARENT PER SHIP. Reading children of every card costs materially more than reading children of parents only.
- 2026-08-25 (FEAT-40 plan): VERIFICATION HOLE in T-09, measured. Its verify is one grep over three files for 'parent_origin|Closes #|closes the milestone', passing when the grep exits 1. At cc84b29 the counts are github-mirror.md 5, commands/harness.md 1, harness-init/SKILL.md ZERO. So the grep discriminates for parts A and B but is completely blind to part C, which adds an INV-31 sentence to harness-init/SKILL.md — that deliverable passes whether or not it is done. T-09's verify needs a second clause asserting INV-31 appears in harness-init/SKILL.md. pm's intent text also says 'all three strings are present in these files', which is true of the SET and false of each file; the loose wording is what hides the hole.
- 2026-08-25 (FEAT-40 plan): RAN check-state.sh myself, exit=1, and it names FEAT-40 twice. (a) 'BRIEF.md is NOT approved — halt that flow and surface to the user' — this is the EXPECTED plan-phase state, not a defect; the checker is doing its job and the answer is the operator's signature, not an edit. (b) 'plan.yaml has no approval: block — cannot tell if the goal is signed' — this one is REAL. Every other feature's plan.yaml carries a top-level approval: near line 4; FEAT-40's has none, so the only 'approval' string in the file is prose at line 225. FEAT-19 and FEAT-28 get a mere NOTE because they HAVE the block set to pending. check-state.sh:134-136 makes a missing block a violation while :139 makes pending a warning.
- 2026-08-25 (FEAT-40 plan): the missing approval: block cannot be fixed by anyone in the run. team-config.yaml grants '.harness/*/features/*/plan.yaml approval:' to main_session ONLY, check-domain.sh denies an agent's write of that mapping, and my own playbook forbids me writing it because it records a signature only the main session can have been given. So adding 'approval:\n  status: pending' is a MAIN SESSION action and belongs in my return, not in a fix cycle to the lead. FEAT-34's plan.yaml carries a comment saying exactly this.
- 2026-08-25 (FEAT-40 plan): check-state.sh also caught something of MINE — 'run dir 2026-08-25-01-product exists on disk but feature.json does not record it — orphaned work'. I create the run dir's parent at dispatch but only record the run when the lead returns a verdict, so the window between dispatch and return is always red on this invariant. Worth knowing before reading a mid-run check-state as evidence of a defect.
- 2026-08-25 (FEAT-40 plan): the lead's F-2 is the best finding of the run and the plan already answers it. If ship's open-child test read the ISSUE's open/closed field it would race the ~10s auto-close workflow — write Done on the children, immediately evaluate the parent, see the children still OPEN, and skip the parent on every normal feature every time. T-04 step 5b pins the STATION reading ('a child counts as OPEN when its card is not at the done station') and step 5 runs children before parents, so the race cannot occur.
- 2026-08-25 (FEAT-40 plan): one sub-claim in the lead's F-2 is STALE and must not travel — it says '#728 does not exercise this... it has no children'. The lead wrote F-1 to F-3 before its members returned, from my dispatch's premise. pm then measured 13 children. #728 DOES exercise the open-child path. If the team digest repeats the no-children claim, correct it against pm's measurement rather than letting both stand.
- 2026-08-25 (FEAT-40 plan): the lead's F-3 is a small real gap not yet in any task. gh-sync.py:218-221's terminal exemption in _apply_parent_rule explains itself as 'ship already closed the parent and GitHub's own Item closed workflow landed it in Done'. This feature reverses that causality; the BEHAVIOUR is unaffected because the exemption keys on local feature status, but the stated reason becomes false. Fold the comment fix into T-04 or T-05, whichever ends up touching the parent rule.
- 2026-08-25 (FEAT-40 plan): eng-lead's arch review answered the best-effort question BETTER than either pm's default or my own framing, and the lesson generalises. Neither of us looked for an EXISTING compensating control. post-merge-sweep.sh:180-195 already declines worktree removal by grepping ship's combined output for the literal 'gh-sync: SKIP', explicitly because an exit code is never positive evidence. The fix is to extend that string gate to 'gh-sync: not moved to' — a partially-failed ship then leaves a worktree the operator trips over. Change the READER, not the writer; keep DEC-146's per-card posture and exit 0. I had been reasoning about exit codes and never asked what already reads ship's output.
- 2026-08-25 (FEAT-40 plan): eng-lead found a SECOND ordering constraint of the same family as the operator's mandated one, and the operator named only the first. T-07 must depend on T-02: otherwise the legal order T-01,T-04,T-05,T-07 installs the gate that refuses a hand-typed close while the merge sweep is still uninstalled — no automatic close path and no manual one. When a plan removes a manual fallback and an automatic path in the same feature, EVERY task that removes a fallback needs an edge to the task that proves the automatic path works, not just the one the operator noticed.
- 2026-08-25 (FEAT-40 plan): eng-lead's sharpest catch is a fail-open GATE. T-01's verify line 1 pipes board_lifecycle.py audit into grep and tests $?, which is GREP's status, while audit exits 4 on any GhError (board_lifecycle.py:120-121). So a network failure makes the clause pass — and that clause is what authorises every later deletion in the feature. T-08 verify line 3 has the identical shape. A verify built as 'command | grep X ; test $? -eq 1' is fail-open by construction whenever the command can fail; capture once, assert the command itself succeeded and produced output, THEN assert the string is absent.
- 2026-08-25 (FEAT-40 plan): I WAS WRONG to tell the lead to scope visual-designer and ui-reviewer out, and it reversed me correctly. My reasoning was 'no user-facing UI surface', but the operator's own constraints mandated four operator-facing STRINGS — ship's held-open line, abandon's report-and-ask flow, the gate's refusal naming the sanctioned command, and the invariant's report line. FEAT-19 is the precedent: CLI-only, no graphical surface, still ran the designer plus three ui-reviewer cycles, because this repo treats operator-facing text as a designed artifact. Test for the scope-out is 'does the change author text a human reads and acts on', not 'is there a GUI'.
- 2026-08-25 (FEAT-40 plan): the design pass paid for itself with a SAFETY finding no other reviewer had. SC-07 requires only that the gate's refusal contain the substring 'gh-sync.py abandon'. Text satisfying that literally routes an operator whose work is FINISHED toward abandon, which closes not_planned and labels the ticket abandoned (gh-sync.py:986-991) — the destructive branch. The design contract puts the finished branch FIRST with the answer 'type nothing, ship writes Done at the merge', makes the abandon line a pasteable command rather than a name, and adds an untracked-issue escape because the gate cannot tell tracked from untracked. A criterion phrased as a substring check invites exactly the text that satisfies it and harms the reader.
- 2026-08-25 (FEAT-40 plan): THE SUITE IS RED BEFORE THIS FEATURE STARTS, and five task verifies depend on it being green. I ran run-unit-tests.sh --kind all at cc84b29 in the worktree: 20 scripts PASS, SIX FAIL — test-validate-feature-json.py, test-layout-migration.py, test-inject-expertise.py, test-factory-config.py, test-branch-create-gate.py, test-board-lifecycle.py. run-unit-tests.sh:145-149 exits 1 if ANY script fails, so 'run-unit-tests.sh --kind all' exits 1 today. T-04, T-05, T-06, T-07 and T-08 all open their verify with exactly that command, so none of them can pass however well the work is done.
- 2026-08-25 (FEAT-40 plan): PROVED the six failures are pre-existing and not mine, not the worktree's. I re-ran four of them in the MAIN clone at main (92b3fec), outside the worktree, with HARNESS_PROJECT_DIR pointing at the main clone: test-validate-feature-json.py, test-inject-expertise.py, test-factory-config.py and test-layout-migration.py all exit 1 there too, and test-branch-create-gate.py reports '5/8 cases passed'. My instantiating FEAT-40's feature.json did not cause any of them.
- 2026-08-25 (FEAT-40 plan): WHAT I HAVE NOT ESTABLISHED about the red suite, stated so nobody over-reads it. I did not diagnose WHY each script fails, I did not confirm all six fail at cc84b29 specifically (the main-clone re-runs were at 92b3fec), and the worktree run may have been truncated before the last few scripts, so six is a FLOOR. The plan's options are to repair or quarantine the pre-existing failures as their own task, or to narrow each verify to the specific test scripts that task touches. That is pm's call, not mine.
- 2026-08-25 (FEAT-40 plan): RAN board_lifecycle.py audit --repo mruangutai/harness myself, read-only, exit 1. It emits NO WORKFLOW finding class at all — 2 LABEL, 13 STATION, 1 STATUS and nothing else — so the three named project workflows including 'Auto-close issue' are all detected PRESENT on board 3. That is strong positive evidence for the feature's central premise, and it means T-01's hard-stop branch is unlikely to fire. The caveat the tool prints itself still stands: detection matches by NAME only, because ProjectV2Workflow exposes neither trigger nor action, so a renamed workflow reads as missing and a DISABLED one with the right name may read as present. T-01's live probe is still the thing that settles it.
- 2026-08-25 (FEAT-40 plan): the audit MEASURES pm's reason for declining a new closed-but-not-Done invariant. Thirteen STATION findings come back, which is exactly FEAT-34's 13 stranded sub-issues — so board_lifecycle.py audit already detects that leak today and a second detector in check-state.sh would be two rules for one fact. pm's argument was that what is missing is a RUNNER, not a detector, and the audit output bears that out.
- 2026-08-25 (FEAT-40 plan): FIRMED UP the red-suite finding after routing it. A complete '--kind unit' run gives 18 PASS, 6 FAIL, runner exit 1 — and the six are exactly the ones I first reported (test-factory-config, test-validate-feature-json, test-branch-create-gate, test-layout-migration, test-inject-expertise, test-board-lifecycle). So all six live in the UNIT bucket and the set is complete for that bucket, not a floor. Consequence for the plan: narrowing the five verifies from '--kind all' to '--kind unit' would NOT help, because --kind unit exits 1 on its own. The integration bucket was still running and its failures, if any, are additional.
- 2026-08-25 (FEAT-40 plan): the red-suite count is SEVEN, not six, and one of them is the test that grades this feature's own hook-install surface. test-hooks-install.py (INTEGRATION bucket) exits 1 in the worktree AND in the main clone. I nearly recorded its failure as an artifact of my own invocation: the worktree run's message named MY worktree path, because the scratch clone's post-merge sweep inherited the HARNESS_PROJECT_DIR I had set, and feature-worktree.py remove then resolved the scratch worktree against the wrong root. That reasoning was plausible and WRONG — the main-clone run fails too. Running the discriminating check instead of accepting the plausible explanation is what caught it.
- 2026-08-25 (FEAT-40 plan): final accounting of the red baseline, with confidence marked per script. Reproduced in the MAIN CLONE, so genuinely pre-existing: test-validate-feature-json.py, test-inject-expertise.py, test-factory-config.py, test-layout-migration.py, test-branch-create-gate.py (5/8 cases) and test-hooks-install.py. NOT re-run in the main clone: test-board-lifecycle.py, which queries the live board and is red for board-state reasons my own audit run corroborates. '--kind unit' alone gives 18 PASS 6 FAIL exit 1, so narrowing the five verifies from all to unit does not rescue them.
- 2026-08-25 (FEAT-40 plan): confidence gap CLOSED — test-board-lifecycle.py also exits 1 in the main clone, so ALL SEVEN failing scripts are reproduced outside the worktree with HARNESS_PROJECT_DIR pointing at the main clone. Nothing in the red baseline is an artifact of the worktree or of FEAT-40.
- 2026-08-25 (FEAT-40 plan): the red baseline is EIGHT scripts, all reproduced in the main clone, and the pattern matters more than the count. test-post-merge-sweep.py also exits 1. The eight are test-validate-feature-json, test-inject-expertise, test-factory-config, test-layout-migration, test-branch-create-gate, test-board-lifecycle, test-hooks-install and test-post-merge-sweep. THREE of them — branch-create-gate, hooks-install and post-merge-sweep — are precisely the enforcement and sweep surfaces FEAT-40 modifies. So the feature is not merely inheriting an unrelated red suite; the tests guarding the machinery it changes are the ones already failing, which is the worst case for T-02 in particular, whose whole job is proving the sweep fires. Severity is real, not cosmetic: test-inject-expertise reports 8/17 cases passed and test-layout-migration 7 failing cases.
- 2026-08-25: FEAT-40 red-suite mystery solved. `test-validate-digest.py`'s `[hook]` cases invoke the real check-digest hook with NO env override (`run_hook_cases` calls subprocess.run with no `env=`), so it reads the live `.harness/.inflight-claims.json`. Stale claims left by a killed suite run make 6 cases fail with "returned with children in flight". That file is UNTRACKED and GITIGNORED (.gitignore line 40), which is exactly why the main checkout and CI were green while this worktree was red. Proof was causal, not inferred: the hook's refusal "fires ONCE" per claim, so re-running the script drained harness-backend-dev from 6 claims to 4 to 2 to 1 to none, and the 4th run passed 14/14 with zero code changes.
- 2026-08-25: A red measurement that contradicts several green ones is a STATE question before it is a code question. Look for untracked, gitignored runtime files first — they are invisible to git status, to a diff, and to CI, so every instinct saying "the two checkouts are identical" is wrong about exactly them.
- 2026-08-25: Before trusting ANY suite measurement, run `ps` for competing runs. I inherited a 25-minute-old `--kind all` still in flight; it was both a concurrency confound AND the source of the stale claims causing the red. The operator's "nothing else running" instruction found a real defect.
