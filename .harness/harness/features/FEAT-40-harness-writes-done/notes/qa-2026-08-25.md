# QA gate — FEAT-40 the harness writes Done — review_sha 3a548fe

## Verdict: PASS, with one finding on SC-16's literal wording and standing open questions

The suite (884/0, exit 0) is real evidence, not a rubber stamp: the retargeted `close-task` →
`start-task` assertions bind the same properties they used to (checked by reading the code each
assertion touches, not just the label), `cmd_ship`'s hard cases (ordering, refresh-scope, unknown
child, one-failure-doesn't-stop-the-rest) are present and I proved two of them discriminate by
mutating the source in this worktree, running the single test file, watching it go RED, and
reverting — tree confirmed clean afterward. The three `inspection` SCs rest on genuine live
measurements, not adjacent ones. The one substantive finding is that SC-16's claim "the assertion
count... does not fall" is literally false by 3 (18→15 across the retargeted blocks) — see below.

## 1. The retargeted assertions (adversarial check)

Diffed `cc84b29:test-gh-sync.py` against `3a548fe:test-gh-sync.py` for all six blocks T-11 names.

- **Block a** (close-task closes one issue / absorbed left open / `--reason completed`, 3
  checks) → replaced by 1 check that `close-task` exits non-zero with an "unknown command"
  message containing `close-task`. Not a retarget — correctly treated as "no surviving subject"
  since the behaviour asserted (an issue close via close-task) no longer exists.
- **Block b** (parent → Review on last task done, 4 checks incl. 2 ordering-vs-close checks) →
  retargeted to `start-task`, 2 checks survive (exit 0, parent → Review). Read
  `_apply_parent_rule` (`gh-sync.py:230`) directly: it is unchanged, reads `plan.yaml` from disk,
  and is genuinely caller-independent — nothing about `start-task`'s own already-done guard
  (`gh-sync.py:846-859`) intercepts this fixture, since the default `GUARD_STATION_NAME`/
  `GUARD_STATE` in `FAKE_GH_STATIONS` report "not Done, not CLOSED". **Discriminates**: break
  `_apply_parent_rule`'s "all done → Review" logic and this goes red.
- **Block c** (Done-feature exemption) → retargeted, narrowed from "no item-edit at all" to "no
  item-edit to `ITEM_40`" (the parent only), because `start-task` now writes the sub-issue's own
  station unconditionally where `close-task` wrote none. The narrowing is justified in a comment
  and correct — the old wider assertion was true only as an artifact of `close-task` never
  touching a station, not because the exemption covered the sub-issue.
- **Blocks d/e** (the loud pair) → retargeted, 3+2 checks preserved. Block d's fixture switched to
  `FAKE_GH_SHIP`/`ship_env` because `start-task` now performs a board read `close-task` never did
  (T-07's #642 guard) — correctly noted in-line. The surviving property ("a failed card write
  does not stop the write that follows it") is the same property, reassigned from "sub-issue
  close follows a failed parent write" to "parent write follows a failed sub-issue write" —
  same shape, different direction, both real.
- **Block f** (no-board precondition) → retargeted, all 6 checks preserved, with the
  "lifecycle ran" proof changed from "`close-task` actually closed T-01's issue" to "T-01 and
  T-02 are both recorded in feature.json" (since there is no longer a close to observe). This is
  a weaker witness than before (recorded ≠ observed-to-run), but it's the only witness left once
  the close is gone, and it's stated as such.

**Finding (low, doc-accuracy)**: SC-16 literally claims "the assertion count for those does not
fall." Summed over blocks b, c, d, e, f: 4+3+3+2+6 = 18 old, 2+2+3+2+6 = 15 new — a fall of 3, all
in the two blocks (b, c) where the deleted subject was the close call's ordering/attempt itself.
T-11's own intent text explicitly authorizes deleting those specific assertions ("no surviving
subject"), so this is a spec-text inconsistency between SC-16's wording and its own implementing
task, not a coverage gap — the *properties* SC-16 names (derivation, loud pair, no-board
precondition) are all still asserted and I verified none of the survivors is vacuous. Flagging
because "does not fall" is checkable and false as written; it should read "does not fall for the
properties that still have a subject."

## 2. `cmd_ship` coverage (T-04's ~16 assertions)

All present in `test-gh-sync.py:2270-2429` (`ship:`, `ship D-10:`, `ship HELD:`, `ship ORDERING:`,
`ship REFRESH:`, `ship UNKNOWN:`, `ship FAILED:`, `ship AUDIT:` blocks). Verified by reading
`cmd_ship` (`gh-sync.py:1154-1305`) against each assertion — implementation matches the plan's
D-01–D-13 exactly, including the `write_done` helper that refreshes the station map in place on
every successful write (not just step 4's), which is what makes REFRESH and ORDERING pass.

**Proved by mutation, not by reading**: I disabled the in-place refresh
(`stations[int(num)] = done`) in this worktree, ran `test-gh-sync.py` alone, and both
`ship ORDERING` and `ship REFRESH` went RED (10 total failures, including exactly those two named
assertions); reverted with `git checkout --`, reran, confirmed 0 failures ("ALL PASSED"), and
`git status --porcelain` on the file is empty. This is real discrimination, not a vacuous check.

- sub_issues READ FAILURE leaves the card unmoved: `ship UNKNOWN:` block (`:2400-2406`), asserts
  #40 not in `moved_to_done`, one stderr line naming the issue and "child list unreadable", exit 0.
  Matches `first_open_child`'s `raise RuntimeError` path (`gh-sync.py:1252`).
- a BoardError on one child doesn't stop the rest: `ship FAILED:` block (`:2408-2428`), #41's
  write fails, #42 still reaches Done, deliberately built so #41 is NOT #40's child (so the HELD
  and FAILED lines can't both fire off the same cause) — a genuinely careful fixture design.
- `gh-sync: SKIP` absent across combined stdout+stderr: asserted per-run in every one of the
  ship blocks above (`:2295`, `:2427`), not just once globally.

## 3. The stub's ceiling — which SCs rest on it, and do the three inspection SCs carry the live half

`test-gh-sync.py` stubs `gh` entirely (`install_gh`), so SC-01, SC-02, SC-03, SC-04, SC-05, SC-06,
SC-07 (partly — the gate itself is offline by design), SC-12, SC-15, SC-16, SC-17 are all
"harness issued the right argv" proofs, never "GitHub did the right thing" proofs. That's stated
plainly in the BRIEF's own "Verification gaps" section and I confirm it's accurate.

Read the three inspection notes directly, not just their `probe_issue:`/`sweep_fired:` header
lines:

- **SC-09** (`probe-sweep-fires.md`): genuinely live — a throwaway `--no-hardlinks` scratch clone,
  a real 2-parent merge commit, captured hook stdout showing two `post-merge-sweep:` lines. The
  note is honest about its own limit: "I did **not** measure what the sweep then does... T-10
  measures that end to end." Establishes exactly what SC-09 claims, no more.
- **SC-10** (`probe-done-closes.md`): genuinely live — issue #847 created, moved to Done via
  `board-station.py` (not the web UI), closed 6s later per `gh issue view`, plus the audit run
  showing no `WORKFLOW: 'Auto-close issue'` finding. This is the load-bearing measurement for the
  entire feature and it's solid: real issue number, real timestamps, real command shown.
- **SC-11** (`acceptance-728.md`): genuinely live — real `gh-sync.py ship` run against FEAT-34's
  directory, no hand-typed `gh` write, all 15 numbers (#728, #806, #818–#830) asserted CLOSED/Done
  individually in the note, output captured verbatim showing children-before-parent ordering
  (13 sub-issue lines print before #728's), no HELD line, no FAILED line, no SKIP. The note also
  surfaces 5 audit findings honestly (3 LABEL, 2 STATUS) as pre-existing drift rather than hiding
  them, which is the correct posture for a report that's supposed to be evidence, not a sales
  pitch.

All three probes measure what their SC claims and nothing more is implied beyond that. They
genuinely carry the live half the stub cannot reach.

## 4. Test-matrix gate against the diff

- `component`, `ui`, `eval` all carry `cmd: null` and `status: unresolved` in `.harness.json`
  (verified by reading the file directly, not trusting the BRIEF's assertion). Checked the 68-file
  diff stat: no `.tsx`/`.stories.*`/`e2e/**`/`evals/**` path appears anywhere in it — confirmed,
  not merely assumed. `change_type` across all 11 tasks is `logic`/`config`/`scaffolding`/`docs`;
  none is `ai_behavior`, `frontend`, or `feature`, so none of the three null-cmd kinds is even
  reachable by the matrix here. Soft-skip, correctly.
- `test-gh-sync.py` is matched by both the `unit` glob (`test-*.py` generic pattern) and the
  `integration` glob (explicit path) in `harness.json`'s `test_kinds.detect` strings — but the
  actual dispatcher, `run-unit-tests.sh`, hardcodes `INTEGRATION_SCRIPTS`/`UNIT_SCRIPTS` arrays
  and the file is listed **only** in `INTEGRATION_SCRIPTS`. Confirmed by grep: the file runs
  exactly once, from the integration bucket, under `--kind all`. This split predates this feature
  (confirmed: `test-gh-sync.py` was already integration-only at `cc84b29`) — not a gap this diff
  introduced. `test-gh-close-gate.py`, the one genuinely new file, was correctly added to
  `INTEGRATION_SCRIPTS` **and** to `harness.json`'s integration detect list in the same task
  (T-07), so the drift detector's cross-check (`run-unit-tests.sh:65-115`) holds.

## 5. Per-SC verdicts

| SC | Verdict | Method | Evidence |
|---|---|---|---|
| SC-01 | pass | automated | `test-gh-sync.py:2277-2287` per-card Done assertions + no-close-argv checks |
| SC-02 | pass | automated | `test-gh-sync.py` HELD-line blocks `:2328-2360` |
| SC-03 | pass | automated, mutation-proved | `ship ORDERING:` `:2371-2374`; confirmed RED under refresh-mutation |
| SC-04 | pass | automated | `closes` deleted, `close-task is not a subcommand` block `:704-715`; grep for `Closes #` clean |
| SC-05 | pass | automated | T-05 verify: `grep -rl parent_origin` over bin/, schema, 24 feature.json — clean at review_sha |
| SC-06 | pass | automated | T-05's abandon tests: dry-run listing, `--yes` order-independence, `--yes` elsewhere → exit 1 |
| SC-07 | unproven (stub) for "denial text is correct GitHub behavior"; pass for "gate denies/allows correctly" | automated, offline by design | `test-gh-close-gate.py` per T-07 fixture list; the gate is deliberately network-free so this SC's automated half is complete on its own terms — no live GitHub call is claimed |
| SC-08 | pass | automated | `test-check-state.py` INV-31 fixtures (3 states) |
| SC-09 | pass | inspection, live | `probe-sweep-fires.md` — real merge, real hook stdout |
| SC-10 | pass | inspection, live | `probe-done-closes.md` — real issue #847, timestamped close |
| SC-11 | pass | inspection, live | `acceptance-728.md` — real ship run, 15 cards named individually |
| SC-12 | pass | automated | review-station regression guard + SC-12 second-clause block `:1592ff` (behavioural, over `status`/`start-task`/`abandon`) + secondary grep |
| SC-13 | pass | inspection | `git show 3a548fe:<path>` for all 3 files, grep for the 4 named phrases: zero matches in each |
| SC-14 | pass | automated | `gen-decisions-index.py --stdout` diffs clean against committed index; `test-gen-decisions-index.py` all green; strike records for DEC-186/192/196 and DEC-138 am.8 confirmed present in DECISIONS.md |
| SC-15 | pass | automated, self-proving | `test-post-merge-sweep.py`'s own "(g2) RED PROOF" block mutates the FAILED literal out and asserts the worktree gets wrongly deleted — a genuine discrimination test written by the dev, confirmed by reading it |
| SC-16 | pass, with finding | automated | see §1 — properties preserved, literal "count does not fall" wording is false by 3 |
| SC-17 | pass | automated | `ship AUDIT:` / `ship AUDIT ORDERING:` blocks `:2442-2466`; `_ship_audit` (`gh-sync.py:1308`) never gates, matches |

## Coverage gaps (Phase 1 vs Phase 2 delta)

Phase 1 (BRIEF/plan only, before reading code) expected, in addition to what's above: a test that
`wayfind.py resolve`'s own `gh issue close` (line 318, named in the BRIEF's "does NOT do" section)
is untouched by this feature — confirmed out of scope by the BRIEF itself, not a gap, just noting
the BRIEF's own boundary held: I grepped and `wayfind.py` is untouched in the diff.

No coverage gap found beyond the SC-16 wording finding above.

## Open questions carried forward (not mine to resolve)

- Q11 (BRIEF/T-09, already flagged in-plan): SC-13 has no positive clause for T-09 part C's
  `INV-31` sentence in `harness-init/SKILL.md`; the *task's own* verify closes that hole, SC-13
  itself does not. Not blocking — already flagged by the plan itself, restated here so it isn't
  lost.
- The D-06 mechanism substitution (env marker → unconditional gate refusal, since a `PreToolUse`
  hook never sees a Python subprocess) is sound and documented, but it means the harness's actual
  compensating control for a hand-typed close is entirely the audit at `ship` time (D-13) — a
  card closed outside the harness is invisible between the close and the next `ship`. This is
  already named as an accepted cost in DEC-203 item 9 and the BRIEF; not new, just confirming it
  was carried through faithfully into the shipped code.

## Tree state

`git status --porcelain` is clean of any change from this QA run (the mutation to `gh-sync.py` was
reverted with `git checkout --` and reran to confirm both the mutation-red and the revert-green).
Unrelated untracked/modified files from other concurrent agents (review notes, observations) are
present but are not mine and not touched.
