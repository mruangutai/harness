# Operator answers — FEAT-40 plan escalation — 2026-08-25

## Q1 (pm's T-11 literal) — RE-MEASURE FIRST. Do not correct the literal yet.

**The red baseline does not reproduce.** Measured by the main session at `cc84b29`, running each
declared gate alone with nothing else competing:

| Gate | Result |
|---|---|
| `run-unit-tests.sh --kind unit` (main checkout) | **355 PASS, 0 FAIL** |
| `run-unit-tests.sh --kind unit` (FEAT-40 worktree, `cc84b29`) | **355 PASS, 0 FAIL** |
| `run-unit-tests.sh --kind unit` (FEAT-40 worktree, `00c5630`) | **355 PASS, 0 FAIL** |
| `run-unit-tests.sh --kind integration` (main checkout) | **517 PASS, 0 FAIL** |
| `run-unit-tests.sh --kind all` earlier the same day | **872 PASS, 0 FAIL, exit 0** |
| GitHub CI on PR #841, same content | `integration: pass` |

Every one of the eight named scripts passes, including the two the escalation says T-11's literal
omits: `PASS test-post-merge-sweep.py` and `PASS test-hooks-install.py`. The six scripts pm named
also pass when invoked directly.

**The operator ruled: re-measure before touching T-11.** Do this, exactly:

1. Run **one** suite at a time, with **nothing else running** — no concurrent agent, no second
   suite, no `check-state.sh` in another shell. The previous run reported three concurrent copies of
   `test-check-state.py` competing and running past 40 minutes, which is a plausible source of
   failures that vanish when the suite runs alone.
2. Use the command `harness.json` declares, unmodified:
   `.agents/skills/harness/bin/run-unit-tests.sh --kind unit`, then the same for `--kind integration`.
3. Report, whatever the result: the **exact command**, the **cwd**, every **environment variable you
   set** (`CLAUDE_PROJECT_DIR` and `HARNESS_PROJECT_DIR` in particular — note that the earlier run
   reported setting `HARNESS_PROJECT_DIR`, and the main session has not confirmed that variable is
   read by anything), the **git sha**, and the **complete list of `^FAIL ` lines**.

**Then:**

- **Green** → delete **T-11** and **D-12**, remove `T-11` from `depends_on` in T-04, T-05, T-06,
  T-07 and T-08, and remove the failure-set-equality clause from those five verifies. The plan drops
  to 10 tasks. Keep any `PASS <script>`-by-name clause that is genuinely useful on its own merits;
  drop the quarantine machinery.
- **Red** → report the exact discriminator between your environment and the one measured above.
  Do not simply re-assert the failure. A measurement that contradicts four independent green runs
  needs its cause named before the plan is built on it.

## Q2 (operator) — CONFIRMED. The environment marker is dropped.

Your measurement stands and it corrects the source ticket. A `PreToolUse:Bash` hook receives only
`tool_input.command`, and `gh-sync.py` reaches `gh` through `subprocess.run`, which never traverses
the Bash tool. **The gate is structurally blind to abandon's close and needs no exemption.**

Build it as your plan already has it: **refuse every `gh issue close` unconditionally**. A hand-typed
`gh issue close 728` is refused; `python3 gh-sync.py abandon <dir>` is not, because the gate never
sees the call inside it.

Record in the decision that this is the *outcome* the ticket mandated reached by a simpler route
than the mechanism it named, and that no marker exists to forge. Constraint 5 of the source ticket
is amended by this answer.

## Q3 (operator) — DEC-203's prose register: **shorter AND plainer**.

Cut the length and the density together. The new entry must be **materially shorter than the three
entries it replaces**, in ordinary words. Nothing essential is dropped — but the dense, clause-heavy
register of the current entries is the thing being removed, not just their word count.

## Q4 (main session) — DONE. The `approval:` skeleton is written.

The main session added it to `plan.yaml` directly after `source_issues`, `status: pending`, with the
DEC-120 comment. Do not write that key; `check-domain.sh` denies every agent. The signature itself
still waits on Q1's outcome.

## Q5 (operator) — acknowledged, and your correction is accepted.

**#728 has thirteen children, #818-#830.** All are closed on GitHub and all sit at the `Review`
station, so under the new rule all thirteen are **open**. The operator's acceptance test is
therefore the hard case: it exercises the open-child skip and the children-first ordering. Keep it
as the acceptance test and make sure BRIEF and plan describe it as the hard case, not as a childless
parent.

## Q6 (operator) — the five carried non-blocking questions

Not ruled yet. Carry them forward unchanged and re-raise them with the re-measure result. The
operator will rule on them in one pass rather than piecemeal.

## Q7 (harness defect) — half stale, half already known.

- **The stray probe file does not exist.** `find . -name ".mutant-*"` returns nothing anywhere in
  the tree. Nothing to clean up; do not re-report it.
- **No pre-commit gate ran on your commit** — correct, and it is the same defect T-02 fixes.
  `core.hooksPath` resolves to `.git/hooks`, which holds only samples. The operator ruled that it is
  set **as part of this change**, not before it, so this stays true until T-02 lands. Expect it.
