# Observations - harness-pm

- 2026-08-31: FEAT-47 amendment. A verify that asserts an EXACT file count is fragile against any sibling task that adds a file to the same directory, and depends_on being a DAG makes 'legal order' insufficient — two tasks ready at once can land either side of the count. Fixed by adding the ordering edge (T-05 depends_on T-07) and stating the arithmetic in the intent, not by loosening the count to >=.
- 2026-08-31: planning an investigation whose fix-site is unknown at plan time: keep the file list glob-shaped over surfaces already in lanes:, make the IDENTIFICATION the first deliverable with two machine-readable lines the verify greps, and write an explicit BLOCKED branch for 'the defect went quiet' so a downstream task cannot be unblocked by a flake that stopped reproducing.
- 2026-08-31: scope split FEAT-47/FEAT-48. The only non-mechanical part of removing T-07..T-09 was T-05's depends_on [T-03,T-04,T-07] and its hardcoded unit count 21: the T-07 edge existed purely for count determinism (old D-09), so dropping the edge and the count in the same edit is required — dropping one alone leaves an unsatisfiable verify. Checker output before/after differed by exactly the T-07..T-09 lines, which is the cheapest proof that no other route resolution moved.
- 2026-08-31: FEAT-47/48 census defect. Two plans each internally consistent and mutually contradictory: FEAT-47 T-03 asserted `[ "$n" = 19 ]` plus an empty-bin sweep while FEAT-48 added two test-*.py to bin. Lesson recorded as D-14: a per-task floor plus a per-file provenance check, and ONE conservation law derived from the merge target, replaces every expected count.
- 2026-08-31: the old T-03 bin sweep was already red before FEAT-48 existed. 36 + 19 = 55 of 56 tracked test-*.py; test-run-unit-tests-kinds.py survives to T-05. Summing a plan's own enumerated sets against `git ls-files` is a 2-minute check that would have caught it at authoring.
- 2026-08-31: reordering two features changes the DECISIONS.md number the later one takes. Eleven `dec: DEC-206` sites in FEAT-47's plan became DEC-207. Any plan whose T-06 hardcodes a decision number acquires a rotted pointer the moment the merge order moves.
- 2026-08-31: FEAT-47 T-06's git grep sweep over "every live file" includes DECISIONS.md, so the OTHER feature's decision entry can redden it. Sequenced features need a step that reconciles the predecessor's entry, not just the historical ones.
- 2026-08-31: proved `if git grep ... | grep .; then exit 1; fi` reports "sweep clean" (rc 0) for a git grep that exits 129, while the replacement ladder (set +e, capture rc, assert rc -le 1 then rc = 1, plus a positive control) reports the error. /tmp/feat47-sweep-proof.py, 4 cases.
- 2026-08-31: a lower-bound rename count over a pathspec containing fixtures is maskable. Proved in a throwaway git repo: one test file recreated by hand while a fixture rename stays clean passes `grep -c "^R" >= N` and fails a per-file `^R<sim> <src> <dst>$` assertion.
- 2026-08-31: FEAT-47 F-01 repair. A sweep specified over the path literal tests/unit is blind to os.path.join(root, "tests", "unit"), which is the form the module it hunts is most likely written in — the pattern would fail its own positive control while reporting a clean sweep. Probed three shapes (slash literal, join components, Path(...).glob) before writing the spec; regex tests['"]?[,/]['"]?unit catches all three and matches 0 of 101 tracked .py at 50dafcf.
- 2026-08-31: F-07 closed by disclosure, not automation: the only instrument that can see the child-process property (suite-census.py children) costs a full suite run, so a gate budget cannot hold it. Disclosure had to name what carries it instead and why a misplacement is cheap (kind reporting, not suite correctness), or it reads as an excuse.
- 2026-08-31: FEAT-47 T-03 instructed a repoint of a directory climb FEAT-48's F-01 had already replaced with harness_boundary.root_above; drafted against a sibling plan's pre-fix draft and never refreshed. Two more paragraphs restated FEAT-48's D-11 watched set, which is under revision this cycle. Lesson shape: an instruction describing the INTERNALS of a file another live feature authors rots without ever reading false — cite the sibling's D-NN as the single definition and state only the contract you depend on.
- 2026-08-31: FEAT-47/48 second-order rot. After de-coupling a sibling's mechanism restatement, the ARGUMENT my own plan had to emit to that mechanism was still stale (--mutation-check "$ROOT" vs "$BIN_DIR") and the sibling's verify asserted the literal, so it was a live red gate, not a harmless quote. Lesson: de-coupling prose is half the job — ask the sibling for every INPUT your plan must literally emit, then gate that input in your OWN verify, derived from something in the same line rather than a literal, so it is rename-proof.
- 2026-08-31: FEAT-47 c1. A negative-token sweep over a live tree is the wrong instrument whenever any preserved sentence legitimately carries the token. Replacing the pathspec with (path, fragment) pairs plus a refusal on any exempted path under an expertise dir turned an unsatisfiable gate into one whose fail-open branch is unreachable. Proved with a prototype + synthetic git tree: 1 green case, 6 red.
- 2026-08-31: FEAT-47 c1. While proving the sweep satisfiable I found a FOURTH site of the same class that both panel halves missed — T-05 step 7 told a live test file under tests/integration/ to assert the deleted token BY NAME. The lesson: when a plan adds a negative-absence gate, grep the plan's OWN task intents for the token, not only the tree.
- 2026-08-31: check-expertise.sh caps a single entry at 50 WORDS (message: "G-04 is 51 words — cap is 50; a rule, not a story"). The 40-line repository-tier file budget is the cap that does NOT fire on a two-entry edit. A naturally-worded replacement entry came in at 51.
- 2026-08-31: expertise-merge.py cannot express a REPLACEMENT — same id with different text is a conflict it refuses to apply. Repairing a falsified Expertise entry is always a direct edit, which is what forces the ownership question.
- 2026-08-31: panel_findings.py and any plan-level `panel:` key do not exist in this checkout, though harness-spec-driven mandates transcribing the panel result through them. Recorded dispositions in the notes artifact instead of hand-typing ids into an unvalidated schema.
- 2026-08-31: FEAT-47 census re-derived after FEAT-45 merged (rebase 56a30a0). Every DERIVED assertion survived the sibling merge untouched (per-file rename, T-03 exact residue via git ls-files, tally set equality); every LITERAL in prose had to be rewritten. That asymmetry is the whole argument for deriving-with-a-floor, and it is now measured twice rather than argued once.
- 2026-08-31: a floor is only insurance if it sits BELOW the count it guards. `--floor 56` stayed true when bin went from 56 to 58 and merely stopped being tight, which is the correct failure mode; the temptation on re-derivation is to raise it to today's count, which would make it satisfiable only by the exact tree it was written against. Wrote the prohibition into the plan beside the number.
- 2026-08-31: reproducing a recorded counting rule before calling drift caught a latent unsatisfiable verify. Three suites print `PASS: (e) ...`; the plan spec said "first token is ok/PASS/FAIL", which scores them 0 against baseline rows of 29/52/34. The recorded baseline had been measured with colon-stripping, so spec and baseline disagreed and a `--strict` run would have been permanently red on a correct tree. Pin the tokenizer in the spec, not just the numbers.
- 2026-08-31: an unreproducible literal is worse than a stale one. BRIEF claimed "a raw grep reports 3219 mentions" with no invocation; no `git grep` form at either ref reproduced it. Withdrew it and wrote the command beside the new figure rather than swapping one bare number for another.
- 2026-08-31: a sibling's new test files must be re-classified by the plan's own criterion, never inherited from the array the sibling registered them in. FEAT-45 put both new files in UNIT_SCRIPTS; both drive their artifact as a real subprocess and are integration under issue 160. Inheriting the registration would have shipped two misclassified files through a plan whose whole point is that the directory is the kind.
- 2026-08-31: the three-file blind spot in my own measurement harness was the same class of bug I was hunting: my first census counted `l.split()[0] in ('ok','not','PASS','FAIL')` and reported 0 for three suites with rc=0, which read like a legitimate re-measurement rather than a broken tokenizer. A uniform surprising verdict across a subset means the instrument, not the tree.
- 2026-08-31: FEAT-47 c2. An enumerated file list in a plan failed twice in one review cycle (two expertise files, then five); the plan already forbade the shape for its TEST census (D-14) and nobody carried it across. Lesson: when a decision states a rule about literals, grep the same plan for every OTHER census that rule never reached, in the same pass.
- 2026-08-31: FEAT-47 c2. `git ls-tree -r --name-only <ref> -- '.harness/*/expertise/*.md'` returned NOTHING while `git ls-files` with the same pathspec worked; selecting with `PurePosixPath.match` over the unfiltered listing is what made the base-ref half of the sweep work. `fnmatch` would over-match, since its `*` crosses `/`.
- 2026-08-31: FEAT-47 c2. Proving a verify block green-somewhere needed a synthetic git repo with `refs/remotes/origin/main` planted via `git update-ref` and a REAL COPY of check-expertise.sh; the fixture's own `# Expertise — harness-<name>` title line was the first thing that reddened it, not the clause under test.
- 2026-08-31: FEAT-47 c2. A probe module ending in `sys.exit()` terminates the snippet that imports it, silently skipping every line after the import: a fixture I believed I had rebuilt was still in the previous case's state, and the resulting MISMATCH read as a defect in the shipped verify block.
- 2026-08-31: FEAT-47 c2. A heredoc into `observations-merge.py --entries -` is refused by bash-write-guard as a redirect; writing the entries to a file with the Write tool and passing that path is the route that works.
- 2026-09-02: goal-checking FEAT-47 I ran `suite-census.py verdict-lines --strict` with my own spawn env intact and got a false red: `test-plan-merge.py expected=285 actual=273 exit=1`, 12 sign-approval cases refused because `HARNESS_AGENT_TYPE=harness-pm` makes `cmd_sign_approval` refuse (DEC-120 self-enforcement). `env -u HARNESS_AGENT_TYPE python3 tests/integration/test-plan-merge.py` exits 0 with exactly 285 lines. harness-qa's repo-tier G-07 already records this; pm's does not. Before reporting any suite figure as a criterion's evidence, strip the agent env.
- 2026-09-02: REQ-07 residue survived T-07 and the fix commit: `.harness/harness/expertise/harness-pm.md` G-08 still presents the deleted KIND-DRIFT cross-check and the two script arrays as live run-unit-tests.sh gates. The residue sweep cannot see it (no literal UNIT_SCRIPTS/INTEGRATION_SCRIPTS/check-kinds token) and SC-07 is written to those three tokens, so every criterion greens while the requirement is unmet — the same shape the pinned reviewer's F-1 caught for the project-tier dev-ops G-03, repaired there and missed here.
