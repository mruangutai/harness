# Observations - harness-orchestrator

- 2026-09-02: FEAT-47 — dispatched to ship a signed plan whose seven tasks are ALL execution_mode main-session-direct. The CLI probe of check-domain.sh was useless: it reads identity from HOOK_PAYLOAD's agent_type, not HARNESS_AGENT_TYPE, so every hand-built probe returned exit 0 and read as 'enforcement off'. The decisive measurement was an actual Write tool call to a forbidden path, which returned the real BLOCKED stderr in one step.
- 2026-09-02: FEAT-47 — check-state.sh INV-17 independently emits 'every task in its plan.yaml is execution_mode main-session-direct (DEC-174), so no squad ran'. That note is a free, mechanical corroboration of an undispatchable plan and it costs one already-required check-state run; I found it after probing rather than before.
- 2026-09-02: FEAT-47 — three pre-existing bookkeeping defects were all invisible to the plan phase and all cheap at entry: feature.json carried an undeclared 'status' key (schema additionalProperties false), all seven task statuses read 'pending' which is outside the FEAT-41 station vocabulary, and the mirror had never been opened. validate-feature-json.py and check-state.sh named all three in under 15s each.
