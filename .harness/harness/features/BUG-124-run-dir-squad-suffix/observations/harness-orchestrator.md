# Observations - harness-orchestrator

- 2026-09-08: a host-cut nested dispatch left runs/2026-09-07-4-validator/ with status:running and its one step `pending` and NO digest.md and no notes/qa-c4.md. That absence is the discriminator between "cut before any member work" (clean re-dispatch) and "cut mid-work" (O-02 assess-not-redo). One `ls` of the run dir plus the persona's own notes path settles which, and getting it wrong either duplicates a member's work or silently drops it. I resumed the SAME run dir rather than opening a new one, so the run_uid and the step's dispatched_at stayed continuous.
- 2026-09-08: feature.json's runs list had silently drifted three runs behind disk (2026-09-07-02-eng, -3-eng, -4-validator all had digests on disk and no json entry) because each recording act died with an interrupted turn. `ls runs/` diffed against the json is the only way to see it; git status cannot, since the runs tree is gitignored here (G-04). cycles_used reconciled exactly by summing lead-reported send-backs plus 1 per FAIL I routed back: 1+1+3 = the recorded 5, +2 for the unrecorded 3-eng = 7. That the arithmetic closed on the recorded number is what made the correction safe rather than a guess.
- 2026-09-08: qa's PASS digest named its own adequacy gap — red-capability carried forward from cycle 1 rather than re-derived at the graded commit. Re-deriving it myself was ONE bounded command (DISPATCH_GUARD_BIN at the main checkout's pre-change guard) and returned the exact expected 8-FAIL set. Closing a validator's self-declared gap at my own tier, when it costs one command, is far cheaper than carrying it into the review seam where a panel re-opens it as a finding.
- 2026-09-08: re-verifying a predecessor's fix claim by re-running the full sweep cost 74s and was worth doing unconditionally. The runner lives at .claude/skills/harness/bin/run-unit-tests.sh, NOT tests/ — a wrong path exits 127 with zero FAIL lines, which greps identically to a clean pass. Checking discovery volume (80 files / 5403 lines) against the exit code is what distinguishes them.
- 2026-09-08 (BUG-124): a task's FINISHED station is `done`, never `review` — `review` is the FEATURE's station alone. I set T-01/T-02 to `review` at the seam, which is not in gh-sync's `finished_stations()` (`done`, `abandoned`), and had to correct them and RE-PIN review_sha because INV-33 compares plan.yaml BYTES at the pin against disk. Order that works: write every station first, commit, THEN pin at that commit, then commit the pin alone.
- 2026-09-08 (BUG-124): `gh-sync.py status <dir> review` refuses while ANY task is not done/abandoned (gh-sync.py:1318). A plan carrying a main-session-direct PRE-SHIP task therefore cannot pass the mirror's review station at the Building->Review seam — the refusal is truthful about the plan, the mirror is never a gate, and falsifying the task's station to make it pass is the wrong fix. Record the refusal and dispatch the panel anyway.
- 2026-09-08 (BUG-124): gh-sync.py disagrees with itself about which root to stand in — `status` needs the WORKTREE (a main-checkout invocation reports 'is not a directory'), while `ship` refuses a feature dir under .claude/worktrees/ and needs the main checkout. Same class as the handoff_done_when/check-state worktree-root defects.
- 2026-09-08 (BUG-124): routing a member's non-blocking open_question INTO the next panel rather than answering it at rung 1 paid — the eng lead's latent fail-open came back as a graded finding with a concrete trigger AND a silent-failure clause (globs non-empty so no SKIPPED line prints, exit 0) that neither the raiser nor any single reviewer had alone.
