# Panel record — goalcheck reader added, INV-32 goalcheck line closed

**BUG-285-yaml-loader-pin · 2026-09-11 · harness-pm**

The `goalcheck` reader is now recorded in `plan.yaml` `panel.readers` as the third entry, and the
INV-32 line `reader goalcheck never ran or was not recorded` is **absent** from
`check-state.py` output — grepped for the exact phrase across the whole run, 0 matches (the phrase
does not appear for ANY feature now). Nothing else in the panel moved: the textual diff on
`plan.yaml` is **3 added lines, 0 removed, 0 changed**.

This recorded a fact; it did not grade anything. The goal-check itself ran and PASSED at seq-1 of
this segment — `notes/research-BUG-285-goalcheck-plan-c3.md`.

## What was written

`plan.yaml` `panel.readers[2]` = `reader: goalcheck`, `status: ran`, `persona: harness-pm`.
Route: `plan-merge.py set-panel --value-file`, the value file generated by loading the live panel
with `safe_load` and appending one reader (never retyped). stdout:

```
PANEL cycle 2 -> .../BUG-285-yaml-loader-pin/plan.yaml
APPLIED .../BUG-285-yaml-loader-pin/plan.yaml
```

No finding was added. The goal-check's three advisories are the lead's to route and are not panel
findings.

## Read-back — asserted programmatically, 15/15 PASS

Post-write `safe_load` compared against the pre-write load held in memory (`/tmp` json, since
deleted with the value file):

- 3 readers `['should-not-exist','scope','goalcheck']`; first two byte-identical to pre-write.
- `findings` list **equal as a whole structure** — all 6 ids, severities, readers, summaries,
  dispositions, `resolved_by` unchanged; `PF-2242299b369215b13ad577fe4279d52e` present; count still 6.
- `last_run: 2026-09-11-02-planpanelc2-validator`, `cycle: 2` unchanged.
- `approval:` (`approved`, mruangutai, 2026-09-09), all 3 `tasks:`, `decisions:`, `lanes:` equal.
- Panel key set unchanged; every panel key except `readers` equal.

Equality was over loaded values (G-07: `set-panel` re-emits through `safe_dump`, so bytes can move
even where values do not). Here they did not move at all — the unified diff is a clean 3-line insert
under `panel.readers`.

## check-state.py — exit status 1, captured into a variable before any pipe

Remaining BUG-285 violations, all expected and NOT touched:

- `BRIEF.md is NOT approved` — by design on this branch.
- five `runs/*/digest.md: does not satisfy the lead digest contract` —
  `2026-09-11-03-panelrecordc2-product`, `-01-panelfix-product`, `-t03intent-product`,
  `-02-planpanelc2-validator`, `-01-amend-product`. This branch is three commits behind main on
  `validate-digest.py` and carries the older contract.

Remaining BUG-285 notes: the two resolved findings report as `disposition resolved` (INV-32's normal
resolved branch), and one orphan — see below.

## Open question, not mine to fix

`run dir 2026-09-11-04-goalcheck-product exists on disk but feature.json does not record it` — the
goal-check segment's own run dir is unregistered in `feature.json`. A resume will not find it by
name. Registration is the orchestrator's write, not pm's.
