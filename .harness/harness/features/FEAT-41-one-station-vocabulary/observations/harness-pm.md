# Observations - harness-pm

- 2026-08-25: FEAT-41 SC-02 carried a hand-typed grep count that drifted twice (31 -> 26, split 13/7) while the criterion itself named the command; re-running it gave 27 with 11/9. A criterion that quotes its own command has no excuse for a number not read off that command.
- 2026-08-25: FEAT-41 R-01 repeated F-03 exactly: a task deleted a shared name by SPAN, and the name had one use outside the span. The post-condition grep named only one of the two dead identifiers, so the plan asserted a deletion no gate measured. When a task deletes an identifier, grep every dead identifier in the verify, and address deletions by name rather than by line span.
- 2026-08-25: an operator ruling that removes a documented exception is also an instruction to measure what the exception was hiding. Removing FEAT-41's ready-to-backlog carve-out cost 0 card moves, but the measurement exposed two masked defects: 16 shipped tasks still recorded pending, and derive_station outranking a recorded terminal station on 22 of 23 parent cards. Neither was visible while the exception absorbed the difference.
- 2026-08-29 (FEAT-41 replan): an absence-verify of the form `grep -rn PAT dir/*/file ; test $? -eq 1` is unusable in this harness's shell twice over. First, the glob expands to non-existent members (measured: 9 of 40 feature dirs have no plan.yaml), so grep exits 2 with or without matches. Second, gitignored __pycache__/*.pyc carry identifier and command-string constants (`_STATION_KEYS`, `plan-merge`) that no source edit removes before the grep runs. Both made a correct execution red. Fix: a python glob+regex assertion, or --exclude-dir=__pycache__. Ran all three broken lines before amending rather than reasoning about them.
- 2026-08-29 (FEAT-41 replan): when a plan's premise is "close this live violation", re-run the gate before believing it. FEAT-40 merging closed FEAT-41's INV-26 subject, which turned T-10's third clause into a criterion true by construction. The plan text was not wrong when written; it was falsified by a merge nobody in the plan controlled.
- 2026-08-29 (FEAT-41 replan): a rebase moves DELETION addresses and NAVIGATION anchors identically, but only the first is dangerous. check-domain.sh moved +29 lines and T-09 carried "REPLACE lines 1011-1017 and only those lines" - literally followed, that deletes unrelated code. Tasks that said "re-derive by content" cost nothing to fix; tasks that named a span cost a wrong edit.
- 2026-08-29: goal-checking a plan against EXTERNAL intent, the ticket's own `## Input` section can delegate its detail to a grilling artifact — so a Settled grilling bullet is ticket-grade intent, and a panel that grades only the ticket's numbered items misses a dropped requirement (FEAT-41: the task-to-issue map).
- 2026-08-29: a criterion whose observation is a tree-wide grep is often carried by NO task verify block; dumping every `verify:` with safe_load and searching for the criterion's own command is a two-minute check that found FEAT-41 SC-02 unpinned.
