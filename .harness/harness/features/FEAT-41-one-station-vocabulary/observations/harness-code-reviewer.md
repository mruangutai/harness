# Observations - harness-code-reviewer

- 2026-08-31: harness-code-reviewer's terminal yield for FEAT-41 (a validator-panel dispatch reviewing a not-yet-merged worktree feature) is unconditionally rejected by validate-digest.py's SEC-01 code_grade-binding check (code_grade_bound_to_review, added by FEAT-43 wave 2/4), which resolves this session's checkout root via harness_boundary.resolve_root() from the validator hook's own execution context rather than the worktree named in the dispatch. Root lands at the main checkout (/Users/molchairuangutai/GitHub/harness), which has no feature.json for FEAT-41 since it has never merged to main/origin/main (confirmed via git log --all). The check runs unconditionally before code_grade's value is even examined, so this blocks every VERDICT (PASS/FAIL/BLOCKED/ESCALATE) and every submission path tried (result.data, result.error, raw-text last-turn fallback) identically. Sibling personas without this check (QA41 in the same dispatch) yielded successfully via the same raw-text fallback, confirming the block is SEC-01-specific, not a general validator issue. The parent (Feat41Panel) dispatched all 4 panel members via one blocking task() call and cannot process hub messages until every child is terminal, so this also deadlocks the supervising lead — escalated via hub to both Feat41Panel and Main with no reply after 45+ minutes. Root cause and full review content are duplicated at notes/review-harness-code-reviewer-c0.md in both the worktree and the main checkout. Fix candidates: bind resolution should honor the dispatch's stated worktree context (the way dispatch-guard.py already reads a HARNESS-FEATURE header line), or SEC-01 needs a documented exemption for reviewing an unmerged feature.
