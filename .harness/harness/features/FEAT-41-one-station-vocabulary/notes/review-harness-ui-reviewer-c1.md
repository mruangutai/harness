# UI Reviewer — FEAT-41 — cycle 1

review_sha `fc0837529caf0bb28b523f08ed4725dddffef9e8`, merge-base `9f2a0702bda6de929d42506f5aced2496669a2dc`
(171 files). Both stages ran: Stage 1 (spec compliance, the slice touching operator-facing text)
and Stage 2 (adversarial code-quality pass over that same text). **PASS.**

## Stage 1 — measured scope, no rendered UI

- No `DESIGN.md` exists for this feature — confirmed with `git cat-file -e` against both
  `DESIGN.md` at merge-base and at review_sha; neither resolves.
- File-extension census over all 171 changed files: **0** hits for
  `html|css|scss|sass|less|tsx|jsx|vue|svelte`, except two `.html` files. Both are
  `notes/ship-review-2026-08-{29,30}-01.html` — generated artifacts, each carrying its own
  footer `"the markdown is the record; do not edit this file. Regenerate with bin/render-brief.py"`
  (confirmed by reading both files). Not product UI, not a design contract; out of scope per this
  role's own precedent for generated ship-review views.
- 45 markdown files touched; none is a `DESIGN.md`-shaped contract (spot-checked: they are
  `plan.yaml`-adjacent notes, digests, BRIEF/STATE/DECISIONS entries).
- Net: **no rendered UI surface in this diff.** The only operator-facing surface is CLI/terminal
  text, which the dispatch names explicitly in scope — see Stage 2.
- Spot-checked SC-02 (`grep -rnE "[\"'](Backlog|Plan|Ready|Building|Review|Done)[\"']"` over
  `.claude/skills/harness/bin/*.py`/`*.sh`, excluding `test-*`) myself: **0** hits, matching the
  criterion's claim. SC-07 (sign-approval refusal names both the verb and the sanctioned route):
  confirmed at source, see below.

## Stage 2 — the five cycle-0 must-fix findings, verified closed at source

All five re-checked by reading the actual code at `review_sha`, not by trusting the handoff note.

- **F-01** (`_commit_terminal_station` printing a raw Python list, `['fatal: ...']`) — CLOSED.
  `gh-sync.py:634-645` extracts `_git_detail(r)`, which returns `lines[-1] if lines else
  "(no output)"` — a string, not a list slice interpolated directly. Docstring at `:637-641`
  states the defect and the fix in its own words; I independently confirmed the return type.
- **F-02** (plan-merge.py `sign-approval --by` value corrupting the YAML on write) — CLOSED.
  `plan-merge.py:248-277`: the writer now goes through `yaml.safe_dump` (`_field_lines`,
  `:263-266`) rather than hand-rolled interpolation, plus an independent round-trip
  verification layer (`:274-277`) that catches silent coercions (`yes`→`True`, `#845 owner`→
  `None`) a load-succeeds check alone would miss.
- **F-03** (plan-sign-gate.py evasion via `--` end-of-options / unbalanced-quote text fallback) —
  CLOSED. `plan-sign-gate.py`'s tokenizing path skips `SEP = "--"` before testing verb adjacency
  (`:120-124`), and the regex text-fallback (`RAW_SIGN`, `:72-79`) independently skips the same
  separator, closing both the tokenizing and non-tokenizing paths the finding named.
- **F-04** (`Plan.yaml` alternate-case spelling walking past the write-denial route) — CLOSED.
  `check-domain.sh:1038-1043`: every shape pattern (`RE_FEATURE_JSON` … `RE_PLAN_YAML`) now
  compiles with `re.IGNORECASE`, with the docstring at `:1064-1067` recording the measurement
  (`echo x > Plan.yaml` overwrites the same inode) that justified applying it to all six patterns,
  not just the one found.
- **F-05** (list-slice-into-f-string rendering defect, same class as F-01, found while extracting
  `_git_detail`) — CLOSED as part of F-01's fix; same function, confirmed above.

## Stage 2 — operator-facing CLI text pass (the surface this dispatch named in scope)

Read every newly-added `print`/`sys.stderr.write`/`raise SystemExit` line across the six named
files (`gh-sync.py`, `plan-merge.py`, `plan-sign-gate.py`, `check-domain.sh`, `check-state.sh`,
`check-plan-routes.py`) plus a targeted grep for the F-01 pattern class (raw collection
interpolated into an f-string) across the whole diff. No second instance of the F-01 class exists
anywhere in the diff — every newly added message that carries a collection formats it through
`", ".join(...)` or an equivalent explicit render, not raw interpolation.

Message-text quality is high and consistent with this feature's own conventions:
- `plan-sign-gate.py`'s `REASON` (`:52-66`) names the refused verb, the reason (whose signature it
  is and why), and a copy-pasteable remedy command — satisfies SC-07 as written.
- `check-domain.sh`'s plan.yaml write-denial (`:1478-1497`) leads with the reason before the
  route, by explicit design ("THE REASON COMES FIRST, THEN THE ROUTE" — `:1479`), and lists four
  concrete remedy commands.
- `gh-sync.py`'s worktree-ship refusal (`:1459-1463`) and `_commit_terminal_station`'s
  deliberately-non-gating WARNING wording (verified against `post-merge-sweep.sh:192-208`, which
  greps combined stdout+stderr for the literal strings `"gh-sync: SKIP"` and `"gh-sync: FAILED"`)
  both correctly avoid or include those two words depending on whether the underlying condition
  should hold worktree removal — no false-success and no false-block case found.

**One low-severity, non-gating inconsistency, new in this diff:** `check-plan-routes.py`'s two new
plan.yaml station-vocabulary violation lines (`:385-387` for the feature's own station, `:420-422`
for a task's) state the illegal value and the legal set but name no remedy verb —
`f"VIOLATION top-level status {feature_station!r} is not one of {_legal} (case sensitive)"`. The
same defect class, enforced independently in `check-domain.sh` (`:1194-1196`,
`"Set one with plan-merge.py set-task-station or set-feature-station, which validate the value
before it lands."`) and `check-state.sh` (`"Set it with plan-merge.py set-feature-station, which
validates the station before it writes."`), does name the remedy in both siblings. This is
internally consistent with `check-plan-routes.py`'s own pre-existing house style — every other
`VIOLATION` line in that file (`:222-227`, `:274-276`, `:445-447`) is similarly terse and
fact-only, so this is not a deviation from that file's convention, only from the other two files'
richer convention for the identical underlying event. Not gating: `check-plan-routes.py` is a
gate-script consumed as a pass/fail signal with the plan and BRIEF open beside it, not a
standalone operator prompt, and the remedy is one hop away in either sibling message or in
plan-merge.py's own `--help`.

**Pre-existing, not introduced by this diff, not filed:** `gh-sync.py` mixes a hyphen (`"ERROR -
{e}"`, 15+ pre-existing call sites, e.g. `:273`, `:886`, `:1006` at merge-base) and an em dash
(`"ERROR — {msg}"`, the `die`/`skip`/`refuse` triad at `:150-165`) for the same prefix. Confirmed
present unchanged at merge-base `9f2a0702bda6de929d42506f5aced2496669a2dc`; this feature touches
neither the mixed lines nor their wording. Per this role's own precedent (G-11/P-11 class): record,
do not file against code the diff never touched.

## Accessibility / theme parity

N/A — every surface reviewed is batch CLI text with no colour-only state encoding and no rendered
theme. (The two generated `.html` ship-review files carry `prefers-color-scheme` + explicit
`data-theme` token pairs for both themes, but they are out of scope per the census above; noted
only in case a later cycle wants it.)

## Known limit

Source-only audit — I did not render anything. Nothing in this diff has a surface that rendering
would add information about (no `.svg`, no live HTML/CSS besides the two generated, footer-marked
reports).
