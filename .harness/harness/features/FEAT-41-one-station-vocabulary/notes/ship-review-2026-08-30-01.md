# FEAT-41 — one station vocabulary: the build cannot be dispatched, and that is the plan working as signed

**2026-08-30 · orchestrator run 2026-08-30-01 · worktree `.claude/worktrees/harness/FEAT-41-one-station-vocabulary` · tip `855a356`**

## The conclusion first

**FEAT-41 is signed, green and ready to build — and not one of its 13 tasks can be executed by me
or by any squad member.** Every task carries `execution_mode: main-session-direct` in the plan you
signed. That is not a defect in the plan; it is the plan being correct about its own subject. This
feature changes the harness's own hooks, validators and gate scripts, and DEC-174 reserves exactly
that work to your hand.

**What I am returning to you is the build itself**, as eight dependency-ordered segments below,
with each task's `verify:` so nothing needs re-derivation.

**Nothing was silently dropped, narrowed, or marked done.** All 13 tasks remain `status: pending`.
The station remains `Ready`. `cycles_used` remains 8 of 10 — I consumed none of your two remaining
cycles, because no rework happened and no squad ran.

## Why no squad could take this — measured, not argued

I probed both directions with the real gate rather than reasoning from the lane table.

**1. I am denied every surface.** A genuine `PreToolUse` payload carrying
`agent_type: harness-orchestrator` is refused at **exit 2** on every surface class this plan
touches — `.claude/skills/harness/bin/**`, `.harness/harness.json`,
`.agents/skills/harness/templates/**`, `.claude/settings.json`, `.omp/agents/**`, both `SKILL.md`
paths, `.harness/team-config.yaml`, `.harness/harness/docs/DECISIONS.md`. The hook names my
permitted set back to me: the feature directory, my answers and ship-review notes, my Expertise,
my observations log. Nothing in this plan is inside it.

**2. No member is granted them either.** `check-domain.sh --resolve` returns **NOBODY** for the
templates, `.claude/settings.json`, both agent trees, both `SKILL.md` paths and
`team-config.yaml`. I probed the best-granted candidate directly: `harness-backend-dev` is refused
`.claude/settings.json` at exit 2. T-08 registers a hook there, so T-08 has no member route at all.

**3. DEC-174 bars the remainder.** Amendment 4 enumerates `check-domain.sh`,
`bash-write-guard.sh`, `validate-digest.py`, `check-state.sh`, `check-plan-routes.py`,
`dispatch-guard.sh` **and the test file of each**, and declares the category — hooks, validators,
gate scripts — governing with the list non-exhaustive. T-02, T-04, T-06, T-07, T-09, T-11 and
T-14 all land in it. `DECISIONS.md:6733` says it outright: *"DEC-174 forbids the orchestrator
`main-session-direct` tasks."* That sentence sits in DEC-200's lineage prose rather than in
DEC-174's own section, which is why grepping the id found it and reading the entry would not have.

**4. The gate says the same thing in one run.** `check-plan-routes.py` exits 0 and prints, for all
13 tasks, either `OK <task>: declared main-session-direct (... ungranted)` or
`DEVIATION <task> ... granted to harness-backend-dev, harness-dev-ops but declared
main-session-direct`. **Not one task prints the ordinary team-lane shape.**

**This shape is precedented — FEAT-41 is the fifth, not an anomaly.** `check-state.sh`'s INV-17
already emits, for FEAT-21, FEAT-40, FEAT-22 and FEAT-15: *"exempt from handoff notes — every task
in its plan.yaml is execution_mode main-session-direct (DEC-174), so no squad ran and no seam was
crossed."*

## Gate verdicts — run by me at `855a356`, in the worktree

| Gate | Verdict | Evidence |
|---|---|---|
| `check-state.sh` | **PASS, exit 0** | Zero hard failures. 13 NOTE lines, **none concerning FEAT-41**. The plan-phase violation — this feature's unapproved BRIEF — closed at your signature exactly as predicted. |
| `check-plan-routes.py` | **PASS, exit 0** | `0 violation(s) across 1 plan(s)`; 40 dirs examined, 39 skipped as shipped. Known-good shape, unchanged. |
| `run-unit-tests.sh` | **PASS, exit 0** | Full suite, 3374 lines, **0 `FAIL` lines**, ~248 s. This is the clean pre-build baseline: any red the build produces is the build's. |
| **qa gate** | **NOT RUN — correctly** | The matrix is enforced against a diff. There is no diff: no build work landed. Running it would grade an empty change. |
| **reviewer panel** | **NOT RUN — correctly** | `review_sha` is `49638bf`, which contains no build work. A panel dispatched now reviews a tree the work is absent from and returns PASS on nothing (my own P-02). |
| **goal-check** | **NOT RUN — 0 of 13 SCs met** | No task executed, so no criterion can be met. Not a failure; an unstarted build. |

I did **not** re-pin `review_sha`. DEC-89 re-pins when the build's own commits land; none did.

**SC baselines measured at `855a356`**, so you inherit numbers instead of re-deriving them —
SC-02's quoted-literal grep returns **27** (target 0), SC-01's `_STATION_KEYS` returns **7**
(target 0), SC-12's `plan-merge` grep returns **35** (target 0). The last matches the BRIEF's own
reading at `0d4845b`, so the rebase moved none of them.

## The build, as eight segments

Dependency-ordered from `depends_on`. **Segment 3 groups T-04 with the two tasks independent of
it** — a T-04 failure then wastes neither. T-04 is the riskiest task in the feature: it migrates
every live `plan.yaml` and rewrites INV-26's compare side.

| # | Tasks | `verify:` |
|---|---|---|
| 1 | **T-01** stations declared once in `harness.json` | `test-factory-config.py`; then `grep -rn --exclude-dir=__pycache__ "_STATION_KEYS" .claude/skills/harness/bin/ ; test $? -eq 1` |
| 2 | **T-02** lowercase everything · **T-03** the writer's station verbs | T-02: `test-gh-board.py`, `test-board-lifecycle.py`, `test-factory-claim.py`, `test-factory-land.py`, `test-check-state.py` · T-03: `test-plan-merge.py` |
| 3 | **T-04** retire pending/building/done · **T-05** document the one route · **T-08** gate `sign-approval` | T-04: `test-check-plan-routes.py`, `test-check-state.py`, `check-plan-routes.py`, plus its two inline `python3 -c` assertions · T-05: the per-file loop over both orchestrator files, then `test-orchestrator-playbook.py` · T-08: `test-plan-sign-gate.py` + the three `harness.json`/`run-unit-tests.sh`/`settings.json` assertions |
| 4 | **T-06** one projection function · **T-09** deny every editor and shell write | T-06: `test-gh-board.py`, `test-check-state.py`, `test-gh-sync.py`, the `set_station(` count = 4, and `grep -n "_EXPECT\|_st26" check-state.sh ; test $? -eq 1` · T-09: `test-check-domain.py` |
| 5 | **T-07** delete `feature.json.status`, repoint eleven readers | `test-validate-feature-json.py`, `test-check-state.py`, `test-worktree-terminal.py`, `test-check-plan-routes.py`, `test-board-lifecycle.py`, `test-gh-sync.py`, the `"status"` absence grep, and the 10-properties/7-required schema assertion |
| 6 | **T-10** both ship defects + the one-time board pass · **T-11** delete the unreachable test | T-10: `test-gh-sync.py`, `test-post-merge-sweep.py` · T-11: `test-check-state.py` + `grep -n "Icebox\|Drafted\|Primed\|Shipped" ... ; test $? -eq 1` |
| 7 | **T-14** stale `review_sha` as INV-32 | `test-check-state.py` captured to a file, then `grep -q "ok - case (inv32.a) a stale review_sha is reported"` |
| 8 | **T-13** rename `plan-merge.py` → `plan-write.py` | the tree-wide `plan-merge` absence grep, then `test-plan-write.py` |

**T-03 before T-09, always.** D-03 is load-bearing: T-09 closes the `Edit` route to `plan.yaml`,
and T-03/T-05 open the replacement. Reversing them leaves no legal way to record a task status —
the failure that already cost five unrecorded statuses.

## Two things I recommend you settle before segment 1

**Q13 — a success criterion nothing grades.** SC-02's own quoted-literal grep appears at
`BRIEF.md:110` and `:113` and in **no task's `verify:` block**. As written, SC-02 is graded
against a command the plan never runs. One line appended to T-01's or T-04's verify closes it.
I did not add it: the plan is signed and its tasks are not mine to edit.

**Q12 — my predecessor's note recorded a false premise, and I am correcting the record rather
than the plan.** The previous STATE.md called the `.harness/harness/docs/**` lanes row vestigial
because "no surviving task touches that surface". **A surviving task does:** T-13 lists
`.harness/harness/docs/DECISIONS.md` at `plan.yaml:1177`, and that path resolves to
`harness-documentor`. The row is live. It stayed invisible because `check-plan-routes.py`
short-circuits on `nobody_paths` before reaching the DEVIATION branch, so T-13 reports OK on its
three ungranted paths and its one granted surface is never printed. This changes nothing about
executability — T-13 is main-session-direct and carries NOBODY paths regardless — but BRIEF.md's
claim that the plan has no team-lane task is now the thing a reader could misread.

## Scope boundary — untouched, as ruled

I recorded **none** of the three deferred clause corrections (`DEC-203` §6, `DEC-191`, `DEC-182`)
and **no** new station-vocabulary decision entry. They belong to FEAT-46 by your Q1 ruling; D-09
is intact so FEAT-46 inherits the content. T-13 touches `DECISIONS.md` only to rename
`plan-merge` references, which is a different act and stays in scope.

## Proposed backlog

Strike any row you do not want; **unstruck rows become backlog issues, and anything not listed
here dies silently.**

| ID | Nature | Item |
|---|---|---|
| B-2 | bug | Q11: the shell substitutes phantom pathnames for an unmatched glob member, so `grep -rn PAT dir/*/f.yaml ; test $? -eq 1` exits 2 on ENOENT even when it matches. This idiom is used harness-wide in `verify:` blocks and is unsound. |
| B-3 | chore | Q10: gitignored `__pycache__/*.pyc` defeat every absence-grep over `bin/`. Make `--exclude-dir=__pycache__` a standing convention, or have `run-unit-tests.sh` clear the cache. |
| B-4 | bug | Q8: nothing allocates run-dir slugs; a same-day collision destroyed a run digest on 2026-08-29. Not re-triggered this run only because no squad ran. |
| B-5 | chore | `check-plan-routes.py` short-circuits on `nobody_paths`, hiding the lane of every other surface on the same task (the Q12 mechanism above). A task's granted surfaces should still be reported. |
| B-6 | chore | I cannot file a harness defect via `xd://report_issue` — `check-domain` denies the orchestrator that path — so B-2 and B-3 reach you only through this table. |

## Provenance — read this before trusting the completeness above

**I spawned no report round.** Per the playbook I read from disk instead. Assembled from:
`STATE.md`, `feature.json`, `plan.yaml`, `BRIEF.md`, `notes/answers-2026-08-29-01.md`,
`notes/ship-review-2026-08-29-01.md`, `notes/handoff-plan.md`, and the `runs/` tree (14 dirs;
`2026-08-29-02-product` is referenced by `feature.json` but its dir is absent — the Q8 collision
casualty, recorded rather than reconstructed).

**Its own gate verdicts are mine, run at `855a356`, not inherited.** The qa gate, the reviewer
panel and the goal-check are reported as NOT RUN with the reason each — none is reported as
passing.
