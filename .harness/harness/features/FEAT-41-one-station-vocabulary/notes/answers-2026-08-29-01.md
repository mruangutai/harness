# Operator answers — replan briefing ship-review-2026-08-29-01, 2026-08-29

Collected in one pass by the main session; this is the complete set. One consolidated revision.

## Q6 (blocking) — RESOLVED: scope INV-32 to non-terminal stations

The orchestrator's recommendation is adopted. T-14's INV-32 must not fire on a feature at a
terminal station; use the idiom check-plan-routes.py already uses. FEAT-27's pin is NOT repaired —
shipped history stays untouched.

## Q7 — RESOLVED: fold into the Q6 edit

T-14's precondition becomes "a real pin exists AND the station is non-terminal", replacing the
lagging "records a validator run". One edit site with Q6.

## Q1 (blocking) — RESOLVED: DROP T-12 from this plan

The operator declines the external dependency. T-12 is removed from plan.yaml entirely — not
deferred in place, removed. Its subject is re-filed with the decisions-authority triage effort,
outside this feature. pm owns the consequences: renumbering or gap-noting per plan convention,
dependency edges, and any REQ/SC whose only discharging task was T-12 must be re-scoped or moved to
the BRIEF's residual record honestly — no criterion may survive with no task behind it.

## Signature — SIGN after this revision lands

The main session writes approval.status: approved in plan.yaml and ## Approval in BRIEF.md once the
consolidated revision (Q6+Q7 fold, T-12 removal) is committed and the gates re-run green at a
re-pinned review_sha. pm does not self-approve.

## Backlog — NO STRIKES

All seven residuals B-1..B-7 stand and become backlog issues. Filed by the main session, not the
squad.
