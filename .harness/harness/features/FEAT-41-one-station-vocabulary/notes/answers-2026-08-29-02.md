# Operator answers — prosecution panel, 2026-08-29 (round 2)

Three readers (fable-advisor, harness-pm, scout) prosecuted the signed plan at pin `0dbd284`.
Panel outputs: `history://FableProsecutor`, `history://PmGoalCheck`, `history://ScoutLiveReqs`.
The main session verified four claims at source before ruling. This is the complete decision set.

## Already applied by the main session — do not redo

**Re-pin.** `review_sha` moved `0dbd284` -> `49638bf22b88c5f404de5866d13aa71d31d84c88` in commit
`92d279c`. Cause: the operator's signature commit `49638bf` rewrote plan.yaml's approval block after
the plan's own measurement was taken at `cc00983`, so the signed claim "the reported set is EMPTY"
was false at HEAD — the set was {FEAT-41 itself}, station Plan, non-terminal, NOT silenced by the Q6
scope. Byte comparison at the new pin is IDENTICAL; check-state.sh clean. T-14's intent text still
carries the stale "reported set is EMPTY" claim and the `cc00983` measurement — **correct that prose
to name the re-measurement at HEAD and the new pin.** No re-pin tool exists (backlog #965).

## A1 — STRIKE T-13, and SC-05 with it

Three independent readers converged. SC-12 pre-authorizes the strike. It is a pure rename (35 lines
across 13 files, hand-built under DEC-174), and its `files:` list includes
`.harness/harness/docs/DECISIONS.md` — this feature has no standing to rewrite a decision record
after the Q1 ruling stripped it of decisions-authority work.

- Remove T-13 entirely, as T-12 was removed. Ids stay gap-noted, never renumbered.
- **SC-05 is struck with it** — it requires the denial message to name `plan-write.py`, which T-09
  forbids in capitals. That contradiction is why pm's Q3 was blocking; the strike dissolves it
  rather than answering it.
- **SC-12 is struck with it** (it exists only to verify the rename).
- T-09 keeps the name `plan-merge.py` throughout. Check every surviving task and SC for a
  `plan-write.py` reference and remove each one; the name must not appear in the plan at all.
- Record in the BRIEF's residual/PB record that the rename is not happening and why.

## A2 — T-07 is broken as specified; fix it (not a preference, a defect)

Verified at source: `SEAM_NOTES[_status]` is a bare dict index at `check-state.sh:637`, and
`SEAM_NOTES` (`:539-552`) is keyed capitalised in parallel with `STATUS_ORDER` (`:538`). T-07 rekeys
STATUS_ORDER to lowercase and **no task names SEAM_NOTES**. That is a KeyError for every feature
whose station is not Backlog or Plan, the moment T-07 lands.

- Add `SEAM_NOTES` to T-07's intent and rekey it in the same edit as STATUS_ORDER. Both structures
  are one vocabulary; they move together or the gate crashes.
- Audit T-07 for any OTHER capitalised-key structure indexed off a station value and name each one.
  Two parallel structures were found by hand; assume there is a third until proven otherwise.
- **SC-02 is run by no verify block anywhere.** Add its grep to T-07's `verify:` so the criterion can
  actually fail. An SC no command evaluates is not a criterion.

## A3 — PB-04: record the contradictions HERE, on the documentor lane

pm's Q4 is correct and the main session confirmed it at `DECISIONS.md:5945-5947`: DEC-188 prescribes
**amendment** for anything softer than a flat contradiction, and striking needs the operator's word
first — which already answers PB-04's strike-vs-subsume question and dissolves the external
dependency it was waiting on.

- Add ONE task on the `harness-documentor` lane (the team lane left empty by T-12's removal, so this
  costs no main-session hand-build) that AMENDS — never strikes — the one contradicted clause each in
  DEC-203 s6, DEC-191 and DEC-182, naming FEAT-41 as the amending feature.
- Rationale, in the task: DEC-188's enforcement is "a human reading a diff" and holds "only while the
  striking actually happens every time". Deferring the recording out of this feature spends the one
  moment that enforcement exists.
- Add a matching SC with a real verify (the amended clauses are present and cite FEAT-41).
- This task does NOT touch decision CONTENT beyond the amendment records. D-09 stays as-is.

## Explicit non-goal this run — do not implement, do not silently adopt

**Scout's T-14-first fast path to #867 is NOT authorized.** Building T-14 against today's
`feature.json` terminal read (rather than plan.yaml's lowercase station after T-07) remains an open
operator question. Build the plan in its signed order. If the budget shows strain during build,
raise it as a question — never adopt the fast path as a builder's improvisation.

Also carried, unresolved, for the ship briefing rather than this revision: fable's dispute of the
value ranking (it argues T-10, not T-14, is the feature's highest-value task, and that T-04/T-07's
migrations will mass-stale every shipped pin and make the Q6 terminal scope permanent).

## Signature

Striking T-13 is a scope change to a signed plan, so approval returns to `pending` in BOTH
plan.yaml and BRIEF.md as part of this revision. The main session re-signs after the gates are green
at a re-pinned `review_sha`. pm does not self-approve.
