# FEAT-41 · one station vocabulary — replan at a new pin, ready for your signature

**The plan survived the migration and is now signature-ready. Nothing is signed; that decision is
yours.** The branch was rebased onto a main carrying FEAT-42-one-root-resolver, FEAT-40 and PR #922
(OMP supervision), which moved 187 files under a plan authored against the older tree. Every one of
the plan's line anchors, counts and quoted snippets had become a claim about a file that had shifted.
Thirteen of fourteen tasks were re-derived against current source. One task lost a third of its
scope. No task died.

**Three blocking conditions stand between this plan and a signature — Q1, Q6 and the T-14 scoping
decision. All three are decisions, not defects, and two of them are one line of work each.**

## What the audit changed, and why

| | |
|---|---|
| Collision surface | 187 files changed between the old pin `e5afc19` and `0d4845b`. The plan names 57 distinct files; **28 changed**. 13 of 14 tasks touch at least one |
| Carried untouched | **T-03 only** — its two files are byte-unchanged since the old pin |
| Re-derived | The other 13, each surviving anchor re-opened at HEAD and re-verified rather than trusted |
| Dead | **One clause**, inside T-10 |
| Signature state | `pending` in both `plan.yaml` and `BRIEF.md`, unchanged |

**There is no path rename, and that is the single largest false alarm this audit killed.**
`.agents/skills` is a tracked symlink (mode 120000) to `../.claude/skills`. Every
`.claude/skills/harness/bin/...` path in the plan is still canonical. Had we "migrated" those paths
we would have rewritten 50-odd correct lines into a second, diverging spelling.

**T-10 lost its repair clause.** Its "close the live INV-26 violation" had no subject left: FEAT-40
shipped, its `feature.json` reads `Done`, `check-plan-routes.py` now skips its plan as shipped, and a
full `check-state.sh` run emits **zero** INV-26 lines. The violation closed itself by merging. It was
also redundant a second way — T-07's migration, already a dependency of T-10, is what writes that
station. T-10's two ship defects are real and are kept. SC-09 was re-based onto the clause that
still fails today.

**T-14 was NOT the casualty it was briefed as.** I dispatched this audit expecting T-14 to be the
certain loss, because `check-state.sh` now resolves its root through
`harness_boundary.resolve_root` with refuse-don't-guess semantics. That was wrong. Its insertion
point is fully intact — the per-feature loop at `:202`, `val()` at `:227`, the INV-6 block at
`:246-253` — and INV-32 is still a free number, INV-31 being the highest at both pins. I opened those
lines myself rather than take them from the digest. What the migration falsified was T-14's stated
*reason*, not its instruction. The reason was corrected; the instruction stands.

**Three verify blocks could never have passed, and were latent long before the rebase.** Two
absence-greps were quietly satisfied by gitignored `__pycache__/*.pyc` files carrying the
searched-for string as a compiled constant. The third could never return the exit code it tested
for. These are the kind of verify that reports green by never being able to report red.

## The pin

`review_sha` was `e5afc19`, which the rebase turned into a dangling object — it still resolves in the
object database but is no longer in branch history, so it silently claimed a review of text that no
longer existed on the branch. It is now **`cc009835da1fd6f33a99127bc80d7a1a6075db3d`**, the commit
carrying the replanned artifacts. `lanes.resolved_at: 0d4845b` was deliberately left alone: the
lanes were resolved against that tree, the replan changed no source file, and it remains truthful.

Commit ids quoted inside task prose as historical measurements were also left alone. They record what
was true when written, and rewriting them would falsify the record.

## Gates

Both run by me, in the worktree, at the new pin.

- `check-plan-routes.py` — **exit 0**, "0 violation(s) across 1 plan(s)", 40 dirs examined, 39
  skipped as shipped.
- `check-state.sh` — exit 1 with **exactly one** violation: FEAT-41's BRIEF is not approved. That is
  correct during a plan phase and closes the moment you sign. Its output is **byte-identical to the
  pre-replan baseline**, so the replan introduced nothing.
- `validate-feature-json.py` — exit 0. `plan.yaml` parses, 14 tasks, approval `pending`.

## What you must decide

1. **Q1 — T-12's external dependency (blocking).** T-12 cannot be fully executed until the
   decisions-authority triage lands a recording form: an in-place clause strike under DEC-188, or the
   correction subsumed into the entry in one voice. The task STOPs and asks rather than guessing.
   **Signing accepts that dependency.** Unchanged by this audit.
2. **Q6 — T-14 would turn a shipped feature red (blocking).** I re-measured this at the new tree and
   it still holds. INV-32 as written fires on two features: FEAT-41, the intended catch, and
   FEAT-27-expertise-repository-tier, which is `Done` with PR 574 merged. FEAT-27's pin resolves and
   its `plan.yaml` bytes have moved; its whole post-pin diff is an approval-amendment record plus one
   task flipping to done — legitimate record-keeping, not a false review claim. **The moment T-14
   executes, `check-state.sh` goes red on a shipped feature and stays red.** Two one-line fixes:
   scope INV-32 to non-terminal stations, an idiom `check-plan-routes.py` already uses, or repair
   FEAT-27's pin. I recommend scoping.
3. **Q7 — the guard is inverted (recommend folding into the Q6 fix).** T-14's precondition "records a
   validator run" is a lagging indicator: INV-32 can only fire *after* a panel has already read the
   wrong text, which is exactly how this feature's own divergence survived. Recommend the
   precondition become "a real pin exists AND the station is non-terminal".

## Disclosure

**No report round was spawned.** Per DEC-69 I assembled this from artifacts on disk, and I am naming
them so you can tell a complete briefing from a partial one:

- `runs/2026-08-29-01-product/digest.md` — this run's lead digest
- `notes/research-FEAT-41-replan-collision-audit.md` — pm's audit, the substantive artifact
- `STATE.md`'s prior `## Current`, which carried the plan-phase record
- my own measurements in the worktree, cited inline above

**I did not re-read the thirteen earlier run digests.** This feature has never left the plan phase;
those runs were plan-support reviews whose conclusions are already recorded in the plan and in
STATE.md, and the subject of this briefing is the replan. They remain on disk under `runs/` if you
want any of them. That is a deviation from the ship-briefing rule and I am flagging it rather than
letting it pass silently.

**One thing in this briefing corrects me, not the squad.** pm reported the third broken verify as a
non-POSIX glob defect. Its own lead reserved on that, and I agreed with the lead — the mundane
reading seemed to cover it. I then measured it, and **pm was right**. See B-1.

## Budget

`cycles_used` **8 of 10**, one send-back this run: product-lead returned a PASS whose *digest file*
was missing six required contract fields, which `check-state.sh` caught and the lead then repaired.
`runs` **14 of 20** — under budget, no INV-22 note for this feature. Two cycles of headroom remain,
which is enough for the Q6 fix and a goal-check but not for an open-ended build. If the answers to
Q1/Q6 expand scope, the ceiling should be raised deliberately rather than discovered mid-build.

## Proposed backlog

Residual findings that do not gate this signature. **Anything you do not strike becomes a backlog
issue; anything not listed here dies silently, so this is the full set.**

| ID | Nature | Finding |
|---|---|---|
| B-1 | bug | **Shell pathname expansion is non-POSIX.** In a clean dir holding only `a/f.txt`, `echo */f.txt` prints `a/f.txt b/f.txt`, and `echo */nomatch.txt` prints both members instead of the literal pattern — the shell substitutes phantom paths that do not exist. So `grep -rn PAT dir/*/f.yaml` errors ENOENT on each phantom and exits 2 even when it finds matches, making `grep … ; test $? -eq 1` unusable harness-wide. Measured twice: minimal probe, and the real tree (31 of 40 dirs held the file, 56 matches found, still exit 2). I could not file it via `xd://report_issue` — `check-domain` denies the orchestrator that path |
| B-2 | chore | Gitignored `__pycache__/*.pyc` defeat every absence-grep over `bin/`, carrying the searched string as a compiled constant. Make `--exclude-dir=__pycache__` a standing convention, or have `run-unit-tests.sh` clear the cache |
| B-3 | bug | Nothing allocates run-dir slugs. An earlier run was dispatched into a slug the same day's earlier run already held and silently overwrote its `digest.md` and `state.yaml`. `runs/` is gitignored, so that loss is unrecoverable |
| B-4 | bug | A lead's structured return can satisfy the digest contract while the `digest.md` it writes does not. That happened this run and cost a cycle. Nothing validates the file at write time — only `check-state.sh` catches it, later |
| B-5 | chore | The orchestrator has no route to file a harness defect: `xd://report_issue` is outside its domain, so defects can only travel up as prose in a return |
| B-6 | enhancement | PB-03, carried: does `check-state.sh` INV-26 flag issue 223 once T-06 routes the compare through `project()`? 223 is a parent card, not a task sub-issue. T-10 STOPs and reports rather than adding the card, so it fails safe either way |
| B-7 | chore | DEC-89 already decides T-14's invariant — a hand edit never inherits a passing review — and says the state check re-pins `review_sha`. Nothing implements that re-pin; the clause lives only in agent prose. T-14 builds the detection half; the re-pin half stays unbuilt |

## Recommendation

**Sign, with the Q6 scoping fix folded into T-14 before build starts.** The plan is in better shape
than it was before the rebase: three verifies that could never have failed now can, one redundant
task clause is gone, and every anchor a builder will follow has been opened at the commit they will
build against. The two blocking questions are genuine decisions — one about how a decision record is
amended, one about whether a new invariant may redden a shipped feature — and neither is discoverable
by more analysis.
