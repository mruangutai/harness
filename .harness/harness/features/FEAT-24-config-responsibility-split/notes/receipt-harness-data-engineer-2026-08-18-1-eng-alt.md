# ALTITUDE angle + J2 distributive-SC judgement — FEAT-24 — harness-data-engineer

## Headline

SC-10 has no implementing verify anywhere in the plan — it is a genuine gap, not a file-global-vs-
per-item shortfall. SC-04's "both entry points" claim is only actually exercised at one entry point.
SC-07's per-consumer claim covers only 1 of 3 named consumers with a named assertion. SC-02, SC-03
and SC-11 (aside from one unbounded-slice defect, currently latent) verify correctly. ALTITUDE itself
turns up nothing that would cost a build cycle — the split is at the right depth (one validator, one
remote primitive, station resolution lives in the module, not bolted onto callers).

## J2 — distributive SC verification, six rows

| SC | Implementing step | Actual command | Per-item or file-global | Smallest fix if file-global |
|---|---|---|---|---|
| SC-02 | T-02 verify, plan.yaml:351-354 | `for k in backlog ready building review done; do has "validate_board accepts the five-key stations map: $k"; has "validate_board rejects a stations map missing $k"; done` | **Per-item.** Five named ok-lines, one per station key, each independently greppable; a reverted single-key lookup reddens exactly one line. | n/a |
| SC-03 | T-04 verify, plan.yaml:560-561 (gh_board.py); T-05 verify, plan.yaml:696-699 (INV-26 slice) | `grep -nE '"(Building\|Review\|Backlog\|Ready\|Done\|Plan)"' <file>` plus a positive control (`grep -qF "def derive_station"` / slice contains `derive_station`) | **Per-file**, which is the SC's own claimed granularity ("asserted per file"). The alternation grep is a negative assertion (none of the six literals may appear) — a single command correctly catches presence of *any* one, so this is not the "passes if one of N satisfies" antipattern; it is the correct shape for "prove absence across N literals in one file." | n/a |
| SC-04 | T-04 verify, plan.yaml:552-554 (8 shapes, via `gh_board.load_board`) | `for shape in "no board key" ... "a station value is empty"; do has "load_board raises naming the file and the key: $shape"; done` | **Per-item, but at only ONE of the two claimed entry points.** SC-04 text: "from the single shared validator and from both of its entry points." T-02's own test list (plan.yaml:433-450) exercises `validate_board` only for the 5 stations-missing shapes plus 2 product_config/board_for cases — it never asserts "board is not a mapping", "owner missing", "number not an int", or "station_field missing" through `board_for`/`product_config`, the fleet-side entry point. | Add the same 8-shape loop to T-02's verify, run against `board_for`/`product_config`, with ok-lines like `product_config's board_for raises naming the file and the key: <shape>`. |
| SC-07 | T-03 verify, plan.yaml:475-488 | Named per-case check only for `test-factory-land.py`: `has "(M1) pr create base is the fleet's default_branch"`. For `test-factory-claim.py` and `test-factory-decompose.py` (the other two named consumers, `factory_claim` and — SC names `factory_workspace`, not `factory_decompose` — see below) the loop only checks `grep -qE "^ok"` (suite produced *some* passing output) and absence of `FAIL` — a file-global "did it run and stay green" check, not a named per-consumer default_branch assertion. | **File-global** for `factory_claim`; **absent entirely** for `factory_workspace` — no task in the plan touches `factory_workspace.py` or a `test-factory-workspace.py` file, and no verify block names a `default_branch`-consuming assertion for it (only D-02's decision prose cites `factory_workspace.py:115`, plan.yaml:161,879,1122). | Add a named ok-line assertion for `factory_claim`'s default_branch read (mirrors the land case) to T-03's verify, and add a `test-factory-workspace.py` case (or a named assertion against the existing one, if it exists outside this plan's file list) asserting `default_branch` resolves with no checkout present, wired into a task's verify by exact ok-line text. |
| SC-10 | **None.** No task's `verify:` block references any of the 8 reader files or 3 non-reader files by a grep/assertion for "moved key" presence or absence. | n/a — the only evidence is prose in the plan's own `resolved_but_not_written` lanes section (plan.yaml:95-141) and `notes/research-FEAT-24-config-split.md:30-47` ("Reader classification"), where the plan's author states results of a one-time manual `grep` at `ada8e99` ("Not a reader."). This is planning-time evidence, not a repeatable automated check. | **File-global-to-nonexistent** — there is no implementing step at all despite `verify: automated, evidence: integration`. | Add an integration test (or a task verify block, e.g. appended to T-10 or a new light task) that greps each of the 8 reader files for absence of every retired key spelling (`repos[].board`, `project_id`, `status_field`, `in_progress_option`, bare three-station literals) after migration, and greps the 3 non-reader files named in the research doc's classification — `wayfind.py`, `layout_migration.py`, `check-plan-routes.py` (each explicitly "Not a reader", research doc :37-38,43-44) — for the same absence, one named ok-line per file. `harness_boundary.py` and `run-unit-tests.sh` are excluded: neither is a "moved key" survey result (they're resolved for unrelated reasons — name/workspace_root stability and test registration). `branch-create-gate.sh` is also excluded: its absence-of-pinned-ids is already covered by the pre-existing `test-branch-create-gate.py:55`, unrelated to this feature. |
| SC-11 | T-10 verify, plan.yaml:1071-1097 | DEC-174 section is correctly bounded: `src[i174:i175]` where `i175 = src.find("## DEC-175")`. DEC-196 section is **not** bounded on the right: `if i196 < 0 or "amendment 1" not in src[i196:]` and `src[i196:]` used again for the anchor check — both search from `## DEC-196` to **end of file**, not to the next `## DEC-` heading. | **Per-entry today, by accident** — DEC-196 is currently the last entry in `DECISIONS.md` (verified: `grep -n "^## DEC-" DECISIONS.md \| tail` shows DEC-196 at :6057 is the final heading, file is 6095 lines), so `src[i196:]` happens to equal DEC-196's own section. If any decision is appended after DEC-196 before this verify runs, the check would pass on "amendment 1" text found in that later, unrelated entry. | Bound it the same way as DEC-174: find the next `## DEC-` heading after `i196` (or default to `len(src)` if none) and slice `src[i196:i197]`. |

## ALTITUDE findings

1. **file** `.harness/harness/features/FEAT-24-config-responsibility-split/BRIEF.md` · **line** 105-113 · **summary**: the `## Verification gaps` section names two accepted residuals (null test kinds, kaya inspection) but not the SC-10 gap found above — SC-10 claims `evidence: integration` with no implementing step. **cost**: a residual is accepted silently rather than named, so a later reader trusts SC-10 is covered by the integration suite when it is not. **fix**: either implement the SC-10 check (see J2 table) or, if descoped, add a third `## Verification gaps` row stating explicitly that file-migration completeness rests on the planning-time grep in `lanes.resolved_but_not_written`, not on a repeatable test. **fold-in** (would-cost-a-build-cycle).

2. **file** `plan.yaml` · **line** 393, 879, 1053, 1122, 161-168 (D-02) · **summary**: the clause "`default_branch` cannot move into the checkout because `factory_workspace.py:115` reads it to create the checkout" is restated near-verbatim in D-02's `because`, and again in T-02, T-07, T-09 and T-10's task intents. **cost**: none currently — each task is dispatched to an isolated agent with no shared context, so the repetition is required reading for that agent, not a second authority that can silently drift; D-02 remains the one *decision* record, the task intents are explanatory prose for isolated executors. **fix**: none needed. **leave** (cosmetic).

No other altitude defects found. Station resolution and remote-config reading are both placed in the
modules that own the data (`factory_config.validate_board` as the one shared validator; `gh_board.py`
delegates to it; `check-state.sh`'s INV-26 carve-out in T-05 reads station names through
`derive_station(plan_doc, board)` rather than re-deriving them) — capability sits at the right depth
throughout, not bolted onto callers (`check-state.sh`, `gh-sync.py`, `board-station.py`).

## Not re-derived (per dispatch)

check-plan-routes.py 0 violations; T-05/07/08/09 correctly main-session-direct; test-no-distribution.py
case3 fixtures; templates/harness.json `board: null` nesting; gen-decisions-index.py `--check` absence
at plan.yaml:1148 (already a logged must_fix, not repeated here); default_branch pre-clone reads;
kaya's live config stale-key nesting.
