# Observations - harness-pm

- 2026-09-05: BUG-1305 BRIEF. DEC-208 ruling 3's own scope sentence ("enforcement sits on the PRE payload route, which is Write-only, so an Edit ... reaches no enforcement point") is contradicted by this feature's probe: the Edit route IS content-checked. Two artifacts now carry the same false claim about one guard — check-domain.sh's inline comment and a signed decision entry. Raised as an open_question rather than widening scope.
- 2026-09-05: the upstream dispatch chain attributed the PyYAML fail-open to DEC-154; the ruling is DEC-171 (bootstrap grant, session-scoped, expires by construction). DEC-154 is "state.yaml is a checkpoint, not a notebook". Verified both headings at source before writing the constraint.
- 2026-09-05: BUG-1305 plan goal-check — the plan's remedy (marker keyed on run_id/feature/squad/host) cannot reach the diagnosis's own leading mechanism, where all four fields are equal by construction. Grading the remedy's KEY FIELDS against the failure scenario's field values is what exposed it; the task text and every anchor were correct.
- 2026-09-05: a probe task whose result no later task may consume (T-04 forbids writing a follow-up) is a measurement, not a route to closure. Check whether any task's intent can act on each of the probe's declared outcomes.
- 2026-09-05: BUG-1305 plan fix c1 — dispatch named notes/plan-fix-BUG-1305-c1.md as my artifact path; check-domain.sh denies it for harness-pm (permitted: notes/research-*.md, notes/uat-*.md). Wrote notes/research-BUG-1305-plan-fix-c1.md instead. plan-merge amend --show/--expect-sha256 round-trips cleanly for multi-KB literal block scalars (T-02/T-04/T-09 intents), preserving the | form.
- 2026-09-05: BUG-1305 c2 — a denial keyed on a SESSION signal cannot separate a resumed owner from a foreign run reusing the slug: both carry identical seed fields, differing sessions, by construction. Moving the refusal one moment earlier (creation vs continuation, `prior_state == ""` at check-domain.sh:1516) made it decidable from disk with no identity at all.
- 2026-09-05: plan-merge amend --show dedents block scalars, so an exact-string replace against `yaml.safe_load` output must use the DEDENTED text; my first two attempts asserted 0 matches. Reading the field back through safe_load and splicing by line index was the reliable route.
- 2026-09-05: a re-specified task leaves stale claims in OTHER task bodies. Retiring T-04 and adding T-10 falsified sentences inside T-01, T-02, T-03 and BRIEF Constraints ("no depends_on edge forces T-03 to land"). Grepping for every id and note filename after the re-spec caught four.
- 2026-09-05: check-instruction-paths.py exits 2 on a plan.yaml — its scope is agents/skills markdown (`--list-scope`), so "selects nothing in scope" is out-of-scope, not a gate failure.
- 2026-09-05: BUG-1305 — striking a symbol from a plan while WRITING A PROHIBITION that names it keeps the acceptance absence-grep non-empty; the first T-01 amend had to be redone under a fresh sha. Phrase the prohibition by shape ("no boolean taking an identity argument"), never by the struck name.
- 2026-09-05: BUG-1305 fix c3. Minting host search: grepped every writer of `state.yaml` under `.claude/skills/harness/bin/` — there is NO programmatic writer, only guards. So a field the harness must mint onto a checkpoint has exactly one host: check-domain.sh's PostToolUse `elif target:` branch (:1930-1950), which reads what landed. Worth re-deriving before any future "just have X write it" proposal.
- 2026-09-05: check-domain.sh:1931 already ASSERTS in a comment that POST sees NotebookEdit ("POST, with a named file: Write, Edit, NotebookEdit") while `.claude/settings.json` registers matcher `Write|Edit|Bash`. Nobody measured it; that comment is itself a candidate REQ-06-class false claim. T-11 measures it.
- 2026-09-05: plan-merge.py amend refuses a list field with exit 4 unless `--yaml-value` is passed, and `--show` on a list field fails the same way — so batching `--show` across mixed scalar/list fields needs two passes. Value files can live in /tmp: the write tool is permitted there, and bash-write-guard never sees a redirect.
- 2026-09-05: BUG-1305 c3 goal-check. Re-deriving the ladder from `git show c369fb1f:` found three defects the previous two goal-checks and the panel all missed, all of them introduced by the fixes that closed earlier findings: two tasks naming incompatible insertion points for the same compare, a write-once witness that can be born with a null seed field and permanently brick its own directory, and a requirement promising a recovery route (truncated prior) that the pre-change code already refuses and no task opens.
- 2026-09-05: pattern worth watching — when a predicate spec gives three of four fields a "recorded non-None" guard and the fourth none, that asymmetry is almost always the bug; read the guard clause per field, never per predicate.
- 2026-09-05: BUG-1305 c4 — two guards keyed on DIFFERENT visibility (checkpoint-internal vs sibling-file identity) cannot share an insertion point; the fix that made them one rule was to specify the DEFERRAL as part of the code (skip when the prior parses with a non-empty run_uid) and to parse the prior once above it, which keeps every existing ladder message byte-identical. Three cycles were spent on this before it was written that way.
- 2026-09-05: BUG-1305 c4 — plan-merge.py amend --yaml-value with a value file holding [] empties a traces list cleanly; check-plan-routes.py exits 0 and traces is not in harness_yaml REQUIRED_TASK_FIELDS, so emptying a retired task's traces is a legal way to stop a dead task reading as REQ coverage.
- 2026-09-05: BUG-1305 send-back 1. A pass that closes a "leading sentence disagrees with its enumeration" finding falsifies its own counts: adding SC-01(f) made "six cases and seven tests" false, and inserting a case made T-02 "the two above" point at the wrong bullets. Positional references (the two above) and bare totals both rot on insertion; naming the members survives. Fourth time in this feature a round fix planted the next round finding.
- 2026-09-05: counting claims cluster where a sentence summarises a list it does not own - T-08 said "three branches" over four bullets, T-09 "the two refusal cases" over three, T-10 "two notes" over a one-entry files list. Sweep pattern that worked: grep the artifact for number words, then re-derive each from the list/files/traces it summarises by loading the plan, never from the neighbouring prose.
- 2026-09-05 (BUG-1305 plan-fix-c5): no plan-merge.py verb reaches `lanes:` — `amend` is scoped to tasks|decisions and `apply` exits 7 CONFLICT on any top-level key whose value differs from the base. A plan that gains a new surface after its lanes block was written records the routing in the task's `execution_reason` instead; do not hand-edit.
- 2026-09-05 (BUG-1305 plan-fix-c5): to amend one field of a long `intent:` without retyping it, `yaml.safe_load` the plan in a python3 heredoc, string-patch the field with an asserted single-occurrence replace, write the value file, then `amend --expect-sha256`. Retyping a 160-line intent by hand is where a transcription defect enters a signed plan.
- 2026-09-05 (BUG-1305 plan-fix-c5): striking a task ripples into OTHER tasks' retirement bodies — T-04's "REQ-01 is covered by T-01, T-02, T-09, T-11 and T-12, every one of them live" went false the moment T-12 was retired. After any strike, grep the whole plan for the struck id, not just `depends_on`.
- 2026-09-05: BUG-1305 send-back 1 — my rank-2 remedy (guarding .run-identity.json) ADDED refusing branches, which falsified T-08 direction two's "the pairs are the five below" in the same pass; a remedy that adds a branch must be re-checked against every arity claim quantifying over branches of that kind. Fixed as a sixth pair; PF- ids unchanged because only resolution: text moved.
- 2026-09-05: BUG-1305 c6 — set-panel replaces the whole panel mapping, so re-keying panel.readers safely means safe_load -> mutate only readers -> safe_dump, never hand-transcription. Measured result: the round-trip re-emitted all nine summary block scalars and transcription_rule byte for byte, so the diff was one hunk inside readers: and no PF- id was at risk. A pre-flight comparing value-file-minus-readers to the baseline before taking the write makes the one-write bound safe.
- 2026-09-05: BUG-1305 goal-check. SC-11 unmet for a pure evidence reason: probe note carried the two column-0 answer lines but no command and no output, and T-11's verify was two greps for exactly those lines — the verify could not see the missing evidence (my own P-01, from the other side). When a criterion demands 'the verbatim command and output', the task's verify must grep for the fence, not the answer.
- 2026-09-05: BUG-1305 SC-13 asked for green-before on TWO guard scripts; the redproof note covered only check-domain. The suite itself carried committed pinned fixtures (tests/integration/fixtures/prior-*.fixture) plus a helper (bug1304_pre_change_guard), so the missing half was a 30-second replay rather than an unsettleable claim. Look for a committed pinned-fixture harness before grading a red-proof gap as unmet.
