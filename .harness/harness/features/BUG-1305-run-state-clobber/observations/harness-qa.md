# Observations - harness-qa

- 2026-09-05: BUG-1305 cycle 15 (F-1 mutation probe): a `CHECK_DOMAIN_BIN` copy of check-domain.sh placed outside the real `.claude/skills/harness/bin/` directory (e.g. a bare `/tmp` scratch file) silently loses its `import harness_boundary` inside the hook's own heredoc — `_root()`'s except-clause fires and the fixture's `CLAUDE_PROJECT_DIR`/`HARNESS_PROJECT_DIR` override is never consulted, producing a FALSE RED (exit 0, empty stderr) on every case regardless of mutation. Fix: also export `PYTHONPATH=<real bin dir>` alongside `CHECK_DOMAIN_BIN` so the heredoc's `sys.path.insert(0, _bin_dir)` still resolves `harness_boundary` via the PYTHONPATH fallback entry. Always sanity-check an UNMUTATED copy at the scratch location first (must reproduce the pin's own exit code/message) before trusting any mutant result from it.
