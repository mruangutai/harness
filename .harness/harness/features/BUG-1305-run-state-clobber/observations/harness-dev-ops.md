# Observations — harness-dev-ops — BUG-1305-run-state-clobber

- 2026-09-05: to probe check-domain.sh's Write/Edit content guards without touching a real `.harness/**/runs/**` path, root resolution is 4-levels-up from the script's own location (`harness_boundary.root_from_script`), so a synthetic fixture needs the script itself copied into a `$TMPDIR/.../.agents/skills/harness/bin/` tree plus a `.harness/team-config.yaml` marker (a malformed/empty one fails closed with "manifest does not parse", not a bypass) — copying the real team-config.yaml verbatim into the fixture works and is read-only against the source.
- 2026-09-05: `check-domain.sh:1236-1238`'s own inline comment ("intentionally Write/PRE-only... Edit... carries no complete incoming payload") is STALE — the runtime at `:1905-1923` reconstructs Edit content via old_string/new_string against the on-disk prior and re-runs the same #1058 prefix compare on it. Confirmed by probe: a mid-file Edit is denied identically to a Write; a pure-append Edit (old_string = last line, new_string = last line + new content) is allowed. `bash-write-guard.sh`'s docstring is the one that's accurate here.
- 2026-09-05: this repo's own `merge-settings.py:_matcher_covers` (`:151-176`) models the Claude Code host's PreToolUse matcher as `re.search`, not an anchored/full match — so an unanchored `"Write|Edit"` matcher already covers `NotebookEdit` via the `"Edit"` substring. Useful precedent for any future "does this matcher cover tool X" question in this repo, though the host's true algorithm is unverifiable from repo code alone.
