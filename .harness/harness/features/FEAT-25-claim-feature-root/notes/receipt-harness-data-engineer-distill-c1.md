# Distillation receipt — harness-data-engineer — FEAT-25 c1

BLUF: two of three relayed candidates accepted as new craft Patterns (P-04, P-05); the third
rejected as already covered by P-01. No observations log existed for this feature (single
ALTITUDE step, zero findings) — the only source material was the relay plus my own ALTITUDE
receipt.

## Candidate 1 — deferred re-stat cost, efficiency measured it and declined to flag

Rejected. P-01 already states the rule this teaches: state a measured cost in the unit matching
its frequency, not a bare number. The candidate's distinct content — defer an out-of-angle
observation to the angle that owns it rather than judging it yourself — is a one-off process note
about this feature's angle split, not a repeatable WHEN/DO that survives outside a
multi-angle review setup. Kept out.

## Candidate 2 — zero findings read as evidence when checks are enumerated

Accepted as P-04: "WHEN a review angle returns zero findings DO report the checks performed, not
just the count — enumerated checks let a zero read as evidence of coverage rather than silence."
Source: the digest's own framing, corroborated by my ALTITUDE receipt's "What I checked and judged
correctly placed" section, which is exactly the enumerated-checks structure the rule names. General
across any review-style task — passes the six-spawns test.

## Candidate 3 — new seam justified by what varies, not what could be drawn

Accepted as P-05: "WHEN judging whether a proposed new seam (module, class, layer split) is
justified DO check whether anything actually varies across it, not whether one could be drawn —
extending an existing classifier/renderer pair often needs no new seam." Source: my own ALTITUDE
receipt item 2 (`_blocker_gate` / `_blocker_reason_text` gaining a new kind rather than a parallel
classification path). This is my own judgment call, not a relay — worth more per the dispatch's
own framing.

## Counts

- Patterns: 3/15 before → 5/15 after
- Gotchas: 3/15 before → 3/15 after (unchanged)
- Outcomes: 0/10 before → 0/10 after
- Open: 0/5 before → 0/5 after

`check-expertise.sh` reports OK on `.harness/expertise/harness-data-engineer.md`.
