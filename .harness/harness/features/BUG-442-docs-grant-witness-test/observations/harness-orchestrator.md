# Observations - harness-orchestrator

- 2026-09-07 (BUG-442): plan-phase premise check. tests/integration/test-harness-yaml.py resolves REPO_ROOT from HARNESS_PROJECT_DIR/CLAUDE_PROJECT_DIR (line 29) but uses it for EXACTLY ONE thing, MANIFEST_PATH at line 32. Every other filesystem read in the file (template at :847, SPEC.md at :850, the guarded-import tree walk) goes through ROOT, derived from __file__ and immune to the env var. Method: grep REPO_ROOT|ROOT over the whole file. Consequence: repointing the env var at a scratch root perturbs only the two manifest tests, so a mutation-subprocess negative control cannot false-red on unrelated tests. Established by READING -- bash-write-guard correctly blocked the cp that would have staged a temp manifest, and the read answered it with no write at all.
