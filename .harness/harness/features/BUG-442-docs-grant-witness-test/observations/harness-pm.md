# Observations - harness-pm

- 2026-09-07: BUG-442 panel transcription. The dispatch named `apply --proposal -` to revise T-01; apply exits 7 CONFLICT on an existing id with a changed value (plan-merge.py:729-743) and only unions NEW ids. Demonstrated the exit 7, then used `amend --key tasks --id T-01 --field <f> --expect-sha256`, which also preserved the original | scalar form. Repo expertise P-04/P-08 already said this; a dispatch asserting otherwise is not evidence.
- 2026-09-07: BUG-442. Dispatch specified typed 8-hex PF- ids from sha256(reader|severity|summary). panel_findings.py:28-33 produces PF-+32hex from reader+newline+normalized summary, severity EXCLUDED. Used the tool: severity in the hash would have re-hashed finding 1 when the lead upheld med over pm advisory, i.e. the ruling would rewrite the id it rules on.
- 2026-09-07: BUG-442. Dispatch dictated `assert BUG442_MUTANT_CHILD not in os.environ` before mutant construction to make a leaked value fail loudly. The child inherits the var, so that assert fires IN THE CHILD and the clean-run rc==0 control can never pass. An inherited-env marker cannot distinguish leaked-parent from child at all; fixed by setting the child marker to the same fresh temp path as HARNESS_PROJECT_DIR. Probe first, then write the intent.
- 2026-09-07: BUG-442 panel.readers repair. plan.yaml's only write route is plan-merge.py (Edit is DENIED by check-domain, FEAT-41 REQ-05), and for `panel` the only verb is set-panel, which re-serializes the block through yaml.safe_dump. A "do not re-serialize / 2-line diff only" instruction is still satisfiable: every panel block on disk was produced by that same dumper, so dumping the parsed panel reproduced lines 31-69 byte-identically and appending one reader gave exactly a 2-line diff. Confirmed the round-trip BEFORE writing rather than trusting it. `apply` is not a route here — `panel` is not a union key, so any differing value exits 7 CONFLICT.
- 2026-09-07: check-state.sh must be run FROM the worktree when grading a feature whose plan.yaml exists only there — run from the main checkout it printed zero BUG-442 lines, which reads exactly like "the violation is gone" and is instead "the feature was never graded".
- 2026-09-07: any uncommitted write to plan.yaml trips INV-33 review_sha STALE, because the pinned sha is normally the last commit touching that file. A record-only repair therefore trades one violation for another until the orchestrator commits and re-pins; worth stating in the return so the lead does not read it as a new defect.
