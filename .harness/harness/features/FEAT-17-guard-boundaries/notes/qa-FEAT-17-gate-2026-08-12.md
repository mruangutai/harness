# QA gate — FEAT-17-guard-boundaries — 2026-08-12

**Verdict: PASS.** Matrix satisfied, all task verify clauses green against `c6a28bd`, and both
substantive probes (Q1, Q2) confirm the guarded behaviour is rule-driven rather than incidental.
Q3 surfaces real, pre-existing coverage gaps in `harness_boundary.py` — none newly introduced by
this feature, none blocking, all named below for the record.

## Pin

HEAD examined: `c6a28bdee6efabe0f7b2a116defaa819ecd88cbd` (matches the pinned `c6a28bd`).
`git status --porcelain` at start: only `.harness/features/FEAT-17-guard-boundaries/feature.json`
modified (pre-existing, updating `review_sha` to this pin — not mine). At finish: that file plus
two review notes from concurrent reviewer agents (`review-harness-code-reviewer-...`,
`review-harness-security-reviewer-...`), neither written by me. Every probe edit I made lived in
`harness_boundary.py`/`bash-write-guard.sh`/`check-domain.sh` **copies** under
`/private/tmp/.../scratchpad/probe/`, entirely outside the repo — verified with the same
`git status --porcelain` command, showing nothing under `.claude/skills/harness/bin/` beyond
gitignored `__pycache__`.

## Matrix resolution

Change types by task: T-01 `cross_module`, T-02/T-03/T-04 `bugfix`, T-05 `logic`, T-06 `config`,
T-07 `docs`.

- `cross_module.always` = `[unit, integration]` → both required.
- `bugfix.always` = `[unit]`; `bugfix.when` names `{kind: __bug_class__, if: match_bug_class}`.
  **Resolved to no additional kind, not silently dropped:** `__bug_class__` is a documented
  placeholder (DEC-35, clarified at `DECISIONS.md:5433-5438`) that names no entry
  under `test_kinds` in this project's `harness.json` — there is no `__bug_class__` kind to run,
  so the `when` clause can never resolve to a command here. This is the same placeholder DECISIONS.md
  calls out as broken in the one onboarded reference project. Result: bugfix tasks require `unit`
  only.
- `logic.always` = `[unit]`.
- `config.always` = `[]`, `docs.always` = `[]` — nothing required for T-06/T-07.

**Union required: `unit` + `integration`.** Both ran.

```
matrix_ok: true
```

## Per-kind results

| kind | cmd | exit | notes |
|---|---|---|---|
| unit | `.claude/skills/harness/bin/run-unit-tests.sh --kind unit` | 0 | 12/12 UNIT_SCRIPTS pass. **Does NOT execute `test-check-domain.py`, `test-bash-write-guard.py`, or `test-check-state.py`** — all three sit in `run-unit-tests.sh`'s `INTEGRATION_SCRIPTS` array despite matching `test_kinds.unit`'s detect glob (`.claude/skills/harness/bin/test-*.py`). This is the pre-filed BRIEF gap (`## Verification gaps`), confirmed still true, and it is broader than BRIEF states: it also silently excludes `test-check-state.py`, which BRIEF's gap note does not name. A green `unit` here is **not** coverage of this diff. |
| integration | `.claude/skills/harness/bin/run-unit-tests.sh --kind integration` | 0 | 84 named cases across 12 scripts pass, including `test-bash-write-guard.py`, `test-check-domain.py`, `test-check-state.py`. **This is the kind that actually exercises the diff**, matching every SC's declared `evidence: integration`. |

## Task verify clauses — verbatim from `plan.yaml`, cross-checked against the dispatch, all match

| task | verify | exit |
|---|---|---|
| T-01 | `test-check-domain.py && test-bash-write-guard.py && grep select_base/classify present && select_base absent from check-domain.sh` | 0 |
| T-02 | `test-check-domain.py` | 0 |
| T-03 | `test-bash-write-guard.py && test-check-domain.py` | 0 |
| T-04 | `test-bash-write-guard.py` | 0 |
| T-05 | `test-check-state.py` | 0 |
| T-06 | inline `python3 -<<PY` heredoc, asserting `git worktree list` names only the main checkout, `archive/worktree-r6` tag exists, and the receipt names `LATE`/`archive/worktree-r6`/`sweep` | 0 (printed `OK`) |
| T-07 | `shasum` before/after `gen-decisions-index.py`, diff clean, plus `test-gen-decisions-index.py` | 0 |

No mismatch between the dispatch's verify text and `plan.yaml`'s. No `BLOCKED`.

## Q1 — do the forbidden halves fail for the RULE's reason?

**Yes, verified by mutation, not by reading.** Built an isolated `bin/` copy in a tempdir
(`check-domain.sh`, `bash-write-guard.sh`, `harness_boundary.py`, `harness_yaml.py` — copied via
`shutil.copy`, not `cp`, since `cp`/redirect targets outside my domain are correctly refused by
`bash-write-guard.sh` even for scratch paths; this is expected qa-persona behaviour, not a bug).
Reproduced the SC-01/02/03/07 fixture shapes by hand (tempdir root + `.git` pointer files, no `git`
invoked), fired both routes:

- Unmodified copy: SC-01/02 forbidden (`<sibling>/allowed/x.txt`) exits 2 on both routes; SC-03
  forbidden (root-side, `CLAUDE_PROJECT_DIR` pointed at the sibling) exits 2 on both routes. All
  four paired allows exit 0, matching BRIEF exactly.
- **Neutered `worktree_owner` to always return `None`** (the exact function the target-side and
  root-side checks both call): all four forbidden cases above **flip from 2 to 0** on both routes.
  The allow cases are unaffected (already 0). This is the mutation proof — the forbidden halves
  fail *because of* `worktree_owner`, not because of an ordinary glob miss or a fixture artefact.
- SC-04 (Bash-route worktree-creation refusal): fired all four forbidden shapes (absolute outside
  `.claude/worktrees/`, same with `-b`, relative, `worktree move`) and all five paired allows —
  every exit code matches BRIEF exactly. **Neutered the containment check** (forced the `if not
  _inside:` refusal branch to `if False:`, so the scan can never refuse on containment) — the
  outside-worktree forbidden case **flips from 2 to 0**, confirming this scan's own containment
  check (not a fixture artefact) is what refuses it.

Q1: **discriminates.** Both mechanisms probed (the shared `worktree_owner` and the Bash-route's
own worktree-creation containment check) flip on neutralisation.

## Q2 — the two post-goal-check "granted by both mechanisms" cases

Built the SC-07 fixture (legitimate worktree `<root>/.claude/worktrees/wt`, write from OUTSIDE it
targeting `<root>/.claude/worktrees/wt/.harness/allowed/x.txt`) and probed each mechanism alone:

| mechanism removed | Bash route | Write route |
|---|---|---|
| neither (baseline) | 0 | 0 |
| DEC-153 carve-out (the `^\.claude/worktrees/` continue) neutered | **0** (DEC-143 alone grants it) | n/a — this carve-out doesn't exist on the Write route |
| DEC-143 stripping (`WORKTREE_REL_RE`) neutered | **0** (DEC-153 alone grants it) | **2** (flips — Write route has no other mechanism) |

This **confirms the code comment's claim empirically**: on the Bash route, removing either
mechanism alone still leaves the case passing — it is doubly redundant and discriminates neither
mechanism by itself, exactly as the comment states. On the Write route, which has no DEC-153
carve-out, the same case depends solely on DEC-143 and flips correctly when that's removed. This
is not a defect — SC-06 is the case that actually proves one-implementation by mutation, and this
pairing is honestly self-described as not doing that job. Flagging it here only because the
dispatch asked me to verify rather than trust the comment; verified, comment is accurate.

## SC-07's own clause — verified by diff, not by behaviour alone

SC-07's text is explicit that it "is checked by diffing both test files: no pre-existing case's
expected exit code changes value." Ran that diff directly: `git diff 52ee5db..c6a28bd -- <both test
files>` (`52ee5db` is `git merge-base main HEAD`, the feature's base — both files are themselves
part of the diff, satisfying P-01/P-05, not merely present-and-untouched).

- `test-check-domain.py`: diff is **purely additive** — zero removed lines carry an assertion or
  exit-code tuple.
- `test-bash-write-guard.py`: every removed line traces to exactly the five cases T-03 names — the
  first three retargets (`docs/a.md` → `docs/harness/a.md`, expected `0` unchanged in all three),
  the fifth retarget (`docs/a.md` → `docs/harness/a.md` in the second-segment-scan case, expected
  `2` unchanged), and the SC-06-pair allow-half retarget (`allowed/x.txt` →
  `.harness/allowed/x.txt`, expected `0` unchanged). The "both write surfaces agree" loop gains a
  **new** `("src/main.py", 2)` tuple alongside its two pre-existing tuples (both exits unchanged) —
  an addition, not a flip. No other removed line in the diff carries an exit-code assertion.

**No pre-existing expected exit code changed value anywhere in either file. SC-07 confirmed by diff.**

## The wording-pair negative assertions — confirmed present in the shipped suites, not just my probe

My own probes only asserted the positive half (`"claude/worktrees" in stderr`). Checked the shipped
suites directly for the negative half SC-03/SC-08 require:

- SC-03 (root-side, both routes): `test-check-domain.py:1542-1543` —
  `".claude/worktrees" in r.stderr and "git worktree remove" not in r.stderr`. Same pattern at
  `test-bash-write-guard.py:359-360`.
- SC-08 (`check-state.sh` INV-25): the negative, scoped to repo C's own-root line, is at
  `test-check-state.py:1264-1267` (`... and all("git worktree remove" not in l for l in
  own_lines)`); the paired positive for the *other* branch (repo B, which must still carry the
  removal instruction) is at `test-check-state.py:1225-1226`.

Both halves are present in all three files. Confirmed, not assumed.

## T-07's DEC-193 — content checked, not just index-regeneration mechanics

T-07's own verify clause only proves the index regenerates byte-identical; it cannot see whether a
decision entry was actually written. Read the entry directly: `docs/harness/DECISIONS.md:5687`
opens `## DEC-193 — Code is written in exactly two locations...`, states all three refusals (create,
write, host-a-session), the re-measured `a29ad06` evidence, and the "what did not converge" section
(the Bash-route DEC-153 worktree allow, the un-enforced product-base domains on Bash, the PyYAML
bootstrap-grant divergence). Its lineage line (`DECISIONS.md:5758-5763`) names DEC-151, DEC-153,
DEC-174 and DEC-189 exactly as the task required. T-07 confirmed substantively.

## Q3 — coverage of `harness_boundary.py`'s branches

No `coverage` module available in this environment; used stdlib `trace --count` via a `python3`
PATH-shim (since `check-domain.sh`/`bash-write-guard.sh` invoke `python3 -` internally) to
instrument my own behavioural probes covering every SC-shape in BRIEF plus the candidate branches
named in the dispatch, then cross-checked untouched branches against the **real** suites by direct
grep/read (since `trace`-wrapping the full suites broke an unrelated `--post` shape-sweep-stamp
timing assumption — noted as a tooling limitation, not a finding about the diff).

**Reached, confirmed by both my instrumented probe and the real suites:**
- `worktree_owner`'s main-checkout (`.git` directory) branch — every in-root case in
  `test-check-domain.py`'s `run_worktree()` reaches it (root's `.git` is built as a directory).
- `worktree_owner`'s well-formed linked-worktree (`.git` file, matching pointer) branch — SC-01
  through SC-08's worktree fixtures.
- `worktree_owner`'s "no `.git` found at all" branch (walks to filesystem root) — every ordinary
  scratch-path case.
- `resolve_fleet`'s label-parameterised BLOCKED exit — `test-check-domain.py:471-481`, case (b),
  a broken `fleet.yaml` asserting `returncode == 2 and "fleet.yaml" in b.stderr`.
- `select_base`'s label-parameterised BLOCKED exit (workspace path, no declared repo) —
  `test-check-domain.py:603-610`, case (i).
- `out_of_place_worktree` outcome on both routes — SC-01/SC-02/SC-03 directly.
- SC-10's fail-closed pair (module absent) — present in both suites' own D-06 blocks (not
  independently re-verified by me beyond reading, given time; the `unit`/`integration` run already
  exercised these named cases and they passed).

**NOT reached by either real suite (`test-check-domain.py`, `test-bash-write-guard.py`) at
`c6a28bd`** — confirmed by grep across both files, not by absence of effort in my own probe (my
probe reached all three and confirmed each returns `None` as documented):
- `worktree_owner`'s **malformed gitdir line** return (`m := re.match(...)` fails,
  `harness_boundary.py:384-385`). Only one `.git`-file shape (`gitdir: <owner>/.git/worktrees/<id>`)
  appears anywhere in either test file (`grep gitdir` → 4 hits, all the same well-formed shape).
- `worktree_owner`'s **unreadable `.git` file** exception path
  (`harness_boundary.py:381-382`).
- `worktree_owner`'s **non-matching pointer structure** return (basename checks at
  `harness_boundary.py:395-396`, e.g. a `gitdir:` line that doesn't parse into a
  `.../.git/worktrees/<id>` shape).
- **The `shared` outcome** (`classify`'s `outcome: "shared"` branch,
  `harness_boundary.py:317-323`). Grepped both suites for `SHARED`/`shared:`/`package.json` write
  cases — the only shared-path assertion in `test-check-domain.py` (line 46-47) is a case that now
  **exits 2** in the harness base (FEAT-15 T-02 narrowed serialized-allow to the product base only),
  and no case in either file writes a shared path against a *product* base to exercise the actual
  allow-plus-notice branch. This branch is unreached by either suite as it stands.
- `select_base`'s product-base branch (`harness_boundary.py:201-202`) is exercised elsewhere in
  `test-check-domain.py` (the two-base fixtures around line 526+ do reach it via ordinary product
  writes) — flagging only because my own probe alone didn't hit it; the real suite does, confirmed
  by reading `test-check-domain.py:556-733`.

**Assessment:** none of the three unreached `worktree_owner` branches, nor the `shared` outcome, is
new code introduced by FEAT-17's move — `worktree_owner` is new (T-02), but its malformed/unreadable/
non-matching returns are defensive "unknown → not-a-worktree" fallbacks, and `shared` is inherited
verbatim from `check-domain.sh`'s pre-existing `domain_check`. D-05 declined a dedicated test file
for `harness_boundary.py` on the ground that "behavioural coverage through both guards is stronger
evidence" — for these four branches specifically, that behavioural coverage does not currently
exist. This does not fail the gate (D-05 is a signed decision this gate does not re-litigate, and
none of BRIEF's SC/REQ names these branches), but it is a real, named gap and belongs on record
per DEC-169 (an absence assertion needs its presence pair, and here there's no assertion at all).

## Findings, ranked

1. **(info, not blocking)** `unit` kind's exclusion of guard/state test files is broader than
   BRIEF's own gap note states — it also silently excludes `test-check-state.py`, not just the two
   BRIEF names. Filed as backlog per BRIEF's own framing; not a FEAT-17 defect, raising as an
   `open_question` so it's visible outside this note.
2. **(info, not blocking)** Four branches in `harness_boundary.py` — `worktree_owner`'s malformed/
   unreadable/non-matching-pointer returns, and `classify`'s `shared` outcome — have zero coverage
   in either shipped suite at `c6a28bd`, confirmed by grep and by my own instrumented probe (which
   reached and validated all four manually). The three `worktree_owner` returns are pre-existing-
   in-spirit (defensive "unknown → not-a-worktree" fallbacks on new code, but the same shape
   `check-domain.sh` never tested either). **The `shared` outcome is not merely pre-existing:**
   `classify`'s branch is moved verbatim from `check-domain.sh`, but `bash-write-guard.sh`'s own
   mapping of that outcome — print the DEC-85 shared-path notice, then continue — is **new in this
   diff** (T-03) and has never been exercised on either route. D-05's rationale ("behavioural
   coverage through both guards is stronger evidence") doesn't currently cover this branch on
   either guard.
3. **(info)** Q2's comment about the Bash-route legitimate-worktree case being non-discriminating
   for either single mechanism is empirically confirmed correct — no action needed, recorded per
   dispatch instruction to verify rather than trust.

## SC evidence (for pm's goal-check)

| SC | evidence |
|---|---|
| SC-01/SC-02 | `test-check-domain.py`'s `run_worktree()` (`WT`-tagged cases) / `test-bash-write-guard.py:282-360` (`.git` pointer-file fixtures, no `git` invoked) |
| SC-03 | `test-check-domain.py:1542-1543` (root-side wording, both halves) + `test-bash-write-guard.py:353-360` |
| SC-04 | `test-bash-write-guard.py` worktree-creation cases (see diff excerpt above, lines ~330-364) |
| SC-05 | `test-bash-write-guard.py:228-262` ("both write surfaces agree", `agree_manifest`) |
| SC-06 | `test-bash-write-guard.py:470` — `WORKTREES_SEGMENT = ".claude/wt-mutant"` mutation case, before/after asserted `(0,0)` → `(2,2)` on both routes |
| SC-07 | diffed directly above — no pre-existing expected exit changed |
| SC-08 | `test-check-state.py:1159` (`run_worktree`/INV-25 docstring) through `1267` (wording pair) |
| SC-09 | T-06 heredoc verify, exit 0, printed `OK` — R-01's own scoped receipt claim, not independently re-derived by me beyond what the verify clause checks |
| SC-10 | `test-check-domain.py:1586-1587` (`harness_boundary` named in stderr, missing-module case) |

All SC-01 through SC-08, SC-10 are `verify: automated, evidence: integration`, run via `--kind
integration` above (exit 0, all named). SC-09 is `verify: inspection`.

## Probe artefacts

All probe code, fixtures, and mutated copies live under
`/private/tmp/claude-501/-Users-molchairuangutai-GitHub-harness/cd83b531-197f-4da6-a4a5-9bb0ec5fcaa5/scratchpad/`
— entirely outside the repository, not part of this artifact's domain, and not referenced by path
for durability (session-scoped scratch). Nothing there was written into the tracked tree.
