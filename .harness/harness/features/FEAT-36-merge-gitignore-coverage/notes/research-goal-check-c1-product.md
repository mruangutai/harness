# Final product goal-check — FEAT-36

**Overall: PASS.** At reviewed candidate `f494553bd9fbb987b4a19f91dcf4c3f37253fe38`, all six success criteria are met by their declared methods. This goal-check did not rerun any command; automated verdicts use the pin-matching c2 QA evidence and pinned source/test content.

| Criterion | Declared verify | Verdict | Clause-level evidence |
| --- | --- | --- | --- |
| SC-01 | automated (integration) | met | The pinned test resolves and invokes the real utility as a subprocess (`test-merge-gitignore.py:9-23`). The case seeds four distinctive existing lines as exact bytes (`:36-42`), requires the merged bytes to begin with that exact sequence, preserving content and order (`:43-46`), and independently requires every Harness rule exactly once (`:47`). c2 QA records all seven named cases passing directly and the program passing again through integration registration (`review-harness-qa-c2.md:28,32`). |
| SC-02 | automated (integration) | met | Separate pinned cases cover a complete target and an incomplete target (`test-merge-gitignore.py:50-73`). The complete case requires exit 0 and exact byte identity (`:55-59`). The incomplete case requires exit 1, checks every missing canonical rule is present in stderr, and requires exact byte identity (`:67-73`). c2 QA records the direct seven-case pass and registered integration pass (`review-harness-qa-c2.md:28,33`). Advisory F-02 concerns accepting extra diagnostic text, which SC-02 does not forbid; no clause is waived. |
| SC-03 | automated (integration) | met | The absent-target case requires target creation and every canonical rule exactly once (`test-merge-gitignore.py:76-84`). The separate partial-target case starts with one canonical rule plus unrelated content, requires the unrelated content retained, and requires the count of every canonical rule—including the pre-existing and all missing rules—to equal one (`:87-98`). c2 QA confirms both cases in the passing integration evidence (`review-harness-qa-c2.md:28,34`). |
| SC-04 | automated (integration) | met | The pinned case runs the real merge twice, captures bytes after the first run, requires both invocations to succeed, and requires exact byte identity after the second (`test-merge-gitignore.py:101-110`). c2 QA records the case within the direct seven-case and registered integration passes (`review-harness-qa-c2.md:28,35`). |
| SC-05 | automated (integration) | met | The utility path is resolved absolutely and passed to the subprocess (`test-merge-gitignore.py:9-10,19-23`). The case creates sibling requested-project and unrelated-caller directories outside the utility, invokes from the caller while passing `project.resolve()` explicitly (`:113-123`), and proves the initially absent requested project target exists afterward (`:116-125`). It also creates a pre-existing caller `.gitignore`, captures its bytes, and requires post-run byte identity (`:120-126`). c2 QA explicitly reports this two-sided assertion and its direct plus registered integration pass (`review-harness-qa-c2.md:28,36`); the c2 validator independently records `requested_target_changed`, `caller_preserved`, and PASS (`runs/review-c2-validator/digest.md:66-70,99-102`). |
| SC-06 | inspection | met | At the pin, `test-merge-gitignore.py` appears in `INTEGRATION_SCRIPTS` and not `UNIT_SCRIPTS` (`run-unit-tests.sh:17-18`), while `.harness/harness.json test_kinds.integration.detect` contains its explicit path (`.harness/harness.json:118-122`). c2 QA confirms the integration-only registration, active configuration, passing kind cross-check, and no `MISCONFIGURED` or `KIND-DRIFT` result (`review-harness-qa-c2.md:3,19-28,37,45-48`). The production utility is unchanged: `git show` of both base `0fa8f336e55dc57bca09a9f7df0524a35195ee7e` and reviewed candidate yields SHA-256 `86cfff73c88a2baa1c74d2e516e3608e38954fef1c9e4ef344113b011e425c12`; code review also records the identical Git blob (`review-harness-code-reviewer-c2.md:23-24`). Therefore the brief's conditional production-change branch was not taken and requires no failing-history demonstration. |

## Task and gate disposition

- T-01 is `status: done` at the pinned candidate (`plan.yaml:40-57`). Its exact declared verification command is the one c2 QA ran successfully at the same SHA (`review-harness-qa-c2.md:3,12-17`; `runs/review-c2-validator/digest.md:39-65`).
- UAT: not required. SC-01 through SC-05 declare `automated`; SC-06 declares `inspection`; no criterion declares `uat`.
- c2 panel: PASS across code, QA, security, and UI, with stage-ordered spec and quality PASS (`runs/review-c2-validator/digest.md:13-38`).
- Must-fix: none. The c2 panel has no new findings and an empty `must_fix` list (`runs/review-c2-validator/digest.md:71-103`).
- Open questions: none.
- Files touched by this goal-check: `.harness/harness/features/FEAT-36-merge-gitignore-coverage/notes/research-goal-check-c1-product.md`.
