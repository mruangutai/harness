# Observations — harness-pm — BUG-1309-mirror-build-entry

- 2026-09-06: BUG-1309 plan authoring. plan-merge.py apply exits 5 UNPARSEABLE on a decisions because: plain scalar containing a colon-space; the message names a line/column of the proposal, not the field. Screen every plain scalar for both colon-space and space-hash before the first apply.
- 2026-09-06: BUG-1309 - gh-sync.py save_recorded's transform rebuilds doc github from a fixed five-key literal plus optional typed, so ANY new github field needs an explicit emit line there or the next save silently deletes it. A task intent that only said "record the field" would have shipped a field that vanishes on the next status call.
- 2026-09-06: BUG-1309 - a new PreToolUse Bash gate costs three registration surfaces here (.claude/settings.json, templates/settings.snippet.json, .omp/extensions/harness-hooks.ts firstBlock array) and the first two resolve to NOBODY, so the whole task is main-session-direct regardless of the gate script's own lane.
- 2026-09-06: BUG-1309 c1 — plan-merge.py `apply --proposal -` cannot re-propose a CHANGED task or decision (exit 7 CONFLICT on any differing value); `amend --key tasks|decisions --id X --field F --expect-sha256 <from --show>` is the only route, and list fields (files, depends_on) need `--yaml-value` on BOTH the --show and the write or --show exits 4.
- 2026-09-06: BUG-1309 c1 — a "one shared definition, three readers" instruction spanning a bash gate and two python scripts forced the constant into feature_schema.py (already imported by check-state.sh at :1345); that added feature_schema.py to a main-session-direct task's files and produced an extra granted-path DEVIATION row in check-plan-routes, still exit 0.
- 2026-09-06: BUG-1309 c1 — when a refusal message must be asserted to NOT name a command, the message cannot even describe that command ("open would create sub-issues" defeats the assertion). Wrote the literal so the token never appears, and made the test assert both the full spelling and the bare token.
- 2026-09-06: BUG-1309 T-06 — introducing a shared classifier in one task does not retire the old
  discriminator inside that same task's own user-facing message. The task that HOSTS the fix is the
  one most likely to keep the pre-fix prose, because a diff reader sees the new function and stops.
  Grep the rejected discriminator token ("merged") across every task intent after a ruling, including
  the task that implements the ruling.
- 2026-09-06: BUG-1309 c2 — a closing clause that says "pass nothing so the field stays absent"
  was wrong against the plan's own step 4: `skip(msg, build_entry=None)` DEFAULTS to recording
  `recovery-required`, so "record nothing" needed an explicit `_NO_RECORD` sentinel. Re-read the
  mechanism the ruling depends on before transcribing the ruling's wording.
- 2026-09-06: BUG-1309 c2 — replacing a stdout-prose grep with `grep -qF "ok    <case name>"` over
  the runner's own pass line makes the verify depend on assertions (an `ok` line prints only when
  the check held) AND discriminate the task's own cases; it requires declaring the case names as a
  contract inside the intent. Probed all six variants including "prose-only stdout", which the old
  grep accepted.
- 2026-09-06: BUG-1309 c2 — a remedy named by a refusal must be checked for reachability on the
  corpus the refusal fires over: T-03 refused at exit 2 on exactly the shape (recorded milestone +
  parent) that every legacy feature has, so the refusal chain named a command that could never run.
- 2026-09-06: BUG-1309 c3 — two test beds in the same repo do not share a runner: test-gh-sync.py's check() prints `ok    <name>` (`:702-708`) while test-post-merge-sweep.py's main() prints `PASS: <name>` / `FAIL: <name>` (`:890-894`). A case-name verify contract copied from one bed to the other would grep for a line that is never printed. Read the runner at source per file.
- 2026-09-06: BUG-1309 c3 — a bare `<cmd> && exit 1` statement inside a `verify:` block aborts under `set -e` when the grep MISSES, i.e. on the healthy path. Probed verbatim: the `if <cmd>; then exit 1; fi` rewrite is green on all-cases-ok and non-zero on every failure shape. `|| exit 1` is not the defect.
- 2026-09-06: BUG-1309 c3 — an era exemption applied to REFUSALS but not to RETENTION is not conservative: an era feature merged after the change keeps its worktree, and check-state.sh INV-29 (`:1929-1935`) then refuses on a standing worktree for a terminal feature. A safe-side retention becomes a downstream refusal on exactly the exempted corpus.
- 2026-09-06: BUG-1309 c3 — a test-case function added to test-post-merge-sweep.py prints nothing unless it is summed into main()'s results tuple (`:876-888`); a verify greping for its PASS line would then fail for a reason unrelated to the assertion. Say so in the intent when the bed uses a results-tuple runner.
- 2026-09-06: BUG-1309 c4 — plan.yaml field edits to EXISTING ids must go through `plan-merge.py amend` (compare-and-swap per field, --show for the sha); `apply` is add-only and exits 7 CONFLICT on any changed value, so "re-propose under the existing id" via apply is impossible for a correction. Ten amends (D-08.choice/because, T-04..T-07 intent+verify) all exit 0 under HARNESS_AGENT_TYPE=harness-pm.
- 2026-09-06: BUG-1309 c4 — a goal-check's blocking finding rested on an unverified mechanism claim (INV-29 refuses a retained worktree). Measured: worktree_terminal.py:389-394 keys on the LANDED plan.yaml status done, and gh-sync.py cmd_ship's :1799-1800 skip returns before the :1975 station write, so the predicted refusal is unreachable. The remedy would have deleted a worktree the operator's settled policy preserves.
- 2026-09-06 (BUG-1309 c4b): a case asserting absence of a word failed against a message that DENIED the thing ("is not refused" contains "refused"), and a sibling case asserted lowercase "merge" against a line spelling it "MERGE". Compose every assertion against the literal string the case will see, case-sensitively, before returning a plan.
- 2026-09-06 (c5): plan.yaml block scalars re-wrap relative to the file's displayed lines - an anchor copied from a `read` of the FILE failed while the same sentence copied from the LOADED value matched. Build replacement anchors from yaml.safe_load output, never from the rendered file lines.
- 2026-09-06 (c5): plan-merge.py amend --show's sha256 is over the RAW file block lines (header + body), not over the loaded scalar; hashing the loaded value gives a different digest and exit 6. Always take the sha from --show.
- 2026-09-06 (c5): a plan clause that tells a test to extend a frozen literal set is a dead end no gate catches - it reads as a legitimate option. The fix that scales is naming the fixture directory LITERALLY in the task text, era names from the snapshot and non-era names synthetic (FEAT-9001-*), so a doer cannot pick a real directory and silently invert the case.
- 2026-09-06: c6 plan fix. A goal-check's proposed remedy (`/harness-init --upgrade`) was wrong at source — it is a skill MODE running upgrade-config.py, which merges a template whose github block is `repo: null`, so nothing pins github.repo. Re-derived before encoding; the grading's suggested clause would have shipped a second unclearable remedy.
- 2026-09-06: adding a discriminating test PAIR to an existing case list forced a fixture-wide requirement (every other fixture root must pin github.repo) that no clause asked for — without it the pre-existing deny cases silently exercise the new branch and the pinned half is uncovered.
- 2026-09-06 (BUG-1309): a dispatch prescribed `plan-merge.py apply` re-proposing CHANGED tasks under existing ids; apply exits 7 CONFLICT on any differing value, so the whole route was wrong. `amend --field ... --expect-sha256 --value-file` is the verb for changing an existing id's value. Probed the refusal first (exit 7 verbatim) rather than arguing it.
- 2026-09-06 (BUG-1309): extracting a long block scalar with `amend --show`, editing the extracted file, and amending it back is far safer than retyping a 200-line `intent:`; the printed sha256 is the compare-and-swap for the same value you just read.
- 2026-09-06 (BUG-1309): the SCOPE-01 honesty clause was settled by RUNNING the path (staged fixture, deleted BRIEF.md, fake gh) — exit 1 through read()'s die() and no `github` key written. The run also revealed the assertion form: `doc.get("github", {})`, because the fixture's feature.json has no github mapping until a write creates one.
- 2026-09-06 (BUG-1309): three registration arrays for the same gate set were in three DIFFERENT orders (settings.json, settings.snippet.json, harness-hooks.ts firstBlock) and one lacked a member the other two had. "The same position" across sites is never a specification.
- 2026-09-06: BUG-1309 T-05 — merge-settings.py HOOK_SPECS is the ONLY site that installs a hook into an onboarded project; the settings.snippet.json template is a one-way drift check (merge-settings.py ~232-246), so a snippet-only entry leaves a gate live in the harness checkout alone. gh-close-gate.sh and plan-sign-gate.sh are in that state today.
- 2026-09-06: bash-write-guard.sh reads the whole bash command line textually, so a python heredoc containing the literal two-char arrow (hyphen + greater-than) inside prose is refused as a redirect. Build the arrow from chr() and substitute after.
- 2026-09-06: BUG-1309 c7 — plan-merge amend replaces the WHOLE field, so an insertion mid-paragraph merges the tail of the replaced sentence onto the new last line; re-wrap in a second amend (fresh --show sha) rather than leaving a 160-char line in a literal block.
- 2026-09-06: BUG-1309 panel repair — set-panel's validator only type-checks last_run/cycle/readers/findings (plan-merge.py:1017-1037); the row schema is enforced downstream by check-state.sh:542-555, which reads persona/reason ONLY on the skipped branch. So a `ran` row is reader+status and nothing else.
- 2026-09-06: check-state.sh resolves its root from its own location — grading a worktree feature requires running the WORKTREE's copy of the script; the main-root copy printed no BUG-1309 lines at all, which reads like "clean" and is actually "not examined".
- 2026-09-06: rebuilding a panel value by loading plan['panel'] and appending one dict (never retyping summaries) is the only safe route past content-hash finding ids; safe_dump of the panel mapping alone is exactly what --value-file wants.
- 2026-09-06: BUG-1309 T-09 verify amend dispatch arrived already satisfied — commit f4b2883d had applied the exact target value before spawn. Pre-check saved a redundant compare-and-swap; the same commit also moved approval.date backwards 2026-09-06 -> 2026-09-04 while leaving status approved, which plan-merge amend provably cannot do (AMENDABLE_KEYS = tasks, decisions). Reading git log for the target field, not just the field, is what surfaced the out-of-scope approval write.
- 2026-09-06: BUG-1309 plan amend. Piping a plan-merge proposal through a quoted bash heredoc is
  BLOCKED by bash-write-guard whenever the YAML body contains an angle-bracket placeholder: the
  guard parses the whole command line textually and read "features/(placeholder):" as a redirect
  target ":". Spell placeholders in words ("plus the feature name") in any heredoc body.
- 2026-09-06: plan-merge.py apply added two tasks to an APPROVED plan and left approval.status
  approved, with no ADDED-tasks reset. The doctrine ("re-planning resets approval") is not
  mechanized in the tool; only sign-approval writes that mapping, and pm cannot. Raise it as an
  open_question every time, because nothing else surfaces it.
- 2026-09-06: check-domain.sh --resolve grants harness-backend-dev/dev-ops/qa on
  post-merge-sweep.sh and check-state.sh themselves, so a team grant never settles DEC-174
  carve-out membership. The usable line is EXERCISES A GATE vs IMPORTS A MODULE A GATE USES.
- 2026-09-06: BUG-1309 R2 fix — re-aiming a unit case away from an integration guard is only honest if the unit input is one a fixture DIRECTORY cannot be (trailing slash, out-of-enum rec value), not merely one nobody wrote yet; probing _build_entry_preflight in-process found two live reds from one missing rstrip at gh-sync.py:1360, and a red case makes the task's own '^FAIL ' verify unsatisfiable, so the intent must say the task cannot reach VERIFY-PASS and route the fix out.
- 2026-09-06: BUG-1309 T-11 - a unit-floor task's non-duplication claim was checked against test-gh-sync.py alone twice; widening the cross-check to the other four integration files that touch the same seams struck 8 more cases beyond the panel's 3. A CLI block that drives every branch of a guard end to end (test-gh-sync.py:3403-3494 over skip()'s four conjuncts) is the thing to read BEFORE listing unit rows, not after.
- 2026-09-06: BUG-1309 cycle-1 panel produced TWO run dirs for one logical run (panelc1 prose-first, panelc1b canonical with the DEC-156 contract block at top); the canonical one is identifiable only by the contract block's presence plus the lead's own Q3 sentence, since both state.yaml files are equivalent. When transcribing a panel, pick last_run by which digest carries the contract block.
- 2026-09-06: re-feeding EVERY recorded panel finding through panel_findings.py after a set-panel splice is a cheap total check — it caught nothing here but would catch any cycle-0 summary mangled by re-serialization, which is exactly what silently voids an operator ruling.
- 2026-09-06: BUG-1309 panel c1 — `plan-merge.py set-panel` takes a BARE panel mapping (no `panel:` wrapper) and re-serialises with `yaml.safe_dump(sort_keys=False)`, then self-refuses if the spliced file does not reload equal to the value supplied (`plan-merge.py:1048-1066`). So a value file built by loading `plan['panel']`, mutating, and `safe_dump`-ing round-trips exactly: findings sorted-JSON sha256 was byte-identical before and after (`01caf790…c99ad`, 11 items). `harness_yaml` has NO `dump` — only loaders; use `yaml.safe_dump`.
- 2026-09-06: a `reason` key on a `panel.readers` row was the only place recording a reader's OTHER cycle. Deleting the row's falsified skip reason would have destroyed that pointer, so the cycle-0 fact was appended to `transcription_rule` instead. Lesson: before deleting any field in a keyed-by-reader roster, grep the plan for whether that field is the sole carrier of a fact the key cannot hold.
- 2026-09-07: BUG-1309 D-12. A plan decision that records a matrix cell as "a gap rather than papered over" and an operator ruling of "not-applicable" are DIFFERENT dispositions - gap asserts owed debt, not-applicable asserts none - so a later ruling needs the earlier decision amended, not merely supplemented. D-10 here was that case.
- 2026-09-07: BUG-1309. D-10 cited post-merge-sweep.sh:29-43 for "nothing can import it"; the heredoc actually spans 29-293. The narrow citation named only the import block and understated the fact by 250 lines while reading as precise. Re-derive the closing anchor of any heredoc/block citation, not just the opener.
- 2026-09-07: BUG-1309. The plan carries NO test_matrix key; the matrix lives in harness.json and the qa gate reads it via change_type. A dispatch phrased as "the test_matrix unit kind for T-07" is about the gate's derived cell, not a plan field - do not go looking for one.
- 2026-09-07: BUG-1309. Four bin scripts host their entire Python body in a shell heredoc (bash-write-guard.sh, gh-close-gate.sh, plan-sign-gate.sh, post-merge-sweep.sh), all inside the DEC-174 carve-out. That makes "unit kind unreachable on a runtime surface" a recurring family here, not a one-off, which is what turned the D-NN into a DEC recommendation.
- 2026-09-07: BUG-1309 CORRECTION to my own 2026-09-07 bullet claiming FOUR bin scripts host their entire Python body in a shell heredoc. FALSE, re-derived at source: the set is TWO — post-merge-sweep.sh:29-293 and bash-write-guard.sh:42 (the latter via `python3 -c '...exec(compile(sys.stdin.read()...))' <<'PY'` with the hook payload moved to HOOK_PAYLOAD so the heredoc can own stdin). gh-close-gate.sh:59/:79 and plan-sign-gate.sh:43/:62 are COUNTER-examples: both carry the comment "THE DECISION LIVES IN A FILE, NOT A HEREDOC" and exec a sibling .py. I counted candidates by which gates looked heredoc-ish and never opened the two that are not; the family is receding, not recurring, which reversed the DEC-217 sibling recommendation that rested on it (panel c2 finding 1, med). Lesson: a recurrence premise is an existence claim over a SET — open every member before writing "the shape recurs".
- 2026-09-07: BUG-1309 panel c2. D-12 cited post-merge-sweep.sh:222 as the retention test; the elif over the allow-set is :228 and :222 is only the `entry =` read. Both of my mis-cites in this feature were the SAME failure — citing a block's neighbour line from prose I had already written rather than from the file — and the second one was inherited by a downstream reader (scope's own note now carries :222). Re-derive an anchor from the file every time it is restated, and expect readers to inherit a bad one.
- 2026-09-07: BUG-1309 panel c2 close. A finding whose fix is a NEW test case must not be folded into the done task that owns the file: editing T-07's declared case list and verify would falsify what was verified when it completed. A new task T-13 depends_on T-07 with change_type scaffolding is the honest vehicle, and the existing table-driven case function takes the new row with no registration change (test-post-merge-sweep.py:886-905 names/shapes zip).
- 2026-09-07: BUG-1309. A coverage-gap task has NO red-first proof available when the production code is already correct — saying "written first and confirmed failing" there would be a false record. The honest substitute is a mutation proof written into the intent: delete the allow-set member, watch that row alone redden, restore, re-run.
- 2026-09-07: BUG-1309 delivery goal-check. SC-03 and SC-07 each ended their text with an ORDERING claim ("must be demonstrated failing against the pre-change copy", "must be shown passing before the invariant lands") while declaring verify: automated. Both owning tasks were execution_mode main-session-direct, which under DEC-174 writes no receipt, so qa recorded "could not establish" and no artifact on disk can ever settle either clause. An ordering claim is only verifiable where a receipt-writing lane owns the task; on a main-session-direct task it is unmeetable by construction, and the criterion must instead pin the checkable property (the assertion's discriminating shape).
- 2026-09-07: BUG-1309. Grading a criterion whose text names its own grader ("both halves are graded by the cases T-07 declares") still needs each PROSE clause checked at source: the two named sweep cases assert returncode and worktree presence only, while the criterion also described the announcement text. Source read at the sha showed the messages exist on exactly those branches, so the clause was true but undefended - MET plus a backlog chore, not a downgrade.
- 2026-09-07: BUG-1309 UAT. Writing a hand-test script and running it verbatim myself caught two defects a desk-written script would have shipped: an empty `tasks:` list in the fixture plan.yaml made gh-sync.py exit 1 on the station-only rule, and the deny message renders /private/tmp on macOS where the operator was told to expect /tmp. Extract the script back OUT of the finished artifact and run that copy - editing the prose around a fence can orphan a line inside it.
- 2026-09-08: BUG-1309 — an ordering clause ("must be demonstrated failing before the fix") on a DEC-174 main-session-direct task is unmeetable by construction; the operator ruling converted it to present-tense DISCRIMINATION, which IS checkable: git archive the pin into /tmp, lay the merge-base copy of the one changed script over it, run the pinned test file. test-check-state.py already ships a CHECK_STATE_BIN escape for exactly this. Amending a criterion invalidates the BRIEF signature — flag re-signing in the digest.
- 2026-09-08: BUG-1309 — reverting one script under the pinned suite reddened 20 of 322 gh-sync cases; the discrimination proof is the FAILURE SHAPE, not the count: the refusal cases printed normal start-task progress (issue -> building), proving the gate was absent rather than the harness broken.
- 2026-09-08: BUG-1309 — INV-37 red run left its message-discriminator case vacuously green ("open" not in "" when the invariant never fires). A sibling assertion over an empty line is ever-green; the fires case carried the proof.
- 2026-09-08: plan amendment c13 (R-1/R-2/R-3). Amending one region of a 170-line intent by retyping the whole value risks silent transcription drift; instead I dumped `amend --show`, spliced the region with a tiny script asserting each anchor occurs exactly once, then compared `--show` after the swap against the value file byte-for-byte. Three compare-and-swaps, no retries.
- 2026-09-08: a scoped "amend only these fields" dispatch can leave a remedy with zero automated evidence: R-3's fix landed in T-04 `intent` while T-04 `verify`'s case list was out of scope, so nothing greps the recover-terminal horn. Raised as blocking Q2 rather than widening the batch.
- 2026-09-08: BUG-1309 c13b — plan-merge amend --show prints a DEDENTED body but its sha256 is over the RAW indented block lines (plan-merge.py:1581); extracting the base with yaml.safe_load gives the same dedented string, so a 190-line intent can be amended by editing an extracted copy instead of retyping. Also: the dispatch claimed six existing T-04 verify case names, source had five.
- 2026-09-08: BUG-1309 goalcheck c14 — a parser fix that GAINS one form can LOSE another: R-2's flag-aware walk caught `git -C X merge B` and silently broke `git merge --no-ff B` (7 of 16 operator forms escape). Enumerating the invocation set and probing each through the real gate under /tmp was the only method that saw it; the 19-name file-global green and the panel both read as satisfied.
- 2026-09-08: when a signed task intent says "a CLASS, NOT A CLOSED LIST", grade the implementation against the named example the plan supplies — `--exec-path` was in the plan prose and absent from the shipped 7-name set, and that one token is a silent allow.
