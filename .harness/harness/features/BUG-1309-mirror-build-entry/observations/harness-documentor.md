# Observations - harness-documentor

- 2026-09-06: BUG-1309 T-09's signed `verify:` called `gen-decisions-index.py --check` AGAIN — the same nonexistent flag that broke FEAT-37 T-09, and which FEAT-19 and FEAT-32 pm observations both already record writing. Four features, four pms, one typo shape: the flag is written from the shape of every other harness checker (`--check`), while this one script alone spells it `--stdout | diff -`. I ran `--help` before my first edit (my own FEAT-37 lesson), so the broken grader was reported as a plan defect instead of being mistaken for a failed edit. The durable fix is upstream and not mine: either teach the generator `--check` as an alias, or lint verify strings against `--help`. Recurrence beats another receipt.
- 2026-09-06: appending a decision at EOF kept the DEC-220 regeneration to exactly one added index row (`@@ -219,0 +220 @@`), zero churn on 219 existing rows — the anchor-stability property is worth stating in the receipt explicitly, because a lead expecting "index regenerated" cannot otherwise tell a clean append from a mass anchor shift.
- 2026-09-06: the T-09 intent attributed `feature_schema.BUILD_ENTRY_ERA_EXEMPT` to INV-37 alone; at the code it bounds three things (INV-37 at check-state.sh:1983-2018, the Build refusal at gh-sync.py:1361, the merge refusal at gh-sync.py:1380). Signed prose fixes names and attributions, not facts — I wrote the wider true scope and said so in the receipt rather than transcribing the narrower claim.
