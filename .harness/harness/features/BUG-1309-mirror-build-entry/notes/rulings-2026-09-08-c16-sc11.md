# Operator ruling R-6 — SC-11 is added — BUG-1309-mirror-build-entry — 2026-09-08

**The operator chose the first of the two options pm put up (Q1, `STATE.md ## Open Questions`;
`notes/research-BUG-1309-planamend-c16.md`): ADD SC-11.** The SC-04 exclusion sentence is declined,
and so is the disclosed-gap option.

Orchestrator transcription of an inline main-session relay. **There is no answers file on disk for
this round and this note is not that channel** — the answers file is the main session's alone
(issue #671); this is the orchestrator's record of a relay it was handed.

## The ruling, as given

> Add SC-11: `git merge --abort/--continue/--quit` must remain allowed to recover from interrupted
> merges even when the current branch owes a receipt.

## What it settles

- **R-4 clause 3 now traces to a criterion.** Before this ruling the shipped allow was graded by
  T-05's `verify:` and by no success criterion, and under SC-04's literal reading it CONTRADICTED a
  signed one (`BRIEF.md` SC-04: "a merge command issued for a feature … whose Build-entry outcome …
  is absent under enabled sync … is denied"; `git merge --abort` is a merge command).
- **SC-04 is not reopened.** Its text is untouched: three cycles of digests grade it as written, and
  the operator declined the exclusion-sentence route that would have edited it mid-flight.
- **The recovery rationale is the operator's own words and belongs in the criterion**: these are
  merge CONTROL operations, the commands that recover an interrupted merge. They are not the merge
  action REQ-07 refuses, and refusing them strands an operator mid-conflict on a branch that owes a
  receipt — which is what `e374c9a2` did (VF-03).
- **It does not change the code contract.** D-16 already specifies the exemption and T-05's `verify:`
  already gates the three case names. SC-11 grades what the plan already requires; the
  main-session-direct implementation packet (`notes/direct-packet-2026-09-08-c16-parser.md`) is
  unchanged by this ruling and remains ready to run.

## Consequences, both signatures

1. **`BRIEF.md` is amended and its `## Approval` is now stale** — SC-11 was appended to
   `## Success Criteria` after the 2026-09-08 signature. Neither the orchestrator nor pm may write
   that block: `.harness/*/features/*/BRIEF.md ## Approval` is a main-session-owned fragment
   (`team-config.yaml main_session.writes`), and `check-domain.sh`'s `approval_guard` DENIES a
   governed agent's edit of it. **Re-signature is the main session's act and it gates the ship.**
2. **`plan.yaml approval:` was already stale** over the T-05 text amended this cycle (Q2). One
   `sign-approval` call repairs it; `sign-approval` is refused to every governed agent at the tool.

## Not ruled here

- The code-grade bar stays OUT of the success criteria (pm's Q3 recommendation, recorded as D-17).
- `SC-10` UAT is still unrequested and still gates the ship; its Step 3b still needs the three
  previously-allowed forms once they deny.
