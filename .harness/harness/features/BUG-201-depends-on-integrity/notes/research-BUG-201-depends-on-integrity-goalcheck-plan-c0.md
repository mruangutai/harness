# Goal-check — BUG-201 drafted plan vs the operator's stated intent (plan panel, step 1)

**Yes — this plan delivers the operator's stated intent, with three qualifications that are findings, not blockers.** Graded against `.harness/notes/grilling-depends-on-integrity-2026-09-06.md` (the authority), not the BRIEF. Every fact the lead pre-verified was re-confirmed at source; none was wrong.

## Destination sentence, clause by clause

**1. "every YAML plan" — MET for every route that reads a dependency edge; two reader classes are outside the seam.** `validate_plan_doc` is reached by `load_plan` (`harness_yaml.py:319`) and by the writer (`plan-merge.py:470`, and also `cmd_amend` at `:1635`, which the plan does not name but inherits the rule for free). Caller by caller: `check-plan-routes.py:366` covered; `check-state.sh:140` covered; `factory_claim.py:107` covered; `factory_decompose.py:471` covered; `gh-sync.py:357`, `:1154`, `:1264` covered. **Uncovered plan readers found:** `handoff_done_when.py:120` and `:203` parse `plan.yaml` with bare `yaml.safe_load`; `check-state.sh:99`, `board_lifecycle.py:463`, `gh-sync.py:269` and `gh-sync.py:423` use `load_file`, which never calls `validate_plan_doc`. None of the six reads `depends_on` — they read the station and `source_issues` — so the intent's subject is untouched. `low` — a later reader added beside these inherits no rule; nobody is harmed today.

**2. "before it can be SIGNED" — MET, by T-03 through the write route.** The concrete command an author actually runs is `python3 .claude/skills/harness/bin/plan-merge.py apply` — the only writer of `plan.yaml` — which refuses at exit 5 with `ILLEGAL PLAN` (`plan-merge.py:510-514`, proven by T-02). The dangling edge therefore never exists to be signed. Second, weaker stop before signature: `check-plan-routes.py`, whose stderr already names the offending pair via the exception text (`:370`) and which DEC-183 makes a required CI step. `sign-approval` itself validates nothing (`cmd_sign_approval`, `plan-merge.py:1159`) — irrelevant while `apply`/`amend` are the only writers, `info`.

**3. "before it can be CONSUMED" — MET in substance, DEGRADED in diagnosis on two consumers.** T-03 makes every `load_plan` consumer refuse the document, so no consumer ever acts on the edge. But the five callers do not report it alike: `check-state.sh:142-143` raises a violation naming the file and the message (the intent's ideal), `check-plan-routes.py:367-371` exits 2 as "the checker cannot run", `factory_decompose.py:471` propagates, `gh-sync.py:358` dies — while `factory_claim.py:108-109` swallows to `plan = None` and `gh-sync.py:1155`/`:1265` swallow to `{}`/`None`. `med` — a signed legacy plan carrying a dangling edge (or one written outside `plan-merge`) is now reported by `factory_claim` as *"no plan could be read at &lt;path&gt; — the feature directory or its plan.yaml is missing or unparseable"* (`factory_claim.py:194-195`), which sends the operator to look for a missing file; and `gh-sync._projected_for` silently places no cards. The plan proposes no task there, correctly per its own one-home decision, but the intent's "rejects" reads louder than "returns None".

**4. "valid plans retain their current behavior" — ASSERTED for consumers, PROVEN only for the loader.** T-04 (with D-04's paired fixture) genuinely proves that today's 67 plans still load and that the walk is not vacuous; that is a real proof, not an assertion, and it covers `load_plan` alone. Nothing in the plan proves a *consumer* of a correct plan behaves as before: T-03's `verify:` runs four suites (`tests/unit/test-plan-depends-on.py`, `tests/integration/test-plan-merge.py`, `tests/unit/test-harness-yaml-corpus.py`, `tests/integration/test-harness-yaml.py`) and none of them is the suite of `check-plan-routes.py`, `gh-sync.py`, `factory_claim.py` or `factory_decompose.py`. `low` — the change is purely additive and the corpus has zero dangling edges, so the risk is small; but REQ-04's second clause ("every consumer of a correct plan behaves as it does today") is carried by no task, and qa's full run is the first thing that would notice.

## Scope boundary, both directions

**Nothing over-reaches.** No task validates cycles or ordering; `T-01:115-116` forbids cases for all three, `T-03:197-198` adds no cycle detection. T-03's sentence that a self-dependency is ACCEPTED is **faithful, not an over-commitment**: with `known` = the set of all task ids, a self-reference is in `known` by construction, so "accepted" describes the null action rather than adding policy. `info`.

**Nothing asked for is missing.** Grilling Settled (missing ids only; the destination) → D-01/D-02/T-03. Facts 1–3 (the chokepoint; `factory_claim`'s unresolvable-blocker reading; `check-plan-routes` validates shape but not integrity) → all re-confirmed at source and reflected. Fact 4 / issue #201 (dangling edge reaches decomposition, GitHub `blocked_by`) → named in T-03's comment discipline. Issue #201 is reachable and corroborates; its "a check alongside `check-plan-routes.py`" suggestion is deliberately declined by D-01, correctly — that route would leave the writer free.

## Q1 — the lane (D-03): **defensible**

My reading rests on one sentence, `DECISIONS.md:4377-4378`: *"A module a gate imports is not itself a gate. A squad may write the library."* The un-quoted last sentence — *"The gate's behaviour changes only by a hand the carve-out governs"* (`:4379-4380`) — is the ruling's *proof obligation*, not a second prohibition: it is discharged by the very evidence DEC-174 names, "the gate's violation set is identical before and after", which D-03 supplies as the af859ee8 corpus measurement (67 plans, 0 dangling → zero delta). Neither `harness_yaml.py` nor `tests/**` is in the enumerated enforcement layer (`:4360-4363`), and no hand wires a gate to anything new. `low`, non-blocking: if the operator reads that last sentence as absolute, only T-03's two `execution_mode` lines change, and it is a one-field edit at signature — but nothing catches a wrong call here, since `check-plan-routes.py` only prints DEVIATION for *granted* main-session-direct paths.

## Q2 — the exit-2 surface: an exit-2 stop **satisfies** "before it can be consumed"

`PlanSchemaError` subclasses `YamlParseError` (`harness_yaml.py:269`), so `check-plan-routes.py:367` reports exit 2 rather than a violation line — and that is sufficient here for two reasons: the stderr line interpolates the exception, so the author sees the T-02→T-99 pair D-02 mandates, and DEC-183 makes the checker a required CI step, so non-zero gates regardless of which non-zero it is. `info`. What does *not* satisfy the clause is the swallowing pair graded in clause 3 (`factory_claim.py:108-109`, `gh-sync.py:1155`/`:1265`): there the refusal becomes `None`/`{}` and the operator is told the wrong cause. `med`, and it is the single largest gap between this plan and the destination sentence. Leaving it out is defensible under D-01 (one home, no consumer edits) and out of the operator's stated scope; it belongs to the operator, not to a task pm can add.

## SC audit (SC-01..SC-05)

Every SC carries exactly one `verify:`; both `automated` kinds are active with real commands (`harness.json test_kinds.unit`, `test_kinds.integration`, `status: active`, `cmd` = `run-unit-tests.sh --kind unit|integration`), and `tests/unit/**`/`tests/integration/**` match the files touched.

**Two SCs a broken implementation could still pass:**
- **SC-02 — `med`.** Its words are "exits non-zero and leaves the target file byte-identical". A `plan-merge` that refused *every* apply passes it, and so does exit 9 (`--file` never resolved, `plan-merge.py:52`), which is the fixture not being built. T-02 case (a) pins exit 5 + `ILLEGAL PLAN` + `T-99` and case (b) the paired allow, so the *task* is discriminating while the *criterion* is not — a later reviewer grading SC-02 on its own text can green a deny-everything build. SC-01+SC-03 exclude deny-everything on the READ route only; no SC pairs an allow on the WRITE route.
- **SC-05 — `low`.** "the recorded run … fails" is satisfied by a FAIL from any cause — an import error or a missing fixture reads identically to the assertion failing for the right reason.

**Three that a broken implementation could not pass:** SC-01 (names both ids), SC-03 (a zero-match walk contradicts its stated "67 load" result, and T-04's ≥67 floor plus the case-B paired detector makes vacuity and stopped-checking both red), SC-04 (a grep of the reviewed tree finds exactly one implementation; D-01 and T-03's "touch NO other file" make it reachable). Tested by reading each criterion's literal words against a hostile implementation — deny-everything, first-edge-only (excluded by SC-01 via T-01 case 3, D-02), and zero-match corpus walk.

## Open questions

- **Q-A (blocking, operator only):** should this feature also make the two swallowing consumers report a dangling plan loudly — `factory_claim.py:108-109` and `gh-sync.py:1155`/`:1265` — or is "not consumed, diagnosed as unreadable" the accepted landing? Widening is outside the grilling's stated scope, so it is the operator's call, not a task pm may add.
- **Q-B (non-blocking):** SC-02 as written is passable by a write route that refuses everything. Only the main session can amend the BRIEF.
