# Observations - harness-orchestrator

- 2026-09-06: BUG-201 plan phase. notes/handoff-<phase>.md CANNOT be written for a brand-new feature from a worktree. check-domain.sh:1747 passes handoff_done_when.problems the worktree-STRIPPED rel path together with the MAIN checkout root, so _feature_dir lands on <main>/.harness/harness/features/<FEAT>, which does not exist until the branch merges. Measured with one identical note body: worktree root -> [], main root -> both Authority pointers unresolved. All four legal authority types resolve through that same base, so no pointer can pass. Folded the five handoff sections into STATE.md ## Current instead — the playbook's disk-only successor path — and raised it as an open question rather than working around the gate.
- 2026-09-06: the plan-panel step-1 goal-check (a product-lead segment) produced its note and NO run dir. Recording it in feature.json runs: would have tripped INV-8 (referenced but its dir is absent), so I left it out and said so in STATE.md. INV-8 and the playbook's "runs names every digest" pull in opposite directions for a segment whose artifact is a note.
- 2026-09-06: pm returned a THIRD panel.readers entry my dispatch never named — goalcheck — because check-state.sh iterates the closed vocabulary {should-not-exist, scope, goalcheck} and hard-fails any member with no entry. A dispatch that enumerates only the team manifest's steps under-specifies the panel record.
- 2026-09-07: BUG-201 plan c1. `plan-merge.py cmd_set_task_station` is a PURE TEXT SPLICE — it never calls `validate_plan_doc`, and `_verify_spliced` is reached only from `apply` (plan-merge.py:796). That is what made the rewritten SC-09 discriminating: `gh-sync start-task` completes its plan write even over a dangling plan and still reaches `_projected_for`. A criterion pinned to `status Ready` instead was greenable by a different site's fix; pm caught one such, and I had to verify the replacement myself before accepting it.
- 2026-09-07: BUG-201 plan c1. The handoff-note write defect reproduces exactly as the c0 orchestrator recorded it, and I now have the site: `check-domain.sh:1141-1149` returns `harness_boundary.checkout_relative(...)[1]` (the stripped rel) and DISCARDS `[0]` (the worktree root), then `:1748` passes the stripped rel with the MAIN root into `handoff_done_when.problems`. Every authority type resolves through that base, so for a feature that exists only in a worktree no pointer can pass. Measured both roots with one body: worktree -> [], main -> both unresolved.
- 2026-09-07: BUG-201 plan c1. Repairing a panel's OWN findings after it returns PASS raises a currency question with an infinite-regress shape. Routing it to the advisor produced a usable boundary rather than a yes/no: a re-run is obliged only by a post-panel edit the panel did not prescribe (weakening/deleting an SC or task, changing a decision/lane/execution_mode, adding a task or SC, or any edit after an approval reset); a repair stays inside the line when it is the digest's own named remedy, strictly additive, below high, and recorded in the same panel entry at `disposition: repaired`.
- 2026-09-07: BUG-201 plan c1. The c1 panel's one med finding was a COVERAGE gap in a criterion that already looked complete: SC-07 named four consumer suites and two more consumers called `load_plan` (`factory_decompose.py:471`, `check-state.sh:140`). Enumerating the callers myself before dispatching the repair let me hand the fix a closed list, and pm then wrote the enumeration into the BRIEF so a reader can check it rather than trust it.
- 2026-09-07: BUG-201 plan c1. An interrupted run left `state.yaml` at `status: running` with its only step `pending` while its artifacts HAD landed. The honest close was to have the same squad write the missing digest recording the host loss and grade the step BLOCKED — not to infer PASS from the artifacts, and not to redo the work. One cycle counted, that one.
