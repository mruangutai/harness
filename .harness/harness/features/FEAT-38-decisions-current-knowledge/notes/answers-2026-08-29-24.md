# Operator answers — FEAT-38 replan, run 24 questions — 2026-08-29

Written by the main session. **Q5's ruling is already applied; the signature is NOT given yet** — the
operator is reading `BRIEF.md` and `plan.yaml` before signing, and no revision is dispatched until
that read finishes and every request arrives as ONE set.

## Blocking questions

- **Q1 — reset both approval fragments.** DONE by the main session at commit `e0ed1ad`.
  `plan.yaml`'s `approval:` and `BRIEF.md`'s `## Approval` both read `status: pending`,
  `approved_by: none`, `date: none`, each carrying a withdrawal note stating that the 2026-08-29
  signature covered the pre-ruling scope. `harness_yaml.load_plan` re-validates: 28 tasks,
  `approval.status: pending`.
- **Q2 — numbering.** CONFIRMED. REQ-08 and SC-09 stay retired in place as tombstones; the live sets
  are deliberately non-contiguous at REQ-01..REQ-10 and SC-01..SC-18. Renumbering was rejected
  because it silently repoints citations in already-landed artifacts.
- **Q3 — empty traces.** ACCEPTED. `traces: []` on T-20 and T-21 stands. The requirement those tasks
  served is retired; citing a tombstone id would imply a live requirement that no longer exists.
- **Q4 — re-grading.** RE-GRADE SC-11 ONLY. Its per-entry read-back re-runs over the six entries
  T-27 touches. **SC-13's UAT judgement STANDS and is not re-run** — T-27 deletes HTML-comment
  markers without touching prose, so the operator's reading of the folded entries is unaffected. If
  T-27 turns out to touch prose in any of the six entries, that assumption is void and SC-13 returns
  to the operator.
- **Q5 — cycle headroom.** RAISED: `max_total_cycles` 10 -> 20 in `feature.json`, written by the main
  session. `cycles_used` stays at 14, unfrozen and unrewritten. This gives the 28-task build roughly
  six cycles of real headroom. `max_total_runs` stays at 20 and is informational.

## Non-blocking questions

Not yet answered. They are held deliberately so they can ride the ONE consolidated revision after the
operator's read-through, rather than arriving as separate rulings — the failure mode DEC-138 records
from FEAT-03's plan phase (seven serialized runs, ~$95, no reviewer finding anything).

Held: Q6 (T-24's unscoped blast-radius sweep serializing the code lane behind T-27), Q7 (positive
guidance in DEC-205), Q8 (filing the accepted semantic-rot gap as backlog), Q9 (T-03's dense intent),
Q10 (the two harness defects).

## Independently measured by the main session

**Q10's approval-guard defect is REAL and reproduced, not relayed.** The identical `harness-pm` Edit
payload against an `approval:` block:

| Target | Exit |
|---|---|
| `FEAT-32-concurrent-write-merge/plan.yaml`, main checkout | 2 — BLOCKED |
| `FEAT-27-expertise-repository-tier/plan.yaml`, main checkout | 2 — BLOCKED |
| `FEAT-37-lead-stop-and-wake/plan.yaml`, main checkout | 2 — BLOCKED |
| `FEAT-38-decisions-current-knowledge/plan.yaml`, this worktree | **0 — ALLOWED** |

Mechanism confirmed directly: `fnmatch(".claude/worktrees/harness/<id>/.harness/harness/features/<id>/plan.yaml", ".harness/*/features/*/plan.yaml")` is `False`, so the fragment denial never fires.
Every feature is planned in a worktree, so the signature guard is inert exactly where it is needed.
It held on this feature by instruction, not by enforcement. This needs its own feature; it is not
FEAT-38's to fix, and FEAT-38 must not be the vehicle for it.
