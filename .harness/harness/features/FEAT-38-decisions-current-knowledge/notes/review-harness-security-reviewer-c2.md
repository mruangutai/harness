# Security review — c2 — delta `2557950..48bbe7e`

**BLUF: PASS, no findings.** Examined the full 26-file delta plus a whole-diff secret sweep. Every
file with a plausible security surface (`.gitignore`, its install template, `fleet.yaml`, both
orchestrator agent definitions) carries a citation-text-only edit; the actual ignore rules,
capability grants, and frontmatter are byte-identical to `2557950`. No script, hook, or `bin/`
file is in this delta at all. No credential-shaped string anywhere in the diff. The new `.html`
is static, script-free, generated by the pre-existing `render-brief.py`.

## What I opened, per dispatch item

**1. Stat census / bin+hooks silent-touch check.**
`git diff 2557950 48bbe7e --stat` → 26 files, matches the dispatch's declared list exactly.
`git diff 2557950 48bbe7e --stat -- .claude/skills/harness/bin/ .claude/hooks/` → empty. Neither
directory appears anywhere in the diff. No cover-of-docs-sweep script change. **Clean.**

**2. `.gitignore` and `.claude/skills/harness/templates/gitignore.snippet`.**
Both diffs are single-hunk, single-line: `(DEC-171 am.1)` → `(DEC-171)` inside a comment line
directly above the `.harness/.pyyaml-bootstrap` rule. The `+`/`-` pair is the comment sentence only
— the ignore-pattern line itself (`.harness/.pyyaml-bootstrap` / `.harness/*/features/*/runs/**`)
does not appear in either hunk, so no path is newly ignored or newly un-ignored. The two files'
touched comments now read identically (`DEC-171`), so they stay consistent with each other post-edit,
same as pre-edit. **No ignore-rule change. Not a finding.**

**3. `.harness/factory/fleet.yaml`.**
Single-line diff, comment only: `mruangutai/harness is DELIBERATELY ABSENT (DEC-174 am.1)` →
`(DEC-174)`. No YAML key, model, permission, path, or repo entry touched — the hunk is entirely
inside a `#`-prefixed line. **No capability change. Not a finding.**

**4. `.claude/agents/harness-orchestrator.md` and `.omp/agents/harness-orchestrator.md`.**
Both files carry the identical single-line edit: `(DEC-100, DEC-102)` → `(DEC-100, DEC-120)`, inside
a prose sentence about flat-mode being dead. Diffed each file's full stat (`+1/-1`) — no frontmatter
block, `tools:` list, or any line outside that one sentence was touched, so no grant changed and the
two copies did not diverge from each other. DEC-102 is one of the "deleted id" targets T-14's intent
names (cite the successor); DEC-120 is a live heading — successor substitution, not a scope or
capability change. **Not a finding.**

**5. Feature-dir records (T-22/T-23) — secret/PII scan.**
`git diff 2557950 48bbe7e | grep -inE '(api[_-]?key|secret|token|password|bearer|-----BEGIN|
ssh-rsa|AKIA[0-9A-Z]{16}|ghp_[A-Za-z0-9]{30,}|xox[baprs]-)'` over the *entire* 26-file delta (not
just the named files, per Expertise P-14) → every hit is the word "token" used in its shell-parsing
sense (allowlist first-token checks, `GIT_CONFIG_*` discussion) inside prose describing a past
review of `check-decision-claims.py`; zero credential- or key-shaped strings. One absolute local
path appears in `ship-review-2026-08-29-16.{md,html}` (`/Users/.../harness/.../review-...-c0.md`)
— it's the operator's own dev-machine path, cited as evidence in a bug report about a host defect
that once misdirected an artifact write; not a secret, and it's already this session's own working
directory. `ship-review-2026-08-29-16.html`: read the full 111-line file — inline `<style>` only,
no `<script>`, no `onerror=`/`onclick=`/`onload=`, no `http(s)://`, no `<iframe>`, no `javascript:`,
no `eval(`/`innerHTML`/`document.`/`window.` — a static CSS-themed render of the paired `.md`,
consistent with the pre-existing `bin/render-brief.py` tool (untouched by this delta per item 1).
**Not a finding.**

## Non-goals confirmed out of scope
DECISIONS.md fold, `gen-decisions-index.py`/`check-decision-claims.py`, and their prior reviews
(`review-*-c1.md`, `research-FEAT-38-goalcheck-2557950.md`) were graded by the prior panel at
`2557950` and are unchanged content, re-surfaced only as feature-dir records — not re-graded here.

## Threat model
No trust boundary is crossed by this delta: it edits doc/comment prose and one config comment.
The one boundary with a real security consequence (`.gitignore` controlling what enters the tracked
tree) was checked hunk-by-hunk and found untouched on its rule lines.
