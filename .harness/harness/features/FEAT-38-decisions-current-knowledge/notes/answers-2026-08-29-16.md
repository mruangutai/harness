# Answers — FEAT-38 ship return 2026-08-29-16

## Q1 — UAT
Unresolved. Re-present the UAT only after the repinned QA/review/goal-check gates are green.

## Q2 — main-session-direct tasks
Resolved.

- T-14 completed and committed as `e88182c`; its signed verify block passes with no output.
- T-22 completed and committed as `c1d657b`; `notes/readback-fold.md` records BELIEF and FALSIFICATION findings for all fifteen entries, and its signed verify block passes.
- T-23 completed and committed as `7cb69a9`; issue #448 is `CLOSED NOT_PLANNED`, issue #686 received the signed partial-landing comment and remains OPEN.
- Each task was moved through `building` to `done`; `gh-sync.py start-task` moved #949, #957 and #958 to Building.

The review pin is now stale by construction. Re-run the required suites and review panel against the new tip before asking for UAT.

## Q3 — signed verify amendments
Unresolved and non-blocking. Preserve the exact proposed replacements for the ship briefing; only the operator may sign them.

## Q4 — possible main-checkout data loss
Acknowledged as unknowable. Do not infer whether prior uncommitted edits existed.

## Q5 — host defects and misplaced artifact
The two host defects remain proposed backlog. The misplaced main-checkout artifact `.harness/harness/features/FEAT-38-decisions-current-knowledge/notes/review-harness-security-reviewer-c0.md` was deleted.

## Q6 — worktree debris
The five zero-byte root files (`100644`, `2`, `1788036665430977000`, and the two 40-hex names) were deleted. The grilling note remains; it is a legitimate feature intake artifact, not debris.
