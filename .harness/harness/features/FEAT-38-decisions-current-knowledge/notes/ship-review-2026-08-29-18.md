# FEAT-38 — decisions as current knowledge · ship review (supersedes the 16 briefing)

**Recommendation: run the UAT, then ship — but you have a second decision to take first, and it is
a record-integrity one, not a technical one. The cycle budget is spent and one over.**

Everything a squad can do is done and green at a current pin. **Twelve of thirteen success criteria
are met at `review_sha` `48bbe7e`.** The thirteenth is your UAT. `must_fix` is empty at every gate.

Two things need you:

1. **Run the UAT** — `notes/uat-FEAT-38.md`, ~15 minutes. It is the blocking gate.
2. **Rule on the cycle budget.** `cycles_used` is **11 of a `max_total_cycles` of 10**. The hard
   bound is crossed, so I stopped rather than continue, which is what the rule requires. Nothing is
   waiting on a fix — see *Budget* below for what the eleven actually were and why I think this is a
   counting artifact rather than a runaway.

`review_sha` is **`48bbe7e`** (was `2557950`). Branch `feat/FEAT-38-decisions-current-knowledge`.
**No PR exists and none was created.**

**How this briefing was assembled — no report round was spawned.** Every claim is read from run
digests already on disk (DEC-69) or measured by me directly. The prior sixteen digests are named in
`notes/ship-review-2026-08-29-16.md`, which this document supersedes. The three digests new since it
are `runs/2026-08-29-11-validator/digest.md` (qa), `runs/2026-08-29-17-validator/digest.md` (review
panel) and `runs/2026-08-29-17-product/digest.md` (goal-check). Note that `runs/**` is gitignored by
design, so those three die with the worktree; the durable evidence is in `notes/`, cited per claim
below.

---

## What changed since the last briefing

You resolved the three carved-out `main-session-direct` tasks yourself, and they landed:

| Task | Commit | What landed |
|---|---|---|
| T-14 | `e88182c` | The citation sweep — 18 edits in 13 files. **This is what SC-04 was waiting for** |
| T-22 | `c1d657b` | `notes/readback-fold.md` — BELIEF and FALSIFICATION recorded for all fifteen entries |
| T-23 | `7cb69a9` | Issue #448 closed `NOT_PLANNED`; #686 given the partial-landing comment and left open |

All 23 tasks now read `done` in `plan.yaml`, so **`gh-sync.py status Review` ran and succeeded** —
the parent and all 23 sub-issues (#936–#958) are at the `Review` station. It refused last time
because it requires every task done; that refusal was the mirror working, and the mirror is never a
gate.

I then re-pinned `review_sha` at `48bbe7e` and re-ran every automated gate there, because a pin
inherited from `2557950` grades a tree the new work is absent from.

---

## The gates, all re-established at `48bbe7e`

**qa — PASS, and this is the project's only blocking gate.** Exit 0, **0** `FAIL`, **0** anchored
`^KIND-DRIFT:`, output captured into a variable and searched rather than piped. Index diff-clean:
`gen-decisions-index.py --stdout` matches the committed index. Both feature checkers still
*discover* — 20 anchors and 11 claims — which is the number that matters, because a checker that
passes over zero markers is indistinguishable from one that works.
Evidence: `notes/qa-2026-08-29-11-validator.md`.

**Review panel — PASS, `severity_max: med`, `must_fix` empty.** All three reviewers opened the named
file set and returned their own verdicts rather than self-scoping out. They adjudicated the three
citation questions I raised:

- **`DEC-102` → `DEC-120`** (3 files) — **correct**, matches the recorded successor in the base index.
- **`DEC-192` → `DEC-191`** — **correct**, and the panel upgraded my own grade of "defensible". The
  rule it set is worth keeping: *cite the decision that supports the claim.* A recorded successor is
  a routing hint for a dead id, not a citation obligation. `DEC-192`'s strike record points to
  `DEC-203`, but `DEC-203` is ticket lifecycle; the sentence being cited is about the closed
  eleven-key set, which is `DEC-191` almost verbatim. Following the successor mechanically would have
  produced a citation that resolves to a live heading and to the wrong decision — the exact defect a
  heading-existence check cannot see.
- **`DEC-19` dropped rather than repointed** in `debug-mission.md` — **correct**, and my premise was
  wrong: I assumed the sweep improvised around two successors, but T-14's own intent names this file
  and instructs the drop. Neither `DEC-84` nor `DEC-85` discusses approval bypasses, and the pattern
  is already stated in words before the parenthetical.

Evidence: `notes/review-harness-code-reviewer-c2.md`, `notes/review-harness-security-reviewer-c2.md`,
`notes/review-harness-ui-reviewer-c2.md`.

**Goal-check — PASS, 12 of 13.** Evidence: `notes/research-FEAT-38-goalcheck-48bbe7e.md`.

| | Criterion | Result at `48bbe7e` |
|---|---|---|
| SC-01 | no amendment structures | **met** — both patterns exit 1; controls at base 25 and 13 |
| SC-02 | 15 ids gone, per id | **met** — fifteen separate assertions |
| SC-03 | DEC-90 survives | **met** — `DECISIONS.md:1057` with its strike record |
| SC-04 | no stale citation anywhere | **met — flipped from `not_met`** |
| SC-05 | index clean and regenerable | **met** — three patterns exit 1; `--stdout` diffs clean |
| SC-06 | generator machinery gone | **met** — orphan detection proved behaviourally |
| SC-07 | amendment guard reddens | **met** — planted heading returns False; unplanted returns True |
| SC-08 | anchor checker reddens | **met** — 3/32 failed at base, 0/20 at pin, 1/21 with a plant |
| SC-09 | claim checker reddens | **met** — 0/11 at pin; the 80→81 mutation names the marker |
| SC-10 | suite green | **met** — cited to the qa run, not re-run |
| SC-11 | folded meaning survives | **met** — 15/15, coverage inherited on proof of byte-identity |
| SC-12 | old convention no longer taught | **met** — front matter rewritten, `DEC-205` is its new home |
| SC-13 | operator reads three folded entries | **unrun — yours** |

**SC-04 is the one that moved, and I checked it twice.** pm ran the three sweeps per id for all
fifteen deleted ids at the pin — all empty — and then proved the measurement *can report red* with a
positive control in the identical scope (`DEC-188` → 26 hits, `DEC-90` → 6 hits). I ran the same
sweep independently before dispatching and got the same result, with my own control returning 17
files. That double-check matters here because this feature has twice been bitten by searches that
traverse nothing: `.agents/**` is a symlink onto `.claude` and returns a confident zero, and
`git grep -E` has no `\b`.

**SC-11 carries the meaning half of the feature and was NOT re-derived this run.** It rests on
`git diff 2557950 48bbe7e -- .harness/harness/docs/DECISIONS.md` being empty — the authority is
byte-identical across the repin, so the earlier per-entry coverage (10 entries by the panel, 5 by pm)
carries. I verified that diff is empty myself. If you distrust the inherited half, the UAT is where
you would feel it.

---

## The UAT — the blocking gate

`gates.uat` is `blocking_when_uat_criteria_exist` and SC-13 is exactly such a criterion, so **the
ship decision waits on you.**

**`notes/uat-FEAT-38.md`** — `status: ready`, ~15 minutes. It walks `DEC-138`, `DEC-174` and
`DEC-181`, giving the folded form and the pre-fold form at `7ebfc9e` side by side, because you
cannot judge "no claim silently disappeared" from the folded text alone.

Its header pinned the stale `2557950`; that was repointed to `48bbe7e` this run and I confirmed zero
stale references remain in the file. Its extractions anchor on `## DEC-NNN` headings rather than line
numbers, so they survive the repin.

**T-22 and the UAT are not the same read.** T-22 is a per-entry record across all fifteen entries;
the UAT is your judgement on three. Do not do it twice.

---

## Still needing your signature: three `verify:` blocks that cannot pass as written

Unchanged from the last briefing and deliberately preserved verbatim, per your answer to Q3. In all
three the **work is correct**; the gate text is wrong. Replacement text is in
`notes/research-verify-block-defects.md` (backlog row B-1).

- **T-15** — calls `check-expertise.sh` with no argument; that script requires a path and exits 2
  without one, so the block cannot exit 0 for any file content.
- **T-10** — its exclusion list names two by-construction failures; there are three. It also demands
  a `FAIL` line that T-11 legitimately turns green, so there is no window in which it passes.
- **T-19** — greps the bare string `KIND-DRIFT`, which the drift detector's own test suite contains
  in its passing case labels. Measured: 6 bare matches, 0 for the anchored `^KIND-DRIFT:`.

Only you can amend a signed plan. Nothing is blocked on this; it is so the record does not carry
three gates that can never go green.

---

## One defect that sits directly in your reading path

The review panel found that `notes/ship-review-2026-08-29-16.html` — the rendered view of the
*previous* briefing — shows an **empty code block** where the markdown carries the RCE payload
example. Its most safety-critical evidence renders blank.

**The panel's proposed remedy was to re-run the renderer. I checked, and that would not have worked.**
`render-brief.py:131` strips `<!--.*?-->` from the whole document *before* it handles fenced blocks,
so a code block whose content is an HTML comment is destroyed every time. Re-rendering reproduces it.
I worked around it in this document by writing the marker's payload without the comment delimiters;
the renderer bug itself is backlog row **B-19**.

For the record, the payload that executed was a claim marker — an HTML comment of the form
`claim: <command> :: <expected>` — whose command read:

```
git -c "alias.zz=!touch /tmp/p_f" zz
```

The first token was `git`, so the old allowlist passed it. That hole was found by the panel at
cycle 0, fixed, re-pinned, and confirmed closed at cycle 1 by a reviewer that probed for *bypasses*
rather than re-running the known vectors. It does not gate.

---

## Budget — the second thing needing your decision

**Cycles: 11 of `max_total_cycles` 10. The hard bound is crossed.** Per the rule I stopped and
returned rather than continue, and I am not treating a hard gate as a soft one. But you should have
the facts, because I do not think this is the runaway the bound exists to kill:

- Nine cycles were spent before this run and are described in the previous briefing.
- **Cycle 10** — inside the qa re-run. qa misread a baseline as 6 `FAIL`, caught and corrected it
  itself (a `.git`-less tar extraction had broken the checker's `git ls-files` subprocess). It
  recorded the false reading rather than sweeping it.
- **Cycle 11** — inside the review panel. The ui-reviewer returned FAIL at `high`, was sent back once
  on out-of-scope evidence, and re-rated it `med` itself.

Both were lead-internal send-backs during **re-verification of already-passing gates**, both
self-corrected within their run, both ended PASS, and **neither changed a line of production code.**
No fix cycle was ever routed to a builder. Nothing is pending that needs an eleventh, let alone a
twelfth.

Your options: **raise `max_total_cycles`** for this feature (a user decision, recorded in
`feature.json`), or **accept the crossing and ship on the UAT result**. I recommend the latter — the
work is done and the count is measuring self-correction inside verification runs, which is the
behaviour you want, not rework on the product.

**Runs: 19 of an informational 20.** That budget never stops a branch; it asks three questions.
My read: each run resolved something. The three added this run were forced by the pin moving, not by
anything going wrong, and two of the three found real defects — the empty-code-block render and the
stale UAT pin — that would otherwise have reached you.

---

## Proposed backlog

One row per residual finding that does not gate. **Strike any row you do not want; anything not
listed here dies silently, so everything is listed.** IDs B-1…B-18 keep the numbering from the
previous briefing so you can strike by name across both; B-19…B-23 are new this run.

| ID | Nature | Finding |
|---|---|---|
| B-1 | bug | Amend the three unsatisfiable `verify:` blocks (T-10, T-15, T-19). Replacement text ready in `notes/research-verify-block-defects.md`. Needs your signature |
| B-2 | bug | **Host defect, hit 4+ times across 3 runs:** the edit/write tool family resolved worktree-relative paths against the MAIN checkout while reporting success and rendering plausible diffs. The one artifact that actually landed there has since been deleted, but the defect stands |
| B-3 | bug | **Host defect:** a member returned an empty `{}` structured result — no VERDICT, no DIGEST — and the `SubagentStop` digest validator did not block it. Seen twice |
| B-4 | chore | A T-06 member ran `git checkout -- <path>` on the MAIN checkout for two generator files. Both are confirmed back at committed content and no stash exists. You ruled this unknowable and told me not to infer; recorded so it is not silently dropped |
| B-5 | chore | ~~Worktree debris~~ **RESOLVED** — the five zero-byte root files were deleted; the grilling note was a legitimate intake artifact and is now committed to the branch |
| B-6 | bug | `DEC-162` presents the removed codebase-map tier as current. Pre-existing at `7ebfc9e`, outside this feature's grep-driven scope |
| B-7 | bug | `SPEC.md` contradicts itself on org depth: team-shape prose uses pre-DEC-120 layer numbers while §10.2 uses DEC-120's cap of 3. Neither half cites a deleted id, so no grep in this feature reached it |
| B-8 | enhancement | Follow-up hardening on `check-decision-claims.py`, none reachable from a PR today: clear `GIT_CONFIG_COUNT`/`GIT_CONFIG_KEY_*`/`GIT_CONFIG_VALUE_*` in `_subprocess_env()`; route `git grep` through the file-option check (`-f`/`--file` skips it); refuse option-like tokens in *every* position |
| B-9 | enhancement | Nobody has swept the rest of `bin/` for F-1's *shape* — any other script building an argv from document or config text |
| B-10 | enhancement | `DECISIONS.md:6286-6288` states D-10's boundary as "refuses any command whose first word is not git or grep" — now a strict understatement of six enforced rules. Fold into the next edit of that entry |
| B-11 | enhancement | `ALLOWED_GIT_SUBCOMMANDS` is new and carries no claim marker |
| B-12 | chore | SC-04 has no automated gate over `.harness/harness/docs/`. T-14's verify roots exclude it, which is how `BUILD.md`'s `am.2` came to be asserted by a criterion and owned by no verify |
| B-13 | chore | `test_kinds.unit.detect`'s catch-all glob and `integration.detect`'s explicit paths BOTH match the three logic test files. Coverage is unaffected; the answer decides whether `logic` needs a signed `_matrix_provenance` entry |
| B-14 | chore | One unexplained transient: a hand-run of `test-gen-decisions-index.py` showed 2 FAIL while a background job finished; 5 isolated reruns and 2 full-suite runs were clean. A race and an intermittent real drift are not distinguishable from this evidence |
| B-15 | chore | **Recurred this run and cost a full send-back.** `run-unit-tests.sh`'s `PASS` total is ambiguous across `^PASS` / `'^PASS '` / `^PASS:` conventions used by different scripts, so one tree reads 1117, 1002, 1285 or 1150 depending on the expression. Exit 0 and `^FAIL = 0` are unanimous. Worth emitting one normalized aggregate so future gate cycles stop producing apparent regressions that are really regex drift |
| B-16 | bug | `plan-merge.py` exits 8 on a new `plan.yaml` whose proposal carries an `approval:` mapping, so the documented "write it through the merge tool" cannot create a plan. It is also ADD-ONLY (exit 7 on a changed value), so a task `status:` transition has no route through it either |
| B-17 | chore | The plan's T-05 intent maps `DEC-137 → DEC-162` as a successor "whose glossary half carries it". It does not. T-05 correctly stated the rule with no citation instead |
| B-18 | chore | `docs` and `config` floor at **zero** required test kinds, so 16 of 23 tasks carried no required test. `matrix_ok: true` is a much weaker claim than "the docs changes are verified" — their weight sits on the UAT and the two committed-index guards |
| B-19 | bug | **New.** `render-brief.py:131` strips `<!--.*?-->` across the whole document before fenced-block handling, so any code block containing an HTML comment renders empty. It silently blanked the RCE evidence in the previous briefing's HTML. Strip comments only outside fences |
| B-20 | bug | **New.** `render-brief.py:36,:53` — `--quiet #7d8b99` on `--paper #fbfcfd` is ~3.39:1 against WCAG AA's 4.5:1, consumed at `:63/:70/:88` (`thead th`, `.eyebrow`, `h4`–`h6`). Affects every briefing ever rendered in light mode; dark mode passes at ~4.86:1 |
| B-21 | chore | **New.** The joint citation `(DEC-100, DEC-120)` at `.claude/agents/harness-orchestrator.md:46`, `.claude/skills/harness-team/SKILL.md:15` and `.omp/agents/harness-orchestrator.md:49` is imprecise: `DEC-100` states "hierarchical works, the flat fallback is not needed"; `DEC-120` does not. Inherited from the pre-sweep pairing — T-14 introduced nothing — so it is a tidy-up, not a regression |
| B-22 | enhancement | **New.** Nothing pins `DEC-N` citations in `.claude/**` against future rot. This feature's two checkers are scoped to the authority document only: the anchor checker matches `path:line` anchors, never decision ids. The surface T-14 just swept by hand has no guard |
| B-23 | bug | **New.** A member reported `SC-13` as `not_met` in its `sc_status` while its own evidence string said "unrun by design". Its lead caught and corrected it. A `not_met` on a `verify: uat` criterion misroutes a ship gate as a build failure, so the digest validator should reject the pair |

---

## What I need from you

1. **Run the UAT** — `notes/uat-FEAT-38.md`. It is the blocking gate and the last thing between this
   feature and a ship decision.
2. **Rule on the cycle budget** — raise it, or accept the crossing. Nothing is waiting on a fix.
3. **Strike any backlog rows** you do not want; the rest become issues on ship acceptance.
4. **Sign the three `verify:` amendments** (B-1), or tell me to leave them.

The GitHub mirror is open and current: milestone 31, parent **#935**, sub-issues #936–#958, all at
the `Review` station. **No PR exists, none was created, and nothing was merged.**
