# Observations - harness-documentor

- 2026-08-29: FEAT-38 T-01 — my first edit used a repo-relative path and the tool resolved it against the session cwd (the MAIN checkout), not the dispatch's worktree; the hunk landed in main. Caught only because the worktree verify still failed. Reverted by content edit (git checkout -- was forbidden). Lesson: when a dispatch names a worktree, every edit path must be absolute from the first edit, and the pre-edit baseline verify run is what exposes a misdirected write.
- 2026-08-29: FEAT-38 T-01 — the plan's intent block and its own verify clause quoted two different wordings of the same replacement sentence ('for a reader to land on' vs 'to repoint its citations to'). Wrote the verify form; a gate outranks prose. Worth checking for this class of self-disagreement whenever an intent block quotes prose a grep also pins.
- 2026-08-29 (FEAT-38 T-02): plan intent claimed a plain `git log` reaches 'only 8' folded amendments; measured 5 amendment markers at the FEAT-22 rename 8ad7d52 vs 15 at HEAD, i.e. 10 post-rename. Mechanism (plain log stops at the rename) was right, the count was not — wrote the mechanism, omitted the number, raised the figure.
- 2026-08-29 (FEAT-38 T-02): a positional gate (head -12 forbids a literal, head -20 requires one) is satisfiable without thinking about line drift only if the required token goes in the FIRST bolded run; putting 'current truth' at line 3 left 17 lines of slack for later front-matter growth.
- 2026-08-29 (FEAT-38 T-03): a task intent mandated quoting a sentence that a SIBLING task's acceptance clause counts with grep -c == 1; quoting it verbatim would have failed T-01. Paraphrased and re-checked the sibling guard. Intent-mandated wording must be diffed against every sibling task's verify block, not just my own.
- 2026-08-29 (FEAT-38 T-03): plan intent attributed the feature.yaml -> feature.json rename to DEC-191; DEC-191 (@6080) is the closed-key-set entry and no entry records the rename. Wrote the verified anchor list instead and raised it. Third time a handed-down DEC attribution in this repo was wrong.
- 2026-08-29 (FEAT-38 T-03): gen-decisions-index.py stamps a brand-new entry's row '`RULING PENDING`' and still exits 0, so an append+regenerate ships a placeholder ruling silently. Flagged to the index-owning task.
- 2026-08-29 (T-04): deleting a struck entry whose citation is an EXEMPLAR ("same failure shape as DEC-140") needs the cited lesson inlined, not the reference dropped — dropping it leaves the surviving entry asserting a comparison to nothing. Deleting one whose citation is a CITER-LIST entry ("DEC-140 and DEC-149 cite it") needs only removal plus verb agreement, and re-checking the remaining citer is real. Two different repointing shapes behind one grep pattern.
- 2026-08-29: a task verify block whose last gate was `bash check-expertise.sh` with no argument exited 1 forever (the script's usage error is exit 2); every content clause passed. Baselining the block before editing is what separated "my edit failed" from "the gate is malformed".
- 2026-08-29: check-domain.sh denies the documentor any scratch path, so a hand-built broken-copy negative control is impossible; the checker's own unit test (test-check-expertise.py, with explicit reject cases) is the substitute.
- 2026-08-29 (T-05): the plan named DEC-162 as DEC-137's successor, but DEC-162 does not carry the map-tier removal at all — it still describes the map as live (`.harness/codebase/glossary.md`, "a mapped codebase", INV-19/INV-20 over map surfaces). Citing it would have repointed three sentences at an entry contradicting them. Stated the rule directly with no citation instead. Lesson: a plan's successor mapping is a hypothesis about what the successor SAYS; open the successor and read it before repointing to it.
- 2026-08-29 (T-05): more than half the citations of the six ids sat INSIDE another of the six spans (DEC-196's span alone held six citations of DEC-186 and DEC-192). Computing all spans FIRST, then classifying each grep hit as in-span or out-of-span, cut the repointing work from 60 sites to 20 and prevented rewriting sentences that were about to be deleted.
- 2026-08-29 (T-05): repointing surfaced a FALSIFIED claim, not just a dangling id — DEC-200's "no source ticket is ever closed by the harness" is reversed by its own successor DEC-203, which moves every recorded card including `source_issues` to Done at ship. A citation sweep is also a staleness sweep; the id going away is what forces someone to read the sentence.
- 2026-08-29 (T-05): the deleted entries left non-id dangling references in prose that no verify grep could see — "Every other row of amendment 4's table" in DEC-138 amendment 8 pointed at a table deleted with DEC-196. Repointed it to the live recorded copy in `.claude/skills/harness/references/github-mirror.md`. After deleting an entry, sweep the survivors for "amendment N" and similar wordless pointers, not just for the id.
- 2026-08-29 (T-05): DECISIONS.md seams are not uniform — only ~50 of ~200 inter-entry boundaries carry a `---`. Cutting exactly heading-through-(next-heading minus 1) reproduces whichever local shape existed, because the trailing rule and blank belong to the deleted span. Checking the seam shape per boundary beat assuming the plan's "horizontal rule before the next heading" was universal (DEC-103/104 had none).
- 2026-08-29 (T-07): the dispatch's own verify block is the only complete citation inventory. I built my
  edit list from a per-id grep and still missed one site (`DEC-67` in DEC-110's narrative, "though DEC-67
  gives doers that authority") because I read that context via `bash sed` output and never folded it into
  the edit plan. The verify caught it on the first run. Lesson: derive the edit list FROM the grep output
  mechanically, not from a hand-transcribed reading of it.
- 2026-08-29 (T-07): a plan-supplied successor can carry the *title relationship* and not the *claim*.
  DEC-86 is titled "CORRECTS DEC-67" but only corrects the roster count; DEC-67's applier split is not in
  it. DEC-120 "supersedes DEC-102's conclusion" but does not carry DEC-102's tool-withholding finding.
  Both had to be repointed elsewhere or stated bare. Check the successor's BODY, never its title suffix.
- 2026-08-29 (T-07): `edit` line numbers from a `write` response do not exist — `write` returns no
  numbering. I followed a fresh `write` with an `edit` using line numbers I had counted by eye and
  corrupted four lines of my own receipt. Always `read` a file I just wrote before hunking it.
- 2026-08-29 (T-07): all 8 cut seams in DECISIONS.md's 19-102 range are single-blank-line; the `---`
  seam shape T-05 measured at ~50/200 boundaries appears near DEC-120 and DEC-122 but nowhere in this
  range. Heading-through-(next-heading − 1) reproduced the local shape with no special case, as predicted.
- 2026-08-29: the Edit tool refused every hunk on .harness/harness/docs/DECISIONS.md — read returned tag #3C90 while edit insisted the file hashed to #7669 and echoed the file with line numbers offset by -10; the file was clean in git and re-reading returned the same tag, so the loop was unbreakable. Worked around it with a /tmp content-anchored span-replacement script (start anchor, end anchor, replacement file, uniqueness asserted on both anchors), which turned out to be the better tool anyway for 28 folds across a 6.7k-line file: no line numbers to go stale between edits.
- 2026-08-29: folding amendments orphans the citations that named them. Six sites in DECISIONS.md cited 'DEC-138 amendment 6/7/8', 'DEC-171 am.1' or a DECISIONS.md:NNNN-NNNN line anchor into a block being deleted; one of them quoted a sentence verbatim from the amendment, so the fold had to keep that sentence in the owner's voice. Grep for the owner id plus the word 'amendment' AFTER folding, not before.
- 2026-08-29 (T-09): the Edit tool defect from T-08 recurred on DECISIONS.md — read returned #34D3 with DEC-181 at 4776, edit claimed #7669 and echoed a DIFFERENT decision's body at those numbers, while md5sum/wc -l were unchanged across the rejection. Same content-anchored python substitution worked first try. Treat Edit as unusable on this file for the rest of the feature.
- 2026-08-29 (T-09): the same Edit tool then landed a hunk in my own receipt at an off-by-one offset, silently eating the three lines after my range. On a file just written by `write`, re-read before editing; do not trust the tag from the write response.
- 2026-08-29 (T-09): T-09's verify block contained a clause that could never fire — `grep -q "feature.yaml 200/20"` against a file that spelled it with backticks. Green at baseline, green after. When a verify clause is the only evidence for a criterion, print the matching line, do not trust the exit code.
- 2026-08-29 (T-09): folding a partial strike orphans the OTHER entry that announced the strike. DEC-188:5111 still says "DEC-181 is struck in part", now false, and it sits in a body T-09 was forbidden to touch. A fold task should carry the announcing entry in its file scope, or the falsehood ships.
- 2026-08-29: folding a supersession marker at DEC-180 was a DELETION, not a rewrite: the marker's body was byte-identical to a sentence already in the entry's own paragraph. Checked for duplication before writing the recommended clause; writing it would have made a third copy.
- 2026-08-29: DECISIONS.md was already ' M' at spawn and carried a sibling's hunk at old :5111; bounding my own change with git diff -U0 hunk headers was what kept the receipt honest about which hunks are mine.
- 2026-08-29 (FEAT-38 T-16): the edit tool rejected a worktree-RELATIVE path by resolving it against the main checkout and rendering main's text at my line numbers ("hash not from this session"); re-issuing with the path exactly as the read header printed it succeeded. The rejection is the safe outcome - a matching stale tag would have written into main silently.
- 2026-08-29 (FEAT-38 T-16): a plan-stated defect count (three rotted anchors, measured at base_sha) read FOUR at HEAD, because a later task in the same feature added an entry that re-CITED two of the rotted anchors as its own evidence. A checker's justification paragraph is itself subject to the check it justifies - grep the checker's own entry before trusting the count.
- 2026-08-29 (FEAT-38 T-16): when a cited fact's file was renamed away, the fact often survives in a DIFFERENT file, not a different line of the same one: FEAT-03's B-13 raise moved from feature.yaml pending: to STATE.md Open Questions. Grep the fact's distinctive words repo-wide before concluding it has no home.
- 2026-08-29 (T-21): grep cannot express `<= N` portably — BSD grep lacks -P, and its `-z -E '^([^\\n]*\\n){0,80}$'` form returned 1 for a 6279-line file (false GREEN). Capped exact-count form `grep -c -m 81 -e '' FILE :: 12` is the portable substitute: output is bounded 0..81 so a short expected number cannot prefix-match a larger over-budget count.
- 2026-08-29 (T-21): check-decision-claims.py run_claim ignores result.returncode despite its docstring claiming a nonzero exit fails the claim; raised as an open_question, not worked around.
- 2026-08-29 (T-11): gen-decisions-index.py refuses to write at all while ANY orphan row survives (build_index returns None, main exits 1), so "regenerate the index" after entry deletions is a two-step job: hand-delete the orphan rows first, THEN run the generator. Reading the generator before editing saved a wrong-shaped attempt.
- 2026-08-29 (T-11): the verify's deleted-id sweep found DEC-186 surviving in DEC-200's hand-written RULING, not in a generated refs graph. Generated fields were already clean. Lesson: when a sweep for a deleted id fires on a partly-generated file, check which SIDE of the ` :: ` separator the hit is on before calling it a generator defect.
- 2026-08-29 (T-11): a newly appended entry (DEC-205) regenerated as a "RULING PENDING" row and failed the budget test, which the dispatch did not predict. Whoever regenerates an index after other agents appended entries inherits their unwritten rulings.
