# Observations - harness-orchestrator

- 2026-08-24: FEAT-38 — a grilling artifact stated "all measured at 513c4a4" but its headline figure (DECISIONS.md 6,984 lines) matches the FEATURE BASE, not 513c4a4 (6,892). The measurement sha was on a live unmerged branch and not an ancestor of the base. Re-deriving three figures at the base took one Bash call and changed the rewrite set from 22 entries to 18. A stated sha is a claim like any other; check ancestry with `git merge-base --is-ancestor` before passing the figures down.
- 2026-08-24: FEAT-38 — the "22 amended entries" figure was definition-dependent: 18 entries carry an amendment BLOCK, 22 merely MENTION the word. Both are defensible readings of the same prose. When a dispatch hands down a count that will size tasks, re-derive it AND state the predicate, or the squad inherits a 22% oversize with no way to see it.
- 2026-08-24: FEAT-38 — check-domain.sh --resolve returned NOBODY for a file the plan must DELETE (.harness/notes/audit-decisions.py). check-plan-routes.py:210 makes a NOBODY path a VIOLATION under execution_mode: team, so a promote-a-script task splits: create-at-new-path is squad, remove-at-old-path is main-session-direct. Resolve the OLD path as well as the new one whenever a task moves a file.
- 2026-08-24: FEAT-38 — the worktree base was 2 commits behind main and the gap contained a 138-file RENAME (the OMP port, issue 589, moved the whole skills tree to a new top-level dir). `git diff --stat` restricted to my four lane files showed NOTHING, because a rename is invisible to a path-restricted stat when you name only the OLD paths. "Behind by N commits" is not enough: run `git diff --name-status base..main` piped through a uniq count of the status column and look for R-codes before letting anyone plan file paths.
- 2026-08-24: FEAT-38 — the hooks governing my subagents resolve through CLAUDE_PROJECT_DIR (the MAIN checkout), not through my worktree. The enforcement layer can therefore be a DIFFERENT VERSION from the files I am editing; when main moves mid-run the guard changes under me while my worktree stays pinned.
- 2026-08-24: FEAT-38 — a lead returned BLOCKED claiming the only remedy was the registry's destructive release-all. The CLI actually offers a targeted per-agent release. A lead has no Bash and so reasons from skill prose; verify any "no safe remedy exists" claim against the tool's own usage string before carrying it upward as fact.
- 2026-08-24: FEAT-38 — bash-write-guard scans the whole command STRING, so quoting a path inside heredoc PROSE tripped a redirect refusal on a tool invocation that wrote nowhere near it. When logging about paths, describe them rather than pasting them.
- 2026-08-29: FEAT-38 — my own 2026-08-24 observation said the OMP port RENAMED the skills tree to a new top-level dir and that every tool path had moved. False. git ls-files -s .agents/skills returns mode 120000: it is a tracked SYMLINK to ../.claude/skills, and the 149 real files never left .claude/skills. A rename and a compat symlink look identical in git diff --name-status R-codes and in a plain ls; only the ls-files mode column tells them apart. I had carried the wrong version into a dispatch brief before re-measuring.
- 2026-08-29: FEAT-38 — the two intake artifacts answered the SAME question in opposite directions two days apart: the 2026-08-24 grilling settled that strike records stay, the 2026-08-26 triage carried an operator ruling deleting seven of the eight struck entries. Neither artifact references the other. When a feature has more than one dated intake artifact, diff their SETTLED lists against each other before dispatching, and name the reversal explicitly — pm cannot see a conflict it was never told exists.
- 2026-08-29: FEAT-38 — a grilling note asserted the feature was entirely squad work with nothing main-session-direct. Re-running check-domain.sh --resolve once per candidate path returned NOBODY for 13 surfaces, including an agent-definition tree (.omp/agents) that no intake artifact had ever seen. A LANES claim in an intake artifact is a snapshot of a resolver whose config moves; re-resolve every path the plan will touch, never inherit the summary.
- 2026-08-29: FEAT-38 — bash-write-guard refused a mktemp-plus-redirect commit-message file even though the target was my own temp path, because it masks the redirect target wholesale. Repeated git commit -m flags carry a multi-paragraph message with no redirect at all and pass cleanly. Reach for repeated -m before reaching for -F.
- 2026-08-29 (FEAT-38, panel revision): a lead's digest quoted the amended verify clause verbatim and it was correct, but I graded the LANDED clause by parsing it out of plan.yaml with `harness_yaml.load_plan` and writing that string to the test script. Quoting and landing are different acts; parsing removes the retype as a failure mode for free.
- 2026-08-29 (FEAT-38, F1): the cheapest way to grade a completion-time verify is `git clone --no-hardlinks` the worktree to /tmp, apply the predecessor tasks with `git rm`/`sed`, DELIBERATELY leave the dependent task unapplied, and run the verify there. A real git index is required — the clause was `git grep` over tracked files, which a plain `cp` cannot model. Signed form exit 1, amended form exit 0, one command each.
- 2026-08-29 (FEAT-38, F1): when a fix SCOPES a check, verify the displaced proof actually lands at its new owner. F1 moved "no sixth reference site exists" from T-24's sweep to SC-14's; applying T-25 in the same sim and running SC-14's unscoped sweep (exit 1, no matches) is what turned that from a plausible reassignment into a measured one.
- 2026-08-29 (FEAT-38): the main session withdrew the signature in plan.yaml only, leaving BRIEF.md's `## Approval` reading `approved` — the two artifacts disagreed for the whole run. Neither is writable by me, so the only correct act was to diff both against the withdrawal commit, prove I wrote neither, and return the discrepancy. Checking BOTH fragments is not implied by being told "the signature was withdrawn".
- 2026-08-29 (FEAT-38): `runs/**` is gitignored (.gitignore:7), so a lead's run dir never appears in `git status` and its absence there is not evidence the lead failed to write it. I nearly read that as a false digest claim; `git check-ignore -v` settled it in one call.
- 2026-08-29 (FEAT-38, F2b): told pm to content-anchor SC-16 on the string "What was considered and refused" and demote `DECISIONS.md:6293-6299` to an as-measured aside, because T-27 and T-28 both SHORTEN that file before `review_sha` is pinned. A line anchor measured at the base commit, inside a feature that edits the same file, is guaranteed stale at grading time.
- 2026-08-29 (FEAT-38): `bash-write-guard.sh` misparsed a multi-line `printf` with backslash continuations piped into a merge tool, reporting `cp targets \, outside your domain`. Writing the entries to a file with the Write tool and passing `--entries <path>` went through unchanged. Backslash continuations are worth avoiding in any guarded command.
