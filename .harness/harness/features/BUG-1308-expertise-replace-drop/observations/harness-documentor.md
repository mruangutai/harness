# Observations - harness-documentor

- 2026-09-05 (BUG-1308 T-04): dispatch said regenerating DECISIONS-INDEX.md was mandatory because appending an entry "shifts every later row source anchor" - false for an EOF append: the index diff was one added line, no earlier @line moved. Regeneration is still mandatory, but because the generator is the only thing that emits the new row and its RULING PENDING sentinel.
- 2026-09-05 (BUG-1308 T-04): the bash-write-guard blocks a shell redirect into a mktemp -d path (reported the target as "xx"); scratch fixtures for a smoke run had to be created with a python3 heredoc writing an explicit /tmp path instead.
- 2026-09-05: BUG-1308 E1. A handed-down `file:line` range measured by another agent is reliable at its start and unreliable at its end — four of five ranges engineering handed me ended one line past the last body line, on the blank separator. Measure the tail, not just the def.
- 2026-09-05: BUG-1308 E1. Classifying a stale citation as cycle-caused vs pre-existing is a two-command check, not a judgement: reading the fix commit's hunk headers gives the moved region, and grepping the def lines out of the integration branch's copy of the same file proves whether the citation was already wrong before the branch. It found `compute_union :111-137` wrong at origin/main, refuting engineering's "still correct".
