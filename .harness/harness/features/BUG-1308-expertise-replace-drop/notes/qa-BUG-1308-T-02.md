# QA — BUG-1308 T-02 (regression cases case11..case20)

**Verdict: PASS.** All three named acceptance runs are green; `tests/integration/test-expertise-merge.py`
is the only production/test file touched; case1..case10 are byte-identical (confirmed via
`git diff | grep -E '^-[^-]'` → zero removed lines).

## What was added

Ten case functions (`case_replace_at_capacity` .. `case_malformed_ops_cli`), registered in `main()`
after `case_stale_lock_recovery(root)`, plus shared helpers `write_ops`, `run_ops`,
`parse_section_entries`, and the module-level `contract_drift(skill_text)` (case17's mechanised
detector) with its support functions `_normalise`, `_harvest_contract_verbs`, `_harvest_help_verbs`,
`_probe_accepted_verbs`, and `_ContractHarvestError`.

## case17 harvest anchor (as required)

Anchor pattern (applied to NORMALISED text — whitespace collapsed, backtick/asterisk/underscore
stripped): `r"op: \w+ # (\w+(?: \| \w+)+)"`. Evidence it matches **exactly once** against the real
`.claude/skills/harness-distill/SKILL.md`: grepped the raw file for `op:` and `#.*\|` beforehand and
found only line 118 (`- op: replace              # add | replace | merge | drop`) carries both an
`op:` key and a following `#`-pipe-list; line 126's `` `op: merge` `` has no `#` so the anchor cannot
match it. The suite's own check `case17: the op: vocabulary anchor matches the real SKILL.md exactly
once` passed (`re.findall` returned exactly 1 match). Second, independent source (`ACCEPTED`) is
harvested from the real `ops --help` output via `r"list of ([\w/]+) ops"`, confirmed live:
`--ops OPS  path to a JSON list of add/replace/drop ops, or - for stdin`.

Mutation demonstration built three throwaway copies (never the real file) and asserted, per copy,
which of the two failure strings (`CONTRACT - ACCEPTED ==...` / `ACCEPTED - CONTRACT ==...`) is
present: (a) phrase-removed → first direction only; (b) extra verb `prune` inserted → first
direction only; (c) verb `drop` removed from the vocabulary line → second direction only. All three
assertions passed on the first run without needing a fix (one unrelated construction-sanity check —
whether copy (c) actually mutated the text — initially failed because the phrase `| drop` also
occurs, unrelated, in SKILL.md's line 112 prose; fixed by scoping that one assertion to the full
`# add | replace | merge | drop` vocabulary substring instead of the bare fragment. This did not
touch any load-bearing `contract_drift` assertion, only the fixture-construction self-check).

## case18 concurrency evidence

Observed wall clock for the hold window: **2.02s** and **2.04s** across the two runs performed during
this session (both ≥ the 2.0s target, confirming the full window elapsed) — `poll()` on both children
never returned non-`None` during the hold, i.e. **both children were still running at the end of the
2.0s hold window** (check: "case18: neither child exits during the 2.0s hold window ... (2.0Xs
observed)" — PASS both times). After the lock was released, both children exited 0 within the 20s
`communicate()` timeout, the Patterns id census matched exactly the original eight plus P-09/P-10,
and the P-07 entry carried child B's marker text.

## Acceptance runs — verbatim tails

**1. T-02's verify block** (grep presence check + full suite), from the worktree root:
```
GREP OK
... [104 PASS lines] ...
PASS test-expertise-merge.py
EXIT=0
```
Full suite: 104 checks, 0 failures, exit 0.

**2. T-01's unit suite, unmodified** — `python3 tests/unit/test-expertise-ops.py`:
```
... [59 PASS lines, u1..u16] ...
PASS test-expertise-ops.py
UNIT_EXIT=0
```
59 checks, 0 failures, exit 0 — confirms T-02 did not disturb T-01's suite.

**3. `git status --porcelain`**:
```
 M .harness/harness/features/BUG-1308-expertise-replace-drop/feature.json
 M .harness/harness/features/BUG-1308-expertise-replace-drop/plan.yaml
 M tests/integration/test-expertise-merge.py
```
`feature.json` and `plan.yaml` are pre-existing harness control-plane state, not mine and not a
production/test file — `tests/integration/test-expertise-merge.py` is the only modified
production/test file, satisfying the acceptance clause and case18's no-bypass rule.

## Open questions

None load-bearing. One process note, not a plan defect: my first attempt at this task edited the
STALE main-checkout copy at the identical relative path (`tests/integration/test-expertise-merge.py`)
because the edit tool's relative-path resolution did not anchor to the worktree by default. I caught
it via `git status --porcelain` in the main checkout, reverted that accidental edit with
`git checkout -- tests/integration/test-expertise-merge.py`, and redid every edit against the
absolute worktree path as the dispatch instructed. No harm done — the main checkout was restored to
its pre-existing state before the worktree edits landed — but it is worth restating in the runbook
for the next T-02-shaped dispatch: **always pass the full absolute worktree path to `edit`/`read`,
never the bare relative path**, even mid-session after the first correct read, since the tool does
not remember a prior session's cwd across separate `edit` calls.
