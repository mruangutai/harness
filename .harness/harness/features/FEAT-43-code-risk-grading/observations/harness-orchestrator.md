# Observations - harness-orchestrator

- 2026-08-27: FEAT-43 dispatch told pm that DEC-174 am.4 makes the checker LIBRARY squad work and the gate cutover main-session-direct. It missed the same amendment's other half: its heading and closing paragraph (DECISIONS.md:4983 and :5006) put `check-plan-routes.py` AND `test-check-plan-routes.py` INSIDE the enumerated enforcement layer. So in-scope defect 1 (check-plan-routes.py reads the wrong config) CANNOT be squad work at all — it is main-session-direct, and so is its test. Check the returned plan.yaml's execution_mode on that task before accepting.
- 2026-08-27: that same enumeration is an argument pm should weigh on the "one task or two" call for the two checker defects: they now differ in execution_mode, not just in file.
- 2026-08-27: SendMessage is DISABLED for the orchestrator session (tools are Read/Glob/Grep/Agent/Write/Bash). There is no mid-flight correction channel to a running lead — a fact found only after mis-firing an Agent call that spawned a second product-lead on a PLACEHOLDER prompt. It returned BLOCKED, touched no files. Corrections must wait for the return and cost a fix cycle, so front-load everything into the dispatch.
- 2026-08-27: VERIFIED defect 2 myself at 6e4a273, so the plan's task for it rests on measurement not on the ticket: `grep -rn 'qa_gate|advisory_unless_high|blocking_when_uat_criteria_exist' --include=*.py --include=*.sh` over the whole worktree returns ZERO hits. Every hit for the bare word `gates` is the English verb inside a comment or docstring. Nothing in code reads the gates block.
- 2026-08-27: defect 1 needs its wording pinned, not inherited. check-plan-routes.py:498 and check-domain.sh:153 look STRUCTURALLY SIMILAR (both try HARNESS_PROJECT_DIR/CLAUDE_PROJECT_DIR then fall back to a derived root), and check-plan-routes.py:508 already prints "IGNORING it and using {derived}". So the divergence is subtler than "checks the branch config instead of the hook's" and the plan's task must state the EXACT divergence and cite lines, or the fix will chase a premise that no longer holds at HEAD. The operator's OK-then-denied measurement stands as a symptom; the mechanism is what needs pinning.
- 2026-08-27: operator added a BRIEF requirement mid-run at 6e4a273 — a plain-English "Definition of Done" section about whether the GRADING is working, not whether code passed. Guards the FEAT-37 shape where one agent wrote both grader and labels. Fold into ONE consolidated revision with the DEC-174 execution_mode finding; do not spend two cycles.
- 2026-08-27: BLOCKING PLAN FINDING, measured at 6e4a273. The feature's central deliverable may be unwritable by any squad member. Grepping every `path:` in .harness/team-config.yaml: the ONLY skill-related domain is `.claude/skills/harness/bin/**` (harness-backend-dev L170, harness-dev-ops L211) — that is Python source inside the `harness` skill, NOT skill markdown. The flat skill layout `.claude/skills/harness-*/` exists (harness-brief, harness-code-review, ... confirmed by ls) and NO domain entry covers it. So a new `.claude/skills/harness-<name>/SKILL.md` is deniable to every agent in the org.
- 2026-08-27: same sweep — NO domain entry covers `.claude/agents/**` either, so wiring the new skill into the five dev specialists' `skills:` frontmatter is also unwritable by a squad member. And NO entry covers `.harness/team-config.yaml`, so the domain cannot be widened by a squad member either. `harness-dev-ops` DOES own `.harness/harness.json` (L210), so the gates-block work IS squad-writable.
- 2026-08-27: consequence — either (a) skill authoring + agent wiring are main-session-direct, which puts the feature's most important artifact in the operator's own hands, or (b) team-config.yaml is amended to grant a skills domain, which is a PERMANENT authority change made for one feature and is itself main-session-direct. Costs on both sides; this is an operator call, not pm's and not mine. Carry it as a blocking open_question.
- 2026-08-27: PROBED the domain gate's default posture rather than inferring it, because the whole skill-unwritable finding turns on it. Attempted Write to `.claude/skills/harness-domain-probe/SKILL.md`; check-domain.sh BLOCKED it and printed "Permitted for you:" followed by an explicit six-path list. So the gate is an ALLOWLIST, deny-by-default — an unclaimed path is denied, not allowed. Probe created nothing (blocked pre-write, directory absent afterwards, `git status --porcelain` shows only my own feature dir). Combined with the team-config sweep, the finding stands: a path no domain names is writable by nobody.
- 2026-08-27: there is only ONE skill tree, not two, and this kills a plausible-but-wrong task before it gets written. `.agents/skills` is a git-TRACKED SYMLINK (`git ls-files -s` reports mode 120000, target `../.claude/skills`), so `.agents/skills/X` and `.claude/skills/X` are the same directory — same inode, link count 1. A new skill is authored ONCE under `.claude/skills/harness-<name>/` and appears at the `.agents/` path for free. Any plan task that says "mirror the skill into both trees" is busywork and must be struck.
- 2026-08-27: option framing for the write-authority question, so the operator gets a choice and not just a problem. Natural domain owners already hold the right tools: `harness-documentor` has Edit+Write and exists to author prose addressed to a reader, which is what a skill is; `harness-dev-ops` has Edit+Write and ALREADY owns `.claude/skills/harness/bin/**` and `.harness/harness.json`, and its remit names scaffolding, so the frontmatter wiring is its natural shape. So option (b) is concrete: grant documentor `.claude/skills/harness-*/**` and dev-ops `.claude/agents/harness-*.md`. Option (a) leaves both with the main session and costs the operator the hand-authoring of the feature's most important artifact.
- 2026-08-27: the product-lead INDEPENDENTLY reached two of my four findings while pm was in flight, and wrote them as pre-return send-back criteria (addendum 1: check-plan-routes.py is a gate not a library, so DEC-174 am.4's library carve-out does not reach it; addendum 2: a skill is not delivered until it is wired into agents' `skills:` frontmatter, and the before-the-gate ordering applies to the WIRING not the authoring). Separate measurements, so this is corroboration rather than an echo. It also hit the same disabled-SendMessage wall and converted its correction into an assessment criterion — the right adaptation, worth reusing.
- 2026-08-27: BRIEF.md defect to check on return — SC-19 is stranded at lines 249-252, BELOW `## Approval` (245) and its `status: pending` (247). SC-01..SC-18 sit properly in `## Success Criteria` (102-193). Content below the signature line is content the operator misses while signing, and an SC outside the SC section is one the goal-check's enumeration can skip. Small and mechanical but real; pm's file to fix, never mine — BRIEF.md is approval-gated and outside my domain.
- 2026-08-27: pm independently reached the write-authority finding AND used a better method than my Write-probe: `check-domain.sh --resolve` reports `NOBODY` for a path no domain claims. BRIEF.md:230-231 records it. Learn the tool — a resolver query beats probing the gate with a throwaway write, costs no file, and names the owner directly.
- 2026-08-27: ANOMALY, raise as an open_question, do not assert a cause. The SubagentStop hook refused my return naming `harness-validator-lead started 2026-08-28T00:14:29Z` (17:14:29 local) as a child in flight. I NEVER DISPATCHED A VALIDATOR-LEAD — my only Agent calls this run were two harness-product-lead spawns. Measured context: BRIEF.md was written 17:13:47, so the validator-lead appeared ~40s after it; no validator run dir exists anywhere (`find` over every worktree for run dirs touched in 20 min returns nothing); my plan-product state.yaml still reads `status: running`, step `pending`. The same hook's FIRST refusal named harness-product-lead and this one does NOT, which suggests the child list is live rather than cumulative.
- 2026-08-27: two candidate explanations for that anomaly and I have evidence for NEITHER, so both stay hypotheses. (a) harness-product-lead dispatched a validator-lead, which DEC-118 forbids (a lead may not dispatch another squad) and dispatch-guard.sh is supposed to REFUSE — that would be a guard gap, a harness defect. (b) the hook attributes a child from another session or another in-flight feature (FEAT-37, FEAT-38, FEAT-39, FEAT-41 all have live worktrees) to whichever agent is returning — that would be a hook-scoping defect. Distinguishing them needs the dispatch record, which I cannot read from here. Report the measurement, name both, claim neither.
- 2026-08-27: I nearly reported a CRITICAL FALSE FINDING and the check overturned it — record it as a failure, not a near-miss. T-05 lists only `.omp/agents/harness-*.md` in `files:`, and `.claude/agents/harness-backend-dev.md` (inode 172568638) vs `.omp/agents/harness-backend-dev.md` (172570399) are DIFFERENT FILES with neither dir a symlink, while CLAUDE.md says definitions are discovered from `.claude/agents/`. That reads like "T-05 edits a tree nothing loads, so the skill silently never reaches the five specialists". It is WRONG: T-05's intent names `.omp/agents` as canonical and regenerates the adapters via `sync-agent-adapters.py --apply`, with `--check` as its verify. Lesson: read the task's `intent` and `verify` before judging its `files` list — a files list is not the whole task.
- 2026-08-27: and I checked that verify can actually FAIL rather than trusting its exit 0. `sync-agent-adapters.py:261` compares each generated adapter's FULL TEXT to what .omp would produce (`target.read_text() != content`) and :271-273 returns 1 on any drift. A changed `skills:` list changes generated content, so the assertion cannot pass vacuously. Note I could NOT probe this by writing drift — bash-write-guard blocked `cp` to the scratchpad AND `.claude/agents/**` resolves to NOBODY for me too — so reading the comparison logic was the only route. Guard behaved correctly.
- 2026-08-27: plan.yaml assessment at 17:20 — routing is CORRECT on every enforcement-layer surface. T-04 skill, T-05 wiring, T-08 cutover, T-09 check-plan-routes all `main-session-direct`; T-01/02/03 library, T-07 gate_policy.py, T-10 vocabulary all `team`. T-07 is right as team because gate_policy.py has NO consumer until T-08 — a module a gate imports is not a gate. T-08 depends_on [T-03,T-05,T-06,T-07], so the skill is authored AND wired before anything gates: the ordering requirement is structural, not a note. No `.agents/` mirror busywork. approval.status pending.
- 2026-08-27: `check-plan-routes.py` on this plan prints `DEVIATION T-09 ... granted to harness-backend-dev, harness-dev-ops but declared main-session-direct`, 0 violations. That DEVIATION is DESIRABLE, not a defect — the domain grants the files but DEC-174 forbids the squad executing them, so the plan chose the more conservative mode and the checker noticed. Do not "fix" a deviation of that direction.
- 2026-08-27: MUST CORRECT ON RETURN — the product-lead's assessment-cycle-1.md:29-36 asserts "`.agents/` does not exist in this worktree OR in the owner checkout... All dead paths", naming harness-expertise, harness-team, my dispatch and FEAT-40's verify: as broken. THAT IS FALSE. Measured: `.agents/` exists here (dir, 17:01) and in the owner checkout (Aug 25); `.agents/skills` is a symlink to `../.claude/skills` in BOTH; `.agents/skills/harness/bin/observations-merge.py` is a real 7987-byte executable I have invoked ~8 times this run, and `--help` RUNS FINE. The paths are live.
- 2026-08-27: and the MECHANISM of that false finding is reusable, which is the valuable part. A symlinked directory is INVISIBLE to symlink-blind search but fully live for direct access: `find .agents -name observations-merge.py` returns 0, `find -L .agents -name observations-merge.py` returns 1, and `python3 .agents/.../observations-merge.py` runs. Glob-style tools typically do not follow symlinks. So "the tree does not exist" from a search tool is NOT evidence of absence — confirm with a direct `ls -ld`/`readlink`/execute before declaring any path dead. A repo-wide path rewrite on that false premise would have been real damage.
