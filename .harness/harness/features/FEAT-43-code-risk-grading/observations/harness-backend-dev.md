# Observations - harness-backend-dev

- 2026-08-29: FEAT-43 c22 — code-grade.py's cognitive-complexity bar (3 for test files) rejected a single-function guard doing discover+parse+compare+report (cognitive 38); splitting into 5 small helpers (expected-set, path-discovery, line-parse, per-file-report, orchestrator) got every qualname to grade 3-5 with no behavior change. Worth defaulting new guards in this file to that shape from the start rather than writing one big function and splitting after code-grade.py fails it.
- 2026-08-29: FEAT-43 C25 T-01 self-bar closure. Cognitive-driven grade-2 functions in code_grade.py (`_body_hashes.collect` cyc9/cog18/abc17.3, `gated_set` cyc8/cog25/abc24.9) both reached grade 4+ by extracting named module-level helpers (`_qualname`, `_strip_docstring`, `_hash_body`, `_resolve_base_source`, `_resolve_pre_image`, `_gate_file_records`) — flattening nesting into short dispatch/orchestration functions dropped both cyclomatic and cognitive together, per the sonar-style nesting penalty. All 6 new helpers measured grade 4-5 individually; no allowlist replacement was added, per the no-exemption ruling. Mutation-tested the resolution order (swapped hash-lookup ahead of qualname-lookup in `_resolve_pre_image`) and confirmed the new `check_pre_image_resolution_priority` test named the exact break, then restored byte-identically (md5 verified). Noticed HEAD advanced from the pinned d2e3b5eb to ea61b5e via two `chore:` commits from a concurrent process mid-task, not from any git action I ran — d2e3b5eb remained an ancestor throughout.
