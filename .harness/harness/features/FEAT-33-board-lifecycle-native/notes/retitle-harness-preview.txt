board_lifecycle: DRY-RUN would rename #777: 'T-22 — Widen INV-26 to accept a done task whose sub-issue is deliberately still open' -> 'FEAT-33-board-lifecycle-native — T-22 — Widen INV-26 to accept a done task whose sub-issue is deliberately still open'
board_lifecycle: DRY-RUN would rename #776: "T-21 — Correct the scaffold template's five-key station instruction" -> "FEAT-33-board-lifecycle-native — T-21 — Correct the scaffold template's five-key station instruction"
board_lifecycle: DRY-RUN would rename #775: "T-20 — Record on board 2 the promotion signal that Ready's one meaning removes" -> "FEAT-33-board-lifecycle-native — T-20 — Record on board 2 the promotion signal that Ready's one meaning removes"
board_lifecycle: DRY-RUN would rename #774: 'T-19 — Record the event-driven station-writer map as a decision amendment' -> 'FEAT-33-board-lifecycle-native — T-19 — Record the event-driven station-writer map as a decision amendment'
board_lifecycle: DRY-RUN would rename #773: 'T-18 — Run the title backfill over the harness board and capture the report' -> 'FEAT-33-board-lifecycle-native — T-18 — Run the title backfill over the harness board and capture the report'
board_lifecycle: DRY-RUN would rename #772: "T-17 — board_lifecycle.py retitle - the backfill, deriving each id from the ticket's own milestone" -> "FEAT-33-board-lifecycle-native — T-17 — board_lifecycle.py retitle - the backfill, deriving each id from the ticket's own milestone"
board_lifecycle: DRY-RUN would rename #771: 'T-16 — A task ticket the harness creates names its feature in its title' -> 'FEAT-33-board-lifecycle-native — T-16 — A task ticket the harness creates names its feature in its title'
board_lifecycle: DRY-RUN would rename #770: 'T-15 — The audit reports a recorded status that disagrees with its parent card' -> 'FEAT-33-board-lifecycle-native — T-15 — The audit reports a recorded status that disagrees with its parent card'
board_lifecycle: DRY-RUN would rename #769: 'T-14 — Name the station writer at every door, and give the main session its own cards' -> 'FEAT-33-board-lifecycle-native — T-14 — Name the station writer at every door, and give the main session its own cards'
board_lifecycle: DRY-RUN would rename #768: "T-13 — Couple the feature status record to the parent card's station, and expose it as a subcommand" -> "FEAT-33-board-lifecycle-native — T-13 — Couple the feature status record to the parent card's station, and expose it as a subcommand"
board_lifecycle: DRY-RUN would rename #767: 'T-12 — Reconcile the kaya-ai project from the harness checkout and capture the report' -> 'FEAT-33-board-lifecycle-native — T-12 — Reconcile the kaya-ai project from the harness checkout and capture the report'
board_lifecycle: DRY-RUN would rename #766: 'T-11 — Reconcile the harness project and capture the zero-finding report' -> 'FEAT-33-board-lifecycle-native — T-11 — Reconcile the harness project and capture the zero-finding report'
board_lifecycle: DRY-RUN would rename #765: 'T-10 — Wire board provisioning and the workflow report into harness-init' -> 'FEAT-33-board-lifecycle-native — T-10 — Wire board provisioning and the workflow report into harness-init'
board_lifecycle: DRY-RUN would rename #764: 'T-09 — Record the plan-station declaration as a DEC-196 amendment' -> 'FEAT-33-board-lifecycle-native — T-09 — Record the plan-station declaration as a DEC-196 amendment'
board_lifecycle: DRY-RUN would rename #763: 'T-08 — Close with an explicit reason and label an abandoned ticket' -> 'FEAT-33-board-lifecycle-native — T-08 — Close with an explicit reason and label an abandoned ticket'
board_lifecycle: DRY-RUN would rename #762: 'T-07 — Guard start-task against driving a closed card backwards' -> 'FEAT-33-board-lifecycle-native — T-07 — Guard start-task against driving a closed card backwards'
board_lifecycle: DRY-RUN would rename #761: 'T-06 — board_lifecycle.py reconcile - move the drifted cards and set the missing reasons' -> 'FEAT-33-board-lifecycle-native — T-06 — board_lifecycle.py reconcile - move the drifted cards and set the missing reasons'
board_lifecycle: DRY-RUN would rename #760: 'T-05 — board_lifecycle.py audit - the finite finding list, including workflow state' -> 'FEAT-33-board-lifecycle-native — T-05 — board_lifecycle.py audit - the finite finding list, including workflow state'
board_lifecycle: DRY-RUN would rename #759: 'T-04 — board_lifecycle.py provision - create the board, the Status field and the missing options' -> 'FEAT-33-board-lifecycle-native — T-04 — board_lifecycle.py provision - create the board, the Status field and the missing options'
board_lifecycle: DRY-RUN would rename #758: 'T-03 — Add the GraphQL primitives for board creation, field options and workflow state' -> 'FEAT-33-board-lifecycle-native — T-03 — Add the GraphQL primitives for board creation, field options and workflow state'
board_lifecycle: DRY-RUN would rename #757: 'T-02 — Widen the required station key set to six and declare plan on the harness board' -> 'FEAT-33-board-lifecycle-native — T-02 — Widen the required station key set to six and declare plan on the harness board'
board_lifecycle: DRY-RUN would rename #756: "T-01 — Declare the plan station in kaya-ai's own harness.json on master" -> "FEAT-33-board-lifecycle-native — T-01 — Declare the plan station in kaya-ai's own harness.json on master"
board_lifecycle: DRY-RUN would rename #740: 'T-08 — Record the decision that the mirror now reads one fact back into execution state' -> 'FEAT-26-pr-linkage-recorded — T-08 — Record the decision that the mirror now reads one fact back into execution state'
board_lifecycle: DRY-RUN would rename #739: 'T-07 — Record the new subcommands and the source_issues key where an author will look for them' -> 'FEAT-26-pr-linkage-recorded — T-07 — Record the new subcommands and the source_issues key where an author will look for them'
board_lifecycle: DRY-RUN would rename #738: 'T-06 — Backfill the eleven shipped features whose pull request number was never recorded' -> 'FEAT-26-pr-linkage-recorded — T-06 — Backfill the eleven shipped features whose pull request number was never recorded'
board_lifecycle: DRY-RUN would rename #737: 'T-05 — Add INV-28, the invariant that a shipped feature with no recorded pull request is visible' -> 'FEAT-26-pr-linkage-recorded — T-05 — Add INV-28, the invariant that a shipped feature with no recorded pull request is visible'
board_lifecycle: DRY-RUN would rename #736: 'T-04 — Render the closing keywords from the recorded source tickets' -> 'FEAT-26-pr-linkage-recorded — T-04 — Render the closing keywords from the recorded source tickets'
board_lifecycle: DRY-RUN would rename #735: 'T-03 — Derive and record the pull request number from the recorded branch' -> 'FEAT-26-pr-linkage-recorded — T-03 — Derive and record the pull request number from the recorded branch'
board_lifecycle: DRY-RUN would rename #734: 'T-02 — Mirror plan.yaml source_issues into feature.json, threaded so no later save erases it' -> 'FEAT-26-pr-linkage-recorded — T-02 — Mirror plan.yaml source_issues into feature.json, threaded so no later save erases it'
board_lifecycle: DRY-RUN would rename #733: 'T-01 — Declare source_issues in the feature schema, with fixtures that pass and fixtures that fail' -> 'FEAT-26-pr-linkage-recorded — T-01 — Declare source_issues in the feature schema, with fixtures that pass and fixtures that fail'
board_lifecycle: DRY-RUN would rename #717: 'T-17 — Propose the DEC-174 amendment 4 wording that adds dispatch-guard.sh to the enumeration' -> 'FEAT-32-concurrent-write-merge — T-17 — Propose the DEC-174 amendment 4 wording that adds dispatch-guard.sh to the enumeration'
board_lifecycle: DRY-RUN would rename #716: "T-16 — Fix validate-digest.py's stale self-citation of its own stop_hook_active line" -> "FEAT-32-concurrent-write-merge — T-16 — Fix validate-digest.py's stale self-citation of its own stop_hook_active line"
board_lifecycle: DRY-RUN would rename #715: 'T-15 — Make the three artifacts that name a plan signer say the same thing' -> 'FEAT-32-concurrent-write-merge — T-15 — Make the three artifacts that name a plan signer say the same thing'
board_lifecycle: DRY-RUN would rename #714: 'T-14 — Make the approval exclusions real by having check-domain.sh READ main_session.writes' -> 'FEAT-32-concurrent-write-merge — T-14 — Make the approval exclusions real by having check-domain.sh READ main_session.writes'
board_lifecycle: DRY-RUN would rename #713: 'T-13 — Record the decisions and regenerate the index' -> 'FEAT-32-concurrent-write-merge — T-13 — Record the decisions and regenerate the index'
board_lifecycle: DRY-RUN would rename #712: 'T-12 — Make the documented write route the merge route in the rules the writers actually read' -> 'FEAT-32-concurrent-write-merge — T-12 — Make the documented write route the merge route in the rules the writers actually read'
board_lifecycle: DRY-RUN would rename #711: 'T-11 — Keep the claim registry out of git' -> 'FEAT-32-concurrent-write-merge — T-11 — Keep the claim registry out of git'
board_lifecycle: DRY-RUN would rename #710: 'T-10 — Register the new test files with the integration kind and close two exposures in it' -> 'FEAT-32-concurrent-write-merge — T-10 — Register the new test files with the integration kind and close two exposures in it'
board_lifecycle: DRY-RUN would rename #709: "T-09 — On SubagentStop, release the returning agent's claim and refuse a return made with children in flight" -> "FEAT-32-concurrent-write-merge — T-09 — On SubagentStop, release the returning agent's claim and refuse a return made with children in flight"
board_lifecycle: DRY-RUN would rename #708: 'T-08 — Cut dispatch-guard.sh over to refuse a second concurrent single-flight dispatch' -> 'FEAT-32-concurrent-write-merge — T-08 — Cut dispatch-guard.sh over to refuse a second concurrent single-flight dispatch'
board_lifecycle: DRY-RUN would rename #707: "T-07 — Capture dispatch-guard.sh's current refusal set as a test, before it changes" -> "FEAT-32-concurrent-write-merge — T-07 — Capture dispatch-guard.sh's current refusal set as a test, before it changes"
board_lifecycle: DRY-RUN would rename #706: 'T-06 — Build inflight_registry.py, the single-flight claim store, and its test' -> 'FEAT-32-concurrent-write-merge — T-06 — Build inflight_registry.py, the single-flight claim store, and its test'
board_lifecycle: DRY-RUN would rename #705: 'T-05 — Rewire expertise-merge.py onto the shared core so there is one lock dialect' -> 'FEAT-32-concurrent-write-merge — T-05 — Rewire expertise-merge.py onto the shared core so there is one lock dialect'
board_lifecycle: DRY-RUN would rename #704: 'T-04 — Build observations-merge.py so two contexts of one agent both keep their bullets' -> 'FEAT-32-concurrent-write-merge — T-04 — Build observations-merge.py so two contexts of one agent both keep their bullets'
board_lifecycle: DRY-RUN would rename #703: 'T-03 — Build plan-merge.py so a second plan writer adds tasks and never deletes them' -> 'FEAT-32-concurrent-write-merge — T-03 — Build plan-merge.py so a second plan writer adds tasks and never deletes them'
board_lifecycle: DRY-RUN would rename #702: 'T-02 — Build harness_merge.py, the one locked read-modify-write core, and its test' -> 'FEAT-32-concurrent-write-merge — T-02 — Build harness_merge.py, the one locked read-modify-write core, and its test'
board_lifecycle: DRY-RUN would rename #701: 'T-01 — Measure the one unknown hook payload key — the name of the DISPATCHED persona' -> 'FEAT-32-concurrent-write-merge — T-01 — Measure the one unknown hook payload key — the name of the DISPATCHED persona'
board_lifecycle: DRY-RUN would rename #672: 'T-19 — SC-09 — DEC-159 is corrected in place so it covers the genuinely mid-flight phase and stops claiming the watchdog is only a post-hoc audit' -> 'FEAT-31-orchestrator-context-watch — T-19 — SC-09 — DEC-159 is corrected in place so it covers the genuinely mid-flight phase and stops claiming the watchdog is only a post-hoc audit'
board_lifecycle: DRY-RUN would rename #662: "T-18 — Keep test_kinds honest for the new hook test — one path appended to test_kinds.integration.detect so the qa matrix and T-12's cross-check both see it as integration" -> "FEAT-31-orchestrator-context-watch — T-18 — Keep test_kinds honest for the new hook test — one path appended to test_kinds.integration.detect so the qa matrix and T-12's cross-check both see it as integration"
board_lifecycle: DRY-RUN would rename #661: "T-17 — SC-13 cutover half — the thin PostToolUse hook that delivers the warning into a running orchestrator's own context, registered on the existing Write|Edit|Bash matcher" -> "FEAT-31-orchestrator-context-watch — T-17 — SC-13 cutover half — the thin PostToolUse hook that delivers the warning into a running orchestrator's own context, registered on the existing Write|Edit|Bash matcher"
board_lifecycle: DRY-RUN would rename #660: 'T-16 — SC-13 library half — context-watch.py can answer "is THIS agent over the threshold, and what should the warning say", as a function and a mode, with no knowledge that a hook exists' -> 'FEAT-31-orchestrator-context-watch — T-16 — SC-13 library half — context-watch.py can answer "is THIS agent over the threshold, and what should the warning say", as a function and a mode, with no knowledge that a hook exists'
board_lifecycle: DRY-RUN would rename #659: 'T-15 — SC-07 — feature.json runs items carry an agent identifier, and the write path refuses a NEW entry that omits it, without invalidating any of the 390 existing entries' -> 'FEAT-31-orchestrator-context-watch — T-15 — SC-07 — feature.json runs items carry an agent identifier, and the write path refuses a NEW entry that omits it, without invalidating any of the 390 existing entries'
board_lifecycle: DRY-RUN would rename #655: 'T-14 — INV-17 shape-checks EVERY notes/handoff-*.md by glob, with the shape check moved out of the SEAM_NOTES loop so no note is reported twice' -> 'FEAT-31-orchestrator-context-watch — T-14 — INV-17 shape-checks EVERY notes/handoff-*.md by glob, with the shape check moved out of the SEAM_NOTES loop so no note is reported twice'
board_lifecycle: DRY-RUN would rename #654: "T-13 — SC-01's live half — an on-demand script that compares context-watch.py against an independent inline recomputation on a REAL orchestrator transcript, deliberately not named test-*.py" -> "FEAT-31-orchestrator-context-watch — T-13 — SC-01's live half — an on-demand script that compares context-watch.py against an independent inline recomputation on a REAL orchestrator transcript, deliberately not named test-*.py"
board_lifecycle: DRY-RUN would rename #653: 'T-12 — Make the two lists unable to disagree silently — run-unit-tests.sh cross-checks its script arrays against harness.json test_kinds, with a red proof that asserts the mismatch is NAMED' -> 'FEAT-31-orchestrator-context-watch — T-12 — Make the two lists unable to disagree silently — run-unit-tests.sh cross-checks its script arrays against harness.json test_kinds, with a red proof that asserts the mismatch is NAMED'
board_lifecycle: DRY-RUN would rename #652: 'T-11 — Close the test-kind misclassification class — every INTEGRATION_SCRIPTS entry gains an explicit path in test_kinds.integration.detect' -> 'FEAT-31-orchestrator-context-watch — T-11 — Close the test-kind misclassification class — every INTEGRATION_SCRIPTS entry gains an explicit path in test_kinds.integration.detect'
board_lifecycle: DRY-RUN would rename #651: 'T-10 — Make INV-17 reject a handoff whose required sections are present but empty, with the emptied Next section as the red proof' -> 'FEAT-31-orchestrator-context-watch — T-10 — Make INV-17 reject a handoff whose required sections are present but empty, with the emptied Next section as the red proof'
board_lifecycle: DRY-RUN would rename #650: 'T-09 — Record the decision that declares the new harness.json budgets key, and regenerate the index' -> 'FEAT-31-orchestrator-context-watch — T-09 — Record the decision that declares the new harness.json budgets key, and regenerate the index'
board_lifecycle: DRY-RUN would rename #649: "T-08 — Print the tool's own blind spots, each line derived from a value the run computed" -> "FEAT-31-orchestrator-context-watch — T-08 — Print the tool's own blind spots, each line derived from a value the run computed"
board_lifecycle: DRY-RUN would rename #648: 'T-07 — Integration tests — the corrected figures against an independent inline recomputation, and the worktree slug' -> 'FEAT-31-orchestrator-context-watch — T-07 — Integration tests — the corrected figures against an independent inline recomputation, and the worktree slug'
board_lifecycle: DRY-RUN would rename #647: 'T-06 — Add the threshold comparison, the warning and the printed headroom, with the before-and-after red proof' -> 'FEAT-31-orchestrator-context-watch — T-06 — Add the threshold comparison, the warning and the printed headroom, with the before-and-after red proof'
board_lifecycle: DRY-RUN would rename #646: 'T-05 — Establish whether upgrade-config.py propagates a new budgets key, and prove the answer with a test' -> 'FEAT-31-orchestrator-context-watch — T-05 — Establish whether upgrade-config.py propagates a new budgets key, and prove the answer with a test'
board_lifecycle: DRY-RUN would rename #645: 'T-04 — Add the same threshold key to the harness.json TEMPLATE so a newly initialised project receives it' -> 'FEAT-31-orchestrator-context-watch — T-04 — Add the same threshold key to the harness.json TEMPLATE so a newly initialised project receives it'
board_lifecycle: DRY-RUN would rename #644: "T-03 — Add the context threshold key to this project's harness.json budgets block" -> "FEAT-31-orchestrator-context-watch — T-03 — Add the context threshold key to this project's harness.json budgets block"
board_lifecycle: DRY-RUN would rename #643: 'T-02 — Unit tests for the corrected arithmetic, the agentType filter and the unmeasured branch, each with a mutant-copy red proof' -> 'FEAT-31-orchestrator-context-watch — T-02 — Unit tests for the corrected arithmetic, the agentType filter and the unmeasured branch, each with a mutant-copy red proof'
board_lifecycle: DRY-RUN would rename #642: 'T-01 — Create context-watch.py — sidecar discovery, corrected context arithmetic, unmeasured rows, feature attribution, worktree slug resolution' -> 'FEAT-31-orchestrator-context-watch — T-01 — Create context-watch.py — sidecar discovery, corrected context arithmetic, unmeasured rows, feature attribution, worktree slug resolution'
board_lifecycle: DRY-RUN would rename #625: 'T-10 — Prove four CONCURRENT writers commit only to their own branch, and prove the same assertion detects a shared checkout' -> 'FEAT-30-worktree-per-feature — T-10 — Prove four CONCURRENT writers commit only to their own branch, and prove the same assertion detects a shared checkout'
board_lifecycle: DRY-RUN would rename #624: "T-09 — State in the orchestrator's own instructions which worktree it works in" -> "FEAT-30-worktree-per-feature — T-09 — State in the orchestrator's own instructions which worktree it works in"
board_lifecycle: DRY-RUN would rename #623: 'T-08 — Register both new test files with the integration kind and the runner' -> 'FEAT-30-worktree-per-feature — T-08 — Register both new test files with the integration kind and the runner'
board_lifecycle: DRY-RUN would rename #622: 'T-07 — Route every Expertise apply through the merge tool at distillation' -> 'FEAT-30-worktree-per-feature — T-07 — Route every Expertise apply through the merge tool at distillation'
board_lifecycle: DRY-RUN would rename #621: 'T-06 — Make two concurrent Expertise close-outs lose no entry, or fail loudly' -> 'FEAT-30-worktree-per-feature — T-06 — Make two concurrent Expertise close-outs lose no entry, or fail loudly'
board_lifecycle: DRY-RUN would rename #620: 'T-05 — Refuse a governed agent that moves HEAD during a run, and refuse a forced worktree removal on the Bash route' -> 'FEAT-30-worktree-per-feature — T-05 — Refuse a governed agent that moves HEAD during a run, and refuse a forced worktree removal on the Bash route'
board_lifecycle: DRY-RUN would rename #619: 'T-04 — Replace the fixed-segment worktree strip with checkout-relative resolution in every consumer' -> 'FEAT-30-worktree-per-feature — T-04 — Replace the fixed-segment worktree strip with checkout-relative resolution in every consumer'
board_lifecycle: DRY-RUN would rename #618: 'T-03 — Pin the current in-worktree grant behaviour for all sixteen agents before the mechanism changes' -> 'FEAT-30-worktree-per-feature — T-03 — Pin the current in-worktree grant behaviour for all sixteen agents before the mechanism changes'
board_lifecycle: DRY-RUN would rename #617: 'T-02 — Add the remove subcommand with its dirty-tree refusal and its landed-artifact check' -> 'FEAT-30-worktree-per-feature — T-02 — Add the remove subcommand with its dirty-tree refusal and its landed-artifact check'
board_lifecycle: DRY-RUN would rename #616: 'T-01 — Create the feature-worktree CLI with its create and list subcommands, and the four-worktree isolation test' -> 'FEAT-30-worktree-per-feature — T-01 — Create the feature-worktree CLI with its create and list subcommands, and the four-worktree isolation test'
board_lifecycle: DRY-RUN would rename #587: 'T-09 — Prove the saving is the query shape by measuring both reads on the same four-item board' -> 'FEAT-29-graphql-budget — T-09 — Prove the saving is the query shape by measuring both reads on the same four-item board'
board_lifecycle: DRY-RUN would rename #586: 'T-08 — Record the polling and measurement operating rules in CLAUDE.md' -> 'FEAT-29-graphql-budget — T-08 — Record the polling and measurement operating rules in CLAUDE.md'
board_lifecycle: DRY-RUN would rename #585: 'T-07 — Cut INV-26 over to the cheap read and prove the identical violation set' -> 'FEAT-29-graphql-budget — T-07 — Cut INV-26 over to the cheap read and prove the identical violation set'
board_lifecycle: DRY-RUN would rename #584: 'T-06 — Capture the pre-change gate baseline before any code moves' -> 'FEAT-29-graphql-budget — T-06 — Capture the pre-change gate baseline before any code moves'
board_lifecycle: DRY-RUN would rename #583: 'T-05 — Correct the 31-point figure and record every cost with its condition' -> 'FEAT-29-graphql-budget — T-05 — Correct the 31-point figure and record every cost with its condition'
board_lifecycle: DRY-RUN would rename #582: 'T-04 — Name the GraphQL budget and its reset when gh fails on rate limit' -> 'FEAT-29-graphql-budget — T-04 — Name the GraphQL budget and its reset when gh fails on rate limit'
board_lifecycle: DRY-RUN would rename #581: 'T-03 — Record every wrapped gh invocation and its GraphQL cost' -> 'FEAT-29-graphql-budget — T-03 — Record every wrapped gh invocation and its GraphQL cost'
board_lifecycle: DRY-RUN would rename #580: 'T-02 — Point board_stations at the low-cost read without changing its contract' -> 'FEAT-29-graphql-budget — T-02 — Point board_stations at the low-cost read without changing its contract'
board_lifecycle: DRY-RUN would rename #579: 'T-01 — Add a targeted low-cost board station read to factory_gh' -> 'FEAT-29-graphql-budget — T-01 — Add a targeted low-cost board station read to factory_gh'
board_lifecycle: DRY-RUN would rename #573: "T-07 — Pin the injection hook's unreadable-file guard with a case proven to redden against a guard-removed mutant" -> "FEAT-27-expertise-repository-tier — T-07 — Pin the injection hook's unreadable-file guard with a case proven to redden against a guard-removed mutant"
board_lifecycle: DRY-RUN would rename #570: 'T-06 — Refresh the stale corpus measurement and the audit scope in the two Expertise skills' -> 'FEAT-27-expertise-repository-tier — T-06 — Refresh the stale corpus measurement and the audit scope in the two Expertise skills'
board_lifecycle: DRY-RUN would rename #569: 'T-05 — Correct the Expertise layout in the harness README and SPEC' -> 'FEAT-27-expertise-repository-tier — T-05 — Correct the Expertise layout in the harness README and SPEC'
board_lifecycle: DRY-RUN would rename #568: 'T-04 — Move the eleven adjudicated repository-specific entries into the harness repository tier' -> 'FEAT-27-expertise-repository-tier — T-04 — Move the eleven adjudicated repository-specific entries into the harness repository tier'
board_lifecycle: DRY-RUN would rename #567: 'T-03 — Give the expertise checker the repository-tier budget and the advisory craft-token scan' -> 'FEAT-27-expertise-repository-tier — T-03 — Give the expertise checker the repository-tier budget and the advisory craft-token scan'
board_lifecycle: DRY-RUN would rename #566: 'T-02 — Add the repository-tier read path to the spawn injection hook, with its own budget and a test' -> 'FEAT-27-expertise-repository-tier — T-02 — Add the repository-tier read path to the spawn injection hook, with its own budget and a test'
board_lifecycle: DRY-RUN would rename #565: 'T-01 — Grant every agent its repository-tier Expertise path in the team manifest' -> 'FEAT-27-expertise-repository-tier — T-01 — Grant every agent its repository-tier Expertise path in the team manifest'
board_lifecycle: DRY-RUN would rename #542: "T-03 — Bring factory_claim.py under the layout detector's features surface" -> "FEAT-25-claim-feature-root — T-03 — Bring factory_claim.py under the layout detector's features surface"
board_lifecycle: DRY-RUN would rename #541: 'T-02 — Give an unreadable plan its own blocker reason naming the path that was tried' -> 'FEAT-25-claim-feature-root — T-02 — Give an unreadable plan its own blocker reason naming the path that was tried'
board_lifecycle: DRY-RUN would rename #540: 'T-01 — Point FEATURES_ROOT at the migrated features tree and pin the unpatched default' -> 'FEAT-25-claim-feature-root — T-01 — Point FEATURES_ROOT at the migrated features tree and pin the unpatched default'
board_lifecycle: DRY-RUN would rename #511: 'T-10 — The two falsified decision statements are amended' -> 'FEAT-24-config-responsibility-split — T-10 — The two falsified decision statements are amended'
board_lifecycle: DRY-RUN would rename #510: "T-09 — kaya-ai's own config moves onto the by-name board shape" -> "FEAT-24-config-responsibility-split — T-09 — kaya-ai's own config moves onto the by-name board shape"
board_lifecycle: DRY-RUN would rename #509: 'T-08 — The config template shows a declared board, stations and all' -> 'FEAT-24-config-responsibility-split — T-08 — The config template shows a declared board, stations and all'
board_lifecycle: DRY-RUN would rename #508: 'T-07 — fleet.yaml becomes the roster, the path and the branch, and nothing else' -> 'FEAT-24-config-responsibility-split — T-07 — fleet.yaml becomes the roster, the path and the branch, and nothing else'
board_lifecycle: DRY-RUN would rename #507: 'T-06 — The harness board declares its five stations' -> 'FEAT-24-config-responsibility-split — T-06 — The harness board declares its five stations'
board_lifecycle: DRY-RUN would rename #506: 'T-05 — INV-26 reads its station names from the board and reports an unusable one' -> 'FEAT-24-config-responsibility-split — T-05 — INV-26 reads its station names from the board and reports an unusable one'
board_lifecycle: DRY-RUN would rename #505: 'T-04 — load_board raises, and derive_station reads its station names from the board' -> 'FEAT-24-config-responsibility-split — T-04 — load_board raises, and derive_station reads its station names from the board'
board_lifecycle: DRY-RUN would rename #504: 'T-03 — Every fleet fixture outside factory_config moves off repos[].board' -> 'FEAT-24-config-responsibility-split — T-03 — Every fleet fixture outside factory_config moves off repos[].board'
board_lifecycle: DRY-RUN would rename #503: "T-02 — One loud board validator, and a fleet member's config is read from its remote" -> "FEAT-24-config-responsibility-split — T-02 — One loud board validator, and a fleet member's config is read from its remote"
board_lifecycle: DRY-RUN would rename #502: 'T-01 — factory_gh gains a single-file read of a repository at a ref' -> 'FEAT-24-config-responsibility-split — T-01 — factory_gh gains a single-file read of a repository at a ref'
board_lifecycle: DRY-RUN would rename #460: 'T-06 — The plan playbook moves its source ticket to Plan at kickoff' -> 'FEAT-23-ship-flow-fixes — T-06 — The plan playbook moves its source ticket to Plan at kickoff'
board_lifecycle: DRY-RUN would rename #459: 'T-05 — A kickoff writer moves one named issue to one named station' -> 'FEAT-23-ship-flow-fixes — T-05 — A kickoff writer moves one named issue to one named station'
board_lifecycle: DRY-RUN would rename #458: 'T-04 — Record DEC-195 and DEC-196 and regenerate the decisions index' -> 'FEAT-23-ship-flow-fixes — T-04 — Record DEC-195 and DEC-196 and regenerate the decisions index'
board_lifecycle: DRY-RUN would rename #457: 'T-03 — Sequence the simplify step into the build and plan playbooks' -> 'FEAT-23-ship-flow-fixes — T-03 — Sequence the simplify step into the build and plan playbooks'
board_lifecycle: DRY-RUN would rename #456: 'T-02 — Ship the four-angle simplify methodology as a harness skill' -> 'FEAT-23-ship-flow-fixes — T-02 — Ship the four-angle simplify methodology as a harness skill'
board_lifecycle: DRY-RUN would rename #455: 'T-01 — gh-sync ship and abandon record the terminal status in feature.json' -> 'FEAT-23-ship-flow-fixes — T-01 — gh-sync ship and abandon record the terminal status in feature.json'
board_lifecycle: DRY-RUN would rename #429: 'T-11 — Capture the post-move boundary against the landed commit' -> 'FEAT-22-docs-layout-migration — T-11 — Capture the post-move boundary against the landed commit'
board_lifecycle: DRY-RUN would rename #428: 'T-10 — Depth-sweep the tree with a resolver rather than a literal, and name every survivor' -> 'FEAT-22-docs-layout-migration — T-10 — Depth-sweep the tree with a resolver rather than a literal, and name every survivor'
board_lifecycle: DRY-RUN would rename #427: 'T-09 — Regenerate the index, prove both boundaries, and land the cluster as one commit' -> 'FEAT-22-docs-layout-migration — T-09 — Regenerate the index, prove both boundaries, and land the cluster as one commit'
board_lifecycle: DRY-RUN would rename #426: 'T-08 — Record the DEC-189 amendment that the move makes necessary' -> 'FEAT-22-docs-layout-migration — T-08 — Record the DEC-189 amendment that the move makes necessary'
board_lifecycle: DRY-RUN would rename #425: 'T-07 — Correct the two Expertise files that carry a stale docs path into every spawn' -> 'FEAT-22-docs-layout-migration — T-07 — Correct the two Expertise files that carry a stale docs path into every spawn'
board_lifecycle: DRY-RUN would rename #424: 'T-06 — Correct the instruction-side literals and the two gate diagnostics' -> 'FEAT-22-docs-layout-migration — T-06 — Correct the instruction-side literals and the two gate diagnostics'
board_lifecycle: DRY-RUN would rename #423: 'T-05 — Invert the tests whose expected verdict flips, and add the two standing cases' -> 'FEAT-22-docs-layout-migration — T-05 — Invert the tests whose expected verdict flips, and add the two standing cases'
board_lifecycle: DRY-RUN would rename #422: 'T-04 — Repoint the six test files that read the real docs and break at runtime' -> 'FEAT-22-docs-layout-migration — T-04 — Repoint the six test files that read the real docs and break at runtime'
board_lifecycle: DRY-RUN would rename #421: 'T-03 — Move the three coupled resolvers and the four detector-invisible claims beside them' -> 'FEAT-22-docs-layout-migration — T-03 — Move the three coupled resolvers and the four detector-invisible claims beside them'
board_lifecycle: DRY-RUN would rename #420: 'T-02 — Grant the destination and physically move the five tracked docs' -> 'FEAT-22-docs-layout-migration — T-02 — Grant the destination and physically move the five tracked docs'
board_lifecycle: DRY-RUN would rename #419: 'T-01 — Capture the pre-move boundary and name the red states the cluster passes through' -> 'FEAT-22-docs-layout-migration — T-01 — Capture the pre-move boundary and name the red states the cluster passes through'
board_lifecycle: DRY-RUN would rename #398: 'T-09 — Prove the boundary state at both commits and land the cluster as one commit' -> 'FEAT-21-features-layout-migration — T-09 — Prove the boundary state at both commits and land the cluster as one commit'
board_lifecycle: DRY-RUN would rename #397: 'T-10 — Re-root the two resolvers that derive the control plane by path arithmetic' -> 'FEAT-21-features-layout-migration — T-10 — Re-root the two resolvers that derive the control plane by path arithmetic'
board_lifecycle: DRY-RUN would rename #396: 'T-08 — Move every feature directory under the harness segment, with nothing left behind' -> 'FEAT-21-features-layout-migration — T-08 — Move every feature directory under the harness segment, with nothing left behind'
board_lifecycle: DRY-RUN would rename #395: 'T-07 — Re-anchor the guard-enforced instruction paths, the branch gate and the run-dir ignore rule' -> 'FEAT-21-features-layout-migration — T-07 — Re-anchor the guard-enforced instruction paths, the branch gate and the run-dir ignore rule'
board_lifecycle: DRY-RUN would rename #394: 'T-06 — Move the test suites whose fixtures and literal expectations are pinned to the old path' -> 'FEAT-21-features-layout-migration — T-06 — Move the test suites whose fixtures and literal expectations are pinned to the old path'
board_lifecycle: DRY-RUN would rename #393: "T-05 — Move check-state.sh's fifteen feature-discovery sites onto a repository segment" -> "FEAT-21-features-layout-migration — T-05 — Move check-state.sh's fifteen feature-discovery sites onto a repository segment"
board_lifecycle: DRY-RUN would rename #392: "T-04 — Move check-plan-routes.py's feature discovery onto a repository segment" -> "FEAT-21-features-layout-migration — T-04 — Move check-plan-routes.py's feature discovery onto a repository segment"
board_lifecycle: DRY-RUN would rename #391: "T-03 — Move check-domain.sh's sweep globs and its four shape regexes onto a repository segment" -> "FEAT-21-features-layout-migration — T-03 — Move check-domain.sh's sweep globs and its four shape regexes onto a repository segment"
board_lifecycle: DRY-RUN would rename #390: 'T-02 — Move the write grants in team-config.yaml onto a wildcard repository segment' -> 'FEAT-21-features-layout-migration — T-02 — Move the write grants in team-config.yaml onto a wildcard repository segment'
board_lifecycle: DRY-RUN would rename #389: 'T-01 — Pin the blame and cause parity between the CI and session-entry renderings' -> 'FEAT-21-features-layout-migration — T-01 — Pin the blame and cause parity between the CI and session-entry renderings'
board_lifecycle: DRY-RUN would rename #364: 'T-04 — Record the surface partition and the cannot-verify posture as a decision' -> 'FEAT-20-migration-detector — T-04 — Record the surface partition and the cannot-verify posture as a decision'
board_lifecycle: DRY-RUN would rename #363: 'T-03 — Add the layout gate as a step of the required integration CI job' -> 'FEAT-20-migration-detector — T-03 — Add the layout gate as a step of the required integration CI job'
board_lifecycle: DRY-RUN would rename #362: 'T-02 — Wire the detector into check-state.sh as INV-27 and prove it against fixtures' -> 'FEAT-20-migration-detector — T-02 — Wire the detector into check-state.sh as INV-27 and prove it against fixtures'
board_lifecycle: DRY-RUN would rename #361: 'T-01 — Build the layout-migration detector module and its red-first unit suite' -> 'FEAT-20-migration-detector — T-01 — Build the layout-migration detector module and its red-first unit suite'
board_lifecycle: DRY-RUN would rename #332: 'T-06 — Amend the orchestrator sync-point table - every subcommand named with an owner, plus how the build branch is created' -> 'FEAT-18-board-truth — T-06 — Amend the orchestrator sync-point table - every subcommand named with an owner, plus how the build branch is created'
board_lifecycle: DRY-RUN would rename #331: "T-05 — Delete branch-create-gate.sh's dormant board-flip block and its four config keys" -> "FEAT-18-board-truth — T-05 — Delete branch-create-gate.sh's dormant board-flip block and its four config keys"
board_lifecycle: DRY-RUN would rename #330: "T-04 — Add INV-26 to check-state.sh comparing every live feature's plan against its cards" -> "FEAT-18-board-truth — T-04 — Add INV-26 to check-state.sh comparing every live feature's plan against its cards"
board_lifecycle: DRY-RUN would rename #329: 'T-03 — Move cards from gh-sync.py - a start-task subcommand, the derived parent, and the loud failure posture' -> 'FEAT-18-board-truth — T-03 — Move cards from gh-sync.py - a start-task subcommand, the derived parent, and the loud failure posture'
board_lifecycle: DRY-RUN would rename #328: 'T-02 — Add gh_board.py with the board config, the station derivation and the board read and write' -> 'FEAT-18-board-truth — T-02 — Add gh_board.py with the board config, the station derivation and the board read and write'
board_lifecycle: DRY-RUN would rename #327: 'T-01 — Add the pending, building, done task-status enum to check-plan-routes.py' -> 'FEAT-18-board-truth — T-01 — Add the pending, building, done task-status enum to check-plan-routes.py'
board_lifecycle: DRY-RUN would rename #310: 'T-11 — Migrate the factory_workspace test fixture to a per-repo board' -> 'FEAT-16-factory-per-repo-board — T-11 — Migrate the factory_workspace test fixture to a per-repo board'
board_lifecycle: DRY-RUN would rename #309: "T-10 — Record the per-repository board in DECISIONS and close DEC-174 am.1's open board loose end" -> "FEAT-16-factory-per-repo-board — T-10 — Record the per-repository board in DECISIONS and close DEC-174 am.1's open board loose end"
board_lifecycle: DRY-RUN would rename #308: 'T-09 — Assert the declared repository-to-board pairing in test-no-distribution.py' -> 'FEAT-16-factory-per-repo-board — T-09 — Assert the declared repository-to-board pairing in test-no-distribution.py'
board_lifecycle: DRY-RUN would rename #307: 'T-08 — Require a per-repo board on every entry and reject a leftover fleet-level board key' -> 'FEAT-16-factory-per-repo-board — T-08 — Require a per-repo board on every entry and reject a leftover fleet-level board key'
board_lifecycle: DRY-RUN would rename #306: 'T-07 — Confirm both boards carry the six factory stations, then give kaya-ai its own board in fleet.yaml' -> 'FEAT-16-factory-per-repo-board — T-07 — Confirm both boards carry the six factory stations, then give kaya-ai its own board in fleet.yaml'
board_lifecycle: DRY-RUN would rename #305: 'T-06 — Migrate the check-domain fleet fixtures to per-repo boards without touching the gate script' -> 'FEAT-16-factory-per-repo-board — T-06 — Migrate the check-domain fleet fixtures to per-repo boards without touching the gate script'
board_lifecycle: DRY-RUN would rename #304: 'T-05 — Migrate the factory integration fixtures to per-repo boards' -> 'FEAT-16-factory-per-repo-board — T-05 — Migrate the factory integration fixtures to per-repo boards'
board_lifecycle: DRY-RUN would rename #303: 'T-04 — Point factory_land at the board of the repository it is landing in' -> 'FEAT-16-factory-per-repo-board — T-04 — Point factory_land at the board of the repository it is landing in'
board_lifecycle: DRY-RUN would rename #302: 'T-03 — Point factory_decompose at the board of the repository it is publishing' -> 'FEAT-16-factory-per-repo-board — T-03 — Point factory_decompose at the board of the repository it is publishing'
board_lifecycle: DRY-RUN would rename #301: 'T-02 — Move factory_claim board reads and station validation inside a per-repository loop' -> 'FEAT-16-factory-per-repo-board — T-02 — Move factory_claim board reads and station validation inside a per-repository loop'
board_lifecycle: DRY-RUN would rename #300: 'T-01 — Teach load_fleet a per-repo board block and add board_for and a repo-scoped station lookup' -> 'FEAT-16-factory-per-repo-board — T-01 — Teach load_fleet a per-repo board block and add board_for and a repo-scoped station lookup'
board_lifecycle: DRY-RUN would rename #275: 'T-12 — Rebuild INV-17 around the six status values so it keeps firing' -> 'FEAT-14-feature-json-schema — T-12 — Rebuild INV-17 around the six status values so it keeps firing'
board_lifecycle: DRY-RUN would rename #274: "T-11 — Repoint check-plan-routes.py's finished-feature skip at Done" -> "FEAT-14-feature-json-schema — T-11 — Repoint check-plan-routes.py's finished-feature skip at Done"
board_lifecycle: DRY-RUN would rename #273: 'T-10 — Regenerate the decisions index' -> 'FEAT-14-feature-json-schema — T-10 — Regenerate the decisions index'
board_lifecycle: DRY-RUN would rename #272: 'T-09 — Record both decisions and rewrite SPEC 11.3 around the closed key set' -> 'FEAT-14-feature-json-schema — T-09 — Record both decisions and rewrite SPEC 11.3 around the closed key set'
board_lifecycle: DRY-RUN would rename #271: 'T-08 — Convert every remaining feature.yaml to feature.json and prove the whole system green' -> 'FEAT-14-feature-json-schema — T-08 — Convert every remaining feature.yaml to feature.json and prove the whole system green'
board_lifecycle: DRY-RUN would rename #270: 'T-07 — Rename the file in every instruction, template and config, and ship the missing template' -> 'FEAT-14-feature-json-schema — T-07 — Rename the file in every instruction, template and config, and ship the missing template'
board_lifecycle: DRY-RUN would rename #269: 'T-06 — Repoint and re-budget the two gate scripts, and wire schema validation into the write-payload path' -> 'FEAT-14-feature-json-schema — T-06 — Repoint and re-budget the two gate scripts, and wire schema validation into the write-payload path'
board_lifecycle: DRY-RUN would rename #268: 'T-05 — Repoint the four python readers of feature.yaml at feature.json' -> 'FEAT-14-feature-json-schema — T-05 — Repoint the four python readers of feature.yaml at feature.json'
board_lifecycle: DRY-RUN would rename #267: 'T-04 — Drop every unread key from every feature.yaml on disk, with a receipt per feature' -> 'FEAT-14-feature-json-schema — T-04 — Drop every unread key from every feature.yaml on disk, with a receipt per feature'
board_lifecycle: DRY-RUN would rename #266: 'T-03 — Install jsonschema in CI and add the validator to the required integration job' -> 'FEAT-14-feature-json-schema — T-03 — Install jsonschema in CI and add the validator to the required integration job'
board_lifecycle: DRY-RUN would rename #265: 'T-02 — Declare jsonschema as a required prerequisite in init and CLAUDE.md' -> 'FEAT-14-feature-json-schema — T-02 — Declare jsonschema as a required prerequisite in init and CLAUDE.md'
board_lifecycle: DRY-RUN would rename #264: 'T-01 — Ship the schema artifact and its validator with unit tests' -> 'FEAT-14-feature-json-schema — T-01 — Ship the schema artifact and its validator with unit tests'
board_lifecycle: DRY-RUN would rename #246: 'T-02 — Record one live read-only rate_limit spot-check of a single board lookup' -> 'FEAT-13-single-issue-board-lookup — T-02 — Record one live read-only rate_limit spot-check of a single board lookup'
board_lifecycle: DRY-RUN would rename #245: 'T-01 — Replace all three whole-board single-issue lookups with one targeted GraphQL helper' -> 'FEAT-13-single-issue-board-lookup — T-01 — Replace all three whole-board single-issue lookups with one targeted GraphQL helper'
board_lifecycle: DRY-RUN would rename #237: 'T-14 — Strike DEC-12 and the falsified half of DEC-113 from the decision record' -> 'FEAT-12-end-copy-distribution — T-14 — Strike DEC-12 and the falsified half of DEC-113 from the decision record'
board_lifecycle: DRY-RUN would rename #236: 'T-13 — Add test-no-distribution.py and register it in the unit suite' -> 'FEAT-12-end-copy-distribution — T-13 — Add test-no-distribution.py and register it in the unit suite'
board_lifecycle: DRY-RUN would rename #235: 'T-12 — Sweep the distribution story out of the four documentation surfaces' -> 'FEAT-12-end-copy-distribution — T-12 — Sweep the distribution story out of the four documentation surfaces'
board_lifecycle: DRY-RUN would rename #234: 'T-11 — Sweep the deploy references out of the rule skills, the templates readme and both team-config files' -> 'FEAT-12-end-copy-distribution — T-11 — Sweep the deploy references out of the rule skills, the templates readme and both team-config files'
board_lifecycle: DRY-RUN would rename #233: 'T-10 — Neutralise every deploy reference in the bin python modules, comments and user-facing strings alike' -> 'FEAT-12-end-copy-distribution — T-10 — Neutralise every deploy reference in the bin python modules, comments and user-facing strings alike'
board_lifecycle: DRY-RUN would rename #232: 'T-09 — Delete the deploy registry from the home directory' -> 'FEAT-12-end-copy-distribution — T-09 — Delete the deploy registry from the home directory'
board_lifecycle: DRY-RUN would rename #231: 'T-08 — Delete the harness-deploy slash command' -> 'FEAT-12-end-copy-distribution — T-08 — Delete the harness-deploy slash command'
board_lifecycle: DRY-RUN would rename #230: 'T-07 — Delete deploy.sh' -> 'FEAT-12-end-copy-distribution — T-07 — Delete deploy.sh'
board_lifecycle: DRY-RUN would rename #229: 'T-06 — Add mruangutai/kaya-ai to the factory fleet declaration' -> 'FEAT-12-end-copy-distribution — T-06 — Add mruangutai/kaya-ai to the factory fleet declaration'
board_lifecycle: DRY-RUN would rename #228: 'T-05 — Commit and push the kaya-ai tooling removal to master, path-scoped' -> 'FEAT-12-end-copy-distribution — T-05 — Commit and push the kaya-ai tooling removal to master, path-scoped'
board_lifecycle: DRY-RUN would rename #227: "T-04 — Re-capture kaya-ai's .harness manifest and prove it is unchanged" -> "FEAT-12-end-copy-distribution — T-04 — Re-capture kaya-ai's .harness manifest and prove it is unchanged"
board_lifecycle: DRY-RUN would rename #226: "T-03 — Unwire all eight harness hook registrations from kaya-ai's settings.json" -> "FEAT-12-end-copy-distribution — T-03 — Unwire all eight harness hook registrations from kaya-ai's settings.json"
board_lifecycle: DRY-RUN would rename #225: "T-02 — Delete kaya-ai's copies of the harness skills, slash commands and agents" -> "FEAT-12-end-copy-distribution — T-02 — Delete kaya-ai's copies of the harness skills, slash commands and agents"
board_lifecycle: DRY-RUN would rename #224: 'T-01 — Capture the kaya-ai .harness state manifest before any deletion' -> 'FEAT-12-end-copy-distribution — T-01 — Capture the kaya-ai .harness state manifest before any deletion'
board_lifecycle: DRY-RUN would rename #215: 'T-01 — Resolve project field ids with one GraphQL query and rewrite the stub tests that pin the old call shape' -> 'FEAT-11-graphql-field-resolve — T-01 — Resolve project field ids with one GraphQL query and rewrite the stub tests that pin the old call shape'
board_lifecycle: DRY-RUN would rename #102: 'T-04 — Add the plan-time route rule to `harness-spec-driven/SKILL.md`' -> 'FEAT-09-plan-time-route-check — T-04 — Add the plan-time route rule to `harness-spec-driven/SKILL.md`'
board_lifecycle: DRY-RUN would rename #101: 'T-03 — Give `templates/PLAN.md` a `## Lanes` section and a mandatory `execution_mode:` field' -> 'FEAT-09-plan-time-route-check — T-03 — Give `templates/PLAN.md` a `## Lanes` section and a mandatory `execution_mode:` field'
board_lifecycle: DRY-RUN would rename #100: 'T-02 — Add `check-plan-routes.py`, its test, and register the test in the unit runner' -> 'FEAT-09-plan-time-route-check — T-02 — Add `check-plan-routes.py`, its test, and register the test in the unit runner'
board_lifecycle: DRY-RUN would rename #99: 'T-01 — Add a `--resolve <path>` mode to `check-domain.sh` that short-circuits before stdin is read' -> 'FEAT-09-plan-time-route-check — T-01 — Add a `--resolve <path>` mode to `check-domain.sh` that short-circuits before stdin is read'
board_lifecycle: DRY-RUN would rename #97: 'T-12 — Update `.harness/README.md`, the layout authority for `.harness/`' -> 'FEAT-08-remove-cost-tracking — T-12 — Update `.harness/README.md`, the layout authority for `.harness/`'
board_lifecycle: DRY-RUN would rename #96: "T-11 — Mark the deleted meter in BUILD's historical tables" -> "FEAT-08-remove-cost-tracking — T-11 — Mark the deleted meter in BUILD's historical tables"
board_lifecycle: DRY-RUN would rename #95: 'T-10 — Remove the cost model from SPEC' -> 'FEAT-08-remove-cost-tracking — T-10 — Remove the cost model from SPEC'
board_lifecycle: DRY-RUN would rename #94: 'T-09 — Record DEC-178, correct the DEC-148 index row, and regenerate the index' -> 'FEAT-08-remove-cost-tracking — T-09 — Record DEC-178, correct the DEC-148 index row, and regenerate the index'
board_lifecycle: DRY-RUN would rename #93: 'T-08 — Remove the per-team spend caps from the two team definitions' -> 'FEAT-08-remove-cost-tracking — T-08 — Remove the per-team spend caps from the two team definitions'
board_lifecycle: DRY-RUN would rename #92: 'T-07 — Remove the lead-facing metering instructions from the team skill' -> 'FEAT-08-remove-cost-tracking — T-07 — Remove the lead-facing metering instructions from the team skill'
board_lifecycle: DRY-RUN would rename #91: 'T-06 — Remove the money budget from the orchestrator playbook, keeping the cycle budget and the' -> 'FEAT-08-remove-cost-tracking — T-06 — Remove the money budget from the orchestrator playbook, keeping the cycle budget and the'
board_lifecycle: DRY-RUN would rename #90: 'T-05 — Remove the cost budget from the orchestrator agent definition' -> 'FEAT-08-remove-cost-tracking — T-05 — Remove the cost budget from the orchestrator agent definition'
board_lifecycle: DRY-RUN would rename #89: 'T-04 — Strip the `cost_model` block and the two USD budgets from both configs' -> 'FEAT-08-remove-cost-tracking — T-04 — Strip the `cost_model` block and the two USD budgets from both configs'
board_lifecycle: DRY-RUN would rename #88: 'T-03 — Delete the meter and its test, and drop it from the unit-suite script list — in one change' -> 'FEAT-08-remove-cost-tracking — T-03 — Delete the meter and its test, and drop it from the unit-suite script list — in one change'
board_lifecycle: DRY-RUN would rename #87: 'T-02 — Remove INV-11, the `cost_model.rates` violation and the rate-staleness warning from' -> 'FEAT-08-remove-cost-tracking — T-02 — Remove INV-11, the `cost_model.rates` violation and the rate-staleness warning from'
board_lifecycle: DRY-RUN would rename #86: 'T-01 — Remove `cost_usd` from the orchestrator digest schema, and its fixtures' -> 'FEAT-08-remove-cost-tracking — T-01 — Remove `cost_usd` from the orchestrator digest schema, and its fixtures'
board_lifecycle: DRY-RUN would rename #57: 'T-10 — State the widened fail gate on the two `qa` return-contract surfaces' -> 'FEAT-07-verify-teeth-batch-probe — T-10 — State the widened fail gate on the two `qa` return-contract surfaces'
board_lifecycle: DRY-RUN would rename #56: 'T-09 — Record the three decisions and regenerate the index' -> 'FEAT-07-verify-teeth-batch-probe — T-09 — Record the three decisions and regenerate the index'
board_lifecycle: DRY-RUN would rename #55: "T-08 — Add the probe-don't-infer rule to the two tiers that relay claims to the user" -> "FEAT-07-verify-teeth-batch-probe — T-08 — Add the probe-don't-infer rule to the two tiers that relay claims to the user"
board_lifecycle: DRY-RUN would rename #54: "T-07 — Batch the user's change requests at the signature gate" -> "FEAT-07-verify-teeth-batch-probe — T-07 — Batch the user's change requests at the signature gate"
board_lifecycle: DRY-RUN would rename #53: 'T-06 — Update SPEC §8.1 — BOTH the eng-devs bullet and the dev-ops bullet' -> 'FEAT-07-verify-teeth-batch-probe — T-06 — Update SPEC §8.1 — BOTH the eng-devs bullet and the dev-ops bullet'
board_lifecycle: DRY-RUN would rename #52: 'T-05 — Require the dispatch prompt to carry the task id and the `verify:` command verbatim' -> 'FEAT-07-verify-teeth-batch-probe — T-05 — Require the dispatch prompt to carry the task id and the `verify:` command verbatim'
board_lifecycle: DRY-RUN would rename #51: 'T-04 — Correct the refusal-shaped dev digest in the TDD skill' -> 'FEAT-07-verify-teeth-batch-probe — T-04 — Correct the refusal-shaped dev digest in the TDD skill'
board_lifecycle: DRY-RUN would rename #50: 'T-03 — Add `task_verify` to the dev-ops inline digest block' -> 'FEAT-07-verify-teeth-batch-probe — T-03 — Add `task_verify` to the dev-ops inline digest block'
board_lifecycle: DRY-RUN would rename #49: 'T-02 — Add `task_verify` and the PLAN cross-check to the canonical dev return contract' -> 'FEAT-07-verify-teeth-batch-probe — T-02 — Add `task_verify` and the PLAN cross-check to the canonical dev return contract'
board_lifecycle: DRY-RUN would rename #48: 'T-01 — Add the gated `task_verify` field AND the fail-value gate to the validator and its fixtures, in one change' -> 'FEAT-07-verify-teeth-batch-probe — T-01 — Add the gated `task_verify` field AND the fail-value gate to the validator and its fixtures, in one change'
board_lifecycle: DRY-RUN would rename #35: 'T-11 — Name the blocking qa gate in the orchestrator playbook — the #24 fix' -> 'FEAT-06-team-layer-inv6 — T-11 — Name the blocking qa gate in the orchestrator playbook — the #24 fix'
board_lifecycle: DRY-RUN would rename #34: 'T-10 — Delete `gate-probe.yaml`' -> 'FEAT-06-team-layer-inv6 — T-10 — Delete `gate-probe.yaml`'
board_lifecycle: DRY-RUN would rename #33: 'T-09 — Teach the team runner the expansion form, so `build.yaml` is hostable' -> 'FEAT-06-team-layer-inv6 — T-09 — Teach the team runner the expansion form, so `build.yaml` is hostable'
board_lifecycle: DRY-RUN would rename #32: 'T-08 — Propagate the team catalog and the qa reconciliation into the design docs' -> 'FEAT-06-team-layer-inv6 — T-08 — Propagate the team catalog and the qa reconciliation into the design docs'
board_lifecycle: DRY-RUN would rename #31: 'T-07 — Add `bin/test-team-catalog.py` and register it in the unit runner' -> 'FEAT-06-team-layer-inv6 — T-07 — Add `bin/test-team-catalog.py` and register it in the unit runner'
board_lifecycle: DRY-RUN would rename #30: 'T-06 — Make the orchestrator playbook dispatch the named `build` team' -> 'FEAT-06-team-layer-inv6 — T-06 — Make the orchestrator playbook dispatch the named `build` team'
board_lifecycle: DRY-RUN would rename #29: 'T-05 — Widen the YAML-validity gate to cover the shipped team definitions' -> 'FEAT-06-team-layer-inv6 — T-05 — Widen the YAML-validity gate to cover the shipped team definitions'
board_lifecycle: DRY-RUN would rename #28: 'T-04 — Write `build.yaml` — the eng-squad build team, born valid' -> 'FEAT-06-team-layer-inv6 — T-04 — Write `build.yaml` — the eng-squad build team, born valid'
board_lifecycle: DRY-RUN would rename #27: 'T-02 — Add the gate-only qa step to the shipped `review` team and quote its templates' -> 'FEAT-06-team-layer-inv6 — T-02 — Add the gate-only qa step to the shipped `review` team and quote its templates'
board_lifecycle: DRY-RUN would rename #26: "T-01 — Close INV-6's truthy hole and give the placeholder vocabulary one home" -> "FEAT-06-team-layer-inv6 — T-01 — Close INV-6's truthy hole and give the placeholder vocabulary one home"
board_lifecycle: renamed: 0; already correct: 0; refused: 0; 218 to rename; projected cost 436 GraphQL points (2 points per rename, measured 2026-08-22 at f5f5185)
