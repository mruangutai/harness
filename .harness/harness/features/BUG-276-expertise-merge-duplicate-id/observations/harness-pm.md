# Observations - harness-pm

- 2026-09-07: BUG-276 — a plan proposal draft cannot be written as notes/*.yaml; check-domain grants pm only notes/research-*.md, so the plan-merge --proposal draft goes in a .md file holding pure YAML (plan-merge is extension-agnostic, cmd_apply just open()s the path).
- 2026-09-07: BUG-276 — plan-merge apply bootstrapped a brand-new plan.yaml with the proposal carrying NO approval mapping; it spliced approval status pending right after the feature line itself. The observations dir must be mkdir-ed first or observations-merge dies on the lock file.
- 2026-09-07: BUG-276 cycle 2. A verify whose first line is the command and whose later lines are English reports the LAST command's status: the prose runs as commands, exits 127, and a correct implementation reads FAIL. Rewrote both blocks as out=$(cmd) && printf '%s\n' "$out" | grep -q '^PASS  <name>' per BUG-1302 plan.yaml:92-95.
- 2026-09-07: Both target suites (tests/unit/test-expertise-ops.py, tests/integration/test-expertise-merge.py) exit 0 at 6d969ed3, so suite-exit alone is a non-discriminating verify; the per-case greps carry it. Proved: each block loaded from plan.yaml and run via bash -c exits 1 today, and the six marker names through the suites' own print(f"PASS  {name}") satisfy every grep.
- 2026-09-07: Both suites print PASS with TWO spaces before the check name; a one-space grep pattern would never match. Read the printer; do not copy the spacing of a shipped pattern from another suite.
- 2026-09-07: plan-merge amend --expect-sha256 --value-file is the route for revising an existing task's verify or intent; apply exits 7 CONFLICT on a changed value. Grep-marker strings must be written into intent verbatim, or the member invents its own check names and the verify can never green.
- 2026-09-07: BUG-276 goal-check — dating the tool decided the field-evidence question outright: `git log --diff-filter=A` put expertise-merge.py at 2026-08-21, nine days after the 2026-08-12 losses the issue cites as evidence for the defect. Commit dates on both sides beat any amount of code reading.
- 2026-09-07: BUG-276 — the plan's own docstring task (add exit codes 9/10/11/12) contradicted its own SC-06 ("no code listed that apply cannot return"): 10 and 12 are ops-only. Reading a criterion's negative clause against the task that traces to it is where plan self-contradictions live.
- 2026-09-07: BUG-276 — the untouched silent-loss route was parse/render, not the union: parse_expertise keeps only ENTRY_RE/SECTION_RE matches and render emits only what was parsed, so `- ARCH-01: x` or `- P-01:` (no space after colon) disappear at exit 0 on both apply and ops. Probing the parser with deliberately nonconforming base lines found it in one call.
- 2026-09-07: BUG-276 plan fix. Strengthened two `verify:` && chains with a final behaviour
  assertion. Proved each new conjunct RED standalone first, because on the pre-fix tree an earlier
  grep conjunct exits before the new one runs, so its discrimination would have been assumed and
  never observed (O-04 in the field).
- 2026-09-07: BUG-276. Amending one paragraph of a long `intent` by hand risks silent drift in the
  untouched prose. Spliced it in python via `safe_load` on the field, then diffed the new value
  against the pre-amend `--show` output: the diff confirmed the change was confined to STEP 3, and
  it caught a dropped blank line before CONSTRAINTS that no reading would have noticed.
- 2026-09-07: BUG-276 panel transcription — `amend --show` prints the DEDENTED field value but the sha256 it prints is over the RAW file block (`intent: |` header plus indentation, plan-merge.py:1581). Rehashing the shown text to "confirm" the pin fails; pass the printed hash through verbatim and prove identity by diffing the post-write `--show` against the intended string instead.
- 2026-09-07: BUG-276 — the safe way to amend one sentence of a long block scalar: capture `--show`, strip only its trailing `sha256: ` line, do a single asserted-count-1 string replace, write that as `--value-file`. Nothing else can drift, and the post-write `--show` equality check then proves it rather than asserting it.
- 2026-09-07: BUG-276 — `set-panel` splices `panel:` before `tasks:` when the key is absent, so a plan with no panel lands it top-level between `decisions:` and `tasks:` with no ordering work. `panel_findings.py id` returns a 32-hex digest, not the template's 8-hex illustration; copy the tool's output, never the template's shape.
- 2026-09-07: BUG-276 — a finding a plan decision "settles" still stays `disposition: open`: D-09 records the exclusion and its cost, but only `approval.rulings` accepts the risk, and that is the main session's write. Closing it at transcription would have manufactured the operator's consent.
- 2026-09-07: BUG-276 panel record — set-panel REPLACES the whole mapping, so I built the value file with a throwaway python script that loads the existing panel and appends, rather than retyping carried rows. Retyping is where a summary gets reworded and its PF- content hash silently moves. Recomputed all nine ids from the summaries AS STORED after the write; nine of nine matched.
- 2026-09-07: BUG-276 — a plan panel's PRODUCT segment (goalcheck, run by me) is a reader INV-32 expects by name, and its findings do not reach the record unless I transcribe them. Two prior runs recorded only the validator segment's two readers, so a third of the findings were invisible until this completion run.
