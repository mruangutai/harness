# Observations - harness-orchestrator

- 2026-09-07: BUG-276 validate fix cycle. A panel FAIL on a MECHANICAL complexity grade needs no re-derivation and no debate — code-grade.py names the function, the driver metric and the bar, so the whole dispatch is 'extract a helper in the shape of the file's existing sibling helpers, keep every proposition'. The ONE thing that made it safe was pairing the LEAVE list (case_u23 closed at grade 2 with a reason; production untouched; case27b signed-REJECTED for deletion) with an ENUMERATION of the nine propositions that had to survive; the dev's receipt then mapped each one to its post-refactor line, and the reviewer could check a list rather than form an opinion.
- 2026-09-07: BUG-276. code-grade.py takes POSITIONAL PATHS as well as --base/--head, so a fix can be graded in the working tree BEFORE it is committed — but the whole-file mode also surfaces every pre-existing sub-bar function in that file (six here), which the diff mode correctly excludes. Grade in file mode to iterate, then re-grade in --base/--head mode to state the gate's actual answer.
- 2026-09-07: BUG-276. Closed an inherited UNVERIFIED redden-ability claim WITHOUT editing a tracked file: the integration test resolved its CLI through 'os.environ.get("EXPERTISE_MERGE_BIN") or <default>', so cp -R of the bin dir to a temp dir + one sed disabling the guard + the env override gave a real mutation run (9 FAIL / 0 PASS). Look for an env-override seam in the test harness BEFORE reaching for a backup-mutate-restore of source; it makes the probe zero-risk instead of merely reversible, and git diff --stat HEAD proves it.
