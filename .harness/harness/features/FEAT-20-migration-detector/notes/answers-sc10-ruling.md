# Operator ruling — SC-10, 2026-08-14

Asked directly with three options (rule the reading; reword to the outcome form; reword to
the split form). The operator ruled: **rule the reading.**

**The ruling:** SC-10 (BRIEF.md:189-191) judges the SHIPPED CODE surface — the eight files
it names, which is exactly what shipped (verified: 27 changed paths at 88b1182..434307a,
the 8 outside .harness/ map onto the closed set one for one, --diff-filter=R empty).
Per-feature bookkeeping under .harness/features/<FEAT>/ — status, state, task statuses,
run digests, receipts, review notes, handoff notes — is exempt by nature: the harness
writes it on every feature and no feature can avoid it.

The signed text stands as written. With this ruling, SC-10 is MET. 15 of 15.

**Carried, not ruled here:** the class-level convention question (containment criteria
stated as outcomes rather than file enumerations) rides to the briefing's backlog.
