# Observations — harness-qa — FEAT-55-issue-types-created-work

- 2026-09-05: full-diff qa gate (eb9d044e..7dc0ee92) resolved FAIL — required `integration` kind red on `tests/integration/test-anchor-directions.py`, which caught two unanchored instruction paths T-12 added to github-mirror.md:19,24 (`.claude/skills/harness/bin/gh_issue_types.py` and `.harness/harness.json`, missing the `<HARNESS_CONTROL_PLANE_ROOT>/` anchor). Confirmed via `git diff eb9d044e..7dc0ee92 -- github-mirror.md` that both flagged lines are net-new to this feature, not pre-existing drift. All other required/named suites (unit driver, integration driver's other 40+ scripts, and the seven individually-named integration/unit files) measured 0 FAIL, corroborating the orchestrator's earlier claim independently.
- 2026-09-05: this feature's `config` task (T-09) adds a wholly new `test_kinds` entry mirroring an existing sibling shape (`locally_run` kind with detect/exclude/cmd/status/runner_note) — judged `touches_config_shape: false` since no existing key's container type/required-ness/nesting changed, only a new instance of an already-recognized shape was added. Moot here since a sibling `cross_module` task already floors `integration`, but worth remembering as a concrete instance of the DEC-212 boundary (additive net-new key mirroring an existing shape != a shape change).
