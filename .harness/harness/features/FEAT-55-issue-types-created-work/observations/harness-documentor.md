# Observations - harness-documentor

- 2026-09-05: FEAT-55 T-11 — an `edit` hashline section path resolves against the PROCESS cwd, not the dispatch worktree. All four hunks landed in the MAIN tree's DECISIONS.md while the snapshot tag still matched, because the worktree copy was byte-identical (G-18 exactly). Detected only because `git -C <worktree> diff --stat` printed nothing afterwards. Recovery: copy the edited main-tree file into the worktree (safe only after `git show HEAD:<path> | cmp - <worktree copy>` proved the worktree file unmodified), then restore the main tree from HEAD. Use ABSOLUTE paths in every hashline header when a worktree is in play.
- 2026-09-05: FEAT-55 T-11 — `gen-decisions-index.py` preserves the hand-written ruling right of ` :: ` by reading the EXISTING index (`:198-213`, `parse_existing_index` at `:218`), so a count inside a ruling ("bounded to seven purposes") is invisible to regeneration and stays false forever unless hand-edited first. Editing the ruling then regenerating keeps the `--stdout | diff` clause clean.
- 2026-09-05: FEAT-55 T-11 — proving an intent-mandated one-physical-line string inside a hard-wrapped doc: `awk length` on the grep'd line number, PLUS a no-DOTALL `re.search` byte-comparison against the plan's pin, PLUS an occurrence count (must be 1) so the non-greedy regex cannot start at a second copy.
