# Observations - harness-code-reviewer

- 2026-09-05: FEAT-55 c1 review: mechanical code-grade.py caught a real production regression (factory_decompose.py:208 write_factory.transform, grade4->grade3, ABC 20.5) introduced by this diff's own added typed-field line — the four functional leads (provenance-by-value, refusal ordering, fail-open, test strength) all held under live-binary integration tests, but the grader is what caught the one thing manual reading of "looks fine" code missed; running it is not optional even when the functional review is clean.
- 2026-09-05: found a stale false comment surviving in tests/manual/probe-issue-types.py:70-73 describing a --repo GraphQL flag defect that operator ruling F-02 explicitly fixed and had its comment deleted in gh-sync.py — the fix touched only the file the ruling named (gh-sync.py+test-gh-sync.py per D-22/T-04) and never swept for a second copy of the same false claim in a sibling file (T-10, an unrelated dependency chain). Worth grep-sweeping for a fixed defect's own description text across the whole tree, not just the file the fix ticket named.
