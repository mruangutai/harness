# Observations - harness-data-engineer

- 2026-09-05: FEAT-55 SIMPLIFY altitude pass: gh_issue_types.py's `_resolve` is the stated authoritative helper for the change_type/nature override lookup, but `type_for_nature` reimplements it inline instead of calling it (gh_issue_types.py:66-76) — the copy-paste already drifted: its raised message wrongly names "change_type"/"DEFAULT_TYPE_BY_CHANGE_TYPE" for a nature input. Good template case of "shared home exists, caller re-derives the rule anyway."
- 2026-09-05: D-20's legal-typed-value set (True|"created"|"adopted") is validated on read in factory_decompose.py's read_factory (filters unknown values) but NOT in gh-sync.py's load_recorded (accepts any value verbatim under the typed dict). No live bug today because every downstream comparison is a positive `== "created"`/`is True` equality check, so an illegal value degrades safely to "not needing type" either way — but it is an asymmetric enforcement of the same rule across the two receipt readers, worth centralizing in gh_issue_types.py if either file gets touched again.
