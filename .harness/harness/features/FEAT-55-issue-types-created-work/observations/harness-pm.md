# Observations - harness-pm

- 2026-09-04: FEAT-55 — plan-merge.py apply DOES create a plan.yaml that does not exist yet: apply_merge (plan-merge.py:602-630) seeds `approval:\n  status: pending` after the top-level `feature:` line and exits 0, provided the proposal carries NO `approval` key (exit 8 otherwise). My repository-tier expertise entry G-07 ("write the file directly, apply exits 8 on an absent base") is falsified at this sha; the exit-8 rule applies only to a proposal that carries an approval mapping. Measured: APPLIED on a first-ever plan.yaml, `approval: {status: pending}` on reload.
- 2026-09-04: FEAT-55 — check-plan-routes.py prints `OK T-12: declared main-session-direct (... ungranted)` rather than DEVIATION for a references/** task, and exits 0 with the lane rows the orchestrator resolved with check-domain.sh --resolve. Passing the plan path (not the feature dir) keeps other features' lines out of the output.
- 2026-09-04: FEAT-55 plan goal-check — the receipt-only recovery backfill ("recorded number, no typed flag -> apply the type") silently reaches ADOPTED parents, because the receipt records adopted and created numbers identically (gh-sync.py:496-497, factory_decompose.py:404-407). Grade every "backfill from the local receipt" rule against the receipt schema, not against the branch that wrote it.
- 2026-09-04 (FEAT-55 plan repair c1): a receipt that stores a bare number cannot distinguish adopted from created, so any "backfill what is untyped" rule silently retypes adopted work. The fix that survives review is a two-valued marker read by KEY PRESENCE, never truthiness, plus a cross-run test (adopt, then rerun without the adopt flag) - the single-run test passed while the leak was live.
- 2026-09-04 (FEAT-55): gating a net-new receipt to the new mode (F-07) removed the only zero-create path on that command, which falsified an "exactly one diagnostic per invocation" claim I had already written into two tasks. Two repairs interacting is what made the claim unmeetable; I substituted a capability-query count and said in the plan that the printed-line leg is unreachable there.
- 2026-09-04 (FEAT-55): plan-merge.py amend shas are per-field, so a batch of --show calls collected up front stays valid across the whole amend run - 20 amendments, no re-show needed.
- 2026-09-04: FEAT-55 c1 goal-check — a repair that adds a NET-NEW receipt key and then reads it with a key-presence test silently reclassifies every pre-existing record: 41 of 41 feature.json files carried a numeric github.parent with no typed key, so the adopted-parent exemption bounded only future adoptions. DECISIONS.md:2977-2979 had already measured the identical failure for parent_origin (null on hand-recorded parents). Grade a new receipt field against the receipts that already exist on disk, not only against the flow that writes it.
- 2026-09-04: FEAT-55 c1 — when a feature directory is untracked, `git diff -- <path>` returns nothing and reads as "no change"; sha256sum before/after plus the `??` status line is the only available byte-unchanged proof, and the edit's own line range is the only diff extent. State which one you used.
- 2026-09-04: FEAT-55 c2. A net-new receipt field read by KEY PRESENCE silently reclassifies every
  record written before it existed (41 legacy feature.json here). The safe shape is to write the
  provenance value in the SAME save as the identifier and treat absence as unknown-never-act; and
  the repair is only real once the fixture cases that asserted the old ordering (T-03 G, T-07 E)
  are corrected in the same edit as the new ones (T-03 J, T-07 H).
- 2026-09-04: plan-merge amend --show prints the field plus its sha256; batching five --show calls
  in one shell loop then five amend calls is the cheapest route for a multi-field plan repair.
- 2026-09-04: FEAT-55 plan goal-check c2 — the repair that added the fail-safe (absent provenance never typed) was pinned by no gate: T-03/T-07 verify blocks are RED-checks that any failing assertion satisfies, and the REQ the property serves (REQ-10) carried no SC at all. Grep the BRIEF for each REQ id when grading a repaired plan; a REQ traced by tasks but named by zero SC is a property nothing reddens.
- 2026-09-04 (FEAT-55, plan repair c3): a red-check verify of the shape 'ast.parse + non-zero exit' is satisfied by ANY failing assertion, so a fixture file that OMITS a case greens it — proved by running the old T-03 block against a stand-in missing case H2 (exit 0). Content-string greps could not separate H from H2 or J from H2 because both cases assert over the same literals (4242, adopted); a per-case 'CASE <id>:' marker loop with a colon terminator was the only portable discriminator (grep -qF, no word boundaries, BSD/GNU alike). Same inspection found T-01's fifteen-value loop covered assertion groups 1-5 only — group 7's classify_capability discriminator was ungated.
- 2026-09-04: panel transcription — driving panel_findings.py from a script over yaml.safe_load of the source, then RE-DERIVING each id from the STORED reader+summary after the write, is what makes "verbatim" falsifiable; comparing the stored strings `==` to the source objects catches reflow that a visual diff of quoted YAML does not.
- 2026-09-04: plan-merge.py set-panel type-checks only last_run(str)/cycle(int)/readers(list)/findings(list) and reload-identity; severity_max, must_fix, fix_order, dispositions and finding shape are UNCHECKED, so the transcriber's own post-write audit is the only gate on them.
- 2026-09-04: BRIEF.md unchanged could not be proven via `git show HEAD:` — the whole FEAT-55 feature directory is untracked (`git status --porcelain` shows `??` on the directory), so sha256 before/after was the only available method.
- 2026-09-04: FEAT-55 goal-check c3 — when a ruling says "add coverage to routes B and C", the repair gates B and C harder than the already-covered route A (T-03's required-string loop lacks the markers T-05/T-07 gained). Check the untouched route's verify against the new bar, not just the edited ones.
- 2026-09-04: cycle-2 panel transcription. No pre-write copy of plan.yaml was kept and HEAD was stale (a plan revision landed in the working tree after the commit), so "nothing outside panel: moved" was proven by splicing HEAD panel: back into the current bytes and hashing to the recorded pre-write sha256 — cheaper and stronger than a field-by-field diff. Keep a pre-write byte copy next time anyway.
- 2026-09-04: panel readers[] are written with a `step:` key, but check-state.sh:537-540 keys INV-32 reader lookups by `reader:` — a signed plan with a correctly recorded panel would report every expected reader as never-ran. Raised as Q1 in the c2 note; not fired yet because INV-32 grades approved plans only.
- 2026-09-04: plan-merge amend takes the field value as PLAIN TEXT in --value-file (no block indent, no `|`); the safest way to build one is a python script that loads the plan with harness_yaml and string-replaces inside the loaded scalar, since bash-write-guard denies redirecting `--show` output to a file.
- 2026-09-04: a "require flag value to EQUAL configured repo" safety clause lived in exactly one task intent and in NO decision — walking the whole decisions list to prove an empty changed-set took one grep and is worth stating explicitly in the note.
- 2026-09-04: FEAT-55 goal-check c4. A required-string loop that greps the whole test FILE is parity, not per-case binding: `partial`/`github.issue_types` are already present in each fixture-fake description, so the folded low finding equalised the gates without strengthening any of them. Worth saying out loud when grading a "now matches" repair.
- 2026-09-04: FEAT-55. When a ruling widens a flag's target, the adjacent UNCHANGED surfaces are the judgement: T-10 verify, T-09 runner_note and BRIEF verification-gaps each describe the DEFAULT invocation, which the ruling did not touch — so "still says configured repo" was correct, not stale. Decide by asking which invocation the sentence describes.
- 2026-09-04 (FEAT-55, panel c3 transcription): when two readers give one defect different severities, cycle 2's precedent (PF-8b5853) is a reader: string carrying both stated values plus the reconciliation note, with the scalar severity: set to the higher one. Averaging, reassigning or `unrated` all destroy information the operator rules on; `unrated` is read as gating-equivalent to high.
- 2026-09-04 (FEAT-55): set-panel validates only last_run:str, cycle:int, severity_max, readers:list, findings:list — extra sibling keys (fix_order_reason, transcription_rule) pass through untouched, so ordering rationale need not be smuggled into the fix_order strings the way cycle 2 did.
- 2026-09-04 (FEAT-55): all six carried PF- ids re-derived clean from their stored reader+summary; the digest deliberately assigns no ids, so pm is the single point where identity is minted.
- 2026-09-05: FEAT-55 c6. plan-merge.py `amend` replaces a WHOLE field, so a one-clause ruling on a 100-line `intent:` means retyping the block; `--show` dedents the block scalar, so the value-file is the dedented text and the tool re-indents. Twelve amends, no conflicts.
- 2026-09-05: FEAT-55 c6. A narrowing ruling (sixteen override keys to four canonical names) is never one edit: the sweep reached D-02, D-12 choice+because, T-01 assertions 5/6/12, T-02, T-03 case E, T-04 section 4, T-07 case D, T-08 section 6. Check every `verify:` string loop for a pinned key literal FIRST — here none broke, because the canonical re-expression happened to preserve Defect/Story/Epic/Maintenance/IT_story.
- 2026-09-05: FEAT-55 c6. Making an inert assertion falsifiable needed a spec edit the ruling did not name: T-03 case F's pre-seeded remnant only discriminates if T-04 section 6 says the backfill runs AFTER the refusal check. An assertion added without the ordering it grades is still inert.
- 2026-09-05: FEAT-55 c6. bash-write-guard reads a heredoc (`<<'EOF'`) as a redirect and blocks the whole call; observations-merge.py --entries takes a file path, so write the entries with the Write tool and pass the path.
- 2026-09-05: FEAT-55 goal-check c5 — a ruled NARROWING of a configuration surface can silently unmeet an SC that enumerates cases: R3 cut sixteen override keys to four canonical names, and the only two integration override fixtures (T-03 case E, T-07 case D) then covered two of SC-06's three legs (chore leg absent everywhere; T-07 D even instructs 'Do not override Task'). Grade an enumeration leg by leg after any surface narrowing.
- 2026-09-05: FEAT-55 — check-plan-routes.py has no --help (prints 'ERROR: --help does not exist'); it takes the plan path positionally and printed '0 violation(s) across 1 plan(s)' at c6.
- 2026-09-05: FEAT-55 — pm's own c6 repair note misnamed the BRIEF edit as SC-12 in its opening paragraph while the diff and its own later section say SC-10. Verify a repair account's headline against the diff, not only its body.
- 2026-09-05: FEAT-55 c7 — plan-merge.py amend --show reports a sha256 that does NOT match sha256 of the value yaml.safe_load returns for the same field (block-scalar trailing-newline handling). Always pass the sha --show printed, never one computed locally.
- 2026-09-05: FEAT-55 c7 — a required-string grep loop pinning type IDS is not parity-satisfied by adding the type NAME: chose IT_maintenance over Maintenance because the acceptance demanded assertion by type id and no type name was pinned in that loop.
- 2026-09-05: FEAT-55 panel c4 transcription — hand-typing a finding's `why` from a validator digest introduced an em-dash-to-hyphen substitution that only a programmatic compare caught. Loading the digest's own `panel_findings` YAML block and copying the mapping is strictly safer; summaries matter doubly since the id hashes them.
- 2026-09-05: FEAT-55 — the digest's `panel_findings` line for finding 2 is 895 chars and complete in the file; the read tool truncates lines at 768 chars, which looks exactly like a truncated source. Confirm length in python before concluding a source is truncated.
- 2026-09-05: FEAT-55 — INV-32 (check-state.sh:533-546) keys panel readers on `reader:`, not `step:`, and demands all three of should-not-exist/scope/goalcheck. A validator digest names its members with `step:`, so transcribing the digest's own key verbatim produces three BAD entries at signature. FEAT-52's signed plan.yaml:1235-1244 is the correct shape.
- 2026-09-05: plan-merge amend --key accepts only tasks|decisions (plan-merge.py:1232); a panel finding's disposition can only be changed with set-panel, which replaces the whole panel mapping. After a set-panel round trip I re-derived all 11 PF- ids with panel_findings.py id to prove no summary string moved.
- 2026-09-05: amend replaces a WHOLE field, so a surgical prose change means reconstructing the field. Building the value file with a python substring replace that refuses unless the pattern occurs exactly once caught two wrong assumptions about line wrapping in the same run - the block's hard-wrapped line breaks must be reproduced character for character or the pattern silently misses.
- 2026-09-05: FEAT-55 c8 grade — a repair note that claims a field ("authority: <file>" on each accepted panel finding) is worth loading the plan with safe_load and printing the finding keys: the disposition had landed, the field never existed. Claim-vs-source checking catches the shape of the claim, not only its truth.
- 2026-09-05: FEAT-55 c5 panel transcription — check-state.sh INV-32 SKIPS any plan whose approval is not approved (check-state.sh:424-427), so a clean run over a pending plan is silence, not proof. Applied the INV-32 predicates directly to the stored block with a step:-keyed positive control (3 BADs) to show they discriminate.
- 2026-09-05: plan-merge.py amend --show REFUSES a list field without --yaml-value (exit 4, "is a list, not text"); the flag is needed on the read as well as the write. Cost one wasted invocation on FEAT-55 T-04.files.
- 2026-09-05: before amending a long block-scalar intent, diff the pre-amend value against the draft with difflib and count hunks. On FEAT-55 T-04 (137 lines) it proved the three ordered edits and nothing else, which no post-hoc read of the file could show.
- 2026-09-05: `git diff -U0 <plan> | grep -E 'approval|PF-|rulings'` exiting 1 is the cheapest proof plan-merge preserved the signature bytes (BUG-1128) — cheaper and stronger than re-reading the approval block.
