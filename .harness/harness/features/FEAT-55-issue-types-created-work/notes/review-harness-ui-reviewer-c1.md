# UI Review — FEAT-55 issue-types-created-work — cycle 1 (Mode B)

**Verdict: PASS.** No rendered UI surface in scope; the one adjacent CLI-message surface named
in the dispatch was audited directly and found sound.

## What I checked

**DESIGN.md presence** — `git show cd6a3c0d:.harness/harness/features/FEAT-55-issue-types-created-work/DESIGN.md`
→ `fatal: path ... does not exist in 'cd6a3c0d'`. Confirmed by listing the feature dir at the pin
(`git show cd6a3c0d:<feat-dir>` → `BRIEF.md STATE.md feature.json notes/ observations/ plan.yaml`,
no DESIGN.md). No design contract exists for this feature.

**Rendered-surface census** — `git diff --name-only <merge-base> cd6a3c0d` (81 files) grepped for
`html|css|scss|tsx|jsx|vue|svelte|less`. One hit:
`notes/ship-review-2026-09-05-01-eng.html`. Read it: last line reads `Derived from
ship-review-2026-09-05-01-eng.md — the markdown is the record; do not edit this file. Regenerate
with bin/render-brief.py.` — a generated ship-review report with a regenerate footer (repo
Expertise P-02), not authored product UI. Out of scope.

**Adjacent CLI-message surface (dispatch-named candidates: the `typed` receipt and any new
operator-facing message)** — audited directly rather than declined:

- `feature.json`'s `factory.typed` block is a JSON provenance map (`{"parent": "created", "T-01":
  "created", ...}`), not rendered/formatted text — no typography, spacing, or colour surface to
  audit. No finding.
- New operator-facing print statements, read at the pinned commit in
  `gh-sync.py:922-925` (`detect_issue_types`), `factory_decompose.py:370-380`
  (`_print_issue_types_state`), and `gh_issue_types.py:refusal_text` (shared by both
  `gh-sync.py:976` and `factory_decompose.py:417`):
  - Capability-absent/query-failed fallback lines (`"issue types unavailable on {repo} - labels
    only"` / `"...could not be determined on {repo} ({message}) - labels only"`) are byte-identical
    between the `gh-sync:` and `factory:` producers apart from their tool prefix — no wording
    drift between the two call sites.
  - The refusal message (`refusal_text`) names a concrete remedy for both failure paths it is
    reached from (task and backlog refusal in `gh-sync.py`, task refusal in
    `factory_decompose.py`): *"set github.issue_types.{key} in .harness/harness.json ... or add
    the type to the repository"* — satisfies G-13 (a message stating only the fact, with no
    remedy, is the common defect class this checks for).
  - Severity-to-stream placement matches each file's own pre-existing convention, checked against
    live examples in the same files: `gh-sync.py` prints informational/degraded-capability lines
    to stdout (matching the existing `"gh-sync: no github.board configured"` pattern at line 323)
    and reserves stderr for `ERROR —`-prefixed lines; the new capability-absent lines follow the
    stdout/informational pattern correctly. `factory_decompose.py` prints all of its new
    diagnostic and refusal lines to stderr, matching its existing `"factory: {TOOL}: {e}"` /
    `EXIT_REFUSED` convention.
  - No ANSI/colour escapes introduced (grepped `\033|\x1b|ANSI|color\(|termcolor` across the four
    touched Python files — no hits), so no colour-only state encoding and no dark/light theme
    parity question — these are plain stdout/stderr lines with no terminal styling.

## Accessibility / theme parity

Not applicable and stated as such per Gotcha G-02: the only in-scope text is plain, unstyled CLI
output with no colour-only encoding and no rendered markup — there is no contrast or theme
dimension to evaluate.

## Findings

None. `must_fix: []`.

## Note (informational, not a finding)

`feature.json` shows as locally modified in the working tree outside this diff — a concurrent
sibling process's edit, not touched by this review and not part of the pinned commit's content.
