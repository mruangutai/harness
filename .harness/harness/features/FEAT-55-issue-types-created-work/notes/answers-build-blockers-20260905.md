# Operator rulings — FEAT-55 build blockers — 2026-09-05

- F-01: **Approve.** Amend task authority for `.claude/skills/harness/bin/feature-schema.json`, then add optional `typed` mappings to both closed `github` and `factory` schema objects. The mapping accepts provenance strings and the promoted boolean.
- F-02: **Approve.** Remove unsupported `--repo` from the GraphQL capability call, delete its false explanatory comment, and amend the existing test assertion to verify the exact GraphQL owner/name values rather than flag presence.
- F-03: **Backlog.** SC-10's recorded capability-absent verdict satisfies its signed criterion. Track the optional live available-path probe explicitly against an organization-owned, Issue-Types-enabled repository.
- F-04: **Approve.** Raise the feature budget to `max_total_cycles: 13` and `max_total_runs: 40` for this amendment and remaining gates.
