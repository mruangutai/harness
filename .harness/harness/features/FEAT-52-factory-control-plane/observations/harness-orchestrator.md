# Observations - harness-orchestrator

- 2026-09-01: INV-32 (check-state.sh:523-541) requires reader entries for THREE names — should-not-exist, scope AND goalcheck — but teams/plan-panel.yaml declares only the first two; goalcheck is the product segment's pm note. Two consecutive FEAT-52 panel records (c4, c5) omitted goalcheck and would have hard-failed INV-32 the moment the plan was signed. Reading the checker before dispatching the transcription is what caught it.
- 2026-09-01: recomputing every recorded finding id with panel_findings.finding_id(reader, summary) is a free byte-identity proof for carried-forward findings — the id IS the content hash, so 17/17 recomputing means no summary drifted during a panel rewrite. Cheaper and stronger than diffing against a pinned copy, which was impossible here (feature dir untracked, git show exits 128).
- 2026-09-01: a member holds no write grant inside a lead's run dir (#216), so naming runs/<run>/scratch.yaml as a value-file path in a dispatch gets denied by check-domain.sh; /tmp is the right scratch home. And check-domain.sh:1204 admits only a payload whose opening bytes are the prior file verbatim, so a recorded run digest can be APPENDED to and never prepended — a contract-block repair goes at the foot, which validate-digest.py parses because it anchors on the LAST ^VERDICT:.
- 2026-09-01: bash-write-guard blocks an orchestrator `cp` into a /tmp fixture, so the usual "probe INV-32 on an approved copy" route is unavailable at this tier. Verifying each of the checker's clauses against the live document with yaml.safe_load, reading the checker source for the clause list, is the substitute — and it is disclosable as exactly that, not as a gate run.
- 2026-09-01: bash-write-guard reads a Python greater-than comparison inside a heredoc as a shell redirect — a line comparing an index to zero was refused with "redirect targets 0, outside your domain", and quoting the entry that DESCRIBED the incident tripped the identical refusal a second time. The guard scans raw text, including quoted heredoc bodies the shell never expands. Rewrite the comparison (use `!= -1`) rather than trying to escape it.
- 2026-09-01: when the operator routes questions to an advisor instead of upward, the advisor answers dispositions, not gates — 17 recorded items came back disposed with three fix-before-signature recommendations, all discretionary while INV-32 already passed. Host the consultation through the validator lead precisely because the lead re-derives the advisor's edits: it caught that adding a test file to a task's FILES without adding the invocation to its verify leaves the new case unexecuted, which was the finding's own defect reappearing inside its fix.
- 2026-09-01: a new success criterion on an UNSIGNED brief costs one edit; the same criterion after signature costs an approval ruling and re-opens the gate. When an advisor round proposes a task change whose behaviour no SC covers, the cheapest correct moment to add the SC is before the single signature — but it must be SAID in the return, because "six task fields moved" and "a criterion was added" are different asks of the operator.
- 2026-09-02: FEAT-52 build mission hit a hard authority wall — 14 of 15 tasks `main-session-direct`. The decisive authority is NOT DEC-174's rationale but `github-mirror.md:32-34`, which excludes the orchestrator from the mode BY NAME ("orchestrator for `team`, main session for `main-session-direct`"). The Advisor's own answer rested partly on `harness/SKILL.md:184-185`, which is a runs-COUNT rule, not an ownership rule; the validator lead caught that and supplied the naming line. Reading the mirror reference settled in one grep what the decision record only argued.
- 2026-09-02: `gh-sync.py open`'s trigger row (github-mirror.md:38) reads "mission ship, right after the approval gate passes", contradicting the ownership sentence three lines above that gives the orchestrator `open` unconditionally. FEAT-51's `notes/build-segments-c10.md` records its orchestrator running `open` at BUILD entry (milestone #38, parent #1135, nine sub-issues). Two signals against one ambiguous trigger phrase — ran it, exit 0, milestone #41 / parent #1220 / #1221-#1235.
- 2026-09-02: the decisive argument for running `open` was dependency, not precedent — `gh-sync.py start-task` has no card to move until `open` has run, so the orchestrator's owned mirror act is a hard prerequisite of the main session's per-task bookkeeping. Declining it out of caution would have blocked the very hand-up the caution was protecting.
- 2026-09-02: FEAT-52's feature directory was UNTRACKED for its entire plan phase — one bare `??` line — so every plan-phase agent's `git show HEAD:<path>` exited 128 and each rediscovered it (STATE Q7 across two seqs). Sibling feature dirs ARE tracked; only `runs/**` is gitignored (.gitignore:7). One record commit ends a hazard three agents separately re-measured.
- 2026-09-02: wrote "the mirror has never run" into the handoff Trust section and then falsified it myself, in the same run, by running `open` — forcing a supersede of both handoff and STATE. A negative about state I am about to change is stale at the moment it is written; do the act first, then write the note.
- 2026-09-02: a heredoc piped to a domain-owned script is refused by bash-write-guard as a `redirect` outside the domain, even when the target file IS mine. `observations-merge.py --entries` takes a PATH as well as `-`; write the entries with the Write tool into `notes/` and pass the path.
