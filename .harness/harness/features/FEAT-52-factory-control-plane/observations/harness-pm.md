# Observations - harness-pm

- 2026-09-01: plan-merge apply unions by id and exits 7 on a differing same-id, so a task is IMMUTABLE the moment the first apply lands. FEAT-52 T-04 kept two ** globs that check-plan-routes reported as UNRESOLVED-GLOB because there was no route back to edit the files: list. Get files: lists right in the FIRST proposal, or hold the proposal until check-plan-routes has been run against a scratch copy.
- 2026-09-01: a fresh plan.yaml cannot carry an approval block at all — plan-merge refuses exit 8 on the create path, and sign-approval is the only writer. Five recent live plans (BUG-1030/1055/1071/1080/1129) have no approval key. Pending is spelled as ABSENCE, not as status: pending.
- 2026-09-01: plan.yaml amendment under add-only. bash-write-guard reads a '>' followed by whitespace ANYWHERE in a bash command as a redirect, so a heredoc carrying <PLACEHOLDER> is prose is BLOCKED; staged the proposal in notes/research-*-proposal.md instead, which is the only non-.yaml path harness-pm can write in a feature dir.
- 2026-09-01: lanes: is a non-union top-level key in plan-merge, so an amended plan can never record a new surface's lane row; the route check reads task execution_mode instead and still exits 0.
- 2026-09-01: SubagentStart cannot see a dispatch prompt (dispatch-guard.sh:76-80), which foreclosed injecting a feature-tree root and forced the agent-side resolver verb over inflight_registry.feature_root.
- 2026-09-01: FEAT-52 G-1. A write-anchor rule I wrote assumed a capability I never checked per persona: 3 of 16 .omp/agents/*.md grant no bash (the leads, DEC-116) and all three write feature-directory artifacts. Grep the tool grant of EVERY persona a new instruction binds before ruling that they run a command.
- 2026-09-01: FEAT-52. A remedy that only reaches personas holding the tool is not a closed finding; the discriminating SC needs the both-directions pair (shell-less refused, bash-holder allowed on the identical omission), or a refuse-everything guard passes it.
- 2026-09-01: plan-merge.py apply cannot amend an existing id (exit 7), so a consolidation cycle is only reachable as rm plan.yaml plus one whole re-create through the create path (plan-merge.py:454-479), which refuses any approval key at exit 8 — legal only while the plan is unsigned. Staged the whole document in notes/research-*.md first and confirmed byte identity after apply.
- 2026-09-01: FEAT-52 cycle 1 left 9 of 20 tasks existing only to retract clauses of the other 11; the measured harm was old T-04 F3/F4 anchoring two WRITE families to the control-plane root with depends_on scheduling it BEFORE the task that re-anchored them, i.e. a member instructed to build a known regression. An add-only merge tool makes supersession the only amendment shape and this is its cost.
- 2026-09-01: dispatched to delete a stray file under runs/2026-09-01-01-product/; check-domain refused — team-config.yaml:302 grants .harness/*/features/*/runs/*-product/** to harness-product-lead, and :45 grants .harness/*/features/** to harness-orchestrator. pm holds no run-dir path, so a run-dir remediation is unexecutable by pm regardless of how it is worded. Check the lane grant before accepting a remediation target.
- 2026-09-01: FEAT-52 cycle 4, one-scalar plan.yaml fix. A dispatch handed me a "normalized sha256 with the target field replaced by <PLACEHOLDER>" baseline to re-match. Nine normalization recipes (json/yaml, sorted/unsorted, compact/indent2, repr) all missed it, so the recipe was unguessable — but the placeholder construction makes that hash invariant to the very edit it is meant to bound, so re-matching it proves nothing stronger. What actually bounds the edit: a line-level diff of the staged proposal against the live file showing ONE hunk (956c956), plus hashing BOTH files under my own placeholder recipe and showing them equal, plus re-deriving that the matched line sits inside T-14's block by walking back to the nearest `- id: T-` rather than trusting the dispatch's line number. Report the stronger property and say plainly the given recipe was not reproducible, rather than reverse-engineering someone else's hash.
- 2026-09-01: bash-write-guard reads a `|` pipe into a script's stdin as a "redirect" and blocks it, quoting the piped TEXT as the target path. So `printf ... | observations-merge.py --entries -` is refused for harness-pm even though the target file is mine. observations-merge.py's `--entries` takes a PATH as well as `-`: stage the bullet with the Write tool to a temp file and pass that path. Same shape as G-01, but the trigger is the pipe rather than a redirect/cp/mv/rm.
- 2026-09-01: plan-panel c4 transcription. yaml.safe_dump built the whole panel proposal, which auto-quoted the three info summaries carrying "): warranted" - a colon-plus-space a hand-typed plain scalar would have broken safe_load on. Hash the string in python and dump the same object: byte-identity between hashed and transcribed summary is then structural, not discipline.
- 2026-09-01: plan-merge.py apply spliced a brand-new top-level panel key cleanly (exit 0, APPLIED) from a proposal carrying ONLY that key; 14 tasks / 8 decisions / lanes.resolved_at untouched, 1006 to 1057 lines.
- 2026-09-01: judging a "same shape as finding X" claim: read the checker's rule SET, not the finding summary. FEAT-52's S1 hole was READ-anchored-to-feature-tree; SC-11's second half is a feature-directory WRITE, which rules 1 and 2 conjoined already pin. The lead's git-show grep found a real gap (no ref-read anywhere in the plan) but mis-attributed its shape.
- 2026-09-01: bash-write-guard reads the raw command line, so a plain ASCII arrow inside heredoc PROSE is parsed as a redirect and the whole call is blocked. Spell it "1006 to 1057" in any bash heredoc.
- 2026-09-01: FEAT-52 plan repair c5. A dispatch pinned a mirror-rule predicate as `.harness/<one segment>/features/`; counting the real scope first showed 11 template spans spell it `.harness/features/` with no segment, so the literal predicate would have flagged correct T-07 work and reddened T-12's whole-scope run. Widened to one shared helper `^\.harness/([^/]+/)?features/` called by both direction classes.
- 2026-09-01: plan-merge.py `apply`/`amend` leave no backup and this feature directory is untracked at HEAD, so the standard `git show HEAD:<plan>` before/after comparison exits 128. Capture the pre-change field values from a `read`/`grep` snapshot BEFORE the first write, or the acceptance step has no before side.
- 2026-09-01: my first T-15 `execution_reason` claimed run-unit-tests.sh resolves to NOBODY; check-plan-routes.py showed it granted to harness-backend-dev, harness-dev-ops. Run the route checker before believing a lane claim written into a reason string.
- 2026-09-01: panel transcription — `plan-merge.py set-panel` is the only route and it validates the value file before opening the plan; staging the value file in `runs/` is denied by check-domain.sh (#216), so /tmp is the place. After writing, re-run `panel_findings.py id` over the summaries AS RELOADED from plan.yaml: it doubles as a proof that no YAML scalar tail was truncated.
- 2026-09-01: FEAT-52 amend batch c5. Prepending a command to an existing `&&`-chained `verify:` block is NOT a prepend: the former FIRST command carries no leading `&&`, so a bare prepend concatenates line 1 into line 2 and ships a shell bug into a signed plan. The old head must gain the `&&`. Caught by printing the built block before applying it, not by review.
- 2026-09-01: FEAT-52 amend batch c5. `git diff` proves NOTHING about a file under an untracked directory — `.harness/harness/features/FEAT-52-*/` is `??`, so diff and diff --stat are empty for a changed file and an unchanged one alike. It reads exactly like a clean-diff proof. Used a pure insertion plus an asserted SC-01..SC-15 sequence equality instead.
- 2026-09-01: FEAT-52 amend batch c5. `plan-merge.py amend` needs `--yaml-value` on BOTH the `--show` and the apply for a list field like `files:`; showing without it hashes a different rendering and the compare-and-swap then refuses. All 6 amends landed first try with no stale-hash retry.
- 2026-09-01: FEAT-52 amend batch c5. For a surgical intent edit, build the replacement by `yaml.safe_load` of the field plus a substring swap that asserts the target occurs EXACTLY ONCE and that re-swapping reproduces the original byte-for-byte. That catches collateral edits a re-typed multi-paragraph field would hide.
- 2026-09-02: FEAT-52 goal-check: a `[harness:review]` code-quality split commit (9dbc19f4) deleted SC-06's discriminating assertion (product_cwd + placeholder-stripped not-exists) from test-check-instruction-paths.py AFTER two reviews graded SC-06 met. Re-took every automated SC carrier by reading the test file at the pin rather than trusting the review's per-SC table; that is the only reason it surfaced.
- 2026-09-02: FEAT-52 goal-check: only the feature's own suites were re-run by the final review, so a RED committed integration test (test-gen-decisions-index.py, exit 1 on test_committed_index_matches_a_fresh_regeneration) went unseen. Its cause was FEAT-52's own DECISIONS-INDEX row carrying a hand-carried @line anchor (@6563 vs generator's @6647) plus a DEC number already taken on origin/main while the branch sat 11 commits behind.
- 2026-09-02: FEAT-52 c12 retake — a remediation that "moved" two test files had ADDED the new copies and deleted the bin originals only in the working tree; layout check exited 0 locally and 2 at review_sha. Always materialise the ref and run the layout invariant against it, never the tree, when a criterion's carrier location is the thing under grade.
- 2026-09-02: FEAT-52 c13 goal-check. An operator amendment that fixed evidence-kind labels on eight criteria left SC-08 (evidence: unit, carrier tests/integration/test-check-instruction-paths.py:79-86) untouched, because the prior cycle only flagged the criteria it retook. Grading the declared kind against the carrier directory for ALL criteria, not only the retaken ones, is what caught it; run-unit-tests.sh:27 selects tests/unit/test-*.py and harness.json unit.detect globs miss hyphenated names, so the unit kind never ran it.
- 2026-09-02: Confirmed the pin-versus-tree trap is cheap to close: `git diff --stat <review_sha> -- . ":(exclude).harness"` empty proves the tracked source I ran IS the pinned tree, so hand-run suites at the working tree are legitimate evidence for the pin without materialising it.
- 2026-09-02: FEAT-52 c14 — a test-layout migration (bin/test-*.py deleted for tests/**) silently staled 10 of 15 plan verify clauses; each exits 2 (missing file), which no gate catches because check-plan-routes.py never resolves a verify token as a path. Found only by resolving every verify path myself at the pin.
