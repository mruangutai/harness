# Observations - harness-documentor

- 2026-09-02: T-13. Repo P-02 paid off again: the worktree's DECISIONS.md topped out at DEC-210, but `git show main:` already held DEC-211, so the "next free" number read from my own branch would have collided on merge. Allocated DEC-212. Separately, `feat/FEAT-46-decision-standard` carries 396 entries topping out at DEC-535 (its DEC-212 is a renumbered DEC-07) — it is a wholesale renumber, so it reserves nothing sequentially and will conflict on merge whatever number I picked.
- 2026-09-02: T-13's signed `verify:` is `gen-decisions-index.py --check`, and that flag has never existed in either script copy — the script's own usage text explicitly says "There is no --check" and names the equivalent (`--stdout | diff -`). Exit 2 before my first edit and after, i.e. the clause can never go green. Ran the baseline first (project G-03), which is the only reason I could show the exit 2 was not my prose. A signed verify clause naming a nonexistent flag is not something a member can repair — plan.yaml is write-only through plan-merge.
- 2026-09-02: T-13's intent anchored the always-exits-0 hook contract at `DECISIONS.md:1503`. Rotten in BOTH trees: 1503 is `check-domain.sh`'s `/**` startswith bug inside DEC-101's neighbourhood. The real statement is a table row at DECISIONS.md:1388, owned by DEC-101. DEC-205's anchor-rot check is existence-plus-range only, so it cannot see this class — a line that still exists and now says something unrelated is exactly the failure DEC-205 records it cannot catch. Cited by number instead.
- 2026-09-02: Before writing prose containing `<HARNESS_CONTROL_PLANE_ROOT>/...` and bare `.harness/...` paths, checked `check-instruction-paths.py --list-scope` (62 files, T-12 made it required CI): DECISIONS.md is NOT in scope. Worth checking every time now, because an in-scope file would have turned ordinary decision prose into CI violations.
- 2026-09-02: The index ruling band (>=20 non-ws chars, <=30 words) is asserted only in test-gen-decisions-index.py:428-432 — repo G-07 confirmed. Mine measured 162 chars / 25 words. Also red-proved the drift check by mutating the row's @line to @9999 (exit 1) and restoring (exit 0); "diff printed nothing" is otherwise indistinguishable from a broken pipeline.
