# Spent — plan-merge proposal staging file, cycle 1

This file was the staging buffer for the `plan-merge.py apply` proposals of plan fix cycle 1. Every
proposal it carried is APPLIED and now lives verbatim in
`plan.yaml` as decisions D-06, D-07 and tasks T-09 through T-17. Nothing here is live.

It exists as a file at all because `plan.yaml` has exactly one write route and `harness-pm`'s only
writable staging path in a feature directory is `notes/research-*.md` — see the open question about
that in `notes/research-FEAT-52-factory-control-plane-plan-fix-c1.md`.

Read the amendment in `plan.yaml`. Read the reasoning in
`notes/research-FEAT-52-factory-control-plane-plan-fix-c1.md`.
