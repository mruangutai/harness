# Receipt — harness-documentor — FEAT-52 close distillation

**No candidate passed: all surviving documentation lessons are already represented by a sharper or equivalent Expertise rule. No expertise file changed and no merge operation ran.**

## Sources reviewed

- Primary: control-plane observations `observations/harness-documentor.md` (five observations, 2026-09-02).
- Product-note skim: `notes/review-harness-code-reviewer-planpanel-c5.md` and `notes/research-FEAT-52-factory-control-plane-goalcheck-plan-c5.md` (the unlogged candidate: a presence-only documentation probe cannot prove correct path direction).

## Counts

| Expertise layer | Patterns before → after | Gotchas before → after | Outcomes before → after | Open before → after |
|---|---:|---:|---:|---:|
| Craft (`.harness/expertise/harness-documentor.md`) | 15 → 15 | 15 → 15 | 10 → 10 | 0 → 0 |
| Repository (`.harness/harness/expertise/harness-documentor.md`) | 4 → 4 | 7 → 7 | 0 → 0 | 0 → 0 |

## Candidate selection and adjudication

Three candidates passed the initial screen; none passed durable-entry adjudication.

### Rejected candidates

- **Observations — signed verify invokes a nonexistent flag:** craft G-03 already requires a baseline run before editing, and P-17 already requires executing a tool rather than inferring its behaviour. The missing-flag incident does not sharpen either durable rule.
- **Observations — instruction-path checker scope precedes path prose:** useful only while this repository's checker and scope exist; the observed rule is a repository recipe, not durable Expertise.
- **Product notes — a presence-only documentation probe cannot prove correct path direction:** already covered by craft O-03's warning that literal, placement, and count checks do not establish substantive intent. The path-direction scenario is an instance, not a sharper rule.

### Screened-out evidence

- Sequential decision-number allocation is a confirming instance of craft P-02.
- A live but semantically rotten `file:line` anchor is covered by craft P-04.
- The index band and red-proof are covered by repository G-07 and craft P-09; measured lengths and line numbers are incident detail.

## Applied operations and changed paths

- Exact merge operations: `[]`.
- Changed expertise paths: `[]`.
- Changed path: this receipt only.
