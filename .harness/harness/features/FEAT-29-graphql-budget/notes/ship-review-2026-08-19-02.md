# FEAT-29 — GraphQL budget · status review at the qa gate

**Supersedes `ship-review-2026-08-19-01.md`.** Written because the blocking gate escalated. Not
shipped.

## The headline is unchanged and it is the whole point

**The board read costs 5 GraphQL points instead of 506, measured against the live API.** One call to
the new `project_item_stations` read all 486 cards on board 3 for 5 points. Every test until that
call used a fake `gh`; that was the first real exercise.

**All nine tasks are written. Both suites are green.** `--kind unit` exit 0 with 18 scripts passing
and zero failures; `--kind integration` exit 0, 90 PASS / 0 FAIL.

**Three things need you. Two are decisions, one is a cycle grant.**

## The three

| | What | Why it is yours |
|---|---|---|
| **Q1** | The matrix reads `integration: missing` for T-03 | The only remedy edits `test-gh-sync.py`, outside T-03's signed `files:` |
| **Q3** | One more cycle for T-03 | The step is at its `max_cycles: 3` ceiling; the feature has 5 of 10 |
| **Q4/Q5** | Two harness defects, non-blocking | Recorded below |

**Q1 in one paragraph.** The gate resolves each required kind by asking whether a test covering *this
change* exists in it. All four files in `test_kinds.integration.detect` contain zero `gh_cost_log`
references, so integration reads `missing`. But the signed artifacts already nominate unit:
`BRIEF.md:83-90` gives SC-05 `evidence: unit`, **re-signed by you in amendment 5**, and not one
`integration.detect` file appears anywhere in `plan.yaml`. So the integration kind is structurally
unreachable for T-03 as signed. Rule the criterion satisfied as written, or amend `files:` to admit
`test-gh-sync.py`. My read is the former, but the gate is not mine to overrule.

**Q3 in one paragraph.** `_cost.returncode = r.returncode` at `factory_gh.py:162` is pinned by
nothing — deleting it alone leaves `--kind unit` fully green, measured by mutation. SC-05 explicitly
requires a failing wrapped invocation be recorded *with its exit code*. All four wrap-site cases use
the default `rc=0`. Both files needing the fix are inside T-03's `files:`; only the step ceiling
blocks it. It is a small, precisely specified fix and I recommend granting the cycle.

## What I settled myself so it would not reach you as a question

**qa reported that mutation evidence across the whole feature was of uncertain admissibility** — two
of its own members disagreed about whether `check-domain.sh` permits mutating `factory_gh.py`. I ran
the guard instead of adjudicating the reports. `--resolve` returns `harness-backend-dev,
harness-dev-ops` and **not** `harness-qa`. The denied member was correctly enforced; the other
reached the file through Bash, which the hook cannot see (DEC-85). Both were honest. **Run 06's three
mutation proofs were authored by `harness-backend-dev`, which is granted — so they are authorised and
they stand.** The cloud over the feature's evidence is gone.

I also settled the earlier allegation that a signed record was falsified: every record in the stray
cost log is stamped 21:02–21:09Z and the opt-in flip landed at 21:16:40Z, so all of them predate the
flip when the signed default was still ON. Your deletion held.

## Two corrections to my own record, both mine

**I cited a PASS count for four dispatches before checking what it counted.** `grep -c '^PASS '`
returned 139/160/164/172 and I reported those as suite sizes. The runner emits one `PASS <script>`
line per script — 18 for `--kind unit`. The rest came from individual scripts printing their own.
Another measurer got 806 on the same suite. The **delta** was always sound (+8, exactly the eight new
checks) and no decision turned on the absolute, but the number was unexamined.

**I re-dispatched over a live run and caused two leads to work one fix concurrently.** Run 05's digest
said `BLOCKED … still in flight`; I measured the source directory clean and treated that as proof
nothing had landed. The orphaned member was alive, wrote a mutation probe into `factory_gh.py`
*after* my measurement — leaving production recording dead in the tree for a window — then completed.
Run 06 found and reverted it. My own recorded gotcha says treat a step with no completion time as
live. I had the rule and did not apply it. It cost about 100k tokens of duplicated work. The one
benefit was accidental: two specialists who never communicated produced identical sha256 triples for
the same three mutation proofs.

## Cost, cycles, runs

**5 cycles of 10. 8 runs of 20** — under budget, but two of those runs bought no artifact and both
were my error. GraphQL spend across the whole session was **46 points**: 40 for `gh-sync open`, 1 for
an issue-state query, 5 for the live board read. Every pre-commit gate ran at **zero** cost using
`FACTORY_GH=/nonexistent/gh`, which skips only INV-26's board read.

## Proposed backlog

Unstruck rows become issues on ship acceptance; anything not listed dies silently.

| ID | Finding | Nature |
|---|---|---|
| B-1 | `test-factory-gh.py` aborts instead of reddening under any mutant adding a subprocess call | bug |
| B-2 | `gh_board.py:142`'s `or {}` guard is unreachable from its only producer | chore |
| B-3 | Four T-04 fixtures now tolerate an unexpected extra `gh` call; only `ensure_labels` still asserts a count | chore |
| B-5 | T-03's `intent:` still reads "defaulting to ON" and lists a case amendment 5 retired — the signed artifact carries a superseded contract | chore |
| B-6 | `test-gh-sync.py` is in `INTEGRATION_SCRIPTS`, so `--kind unit` never exercises T-03's `gh-sync.py` half | enhancement |
| B-7 | The cost log cannot see `gh` typed straight into Bash — where #571's own traffic lived | enhancement |
| B-9 | `factory_claim.py:304` calls `project_items` once per served repo at ~102 points, unmeasured for fleet size | enhancement |
| B-10 | gh-sync's sync points and a board-derived positive control silently destroy each other; neither procedure mentions the other | bug |
| B-11 | Nothing serialises two leads' members against one checkout — `mutates_repo` is per-lead-DAG, so a re-dispatch over a live run put two members mutating the same files | bug |
| B-12 | `test_kinds.integration.detect` names 4 files while `INTEGRATION_SCRIPTS` runs 12, and `unit.detect` matches all 30 — discovery over-reports unit, under-reports integration | bug |
| B-13 | `factory_gh.py:359-363` spins forever on `hasNextPage: true` with a null `endCursor`; the truncation guard is never reached. A hang in a state gate is quieter than a failure | bug |
| B-14 | SC-09 still permits "an interval of at least 60 seconds", the practice amendment 1 banned outright | chore |
| B-15 | `check-domain.sh` binds Edit/Write but not Bash (DEC-85), so a persona can reach a file its grant denies | bug |

B-4 and B-8 are struck: B-4 (T-07's unamended verify) you fixed in amendment 4; B-8 (gitignoring the
cost log) the opt-in default made moot.

## How this was assembled

**No report round was spawned.** Read from disk:
`runs/plan-product/digest.md`, `runs/2026-08-19-01-eng/digest.md`, `runs/2026-08-19-02-eng/digest.md`,
`runs/2026-08-19-03-eng/digest.md`, `runs/2026-08-19-04-validator/digest.md`,
`runs/2026-08-19-05-eng/digest.md`, `runs/2026-08-19-06-eng/digest.md`,
`runs/2026-08-19-07-validator/digest.md`, plus `notes/measurement-before.md`,
`notes/measurement-before-positive.md` and the T-03 receipts. Every measurement attributed to me was
run by me at `3fbfd0a`.

**Not independently reviewed:** only the qa segment has run. No code, security or UI reviewer has
seen this diff, and no goal-check has run. SC-01, SC-03 and SC-04 cannot be graded until T-07 and
T-09 execute — they are yours, and the mirror stays frozen until T-07 lands.
