# FEAT-29 — GraphQL budget · ship review

**Supersedes `ship-review-2026-08-19-02.md`.** The feature works and is proven. It does **not** yet
meet its own goal: **8 of 10 success criteria are met**, and the two that fail are both record-keeping,
not behaviour. One of them is ship-blocking and the remedy is a single commit.

## The result

**`check-state.sh` costs 5 GraphQL points against a 506 baseline** — measured live, differenced either
side of the run, on a board of 473 items. T-07 needed no code edit at all: T-02 had already made
INV-26's path cheap, so the task became a measurement rather than a diff.

**The saving is the query shape, and that is proven rather than assumed.** Board 6 — the four-item
fixture — read **102 points on the old shape and 1 on the new**, with `board_items: 4` on *both* sides.
Identical item count on both measurements, so the item count cannot explain a 102-versus-1 difference.

**The cheap read still discovers what the expensive one did.** This was the real risk: issue #588
records that INV-26 prints nothing both when the board agrees *and* when the read fails, so a silent
break and a working read were previously indistinguishable. Three independent instruments close it —
the positive control's seven expected lines reappearing verbatim at 5 points; the live run returning
**two different station values across nine cards** (four `Backlog` reported, three `Done` correctly
silent, two pending agreeing), which no truncating or failing read can do; and
`test-gh-board.py:235-236` covering the one failure mode the live run does not exercise.

## The two criteria that fail, and what each needs from you

**SC-09 — BLOCKING. T-08's deliverable is not in the tree you would ship.** pm found this; the product
lead could not confirm it because leads hold no `Bash`; I re-ran it and it holds, and it is worse than
described:

```
git show 4f2e5d0:CLAUDE.md | grep "wait loop\|Monitor\|run_in_background"   -> ABSENT
CLAUDE.md (working tree):55                                                -> present
committed 75 lines · working tree 55 lines · diff: 9 insertions, 29 deletions
```

T-08's `verify:` passed by reading the **working tree**, not the tree under review. A gate shaped that
way passes for any task whose deliverable was never committed — that is the general defect, and it is
worth more than this instance. **The remedy is yours and it is one commit**; `CLAUDE.md` is `NOBODY`
and you have twice told me it is yours. Note the uncommitted edit also *removes* 20 lines, so it is a
rewrite rather than an addition — worth a glance before it lands.

SC-09's third clause is separately unsatisfiable: it demands the 2-points-per-poll figure appear in
`CLAUDE.md`, and no cost figure is present in either version. **Budget is not the obstacle** — the
file is 55 lines against DEC-181's 80, so there is room. Amend the clause, or add the citation.

**SC-08 — the absence clause is false, and my sweep narrows it usefully.** SC-08 requires that *no
surviving document* asserts `project item-list` is cheap enough to ignore. It does survive — but not
everywhere pm's single finding implied. I swept the corpus:

| Document | Status |
|---|---|
| `.harness/notes/grilling-graphql-cost-2026-08-10.md` | **struck by T-05** ✓ |
| `FEAT-11/BRIEF.md:171`, `STATE.md:31`, `notes/uat-SC-01-graphql-cost.md`, `notes/ship-review-close.md` + `.html` | **refute SC-08** — assert 31 points, "out of scope" |
| `FEAT-13/BRIEF.md:115`, `.harness/notes/grilling-board-read-lookups-2026-08-10.md:89` | **already say the figure is stale** — do not refute |

So the live set is **one shipped feature's artifacts**. `FEAT-11` reads `status: Done`. **This is a
scope decision only you can make**, and it collides with a principle: rewriting a shipped ship-review
and UAT would falsify a dated record. T-05's own precedent points at the honest third option — strike
in place with the correction beside it, exactly as it did for the grilling note — rather than either
deleting the lines or amending SC-08 to look away.

The root cause is a planning mismatch, not an execution failure: **a criterion quantifying over "every
surviving document" cannot be discharged by a task whose `files:` names one.**

## What was fixed since the last review

SC-05 was graded both ways in this feature's own record — unmet at run 11, met at run 12 — because the
first product-lead read pm's artifact **mid-write** and built a digest from a document still being
revised. It corrected itself and recorded the lesson. I had already spent a cycle on the unmet grade;
the resulting test is not wasted — it pins SC-05's sentence on the production path and closes the
ambiguity permanently — but the cycle traces to a premature close, not to a defect.

## Cost, cycles, runs

**46 GraphQL points** spent by me across the entire feature — against a feature whose subject is a
506-point-per-run leak. Every pre-commit gate ran at **zero** cost via `FACTORY_GH=/nonexistent/gh`,
which skips only INV-26's board read. **8 cycles of 10. 14 runs of 20.**

**Five lead runs closed with a member still in flight.** Three bought no artifact; one produced a
digest built on a mid-write read; one became a false premise in a brief I then wrote. That single
harness defect is the largest source of waste in this feature by a wide margin.

## Proposed backlog

Unstruck rows become issues on ship acceptance; anything not listed dies silently.

| ID | Finding | Nature |
|---|---|---|
| B-1 | `test-factory-gh.py` aborts instead of reddening under any mutant adding a subprocess call | bug |
| B-2 | `gh_board.py:142`'s `or {}` guard is unreachable from its only producer | chore |
| B-3 | Four T-04 fixtures tolerate an unexpected extra `gh` call; only `ensure_labels` asserts a count | chore |
| B-5 | T-03's `intent:` still carries text amendment 5 superseded | chore |
| B-6 | `test-gh-sync.py` is in `INTEGRATION_SCRIPTS`, so `--kind unit` never exercises T-03's `gh-sync.py` half | enhancement |
| B-7 | The cost log cannot see `gh` typed straight into Bash — where #571's own traffic lived | enhancement |
| B-8 | `.harness/logs/gh-cost-*.jsonl` has no ignore rule and is written `0644` — the **narrow** pattern, not blanket `.harness/logs/` | chore |
| B-9 | `factory_claim.py:304` calls `project_items` per served repo at ~102 points, unmeasured for fleet size | enhancement |
| B-10 | gh-sync's sync points and a board-derived positive control silently destroy each other | bug |
| B-11 | Nothing serialises two leads' members against one checkout; `mutates_repo` is per-lead-DAG | bug |
| B-12 | `integration.detect` names 4 files while `INTEGRATION_SCRIPTS` runs 12; `unit.detect` matches all 30 | bug |
| B-13 | `factory_gh.py:359-363` spins forever on `hasNextPage: true` with a null `endCursor` | bug |
| B-15 | `check-domain.sh` binds Edit/Write but not Bash (DEC-85), so a persona can reach a file its grant denies | bug |
| B-16 | The `SubagentStop` hook forces a digest from a lead with a member still in flight; nothing preloaded says an in-flight `BLOCKED` is correct. **Five occurrences here** | bug |
| B-17 | `factory_config.harness_root()` falls back to the real checkout when `CLAUDE_PROJECT_DIR` lacks `SPEC.md`; a benchmark wrote 5,000 records into the operator's tree | bug |
| B-18 | **A task `verify:` that reads the working tree cannot distinguish committed from uncommitted work** — this is how SC-09 got to ship review green. Generalises past T-08 | bug |
| B-19 | Issue #588 survives this feature: `check-state.sh:1172-1176` still swallows a failed board read into silence | bug |
| B-20 | Leads hold no `SendMessage`, so a lead cannot course-correct an in-flight member even though the Agent tool's description says it can | bug |

B-4 and B-14 remain struck — you fixed T-07's verify in amendment 4, and SC-09's interval clause in
amendment 1.

## How this was assembled

**No report round was spawned.** Read from disk: `runs/plan-product/`, `runs/2026-08-19-01-eng/`
through `runs/2026-08-19-13-eng/` digests, `notes/research-goalcheck-FEAT-29.md`,
`notes/qa-matrix-gate-final-c472a02.md`, the three `measurement-*.md` files, and the T-03 receipts.
Every measurement attributed to me — the corpus sweep, the `git show` on the committed `CLAUDE.md`,
the `8c2c24d..4f2e5d0` diff, both suite runs — I ran myself.

**Not independently reviewed:** no UI reviewer ran (no UI surface, the validator lead's call). The
security reviewer's pass produced B-8 and covered the cost log only.
