<!-- index-contract v1 -->
<!-- GENERATED except the text after ` :: ` on each row.
     Regenerate: .agents/skills/harness/bin/gen-decisions-index.py -->

# DECISIONS — index

**A row is an open-or-skip filter, never the rule itself.** Its only job is to answer "do I open this
entry?" Never act on a ruling here: open `.harness/harness/docs/DECISIONS.md` at the `@line` anchor and read
the entry. Rows written during the one-time backfill are second-hand paraphrase.

**Never read the authority whole (DEC-150).** Grep this index, then open the two or three entries that
bear on your task. Decisions cited in a dispatch are a floor, not a ceiling.

**Adding a decision:** its author writes its ruling here, in the same commit that appends the entry.

Row: `- DEC-NN @<line> [tags] refs: <graph> :: <ruling>`.

- DEC-01 @33 [state,plan,skills] refs:  :: GSD is removed entirely and the harness ships native replacements for orchestration, persistent state, execution and rule delivery.
- DEC-02 @47 [plan,state] refs:  :: The harness hosts its own development on `.harness/` and crews, making it the first project built with the harness.
- DEC-03 @59 [state,orchestrator,plan] refs:  :: The backbone is a small set of harness-owned state files plus `harness.json` and one directory, not a second execution engine.
- DEC-04 @70 [plan,state] refs:  :: Project state lives under a fresh `.harness/` root that is deliberately not backward-compatible with `.planning/`.
- DEC-05 @78 [orchestrator] refs:  :: Composition is declarative crew YAML read by one generic runner: the orchestrator routes, and workers never pick who runs next.
- DEC-06 @90 [skills,deploy] refs:  :: The crew runner is a skill under `.claude/skills/harness/` with crew configs beside it, because deploy distributes skills and agents but not commands.
- DEC-07 @101 [domain,brief,orchestrator,plan] refs:  :: Three squads — product, engineering, validation — each have a domain lead coordinated by the orchestrator, and the human is the CEO, so there is no CEO agent.
- DEC-08 @113 [plan,qa,domain,tests] refs: DEC-34 :: `pm` covers research plus planning in one context, `qa` covers test-writing plus coverage assessment, and `eng-lead` owns architecture review.
- DEC-09 @127 [domain,gates,plan] refs:  :: Each member declares `consult-when` in the manifest and its lead matches the request semantically at delegation time; `change_type` stays purely a test-obligation axis.
- DEC-10 @141 [domain,org,orchestrator] refs: DEC-13 :: The org is data: `team-config.yaml` holds teams, leads, members, `consult-when` and `domain` per project.
- DEC-11 @151 [domain,hooks,skills,state] refs: DEC-63 DEC-110 DEC-152 :: The manifest carries policy (team, lead, `consult-when`, `domain`) and agent frontmatter carries capability including `skills` and `effort` but never `hooks`; nothing is declared twice.
- DEC-13 @169 [deploy,domain,org,state] refs:  :: Templates carry `schema_version`, and adopting a newer org is a user-triggered `/harness-init --upgrade` rather than a deploy side effect.
- DEC-14 @178 [approval,brief] refs:  :: `/harness-init` is one onboarding interview covering technical detection, product requirements, BRIEF approval and an optional design pass.
- DEC-15 @190 [orchestrator] refs:  :: Ideation is the orchestrator working directly with the CEO, backed by `pm` research.
- DEC-16 @198 [state] refs:  :: Every project carries the whole roster, and an irrelevant agent self-scopes to "not in scope" rather than being pruned.
- DEC-17 @207 [] refs:  :: `shared_context` holds exactly one entry, `handoff.md`, because every entry loads into every agent at every spawn.
- DEC-18 @215 [digest,orchestrator,state,cost] refs: DEC-85 :: Leads hold neither `Edit` nor `Bash`, and their `Write` is scoped to their own run dir, so capability rather than instruction keeps a lead delegating.
- DEC-21 @231 [deploy,domain] refs:  :: Rules are the constitution: human-authored in the harness repo, overwritten on every deploy, with no per-project overlay and no agent-authored edits.
- DEC-22 @243 [expertise,qa,state,tests] refs:  :: Each agent keeps a bounded, structured Expertise file at `.harness/expertise/<agent>.md` so durable knowledge lands in the agent that will act on it.
- DEC-23 @255 [approval,plan] refs:  :: A choice belongs in the plan's `## Decisions` section and is approval-gated; an observation about how the codebase behaves belongs in Expertise.
- DEC-24 @265 [expertise,digest] refs:  :: Expertise write quality is advisory in three layers — one shared rule, DIGEST visibility with a one-line why, and lead curation — because a hook cannot judge an insight.
- DEC-25 @277 [expertise] refs:  :: On overflow the member flags `expertise_full`, its lead emits a `KEEP`/`DROP`/`MERGE` note, and the member applies that note itself.
- DEC-26 @288 [digest,orchestrator] refs:  :: A lead monitors its squad through members' DIGESTs and artifacts, and squad-level patterns accumulate in the lead's own Expertise.
- DEC-27 @299 [expertise] refs:  :: Expertise has two tiers — committed project-scoped and local global — with project winning on conflict and promotion requiring evidence from more than one project.
- DEC-28 @311 [orchestrator,approval,brief,gates] refs:  :: CEO feedback is classified by kind: requirement changes go to BRIEF and PLAN via pm, craft feedback to the addressed lead's Expertise, and course-corrections into the next instruction only.
- DEC-29 @323 [digest,orchestrator] refs:  :: Every agent returns `VERDICT:` for control, `DIGEST:` for routing and `artifact:` as a path, and the orchestrator routes on the DIGEST without opening the artifact.
- DEC-30 @335 [] refs:  :: A malformed return is re-prompted once, then recorded as `VERDICT: BLOCKED (contract violation)` and escalated, never inferred from the text.
- DEC-31 @344 [plan,state] refs:  :: Reviewers are advisory: a non-empty `must_fix` or `severity_max` at high or above fails, while concerns at med or below pass with notes.
- DEC-32 @354 [orchestrator,brief,state] refs:  :: Autonomy is scoped by reversibility: cheap and reversible is decided and recorded, expensive or hard to reverse is asked via `open_questions`, and scope, goal or decision changes are always asked.
- DEC-33 @368 [approval,brief,gates,orchestrator] refs:  :: Merge is user-gated by default, so the orchestrator surfaces passing gates for approval; a project may opt into autonomous merge in `harness.json`.
- DEC-34 @377 [approval,gates,plan,brief] refs:  :: Two self-review softnesses are accepted (pm authors the plan and checks the goal, eng-lead routes the build and reviews architecture), with the CEO approvals as compensating control.
- DEC-35 @389 [qa] refs:  :: The change-type-to-required-test-kinds table in `harness.json` is a floor qa may add to but never drop below, with conditionals as named predicates and `test_kinds` supplying detection and command.
- DEC-36 @402 [qa,state] refs:  :: A missing or unresolvable test `cmd` yields `VERDICT: BLOCKED — test command unresolved`, a loud third state distinct from the not-applicable soft skip.
- DEC-38 @412 [orchestrator,state] refs:  :: The orchestrator and the crew runner are one actor at two delegation granularities: delegating to a persona is a direct spawn, delegating to a team runs that crew's DAG.
- DEC-39 @422 [orchestrator,digest,domain,org] refs:  :: Hosting is hierarchical — orchestrator to domain lead to workers, with the crew's `lead:` field naming the host — and all three leads exist as spawnable personas.
- DEC-40 @440 [orchestrator,org] refs:  :: Whether a lead fans out its review panel in parallel is a performance question left to measurement, and both acceptable answers preserve lead authority over the assessment.
- DEC-41 @450 [] refs:  :: `validator-lead` consolidating and assessing its panel's findings is its defining job, so synthesis has a named owner in both hosting modes.
- DEC-42 @457 [approval,orchestrator] refs:  :: Approval steps sit at crew boundaries and never mid-DAG, because a subagent cannot call `AskUserQuestion`; questions ride up via `open_questions` and the orchestrator asks.
- DEC-43 @466 [orchestrator,approval,cost,plan] refs:  :: One mechanism serves every human-in-the-loop case: `open_questions` upward, the orchestrator asks, and re-delegation carries `resume_from` plus a path to the answers.
- DEC-44 @476 [] refs:  :: User answers are written durably to a named answers file under `notes/`, never only into a run dir, so lateral routing and later artifacts can read them.
- DEC-45 @483 [state] refs:  :: `STATE.md` holds only `## Current` and `## Open Questions`, both self-clearing; the activity stream lives in dated files under `logs/` and is never loaded at spawn.
- DEC-46 @496 [state] refs:  :: One branch and one PR per feature, and each run belongs to one squad so exactly one lead owns that run's `state.yaml`.
- DEC-47 @505 [plan,approval,orchestrator,state] refs:  :: The plan's `## Features` section is the pm-owned approval-gated declaration, and `feature.yaml` is orchestrator-owned execution fact carrying only a `feature_id` join key.
- DEC-48 @516 [brief,plan] refs:  :: REQ, FEAT, D and T are four distinct levels, the test for a REQ being that it survives changing your mind about implementation; REQ coverage is computed, never tracked.
- DEC-49 @530 [brief,budget,state] refs:  :: The retry budget is feature-level — `cycles_used` against `max_total_cycles` in `feature.yaml` — because a fix cycle spans a new eng run plus a new validator run.
- DEC-50 @538 [dispatch,state] refs:  :: Reviewers diff `base` to a `review_sha` pinned at review dispatch, with local `git diff` as ground truth and no network or auth.
- DEC-51 @546 [dispatch,state] refs:  :: A dead host is resumed with `resume_from` at the in-flight step, reading `state.yaml` for what was dispatched and `git log` for what actually landed, aided by a `[harness:<step-id>]` commit prefix.
- DEC-52 @558 [dispatch,plan] refs:  :: A persona whose deliverable is a canonical file writes it in place, run dirs hold reports and intermediates, and any staged canonical file is promoted before a consumer step dispatches.
- DEC-53 @568 [] refs:  :: Reviewer reports are namespaced per persona and run so a parallel panel cannot collide on one report path.
- DEC-54 @574 [] refs:  :: Crews are flat and standalone in v1 with no sub-crew composition, so the review panel is listed in both `ship-feature` and `review-team`.
- DEC-55 @585 [gates,orchestrator] refs:  :: `review-team` is advisory: it returns `must_fix` and the calling context owns remediation and merging.
- DEC-56 @592 [security] refs:  :: pm's goal-check is its own step after the panel and its assessment, so "did we deliver?" is never averaged with code nits.
- DEC-57 @599 [qa,security] refs: DEC-08 :: The review panel has no CEO seat and no separate eng-reviewer, and its membership is crew config rather than auto-selected per diff.
- DEC-58 @611 [domain,deploy,tdd] refs: DEC-85 :: Engineering has five peer domains with no catch-all, `dev-ops` owning infra, CI, config, tooling and deploy, and eng-lead routes each task to exactly one.
- DEC-59 @624 [domain,skills,expertise,state] refs:  :: The declarative `## Skills`, `## Expertise` and `## Domain` body sections buy a uniform shape and greppable auditing, not enforcement — Claude Code does not parse them.
- DEC-60 @636 [plan,approval,brief,org] refs:  :: `.harness/README.md` is owned by `documentor` and rewritten as a defect fix, because the on-disk version describes a pre-restructure org and omits the hardest-gated schema fields.
- DEC-61 @647 [skills,deploy] refs: DEC-06 DEC-07 DEC-63 :: The Phase-1 exploration findings live on inside the `Because` clauses of the decisions they support, with no standalone findings section.
- DEC-62 @658 [map,qa] refs: DEC-34 DEC-35 :: `visual-designer` authors the design contract and `ui-reviewer` audits it in two modes, pre-build soundness and a post-build scored audit, so authorship stays separate from audit.
- DEC-63 @677 [skills,cost] refs:  :: Each rule is a skill declared in an agent's `skills:` frontmatter field, and Claude Code injects the full rule content before the agent's first action.
- DEC-64 @694 [expertise,hooks] refs: DEC-65 DEC-66 :: A `SubagentStart` hook receives `agent_type` and returns that agent's Expertise file as `hookSpecificOutput.additionalContext`, so delivery needs neither obedience nor a tool grant.
- DEC-65 @706 [expertise] refs: DEC-18 DEC-66 :: Expertise is hand-rolled at `.harness/expertise/<agent>.md`, because native `memory:` auto-enables Read, Write and Edit and would dissolve the lead and reviewer capability limits.
- DEC-66 @721 [expertise] refs: DEC-22 DEC-24 DEC-30 :: Expertise entries carry stable per-section IDs and updates are ops — `add`, `replace`, `merge`, `drop` — each naming its target with a `why`, reconciled at propose time.
- DEC-68 @735 [digest,orchestrator] refs: DEC-25 :: A curation note is applied immediately, and the recommending tier spawns the target agent solely to apply it when that agent has already closed.
- DEC-69 @748 [orchestrator,brief,digest,cost] refs:  :: The orchestrator recommends curation of a lead's Expertise from the cross-lead view it holds at the CEO briefing, and a plain-English summary in the briefing applies unless the CEO objects.
- DEC-70 @777 [qa,dispatch,gates,state] refs:  :: `ai_behavior` requires an `eval` for prompt, model and tool-integration changes: `ai-dev` authors it, `qa` gates it, `validator-lead` judges adequacy. A preloaded markdown playbook is graded by conduct instead.
- DEC-71 @801 [orchestrator,state,digest,expertise] refs:  :: The orchestrator delegates only to leads, so even a single-task request enters through the lead that owns the relevant persona.
- DEC-72 @812 [budget] refs: DEC-73 :: A crew's `goal` resolves to a FEAT plus the `SC-NN` entries its REQs trace to, and the crew is done when those criteria are met.
- DEC-73 @823 [qa,plan,brief] refs: DEC-34 :: Every `SC-NN` declares `verify: automated | inspection | uat` when authored, and pm's goal-check assembles evidence from the validators with a pointer per criterion instead of re-testing.
- DEC-74 @837 [brief,tests] refs:  :: Any criterion marked `verify: uat` produces a step in a per-feature UAT file, and that UAT is a blocking section of the ship-review briefing.
- DEC-75 @851 [plan,approval,digest] refs:  :: `visual-designer` decides during the design pass whether a feature needs an interactive high-fidelity prototype, and that prototype is approved bundled with plan approval as one signature.
- DEC-76 @867 [approval,skills] refs:  :: Teams carry binding technology conventions in a `conventions:` block in the manifest, and deviating from one requires a plan `## Decisions` entry and therefore CEO approval.
- DEC-77 @886 [state,brief,budget,orchestrator] refs:  :: Cycle-budget exhaustion stops only that branch, preserves run history, the branch and every commit, leaves the feature `in_progress`, and rolls up `BLOCKED` with what each cycle tried.
- DEC-78 @900 [digest,plan,approval] refs:  :: The consolidated DIGEST carries an `escalations:` list naming the question, who raised it, where it routed and how it resolved.
- DEC-79 @913 [state] refs:  :: Feature-scoped durable artifacts carry the FEAT id in the filename and repeat feature and run in the header, so `ls` answers which feature an artifact belongs to.
- DEC-80 @923 [expertise,skills] refs: DEC-63 :: The artifact has one name, Expertise: the file is `.harness/expertise/<agent>.md` and the rule is the `harness-expertise` skill.
- DEC-81 @937 [domain,plan] refs:  :: Agent frontmatter takes named colors only, a blocking hook must `exit 2`, tool input reaches a hook as JSON on stdin, and concurrency is 20 subagents at once.
- DEC-83 @953 [org,plan] refs:  :: Spawn depth is set explicitly to 2 in every project, with omitting `Agent` from a worker's `tools:` as the primary control and the depth cap as defence in depth.
- DEC-84 @988 [domain] refs: DEC-85 :: Destructive-operation restraint is a `Bash` matcher in `check-domain.sh` that exits 2, or it does not exist.
- DEC-85 @999 [domain,state,worktree] refs:  :: Serialized repo-mutators on one branch plus `isolation: worktree` for genuinely parallel mutation is what makes fan-out safe; the domain hook is a guardrail over the common write case.
- DEC-86 @1022 [qa,security,tests] refs: DEC-85 :: The roster is 3 leads plus 9 doers plus 3 reviewers, `qa` counting as a doer because it writes tests, so six agents are write-less.
- DEC-87 @1033 [domain,expertise] refs:  :: A doer's own Expertise path appears in its manifest `domain`, so the hook permits the self-apply mechanism it depends on.
- DEC-89 @1041 [qa,gates,state] refs:  :: Hand edits are a legal path: they commit with a `[harness:human]` prefix and the state check re-pins `review_sha`, so the reviewer and qa gates re-open for the affected paths.
- DEC-90 @1057 [expertise,state,cost,orchestrator] refs: DEC-120 DEC-188 :: STRUCK 2026-08-21 under DEC-188 — stated the single-operator scope boundary and claimed no lock anywhere; expertise-merge.py holds one, so the claim is false and the entry is struck.
- DEC-91 @1072 [state] refs:  :: The value claim is that Claude executes reliably at each stage without mid-stage supervision, since supervision is batched at decision boundaries.
- DEC-93 @1081 [cost,org,worktree] refs:  :: The pilot carries two instruments: org-arm runs settle cost and touchpoints, while `kaya-ai`'s git history supplies the base defect rate.
- DEC-94 @1107 [qa,state,tests] refs: DEC-36 :: `kaya-ai` is the pilot host, with `uv run pytest` plus two pnpm suites and no Playwright, so the `ui` kind soft-skips.
- DEC-95 @1122 [worktree,expertise,state] refs: DEC-85 DEC-89 :: A git worktree is the unit of concurrency — one feature per worktree, as many worktrees as you like, with `.harness/` as per-worktree state.
- DEC-96 @1152 [org,cost,state,brief] refs:  :: The measured base defect rate in `kaya-ai` history is 0.44 escaped defects per feature, with about 79% of them within reach of the four artifacts.
- DEC-97 @1196 [tests,cost,org,qa] refs: DEC-96 :: A throwaway dry run has all four artifacts behaving as specified, and code review catches a fail-open defect that a green happy-path suite misses.
- DEC-98 @1236 [tests,qa,state] refs: DEC-83 :: `harness-qa-gate` resolves a kind to satisfied, missing, not-applicable or `misconfigured`, discriminating on failure kind rather than test count, and `misconfigured` returns `BLOCKED`.
- DEC-99 @1265 [cost,org,state,digest] refs: DEC-96 DEC-97 DEC-98 :: The full agentic workflow gets built with its cost monitored in practice, which makes instrumentation a build requirement and lets the org proceed.
- DEC-100 @1299 [skills,domain,hooks,deploy] refs: DEC-40 :: All four platform unknowns are settled empirically: `SubagentStart` fires for nested spawns, skill dirs sit exactly one level under `.claude/skills/`, and a `PreToolUse` `exit 2` blocks a subagent's Write.
- DEC-101 @1381 [domain,digest,state,expertise] refs: DEC-83 DEC-87 DEC-98 :: Four `bin/` scripts ship verified against real inputs, and a fix is done only once run against an input that would expose it.
- DEC-105 @1422 [brief,expertise,plan,state] refs: DEC-71 :: The per-spawn baseline is ~15.3k tokens and ~522k per feature, of which the CLAUDE.md hierarchy is 31% and the universal rules 11%, making CLAUDE.md the largest available trimming lever.
- DEC-106 @1466 [domain,digest,expertise] refs:  :: Reviewers hold `Write` scoped by the domain hook to exactly two paths — their namespaced report and their own Expertise file — with no `Edit` at all.
- DEC-107 @1480 [domain,qa,security,skills] refs: DEC-18 DEC-65 DEC-100 :: The roster is complete at 3 leads, 9 doers and 3 reviewers, validated by script on frontmatter, colour, tool grants, preloaded rules and manifest entry.
- DEC-108 @1532 [domain,expertise,hooks,docs] refs: DEC-64 DEC-85 DEC-100 :: Expertise injection works end to end through the `SubagentStart` hook, and project agent definitions win over global ones.
- DEC-109 @1606 [domain,expertise,docs,hooks] refs: DEC-87 DEC-108 :: A relative command path leaves the frontmatter domain hook still silent, which eliminates `${CLAUDE_PROJECT_DIR}` interpolation as the cause; and declaring the stale-wording markers is half of writing a superseding decision.
- DEC-110 @1644 [hooks,domain,orchestrator,state] refs: DEC-85 :: `PreToolUse` declared in agent frontmatter does not fire for spawned subagents, so the domain hook lives as one global `settings.json` registration dispatching on `agent_type`.
- DEC-111 @1698 [domain,dispatch,expertise,hooks] refs: DEC-109 DEC-110 :: `/harness-init` provisions the `PreToolUse` domain hook into `settings.json` alongside spawn depth and `SubagentStart`, and `check-state.sh` INV-9 verifies the set at every entry.
- DEC-112 @1732 [domain,state,brief,approval] refs: DEC-100 DEC-108 DEC-110 :: `/harness-init` ships as a flat skill with eight templates and deterministic merge scripts, writing the user's explicit approval without ever self-approving.
- DEC-113 @1825 [skills,state] refs:  :: Team and crew overrides live outside the tool tree, in project-owned state that harness development never edits, and are resolved first.
- DEC-114 @1837 [cost,budget,orchestrator,state] refs: DEC-99 :: Cost is computed from transcript tokens keyed by `(model, speed, inference_geo)`, and `max_cost_usd` bounds spend beside `max_cycles` with INV-11 making the meter non-optional.
- DEC-115 @1918 [hooks,state,domain,map] refs: DEC-02 :: GSD removal is scoped to this repo, and a hook's `settings.json` entry comes out before the script is left inert and restorable.
- DEC-116 @1985 [domain,cost,skills,dispatch] refs: DEC-100 DEC-112 DEC-113 :: A team step's `outputs:` resolve to the producing agent's own domain, and a lead sets `cost: pending_orchestrator` because with no `Bash` it cannot meter its own run.
- DEC-117 @2051 [dispatch,plan,cost,domain] refs: DEC-49 DEC-116 :: `{{cycle}}` resolves in the output path of anything that re-runs, so a loop-back writes a new report instead of overwriting the one that caused the cycle.
- DEC-118 @2128 [orchestrator,dispatch,plan,state] refs: DEC-116 :: A team is single-squad by construction, so a multi-squad lifecycle such as `plan-feature` or `ship-feature` is an orchestrator playbook sequencing one lead-owned run per squad segment.
- DEC-119 @2174 [digest,cost,domain,orchestrator] refs: DEC-101 DEC-116 DEC-117 :: **Team** is the live noun on every surface, there is exactly one artifact type per tier, and the two cycle counters have named owners.
- DEC-120 @2226 [orchestrator,state,brief,cost] refs: DEC-06 DEC-114 DEC-118 :: `harness-orchestrator` is a spawned agent at layer 1, one per in-flight feature, spawn depth is capped at 3, and the main session is the thin layer-0 user channel.
- DEC-121 @2319 [digest,expertise,orchestrator,org] refs: DEC-101 DEC-119 :: Every digest field is required, and nothing is said with an explicit `[]` or `none`, because absence is ambiguous where emptiness asserts the agent looked.
- DEC-122 @2359 [digest,domain,docs,hooks] refs: DEC-101 DEC-110 DEC-119 DEC-121 :: The digest contract is enforced by `validate-digest.py` as a `SubagentStop` hook whose exit 2 stops the subagent stopping, so a malformed return is rejected at source.
- DEC-123 @2406 [digest,orchestrator,state] refs: DEC-110 DEC-112 DEC-119 DEC-122 :: `validate-digest.py` computes the team verdict roll-up `BLOCKED > ESCALATE > FAIL > PASS` and rejects a lead return that reports better than its worst member.
- DEC-124 @2450 [state,dispatch,digest] refs: DEC-112 DEC-119 DEC-123 :: Parallel members are dispatched in a single message, and an agent's own account of what it did is never the evidence — the spawn records are.
- DEC-125 @2510 [expertise,digest,orchestrator,state] refs: DEC-124 :: An agent seeing no injected Expertise block creates its file from the skeleton and updates it read-modify-write, since writing it from new entries alone deletes the earlier ones.
- DEC-126 @2598 [digest,skills,approval,cost] refs:  :: A digest schema shared by two or more agents lives in one canonical template delivered by `skills:` preload, while a schema with exactly one agent keeps its template inline.
- DEC-127 @2623 [digest,orchestrator,brief,cost] refs: DEC-100 DEC-101 DEC-110 DEC-119 DEC-121 DEC-122 DEC-123 DEC-124 :: `validate-digest.py` reads member verdicts by key rather than matching text, exits 2 on a contract violation, and fails open loudly on its own exception.
- DEC-128 @2740 [orchestrator,brief,cost,plan] refs: DEC-105 DEC-116 DEC-126 :: `harness-orchestrator` is the sixteenth agent, holding `Bash` to meter the run, and it preloads the `harness` skill whose `SKILL.md` is the orchestrator playbook.
- DEC-129 @2772 [brief,plan,state,docs] refs: DEC-120 :: A feature's whole world is one directory: `.harness/features/<FEAT>/` holds `BRIEF.md`, `PLAN.md`, `DESIGN.md`, `STATE.md` and `feature.yaml`, and INV-1..5 are per-feature.
- DEC-130 @2797 [qa,brief,docs,org] refs: DEC-129 :: Every feature-scoped note lives under `features/<FEAT>/notes/`, so retiring a feature is one directory, and `.harness/notes/` holds only genuinely project-scoped artifacts.
- DEC-131 @2816 [orchestrator,dispatch,state,brief] refs: DEC-129 :: A child spawn survives the interruption of its parent and runs on as an orphan, so INV-12 warns on any run dir absent from `feature.yaml`.
- DEC-132 @2837 [state,brief,orchestrator] refs:  :: pm authors the success criteria — wording, numbering and `verify:` method — and adding criteria beyond the user's mandated outcomes is expected rather than merely permitted.
- DEC-133 @2855 [brief] refs:  :: A feature id is `FEAT-NN-<kebab-slug>`, coined by pm at BRIEF time from the goal, and it is immutable once created.
- DEC-134 @2866 [budget,cost,orchestrator,brief] refs:  :: The cost budget is informational — a crossing is flagged in the headline, never blocking — while `max_total_cycles` stays hard and every agent pins its model in frontmatter.
- DEC-135 @2885 [docs,brief,skills] refs: DEC-105 :: CLAUDE.md is about 0.8k tokens — the Project blurb, the Harness section and two conventions — which keeps stale claims out of the propagation checker's scan surface.
- DEC-136 @2902 [plan,map,deploy,state] refs: DEC-63 DEC-106 DEC-112 DEC-128 DEC-135 :: The GSD dev state is triaged item by item into the harness's own ledger, with history left in git and no `.planning/` directory.
- DEC-138 @2925 [plan,github,brief,state] refs: DEC-133 DEC-166 :: The GitHub Issues mirror is write-only and orchestrator-executed, gated by `github.sync: true`; there is no `absorbs:` third category, and origin no longer decides a parent's closure.
- DEC-139 @3053 [gates,plan,qa,domain] refs: DEC-118 :: Debug is an investigation segment in front of the normal flow, and a bug is a full gated flow named `BUG-NN-<kebab-slug>` on its own number sequence.
- DEC-141 @3074 [map,brief] refs:  :: `render-map.py` owns map rendering — heading nesting, prose caps, scrollable diagrams — while map authoring labels every edge and opens each view with `## In brief`.
- DEC-142 @3114 [dispatch,orchestrator,brief,org] refs: DEC-133 :: Every dispatch title reads `<flow-id> · <step> · <what>`, and a named spawn takes a flow-traceable slug like `FEAT-05-ship` because `·` is illegal in a name.
- DEC-143 @3148 [worktree,dispatch,plan,approval] refs: DEC-85 :: `check-domain.sh` matches the raw path and then the worktree-stripped path against the same globs, so an agent inside `.claude/worktrees/<id>/` writes exactly what its domain grants.
- DEC-144 @3177 [github,gates,orchestrator,state] refs: DEC-138 :: `branch-create-gate.sh` is a mandatory `PreToolUse:Bash` gate requiring every new git branch to name the flow it serves, and it DENIES on any unverifiable `gh` state.
- DEC-145 @3201 [expertise,budget,digest,deploy] refs: DEC-24 DEC-25 DEC-69 :: Expertise splits recording from distillation: every agent appends observations mid-run to a never-injected log, and Expertise is written only under a distillation dispatch.
- DEC-146 @3255 [gates] refs: DEC-144 :: The board In-Progress flip resolves one GraphQL query filtered by the pinned `project_id`, and the flip stays best-effort by design.
- DEC-147 @3271 [expertise,dispatch,orchestrator,map] refs:  :: The flat-roster invariant lives in the rule skills: a teammate omits `name:` and therefore cannot spawn a teammate.
- DEC-148 @3285 [orchestrator,cost,map,budget] refs: DEC-145 :: The dominant cost term is context length × turn count; DEC-178 dropped the watchdog with the meter, and DEC-159 now carries the relay half as mandatory one-phase-per-orchestrator.
- DEC-149 @3306 [map,dispatch,domain,skills] refs:  :: Two MIT-licensed design imports re-home onto existing machinery as the `harness-codebase-design` and `harness-spec-driven` skills; the third, mission `deepen`, retired with the map tier.
- DEC-150 @3341 [state,domain,brief,digest] refs: DEC-85 DEC-148 :: State files have caps enforced as physics: `check-domain.sh` denies an over-budget `feature.yaml` or STATE.md at write and reports after it (DEC-180); step-1 read scoping stays advisory.
- DEC-151 @3378 [domain,qa,orchestrator,security] refs: DEC-85 :: `bash-write-guard.sh` is a mandatory `PreToolUse:Bash` hook: the three reviewers are read-only, and every other harness agent except dev-ops has extractable write targets checked against its domain.
- DEC-152 @3406 [orchestrator,dispatch,docs,domain] refs: DEC-145 :: Frontmatter `effort:` is pinned per tier and overrides the spawning session's setting: four judging agents at `high`, the other twelve at `medium`.
- DEC-153 @3429 [orchestrator,qa,worktree,domain] refs: DEC-151 :: Perturbation proofs run under `.claude/worktrees/**` where the guard permits governed-agent writes, and the commit pen belongs to the orchestrator alone.
- DEC-154 @3454 [state,digest,dispatch,budget] refs: DEC-156 :: A run `state.yaml` is a checkpoint read mechanically: every value is an identifier, enum, counter, path or marker, with a one-line `note:` per step as the prose ceiling.
- DEC-155 @3479 [dispatch,cost,org,state] refs: DEC-31 DEC-152 DEC-156 :: A member runs on the model pinned in its own agent definition, so a dispatch never passes `model:`, mechanized by `dispatch-guard.sh`.
- DEC-156 @3508 [digest,dispatch,state,cost] refs: DEC-122 DEC-154 DEC-155 :: Three prose-only rules become scripts: `validate-digest.py --hook` validates a lead's returned artifact file, `check-state.sh` INV-15 and INV-16 gate run state, and `dispatch-guard.sh` rejects `model:` in a dispatch.
- DEC-157 @3551 [budget,orchestrator,cost,plan] refs: DEC-134 DEC-160 DEC-178 :: A cycle is a rework loop, so `cycles_used` increments only on a routed-back FAIL, an unmet SC re-dispatch or a lead's send-back, and `max_total_cycles` defaults to 10.
- DEC-158 @3606 [orchestrator,skills,state,cost] refs: DEC-100 DEC-125 DEC-145 DEC-148 DEC-155 DEC-156 :: Rule skills carry the rule, one clause of why and a DEC pointer; move 3 keys on SHAPE: bounded procedures go to `references/`, resident rules stay inline.
- DEC-159 @3678 [orchestrator,plan,state,dispatch] refs: DEC-148 DEC-150 DEC-156 DEC-158 DEC-160 DEC-198 :: An orchestrator's mission is exactly one phase (plan, build, validate, ship), so ending at that boundary is normal termination and the successor reads a capped handoff note.
- DEC-160 @3747 [state,deploy,domain,plan] refs: DEC-113 DEC-154 DEC-157 DEC-159 :: `check-domain.sh` denies a run `state.yaml` Write carrying non-whitelisted or duplicate top-level keys, and a decision adding a `harness.json` key must say so.
- DEC-162 @3774 [map,domain,approval,state] refs: DEC-149 :: Mission map assigns pm `.harness/codebase/glossary.md` alongside `product-surface.md`, and check-state INV-19 warns when a mapped codebase carries no glossary.
- DEC-163 @3795 [map,brief,plan,approval] refs: DEC-100 DEC-134 :: `cmd: null` is the honest record when no runner exists for a test kind, and INV-20 only warns when such a kind's mapped surface exceeds a stub.
- DEC-164 @3827 [plan,brief,org,state] refs: DEC-120 DEC-138 DEC-165 :: `harness-grilling` is blocking step zero at `/harness-plan` and inside `harness-init`'s interview, main-session only, and only the user's explicit call skips it.
- DEC-165 @3858 [map,github,plan,state] refs: DEC-138 DEC-159 DEC-164 :: Wayfinding is the multi-sitting path from vague to plannable: a map of typed tickets survives between sessions and its frontier tells a fresh session where to stand.
- DEC-166 @3912 [map,github,plan,approval] refs: DEC-138 DEC-165 :: Wayfinding storage is chosen by config: tracker mode when `github.sync` is on with a working `gh`, otherwise local markdown, and `bin/wayfind.py` owns every tracker operation.
- DEC-167 @3948 [map,cost,github,digest] refs: DEC-159 DEC-166 :: Independent shallow frontier questions batch: `research` tickets fire in parallel, HITL tickets as one numbered round. Tracker mode: GitHub is the sole canonical store, no markdown shadow.
- DEC-168 @3994 [cost,docs,state] refs: DEC-138 :: Sub-issue closure is manual in both directions, measured; its conclusion that a parent is closed deliberately at ship acceptance is superseded by DEC-203.
- DEC-169 @4017 [orchestrator,org,skills] refs:  :: An absence check proves only that the wrong words are gone, so every absence assertion carries a presence assertion beside it.
- DEC-170 @4056 [org,cost,dispatch,state] refs: DEC-138 DEC-155 :: An agent whose decision or verdict changed because of advisor input discloses that in its DIGEST, naming what changed.
- DEC-171 @4101 [state,digest,domain,cost] refs: DEC-101 DEC-123 DEC-129 DEC-191 :: PyYAML is REQUIRED with no fallback, `safe_load` replaces hand-rolled YAML regex, init's prerequisite gate checks the import, and the two write hooks fail CLOSED without it.
- DEC-172 @4171 [digest,map,cost,skills] refs: DEC-122 :: The three-part agent return is wrapped in a ```yaml fence and `safe_load`ed; unfenced returns are blocked outright, so the 16 agent templates and the parser must ship together.
- DEC-173 @4211 [digest,qa,state,dispatch] refs: DEC-100 DEC-121 :: `n/a` is the one spelling for "this did not happen", legal on six more scalars; declining a gate while claiming PASS is rejected, keyed by persona not field.
- DEC-174 @4268 [plan,dispatch,cost,state] refs: DEC-142 DEC-173 DEC-183 :: The harness plans its own work but never EXECUTES changes to its own hooks, validators or gate scripts, each gate's test included; the category governs, not the enumeration.
- DEC-175 @4390 [digest,plan,qa,dispatch] refs: DEC-100 DEC-121 DEC-173 DEC-174 :: `dev`/`dev-ops` returns declare `task: T-NN|none`; `task_verify` binds only on a real task, and a self-reported gate FAILURE with PASS is now rejected by a second, fail-value gate.
- DEC-176 @4489 [cost,dispatch,plan,brief] refs:  :: At the BRIEF/PLAN signature gate the main session collects every change request from ONE review pass and dispatches ONE consolidated fix; no escape hatch, deliberately.
- DEC-177 @4511 [budget,cost,orchestrator,skills] refs:  :: A bounded runtime-environment question is measured before any claim about it is relayed — two surfaces only (`commands/harness.md`, `harness/SKILL.md`), never `harness-handoff`.
- DEC-178 @4533 [cost,budget,state,orchestrator] refs: DEC-134 DEC-148 DEC-159 :: Cost tracking is removed entirely: meter, budgets, invariant and reporting surfaces, because the meter never saw main-session work; historical `cost_usd` values stay as the only surviving record.
- DEC-179 @4579 [plan,domain,skills,approval] refs: DEC-174 :: Routing is resolved at plan time by delegating every literal `files:` path to `check-domain.sh --resolve`, so an ungranted surface becomes a declared `main-session-direct` step, never a mid-run rejected write.
- DEC-180 @4623 [state,domain,budget,worktree] refs: DEC-150 DEC-174 :: SHAPE is independent of DOMAIN: it binds every author including the main session, and a `PostToolUse` mode reports from DISK on the Edit and Bash routes.
- DEC-181 @4769 [budget,domain,state,skills] refs: DEC-135 DEC-180 DEC-188 :: CLAUDE.md gets an 80-line budget on all four write routes, enforced at check-domain.sh; its propagation-checker half is STRUCK under DEC-188.
- DEC-182 @4820 [plan,budget,state,cost] refs: DEC-154 DEC-181 :: The plan is `plan.yaml`, real YAML loaded with `safe_load`; PLAN.md was markdown that looked like YAML and failed `safe_load` 43 times in 44.
- DEC-183 @4907 [plan,github,tests,cost] refs: DEC-174 DEC-179 DEC-182 :: The route check is a step of the required `integration` CI job and asserts the plan COUNT; it is unguarded, because a guard cannot protect its own host step.
- DEC-184 @5005 [docs,github,brief,state] refs:  :: Design 0001, reconstructed stub: the work-graph engine is a deferred recorded design (Beads / fork / build when atomic claiming binds); consumed for effort #181 by the GitHub ruling.
- DEC-185 @5021 [docs] refs: DEC-184 :: Design 0002, reconstructed stub: the minimal roster is a starting point — management seats exist, and bootstrap is Harness-first with the outside-ship tripwire kept.
- DEC-187 @5035 [state,tests,qa] refs: DEC-35 DEC-36 DEC-163 :: The test matrix is per-project: every kind it names must be `active` or `excluded` with a signed decision, and a null `cmd` is BLOCKED, never an inferred skip.
- DEC-188 @5109 [state,docs,budget] refs: DEC-90 DEC-165 DEC-181 :: A decision the tree flatly contradicts is STRUCK from the record and removed from every gate, never marked stale; the propagation checker is deleted with it.
- DEC-189 @5147 [docs,state,plan,github] refs: DEC-85 DEC-151 DEC-174 :: The write guard resolves a target against TWO bases, harness and a declared product checkout, chosen from the TARGET; the four named harness paths are target-side only.
- DEC-190 @5226 [skills,gates,github,map] refs: DEC-101 DEC-171 :: `jsonschema` is a required dependency, declared in `harness-init`'s prerequisite gate and CI rather than a manifest; a missing import is a loud error, never a quieter mode.
- DEC-191 @5257 [state,github,cost,domain] refs: DEC-150 DEC-154 DEC-160 DEC-174 DEC-178 DEC-183 DEC-190 :: A feature's execution state has a closed key set — eleven top-level keys, `additionalProperties: false`, eight required — enforced on `check-domain.sh`'s write path and in CI.
- DEC-193 @5297 [worktree,domain,state,map] refs: DEC-150 DEC-151 DEC-153 DEC-174 DEC-180 DEC-189 :: Exactly two locations hold code under harness authority — `.claude/worktrees/<id>/` and `workspace_root/<repo>` — and any other checkout of this repository is refused on both write routes.
- DEC-194 @5393 [docs,state,plan,dispatch] refs: DEC-174 DEC-183 :: A partial layout migration is judged per coupled surface — features and docs independently, with no ordering tie — and a coupled reader matching neither form is cannot-verify, never clean.
- DEC-195 @5491 [domain,plan,cost,skills] refs: DEC-86 DEC-107 DEC-118 DEC-174 :: The four-angle simplify pass is the last build step, before `review_sha` pins, and a plan-flow step; it applies only where the guard grants the file, and never the validator lead.
- DEC-197 @5578 [qa,state,tests,skills] refs:  :: A test file matching two `detect` globs resolves to the kind that names it explicitly, never the catch-all; nothing implements it, so this entry is the enforcement.
- DEC-198 @5614 [budget,orchestrator,state,skills] refs: DEC-148 DEC-160 DEC-178 :: `budgets.orchestrator_context_warn_tokens`: an integer CONTEXT token count (not money), default 200000, in both harness.json and the upgrade template; crossing it advises, never refuses; propagated by `upgrade-config.py`'s generic merge.
- DEC-199 @5671 [digest,dispatch,plan,state] refs: DEC-193 :: One locked union-merging core, `harness_merge.py`, carries every concurrent-write artifact, and a named persona is single-flight per checkout; #551's dispatch cause closes, its false-reporting consequences do not.
- DEC-200 @5742 [github,state,approval,plan] refs: DEC-138 DEC-153 DEC-203 :: Ship derives a feature's PR number from the branch's exactly-one merged PR; DEC-138 write-only holds by destination AND absent local receipt; the read is inside DEC-203's read-back bound.
- DEC-201 @5809 [orchestrator,dispatch,state,budget] refs: DEC-118 DEC-120 DEC-148 DEC-158 DEC-159 DEC-198 DEC-199 :: Neither an orchestrator nor a lead waits: every dispatch ends the turn and the platform wakes the caller, which verifies claims against disk; the context threshold only advises.
- DEC-202 @5927 [skills,expertise,digest,hooks] refs: DEC-63 DEC-64 DEC-100 DEC-108 DEC-110 DEC-111 DEC-122 DEC-174 DEC-193 :: OMP is canonical; `.claude/skills` is the one authored skill tree and `.agents/skills` links to it for provider-neutral discovery.
- DEC-203 @5999 [github,state,plan,cost] refs: DEC-138 DEC-168 DEC-174 DEC-188 DEC-191 DEC-200 :: A ticket is open until its card reaches `Done`; the harness writes `Done` at ship and skips any parent with an open child; read-back bounded to seven purposes.
- DEC-204 @6142 [dispatch,orchestrator,github,budget] refs: DEC-199 DEC-201 DEC-202 DEC-203 :: OMP wakes main from an async orchestrator while nested Harness agents run at blocking task boundaries; claims are feature-scoped, process-owned, and targeted.
- DEC-205 @6240 [state,cost,skills,dispatch] refs: DEC-145 DEC-188 DEC-191 :: This file states current truth: no amendments, supersession is deletion, deleted numbers are never reused, and two mechanical checks — anchor rot and executable `claim:` markers — guard it.
