# Expertise — harness-product-lead
## Patterns
- P-01: WHEN a finding's remedy lands under a path DEC-174 marks main-session-direct DO record it as an operator question rather than routing it — no lead owns such a path, so the dispatch is refused only after the spawn is spent.
- P-02: WHEN your dispatch will assert what a harness bin script can do or how its verb is spelled DO read the control-plane copy under .agents/skills/harness/bin, never the feature worktree's: the worktree predates commits landed on main, and members inherit the wrong interface from you.
- P-03: WHEN check-domain refuses your run-dir write naming a claim another feature holds on your agent type DO return the fenced digest block inline for the tier above to land, never retry or hand-edit: the refusal reaches lead run bookkeeping only, and members' own writes are unaffected.
## Gotchas
## Outcomes
## Open
