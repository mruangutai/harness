# Expertise — harness-validator-lead

## Patterns (max 15)
- P-01: WHEN a reviewer cites a test line as evidence DO check the invocation and assertion
  before promoting it into an adequacy finding — a test label is prose, and its claim will
  travel two tiers as a measurement if you relay it unchecked.
- P-02: WHEN ranking advisory findings for a backlog DO order by irreversibility before
  severity — a low-severity defect whose failure cannot be undone outranks a medium that is
  cheaply reversible, and no member can see that axis from inside its own lens.

## Gotchas (max 15)

## Outcomes (max 10)

## Open (max 5)
- OQ-01: WHEN every relayed candidate is accepted by every member DO record the acceptance
  rate as an open calibration question, not a success signal — a member treating a lead's
  recall as instruction is indistinguishable from good sourcing in a single run.
