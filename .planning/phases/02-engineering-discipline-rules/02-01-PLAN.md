---
phase: 02-engineering-discipline-rules
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/config.json
  - .planning/harness.json
  - .claude/skills/harness/rules/SKILL.md
  - .claude/skills/harness/tdd/SKILL.md
  - .claude/skills/harness/rules/tdd-enforcement.md
  - .claude/skills/harness/rules/spec-driven.md
  - .claude/skills/harness/rules/systematic-debugging.md
autonomous: true
requirements: [ENG-01, ENG-02, ENG-03, ENG-04]

must_haves:
  truths:
    - "gsd-executor receives TDD Iron Law enforcement via agent_skills injection"
    - "gsd-planner receives spec-driven constraints via agent_skills injection"
    - "gsd-debugger receives systematic debugging protocol via agent_skills injection"
    - "tdd-enforcement.md contains Iron Law mandate, 13-item red flag checklist, deletion penalty, and zero-placeholder gate"
    - "spec-driven.md contains task completeness requirements and placeholder rejection for plan-writing time"
    - "systematic-debugging.md contains 4-phase RCA protocol and 3-failure cap"
    - "rules/SKILL.md routes agents to their correct rule files via role-based loading table"
  artifacts:
    - path: ".planning/config.json"
      provides: "agent_skills entries for gsd-planner and gsd-debugger"
      contains: "gsd-planner"
    - path: ".claude/skills/harness/rules/SKILL.md"
      provides: "Role-based routing index for 5 rule files"
      contains: "Role-Based Loading"
    - path: ".claude/skills/harness/tdd/SKILL.md"
      provides: "TDD skill index pointing to enforcement content"
      contains: "tdd-enforcement.md"
    - path: ".claude/skills/harness/rules/tdd-enforcement.md"
      provides: "TDD Iron Law enforcement + zero-placeholder gate"
      contains: "Iron Law"
    - path: ".claude/skills/harness/rules/spec-driven.md"
      provides: "Spec-driven planning constraints"
      contains: "Task Completeness Requirements"
    - path: ".claude/skills/harness/rules/systematic-debugging.md"
      provides: "4-phase RCA + 3-failure cap"
      contains: "Phase 1: Observe"
  key_links:
    - from: ".planning/config.json"
      to: ".claude/skills/harness/rules/SKILL.md"
      via: "agent_skills gsd-planner and gsd-debugger entries point to rules/ directory"
      pattern: "gsd-planner.*rules"
    - from: ".claude/skills/harness/rules/SKILL.md"
      to: ".claude/skills/harness/rules/spec-driven.md"
      via: "Role-Based Loading table routes gsd-planner to spec-driven.md"
      pattern: "gsd-planner.*spec-driven"
---

<objective>
Populate config infrastructure and three executor/planner/debugger rule files with full enforcement content.

Purpose: Establish the agent_skills injection paths for gsd-planner and gsd-debugger (extending the gsd-executor and gsd-verifier paths created in Phase 1), update the rules/SKILL.md and tdd/SKILL.md indexes, and write the three core discipline files: tdd-enforcement.md (ENG-01/ENG-03), spec-driven.md (ENG-02), and systematic-debugging.md (ENG-04).

Output: 7 files modified/created — config.json, harness.json, rules/SKILL.md, tdd/SKILL.md, tdd-enforcement.md, spec-driven.md, systematic-debugging.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-engineering-discipline-rules/02-CONTEXT.md
@.planning/phases/02-engineering-discipline-rules/02-RESEARCH.md

<interfaces>
<!-- Current config.json agent_skills structure (to be extended): -->
```json
"agent_skills": {
  "gsd-executor": [
    ".claude/skills/harness/tdd"
  ],
  "gsd-verifier": [
    ".claude/skills/harness/rules"
  ]
}
```

<!-- Current harness.json (tdd_exempt_plan_types referenced by tdd-enforcement.md): -->
```json
{
  "tdd_exempt_plan_types": ["config", "docs", "scaffolding"]
}
```

<!-- Current rules/SKILL.md stub (to be replaced): -->
```markdown
# Harness: Engineering Rules
Skill content delivered in Phase 2...
## Files
- tdd-enforcement.md (stub)
- verification-rules.md (stub)
- code-review.md (stub)
- systematic-debugging.md (stub)
```

<!-- Current tdd/SKILL.md stub (to be replaced): -->
```markdown
# Harness: TDD Enforcement
Skill content delivered in Phase 2...
## Status
Stub -- populated during Phase 2.
```

<!-- GSD buildAgentSkillsBlock() requires SKILL.md at {path}/SKILL.md — always point agent_skills entries to DIRECTORIES, not files. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Config infrastructure and SKILL.md indexes</name>
  <files>
    .planning/config.json
    .planning/harness.json
    .claude/skills/harness/rules/SKILL.md
    .claude/skills/harness/tdd/SKILL.md
  </files>
  <read_first>
    .planning/config.json
    .planning/harness.json
    .claude/skills/harness/rules/SKILL.md
    .claude/skills/harness/tdd/SKILL.md
    .planning/phases/02-engineering-discipline-rules/02-CONTEXT.md
    .planning/phases/02-engineering-discipline-rules/02-RESEARCH.md
  </read_first>
  <action>
**1. config.json** — Add `gsd-planner` and `gsd-debugger` entries to the `agent_skills` object (per D-02, D-03, D-19). The final agent_skills block must be:

```json
"agent_skills": {
  "gsd-executor": [
    ".claude/skills/harness/tdd"
  ],
  "gsd-verifier": [
    ".claude/skills/harness/rules"
  ],
  "gsd-planner": [
    ".claude/skills/harness/rules"
  ],
  "gsd-debugger": [
    ".claude/skills/harness/rules"
  ]
}
```

All entries point to DIRECTORIES (not files) because `buildAgentSkillsBlock()` in init.cjs appends `/SKILL.md` automatically. Do NOT change any other fields in config.json.

**2. harness.json** — Update the `agent_skills_reference` block to reflect the new mappings. Add `gsd-planner` and `gsd-debugger` entries:

```json
"agent_skills_reference": {
  "_comment": "Canonical skill mappings. Actual injection uses config.json agent_skills field.",
  "gsd-executor": [".claude/skills/harness/tdd"],
  "gsd-verifier": [".claude/skills/harness/rules"],
  "gsd-planner": [".claude/skills/harness/rules"],
  "gsd-debugger": [".claude/skills/harness/rules"]
}
```

Do NOT change any other fields in harness.json.

**3. rules/SKILL.md** — Replace the stub with the full index, implementing Option B from research (explicit role-based routing table per D-02, D-03, D-19). The exact content:

```markdown
# Harness: Engineering Rules

Engineering discipline rules injected into GSD agents via `agent_skills`. Each rule file is an enforcement specification — imperative, not guidance.

## Role-Based Loading

Read ONLY the file(s) that apply to your agent type:

| Agent Type | Files to Read |
|------------|---------------|
| gsd-executor | tdd-enforcement.md |
| gsd-planner | spec-driven.md |
| gsd-debugger | systematic-debugging.md |
| gsd-verifier | verification-rules.md |
| harness-code-reviewer | code-review.md |

Do NOT read files not listed for your agent type. Cross-contamination between discipline files causes conflicting instructions.

## Files

- tdd-enforcement.md — TDD Iron Law + zero-placeholder executor gate (ENG-01, ENG-03)
- spec-driven.md — Spec-driven planning constraints (ENG-02)
- systematic-debugging.md — 4-phase RCA + 3-failure cap (ENG-04)
- code-review.md — Two-stage spec + quality review protocol (ENG-05, ENG-06)
- verification-rules.md — Post-execution verification augmentations
```

**4. tdd/SKILL.md** — Replace the stub. This is the injection point for gsd-executor (per D-06). The exact content:

```markdown
# Harness: TDD Enforcement

TDD discipline rules for the gsd-executor agent. This skill is injected via `agent_skills` during plan execution.

## Authority

Follow ALL rules in the files below. No exceptions without explicit human approval.

## Files

- ../rules/tdd-enforcement.md — Read this file. It contains the TDD Iron Law, anti-rationalization red flags, deletion penalty, and zero-placeholder gate.

## Exemptions

Check `.planning/harness.json` field `tdd_exempt_plan_types`. If the current plan's type matches one of ["config", "docs", "scaffolding"], the TDD Iron Law does not apply. All other rules (zero-placeholder gate) still apply regardless of plan type.
```
  </action>
  <verify>
    <automated>node ~/.claude/get-shit-done/bin/gsd-tools.cjs agent-skills gsd-executor 2>/dev/null | grep -q "tdd" && node ~/.claude/get-shit-done/bin/gsd-tools.cjs agent-skills gsd-planner 2>/dev/null | grep -q "rules" && node ~/.claude/get-shit-done/bin/gsd-tools.cjs agent-skills gsd-debugger 2>/dev/null | grep -q "rules" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - config.json contains `"gsd-planner": [".claude/skills/harness/rules"]`
    - config.json contains `"gsd-debugger": [".claude/skills/harness/rules"]`
    - config.json retains existing `gsd-executor` and `gsd-verifier` entries unchanged
    - harness.json `agent_skills_reference` contains `gsd-planner` and `gsd-debugger`
    - rules/SKILL.md contains `## Role-Based Loading` with a 5-row agent routing table
    - rules/SKILL.md lists exactly 5 files: tdd-enforcement.md, spec-driven.md, systematic-debugging.md, code-review.md, verification-rules.md
    - tdd/SKILL.md contains `../rules/tdd-enforcement.md` reference
    - tdd/SKILL.md contains `tdd_exempt_plan_types` exemption check instruction
    - `gsd-tools agent-skills gsd-planner` outputs a block containing "rules" reference
    - `gsd-tools agent-skills gsd-debugger` outputs a block containing "rules" reference
  </acceptance_criteria>
  <done>config.json has 4 agent_skills entries, harness.json reference block updated, rules/SKILL.md has role-based routing for 5 agent types, tdd/SKILL.md points to tdd-enforcement.md with exemption check</done>
</task>

<task type="auto">
  <name>Task 2: TDD enforcement rule file (ENG-01 + ENG-03)</name>
  <files>
    .claude/skills/harness/rules/tdd-enforcement.md
  </files>
  <read_first>
    .claude/skills/harness/rules/tdd-enforcement.md
    .planning/phases/02-engineering-discipline-rules/02-CONTEXT.md
    .planning/phases/02-engineering-discipline-rules/02-RESEARCH.md
    ~/.claude/get-shit-done/references/tdd.md
  </read_first>
  <action>
Replace the stub content of `tdd-enforcement.md` with the full enforcement specification. This file is ADDITIVE to GSD's `~/.claude/get-shit-done/references/tdd.md` (per D-07) — do NOT duplicate the red-green-refactor cycle explanation.

The file MUST contain these exact sections in this order:

**Section 1: Authority**
State that this file is injected via agent_skills and all rules are mandatory.

**Section 2: The Iron Law (per D-08)**
Exact mandate text: "You MUST write a failing test before writing any production code. Code written before tests must be deleted and rewritten in correct TDD order. There are no exceptions without explicit human approval."

Subsections:
- **Deletion Penalty:** If production code is discovered to have been written before a failing test existed, the production code MUST be deleted — not kept as reference, not adapted. Delete it and restart in correct TDD order.
- **Human Approval Gate:** The ONLY way to skip TDD is explicit human approval in the current session. "The user implied it was okay" or "the task description didn't mention tests" are NOT valid approvals.

**Section 3: Red Flags — Stop Immediately If You Notice Any of These (per D-10)**
Use the exact 13-item checklist from RESEARCH.md Pattern 1:

```
- [ ] You are writing production code without a failing test already existing
- [ ] You wrote the test after the implementation
- [ ] You wrote multiple tests before any implementation
- [ ] You are refactoring while any test is red
- [ ] You added a feature while in the GREEN phase
- [ ] You skipped verifying the RED state (confirming the test actually fails before writing production code)
- [ ] You are about to say "this is just a simple function, tests aren't needed"
- [ ] You are about to say "the test would be too hard to write"
- [ ] You are about to say "I'll add tests after I get it working"
- [ ] You are about to say "we're in a rush, skip for now"
- [ ] You are about to say "it's obvious code, testing adds no value"
- [ ] You are modifying existing tests to make them pass instead of writing new code
- [ ] You cannot demonstrate the failing test run before your code changes
```

Include the instruction: "If you observe any of these, STOP, delete the out-of-order production code, and restart in correct TDD sequence."

**Section 4: Zero-Placeholder Gate (per D-11, ENG-03)**
Before executing any task, scan it for forbidden patterns:
- The literal string "TBD" or "TODO"
- "[placeholder]", "[fill in]", or similar bracket-notation deferral
- Task descriptions like "implement X" without exact file paths and code intent
- References like "similar to task N above" that defer specification
- Vague verbs without targets: "add error handling", "improve performance", "update the config"
- Absence of at least one concrete file path in the task

Protocol: "If any are found: STOP. Do not attempt to infer intent. Report the violation: 'Task [name] contains a placeholder at [location]. Plan revision required before execution.'"

**Section 5: Exemptions (per D-09)**
"Check `.planning/harness.json` field `tdd_exempt_plan_types`. If the current plan's `type` frontmatter matches one of the exempt types (currently: config, docs, scaffolding), the Iron Law (Section 2) and Red Flags (Section 3) do not apply. The Zero-Placeholder Gate (Section 4) ALWAYS applies regardless of plan type."

Use imperative voice throughout. No "should", "consider", "it's recommended". Every sentence is "MUST", "MUST NOT", or "STOP".
  </action>
  <verify>
    <automated>grep -c "Iron Law\|Red Flags\|Zero-Placeholder Gate\|Exemptions\|MUST" .claude/skills/harness/rules/tdd-enforcement.md | xargs -I{} test {} -ge 5 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - tdd-enforcement.md contains "## The Iron Law" section header
    - tdd-enforcement.md contains the exact string "You MUST write a failing test before writing any production code"
    - tdd-enforcement.md contains "Code written before tests must be deleted and rewritten in correct TDD order"
    - tdd-enforcement.md contains "## Red Flags" section with 13 checklist items (grep for `- \[ \]` returns 13 lines)
    - tdd-enforcement.md contains "## Zero-Placeholder Gate" section
    - tdd-enforcement.md contains "TBD" and "TODO" as forbidden patterns
    - tdd-enforcement.md contains "Plan revision required before execution"
    - tdd-enforcement.md contains "## Exemptions" section referencing `tdd_exempt_plan_types`
    - tdd-enforcement.md does NOT contain "red-green-refactor" (that lives in GSD's tdd.md)
    - tdd-enforcement.md does NOT contain "should", "consider", "it's recommended", "where possible" (guidance language)
  </acceptance_criteria>
  <done>tdd-enforcement.md is a complete enforcement specification with Iron Law mandate, 13-item red flag checklist, zero-placeholder gate with 6 forbidden patterns, deletion penalty, and harness.json exemption check</done>
</task>

<task type="auto">
  <name>Task 3: Spec-driven and systematic-debugging rule files (ENG-02 + ENG-04)</name>
  <files>
    .claude/skills/harness/rules/spec-driven.md
    .claude/skills/harness/rules/systematic-debugging.md
  </files>
  <read_first>
    .claude/skills/harness/rules/systematic-debugging.md
    .planning/phases/02-engineering-discipline-rules/02-CONTEXT.md
    .planning/phases/02-engineering-discipline-rules/02-RESEARCH.md
  </read_first>
  <action>
**1. Create `.claude/skills/harness/rules/spec-driven.md`** (NEW file, per D-02, D-12, D-14)

This file is injected into gsd-planner via agent_skills. It constrains what plans are allowed to contain. The file MUST contain these sections:

**Section 1: Authority**
"This file is injected via agent_skills into gsd-planner. Follow ALL rules below. These constraints apply at plan-writing time."

**Section 2: Task Completeness Requirements (per D-12)**
Every plan task MUST include:
1. Exact file paths — not "update the config file" but "edit `.planning/config.json`"
2. Complete code intent — not "implement X" but the actual logic, types, structure, and values
3. A verification step with the exact command and expected output
4. A reference to the CONTEXT.md decision (D-XX) or REQUIREMENTS.md requirement (REQ-XX) it satisfies

**Section 3: Placeholder Rejection (per D-12, D-14)**
Reject any task that contains:
- "TBD", "TODO", or any placeholder notation
- Vague verbs without targets ("add error handling", "improve performance")
- Instructions that defer specification ("similar to above", "follow existing pattern")
- "implement X" without specifying what X produces, which files it touches, and how to verify it

If a task cannot be fully specified, it is a signal that discuss-phase or research-phase is incomplete. Return the task to the orchestrator with the specific gap identified.

**Section 4: Spec Traceability (per D-14)**
"CONTEXT.md is the spec. Do NOT create a separate spec artifact. Every plan task must trace back to a specific CONTEXT.md decision or REQUIREMENTS.md requirement. If a task cannot cite its source, it is either out of scope or the spec is incomplete."

**Section 5: Verification Requirements**
Every task's `<verify>` block MUST contain an `<automated>` command that:
- Runs in under 60 seconds
- Returns a clear PASS/FAIL signal
- Does not require human interpretation

If no automated verification is possible, the task must include `<automated>MISSING — [explanation of what test must be created first]</automated>`.

---

**2. Replace stub in `.claude/skills/harness/rules/systematic-debugging.md`** (per D-03, D-19, D-20, D-21)

This file is injected into gsd-debugger via agent_skills. The file MUST contain these sections:

**Section 1: Authority**
"This file is injected via agent_skills into gsd-debugger. Follow ALL rules below. Evidence before fixes. Hypothesis before code changes."

**Section 2: The 4-Phase Protocol (per D-20)**

Phase 1 — Observe:
- Reproduce the bug consistently before any code change
- Document the exact failure: input, expected output, actual output
- Check: does it fail the same way every time? Under what conditions?
- Capture: error messages, stack traces, log output, relevant state

Phase 2 — Hypothesize:
- Form ONE specific hypothesis: "The bug is caused by X because Y"
- Write the hypothesis down (in the session, not just internally)
- Identify what evidence would confirm or falsify this hypothesis
- Do NOT touch any code during this phase

Phase 3 — Test:
- Gather the evidence identified in Phase 2
- Write a failing test that proves the hypothesis IF possible
- Confirm or falsify the hypothesis with evidence, not intuition
- If hypothesis is falsified, return to Phase 2 with a new hypothesis

Phase 4 — Fix:
- ONLY after Phase 3 confirms the hypothesis
- Implement the minimal change that fixes the confirmed root cause
- Run the test from Phase 3 to confirm it now passes
- Run the full test suite to confirm no regression

**Section 3: 3-Failure Cap (per D-21)**
"If you have attempted 3 fixes and the bug is not resolved: STOP. Do not attempt a 4th fix. Report to the user: which hypotheses were tested, what evidence was gathered, and what remains uncertain. Request human intervention."

"This cap is separate from GSD's `node_repair_budget` (which handles plan-level retries). The 3-failure cap applies within a single debugging session."

**Section 4: Forbidden Patterns**
- "Let's just try X" — No fix attempts without a confirmed hypothesis
- "It might be Y, let me change it and see" — Changing code to test a hypothesis is not testing; use logging, assertions, or read-only investigation
- "I'll fix this and the other thing while I'm here" — One fix per cycle. Do not bundle unrelated changes.

Use imperative voice throughout both files. No guidance language.
  </action>
  <verify>
    <automated>test -f .claude/skills/harness/rules/spec-driven.md && grep -q "Task Completeness" .claude/skills/harness/rules/spec-driven.md && grep -q "Phase 1: Observe" .claude/skills/harness/rules/systematic-debugging.md && grep -q "3-Failure Cap" .claude/skills/harness/rules/systematic-debugging.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - spec-driven.md exists as a new file (not a stub)
    - spec-driven.md contains "## Task Completeness Requirements" section
    - spec-driven.md contains "CONTEXT.md" at least 2 times (traceability references)
    - spec-driven.md contains "## Placeholder Rejection" section with "TBD" and "TODO" as rejected patterns
    - spec-driven.md contains "## Spec Traceability" section stating CONTEXT.md is the spec
    - spec-driven.md does NOT contain "should", "consider", "it's recommended"
    - systematic-debugging.md contains "## The 4-Phase Protocol" section header
    - systematic-debugging.md contains "Phase 1: Observe", "Phase 2: Hypothesize", "Phase 3: Test", "Phase 4: Fix" subsections
    - systematic-debugging.md contains "## 3-Failure Cap" section with "STOP" and "Do not attempt a 4th fix"
    - systematic-debugging.md contains "## Forbidden Patterns" section with 3 anti-patterns
    - systematic-debugging.md contains "`node_repair_budget`" reference explaining scope distinction (per D-21)
    - systematic-debugging.md does NOT contain "should", "consider", "it's recommended"
  </acceptance_criteria>
  <done>spec-driven.md is a complete planner constraint spec with task completeness, placeholder rejection, and spec traceability. systematic-debugging.md is a complete 4-phase RCA protocol with 3-failure cap and forbidden patterns.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A — no user input, credentials, or network requests handled by deliverables. This plan produces markdown rule files and JSON config edits only.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | config.json agent_skills | accept | Project-local file; no external attack surface. Git history tracks changes. |
| T-02-02 | Information Disclosure | rule files | accept | No secrets, credentials, or PII in any rule file. All content is enforcement prompts. |
</threat_model>

<verification>
1. `node ~/.claude/get-shit-done/bin/gsd-tools.cjs agent-skills gsd-executor` returns block with tdd/ reference
2. `node ~/.claude/get-shit-done/bin/gsd-tools.cjs agent-skills gsd-planner` returns block with rules/ reference
3. `node ~/.claude/get-shit-done/bin/gsd-tools.cjs agent-skills gsd-debugger` returns block with rules/ reference
4. `grep "Iron Law" .claude/skills/harness/rules/tdd-enforcement.md` returns a match
5. `grep "Task Completeness" .claude/skills/harness/rules/spec-driven.md` returns a match
6. `grep "3-Failure Cap" .claude/skills/harness/rules/systematic-debugging.md` returns a match
7. rules/SKILL.md contains a 5-row role-based loading table
</verification>

<success_criteria>
- config.json has 4 agent_skills entries (gsd-executor, gsd-verifier, gsd-planner, gsd-debugger)
- rules/SKILL.md has role-based routing for all 5 agent types with explicit "read only your files" instruction
- tdd/SKILL.md points to tdd-enforcement.md with exemption check
- tdd-enforcement.md: Iron Law + 13 red flags + zero-placeholder gate + exemptions (no guidance language)
- spec-driven.md: task completeness + placeholder rejection + spec traceability (no guidance language)
- systematic-debugging.md: 4-phase RCA + 3-failure cap + forbidden patterns (no guidance language)
- All 3 `gsd-tools agent-skills` smoke tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-engineering-discipline-rules/02-01-SUMMARY.md`
</output>
