---
phase: 02-engineering-discipline-rules
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - .claude/skills/harness/rules/code-review.md
  - .claude/skills/harness/rules/verification-rules.md
  - .claude/agents/harness-code-reviewer.md
  - CLAUDE.md
autonomous: true
requirements: [ENG-05, ENG-06]
must_haves:
  truths:
    - "After /gsd-execute-phase completes on an implementation plan, CLAUDE.md instructs Claude to spawn harness-code-reviewer before shipping"
    - "harness-code-reviewer agent exists with read-only tools and two-stage review protocol"
    - "code-review.md contains Stage 1 (spec compliance) and Stage 2 (code quality) as structurally separated gates"
    - "verification-rules.md augments gsd-verifier with harness-specific post-execution checks"
    - "CLAUDE.md harness section stays under 50 tokens of non-comment text"
  artifacts:
    - path: ".claude/agents/harness-code-reviewer.md"
      provides: "Code review agent with two-stage protocol"
      contains: "Stage 1"
    - path: ".claude/skills/harness/rules/code-review.md"
      provides: "Two-stage review protocol (spec compliance then code quality)"
      contains: "Spec Compliance"
    - path: ".claude/skills/harness/rules/verification-rules.md"
      provides: "Post-execution verification augmentations for gsd-verifier"
      contains: "Authority"
    - path: "CLAUDE.md"
      provides: "Spec gate and review gate trigger instructions"
      contains: "harness-code-reviewer"
  key_links:
    - from: "CLAUDE.md"
      to: ".claude/agents/harness-code-reviewer.md"
      via: "Gate instruction triggers agent spawn after execute-phase"
      pattern: "harness-code-reviewer"
    - from: ".claude/agents/harness-code-reviewer.md"
      to: ".claude/skills/harness/rules/code-review.md"
      via: "Agent role prompt references the review protocol"
      pattern: "code-review.md"
---

<objective>
Create the code review agent, populate the code-review and verification-rules files, and add gate trigger instructions to CLAUDE.md.

Purpose: Complete the engineering discipline layer by adding the execute-to-ship review gate (ENG-05) with two-stage independent review (ENG-06), and augment the verifier with harness-specific checks. The CLAUDE.md gate instructions activate these at the correct workflow boundaries.

Output: 4 files modified/created -- harness-code-reviewer.md agent, code-review.md, verification-rules.md, CLAUDE.md harness section updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-engineering-discipline-rules/02-CONTEXT.md
@.planning/phases/02-engineering-discipline-rules/02-RESEARCH.md
@.planning/phases/02-engineering-discipline-rules/02-01-SUMMARY.md

<interfaces>
<!-- harness-ceo-reviewer.md agent pattern (to match for harness-code-reviewer.md).
     YAML frontmatter fields: name, description, tools. -->

From .claude/agents/harness-ceo-reviewer.md:
  name: harness-ceo-reviewer
  description: "CEO/Product perspective gate"
  tools: [Read, Glob, Grep, Bash]

<!-- Current CLAUDE.md harness section (to be extended): -->

From CLAUDE.md between GSD:harness-start/end markers:
  Line 1: "Unified workflow harness active. Skills: `.claude/skills/harness/`"
  Line 2: "Config: `.planning/harness.json`"
  Line 3: "When dispatching subagents, include `.planning/harness.json` in the `<files_to_read>` block."

<!-- harness.json tdd_exempt_plan_types (code review gate uses same exemption list per D-17): -->
  tdd_exempt_plan_types: ["config", "docs", "scaffolding"]

<!-- rules/SKILL.md (created by Plan 01) routes harness-code-reviewer to code-review.md -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verification-rules and code-review rule files (ENG-05 + ENG-06)</name>
  <files>
    .claude/skills/harness/rules/verification-rules.md
    .claude/skills/harness/rules/code-review.md
  </files>
  <read_first>
    .claude/skills/harness/rules/verification-rules.md
    .claude/skills/harness/rules/code-review.md
    .planning/phases/02-engineering-discipline-rules/02-CONTEXT.md
    .planning/phases/02-engineering-discipline-rules/02-RESEARCH.md
    ~/.claude/get-shit-done/workflows/verify-phase.md
  </read_first>
  <action>
**1. Replace stub in `.claude/skills/harness/rules/verification-rules.md`** (per D-05)

This file augments gsd-verifier. Read `~/.claude/get-shit-done/workflows/verify-phase.md` first to identify what GSD's verifier already checks, then add ONLY what is missing. The file MUST contain:

**Section 1: Authority**
"This file is injected via agent_skills into gsd-verifier. These rules augment GSD's built-in verification -- do NOT duplicate checks GSD already performs."

**Section 2: Harness-Specific Verification**
After GSD's standard verification completes, additionally verify:
1. **TDD compliance check** -- For implementation plans: confirm test files exist for every new production file. If a production file has no corresponding test, flag it.
2. **Spec traceability check** -- Every plan task should trace to a CONTEXT.md decision or REQUIREMENTS.md requirement. Flag tasks with no traceability.
3. **Gate completion check** -- If the plan type is "execute" (implementation), verify that the code review gate was triggered (harness-code-reviewer was spawned). If not, flag as incomplete.

**Section 3: What NOT to Duplicate**
"GSD's verifier already checks: must_haves truths/artifacts/key_links, plan success criteria, file existence, automated test commands. Do NOT re-check these."

Use imperative voice. No guidance language.

**2. Replace stub in `.claude/skills/harness/rules/code-review.md`** (per D-04, D-16)

This file is the harness-code-reviewer agent's role prompt. It is NOT a SKILL.md injection -- it is loaded directly by the agent. The file MUST contain these structurally separated sections:

**Section 1: Authority**
"You are harness-code-reviewer. You have read-only access (Read, Glob, Grep). You do NOT modify files. You return findings for the executor to act on."

**Section 2: Review Protocol Overview**
"Two-stage review. Stage 1 MUST complete and PASS before Stage 2 begins. Do not mix concerns across stages."

**Section 3: Stage 1 -- Spec Compliance (per D-16)**
Read: CONTEXT.md decisions (locked decisions section), changed files from the plan's SUMMARY.md
Ask:
1. Does every changed file serve a decision documented in CONTEXT.md?
2. Are there changes not required by any CONTEXT.md decision? (scope creep)
3. Are there CONTEXT.md decisions with no corresponding code change? (omission)
4. Do implementation details match the specific values/approaches in CONTEXT.md?
Output: PASS or FAIL with specific findings (file path, decision ID, violation)

**Section 4: Stage 2 -- Code Quality (per D-16)**
Read: Changed files only (do NOT re-read CONTEXT.md -- spec compliance is already verified)
Ask:
1. Are there unhandled edge cases?
2. Are there naming inconsistencies?
3. Are there complexity hotspots that should be decomposed?
4. Are there missing error paths?
5. Is there dead code or unreachable branches?
Output: PASS or FAIL with specific findings (file path, line reference, issue)

"ONLY proceed to Stage 2 if Stage 1 is PASS."

**Section 5: Loop Condition**
"If either stage FAILs, return findings to the executor for revision. Re-review after revision. Maximum 3 review cycles total. After 3 cycles, escalate to the user with unresolved findings."

**Section 6: Gate Applicability (per D-17)**
"This review applies to implementation plans only. Plans with type matching `tdd_exempt_plan_types` in harness.json (currently: config, docs, scaffolding) skip the code review gate."

Use imperative voice throughout.
  </action>
  <verify>
    <automated>grep -q "Stage 1" .claude/skills/harness/rules/code-review.md && grep -q "Stage 2" .claude/skills/harness/rules/code-review.md && grep -q "Harness-Specific Verification" .claude/skills/harness/rules/verification-rules.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - code-review.md contains "## Stage 1" with "Spec Compliance" in the header
    - code-review.md contains "## Stage 2" with "Code Quality" in the header
    - code-review.md contains "ONLY proceed to Stage 2 if Stage 1 is PASS"
    - code-review.md contains "Maximum 3 review cycles"
    - code-review.md contains "read-only access" and lists Read, Glob, Grep
    - code-review.md contains "CONTEXT.md" in Stage 1 section
    - code-review.md contains "tdd_exempt_plan_types" reference in gate applicability section
    - code-review.md does NOT contain "should", "consider", "it's recommended"
    - verification-rules.md contains "## Harness-Specific Verification" section
    - verification-rules.md contains "TDD compliance check", "Spec traceability check", "Gate completion check"
    - verification-rules.md contains "## What NOT to Duplicate" section
    - verification-rules.md does NOT contain "should", "consider", "it's recommended"
  </acceptance_criteria>
  <done>code-review.md has structurally separated Stage 1 (spec compliance) and Stage 2 (code quality) with loop condition and gate applicability. verification-rules.md augments gsd-verifier with 3 harness-specific checks without duplicating GSD's built-in verification.</done>
</task>

<task type="auto">
  <name>Task 2: Code reviewer agent and CLAUDE.md gate instructions (ENG-05 + ENG-06)</name>
  <files>
    .claude/agents/harness-code-reviewer.md
    CLAUDE.md
  </files>
  <read_first>
    .claude/agents/harness-ceo-reviewer.md
    .claude/agents/harness-eng-reviewer.md
    CLAUDE.md
    .planning/phases/02-engineering-discipline-rules/02-CONTEXT.md
    .planning/harness.json
  </read_first>
  <action>
**1. Create `.claude/agents/harness-code-reviewer.md`** (per D-18, D-04, D-16)

Follow the same YAML frontmatter pattern as harness-ceo-reviewer.md but with READ-ONLY tools (per Pitfall 4 from research -- reviewer must NOT have Write/Edit/Bash tools).

The file must have YAML frontmatter with exactly these fields:
  name: harness-code-reviewer
  description: "Two-stage code review -- spec compliance then code quality -- for implementation plans"
  tools: [Read, Glob, Grep]

Then the body content:

# Harness: Code Reviewer

Two-stage code review agent spawned at the execute-to-ship boundary for implementation plans.

## Role

You review code changes for spec compliance and code quality. You do NOT modify files.
You return findings for the executor to act on.

## Protocol

Read `.claude/skills/harness/rules/code-review.md` for the full two-stage review protocol.

## Inputs

When spawned, you receive:
1. The phase CONTEXT.md (locked decisions = the spec)
2. The plan SUMMARY.md (files changed, tasks completed)
3. The changed files themselves

## Output Format

Return a structured findings report:

### Stage 1: Spec Compliance
- **Result:** PASS or FAIL
- **Findings:** [list of specific violations with file path and decision ID]

### Stage 2: Code Quality
- **Result:** PASS or FAIL (only if Stage 1 passed)
- **Findings:** [list of specific issues with file path and line reference]

### Verdict
- **Overall:** PASS, FAIL, or ESCALATE (after 3 cycles)

**2. Update CLAUDE.md harness section** (per D-22, D-23)

The current harness section between `<!-- GSD:harness-start -->` and `<!-- GSD:harness-end -->` contains 3 lines of non-comment text (~21 tokens). Add 2 gate instruction lines (per D-22) while keeping total non-comment text under 50 tokens.

Replace the content between the markers with exactly:

```
<!-- GSD:harness-start -->
## Harness

Unified workflow harness active. Skills: `.claude/skills/harness/`
Config: `.planning/harness.json`

When dispatching subagents, include `.planning/harness.json` in the `<files_to_read>` block.
Before /gsd-plan-phase: verify CONTEXT.md has approaches-with-tradeoffs and user approval.
After /gsd-execute-phase on implementation plans: spawn harness-code-reviewer before /gsd-ship.
<!-- GSD:harness-end -->
```

The two new lines are the spec gate (D-13) and review gate (D-15). They are concise trigger instructions -- full protocol detail lives in the agent/skill files.

IMPORTANT: Do NOT modify anything outside the `GSD:harness-start/end` markers. Do NOT modify any other section of CLAUDE.md.
  </action>
  <verify>
    <automated>test -f .claude/agents/harness-code-reviewer.md && grep -q "harness-code-reviewer" .claude/agents/harness-code-reviewer.md && grep -q "harness-code-reviewer" CLAUDE.md && grep -q "approaches-with-tradeoffs" CLAUDE.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - `.claude/agents/harness-code-reviewer.md` exists
    - harness-code-reviewer.md YAML frontmatter contains `name: harness-code-reviewer`
    - harness-code-reviewer.md YAML frontmatter tools list contains ONLY Read, Glob, Grep (no Write, Edit, Bash, MultiEdit)
    - harness-code-reviewer.md contains "code-review.md" reference for protocol
    - harness-code-reviewer.md contains "Stage 1: Spec Compliance" and "Stage 2: Code Quality" in output format
    - CLAUDE.md contains "Before /gsd-plan-phase: verify CONTEXT.md has approaches-with-tradeoffs" between harness markers
    - CLAUDE.md contains "spawn harness-code-reviewer before /gsd-ship" between harness markers
    - CLAUDE.md harness section non-comment text is under 50 tokens (count manually: ~45 tokens with both new lines)
    - CLAUDE.md content outside GSD:harness-start/end markers is unchanged
    - `grep "harness-code-reviewer" CLAUDE.md` returns the review gate instruction
  </acceptance_criteria>
  <done>harness-code-reviewer.md agent exists with read-only tools and two-stage output format. CLAUDE.md harness section has spec gate and review gate instructions under the 50-token budget.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A -- no user input, credentials, or network requests handled by deliverables. This plan produces markdown rule files, one agent definition, and a CLAUDE.md edit only.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Elevation of Privilege | harness-code-reviewer.md tools | mitigate | Agent YAML frontmatter lists Read, Glob, Grep ONLY -- no Write, Edit, Bash, or MultiEdit. Prevents reviewer from modifying code it reviews (per D-16 and Pitfall 4). |
| T-02-04 | Tampering | CLAUDE.md gate instructions | accept | Project-local file; gate instructions are triggers only. Full protocol lives in agent/skill files. |
</threat_model>

<verification>
1. `cat .claude/agents/harness-code-reviewer.md` shows YAML with tools: [Read, Glob, Grep] only
2. `grep "Stage 1" .claude/skills/harness/rules/code-review.md` returns "Spec Compliance"
3. `grep "Stage 2" .claude/skills/harness/rules/code-review.md` returns "Code Quality"
4. `grep "harness-code-reviewer" CLAUDE.md` returns the review gate instruction
5. `grep "approaches-with-tradeoffs" CLAUDE.md` returns the spec gate instruction
6. `grep "Harness-Specific" .claude/skills/harness/rules/verification-rules.md` returns verification section
</verification>

<success_criteria>
- harness-code-reviewer.md agent exists with read-only tools (Read, Glob, Grep) and references code-review.md protocol
- code-review.md has structurally separated Stage 1 (spec compliance) and Stage 2 (code quality) with max 3 review cycles
- verification-rules.md augments gsd-verifier with 3 harness-specific checks (TDD compliance, spec traceability, gate completion)
- CLAUDE.md harness section has spec gate + review gate instructions, total non-comment text under 50 tokens
- All 6 ENG requirements (ENG-01 through ENG-06) are covered across Plans 01 and 02
</success_criteria>

<output>
After completion, create `.planning/phases/02-engineering-discipline-rules/02-02-SUMMARY.md`
</output>
