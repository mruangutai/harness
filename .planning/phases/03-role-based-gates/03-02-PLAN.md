---
phase: 03-role-based-gates
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/agents/harness-qa-reviewer.md
  - .claude/agents/harness-security-reviewer.md
  - CLAUDE.md
autonomous: true
requirements:
  - ROLE-03
  - ROLE-04

must_haves:
  truths:
    - "harness-qa-reviewer.md contains a two-phase protocol: spec-only Phase 1 produces test cases, then implementation Phase 2 verifies them"
    - "harness-security-reviewer.md contains a self-scoping step that can skip audit on non-security phases"
    - "harness-security-reviewer.md runs OWASP Top 10 + STRIDE analysis when security-sensitive code is detected"
    - "CLAUDE.md harness section contains CEO trigger and QA/Security pre-ship trigger instructions"
    - "Neither agent contains gstack-specific references"
    - "Both agents produce advisory reports, not hard blocks"
  artifacts:
    - path: ".claude/agents/harness-qa-reviewer.md"
      provides: "QA adversarial testing gate agent"
      contains: "## Protocol"
    - path: ".claude/agents/harness-security-reviewer.md"
      provides: "Security audit gate agent"
      contains: "## Protocol"
    - path: "CLAUDE.md"
      provides: "Gate trigger instructions for CEO and QA/Security"
      contains: "harness-ceo-reviewer"
  key_links:
    - from: ".claude/agents/harness-qa-reviewer.md"
      to: ".planning/phases/*/CONTEXT.md"
      via: "Phase 1 reads CONTEXT.md as spec before any source files"
      pattern: "CONTEXT\\.md"
    - from: ".claude/agents/harness-security-reviewer.md"
      to: ".planning/phases/*/*-PLAN.md"
      via: "Self-scoping reads PLAN.md to detect security-sensitive keywords"
      pattern: "PLAN\\.md"
    - from: "CLAUDE.md"
      to: ".claude/agents/harness-ceo-reviewer.md"
      via: "Trigger instruction spawns CEO reviewer at init/scope-change"
      pattern: "spawn harness-ceo-reviewer"
    - from: "CLAUDE.md"
      to: ".claude/agents/harness-qa-reviewer.md"
      via: "Trigger instruction spawns QA reviewer before ship"
      pattern: "spawn harness-qa-reviewer"
---

<objective>
Create two new reviewer agent files (harness-qa-reviewer.md and harness-security-reviewer.md) and add CEO + QA/Security trigger instructions to the CLAUDE.md harness section.

Purpose: Delivers ROLE-03 (QA adversarial testing with spec-then-verify protocol) and ROLE-04 (Security audit with OWASP/STRIDE and self-scoping), plus wires all 4 role gates into CLAUDE.md trigger instructions (D-19).
Output: Two new agent files in .claude/agents/ and updated CLAUDE.md harness section with 5 total trigger instructions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-role-based-gates/03-CONTEXT.md
@.planning/phases/03-role-based-gates/03-RESEARCH.md

<interfaces>
<!-- Structural template all Phase 3 agents must follow (from harness-code-reviewer.md): -->

```markdown
---
name: harness-<role>-reviewer
description: "<one-line role description>"
tools:
  - Read
  - Glob
  - Grep
  - Bash
---

# Harness: <Role> Reviewer

<One-line purpose statement>

## Role

<What this agent does and does NOT do>

## Protocol

<How the agent executes -- phase sequence if multi-phase>

## Inputs

When spawned, you receive:
<numbered list of inputs>

## Output Format

<structured output template>
```

<!-- Current CLAUDE.md harness section (lines 201-210): -->

```markdown
<!-- GSD:harness-start -->
## Harness

Unified workflow harness active. Skills: `.claude/skills/harness/`
Config: `.planning/harness.json`

When dispatching subagents, include `.planning/harness.json` in the `<files_to_read>` block.
Before /gsd-plan-phase: verify CONTEXT.md has approaches-with-tradeoffs and user approval.
After /gsd-execute-phase on implementation plans: spawn harness-code-reviewer before /gsd-ship.
<!-- GSD:harness-end -->
```

Phase 3 adds two lines BEFORE the `<!-- GSD:harness-end -->` marker:
- "At new-project init or scope-change: spawn harness-ceo-reviewer."
- "Before /gsd-ship: spawn harness-qa-reviewer and harness-security-reviewer."

<!-- Gstack content patterns to adapt (from .planning/research/FEATURES.md): -->

QA sources:
- /qa: Tests in real browser, fixes bugs atomically, auto-generates regression tests
- /qa-only: Finds bugs without code changes -- QA reporter mode

Security sources:
- /cso: OWASP Top 10 + STRIDE threat modeling security audit

<!-- QA Two-Phase Pattern (from 03-RESEARCH.md): -->

Phase 1 (SPEC ONLY):
  - Read: phase CONTEXT.md + success criteria
  - Output: test case list with expected behaviors
  - INSTRUCTION: Do NOT read source files yet.

Phase 2 (IMPLEMENTATION VERIFICATION):
  - Read: source files (Glob/Grep to find changed files)
  - For each test case: verify pass or fail against implementation
  - Output: test case status + spec gaps + regression test suggestions

<!-- Security Self-Scoping Pattern (from 03-RESEARCH.md): -->

Step 1: Read PLAN.md (or phase summary)
Step 2: Scan for security-sensitive keywords: auth, session, cookie, token,
        API key, password, hash, encrypt, database, SQL, HTTP, CORS, CSRF,
        XSS, injection, permission, role, privilege
Step 3a: If found -> run full OWASP Top 10 + STRIDE analysis
Step 3b: If not found -> output "No security-sensitive changes found -- audit skipped."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create harness-qa-reviewer.md with spec-then-verify protocol (ROLE-03)</name>
  <files>.claude/agents/harness-qa-reviewer.md</files>
  <read_first>
    - .claude/agents/harness-code-reviewer.md (structural template to follow)
    - .planning/research/FEATURES.md (gstack /qa and /qa-only patterns)
    - .planning/phases/03-role-based-gates/03-CONTEXT.md (D-12, D-13, D-14 locked decisions)
    - .planning/phases/03-role-based-gates/03-RESEARCH.md (QA two-phase pattern specification)
  </read_first>
  <action>
Create a new file .claude/agents/harness-qa-reviewer.md with this complete content:

**YAML frontmatter:**
```yaml
---
name: harness-qa-reviewer
description: "QA adversarial testing gate -- spec-first test case generation then implementation verification"
tools:
  - Read
  - Glob
  - Grep
  - Bash
---
```

**# Harness: QA Reviewer**

One-line: "QA adversarial testing agent spawned before /gsd-ship to verify implementation against spec without implementation bias."

**## Role**

State what this agent does: generates test cases from the spec (CONTEXT.md) independently of implementation, then verifies each test case against the actual source code. This two-phase approach prevents implementation knowledge from contaminating test expectations. State what it does NOT do: does not block the workflow, does not modify files, does not run browser automation (v1 limitation). All output is advisory (per D-01).

**## Protocol**

Two-phase sequential protocol. CRITICAL: Complete Phase 1 in full and output all test cases BEFORE reading any source files. This sequencing is mandatory -- it prevents implementation details from contaminating spec-derived expectations.

**Phase 1: Spec Analysis (NO source code)**

1. Read phase CONTEXT.md -- extract all locked decisions (D-XX entries)
2. Read ROADMAP.md phase section -- extract success criteria
3. For each decision and success criterion, generate test cases:
   - **Test case ID** (TC-01, TC-02, ...)
   - **Source** (which D-XX or success criterion)
   - **Description** (what behavior is expected)
   - **Expected outcome** (observable result)
4. Output the complete test case list
5. STOP. Do NOT proceed to Phase 2 until all test cases are written.

**Phase 2: Implementation Verification**

1. Use Glob/Grep to find source files modified in this phase (check SUMMARY.md or scan phase directory)
2. For each test case from Phase 1:
   - Read the relevant source file(s)
   - Verify whether the expected behavior is implemented
   - Mark: PASS (implemented as specified), FAIL (missing or incorrect), PARTIAL (partially implemented)
3. Identify spec gaps -- requirements in CONTEXT.md that have no corresponding implementation
4. Generate regression test suggestions -- specific test descriptions that would catch regressions in the implemented behaviors

**## Inputs**

When spawned, you receive:
1. Phase `CONTEXT.md` -- locked decisions (the spec, read in Phase 1)
2. `.planning/ROADMAP.md` -- phase success criteria (read in Phase 1)
3. Phase `SUMMARY.md` files -- files changed (read in Phase 2 only)
4. `.planning/harness.json` -- gate configuration
5. Access to codebase via Glob/Grep -- source files (read in Phase 2 only)

**## Output Format**

```markdown
# QA Review

## Phase 1: Test Cases (from spec)

| TC ID | Source | Description | Expected Outcome |
|-------|--------|-------------|-----------------|
| TC-01 | D-XX | [behavior] | [observable result] |
| TC-02 | SC-1 | [behavior] | [observable result] |

---

## Phase 2: Verification Results

| TC ID | Status | Evidence | File |
|-------|--------|----------|------|
| TC-01 | PASS/FAIL/PARTIAL | [what was found] | [file path] |

### Summary
- **Pass:** X/Y test cases
- **Fail:** X/Y test cases
- **Partial:** X/Y test cases

## Spec Gaps
- [Requirements from CONTEXT.md with no corresponding implementation]

## Regression Test Suggestions
1. [Test description that would catch regression in implemented behavior]
2. [Test description]

## Advisory Verdict
- **Ship / Fix Required / Review Needed**
- [1-2 sentence recommendation]
```

Ensure NO gstack-specific references appear anywhere in the file.
  </action>
  <verify>
    <automated>test -f .claude/agents/harness-qa-reviewer.md && grep -c "## Role" .claude/agents/harness-qa-reviewer.md && grep -c "## Protocol" .claude/agents/harness-qa-reviewer.md && grep -c "Phase 1" .claude/agents/harness-qa-reviewer.md && grep -c "Phase 2" .claude/agents/harness-qa-reviewer.md && grep -c "Do NOT" .claude/agents/harness-qa-reviewer.md && grep -c "CONTEXT.md" .claude/agents/harness-qa-reviewer.md && grep -c "Spec Gaps" .claude/agents/harness-qa-reviewer.md && grep -c "Regression Test" .claude/agents/harness-qa-reviewer.md && echo "--- Negative checks ---" && grep -ci "gstack\|10-star\|supabase\|\.gstack\|/learn\|browser automation" .claude/agents/harness-qa-reviewer.md || echo "NO_GSTACK_REFS"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at .claude/agents/harness-qa-reviewer.md
    - File has YAML frontmatter with `name: harness-qa-reviewer` and tools: Read, Glob, Grep, Bash
    - File contains `## Role` section with advisory-only language
    - File contains `## Protocol` section with explicit Phase 1 and Phase 2 headings
    - Phase 1 section contains "Do NOT" instruction about not reading source files
    - Phase 1 section contains "STOP" instruction before proceeding to Phase 2
    - Phase 2 section reads source files and verifies test cases
    - File contains `## Inputs` section listing CONTEXT.md (Phase 1) and source files (Phase 2 only)
    - File contains `## Output Format` with test case table, verification results, spec gaps, regression suggestions
    - `grep -ci "gstack\|10-star\|supabase\|\.gstack\|/learn" .claude/agents/harness-qa-reviewer.md` returns 0
    - File has more than 60 lines (fully populated)
  </acceptance_criteria>
  <done>harness-qa-reviewer.md exists as a complete self-contained agent file with two-phase spec-then-verify protocol, explicit Phase 1/Phase 2 boundary enforcement, and advisory output format</done>
</task>

<task type="auto">
  <name>Task 2: Create harness-security-reviewer.md with OWASP/STRIDE self-scoping protocol (ROLE-04)</name>
  <files>.claude/agents/harness-security-reviewer.md</files>
  <read_first>
    - .claude/agents/harness-code-reviewer.md (structural template to follow)
    - .planning/research/FEATURES.md (gstack /cso pattern)
    - .planning/phases/03-role-based-gates/03-CONTEXT.md (D-15, D-16, D-17, D-18 locked decisions)
    - .planning/phases/03-role-based-gates/03-RESEARCH.md (Security self-scoping pattern specification)
  </read_first>
  <action>
Create a new file .claude/agents/harness-security-reviewer.md with this complete content:

**YAML frontmatter:**
```yaml
---
name: harness-security-reviewer
description: "Security audit gate -- OWASP Top 10 + STRIDE threat modeling with self-scoping"
tools:
  - Read
  - Glob
  - Grep
  - Bash
---
```

**# Harness: Security Reviewer**

One-line: "Security audit agent spawned before /gsd-ship to run OWASP Top 10 and STRIDE threat analysis on security-sensitive changes."

**## Role**

State what this agent does: audits code changes for security vulnerabilities using OWASP Top 10 categories and STRIDE threat modeling. Self-scopes by reading the plan/summary first -- if no security-sensitive changes are detected, outputs a skip declaration instead of a full audit. State what it does NOT do: does not block the workflow, does not modify files, does not perform penetration testing or dynamic analysis. All output is advisory (per D-01).

**## Protocol**

Three-step protocol with self-scoping:

**Step 1: Scope Assessment**

Read PLAN.md or phase SUMMARY.md files. Scan for security-sensitive keywords:
- Authentication: `auth`, `login`, `password`, `credential`, `token`, `JWT`, `OAuth`, `session`, `cookie`
- Data protection: `encrypt`, `hash`, `salt`, `secret`, `API key`, `private key`
- Input handling: `SQL`, `query`, `injection`, `XSS`, `sanitize`, `validate`, `escape`
- Access control: `permission`, `role`, `privilege`, `RBAC`, `authorization`, `admin`
- Network: `HTTP`, `HTTPS`, `CORS`, `CSRF`, `header`, `redirect`, `webhook`
- Data storage: `database`, `PII`, `personal data`, `GDPR`

If NO security-sensitive keywords found: output scope declaration and skip. This is a valid, first-class outcome.

If security-sensitive keywords found: proceed to Step 2.

**Step 2: OWASP Top 10 Analysis**

Read relevant source files via Glob/Grep. For each applicable OWASP category, check:

- **A01 Broken Access Control** -- Missing auth checks, privilege escalation, IDOR
- **A02 Cryptographic Failures** -- Weak algorithms, hardcoded secrets, missing encryption
- **A03 Injection** -- SQL injection, XSS, command injection, template injection
- **A04 Insecure Design** -- Missing threat modeling, business logic flaws
- **A05 Security Misconfiguration** -- Default credentials, unnecessary features, missing headers
- **A06 Vulnerable Components** -- Known CVEs in dependencies (check package.json/lock files)
- **A07 Authentication Failures** -- Weak passwords, missing MFA, session fixation
- **A08 Data Integrity Failures** -- Unsigned updates, deserialization, CI/CD tampering
- **A09 Logging Failures** -- Missing audit logs, sensitive data in logs
- **A10 SSRF** -- Unvalidated URLs, internal service access

**Step 3: STRIDE Threat Modeling**

For each identified trust boundary:
- **S**poofing -- Can an attacker impersonate a legitimate user/component?
- **T**ampering -- Can data be modified in transit or at rest without detection?
- **R**epudiation -- Can actions be performed without audit trail?
- **I**nformation Disclosure -- Can sensitive data leak to unauthorized parties?
- **D**enial of Service -- Can the service be disrupted?
- **E**levation of Privilege -- Can an attacker gain higher access than intended?

**## Inputs**

When spawned, you receive:
1. Phase PLAN.md or SUMMARY.md files -- for scope assessment (read first)
2. `.planning/harness.json` -- gate configuration
3. Access to codebase via Glob/Grep -- source files for security analysis (read only if Step 1 finds keywords)

**## Output Format**

When audit is skipped:
```markdown
# Security Audit

## Scope Declaration
- **Status:** Not in scope
- **Reason:** No security-sensitive changes detected in phase scope.
- **Keywords scanned:** [list of files scanned]

No further analysis required.
```

When audit runs:
```markdown
# Security Audit

## Scope Declaration
- **Status:** In scope
- **Security-sensitive areas:** [list of detected keywords/patterns and their locations]

## OWASP Top 10 Findings

| Category | Severity | Finding | File | Recommendation |
|----------|----------|---------|------|----------------|
| A01 | Critical/High/Medium/Low | [specific finding] | [file:line] | [specific fix] |

## STRIDE Threat Model

| Trust Boundary | Threat | Category | Severity | Mitigation |
|----------------|--------|----------|----------|------------|
| [boundary] | [threat description] | S/T/R/I/D/E | [severity] | [specific mitigation] |

## Summary
- **Critical:** X findings
- **High:** X findings
- **Medium:** X findings
- **Low:** X findings

## Advisory Verdict
- **Ship / Fix Required / Review Needed**
- [1-2 sentence recommendation]
```

Ensure NO gstack-specific references appear anywhere in the file.
  </action>
  <verify>
    <automated>test -f .claude/agents/harness-security-reviewer.md && grep -c "## Role" .claude/agents/harness-security-reviewer.md && grep -c "## Protocol" .claude/agents/harness-security-reviewer.md && grep -c "OWASP" .claude/agents/harness-security-reviewer.md && grep -c "STRIDE" .claude/agents/harness-security-reviewer.md && grep -c "Scope Assessment" .claude/agents/harness-security-reviewer.md && grep -c "Not in scope" .claude/agents/harness-security-reviewer.md && grep -c "A01\|A02\|A03" .claude/agents/harness-security-reviewer.md && echo "--- Negative checks ---" && grep -ci "gstack\|10-star\|supabase\|\.gstack\|/learn" .claude/agents/harness-security-reviewer.md || echo "NO_GSTACK_REFS"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at .claude/agents/harness-security-reviewer.md
    - File has YAML frontmatter with `name: harness-security-reviewer` and tools: Read, Glob, Grep, Bash
    - File contains `## Role` section with advisory-only language and self-scoping description
    - File contains `## Protocol` with Step 1 (Scope Assessment), Step 2 (OWASP), Step 3 (STRIDE)
    - Step 1 contains keyword list: auth, session, token, password, encrypt, SQL, injection, XSS, permission, role
    - Step 1 contains explicit skip path ("output scope declaration and skip")
    - Step 2 contains all 10 OWASP categories (A01 through A10) with brief descriptions
    - Step 3 contains all 6 STRIDE categories (Spoofing, Tampering, Repudiation, Information Disclosure, Denial of Service, Elevation of Privilege)
    - File contains two output format variants: "Not in scope" (skipped) and full audit report
    - `grep -ci "gstack\|10-star\|supabase\|\.gstack\|/learn" .claude/agents/harness-security-reviewer.md` returns 0
    - File has more than 80 lines (fully populated with all OWASP/STRIDE content)
  </acceptance_criteria>
  <done>harness-security-reviewer.md exists as a complete self-contained agent file with self-scoping, OWASP Top 10 analysis (all 10 categories), STRIDE threat modeling (all 6 categories), and dual output format (skip and full audit), producing advisory output only</done>
</task>

<task type="auto">
  <name>Task 3: Add CEO and QA/Security trigger instructions to CLAUDE.md harness section (D-19)</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (full file -- find the GSD:harness-start/end markers at lines ~201-210)
    - .planning/phases/03-role-based-gates/03-CONTEXT.md (D-19 locked decision -- exact trigger wording)
    - .planning/phases/03-role-based-gates/03-RESEARCH.md (token budget analysis -- section on CLAUDE.md)
  </read_first>
  <action>
Edit CLAUDE.md to add two new instruction lines within the harness section, BEFORE the `<!-- GSD:harness-end -->` marker. Do NOT modify any existing lines.

The current harness section (lines ~201-210) is:
```markdown
<!-- GSD:harness-start -->
## Harness

Unified workflow harness active. Skills: `.claude/skills/harness/`
Config: `.planning/harness.json`

When dispatching subagents, include `.planning/harness.json` in the `<files_to_read>` block.
Before /gsd-plan-phase: verify CONTEXT.md has approaches-with-tradeoffs and user approval.
After /gsd-execute-phase on implementation plans: spawn harness-code-reviewer before /gsd-ship.
<!-- GSD:harness-end -->
```

Add these exact two lines AFTER "After /gsd-execute-phase..." and BEFORE `<!-- GSD:harness-end -->`:

```
At new-project init or scope-change: spawn harness-ceo-reviewer.
Before /gsd-ship: spawn harness-qa-reviewer and harness-security-reviewer.
```

The resulting harness section will be:
```markdown
<!-- GSD:harness-start -->
## Harness

Unified workflow harness active. Skills: `.claude/skills/harness/`
Config: `.planning/harness.json`

When dispatching subagents, include `.planning/harness.json` in the `<files_to_read>` block.
Before /gsd-plan-phase: verify CONTEXT.md has approaches-with-tradeoffs and user approval.
After /gsd-execute-phase on implementation plans: spawn harness-code-reviewer before /gsd-ship.
At new-project init or scope-change: spawn harness-ceo-reviewer.
Before /gsd-ship: spawn harness-qa-reviewer and harness-security-reviewer.
<!-- GSD:harness-end -->
```

CRITICAL CONSTRAINTS:
- Do NOT modify any existing lines in the harness section
- Do NOT modify the "Before /gsd-plan-phase" line (this is the Phase 2 spec gate, NOT an agent spawn -- per Pitfall 5 in RESEARCH.md)
- Keep wording maximally terse -- token budget is ~50 tokens for non-comment content
- The two new lines add approximately 15-18 tokens -- total will be borderline but acceptable
  </action>
  <verify>
    <automated>grep "spawn harness-ceo-reviewer" CLAUDE.md && grep "spawn harness-qa-reviewer and harness-security-reviewer" CLAUDE.md && grep "approaches-with-tradeoffs" CLAUDE.md && grep "spawn harness-code-reviewer before /gsd-ship" CLAUDE.md && echo "--- All 4 reviewer names present ---" && grep -c "harness-ceo-reviewer\|harness-code-reviewer\|harness-qa-reviewer\|harness-security-reviewer" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md contains "At new-project init or scope-change: spawn harness-ceo-reviewer."
    - CLAUDE.md contains "Before /gsd-ship: spawn harness-qa-reviewer and harness-security-reviewer."
    - Existing line "Before /gsd-plan-phase: verify CONTEXT.md has approaches-with-tradeoffs and user approval." is UNCHANGED
    - Existing line "After /gsd-execute-phase on implementation plans: spawn harness-code-reviewer before /gsd-ship." is UNCHANGED
    - Both new lines appear between the last existing instruction and `<!-- GSD:harness-end -->`
    - `grep -c "harness-ceo-reviewer\|harness-code-reviewer\|harness-qa-reviewer\|harness-security-reviewer" CLAUDE.md` returns at least 4
    - The `<!-- GSD:harness-start -->` and `<!-- GSD:harness-end -->` markers are still present and correctly positioned
  </acceptance_criteria>
  <done>CLAUDE.md harness section contains 5 instruction lines: subagent dispatch, spec gate, code review trigger, CEO trigger, and QA/Security trigger -- all within GSD:harness markers and within token budget</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLAUDE.md instructions -> Claude behavior | CLAUDE.md trigger instructions control when agents are spawned; incorrect triggers could cause unexpected agent invocations |
| Agent prompt -> Claude behavior | QA and Security agent prompts define how Claude behaves when spawned |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering | harness-qa-reviewer.md | accept | Static markdown under git version control; tampering requires repo write access |
| T-03-05 | Tampering | harness-security-reviewer.md | accept | Same as T-03-04 |
| T-03-06 | Elevation of Privilege | CLAUDE.md trigger instructions | accept | Trigger instructions spawn read-only advisory agents; agents cannot modify files or escalate privileges beyond their tools list |
| T-03-07 | Information Disclosure | Security reviewer Grep access | accept | Agent has read-only codebase access by design (required to find security patterns); no external network; output stays in terminal |
| T-03-08 | Spoofing | QA Phase 1/2 boundary | mitigate | Agent prompt contains explicit imperative instruction "Do NOT read source files" in Phase 1 and "STOP" before Phase 2 to prevent spec contamination |
</threat_model>

<verification>
After all tasks complete:
1. harness-qa-reviewer.md exists with two-phase protocol and >60 lines
2. harness-security-reviewer.md exists with self-scoping and OWASP/STRIDE and >80 lines
3. CLAUDE.md has all 4 reviewer agent names referenced
4. CLAUDE.md harness section has 5 instruction lines (3 existing + 2 new)
5. Zero gstack-specific references in any created/modified file
6. All existing CLAUDE.md content is unchanged except the two added lines
</verification>

<success_criteria>
- harness-qa-reviewer.md is a fully populated self-contained agent with spec-then-verify two-phase protocol
- harness-security-reviewer.md is a fully populated self-contained agent with OWASP/STRIDE and self-scoping skip path
- CLAUDE.md harness section has CEO and QA/Security trigger instructions added (D-19)
- All agents produce advisory reports (not hard blocks)
- All agents reference GSD artifacts exclusively
- All existing CLAUDE.md instructions are preserved unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/03-role-based-gates/03-02-SUMMARY.md`
</output>
