---
phase: 01-router-context-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/harness/SKILL.md
  - .claude/skills/harness/rules/SKILL.md
  - .claude/skills/harness/personas/SKILL.md
  - .claude/skills/harness/tdd/SKILL.md
  - .claude/skills/harness/rules/tdd-enforcement.md
  - .claude/skills/harness/rules/verification-rules.md
  - .claude/skills/harness/rules/code-review.md
  - .claude/skills/harness/rules/systematic-debugging.md
  - .claude/skills/harness/personas/ceo-review.md
  - .claude/skills/harness/personas/eng-review.md
  - .claude/skills/harness/personas/qa-gate.md
  - .claude/skills/harness/personas/cso-audit.md
  - .planning/harness.json
  - .planning/config.json
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03
  - INFRA-04
  - INFRA-05

must_haves:
  truths:
    - "Harness skill directory tree exists with nested subdirectories per D-01"
    - "agent_skills config in config.json maps skill paths to GSD agent types per D-02"
    - "harness.json sidecar config exists with gate toggles, role triggers, TDD exemptions per D-07/D-09"
    - "All harness files are self-contained copies with no upstream references per INFRA-05"
    - "Root SKILL.md contains passive discovery guard preventing subagents from loading all rule files per D-02/D-03"
  artifacts:
    - path: ".claude/skills/harness/SKILL.md"
      provides: "Routing index and passive discovery guard"
      contains: "Do NOT read"
    - path: ".planning/harness.json"
      provides: "Harness config sidecar"
      contains: "tdd_enforcement"
    - path: ".planning/config.json"
      provides: "GSD config with agent_skills entries"
      contains: "agent_skills"
    - path: ".claude/skills/harness/rules/SKILL.md"
      provides: "Rules subdirectory index"
      contains: "Phase 2"
    - path: ".claude/skills/harness/tdd/SKILL.md"
      provides: "TDD subdirectory index"
      contains: "Phase 2"
    - path: ".claude/skills/harness/personas/SKILL.md"
      provides: "Personas subdirectory index"
      contains: "Phase 3"
  key_links:
    - from: ".planning/harness.json"
      to: ".planning/config.json"
      via: "agent_skills_reference mirrors config.json agent_skills"
      pattern: "agent_skills"
    - from: ".claude/skills/harness/SKILL.md"
      to: ".planning/harness.json"
      via: "Config reference in routing SKILL.md"
      pattern: "harness\\.json"
---

<objective>
Create the harness directory tree, config files, and routing SKILL.md that form the infrastructure skeleton for the entire harness.

Purpose: This is the foundation everything else plugs into. Phase 2-4 populate stubs with real content. Without this structure, there is nothing to route to, no config to toggle, and no skill files to inject.

Output: `.claude/skills/harness/` directory tree with SKILL.md files, `.planning/harness.json` sidecar config, updated `.planning/config.json` with agent_skills entries.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-router-context-infrastructure/01-CONTEXT.md
@.planning/phases/01-router-context-infrastructure/01-RESEARCH.md
@.planning/config.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create harness directory tree with SKILL.md routing index and stub files</name>
  <files>
    .claude/skills/harness/SKILL.md
    .claude/skills/harness/rules/SKILL.md
    .claude/skills/harness/rules/tdd-enforcement.md
    .claude/skills/harness/rules/verification-rules.md
    .claude/skills/harness/rules/code-review.md
    .claude/skills/harness/rules/systematic-debugging.md
    .claude/skills/harness/personas/SKILL.md
    .claude/skills/harness/personas/ceo-review.md
    .claude/skills/harness/personas/eng-review.md
    .claude/skills/harness/personas/qa-gate.md
    .claude/skills/harness/personas/cso-audit.md
    .claude/skills/harness/tdd/SKILL.md
  </files>
  <read_first>
    .planning/phases/01-router-context-infrastructure/01-RESEARCH.md
    .planning/phases/01-router-context-infrastructure/01-CONTEXT.md
  </read_first>
  <action>
Create the `.claude/skills/harness/` directory tree per D-01 with nested subdirectories: `rules/`, `personas/`, `tdd/`.

**Root SKILL.md** (`.claude/skills/harness/SKILL.md`):
Per D-02/D-03, this is the passive discovery guard AND routing index. Content must include:

```markdown
# Harness: Unified Workflow Router

Route-not-stack architecture. Each lifecycle phase has one framework authority.

## Selective Loading

Do NOT read subdirectory rule files (rules/, personas/, tdd/) directly. They are injected into specific agent types via `agent_skills` in `.planning/config.json`.

Only load a rule file if it appears in your `<agent_skills>` block.

## Config

Gate toggles and role triggers: `.planning/harness.json`
Skill injection paths: `.planning/config.json` `agent_skills` field

## Lifecycle Routing

| Phase | Owner | Injected Skills |
|-------|-------|-----------------|
| Project init | GSD | CEO gate at boundary (agent) |
| Requirements -> Roadmap | GSD | None |
| Phase Discussion | GSD | Eng gate at boundary (agent) |
| Phase Planning | GSD | None |
| Implementation execution | GSD | tdd-enforcement via agent_skills |
| Non-implementation execution | GSD | None |
| Phase Verification | GSD | verification-rules via agent_skills |
| Code Review | Harness | code-review via agent_skills |
| Bug Investigation | Harness | systematic-debugging via agent_skills |
| Pre-ship QA | Harness | QA gate (agent) |
| Pre-ship Security | Harness | Security audit (agent) |

## Subagent Dispatch Note

When dispatching subagents, include `.planning/harness.json` in the `<files_to_read>` block so they can read gate configuration.
```

**Subdirectory SKILL.md files** — each follows this pattern (per Pitfall 5 from research):

`rules/SKILL.md`:
```markdown
# Harness: Engineering Rules

Skill content delivered in Phase 2 (Engineering Discipline Rules). Do not act on this file.

Only follow rules from this directory if they appear in your `<agent_skills>` block.

## Status

Stub -- populated during Phase 2.

## Files

- tdd-enforcement.md (stub)
- verification-rules.md (stub)
- code-review.md (stub)
- systematic-debugging.md (stub)
```

`personas/SKILL.md`:
```markdown
# Harness: Role-Based Personas

Skill content delivered in Phase 3 (Role-Based Gates). Do not act on this file.

Only follow rules from this directory if they appear in your `<agent_skills>` block.

## Status

Stub -- populated during Phase 3.

## Files

- ceo-review.md (stub)
- eng-review.md (stub)
- qa-gate.md (stub)
- cso-audit.md (stub)
```

`tdd/SKILL.md`:
```markdown
# Harness: TDD Enforcement

Skill content delivered in Phase 2 (Engineering Discipline Rules). Do not act on this file.

Only follow rules from this directory if they appear in your `<agent_skills>` block.

## Status

Stub -- populated during Phase 2.
```

**Rule and persona stub files** — each follows this minimal pattern:
```markdown
# Harness: {Rule/Persona Name}

Content delivered in Phase {N}. Do not act on this file.
```

Specific stubs to create:
- `rules/tdd-enforcement.md` — "Content delivered in Phase 2."
- `rules/verification-rules.md` — "Content delivered in Phase 2."
- `rules/code-review.md` — "Content delivered in Phase 2."
- `rules/systematic-debugging.md` — "Content delivered in Phase 2."
- `personas/ceo-review.md` — "Content delivered in Phase 3."
- `personas/eng-review.md` — "Content delivered in Phase 3."
- `personas/qa-gate.md` — "Content delivered in Phase 3."
- `personas/cso-audit.md` — "Content delivered in Phase 3."

IMPORTANT per INFRA-05: No file should contain any reference to gstack or superpowers repositories, URLs, or import paths. All files must be self-contained.

IMPORTANT (GSD v1.32.0 worktree awareness): With `workflow.use_worktrees: true` (new default), executor subagents run in isolated git worktrees that only see committed files. All harness skill files created by this plan MUST be committed before Plan 01-03 runs, otherwise `buildAgentSkillsBlock()` path validation will fail in worktree context. The wave structure (01-01 in wave 1, 01-03 in wave 2) ensures this ordering, but the commit must happen as part of this plan's completion.
  </action>
  <verify>
    <automated>ls -R .claude/skills/harness/ && grep -c "Do NOT read" .claude/skills/harness/SKILL.md && grep -c "agent_skills" .claude/skills/harness/SKILL.md && grep -c "Do not act" .claude/skills/harness/rules/SKILL.md && grep -c "Do not act" .claude/skills/harness/personas/SKILL.md && grep -c "Do not act" .claude/skills/harness/tdd/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - .claude/skills/harness/SKILL.md exists and contains "Do NOT read subdirectory rule files"
    - .claude/skills/harness/SKILL.md contains "Lifecycle Routing" table with 11 rows
    - .claude/skills/harness/SKILL.md contains "agent_skills" at least 3 times
    - .claude/skills/harness/SKILL.md contains "harness.json" reference
    - .claude/skills/harness/rules/SKILL.md exists and contains "Phase 2"
    - .claude/skills/harness/personas/SKILL.md exists and contains "Phase 3"
    - .claude/skills/harness/tdd/SKILL.md exists and contains "Phase 2"
    - .claude/skills/harness/rules/tdd-enforcement.md contains "Content delivered in Phase 2"
    - .claude/skills/harness/rules/verification-rules.md contains "Content delivered in Phase 2"
    - .claude/skills/harness/rules/code-review.md contains "Content delivered in Phase 2"
    - .claude/skills/harness/rules/systematic-debugging.md contains "Content delivered in Phase 2"
    - .claude/skills/harness/personas/ceo-review.md contains "Content delivered in Phase 3"
    - .claude/skills/harness/personas/eng-review.md contains "Content delivered in Phase 3"
    - .claude/skills/harness/personas/qa-gate.md contains "Content delivered in Phase 3"
    - .claude/skills/harness/personas/cso-audit.md contains "Content delivered in Phase 3"
    - No file in .claude/skills/harness/ contains "gstack" or "superpowers" or "github.com/garrytan" or "github.com/obra"
  </acceptance_criteria>
  <done>Directory tree `.claude/skills/harness/` exists with 3 subdirectories (rules/, personas/, tdd/), each containing a SKILL.md index and stub rule files. Root SKILL.md contains routing table and passive discovery guard. All files are self-contained with no upstream references.</done>
</task>

<task type="auto">
  <name>Task 2: Create harness.json sidecar config and register agent_skills in config.json</name>
  <files>
    .planning/harness.json
    .planning/config.json
  </files>
  <read_first>
    .planning/config.json
    .planning/phases/01-router-context-infrastructure/01-RESEARCH.md
  </read_first>
  <action>
**Create `.planning/harness.json`** per D-07/D-09 with this exact content:

```json
{
  "version": "1.0",
  "gates": {
    "tdd_enforcement": true,
    "role_reviews": true,
    "bypass_protection": true
  },
  "role_triggers": {
    "ceo_review": ["new-project", "scope-change"],
    "eng_review": ["discuss-phase"],
    "qa_gate": ["pre-ship"],
    "security_audit": ["pre-ship"]
  },
  "tdd_exempt_plan_types": ["config", "docs", "scaffolding"],
  "agent_skills_reference": {
    "_comment": "Canonical skill mappings. Actual injection uses config.json agent_skills field.",
    "gsd-executor": [".claude/skills/harness/tdd"],
    "gsd-verifier": [".claude/skills/harness/rules"]
  }
}
```

**Update `.planning/config.json`** to add agent_skills entries. Use GSD's own `config-set` CLI per the "Don't Hand-Roll" guidance:

```bash
node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" config-set 'agent_skills.gsd-executor' '[".claude/skills/harness/tdd"]'
node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" config-set 'agent_skills.gsd-verifier' '[".claude/skills/harness/rules"]'
```

After running config-set, read back config.json and verify the `agent_skills` field contains:
```json
{
  "agent_skills": {
    "gsd-executor": [".claude/skills/harness/tdd"],
    "gsd-verifier": [".claude/skills/harness/rules"]
  }
}
```

Per Pitfall 1: paths MUST be project-relative (`.claude/skills/...`), NOT absolute paths. `buildAgentSkillsBlock()` calls `validatePath()` which rejects absolute paths.

Per Pitfall 2: harness.json `agent_skills_reference` and config.json `agent_skills` must contain the same mappings. Verify they match after creation.

**GSD v1.32.0 agent-skills lookup keys:** The execute-phase workflow looks up `gsd-executor` and `gsd-verifier` — these match the config keys used here. Note: the plan-phase workflow uses different lookup keys (`gsd-researcher`, `gsd-planner`, `gsd-checker`) which differ from agent definition filenames (`gsd-phase-researcher`, `gsd-planner`, `gsd-plan-checker`). If future phases need to inject harness skills into those agents, use the workflow lookup keys, not agent filenames.

IMPORTANT per D-11: Do NOT modify any files in `~/.claude/get-shit-done/`. Only modify project-local files.
  </action>
  <verify>
    <automated>cat .planning/harness.json | python3 -c "import sys,json; d=json.load(sys.stdin); assert d['version']=='1.0'; assert d['gates']['tdd_enforcement']==True; assert d['gates']['bypass_protection']==True; assert 'gsd-executor' in d['agent_skills_reference']; print('harness.json OK')" && cat .planning/config.json | python3 -c "import sys,json; d=json.load(sys.stdin); assert d['agent_skills']['gsd-executor']==['.claude/skills/harness/tdd']; assert d['agent_skills']['gsd-verifier']==['.claude/skills/harness/rules']; print('config.json OK')"</automated>
  </verify>
  <acceptance_criteria>
    - .planning/harness.json exists and is valid JSON
    - harness.json contains "version": "1.0"
    - harness.json contains gates.tdd_enforcement = true
    - harness.json contains gates.role_reviews = true
    - harness.json contains gates.bypass_protection = true
    - harness.json contains role_triggers with ceo_review, eng_review, qa_gate, security_audit keys
    - harness.json contains tdd_exempt_plan_types array with "config", "docs", "scaffolding"
    - harness.json contains agent_skills_reference.gsd-executor = [".claude/skills/harness/tdd"]
    - harness.json contains agent_skills_reference.gsd-verifier = [".claude/skills/harness/rules"]
    - .planning/config.json agent_skills.gsd-executor = [".claude/skills/harness/tdd"]
    - .planning/config.json agent_skills.gsd-verifier = [".claude/skills/harness/rules"]
    - All skill paths in config.json are project-relative (start with ".claude/", no absolute paths)
    - No files in ~/.claude/get-shit-done/ were modified
  </acceptance_criteria>
  <done>harness.json sidecar config exists with gate toggles, role triggers, and TDD exemptions. config.json agent_skills entries registered for gsd-executor and gsd-verifier with project-relative paths. Both files contain matching skill path mappings.</done>
</task>

</tasks>

<verification>
1. Directory structure: `ls -R .claude/skills/harness/` shows 3 subdirectories with SKILL.md in each
2. Root SKILL.md: Contains "Do NOT read" guard and lifecycle routing table
3. Stub files: All 8 rule/persona stubs exist with "Do not act on this file" text
4. harness.json: Valid JSON with version, gates, role_triggers, tdd_exempt_plan_types, agent_skills_reference
5. config.json: agent_skills field has gsd-executor and gsd-verifier entries with project-relative paths
6. No upstream references: `grep -r "gstack\|superpowers\|github.com/garrytan\|github.com/obra" .claude/skills/harness/` returns nothing
7. No GSD modifications: No files in ~/.claude/get-shit-done/ were changed
</verification>

<success_criteria>
- Complete `.claude/skills/harness/` directory tree with root SKILL.md + 3 subdirectories + stub files
- `.planning/harness.json` with full schema (gates, triggers, exemptions, skill references)
- `.planning/config.json` with agent_skills entries for gsd-executor and gsd-verifier
- All files self-contained (INFRA-05), no upstream repo references
- Root SKILL.md contains passive discovery guard (INFRA-03) and routing table (INFRA-02)
</success_criteria>

<output>
After completion, create `.planning/phases/01-router-context-infrastructure/01-01-SUMMARY.md`
</output>
