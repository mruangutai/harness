---
phase: 01-router-context-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - .claude/agents/harness-ceo-reviewer.md
  - .claude/agents/harness-eng-reviewer.md
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-05
must_haves:
  truths:
    - "CLAUDE.md contains a harness marker block that survives GSD generate-claude-md regeneration"
    - "CLAUDE.md harness section stays under 50 tokens of non-comment text per D-05"
    - "Gate trigger instructions in CLAUDE.md tell the orchestrator when to spawn harness agents per D-06"
    - "Harness agent definition stubs exist for Phase 3 role gates per D-04"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Harness entry point in project CLAUDE.md"
      contains: "GSD:harness-start"
    - path: ".claude/agents/harness-ceo-reviewer.md"
      provides: "CEO reviewer agent stub for Phase 3"
      contains: "Phase 3"
    - path: ".claude/agents/harness-eng-reviewer.md"
      provides: "Eng reviewer agent stub for Phase 3"
      contains: "Phase 3"
  key_links:
    - from: "CLAUDE.md"
      to: ".claude/skills/harness/"
      via: "Harness section points to skill directory"
      pattern: "\\.claude/skills/harness/"
    - from: "CLAUDE.md"
      to: ".planning/harness.json"
      via: "Harness section references config sidecar"
      pattern: "harness\\.json"
---

<objective>
Add the harness marker section to CLAUDE.md and create stub agent definitions for gate roles.

Purpose: CLAUDE.md is always loaded by Claude. The harness section is the entry point that tells the orchestrator the harness exists, where to find skills, and when to trigger gate reviews. Agent stubs ensure the spawn targets exist (populated in Phase 3).

Output: Updated CLAUDE.md with harness marker block, stub agent definitions in `.claude/agents/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-router-context-infrastructure/01-CONTEXT.md
@.planning/phases/01-router-context-infrastructure/01-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add harness marker section to CLAUDE.md with gate trigger instructions</name>
  <files>CLAUDE.md</files>
  <read_first>
    CLAUDE.md
    .planning/phases/01-router-context-infrastructure/01-RESEARCH.md
  </read_first>
  <action>
Append the following marker-bounded section to the END of CLAUDE.md (after all existing content). Per D-05, the harness section uses `<!-- GSD:harness-start -->` / `<!-- GSD:harness-end -->` markers. GSD's `generate-claude-md` manages only 5 sections (project, stack, conventions, architecture, workflow) and will not touch a `harness` marker.

Append this exact block (6 lines total):

Line 1: `<!-- GSD:harness-start -->`
Line 2: `## Harness`
Line 3: (blank)
Line 4: `Unified workflow harness active. Skills: `.claude/skills/harness/``
Line 5: `Config: `.planning/harness.json``
Line 6: (blank)
Line 7: `When dispatching subagents, include `.planning/harness.json` in the `<files_to_read>` block.`
Line 8: `<!-- GSD:harness-end -->`

Per D-05: The section MUST stay under 50 tokens of non-comment text. The above is approximately 30 tokens. Do NOT add routing tables, lifecycle maps, or conditional logic here -- that belongs in `.claude/skills/harness/SKILL.md`.

Per D-06: Gate triggers are expressed as CLAUDE.md instructions. The instruction about dispatching subagents with harness.json serves as the mechanism for D-08 (subagents receiving harness config). No wrapper workflows, no new slash commands.

IMPORTANT: Do NOT modify any of the existing CLAUDE.md content above the harness section. Only append.
  </action>
  <verify>
    <automated>grep "GSD:harness-start" CLAUDE.md && grep "GSD:harness-end" CLAUDE.md && grep "\.claude/skills/harness/" CLAUDE.md && grep "harness.json" CLAUDE.md && python3 -c "
content = open('CLAUDE.md').read()
start = content.index('GSD:harness-start')
end = content.index('GSD:harness-end')
section = content[start:end]
lines = [l for l in section.split('\n') if not l.strip().startswith('<!--') and l.strip()]
words = ' '.join(lines).split()
print(f'Approximate tokens: {len(words)}')
assert len(words) < 50, f'Too many tokens: {len(words)}'
print('Token budget OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - CLAUDE.md contains `<!-- GSD:harness-start -->` marker
    - CLAUDE.md contains `<!-- GSD:harness-end -->` marker
    - CLAUDE.md harness section contains `.claude/skills/harness/`
    - CLAUDE.md harness section contains `.planning/harness.json`
    - CLAUDE.md harness section contains "When dispatching subagents"
    - CLAUDE.md harness section is under 50 tokens of non-comment text
    - All existing CLAUDE.md content above the harness section is unchanged
    - The harness section does NOT contain routing tables or lifecycle maps
    - The harness section does NOT contain "gstack" or "superpowers"
  </acceptance_criteria>
  <done>CLAUDE.md has a marker-bounded harness section under 50 tokens that declares harness existence, points to skill directory and config, and instructs orchestrator to pass harness.json to subagents.</done>
</task>

<task type="auto">
  <name>Task 2: Create stub agent definitions for harness gate roles</name>
  <files>
    .claude/agents/harness-ceo-reviewer.md
    .claude/agents/harness-eng-reviewer.md
  </files>
  <read_first>
    .planning/phases/01-router-context-infrastructure/01-CONTEXT.md
  </read_first>
  <action>
Per D-04, gate roles are implemented as custom agent definitions -- `.md` files spawned as Task() at phase transitions. Create stub agent definitions that Phase 3 will populate with actual role prompts.

Each file follows the GSD agent definition pattern: YAML frontmatter with name, description, and tools list, followed by markdown body.

**File: `.claude/agents/harness-ceo-reviewer.md`**

YAML frontmatter fields:
- name: harness-ceo-reviewer
- description: "CEO/Product perspective gate -- challenges scope, validates market fit, asks forcing questions"
- tools: [Read, Glob, Grep, Bash]

Markdown body:
- H1: "Harness: CEO Reviewer"
- Paragraph: "Agent content delivered in Phase 3 (Role-Based Gates). Do not use this agent until Phase 3 is complete."
- H2: "Status"
- Paragraph: "Stub -- populated during Phase 3."

**File: `.claude/agents/harness-eng-reviewer.md`**

YAML frontmatter fields:
- name: harness-eng-reviewer
- description: "Architect/Engineering perspective gate -- locks architecture, analyzes data flow, enumerates edge cases"
- tools: [Read, Glob, Grep, Bash]

Markdown body:
- H1: "Harness: Engineering Reviewer"
- Paragraph: "Agent content delivered in Phase 3 (Role-Based Gates). Do not use this agent until Phase 3 is complete."
- H2: "Status"
- Paragraph: "Stub -- populated during Phase 3."

Per INFRA-05: No references to gstack or superpowers repos in these files. They are harness-owned.

Per the anti-pattern note in RESEARCH.md: Do NOT add these agents to GSD's `<available_agent_types>` list. They are standalone agents spawned by CLAUDE.md gate instructions, not GSD agent types.
  </action>
  <verify>
    <automated>grep "name: harness-ceo-reviewer" .claude/agents/harness-ceo-reviewer.md && grep "name: harness-eng-reviewer" .claude/agents/harness-eng-reviewer.md && grep "Phase 3" .claude/agents/harness-ceo-reviewer.md && grep "Phase 3" .claude/agents/harness-eng-reviewer.md</automated>
  </verify>
  <acceptance_criteria>
    - .claude/agents/harness-ceo-reviewer.md exists with YAML frontmatter containing name: harness-ceo-reviewer
    - .claude/agents/harness-eng-reviewer.md exists with YAML frontmatter containing name: harness-eng-reviewer
    - Both files contain "Phase 3" reference
    - Both files contain "Do not use this agent until Phase 3"
    - Neither file contains "gstack" or "superpowers"
    - Both files have tools list in frontmatter (Read, Glob, Grep, Bash)
  </acceptance_criteria>
  <done>Stub agent definitions exist for harness-ceo-reviewer and harness-eng-reviewer. Both have valid YAML frontmatter and placeholder content directing to Phase 3 for actual role prompts.</done>
</task>

</tasks>

<verification>
1. CLAUDE.md harness section: `grep "GSD:harness-start" CLAUDE.md` finds the marker
2. Token budget: Harness section non-comment text is under 50 tokens
3. GSD regeneration safety: `grep -c "GSD:harness" CLAUDE.md` returns 2 (start + end markers)
4. Agent stubs: Both `.claude/agents/harness-*.md` files exist with valid YAML frontmatter
5. No upstream refs: `grep -r "gstack\|superpowers" .claude/agents/harness-*.md` returns nothing
</verification>

<success_criteria>
- CLAUDE.md has marker-bounded harness section under 50 tokens (INFRA-01)
- Harness section maps to skill directory (INFRA-02)
- Agent stubs exist for CEO and Eng reviewer roles (INFRA-05)
- No references to upstream gstack/superpowers repos in any created file
</success_criteria>

<output>
After completion, create `.planning/phases/01-router-context-infrastructure/01-02-SUMMARY.md`
</output>
