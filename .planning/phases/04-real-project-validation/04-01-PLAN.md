---
phase: 04-real-project-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/agents/harness-eng-reviewer.md
  - CLAUDE.md
autonomous: true
requirements:
  - VAL-01
  - VAL-02
  - VAL-03

must_haves:
  truths:
    - "harness-eng-reviewer can be spawned in pre-discuss mode and produces an architectural questions list"
    - "harness-eng-reviewer retains all existing post-discuss mode content unchanged"
    - "CLAUDE.md harness section contains a pre-discuss spawn trigger for architectural phases"
    - "CLAUDE.md harness section contains a post-discuss spawn trigger for harness-eng-reviewer"
    - "grep for 'harness-eng-reviewer' in CLAUDE.md returns exactly 2 matches after this plan"
  artifacts:
    - path: ".claude/agents/harness-eng-reviewer.md"
      provides: "Dual-mode eng reviewer (pre-discuss + post-discuss)"
      contains: "Pre-Discuss Mode Protocol"
    - path: "CLAUDE.md"
      provides: "Two eng reviewer trigger lines in harness section"
      contains: "harness-eng-reviewer"
  key_links:
    - from: "CLAUDE.md harness section"
      to: ".claude/agents/harness-eng-reviewer.md"
      via: "Two spawn trigger instructions (pre-discuss and post-discuss)"
      pattern: "harness-eng-reviewer"
    - from: "harness-eng-reviewer.md Pre-Discuss Mode Protocol"
      to: "ROADMAP.md phase goal"
      via: "Step 1 reads phase goal to generate questions"
      pattern: "ROADMAP.md"
---

<objective>
Add pre-discuss operational mode to harness-eng-reviewer and add both eng reviewer trigger lines to CLAUDE.md.

Purpose: The eng reviewer currently has no CLAUDE.md spawn trigger (a Phase 3 carry-forward gap). It also lacks a pre-discuss mode for architectural phases where discuss-phase would otherwise produce behavioral decisions with no architectural grounding. This plan closes both gaps per D-06.

Output: harness-eng-reviewer.md gains a Pre-Discuss Mode Protocol section. CLAUDE.md harness section gains two trigger lines — one for pre-discuss (architectural phases) and one for post-discuss (all phases).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-real-project-validation/04-CONTEXT.md
@.planning/phases/04-real-project-validation/04-RESEARCH.md

<interfaces>
<!-- Current CLAUDE.md harness section (lines 201-212): -->
```markdown
<!-- GSD:harness-start -->
## Harness

Unified workflow harness active. Skills: `.claude/skills/harness/`
Config: `.planning/harness.json`

When dispatching subagents, include `.planning/harness.json` in the `<files_to_read>` block.
Before /gsd-plan-phase: verify CONTEXT.md has approaches-with-tradeoffs and user approval.
After /gsd-execute-phase on implementation plans: spawn harness-code-reviewer before /gsd-ship.
At new-project init or scope-change: spawn harness-ceo-reviewer.
Before /gsd-ship: spawn harness-qa-reviewer and harness-security-reviewer.
<!-- GSD:harness-end -->
```

<!-- harness-eng-reviewer.md current structure (key sections): -->
- `## Role` — advisory role description
- `## Protocol` — 4-step post-discuss protocol (read inputs, inspect codebase, analyze architecture, render verdict)
- `## Inputs` — CONTEXT.md, ROADMAP.md, harness.json, codebase access
- `## Output Format` — Architecture Review report with Data Flow, Edge Cases, Test Matrix, Verdict sections

<!-- CRITICAL: harness-eng-reviewer has ZERO spawn triggers in CLAUDE.md currently. Both trigger lines are new additions. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Pre-Discuss Mode Protocol to harness-eng-reviewer.md</name>
  <files>.claude/agents/harness-eng-reviewer.md</files>
  <action>
Read `.claude/agents/harness-eng-reviewer.md` first to confirm current state.

Add two new sections to the file WITHOUT modifying any existing content:

**1. Add `## Operating Modes` section immediately after the `## Role` section and before `## Protocol`:**

```markdown
## Operating Modes

This agent has two modes. Read your invocation context to determine which applies.

- **Pre-discuss mode** — spawned BEFORE /gsd-discuss-phase on architectural phases. Produces architectural questions for the user. Does NOT review CONTEXT.md (it doesn't exist yet).
- **Post-discuss mode** — spawned AFTER /gsd-discuss-phase. Reviews CONTEXT.md decisions for edge cases, trust boundaries, and test matrix gaps. This is the default mode.

Determine your mode from the task description you were given when spawned. If it says "pre-discuss mode", follow `## Pre-Discuss Mode Protocol`. Otherwise, follow `## Protocol` (post-discuss mode).
```

**2. Add `## Pre-Discuss Mode Protocol` section immediately before `## Protocol` (the existing post-discuss section):**

```markdown
## Pre-Discuss Mode Protocol

When spawned in pre-discuss mode, you receive a phase goal (from ROADMAP.md) and produce architectural questions for the user to consider before their discuss-phase session.

**Step 1: Read the phase goal**

Read `.planning/ROADMAP.md` and locate the current phase's Goal, Success Criteria, and Requirements. Understand what is being built before inspecting anything else.

**Step 2: Inspect the codebase for architectural context**

Use Glob and Grep to find source files relevant to the phase scope. You are looking for:
- Existing APIs, schemas, or data models the new phase will connect to or modify
- Integration points that constrain the phase's architectural choices
- Existing patterns the phase must follow for consistency
- Any code that defines contracts the phase will depend on

Do NOT read every file. Focus on files directly relevant to the phase scope.

**Step 3: Generate architectural questions**

Produce 5–10 architectural questions the user should have clear answers to before their discuss-phase session. Each question must be:
- **Specific to this phase** (not generic architecture questions)
- **Answerable by the user** (not "what does the codebase do" — you've already explored the codebase in Step 2)
- **Load-bearing**: the answer meaningfully changes how the phase is designed or implemented

Focus your questions on:
- System interfaces: what APIs, contracts, or data schemas does this phase define or modify?
- Data models: what entities, fields, relationships, or constraints are introduced?
- Integration points: what existing systems does this phase connect to, and what are the constraints?
- Trust boundaries: what data crosses system boundaries, and what validation is needed?
- Behavioral contracts: what invariants must hold, and what failure modes need explicit handling?

**Output format for pre-discuss mode:**

```markdown
# Architecture Pre-Questions

**Phase:** [phase name and number]
**Mode:** Pre-discuss (questions for user before discuss session)
**Codebase inspected:** [list of files or directories examined, or "No relevant existing code found"]

## Architectural Questions

1. **[Question title]** — [1-sentence rationale: why this answer changes how the phase is built]
   Question: [specific, answerable question]

2. **[Question title]** — [rationale]
   Question: [question]

[5–10 questions total]

## Why These Questions Matter

[2–3 sentences on what decisions these questions unlock — what the executor and planner need that discuss-phase must produce to avoid implementation rework]
```
```

**Verification:** After editing, confirm the existing `## Protocol` section (post-discuss) is fully intact. Run:
`grep -c "Data Flow\|Edge Case\|Test Matrix\|Verdict\|Advisory Verdict" .claude/agents/harness-eng-reviewer.md`

This must return 5 or more — verifying the post-discuss output format sections are preserved.
  </action>
  <verify>
    <automated>grep -q "## Pre-Discuss Mode Protocol" .claude/agents/harness-eng-reviewer.md && grep -q "## Operating Modes" .claude/agents/harness-eng-reviewer.md && grep -c "Data Flow\|Edge Case\|Test Matrix\|Advisory Verdict" .claude/agents/harness-eng-reviewer.md | xargs -I{} test {} -ge 4 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>harness-eng-reviewer.md contains both Operating Modes and Pre-Discuss Mode Protocol sections. All existing post-discuss Protocol, Inputs, and Output Format content is unchanged. grep for "Data Flow", "Edge Case", "Test Matrix", "Advisory Verdict" returns 4+ matches.</done>
</task>

<task type="auto">
  <name>Task 2: Add eng reviewer trigger lines to CLAUDE.md harness section</name>
  <files>CLAUDE.md</files>
  <action>
Read `CLAUDE.md` first. Locate the `<!-- GSD:harness-start -->` / `<!-- GSD:harness-end -->` block.

Verify that NEITHER of these strings currently exists in CLAUDE.md before editing:
- "harness-eng-reviewer"

Run: `grep -c "harness-eng-reviewer" CLAUDE.md`
This must return 0. If it returns anything else, do not proceed — report the discrepancy.

Add two new trigger lines inside the harness block, appended after the existing 5 trigger lines (before `<!-- GSD:harness-end -->`):

```
Before /gsd-discuss-phase on architectural phases (agents, APIs, data models, schemas): spawn harness-eng-reviewer in pre-discuss mode.
After /gsd-discuss-phase: spawn harness-eng-reviewer.
```

The final harness block must read exactly:

```markdown
<!-- GSD:harness-start -->
## Harness

Unified workflow harness active. Skills: `.claude/skills/harness/`
Config: `.planning/harness.json`

When dispatching subagents, include `.planning/harness.json` in the `<files_to_read>` block.
Before /gsd-plan-phase: verify CONTEXT.md has approaches-with-tradeoffs and user approval.
After /gsd-execute-phase on implementation plans: spawn harness-code-reviewer before /gsd-ship.
At new-project init or scope-change: spawn harness-ceo-reviewer.
Before /gsd-ship: spawn harness-qa-reviewer and harness-security-reviewer.
Before /gsd-discuss-phase on architectural phases (agents, APIs, data models, schemas): spawn harness-eng-reviewer in pre-discuss mode.
After /gsd-discuss-phase: spawn harness-eng-reviewer.
<!-- GSD:harness-end -->
```

Do NOT modify any other part of CLAUDE.md. The harness section must remain within the `<!-- GSD:harness-start -->` / `<!-- GSD:harness-end -->` markers.
  </action>
  <verify>
    <automated>grep -c "harness-eng-reviewer" CLAUDE.md | xargs -I{} test {} -eq 2 && grep -q "pre-discuss mode" CLAUDE.md && grep -q "After /gsd-discuss-phase: spawn harness-eng-reviewer" CLAUDE.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>CLAUDE.md harness section contains exactly 2 references to harness-eng-reviewer — one pre-discuss trigger and one post-discuss trigger. All 5 existing trigger lines are unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spawner→agent | Mode instruction passed in Task() text — determines which protocol the agent runs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | harness-eng-reviewer.md | accept | Project-local file, no external attack surface. Git history tracks changes. |
| T-04-02 | Spoofing | Mode detection in agent | accept | Mode is plaintext in Task() prompt — low-value target, no authentication surface for a local dev tool. |
</threat_model>

<verification>
1. `grep -c "harness-eng-reviewer" CLAUDE.md` returns exactly 2
2. `grep -q "## Pre-Discuss Mode Protocol" .claude/agents/harness-eng-reviewer.md` exits 0
3. `grep -q "## Operating Modes" .claude/agents/harness-eng-reviewer.md` exits 0
4. `grep -q "After /gsd-discuss-phase: spawn harness-eng-reviewer" CLAUDE.md` exits 0
5. `grep -q "pre-discuss mode" CLAUDE.md` exits 0
6. `grep -c "Data Flow\|Edge Case\|Test Matrix\|Advisory Verdict" .claude/agents/harness-eng-reviewer.md` returns 4+
</verification>

<success_criteria>
- harness-eng-reviewer.md has `## Operating Modes` and `## Pre-Discuss Mode Protocol` sections
- Pre-Discuss Mode Protocol has 3-step process: read phase goal, inspect codebase, generate 5-10 questions
- Pre-discuss output format is numbered questions with rationale (distinct from post-discuss report format)
- Existing post-discuss `## Protocol`, `## Inputs`, `## Output Format` sections are fully preserved
- CLAUDE.md harness section has exactly 7 trigger lines (5 existing + 2 new)
- Both new trigger lines use imperative single-sentence format matching existing trigger style
- `grep -c "harness-eng-reviewer" CLAUDE.md` returns 2
</success_criteria>

<output>
After completion, create `.planning/phases/04-real-project-validation/04-01-SUMMARY.md`
</output>
