<!-- index-contract v1 -->
<!-- GENERATED except the text after ` :: ` on each row.
     Regenerate: .claude/skills/harness/bin/gen-decisions-index.py -->

# DECISIONS — index

**A row is an open-or-skip filter, never the rule itself.** Its only job is to answer "do I open this
entry?" Never act on a ruling here: open `docs/harness/DECISIONS.md` at the `@line` anchor and read
the entry. Rows written during the one-time backfill are second-hand paraphrase.

**Never read the authority whole (DEC-150).** Grep this index, then open the two or three entries that
bear on your task. Decisions cited in a dispatch are a floor, not a ceiling.

**Adding a decision:** its author writes its ruling here, in the same commit that appends the entry.

Row: `- DEC-NN @<line> [am-span] [tags] refs: <graph> :: <ruling>`.
The `am-span` token appears only on a decision carrying amendments — `am.1`, a contiguous
`am.1-am.N`, or an enumerated `am.1,am.3` that never hides a gap.
A row ending `— SUPERSEDED BY DEC-NN` is one you must not act on.

- DEC-01 @20 [state,plan,skills] refs:  :: GSD is removed entirely and the harness ships native replacements for orchestration, persistent state, execution and rule delivery.
- DEC-02 @34 [plan,state] refs:  :: The harness hosts its own development on `.harness/` and crews, making it the first project built with the harness.
- DEC-03 @46 [state,orchestrator,plan] refs:  :: The backbone is a small set of harness-owned state files plus `harness.json` and one directory, not a second execution engine.
- DEC-04 @57 [plan,state] refs:  :: Project state lives under a fresh `.harness/` root that is deliberately not backward-compatible with `.planning/`.
- DEC-05 @65 [orchestrator] refs:  :: Composition is declarative crew YAML read by one generic runner: the orchestrator routes, and workers never pick who runs next.
- DEC-06 @77 [skills,deploy] refs:  :: The crew runner is a skill under `.claude/skills/harness/` with crew configs beside it, because deploy distributes skills and agents but not commands.
- DEC-07 @88 [domain,brief,orchestrator,plan] refs:  :: Three squads — product, engineering, validation — each have a domain lead coordinated by the orchestrator, and the human is the CEO, so there is no CEO agent.
- DEC-08 @100 [plan,qa,domain,tests] refs: DEC-34 :: `pm` covers research plus planning in one context, `qa` covers test-writing plus coverage assessment, and `eng-lead` owns architecture review.
- DEC-09 @114 [domain,gates,plan] refs:  :: Each member declares `consult-when` in the manifest and its lead matches the request semantically at delegation time; `change_type` stays purely a test-obligation axis.
- DEC-10 @128 [domain,org,orchestrator] refs: DEC-13 :: The org is data: `team-config.yaml` holds teams, leads, members, `consult-when` and `domain` per project.
- DEC-11 @138 [domain,hooks] refs:  :: The manifest carries policy (team, lead, `consult-when`, `domain`) and agent frontmatter carries capability (`name`, `description`, `tools`, `model`, `color`, `hooks`); nothing is declared twice.
- DEC-12 @149 [deploy,domain,org,state] refs:  :: `/harness-deploy` distributes the tool and never touches project state, `/harness-init` writes every project artifact, and enrolling a project is deploy plus init.
- DEC-13 @160 [deploy,domain,org,state] refs:  :: Templates carry `schema_version`, and adopting a newer org is a user-triggered `/harness-init --upgrade` rather than a deploy side effect.
- DEC-14 @169 [approval,brief] refs:  :: `/harness-init` is one onboarding interview covering technical detection, product requirements, BRIEF approval and an optional design pass.
- DEC-15 @181 [orchestrator] refs:  :: Ideation is the orchestrator working directly with the CEO, backed by `pm` research.
- DEC-16 @189 [state] refs:  :: Every project carries the whole roster, and an irrelevant agent self-scopes to "not in scope" rather than being pruned.
- DEC-17 @198 [] refs:  :: `shared_context` holds exactly one entry, `handoff.md`, because every entry loads into every agent at every spawn.
- DEC-18 @206 [digest,orchestrator,state,cost] refs: DEC-19 :: Leads hold neither `Edit` nor `Bash`, and their `Write` is scoped to their own run dir, so capability rather than instruction keeps a lead delegating.
- DEC-19 @221 [domain,worktree] refs:  :: One shipped shell script, `check-domain.sh`, enforces per-agent `domain` path globs through a `PreToolUse` hook — the single deliberate exception to files-only delivery. — SUPERSEDED BY DEC-84 — SUPERSEDED BY DEC-85
- DEC-20 @238 [skills,map,tdd] refs:  :: Each rule-carrying persona reads its own rule file as step 0 of its protocol, keeping rules single-sourced and needing no spawn-time config. — SUPERSEDED BY DEC-63
- DEC-21 @252 [deploy,domain] refs:  :: Rules are the constitution: human-authored in the harness repo, overwritten on every deploy, with no per-project overlay and no agent-authored edits.
- DEC-22 @264 [expertise,qa,state,tests] refs:  :: Each agent keeps a bounded, structured Expertise file at `.harness/expertise/<agent>.md` so durable knowledge lands in the agent that will act on it.
- DEC-23 @276 [approval,plan] refs:  :: A choice belongs in the plan's `## Decisions` section and is approval-gated; an observation about how the codebase behaves belongs in Expertise.
- DEC-24 @286 [expertise,digest] refs:  :: Expertise write quality is advisory in three layers — one shared rule, DIGEST visibility with a one-line why, and lead curation — because a hook cannot judge an insight.
- DEC-25 @298 [expertise] refs:  :: On overflow the member flags `expertise_full`, its lead emits a `KEEP`/`DROP`/`MERGE` note, and the member applies that note itself.
- DEC-26 @309 [digest,orchestrator] refs:  :: A lead monitors its squad through members' DIGESTs and artifacts, and squad-level patterns accumulate in the lead's own Expertise.
- DEC-27 @320 [expertise] refs:  :: Expertise has two tiers — committed project-scoped and local global — with project winning on conflict and promotion requiring evidence from more than one project.
- DEC-28 @332 [orchestrator,approval,brief,gates] refs:  :: CEO feedback is classified by kind: requirement changes go to BRIEF and PLAN via pm, craft feedback to the addressed lead's Expertise via a feedback queue, and course-corrections into the next instruction only.
- DEC-29 @344 [digest,orchestrator] refs:  :: Every agent returns `VERDICT:` for control, `DIGEST:` for routing and `artifact:` as a path, and the orchestrator routes on the DIGEST without opening the artifact.
- DEC-30 @356 [] refs:  :: A malformed return is re-prompted once, then recorded as `VERDICT: BLOCKED (contract violation)` and escalated, never inferred from the text.
- DEC-31 @365 [plan,state] refs:  :: Reviewers are advisory: a non-empty `must_fix` or `severity_max` at high or above fails, while concerns at med or below pass with notes.
- DEC-32 @375 [orchestrator,brief,state] refs:  :: Autonomy is scoped by reversibility: cheap and reversible is decided and recorded, expensive or hard to reverse is asked via `open_questions`, and scope, goal or decision changes are always asked.
- DEC-33 @389 [approval,brief,gates,orchestrator] refs:  :: Merge is user-gated by default, so the orchestrator surfaces passing gates for approval; a project may opt into autonomous merge in `harness.json`.
- DEC-34 @398 [approval,gates,plan,brief] refs:  :: Two self-review softnesses are accepted — pm authors the plan and checks the goal, eng-lead routes the build and reviews architecture — with the two CEO approvals plus the merge gate as the compensating control.
- DEC-35 @410 [qa] refs:  :: The change-type-to-required-test-kinds table in `harness.json` is a floor qa may add to but never drop below, with conditionals as named predicates and `test_kinds` supplying detection and command.
- DEC-36 @423 [qa,state] refs:  :: A missing or unresolvable test `cmd` yields `VERDICT: BLOCKED — test command unresolved`, a loud third state distinct from the not-applicable soft skip.
- DEC-37 @433 [gates,plan,state] refs:  :: AI and LLM behavior coverage is a declared v1 gap: prompt, model and agent changes are not meaningfully covered by the matrix, so `ai-dev` work passes on human judgment. — SUPERSEDED BY DEC-70
- DEC-38 @445 [orchestrator,state] refs:  :: The orchestrator and the crew runner are one actor at two delegation granularities: delegating to a persona is a direct spawn, delegating to a team runs that crew's DAG.
- DEC-39 @456 [orchestrator,digest,domain,org] refs:  :: Hosting is hierarchical — orchestrator to domain lead to workers, with the crew's `lead:` field naming the host — and all three leads exist as spawnable personas in either hosting mode.
- DEC-40 @474 [orchestrator,org] refs:  :: Whether a lead fans out its review panel in parallel is a performance question left to measurement, and both acceptable answers preserve lead authority over the assessment.
- DEC-41 @484 [] refs:  :: `validator-lead` consolidating and assessing its panel's findings is its defining job, so synthesis has a named owner in both hosting modes.
- DEC-42 @492 [approval,orchestrator] refs:  :: Approval steps sit at crew boundaries and never mid-DAG, because a subagent cannot call `AskUserQuestion`; questions ride up via `open_questions` and the orchestrator asks.
- DEC-43 @501 [orchestrator,approval,cost,plan] refs:  :: One mechanism serves every human-in-the-loop case: `open_questions` upward, the orchestrator asks, and re-delegation carries `resume_from` plus a path to the answers.
- DEC-44 @511 [] refs:  :: User answers are written durably to a named answers file under `notes/`, never only into a run dir, so lateral routing and later artifacts can read them.
- DEC-45 @518 [state] refs:  :: `STATE.md` holds only `## Current` and `## Open Questions`, both self-clearing; the activity stream lives in dated files under `logs/` and is never loaded at spawn.
- DEC-46 @531 [state] refs:  :: One branch and one PR per feature, and each run belongs to one squad so exactly one lead owns that run's `state.yaml`.
- DEC-47 @540 [plan,approval,orchestrator,state] refs:  :: The plan's `## Features` section is the pm-owned approval-gated declaration, and `feature.yaml` is orchestrator-owned execution fact carrying only a `feature_id` join key.
- DEC-48 @551 [brief,plan] refs:  :: REQ, FEAT, D and T are four distinct levels, the test for a REQ being that it survives changing your mind about implementation; REQ coverage is computed, never tracked.
- DEC-49 @565 [brief,budget,state] refs:  :: The retry budget is feature-level — `cycles_used` against `max_total_cycles` in `feature.yaml` — because a fix cycle spans a new eng run plus a new validator run.
- DEC-50 @573 [dispatch,state] refs:  :: Reviewers diff `base` to a `review_sha` pinned at review dispatch, with local `git diff` as ground truth and no network or auth.
- DEC-51 @581 [dispatch,state] refs:  :: A dead host is resumed with `resume_from` at the in-flight step, reading `state.yaml` for what was dispatched and `git log` for what actually landed, aided by a `[harness:<step-id>]` commit prefix.
- DEC-52 @593 [dispatch,plan] refs:  :: A persona whose deliverable is a canonical file writes it in place, run dirs hold reports and intermediates, and any staged canonical file is promoted before a consumer step dispatches.
- DEC-53 @603 [] refs:  :: Reviewer reports are namespaced per persona and run so a parallel panel cannot collide on one report path.
- DEC-54 @609 [] refs:  :: Crews are flat and standalone in v1 with no sub-crew composition, so the review panel is listed in both `ship-feature` and `review-team`.
- DEC-55 @620 [gates,orchestrator] refs:  :: `review-team` is advisory: it returns `must_fix` and the calling context owns remediation and merging.
- DEC-56 @627 [security] refs:  :: pm's goal-check is its own step after the panel and its assessment, so "did we deliver?" is never averaged with code nits.
- DEC-57 @634 [qa,security] refs: DEC-08 :: The review panel has no CEO seat and no separate eng-reviewer, and its membership is crew config rather than auto-selected per diff.
- DEC-58 @646 [domain,deploy,tdd] refs: DEC-19 :: Engineering has five peer domains with no catch-all, `dev-ops` owning infra, CI, config, tooling and deploy, and eng-lead routes each task to exactly one.
- DEC-59 @658 [domain,skills,expertise,state] refs:  :: The declarative `## Skills`, `## Expertise` and `## Domain` body sections buy a uniform shape and greppable auditing, not enforcement — Claude Code does not parse them.
- DEC-60 @670 [plan,approval,brief,org] refs:  :: `.harness/README.md` is owned by `documentor` and rewritten as a defect fix, because the on-disk version describes a pre-restructure org and omits the hardest-gated schema fields.
- DEC-61 @681 [skills,deploy] refs: DEC-06 DEC-07 DEC-20 :: The Phase-1 exploration findings live on inside the `Because` clauses of the decisions they support, with no standalone findings section.
- DEC-62 @691 [map,qa] refs: DEC-34 DEC-35 :: `visual-designer` authors the design contract and `ui-reviewer` audits it in two modes — pre-build soundness and a post-build scored audit of the built UI — so authorship stays separate from audit.
- DEC-63 @710 [skills,cost] refs: DEC-20 :: Each rule is a skill declared in an agent's `skills:` frontmatter field, and Claude Code injects the full rule content before the agent's first action.
- DEC-64 @728 [expertise,hooks] refs: DEC-65 DEC-66 :: A `SubagentStart` hook receives `agent_type` and returns that agent's Expertise file as `hookSpecificOutput.additionalContext`, so delivery needs neither obedience nor a tool grant.
- DEC-65 @740 [expertise] refs: DEC-18 DEC-66 DEC-67 :: Expertise is hand-rolled at `.harness/expertise/<agent>.md`, because native `memory:` auto-enables Read, Write and Edit and would dissolve the lead and reviewer capability limits.
- DEC-66 @755 [expertise] refs: DEC-22 DEC-24 DEC-30 :: Expertise entries carry stable per-section IDs and updates are ops — `add`, `replace`, `merge`, `drop` — each naming its target with a `why`, reconciled at propose time.
- DEC-67 @769 [orchestrator,digest,domain,approval] refs:  :: Doers reconcile and apply their own Expertise file in place, while the write-less leads and reviewers propose ops that the orchestrator validates and applies. — SUPERSEDED BY DEC-86
- DEC-68 @786 [digest,orchestrator] refs: DEC-25 :: A curation note is applied immediately, and the recommending tier spawns the target agent solely to apply it when that agent has already closed.
- DEC-69 @799 [brief,orchestrator,approval,cost] refs:  :: The orchestrator recommends curation of a lead's Expertise from the cross-lead view it holds at the CEO briefing, and a plain-English summary in the briefing applies unless the CEO objects.
- DEC-70 @815 [qa,gates,state] refs: DEC-37 :: `change_type: ai_behavior` requires an `eval` test kind: `ai-dev` authors the eval, `qa` runs it and owns the gate, and `validator-lead` assesses the eval's adequacy.
- DEC-71 @832 [orchestrator,state,digest,expertise] refs:  :: The orchestrator delegates only to leads, so even a single-task request enters through the lead that owns the relevant persona.
- DEC-72 @843 [budget] refs: DEC-73 :: A crew's `goal` resolves to a FEAT plus the `SC-NN` entries its REQs trace to, and the crew is done when those criteria are met rather than when its steps complete.
- DEC-73 @854 [qa,plan,brief] refs: DEC-34 :: Every `SC-NN` declares `verify: automated | inspection | uat` when authored, and pm's goal-check assembles evidence from the validators with a pointer per criterion instead of re-testing.
- DEC-74 @868 [brief,tests] refs:  :: Any criterion marked `verify: uat` produces a step in a per-feature UAT file, and the UAT is a blocking section of the ship-review briefing that pm marks `ready` only once automated and inspection criteria have passed.
- DEC-75 @882 [plan,approval,digest] refs:  :: `visual-designer` decides during the design pass whether a feature needs an interactive high-fidelity prototype, and that prototype is approved bundled with plan approval as one signature.
- DEC-76 @898 [approval,skills,state] refs:  :: Teams carry binding technology conventions in a `conventions:` block in the manifest, and deviating from one requires a plan `## Decisions` entry and therefore CEO approval.
- DEC-77 @917 [state,brief,budget,orchestrator] refs:  :: Cycle-budget exhaustion stops only that branch, preserves run history, the branch and every commit, leaves the feature `in_progress`, and rolls up `BLOCKED` with what each cycle tried.
- DEC-78 @931 [digest,plan,approval] refs:  :: The consolidated DIGEST carries an `escalations:` list naming the question, who raised it, where it routed and how it resolved, and a resolution that is a real architectural or scope choice is promoted to a numbered plan decision.
- DEC-79 @944 [state] refs:  :: Feature-scoped durable artifacts carry the FEAT id in the filename and repeat feature and run in the header, so `ls` answers which feature an artifact belongs to.
- DEC-80 @954 [expertise,skills] refs: DEC-63 :: The artifact has one name, Expertise: the file is `.harness/expertise/<agent>.md` and the rule is the `harness-expertise` skill.
- DEC-81 @968 [domain,plan] refs: DEC-19 :: Agent frontmatter takes named colors only, a blocking hook must `exit 2`, tool input reaches a hook as JSON on stdin, and concurrency is 20 subagents at once with 200 per session.
- DEC-82 @984 [dispatch,orchestrator,state] refs: DEC-39 DEC-40 :: Subagents can spawn subagents, gated by the subagent-spawn-depth setting, and the `tools:` token that grants spawning is `Agent`, so omitting it keeps an agent a leaf. — SUPERSEDED BY DEC-83
- DEC-83 @1001 [org,plan] refs: DEC-39 DEC-82 :: Spawn depth is set explicitly to 2 in every project, the CLI floor is pinned, omitting `Agent` from a worker's `tools:` is the primary control with the depth cap as defence in depth, and a platform claim counts only with a URL, a quote and a min-version marker.
- DEC-84 @1036 [domain] refs: DEC-19 DEC-85 :: Destructive-operation restraint is a `Bash` matcher in `check-domain.sh` that exits 2, or it does not exist.
- DEC-85 @1047 [domain,state,worktree] refs: DEC-19 :: Serialized repo-mutators on one branch, plus `isolation: worktree` for genuinely parallel mutation, is what makes fan-out safe; the domain hook is a guardrail over the common write case, and manifest `shared:` paths are always serialized and owned by nobody.
- DEC-86 @1070 [qa,security,tests] refs: DEC-67 DEC-85 :: The roster is 3 leads plus 9 doers plus 3 reviewers, `qa` counting as a doer because it writes tests, so six agents are write-less.
- DEC-87 @1081 [domain,expertise] refs:  :: A doer's own Expertise path appears in its manifest `domain`, so the hook permits the self-apply mechanism it depends on.
- DEC-88 @1089 [state,expertise,plan] refs:  :: One feature is in flight at a time as a stated operating constraint, so a blocked feature is a stop-and-decide rather than a switch-tasks. — SUPERSEDED BY DEC-95
- DEC-89 @1100 [qa,gates,state] refs:  :: Hand edits are a legal path: they commit with a `[harness:human]` prefix, the state check reports them since the pinned `review_sha` and re-pins it, so the reviewer and qa gates re-open for the affected paths.
- DEC-90 @1116 [state,expertise,orchestrator] refs:  :: The harness is single-operator by design and says so as a stated scope boundary — every single-writer guarantee means one agent, one session, one machine, with no lock anywhere.
- DEC-91 @1123 [state] refs:  :: The value claim is that Claude executes reliably at each stage without mid-stage supervision, since supervision is batched at decision boundaries.
- DEC-92 @1132 [org,cost,budget,skills] refs:  :: The org is priced before it is built: the four surviving artifacts run as plain skills over real features, with spawns, tokens, dollars, wall-clock, touchpoints and defects escaped to merge deciding whether the agent org exists. — SUPERSEDED BY DEC-93 — SUPERSEDED BY DEC-99
- DEC-93 @1151 [cost,org,worktree] refs: DEC-92 :: The pilot carries two instruments — org-arm runs settle cost, touchpoints and whether the artifacts fire, while `kaya-ai`'s git history supplies the base defect rate — and SC-5 is retained but labelled underpowered and inadmissible as evidence.
- DEC-94 @1177 [qa,state,tests] refs: DEC-36 :: `kaya-ai` is the pilot host, with `uv run pytest` plus two pnpm suites and no Playwright, so the `ui` kind soft-skips and the UAT is the only user-facing verification the pilot can exercise.
- DEC-95 @1192 [worktree,expertise,state] refs: DEC-85 DEC-88 DEC-89 :: A git worktree is the unit of concurrency — one feature per worktree, as many worktrees as you like, with `.harness/` as per-worktree state — leaving divergent committed Expertise and the shared global tier as honest residue.
- DEC-96 @1222 [org,cost,state,brief] refs:  :: The measured base defect rate in `kaya-ai` history is 0.44 escaped defects per feature (19 of 43 units, zero reverts, ~111 lines each), and gate attribution puts about 79% of them within reach of the four artifacts.
- DEC-97 @1266 [tests,cost,org,qa] refs: DEC-96 :: A throwaway dry run has all four artifacts behaving as specified, and code review catches a fail-open defect that a green happy-path suite misses — which also exposes a fourth, unhandled state in `harness-qa-gate`.
- DEC-98 @1306 [tests,qa,state] refs: DEC-83 :: `harness-qa-gate` resolves a kind to satisfied / missing / not-applicable / `misconfigured`, discriminating on failure kind — load, import, collection or no-files-matched — rather than on test count, and `misconfigured` returns `BLOCKED`.
- DEC-99 @1335 [cost,org,state,digest] refs: DEC-92 DEC-96 DEC-97 DEC-98 :: The full agentic workflow gets built and its cost monitored in practice, which makes instrumentation a build requirement, brings the whole deferred fix list back into scope, and lets the org proceed.
- DEC-100 @1369 [skills,domain,hooks,deploy] refs: DEC-40 :: All four platform unknowns are settled empirically: `SubagentStart` fires for nested spawns, skill dirs must sit exactly one level under `.claude/skills/`, parallel fan-out from inside a subagent works, and a `PreToolUse` `exit 2` blocks a subagent's Write.
- DEC-101 @1451 [domain,digest,state,expertise] refs: DEC-83 DEC-87 DEC-98 :: Four zero-dependency `bin/` scripts ship verified against real inputs — `inject-expertise.sh`, `check-domain.sh`, `check-state.sh`, `validate-digest.py` — and a fix counts as done only once it has been run against an input that would expose it.
- DEC-102 @1494 [map,org,state] refs: DEC-100 :: `depth="2"` is exactly the harness shape, and the platform enforces workers-as-leaves by withholding the `Agent` tool at the limit, so a worker finds no tool and does the work itself.
- DEC-103 @1540 [state,digest,gates,plan] refs: DEC-91 DEC-100 DEC-101 DEC-102 :: Propagation needs mechanical enforcement rather than a habit of re-reading SPEC: ten stale SPEC/BUILD statements are swept, and the split's unmet re-readability premise stands recorded as a live defect.
- DEC-104 @1577 [state,digest,docs,map] refs: DEC-83 DEC-103 :: Propagation is enforced by `bin/check-docs.sh` as INV-10, whose registry is DECISIONS itself via inline `<!-- stale: -->` markers with an explicit `<!-- ok-stale -->` escape; it catches the literal-string class only.
- DEC-105 @1624 [brief,expertise,plan,state] refs: DEC-71 :: The per-spawn baseline is ~15.3k tokens and ~522k per feature, of which the CLAUDE.md hierarchy is 31% and the universal rules 11%, making CLAUDE.md the largest available trimming lever.
- DEC-106 @1668 [domain,digest,expertise] refs:  :: Reviewers hold `Write` scoped by the domain hook to exactly two paths — their namespaced report and their own Expertise file — with no `Edit` at all and no source path in the domain.
- DEC-107 @1682 [domain,qa,security,skills] refs: DEC-18 DEC-65 DEC-100 :: The roster is complete at 3 leads, 9 doers and 3 reviewers, validated by script on frontmatter, colour, tool grants, preloaded rules and manifest entry, with `documentor` the only role pinned to `sonnet`.
- DEC-108 @1734 [domain,expertise,hooks,docs] refs: DEC-64 DEC-85 DEC-100 :: Expertise injection works end to end through the `SubagentStart` hook and project agent definitions win over global ones, while the domain hook declared in agent frontmatter never executes — a delivery failure, not a logic one.
- DEC-109 @1809 [domain,docs,expertise,hooks] refs: DEC-67 DEC-103 DEC-108 :: A relative command path leaves the frontmatter domain hook still silent, which eliminates `${CLAUDE_PROJECT_DIR}` interpolation as the cause; and declaring the stale-wording markers is half of writing a superseding decision.
- DEC-110 @1845 [domain,hooks,orchestrator,state] refs: DEC-85 :: `PreToolUse` declared in agent frontmatter does not fire for spawned subagents, so the domain hook lives as one global `.claude/settings.json` registration that dispatches on the payload's `agent_type` and exits 0 for the unnamed orchestrator.
- DEC-111 @1900 [domain,dispatch,expertise,hooks] refs: DEC-103 DEC-104 DEC-109 DEC-110 :: `/harness-init` provisions the `PreToolUse` domain hook into `settings.json` alongside spawn depth and `SubagentStart`, and `check-state.sh` INV-9 verifies the set at every entry.
- DEC-112 @1933 [domain,state,brief,approval] refs: DEC-100 DEC-108 DEC-110 :: `/harness-init` ships as a flat skill with eight templates and deterministic merge scripts; it writes the user's explicit approval without ever self-approving, and a guardrail test must defeat the agent's good behaviour or it tests the prose.
- DEC-113 @2026 [deploy,skills,state,plan] refs: DEC-112 :: `/harness-deploy` reconciles the destination against the repo, dry-run by default, distributing agents globally only, keeping crew overrides in `.harness/crews/`, and reporting `agent_skills` cleanup for the user to perform.
- DEC-114 @2091 [cost,budget,orchestrator,state] refs: DEC-99 :: Cost is computed from transcript tokens keyed by `(model, speed, inference_geo)`, because no available source carries both native dollars and per-agent attribution; `max_cost_usd` bounds spend beside `max_cycles`, and INV-11 makes the meter non-optional.
- DEC-115 @2172 [hooks,state,domain,map] refs: DEC-02 :: GSD removal is scoped to this repo, with the global GSD surface recorded as its own task gated on task 17; a hook's `settings.json` entry comes out before the script is left inert and restorable.
- DEC-116 @2239 [domain,cost,skills,dispatch] refs: DEC-100 DEC-112 DEC-113 :: A team step's `outputs:` resolve to the producing agent's own domain and the run dir is lead-only bookkeeping, which keeps step paths disjoint under parallel fan-out; a lead sets `cost: pending_orchestrator` and writes monotonic ordering markers, because with no `Bash` it can neither meter the run nor read a clock, and the orchestrator reports cost after the lead returns.
- DEC-117 @2305 [dispatch,plan,cost,domain] refs: DEC-49 DEC-116 :: `{{cycle}}` resolves in the output path of anything that re-runs, so a loop-back writes a new report instead of overwriting the one that caused the cycle; parallel steps with no interdependency dispatch together, and the fan-in is the lead's own consolidated digest rather than an extra dispatched step.
- DEC-118 @2376 [orchestrator,dispatch,plan,state] refs: DEC-102 DEC-116 :: A team is single-squad by construction — the spawn-depth cap strips `Agent` below the lead, so no lead reaches outside its own squad or through a peer lead — and a multi-squad lifecycle such as `plan-feature` or `ship-feature` is an orchestrator playbook sequencing one lead-owned run per squad segment.
- DEC-119 @2421 [digest,cost,domain,orchestrator] refs: DEC-101 DEC-116 DEC-117 :: **Team** is the live noun on every surface (`harness-team/`, `teams/`, the `team:` digest field), there is exactly one artifact type per tier — members send a digest, the lead's `<run_dir>/digest.md` is a digest of digests, validated by `validate-digest.py` in the runner's collect step — and the two counters have owners: step `cycles` is the lead's in `state.yaml`, `cycles_used` the orchestrator's in `feature.yaml`.
- DEC-120 @2473 [orchestrator,state,brief,cost] refs: DEC-06 DEC-102 DEC-114 DEC-118 :: `harness-orchestrator` is a spawned agent, one per in-flight feature at layer 1, so several flows run concurrently with bounded contexts while the main session is a thin layer-0 user channel that asks and re-delegates; spawn depth is capped at 3 with members leaf at layer 3 (probed, not inferred), `STATE.md` lives at `.harness/features/<FEAT>/STATE.md` under that feature's orchestrator, and entry is the `/harness` door plus the verb doors, all skills.
- DEC-121 @2571 [digest,expertise,orchestrator,org] refs: DEC-101 DEC-119 :: Every digest field is required, and nothing is said with an explicit `[]` or with `none` for a scalar that is genuinely inapplicable, because absence is ambiguous where emptiness asserts the agent looked; `sc_status` is declared on pm at source and on the lead as a passthrough.
- DEC-122 @2611 [digest,domain,docs,hooks] refs: DEC-19 DEC-101 DEC-110 DEC-119 DEC-121 :: The digest contract is enforced by `validate-digest.py` running as a `SubagentStop` hook registered in `settings.json`, where exit 2 prevents the subagent from stopping so a malformed return is rejected at source — covering leads, which have no `Bash`; it declines to govern a non-`harness-*` `agent_type` and a `stop_hook_active` re-run, and fails open loudly on stderr on its own error.
- DEC-123 @2659 [digest,orchestrator,state] refs: DEC-19 DEC-110 DEC-112 DEC-119 DEC-122 :: `validate-digest.py` computes the team verdict roll-up `BLOCKED > ESCALATE > FAIL > PASS` and rejects a lead return that reports better than its worst member, so every member entry carries its own `verdict:`; reporting worse stays legal, and the judgement parts of collation — dedupe, contradictions, sending work back — stay prose.
- DEC-124 @2702 [state,dispatch,digest] refs: DEC-112 DEC-119 DEC-123 :: Single-message dispatch of parallel members is preferred because it does not depend on the platform backgrounding subagents, an agent's own account of what it did is never the evidence (the spawn records are), `<run_dir>/digest.md` is human-readable prose while the return carries the contract block the hook checks, and the digest parser stays hand-rolled because `import yaml` fails on this repo's own host.
- DEC-125 @2763 [expertise,digest,orchestrator,state] refs: DEC-19 DEC-124 :: An agent that sees no injected Expertise block is the first of its role and creates `.harness/expertise/<agent>.md` from the skeleton, updating it read-modify-write from the copy in context — writing it from new entries alone deletes the earlier ones — and the op in the DIGEST is a receipt rather than a delivery mechanism; leads and reviewers hold `Write` and self-apply, scoped by the domain hook to that one path.
- DEC-126 @2853 [digest,skills,approval,cost] refs:  :: A digest schema shared by two or more agents lives in one canonical template delivered by `skills:` preload — devs via `harness-digest-dev`, leads via `harness-team`'s "Reporting up" — while a schema with exactly one agent keeps its template inline, and the three reviewers stay inline because they share only a few lines.
- DEC-127 @2878 [digest,orchestrator,brief,cost] refs: DEC-100 DEC-101 DEC-110 DEC-119 DEC-121 DEC-122 DEC-123 DEC-124 :: Member verdicts are read by key from the parsed digest rather than matched as text anywhere in an entry, an inline value's brackets are followed across lines, `members: []` beside `steps_run > 0` is a violation, an unbalanced inline value is reported rather than coerced to empty, hook mode fails open loudly on the validator's own exception while a contract violation exits 2, and `--hook` mode carries test cases asserting the exact exit code and the stderr text.
- DEC-128 @2995 [orchestrator,brief,cost,plan] refs: DEC-105 DEC-116 DEC-126 :: `harness-orchestrator` is the sixteenth agent — layer 1, one per feature, holding `Bash` so it can meter the run the leads cannot — and it preloads the `harness` skill, whose `SKILL.md` is the orchestrator playbook; `/harness` carries the whole main-session relay protocol and `/harness-plan` and `/harness-ship` are thin missions over it, with debugging on demand rather than a door.
- DEC-129 @3027 [brief,plan,state,docs] refs: DEC-120 :: A feature's whole world is one directory — `.harness/features/<FEAT>/` holds generically named `BRIEF.md`, `PLAN.md`, `DESIGN.md` beside `STATE.md` and `feature.yaml`, decisions are PLAN's `## Decisions` `D-NN` section, onboarding keys off `harness.json` plus `team-config.yaml`, and INV-1..5 are per-feature — and every BRIEF states `## Problem` before `## Goal`.
- DEC-130 @3053 [qa,brief,docs,org] refs: DEC-129 :: Every feature-scoped note — research, reviews, qa assessments, answers, ship-review, uat, mockups, prototypes — lives under `features/<FEAT>/notes/`, so the directory carries the feature id and retiring a feature is one directory; `.harness/notes/` holds only genuinely project-scoped artifacts.
- DEC-131 @3073 [orchestrator,dispatch,state,brief] refs: DEC-129 :: A child spawn survives the interruption of its parent and runs on as an orphan, so INV-12 warns on any run dir absent from `feature.yaml` and a resume reconciles the surviving work rather than redoing it.
- DEC-132 @3094 [state,brief,orchestrator] refs:  :: pm authors the success criteria — wording, numbering and `verify:` method — and the spawn prompt carries the user's mandated outcomes as goal constraints only; adding criteria beyond the user's is expected rather than merely permitted, and the user's signature is where over-reach is pruned.
- DEC-133 @3114 [brief] refs:  :: A feature id is `FEAT-NN-<kebab-slug>`: the number stays the stable join key, the slug carries the meaning, pm coins it from the goal in two to four words at BRIEF time, and it is immutable once created because every recorded reference breaks on rename.
- DEC-134 @3125 [budget,cost,orchestrator,brief] refs:  :: The cost budget is informational — actual-versus-budget rides every orchestrator return and briefing, a crossing is flagged in the headline and a divergence of multiples raises a non-blocking question — while `max_total_cycles` stays hard; and every agent pins its model in frontmatter, `opus` for the judgment tier (three leads, pm, visual-designer, orchestrator) and `sonnet` for the other ten.
- DEC-135 @3144 [docs,brief,skills] refs: DEC-105 :: CLAUDE.md is about 0.8k tokens — the Project blurb, the Harness section, and two conventions — and the GSD-era stack analysis lives in git history alone, which keeps stale claims out of the propagation checker's scan surface; per-feature CLAUDE.md load is about 35k tokens.
- DEC-136 @3161 [plan,map,deploy,state] refs: DEC-63 DEC-106 DEC-112 DEC-128 DEC-135 :: The GSD dev state is triaged item by item into the harness's own ledger — the scoping gap resolved by the plan flow's eng-lead architecture segment, personas by the squad agents, kaya-ai as task 17 — with history in git and no `.planning/` directory; the surviving `gsd` mentions are legitimate, being deploy's registry migration and CLAUDE.md's historical prose.
- DEC-137 @3184 am.1-am.2 [map,domain,expertise,plan] refs: DEC-85 DEC-118 DEC-125 DEC-126 DEC-135 :: The codebase map is a third knowledge tier at `.harness/codebase/` — per-project structural knowledge, authored by the role that consumes it and carved to that role in `team-config.yaml`, with role lenses pointing into one shared skeleton, `INDEX.md` capped near 60 lines and preloaded at every spawn, every claim carrying a `file:line` anchor and every section its `author · date · anchors-verified` provenance; on ship-refresh documentor updates the skeleton and marks affected role sections stale, the owning specialist rewrites its own, and `bin/render-map.py` derives `codebase/map.html` from the markdown, which is never authored by hand.
- DEC-138 @3238 am.1-am.7 [plan,github,map,orchestrator] refs: DEC-19 DEC-126 DEC-133 DEC-135 DEC-137 :: The GitHub Issues mirror is write-only and orchestrator-executed at its existing checkpoints, gated by `github.sync: true`, and a missing or unauthenticated `gh` skips the sync while the flow succeeds: the milestone comes from BRIEF and one sub-issue per `T-NN` hangs off a single parent that is adopted or created but never discovered, recorded as `github.parent` plus `github.parent_origin` in `feature.yaml` and attached by internal `id`; `absorbs:` is a citation that closes nothing, because absorption is normally partial and only a human signature closes an absorbed issue; Issues are pm's plan-time input and never a write path into an approval-gated artifact; `ship` and `abandon` close the milestone always but a parent only when they created it (`abandon` with `state_reason=not_planned`, and it also closes the feature's own sub-issues); and anything posted is text the user signed, taken verbatim from a file path so the mirror has no words of its own.
- DEC-139 @3343 [gates,plan,qa,domain] refs: DEC-19 DEC-118 :: Debug is an investigation segment in front of the normal flow — an eng specialist reproduces, localizes and root-causes with evidence and no fix, and that report seeds pm's mini-plan before signature and ship run the standard gates; known-cause bugs skip the segment, three failed hypothesis cycles is `BLOCKED`, and a bug is a full gated flow named `BUG-NN-<kebab-slug>` on its own number sequence under the same `.harness/features/` root.
- DEC-140 @3363 [map,cost,github,orchestrator] refs: DEC-125 DEC-138 :: The map runs at init — the last act of `/harness-init` spawns the orchestrator on mission map in the background where dev-ops detection found existing source, greenfield skips with a note, and INV-14 warns at every gate whenever real source (>5 files by extension, dot-dirs and vendor trees pruned) exists with no `.harness/codebase/INDEX.md`, a warn only, so unmapped flows still run and pay the re-derivation cost.
- DEC-141 @3384 [map,brief] refs:  :: `render-map.py` folds each view's own title into its section heading with content nesting at h4+, strips HTML comments from view bodies, caps prose at 60rem while the page is uncapped, and gives every diagram a full-window scroll container with `useMaxWidth:false` and a 78vh ceiling; map authoring labels every edge with what flows in both directions, keeps directory layout from impersonating architecture, orients layered diagrams top-down, and opens every view with `## In brief` — three to six sentences of plain English before any anchored detail; documentor runs `model: opus`, and ui-reviewer audits source, declaring rendered size and layout as needs-eyes.
- DEC-142 @3424 [dispatch,orchestrator,org,state] refs:  :: Every dispatch title at every tier reads `<flow-id> · <step or task id> · <what, 3–6 words>` — stated at the door, in the playbook and in the team runner, with the flow id carried all the way down, so a spawn title that cannot be traced to its flow is a dispatch defect; nothing routes on titles, but the spawn tree is the user's only live view of a running org.
- DEC-143 @3439 [worktree,dispatch,plan,approval] refs: DEC-85 :: `check-domain.sh` matches the raw path first and then strips the worktree prefix and matches the in-worktree path against the same globs, so an agent standing in `.claude/worktrees/<id>/` may write exactly what its domain grants in the main checkout; worktrees branch from the LOCAL branch, and a task that cannot be split across commits needs a declared shared-workspace mode, since per-agent worktree isolation cannot give sequenced squad runs one workspace with no intermediate commit.
- DEC-144 @3468 [github,gates,orchestrator,state] refs: DEC-138 :: `branch-create-gate.sh` is a `PreToolUse:Bash` gate requiring every new git branch to name the work it serves, registered unconditionally as a mandatory `settings.json` hook because it self-gates on `github.sync` and exits instantly where the mirror is off: it accepts `<type>/FEAT-NN-slug` and `BUG-NN-slug` validated against the flow existing on disk, pins the repo from `github.repo`, runs on python3 stdlib alone, flips the project board to In-Progress only where the optional `harness.json` board keys are present, and DENIES with the reason on any unverifiable gh state.
- DEC-145 @3493 am.1-am.2 [expertise,digest,orchestrator,budget] refs: DEC-24 DEC-25 DEC-69 :: Expertise v2 splits recording from distillation: mid-run every agent appends granular observations to `.harness/features/<FEAT>/observations/<agent>.md`, which is never injected, and Expertise is written only under a distillation dispatch — the orchestrator playbook's feature-close step or the `/harness-curate` skill — where entries are WHEN/DO rules or durable repo facts, ≤50 words, no FEAT/T/issue tokens, four canonical sections and a 150-line file budget enforced by `bin/check-expertise.sh` with injection hard-truncating at 150 lines; a harness defect is a bug report routed to `open_questions`, `merge`'s result may be no longer than the longer input, and (as amended through am.2) the feature-close digest-skim gives the lead ≤3 sourced, observation-phrased candidates per member with rejection-with-reason a first-class outcome, displacement-never-merge at a full section, and per-source accept counts in the distillation digest.
- DEC-146 @3553 [gates] refs: DEC-144 :: The board In-Progress flip resolves one GraphQL query on the issue node's `projectItems`, filtered by the pinned `project_id` — O(boards-per-issue), so there is no pagination and no item cap — and the flip itself stays best-effort by design.
- DEC-147 @3569 [expertise,dispatch,orchestrator,map] refs:  :: The flat-roster platform invariant lives in the rule skills — the playbook's delegation rule and the team runner's dispatch step both state that a teammate omits `name:` and cannot spawn a teammate — and the general heuristic holds: a lesson three or more agents record independently is constitution or codebase-map content rather than Expertise, which is per-agent and probabilistic.
- DEC-148 @3583 [orchestrator,cost,map,budget] refs: DEC-145 :: The dominant cost term is context length × turn count, so `cost-report.py` counts turns per agent line and flags any agent whose average cache-read per turn exceeds `budgets.context_per_turn_tokens` (default 200k), and the playbook has the orchestrator end its run at a mission-phase boundary once the phase has cost ~10+ dispatches, reporting "phase complete, spawn a successor" — a successor loses nothing because state lives on disk, and payloads never ride forward: a member's output stays in its digest file and the successor's context takes the verdict and the path.
- DEC-149 @3604 [dispatch,domain,map,skills] refs:  :: Three MIT-licensed design imports re-home onto existing machinery: `harness-codebase-design` carries the deep-module vocabulary plus four tests (deletion, the-interface-is-the-test-surface, one-adapter-hypothetical-two-real, state-the-lifetime-with-the-seam), preloaded by eng-lead and code-reviewer with doers receiving it through dispatch prompts; `harness-spec-driven` carries the D-NN bar — a choice earns one only when hard-to-reverse ∧ surprising-without-context ∧ a real trade-off — and the ubiquitous language at `.harness/codebase/glossary.md` as a pm-owned map lens where code wins over stated meaning; and mission `deepen` runs between features only, scanning the last ship's hot spots in the design vocabulary, with a validator panel verifying each candidate adversarially and the user picking at a briefing.
- DEC-150 @3639 [state,domain,brief,digest] refs: DEC-85 DEC-148 :: State files have physics: `check-domain.sh`'s stage two DENIES a `Write` to `.harness/features/*/feature.yaml` over 200 lines or 20 comment lines, and to STATE.md over 120 lines or carrying any section besides `## Current` and `## Open Questions`, naming the routing table as the reason — current truth into `## Current`, per-run findings into that run's digest, rationale into notes/ — and because the governed writers hold Write and not Edit, every increment re-passes the whole file through the gate; the playbook scopes step-1 reads (grep BRIEF/PLAN by task id, the resume handoff prompt IS the working set, runs/ and notes/ read by pointer), REPLACES `## Current` at step 5 and keeps feature.yaml data rather than narrative, with the reading rule advisory, the write gate physics, and the context watchdog naming whoever ignores the advisory half.
- DEC-151 @3676 [domain,qa,orchestrator,security] refs: DEC-85 :: `bin/bash-write-guard.sh` is a `PreToolUse:Bash` hook registered as a mandatory `settings.json` entry alongside the branch gate: the three reviewers are READ-ONLY and any detected write pattern (redirects, sed/perl/awk in-place, tee, sponge, rm/mv/cp) is denied outright, every other harness agent except dev-ops has its extractable target paths checked worktree-normalized against its team-config domain including shared paths, in-domain and unparseable commands pass, and the deny message names the rule — a path the domain hook denied does not become writable by switching tools; the guard converts the casual bypass into deliberate obfuscation, which is caught post-hoc, so it narrows the Bash side door rather than closing it.
- DEC-152 @3704 [docs,orchestrator] refs: DEC-145 :: Frontmatter `effort:` is pinned per tier and overrides the spawning session's setting: the seven judging agents — orchestrator, three leads, three reviewers — run `effort: high`, the nine doers run `effort: medium`, and because the DEC-145..148 baseline was measured at inherited-low effort, effort is a known confound in the FEAT-02 comparison.
- DEC-153 @3719 [orchestrator,qa,worktree,domain] refs: DEC-151 :: Two user-decided sanctions close the bypass audit: perturbation proofs have a legal home — bash-write-guard permits governed-agent writes under `.claude/worktrees/**` while the main checkout stays hard-gated and reviewers stay read-only everywhere, with qa's rule skill stating the protocol (worktree, byte-identity restore verification, never "just briefly" in the main checkout) — and the commit pen belongs to the orchestrator alone: it stages by explicit pathspec and commits the feature branch it owns, merge/PR/deploy stay user-gated, and probe edits are byte-verified restored before any commit; python-heredoc writes evade the guard's shell parsing and sit in the deliberate-obfuscation class caught post-hoc.
- DEC-154 @3744 [state,digest,dispatch,budget] refs: DEC-156 :: A run `state.yaml` is a checkpoint a fresh context reads mechanically, so every value in it is an identifier, an enum, a counter, a path or a sequence marker with a one-line `note:` per step as the prose ceiling, while findings, citations and assessment reasoning live in `digest.md` and the step entry records only the verdicts they justify — the test is that a value which must be read rather than matched is in the wrong file, mechanized by INV-16's key whitelist.
- DEC-155 @3769 [dispatch,cost,org,state] refs: DEC-31 DEC-152 DEC-156 :: A member runs on the model pinned in its own agent definition: a dispatch never passes `model:`, because a per-invocation parameter outranks frontmatter and silently re-decides the org's model-per-tier design with nothing recording which model ran; believing a task needs a stronger model is an escalation raised in `open_questions` with evidence and decided above the lead, stated in `harness-team`'s dispatch step and the leads' zero-micro-management red flags and mechanized by `dispatch-guard.sh`.
- DEC-156 @3798 [digest,dispatch,state,cost] refs: DEC-19 DEC-122 DEC-154 DEC-155 :: Three prose-only rules get scripts: `validate-digest.py --hook` resolves a lead's returned `artifact:` path and validates that FILE against the lead schema at exit 2 while the lead is still alive to fix it, passing through loudly where the path cannot be resolved; `check-state.sh` INV-15 requires an existing, schema-passing `digest.md` for every `status: complete` run with a lead host; INV-16 requires run `state.yaml` top-level keys to come from the checkpoint whitelist with no key repeating, naming prose keys in the rejection with `digest.md` as their route; and `dispatch-guard.sh`, a `PreToolUse` hook on the spawn tool, exits 2 on a harness agent's dispatch carrying `model:` while the main session, having no `agent_type`, is never governed.
- DEC-157 @3841 [budget,orchestrator,plan,cost] refs: DEC-134 :: A cycle is a rework loop: `cycles_used` increments only when a FAIL is routed back, an unmet SC re-dispatches, or a lead reports send-backs inside a run, so a clean first-pass run contributes zero and a lead's digest `cycles_used` is its send-back count; the default `budgets.max_total_cycles: 10` lives in `harness.json` and the orchestrator seeds `feature.yaml` from it, with a per-feature raise a user decision recorded there, and first-pass work stays bounded by the PLAN's task list instead.
- DEC-158 @3876 [skills,dispatch,orchestrator,budget] refs: DEC-19 DEC-148 DEC-155 DEC-156 :: Every rule skill carries the rule, one clause of why, and a DEC pointer, with incident history and superseded rationale living in DECISIONS.md alone; conditionally-relevant skills load on demand — `harness-systematic-debugging` is off the dev specialists' preload and a debug-mode dispatch tells the specialist to Read the file; rare-mission playbook sections (map, deepen) live in `.claude/skills/harness/references/missions.md` and are read by path when dispatched, while ship-refresh and feature-close distillation stay inline; and a shared contract has one canonical copy with pointers elsewhere — red-flag tables, the whys, and everything load-bearing for compliance are kept deliberately.
- DEC-159 @3910 [orchestrator,plan,state,dispatch] refs: DEC-148 DEC-150 DEC-156 DEC-158 DEC-160 :: An orchestrator's mission IS one phase — plan → build → validate → ship — so ending at the boundary is normal termination and continuing is the exception: `feature.yaml` carries `phase:`, transitions are STATE.md log entries, exit predicates are disk-checkable (build exits when every planned `T-NN` has a PASS run), and the fix loop is the sanctioned exception worked inside the validate-phase orchestrator with a mid-phase relay as the bounded escape; the handoff `notes/handoff-<ending-phase>.md` from `templates/HANDOFF.md` holds exactly `## Next` (cited to PLAN), `## Trust` (`claim — evidence pointer — verified-at <sha> | UNVERIFIED`), `## Dead ends` and `## Working set` (3–5 paths), fits a ~60-line cap, is rewritten per crossing rather than appended to, is shape-gated at write time by check-domain.sh and swept by INV-17, and prices trust rather than granting it — the successor's step zero validates `## Next` against PLAN/STATE, and disk-only reconstruction stays fully supported so nothing may ever require the note.
- DEC-160 @3961 [state,deploy,domain,plan] refs: DEC-113 DEC-154 DEC-157 DEC-159 :: The handoff-note cap is 60 lines; `check-domain.sh` denies a run `state.yaml` Write carrying non-whitelisted or duplicate top-level keys, the whitelist living in both check-state.sh (INV-16) and check-domain.sh, deliberately duplicated and cross-referenced by comment because the harness is files-only with no shared import; INV-18 flags a run dir that exists with no `feature.yaml`, since a feature can otherwise complete a whole phase invisible to every feature-keyed invariant; and because deploy.sh never writes project state, a decision that adds a `harness.json` key must say so, with `/harness-init --upgrade` the path for config-schema additions.
- DEC-161 @3988 [deploy,map,plan] refs: DEC-149 :: `/harness-deepen` and `/harness-map` are thin doors over the general `/harness` router, routed in harness.md step 1, and `deploy.sh` treats `commands/harness*.md` as part of the distributable — copied global and per-project, with stale harness-named commands pruned and project-own commands untouched.
- DEC-162 @4002 [map,domain,approval,state] refs: DEC-140 DEC-149 :: Mission map assigns pm `.harness/codebase/glossary.md` alongside `product-surface.md` — the map is the checkable moment the domain language gets recorded — and check-state INV-19 warns at INV-14's level when a mapped codebase has no glossary, with existing mapped projects seeded from their shipped features' pinned vocabulary rather than waiting for a re-map; multi-context maps stay unadopted because no project has bounded contexts and speculative structure fails the deletion test.
- DEC-163 @4022 [map,brief,plan,approval] refs: DEC-100 DEC-134 :: `cmd: null` stays the honest record dev-ops writes when no runner exists for a test kind, and the codebase map is the discriminating check on whether that matters — so check-state INV-20 warns at every `/harness` entry when a null kind's mapped surface view exceeds a self-scoped stub, naming the kind, the view and both remedies; pm may never rest an SC on a null kind and records in BRIEF's `## Verification gaps`, where the user signs, what is therefore not proven and what carries it instead; and `harness-init` puts every remaining null kind to the user as a decision, with an accepted gap going to the backlog — nothing blocks a feature over a null kind, because a missing runner is a priority call the user makes.
- DEC-164 @4054 [plan,brief,org,state] refs: DEC-120 DEC-138 DEC-165 :: `harness-grilling` is blocking step zero — at `/harness-plan` and inside `harness-init`'s interview, whose answers also seed `harness.json`, the domain description and the first glossary terms — and it is main-session only, the sole tier with a user channel: one question at a time with a recommendation attached, dependencies first, facts the agent's to find and decisions the user's, language challenged against `glossary.md` as you go, and nothing acted on until the user declares shared understanding; the destination is named first, `## Not yet specified` holds in-scope questions not yet sharp enough to state and `## Out of scope` what is ruled beyond the destination, and the one-screen artifact `.harness/notes/grilling-<slug>-<date>.md` reaches pm as a path — never a transcript — with `## Settled` seeding BRIEF's REQs and `## Facts I verified` saving a research pass; skipping it is the user's explicit call.
- DEC-165 @4085 [map,github,plan,state] refs: DEC-138 DEC-159 DEC-164 :: Wayfinding is the multi-sitting half of the same vague-to-plannable pipeline: a map — in local-markdown mode `.harness/efforts/<slug>/` with `MAP.md` as the index and `tickets/T-NN-<slug>.md` one decision each — survives between sessions, and the frontier (open tickets whose blockers are closed) is what a fresh session reads to know where to stand; tickets are typed `research` (the agent alone, because a decision waiting on a fact is never a question for the user), `prototype`, `grilling` (the default) or `task`, and a HITL ticket the agent answers itself is a fabricated decision; one decision per session with research excepted, charting and resolving are separate sessions, and the entry test routes a one-conversation idea to `/harness-grill` and a fuzzy destination or fact-blocked decisions to `/harness-wayfind`, with a stalled grilling promoted to a map carrying what is settled — either door terminates in decisions, never deliverables, and `/harness-plan` takes the artifact path while pm still owns REQs, SCs and tasks.
- DEC-166 @4139 [map,github,plan,approval] refs: DEC-19 DEC-138 DEC-165 :: Wayfinding storage is chosen by config, not preference: `github.sync: true` with a working `gh` puts the map on the tracker in the faithful wayfinder shape — a map issue, tickets as sub-issues, native `blocked_by` edges, assignee as the claim, resolution as a comment — because a map that spans days is a shared artifact a human reads without opening a session, and otherwise local markdown, which stays fully supported with no repo, no network and no sync; `bin/wayfind.py` owns every tracker operation (`map` · `frontier` · `chart` · `ticket` · `block` · `claim` · `resolve`) because the traps demand a script — the sub-issue API takes the child's internal `id` and not its `number`, the frontier is a compound query no single `gh` invocation expresses, and a ticket created without its `wayfinder:<type>` label is invisible to every later query — with mutations dry-run until `--apply` and the script refusing to run at all while sync is off.
- DEC-167 @4175 [map,cost,github,digest] refs: DEC-159 DEC-166 :: Independent decisions run in parallel: every frontier `research` ticket fires at once with no user needed, and the frontier's HITL tickets go to the user as one numbered round with a recommendation each — batching only what is BOTH independent and shallow, serialising anything dependent (an answer changes the later questions) or deep (it needs back-and-forth), never listing a `prototype` ticket in a round since the artifact is the exchange, and yielding entirely to the user's stated preference about how they want to be asked; `wayfind.py round <n>` computes both lists, a session is a context rather than a calendar day, and in tracker mode GitHub is the canonical store with no markdown shadow: the dialog is saved nowhere, the decision is the ticket's resolution comment, the gist is one line that `resolve --gist` writes into the map body's `## Decisions so far` in the same invocation as the comment and the close, and a substantial asset is a repo file linked from the comment.
- DEC-168 @4221 [plan,github,state,brief] refs: DEC-138 DEC-166 :: Sub-issue closure is manual in both directions, measured live: closing one sub of two leaves the parent open, closing the last open sub still leaves the parent open at `completed: 2, 100%`, and closing a parent leaves its subs open — so `close-task` on a task's sub-issue closes exactly that task, a parent is closed deliberately at ship acceptance, and a parent closed early cannot orphan-close outstanding tasks; the operational gotcha is that `sub_issues_summary` is eventually consistent, so never assert on it right after a write.
- DEC-169 @4376 [orchestrator,org,skills] refs:  :: An absence check proves only that the wrong words are gone, never that the right words are there, so every absence assertion carries a presence assertion beside it — for prose greps, code greps and test assertions equally — because deletion satisfies an absence criterion completely while destroying what must remain; an SC resting on absence alone is not verifiable however green it runs, which is why its automatable half is the gameable half and `verify: inspection` is the honest label, and `harness-verification-rules` and `harness-code-review` ask at review: for every absence assertion, what presence assertion sits beside it?
